_added 2026-09-01T18:51:42.131Z by planner_

STEP 0 FULL SCOPE, from the 48055 reconnaissance close (_inbox/2026-09-01_parcel-fill-48055_close.json): (1) merge #57 as the base; (2) VENDOR the engine at >= 22e71e1 and recompile — the factory copies vendored JS pinned at bfa9642 and a Dockerfile ARG bump CANNOT produce 65 rails; (3) replace the sample-only path: runParcelRecordFill always calls selectSampleForCounty and LANDING_BY_DISPOSITION_SQL is LIMIT 20 — build the county-wide paged walk (page landing by prop_id, load CAD per page, chunk manifest); (4) add a LAPTOP_WRITE_FROZEN gate in the job so Cloud-Run-only is a mechanism, not a card rule; (5) deploy factory-parcel-record-fill in us-east4 and execute for 48021 with a run row; verify absolute against 48021 containment. The board now gates 48055-r2, 48309, 48453 on YOUR close — the other counties open when the job is real and one county is proven.
