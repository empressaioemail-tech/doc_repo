_added 2026-09-01T19:18:05.171Z by planner_

MECHANISM NAMED for the CAD-join anomaly: Williamson landing prop_ids are R-prefixed AND zero-padded (verified live: 48491:R000009) while cad_property prop_ids are numeric — the CAD join matched ZERO rows and all 282,570 improvement/living cells are honestly unaccounted (~7 value cells/parcel vs Bastrop's ~12). Ledger duties: (1) measure the CAD-join match rate per county (records with >=1 cad_property-sourced value cell / records); (2) confirm or refute the key-format mechanism PER COUNTY (Bastrop had partial match with zero $0/living overlap — possibly a different scheme); (3) identify the authoritative landing<->CAD crosswalk (the geometry/parcel-node build that produced landing keys) rather than proposing a prefix-strip heuristic — a wrong normalization silently mis-joins, which is worse than starving. The reconciliation card this feeds: crosswalk + re-run CAD ingest; the fill is idempotent so healing is a re-execution, not a migration.
