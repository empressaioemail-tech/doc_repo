_added 2026-09-02T21:25:55.468Z by planner_

CRITICAL, inspected 2026-09-02T21:2xZ before losing the claim race: factory PR #75 (hauska-factory, still OPEN, CI green) does NOT match B-READER's shipped contract. B-READER (legacy-design-tools PR #581, MERGED, parcelGateVerdictRead.ts header) defines table parcel_gate_verdict with verdict text NOT NULL CHECK (verdict IN ('pass','refuse','excluded')) plus a separate unaccounted_count integer column, run_id text NOT NULL. PR #75's migrations/0008_parcel_record_publish_verdict.sql instead creates a DIFFERENT table, parcel_record_publish_verdict, with verdict as jsonb ({ok, unaccountedCount, excludedDeclaredAhead}) and run_id uuid REFERENCES runs(id) with no NOT NULL. If merged as-is, B-READER's reader queries a table that will never exist/populate -- the allowlist fails closed to legacy FOREVER, silently, and every CI-green claim on both sides is true in isolation while the two systems never actually connect. This is precisely the dispatch's own stop-and-report clause (item 4) and B-READER's header comment ('this card ships FIRST and defines the table; if PARCEL-B-GATE-SCHED proposes a different shape, that is a stop-and-report'). Recommendation: PR #75 should be revised to conform to B-READER's already-merged, already-canary-verified contract (rename table to parcel_gate_verdict, verdict as the text enum, unaccounted_count as its own column, run_id text NOT NULL or a NOT NULL uuid FK) -- do not revise B-READER, which is closed and deployed. The merged engine half (#378) exposes evaluateRailGate which should already give per-rail unaccounted counts needed to populate the enum + integer shape. Do not merge #75 without reconciling this. Filed instead of fixed: lost the claim race to another property lane before I could act on it.
