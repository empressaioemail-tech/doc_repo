#!/bin/sh
# P-280: the doc_repo close gates, run by git in every worktree. Installed with
#   node scripts/enforcement/git-commit-gates.mjs --install
# Named bypass: --no-verify (leaves no record). If node cannot start, the commit is refused.
exec node "$(dirname "$0")/../scripts/enforcement/git-commit-gates.mjs" pre-commit
