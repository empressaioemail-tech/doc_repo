# Govtech seat state

**Last updated: 2026-09-03.** Namespace `govtech`. Branch `seat/govtech`. OPS-17 lanes B, C, D: SmartCity Dashboards, plan review, Smart Files, ICC.

## Where things stand

**Wave 1 CLOSED** (A-104). **G-115 (PermitFlow island cut) CLOSED**, items 1–5 all MET (A-108–A-110). `plan-review` PR #14 merged, deployed `plan-review-00014-bbg`@100%. Coverage measured honestly: `14-02-003` (front setback) is the only adjudicable UDC section; `14-02-008` (permitted-use) has a real citation but no adjudication logic anywhere, by design; 5 of 6 real setback/height dimensions on the setback-rule atom are fetched but unused. A live GIS-source conflict (two disagreeing City of Bastrop layers) was found on both sampled parcels, unresolved, property/substrate's to fix. **Item 6** (real staff go-live) is unblocked but is the operator's own action, not a dispatch — scope it against the coverage findings above before scheduling.

**G-116 (real `bastrop_tx` pack, real data feeds, real auth) — CLOSED. All 10 real-domain routes + real Hauska auth are live end to end on `smartcity-dashboards`** (A-111 through A-119). Decision trail: `_decisions/2026-09-03_bastrop_tx_dashboards_pack_ratified.md`, `_decisions/2026-09-03_smartcity_os_platform_read_authorization.md` (the absolute-no-touch line on `smartcity-os` crossed once, narrowly, on explicit operator authorization — real changes now exist in that repo, not read-only anymore for this one purpose).

**What's real and live right now:** a tenant-private `bastrop_tx` pack (`environment: staging`) on `smartcity-dashboards`, wired through `smart-files`, deployed as `smartcity-dashboards-00048-kic`@100% traffic (verified by field, not by CLI summary). Ten domains have a real (non-fixture) source defined; **eight are genuinely returning live Bastrop data**, live-verified directly against production before and after the traffic shift:
- permits (312 records), work-orders (174), inspections (2015), code-violations (1495), business-licenses (72) — all via `smartcity-os`'s new `/api/platform/mygov/*` routes.
- fleet-vehicles (75 real Samsara vehicles), patrol-vehicles (24 real Spireon units), cip-projects (14 real PowerBI capital projects).

**Two are honestly unavailable, not broken** — grants provisioned, code correct, blocked on a real-world action outside engineering: fire-apparatus (FirstDue's own credential lacks apparatus/assets API scope — contact `dashboards@firstarriving.com`) and call-analytics (GoTo's OAuth consent was never completed by a human — `GET /api/goto/authorize` on `smartcity-os`). GoTo's real feed is deliberately aggregate-only (no individual call detail — real citizen phone numbers would be a genuinely higher sensitivity class than the fixture domain's own already-stated concern about inventing them).

**Verkada (police-cameras)** remains the one domain with **no credential anywhere** — needs real vendor onboarding, not more engineering.

**Real statuses are never force-mapped onto this product's own invented fixture taxonomies** (checked per domain, not assumed) — every real record keeps its real status/telemetry value as-is; each compose function's `extras` groups honestly instead of guessing a translation.

**Real auth is live.** A real Hauska tenant key was minted (`hauska-mcp-server` admin API, `tier: team`, `product: public`, `jurisdiction_tenant: bastrop_tx`, owner the operator) and a minimal browser bootstrap ships in `web/app.js` (reads `?hauskaKey=` once, persists to `localStorage`, attaches `x-hauska-key` to same-origin API calls). Verified end to end against the live 100%-traffic revision: real key → `resolveHauskaTenant` → `canReadPack` tenant match → real composed data (`origin: feed`, `source: live`, 312 real permit records). Raw key value delivered to the operator directly (never logged in this repo); not needed again unless a new caller is provisioned.

**What's genuinely still open (all real-world, not engineering):**
1. **fire-apparatus / call-analytics** — vendor scope grant (FirstDue) and human OAuth consent (GoTo), respectively.
2. **Verkada** — real vendor onboarding, no credential exists anywhere yet.
3. Small, non-blocking: per-pack default subject parcel for the SmartSite staff-map embed (currently one global constant, harmlessly correct for Bastrop by coincidence).

**Two real bugs found and fixed during the operator's own first look at the live dashboard, not by a dispatch** (A-120): (1) the Development Services / Pipeline page — the exact page the operator screenshotted earlier — never dispatched to real data at all; `/api/domains/:id` and `/api/city-domains` had the real-source branch, this one route was missed and kept calling the fixture composer unconditionally even for `bastrop_tx`. (2) None of the 8 live real domains populated `extras.metrics` (only `extras.realStatusCounts`), so every domain's summary tiles showed "Not read" despite real records in the table underneath. (3) Found alongside: every one of the 24 static nav hrefs in `index.html` carries no `cityKey`, so clicking ANY sidebar link dropped the visitor back onto the default pack — surfaced live as "my bastrop login went away" when it was really "my city selection went away" (the Hauska key itself never left `localStorage`). All three fixed together (`smartcity-dashboards` PR #46, merged `6aed1a5`), deployed `smartcity-dashboards-00050-hiz`@100%, live-verified against the real deployed service both before and after the traffic shift (312 real permits, real status counts present, nav fix confirmed shipped in served `app.js`). 512/512 tests.

**Two more real bugs found and fixed on the operator's Fleet page QA** (A-121): (4) the 5 vendor-live composers (fleet/patrol/fire-apparatus/cip/call-analytics) never populated `extras.realStatusCounts` either — the same "Not read" tile symptom as (2), just missed on this half of the real domains. (5) `renderRegion`/`renderPipeline`/`sourcedLabel` showed the fixture-only "Demo records" pill and nav badge on ANY `ok` payload, with zero real/fixture check — a live, active false claim over 75 genuinely real Samsara vehicles. Fixed at the call edges (`payload.source === "live"` / `pipeline.realStatusCounts` / `regions.some(r => r.source === "live")`) rather than inside `LENS_BADGE`/`LENS_BADGE_ORDER`, which stay exactly the fixture seam's guarded five-word vocabulary (`src/lens-claims.test.mjs` holds them equal to `DOMAIN_STATUSES`) — a real domain isn't a seam status and doesn't get a sixth word spliced in. A real `ok` region now reads "Live records". Both fixed together, `smartcity-dashboards` PR #47 merged `9ad2a70`, deployed `smartcity-dashboards-00052-vug`@100%. 513/513 tests.

**A third, real-world root cause found alongside (6):** every real Samsara vehicle showed status "unknown" with no odometer/fuel — not a mapping bug. `smartcity-os`'s own `/api/platform/samsara/vehicles` route only ever called Samsara's static vehicle-roster endpoint, never the separate stats-batch endpoint that actually carries live telemetry, despite the route's own comment claiming otherwise. Fixed (`smartcity-os` PR #43, merged `e0b627a`, deployed `smartcity-api-00127-quv`@100%) by adding the same stats-batch call the existing session-gated `/api/samsara/vehicles` route already proved. **Live-verified, partial and honest, not force-completed:** odometer now real for 72 of 75 vehicles (e.g. 10,167 mi), engine hours present — genuine improvement. Engine on/off state and fuel percent remain empty for all 75, using the identical proven field-extraction the working route already uses; read as a real hardware/sensor-coverage gap on this fleet (OBD-derived stats like odometer are broadly available, engine-state/fuel-level sensors are not guaranteed on older or retrofitted municipal vehicles), not a further code defect — not chased further without vendor-side evidence otherwise. Before deploying, confirmed by ancestry check that `smartcity-os` `main`'s tip was exactly "what's already live + this one commit" (a stale local branch pointer briefly made it look like 10 commits of unrelated concurrent work had landed; verified via `git log`/`git show` before touching the no-touch repo's live deploy, not assumed).

**Field-enrichment pass against production (A-122), 5 parallel agents, one per domain family, all against real production reference (`smartcity-os`'s own live staff pages).** A read-only research pass first compared production's actual staff dashboard structure to the 10-domain build here (findings: several "Not built"/"Empty" nav items — Parks, Records search, People and access, Assets — turn out to be honest, since production doesn't have them live either; call-analytics' aggregate-only design was confirmed the *right* call, since production exposes full per-call detail including citizen phone numbers). Operator chose to enrich the 9 already-built domains only, not start new ones (Finance etc. named, deliberately deferred). 5 agents dispatched, one per domain family, each producing its own smartcity-os + smartcity-dashboards PR pair, no merges/deploys performed by the agents themselves:
- **MyGov family** (permits/work-orders/inspections/violations/licenses): applicant/contractor/ownerName/itemized fees added to permits; a real smartcity-os bug fixed along the way (work-order fees/assignee lived in an unparsed blob the platform route never decoded — now reuses `parseWOEnriched`, the same function production's own dashboard already calls); a bonus field found (inspection comments). `smartcity-os` PR #47 merged, `smartcity-dashboards` PR #51 merged.
- **Fleet (Samsara)**: DVIR unresolved-defect count + last inspection, 7-day safety event count, high-mileage/low-fuel flags — all from functions the session-gated production route already calls, reused not re-derived. `smartcity-os` PR #45 merged, `smartcity-dashboards` PR #49 merged.
- **Patrol (Spireon)**: NSpire active status, maintenance alert count, recent (7-day) alert count — same pattern. `smartcity-os` PR #48 merged, `smartcity-dashboards` PR #52 merged.
- **Fire apparatus (FirstDue)**: agent correctly DECLINED the originally-named occupancy pre-plan fields once it found they belong to a different FirstDue endpoint than apparatus — would have been a fabricated shape, the same category of mistake this card has caught twice before. Instead found and fixed a real, separate bug: the apparatus table already had type/station columns the live mapper never populated. Still live-unverified (FirstDue API remains 403, unrelated to this fix). `smartcity-os` PR #44 (docs-only) merged, `smartcity-dashboards` PR #48 merged.
- **CIP (PowerBI)**: found the real Gantt phase data (`phases`/`overallCompletion`/`currentPhase`/`status`) was already exposed server-side and simply never read by the dashboards mapper — `smartcity-os` PR #46 is test-only (zero production changes, locks the shape in with coverage). `smartcity-dashboards` PR #50 hit a stale, unrelated a11y adjudication entry on first CI run (pinned to the Inspections/Licenses pages, not CIP — a known-flaky check category per that file's own commentary); reran clean, merged.

**All 5 smartcity-os PRs deployed together** (`smartcity-api-00130-loc`@100%, 56/56 tests, tsc 410/410 unchanged), **live-verified per field before shifting traffic, not assumed** — caught and corrected two of my own smoke-test misreads in the process (checked `stats.dvir`/`stats.safetyEvents7d` when the real fields are top-level `dvir`/`safetyEvents7d`; checked for a literal `activeInNspire` key in the smartcity-os response when that name is a dashboards-side rename of the source's own `active` field) — both resolved by reading the actual merged code rather than assuming either side was wrong. Two genuine real-world data-sparsity findings, reported honestly rather than chased as bugs: DVIR records are null for all 75 fleet vehicles (plausible non-use of the driver-side DVIR submission workflow) and work-order fees/contractor are null for all 194 current work orders (same parsing function production's own dashboard already uses, so most likely genuine absence of enrichment data for this tenant, not broken wiring).

**All 5 of 5 smartcity-dashboards field-enrichment PRs merged and deployed** (`smartcity-dashboards-00056-bor`@100%, 546/546 tests) — MyGov, fleet, patrol, fire-apparatus, CIP. CIP's PR turned out to carry a real, new a11y regression on its first CI run, not the stale/flaky check first suspected — a new "Project phases" table shipped `<th>` header cells with a permanently-empty body on any pack that doesn't populate `record.phases` (every fixture pack, including the one CI scans by default), a genuine `th-has-data-cells` violation. Fixed directly (hide the table, show only the caption, when no real phase rows exist — matching every other empty-state region in this app) rather than re-running against a guess. The same PR also picked up a real merge conflict against `main` once the other 4 domain PRs had already landed (all touching the same shared files) — GitHub couldn't even run CI against a conflicting merge tree, which is why the first "rerun" attempt produced the exact same stale-looking failure twice. Resolved by merging `main` into the PR branch (two clean side-by-side-addition conflicts, nothing dropped from either side), re-tested (546/546), re-pushed, then genuinely green. Live-verified before the shift: real `currentPhase`, real per-project `phases` (Gantt rows), real `status`, all present on the tagged revision.

**G-116 field-enrichment pass (A-122) is now fully closed** — all 10 smartcity-os platform routes and all 5 domain families live on smartcity-dashboards.

**Map architecture — researched, documented, deliberately not built (A-123).** Operator asked to stop embedding SmartSite in the staff map and instead read from whatever SmartSite itself is fed from, then separately floated a Bastrop-only water/utility layer on the same base map. Three-repo research found: (1) the REAL, live, working "type an address, see the permit/zoning/flood-plain" map has zero relationship to SmartSite — it's `smartcity-os`'s own Leaflet + live Bastrop/Bastrop-CAD ArcGIS feature services; (2) SmartSite/`hauska-map` is a genuinely separate property-seat product with real, public, unauthenticated tile data but no permit/zoning richness of its own; (3) the water/utility-layer idea is real Asset Management scope (G-24, already priced) standing against the operator's own prior "Do not fill G-24" ruling (A-054) — flagged, not resolved. One clean finding: the current map embed's entire animated panel shell is completely content-agnostic (`MountStage.mount(src)` is exactly `this.frame.src = src`), so preserving "how it behaves" while changing what's shown is a one-URL change whenever this becomes real scope. Operator's call: document only, keep the current cutover on what already works, treat this as a separately and carefully scoped future project. Full findings: `_inbox/2026-09-04_dashboards_map_architecture_scoping.md`.

**Two new lanes dispatched (A-125), open, nothing merged/deployed yet.** (1) **Native property map**: the deferred map question (A-123) got one concrete, buildable piece approved — preserve the existing `MountStage` chrome, swap what's inside from SmartSite to Bastrop's real functioning map. The literal iframe-swap was checked live first and confirmed genuinely blocked (`smartcityos.io` sends `x-frame-options: SAMEORIGIN`/`frame-ancestors 'self'` on every response — not assumed, curled directly), so the real build is a Dashboards-owned Leaflet page reading the same real ArcGIS data through a new narrow platform-internal route on `smartcity-os` (same pattern as every G-116 domain), mounted into the exact same panel slot. First slice only: parcel + zoning + flood + permits/inspections/code-cases, not the other ~35 ArcGIS layers. (2) **Compass groundwork**: per `_decisions/2026-09-04_compass_rework_starts_parallel_to_plan_review.md` (operator explicitly relaxed the 2026-09-02 Plan-Review-leads sequencing for this), Compass's next-generation build — the already-specified atom-backed sidebar rework, NOT a port of `smartcity-os`'s existing chat panel — starts now as a parallel lane, with Smart Files context-awareness as the operator's own named first priority before broadening to the rest of the platform's data. A research pass into Smart Files' real data model/search surface/access control is running to ground that before any Compass code gets written.

**Compass groundwork found Smart Files has no real search today** (A-126) — no full-text index, no PDF content extraction, no keyword query API; the product one-pager's own "search a phrase you half remember, including the text inside the PDFs" claim is not implemented. Reported honestly rather than building Compass against a premise that isn't true yet.

**Reprioritization (A-127): "make these systems function together" named as the actual thesis, the map downgraded to a good later project.** Operator's own framing on hearing the Smart Files finding: the map was always one instance of a broader thesis (systems actually reading from/linking to each other, not just coexisting), not the goal itself. Recorded as `_decisions/2026-09-04_systems_linking_is_the_thesis.md`. The already-dispatched native-map agent (A-125) stays running — its PR will be reviewed and held once it lands, not rushed to deploy. Current active priority: a scoping-only research pass (not a build) into what real Smart Files search would require, AND whether Smart Files' own `smart_file_placements` table (which already models document↔permit/parcel/project/asset/meeting links in schema) is populated or read by anyone today on either side — grounding for the smallest real cross-system linkage to make true first.

**Map agent returned (A-128): `smartcity-os` side real and PR'd (#49, `getPropertySummary()` reused, not re-derived), `smartcity-dashboards` side correctly self-halted on a real conflict** — this product has a founding, tested "No Leaflet island" rule the dispatch's own Leaflet instruction violated. Good catch, not a mistake.

**Both lanes un-parked (A-129), both back in active motion, both with explicit operator decisions recorded first.** (1) **"No Leaflet island" deliberately overridden for the property map only** — operator underestimated its real cost when the rule was written, a SmartSite launch is imminent and shouldn't be risked, this is an accepted temporary stopgap swapped out later (`_decisions/2026-09-04_no_leaflet_island_overridden_for_bastrop_map.md`). Map agent resumed with authorization, instructed to narrow the regression tests' exception to just the new page, not weaken them generally. (2) **Smart Files real search dispatched as a scoped wave** (`_decisions/2026-09-04_smart_files_search_wave_scope.md`): PDF extraction + Postgres full-text indexing + a real access-gated search endpoint, synchronous-on-upload (right-sized for today's real volume, not premature job-queue infrastructure); plus fixing the one real linkable content (municode meeting placements, currently hard-coded to `folder` despite the schema already modeling `meeting`). Checked before dispatch, not assumed: no permit/work-order document content exists upstream anywhere today (`smartcity-os`'s own schema confirms it), so that cross-linking stays explicitly out of scope, named as a future initiative rather than half-built now. Both dispatched to background agents, isolated worktrees, PR-only.

**Both G-117 lanes landed real, working code (A-130).** `smartcity-os`'s property-intel route (PR #49) merged and deployed, `smartcity-api-00132-vof`@100%, live-verified with a real address (710 Chestnut St) returning real geocoded coordinates/parcel polygon/owner/zoning. Smart Files' real search wave (PR #13) merged and deployed, `smart-files-00013-ruq`@100% — real PDF extraction, real Postgres full-text index, a real access-gated search endpoint, plus the meeting-placement fix. The one genuinely consequential step, applying the new migration to the real production Neon DB, was done carefully via this repo's own established apply tool and verified, not assumed. Along the way, a stale, unrelated local checkout (`P:/smart-files`, not the seat-registered worktree) was investigated before being touched and correctly left alone — its ~20 "modified" files turned out to be a pure Windows line-ending artifact, not real work. `smartcity-dashboards`' own map page (PR #53) — the actual Leaflet UI, reviewed personally before merge (routing/auth, the `nativePropertyMap` conditional, the narrowed "no leaflet" test exception) — is open with CI still running; will merge and deploy once green.

**G-117 CLOSED (A-131) — the real Bastrop property map is live end to end.** `smartcity-dashboards` PR #53 merged, deployed `smartcity-dashboards-00058-pen`@100%, 562/562 tests. Live-verified the thing that actually matters — the swap is genuinely conditional, not blanket: `template-city` composes exactly as before (unchanged "missing parcelNodeId" state), `bastrop_tx` alone now composes the native `/property-map.html` page. That page's own live address search, hit for real against "710 Chestnut St," returned real geocoding, a real parcel polygon, real owner, real zoning — reachable through the actual UI now, not just the platform route. Two apparent problems during verification were both my own query mistakes (wrong JSON path, comparing against the wrong baseline), caught before being logged as real findings.

**Ready for the operator's deep UI QA session on everything closed so far** — data feeds, real auth, the production-comparison enrichment pass, all bugs the operator's own QA has surfaced, and now the real Bastrop property map and Smart Files search are all live.

## Reconciliation corrections (2026-09-02, A-105–A-107 — inherit, don't re-derive)

- Real, licensed IBC 2018 content already exists in the substrate (4,825 atoms, `icc-code-connect` adapter, entitled through Dec 30 2026). Earlier claim that "no real ICC content exists anywhere" (A-102/A-103) was wrong — corrected to a wiring gap, closed by G-115 item 4.
- ADR-023 DOC-5 amendment **RATIFIED**: city plan review lives in the standalone `plan-review` repo, not `legacy-design-tools/artifacts/plan-review`. G-15/G-16/G-22/G-31/G-51 marked out-of-scope-for-Bastrop (they measure the wrong repo; still real rows for AEC-cortex's own surface whenever built).
- **G-52** (SmartCity-initiated engagement from a MyGov permit record) stays genuinely blocked — needs a live permit feed (`grantedAdapters` still `[]` on the packs that would carry one, per G-63's close). Do not start G-52.

## Standing rules

- Product repos: branch, PR, merge on green. May merge own branches.
- Does not write property, markets, or substrate repos. Request changes from the owning seat.
- Deploys are planner-owned. Merged ≠ live. Grade on the deployed surface with violation probes, not the diff.
- **G-115/G-116 standing constraints (operator, binding on every dispatch under either card):** live Bastrop (`smartcityos.io`, PermitFlow) stays 100% live and untouched throughout; additive-only — reuse existing data/component mapping, no rebuilding what already works.
- `smartcity-os` remains ABSOLUTE NO-TOUCH — this seat's subject, not its property.
- Stay on the product surface actually in focus. Findings that belong to another seat's domain (property/substrate GIS data, etc.) get named as leave-behinds, not chased deeper — see `[[feedback-govtech-scope-discipline]]` (auto-memory).

## Notes from this session

- Confirmed this worktree (`P:/seat-worktrees/govtech/doc_repo`, branch `seat/govtech`) matches `_catalog/seat_register.json`'s current govtech entry exactly.
- **Registry drift found, still open:** `icc-portal` is registered (`P:/seat-worktrees/govtech/icc-portal`) but does not exist on disk. Not currently blocking. Flag before opening a session there.
- Both `smartcity-dashboards` and `smart-files` worktrees were one commit behind `origin/main` at session start (G-114) — resynced before branching each time. General reminder: check worktree staleness at the start of any new lane, don't assume current.
- The old "Authoritative tracker: `canvases/govtech-master-program.canvas.tsx`" pointer from a prior version of this file no longer resolves — file doesn't exist on disk. Dropped.

## Filed this thread

- `_sessions/2026-09-02_govtech_wave1_close_and_bastrop_permitflow_start_claude_code.md` — prior session record
- `_inbox/2026-09-03_govtech_seat_writepath_handoff.md` — seat/write-path model + reset checklist
- `_inbox/2026-09-02_bastrop_permitflow_islandcut_WDLL.md` — G-115 frozen card, all items graded
- `_inbox/2026-09-03_g115-matrix-verify_close.json`, `_inbox/2026-09-03_g115-coverage-measure_close.json` — G-115 items 2/3 closes
- `_decisions/2026-09-02_plan_review_leads_the_bastrop_push.md`
- `_decisions/2026-09-03_bastrop_tx_dashboards_pack_ratified.md` — G-116 ratification
- `_inbox/2026-09-03_g116-bastrop-dashboards-pack_close.json` — G-116 Phase 0+1 close
