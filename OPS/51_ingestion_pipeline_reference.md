---
id: OPS_51_ingestion_pipeline_reference_pointer
title: Ingestion Pipeline Reference, RECONCILED and moved
status: pointer
last_updated: 2026-08-20
applies_to: portfolio
related: [51_ingestion_pipeline_reference]
owner: nick
---

# POINTER, reconciled 2026-08-20

This path held a second body of `51_ingestion_pipeline_reference.md` carrying the same `id`
and the same frontmatter as the root copy. The two diverged in both directions. Root was
committed at `492a452` as though it were the only one; that was wrong and is recorded rather
than quietly corrected.

Neither was a superset. The union now lives at `51_ingestion_pipeline_reference.md` at repo
root: this body as the base, since it carried five sections and substantially richer
treatment of the derivation states, the harness self-validation failure, and starvation
granularity, plus the three passages that existed only at root.

Canonical copy: `51_ingestion_pipeline_reference.md` at repo root. Edit that file. Do not
maintain a second body here.

Note for the reader: this `id` deliberately differs from the canonical one so that no
instrument sees two documents claiming the same identity again.
