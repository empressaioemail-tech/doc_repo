# Scratch — Bastrop BDC setback correction (2026-07-29)

## GROUND-TRUTH
- 2026-07-30 CERTIFIED-CLEAN PASSED: `_inbox/2026-07-30_BASTROP_CERTIFIED_CLEAN_audit.md`. CTX still HELD (operator go).
- Serving: engine `00148-zec` @100% tag `bdc`; retrieval `00045-yek` @100% tag `bdc`.
- PE: `PROPERTY_ATOM_PATH=1` redeployed; customer path `/api/spine/property-atoms/:id/facets` → atom-chain-warm. Jefferson SF-1 front 30 / side 20 (corner) / rear 30 cite BDC 14.02.003. GC 24712 honest-decline.
- Stamp Zoned_Parcels/83: 5768 matched; txgio Jefferson SF-1. Surgical tier1 zoning refresh 5773 snapshots (full bake deadlocked).
- Rewarm city cohort: promoted 1777, verifyFail 819, no-road-adjacency 96.
- Substrate `bastrop_tx.currentEditionId` = `bastrop_tx/bdc-2026-adopted`. Conditional setbacks retired 3006 (absence kind conditional-district-no-scalar-row).
- Local secrets: `P:\tmp\bdc-rewarm\cortex.url` (txgio), `substrate.url` (hauska_mcp).

## OPEN
- CTX HELD until operator go.
- Residuals: health pack still Zoning_Place_Type; substrate BDC sectionIds empty; re-run quiet tier1 bake; LDT adapters lack BDC JSON; 213k phase-1a out of scope.

## LESSON
- Dual-fork was red herring — both described repealed B3.
- Ordinance text > GIS card for numbers (CORRECTION A).
- Atom stamp ≠ PE customer-done: PE reads place_layer_snapshots / property-atoms BFF; must refresh snapshots + enable PROPERTY_ATOM_PATH.
- Full tier1 bake + concurrent SQL patch → deadlock; surgical patch first or bake alone.
