# R-05 W2 scratch (Tier 2)

## GROUND-TRUTH 2026-08-21T12:10Z

- Seat integration. `P:/doc_repo` branch `main` commit `d254467788c795c6f8fa5a9256ad6a074859b615`.
- Two R-02 census products exist: `P:/tmp/r02-census` (named-loader, 1998/702 at 4b174d1) and `P:/tmp/r02-census-run` (bulk-CI, 2406/2 at 8b68e432). Integration close is the second.
- Duplicate ids on this dirty tree, YAML fence parse, walk skip `.git`+`node_modules` only: **18**. Tracked-only YAML parse: **7**. `git grep ^id:` first-15-lines: **8** (false body hit).
- `doc-staleness.mjs --json --today 2026-08-21` at d254467: scanned **2367**, noFrontmatter **389**. Vocab FAIL 1223 is a different gate.
- Census-run parser (`startsWith('---\n')` only) noFM **896** at d254467. Exact split: true no-fence 438 + CRLF/BOM false negatives 458.

## LESSON

- A frontmatter detector that requires literal `---\n` will count every CRLF and UTF-8-BOM file as missing a fence. Staleness `lines[0].trim() === '---'` accepts both. That is how 321 and 880 can describe the same tree.
- Assigning CI to every file in the doc-staleness walk makes consumer NONE collapse to "files the walker skips." That is not the dispatch question.
- `SKIP_DIRS.has('.claude/skills')` never matches a directory basename. Dead skip.

## DEAD-END

- Reconstructing planner 20 from `git ls-tree` at e1fdc92. Tracked YAML dups are 7. Unfiltered `^id:` grep is 10 (includes `01_doc_conventions.md` example `id: 10_ground_truth` and a session-body `id:`). 20 is not in git.

## OPEN

- Planner: which consumer question is canonical for R-02 second half (named loader vs bulk walker).
- 18 dirty-tree dups include untracked shadow masters and inbox copies; quarantine half still blocked on R-01.
