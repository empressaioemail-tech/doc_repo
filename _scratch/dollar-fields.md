# dollar-fields scratch

## GROUND-TRUTH 2026-09-01T16:50Z
Store cortex-prod held by zoning-band1 until 17:44:18Z. parcel-record-fix held it 16:38-16:44. dollar-fields claim refused STORE_TOKEN_HELD. Did not --force.

LDT tree P:/tmp/legacy-design-tools-dollar-fields feat/dollar-fields from 91e39991. Reader switched to cad_property. 94 targeted tests passed (dummy DATABASE_URL).

## LESSON
#575 cadRollFromClaim reads the atom claim. That is the starve-and-invent path. landUseJoinKey returns null for Hays/Williamson (TxGIO-to-CAD gate). Using it for CAD-to-CAD dollars would starve the two hollow-atom counties and report success.

The conformant bake CLI iterates hauska_mcp atoms. Williamson bake n is 602050; roll atoms are 319487. A Wave R that only re-runs that CLI misses the extra bake rows. Patch existing place_layer_snapshots JOIN cad_property.

## DEAD-END
Do not wait to implement until the store frees. Two lanes queued; the reader can land without SQL. Do not steal the token.

## OPEN
Claim the moment zoning-band1 releases. Dry-run golds, then apply six counties (small first). Verify 0 → cad_property pop. Live wire 48021:34137 + hollow golds. Commit/push feat/dollar-fields. Close and release.
