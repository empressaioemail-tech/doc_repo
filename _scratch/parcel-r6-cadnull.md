# parcel-r6-cadnull scratch

Seat property. Engine-only. No store writes.

## OPEN
- Card fixture (join-miss R-prefix) is not the live Williamson store (join-hit R-prefix). R6B negative control will see improvement move if it identity-joins 48491. That is not a join-miss leak.
- R6B must SELECT tax_year into CadPropertyRow. Current CAD_ROWS_SQL does not.
- R1B cannot overwrite absent-verified under the unaccounted-only stamp guard. Sequence R1 before locking Williamson dollar rails, or accept the lock.

## LESSON
- "Values exist under a different key" is not the same fact as "the join missed." Williamson is the second shape.

## DEAD-END
- A 48491 or R-prefix skip would protect R6B's negative control and is exactly the bypassable runtime check the dispatch forbids.

## GROUND-TRUTH
- 2026-09-01T20:26:21Z claimed parcel-r6-cadnull on P:/tmp/hauska-engine-parcel-r6 feat/parcel-r6-cadnull at 22e71e1.
