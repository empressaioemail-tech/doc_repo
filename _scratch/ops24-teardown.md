# OPS-24 teardown — running findings log (Tier-2 scratch)

Snapshot: hauska-factory origin/main 84642360184a685596417f2e0b066cd8a56cacaf; doc_repo origin/main cd1d798e8d9dd4f49526aad9473dd96b9d3caf21.

## Stage 0 / P-186 (per-place gate)
- GROUND-TRUTH: DECLARED_COMPLETE / EXPLICITLY_HELD are REAL, exist in hauska-factory src/config/zoning-layer-completeness.mjs:69,107 -- but they are ZONING-ONLY, scoped to CITY (22 cities in DECLARED_COMPLETE per test/parcel-r5-zoning.test.mjs:726), not a general "roster place" completeness across all data types/rails. OPS-24's P-186 predicate wants "every roster place in the county carries DECLARED_COMPLETE or EXPLICITLY_HELD" -- these two states currently exist for exactly one rail family (zoning), not generalised. NEEDS: check if OPS-24/map claims this generalisation already or as future work -- map stage 0 just says "no gate reads per-place coverage" (honest); OPS-24 P-186 instrument column lists tx-source-truth.mjs + P-156 declaration + "the gate read" -- none of the three, individually, implements a per-place (not per-city, not per-county) completeness gate today.
- GROUND-TRUTH: tx-source-truth.mjs (doc_repo scripts/tx-source-truth.mjs) is real, self-tests pass conceptually (read the self-test logic), but its output granularity is PER-COUNTY (one row per t6_cad_probe_<fips>.json), not per-place. It has zero per-place logic. So it cannot be "the gate" OPS-24 describes for P-186 on its own.

## Stage 1 / P-187 (farm manifest)
- GROUND-TRUTH: _LDT_SHA pin exists across hauska-factory cloudbuild.*.yaml (e.g. cloudbuild.bake-migrate.yaml:66, cloudbuild.building-footprint-reconcile.yaml:47, etc.), single-SHA, CI-monitored via .github/workflows/ldt-pin-staleness.yml. This is a SINGLE-repo pin (LDT only), NOT the four-line manifest (LDT+engine+factory+2 package versions) the map/OPS-24 say stage 1 needs. CONFIRMS "not built" is accurate for the actual four-line manifest.

## Stage 4 / P-190 (identity)
- GROUND-TRUTH: hauska-factory scripts/ops21-h1/hays-identity-reconciliation.mjs is real, Hays-specific by design (refuses to run for 48209 without --allow-hays, since live Hays measurement is deliberately ON HOLD), has a --control mode for Bastrop/Caldwell with NO crosswalk file, asserting zero manufactured dispositions. Takes --county=<fips> generically already (mechanism is county-parameterized) but OPS-24 P-190 instrument says "generalised" as if that work remains -- worth checking further whether --county=<fips> already IS the generalisation or whether Hays-specific logic (buildCrosswalk, DISPOSITIONS) still hardcodes Hays assumptions.

## Stage 9 / P-195 (THE GATE) -- deep dive, most load-bearing claim in the whole program
- CONFIRMED BY DIRECT CODE TRACE (not just citing the map's prose):
  - hauska-factory/src/lib/publish-readiness-gate.mjs:192-204 evaluateOverlap: `if (wantSet.size===0) return {code:"EMPTY_DENOMINATOR", ratio:null,...}` -- no refusal.
  - :216-300 evaluatePopulation: the ONLY unconditional refuse is line 224 `if (roll.size>0 && bake.size===0)` -- requires roll.size>0. For a TRULY empty county (roll=bake=real=landing=record all size 0), every subsequent overlap call (lines 233,257,270) returns EMPTY_DENOMINATOR, and every refuse-check is gated on `code === "OK"` or `code === KEYSPACE_MISMATCH` (lines 234,243,258,271,279) -- EMPTY_DENOMINATOR matches neither, so NO refuse() call fires anywhere in the function. Function returns `{ok:true, ...}`.
  - hauska-factory/src/lib/parcel-record-engine/publish-gate.js:9-47 evaluatePublishGate(records=[]): for-loop over 0 records never executes; unaccountedCount stays 0; returns `{ok:true}`. This is the actual top-level gate function (assertPublishableCounty wraps it, throws only if !ok).
  - :72-84 evaluateRailGate(cells=[]): `cells.some(...)` on empty array is false -> `live=false` -> returns `{ok:true, excludedDeclaredAhead:[railKey]}`.
- IMPORTANT NUANCE: evaluateRailGate's "excludedDeclaredAhead" behavior on zero-earned-cells is DELIBERATE, TESTED, CORRECT design for hard-scoped writers (test/gate-county-scoped-rail.test.mjs:28-34, with explicit comment "prove -- empirically... that a county with zero earned cells... reads as excluded, never refuse"). This is NOT itself a bug -- it's solving a different, legitimate problem (writer scoped to one county must not false-refuse elsewhere). Do not conflate this with the real defect.
- THE REAL, UNGUARDED GAP is evaluatePopulation/evaluatePublishGate's behavior when EVERYTHING is empty (no roll, no records at all -- i.e. a county never acquired). No test anywhere calls evaluatePopulation with all five populations empty. Closest test (test/publish.test.mjs:1965-1977) has roll.size=3 (non-empty) + bake.size=0 -> correctly refuses BAKE_POPULATION_MISSING. That is the PARTIAL-absence case, correctly refused, matching "PARTIAL absence REFUSES."
- test/publish.test.mjs:2031-2036 test title says "empty denominator refuses" but ONLY asserts `evaluateOverlap(...).code === "EMPTY_DENOMINATOR"` -- never asserts anything actually refuses/throws. Documentation-drift / aspirational test name, DEV_PROCESS 2.2 pattern (gating indicator not tested for its ability to FIRE on the true empty-county case).
- VERDICT: map section 9 / OPS-24 P-195 "broken" SURVIVES the teardown, verified at source with file+line, going beyond the map's own citation. OPS-24's own P-195 row is honest: instrument column says "publish-gate with the empty-county test built first" -- correctly implying this test does not exist yet. Law 2 is therefore currently UNENFORCED (nothing today proves the gate refuses an empty county; it demonstrably does not).

## Instrument named in Law 1 (surface-probe.mjs) -- ran --self-test offline (fixtures, no network), EXIT 0, all checks pass, non-vacuous both directions (verified empirically, not just read).
- CRITICAL FINDING: scripts/surface-probe.mjs ROWS object (grep for "P-1XX" keys) contains ONLY OPS-23 rows: P-151,152,153,154,155,157,158,159,167,169,171,172,173,174,175. ZERO rows for P-186 through P-198 (every OPS-24 row). Grep for P-18[6-9]|P-19[0-8] in the file: NO MATCHES.
- This directly contradicts Law 1's framing ("scripts/surface-probe.mjs ... is the finish line for every stage") as CURRENTLY TRUE for OPS-24: the probe cannot yet measure ANY OPS-24 predicate. It is a real, working, well-tested instrument -- but wired only to OPS-23 today. OPS-24 stage 11 (P-197) says "exists (probe)" which is true of the FILE but not of OPS-24 coverage within it.

## TODO remaining
- Stages 2,3,5,6,7,8,10,12 existence checks
- cost meter (P-197) existence
- Six laws: one violation each, file+line, enforceability class
- Four holes: atoms partitioning, address points, retract-by-run-id, per-place gate
- What's missing: cost metering row, access policy, calibration hook, operator stop points
- Burnet: t6_cad_probe, roster, StratMap coverage, address points (txgio_address 6/254 as of 2026-08-08 -- VERIFY CURRENT)

## Stage 2 / P-188 (pre-bake audit, "94 assumption rows")
- The three assumption registers (_inbox/2026-09-13_assumption_register_{ldt,engine,factory}.md, plan_row P-181) are REAL, substantive, source-cited documents (each entry: ID/BELIEF/WHERE/GUARDED/FAILURE MODE/KNOWN VIOLATORS/CONFIDENCE).
- COUNT DISCREPANCY (DEV_PROCESS 1.4 free finding): using the most internally-consistent per-row marker (WHERE:/FAILURE MODE:/CONFIDENCE:, which recur at equal counts within each file): factory 29, engine 27, ldt ~31 (its four structural markers range 30-32, not perfectly self-consistent). Total ~87, not the 94 claimed in OPS-16 A-149, OPS-24 stage 2's row, and the map. A cruder heading-count method (###/#### per file) independently converges on 87 too (38+29+20). BELIEF: as a marker undercounts further (68) because it's missing on some rows relative to their own WHERE/GUARDED/etc siblings -- not a reliable marker.
- CRITICAL CONTEXT: _sessions/2026-09-13_ops23_session_close_and_handoff_claude_code.md:106-111 -- the session that RECEIVED P-181's audit explicitly flagged it as "landed... UNVERIFIED by the overseer... Their numbers read as plausible and well-constructed but have not been read against source... Treat as reported, not confirmed, until someone does that read." OPS-24 (compiled the next day) propagated "94" as if settled, without that read happening. This IS the read; it does not fully confirm the headline number.
- P-181 entry 1 (_inbox/2026-09-13_dead_controls_ranked_fixes.md:32-56) is the SAME defect I independently re-derived by code trace for stage 9/evaluateRailGate -- strong triangulation, and confirms the current (2026-09-14) code is UNCHANGED from the 2026-09-13 finding (fix proposed, not yet landed).

## Stage 6 / P-192 (rail fill, owner/land-use/flood writers)
- CONFIRMED: hauska-factory src/jobs/parcel-owner.mjs has ZERO lease-acquisition code (no INSERT INTO atoms_writer_lease_v2 or any lease table), yet unconditionally writes `lease_released: true` in its run-termination record (lines ~380,392) on BOTH success and failure paths. Same pattern in flood-ingest.mjs (lines ~274,286). This is sharper than the map's "no lease" claim: the run record affirmatively claims a lease was released when none was ever taken -- a false-compliance signal in the run-record schema itself.
- Could not verify "throws on first batch" / re-run behavior read-only (would require executing against a store); flagged as open.

## Stage 7 / P-193 (atoms)
- NUANCED, thesis partially does NOT survive as literally worded: atoms_writer_lease_v2's actual scope_id is `${entityType}:${countyFips}` (confirmed 3 call sites: conformant.mjs:319 cad-parcel-roll, conformant.mjs:598 flood-hazard-fact, conformant.mjs:953, restamp-access.mjs) -- i.e. ALREADY scoped per (entity_type, county), matching AGENT_CONTRACT section 3's current law verbatim. No v1 single-row global lease code exists anywhere in current origin/main (fully migrated). So "atoms_writer_lease = ONE GLOBAL RESOURCE" is not literally true of the lease mechanism today.
- BUT the deeper "holes" section claim survives independently: atom_did (src/stages/write/edges.mjs:9-19, parcelNodeDid/selectorAtomDid) is `did:hauska:${entityType}:${id}` -- NOT prefixed by county_fips, unlike parcel_record's place_key (`{fips}:{prop_id}`). So the DATA STORE'S identity scheme is genuinely not county-partitioned even though the WRITE LEASE now is. A farm's local atom minting really would need real reconciliation on merge-back, for this reason, not because of lease contention.

## Stage 11 / P-197 (surface-probe.mjs + cost meter) -- see above, both confirmed.
- probe-close-gate.mjs hook (doc_repo .claude/hooks/probe-close-gate.mjs, rules in scripts/enforcement/probe-close-gate.mjs:37) -- GATED_ROW regex is literally `/^P-1(5[1-9]|6[0-7])$/` = P-151..P-167 ONLY. OPS-24 rows (P-186..P-198) are entirely outside this hook's range; a close citing no probe artifact for P-186/195/198 would NOT be blocked. Combined with zero OPS-24 rows in surface-probe.mjs's ROWS object: Law 1 is currently enforceable ONLY by a person remembering to (a) add ROWS entries and (b) widen GATED_ROW -- not by any live hook or type today.

## Hooks registered (.claude/settings.json) -- all point at hardcoded P:/doc_repo/.claude/hooks/* (the integration seat's path), not the invoking worktree's own path. Not asserting this is broken (plausible deliberate single-canonical-copy design) -- flagged as an observation, not a confirmed defect, since verifying its actual runtime resolution is outside read-only git-based reach.
- No hook anywhere in the 10-file .claude/hooks/ listing addresses "new credential or secret mount" or "a ruling" (the other two of Law 4's three operator stop points) -- only production-traffic-write (traffic-lease-gate.mjs) has a hook. The other two stop points are enforceable only by a person today.
