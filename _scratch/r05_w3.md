# R-05 W3 parts triage scratch

## GROUND-TRUTH @ 2026-08-21T11:55:00Z

- Seat integration, worktree `P:/doc_repo`, branch `main`, commit `d254467788c795c6f8fa5a9256ad6a074859b615`
- Triaged R-03 close at `_inbox/2026-08-21_r03-parts_close.json` against `_catalog/parts_inventory.json` compiled `4b174d1` @ `2026-08-21T02:15:00Z`
- Counts match close: 10 NONE, 14 ZOMBIE, 6 inventory parts with `owningSeat: UNASSIGNED` covering 5 named gaps (plan-review service + app share one repo)
- No production store probe this pass. UNKNOWN left UNKNOWN. No LIVE invented.
- Artifact: `_inbox/2026-08-21_r05_w3_parts_triage.md`

## LESSON

- R-03 `isNoTermination` in `scripts/build-parts-inventory.mjs` matches `NONE`, `when we decide`, `N unset`, `effectively NONE`. It does not match `superseded`. `cert-grade-block13`, `multi-shapefile-zip-sweep`, and `L16B-pipelines-runner` carry NONE-shaped off-ramps with extra words and are absent from the 10.
- extraStores stamps every IDLE store `consumers: "NONE until utility rail writer"`. That sentinel landed on `neondb.code_atoms`, which is not a utility store.
- `plan-review-neon` is `owningSeat: property` because extraStores defaults property. The plan-review service/app are UNASSIGNED. Same housing, two owners. Derivation, not a ruling.
- SEAT-01 (`8c386a9` fix, `5e9385a` narrow): an unlisted product path is `unregistered_worktree` and agent writes refuse. Unassigned is not "anyone may write." It is "harness cannot write, native git still can." Collision class is OVERRIDE plus shared checkout, not an open register hole.
- R-03 close said the 10 NONE parts were quarantined. Canonical stores (`hauska_mcp.atoms`, `smart-files-neon`, dimension tables) are not quarantine candidates. Quarantine is for leftover names and parallel stores, not the catalog.

## DEAD-END

- Do not round `neondb.reasoning_atoms` or `hauska_mcp.document_blobs` to DORMANT because size exists. Size without a traced consumer is UNKNOWN.
- Do not assign `mcp.hauska.dev-dns` into `seats[].repos[]`. It is not a git repo. SEAT-01 cannot own DNS.

## OPEN

- Operator ruling owed: add hauska-mcp-server, plan-review, icc-portal, smartcity-os to property `repos[]` (smartcity-os with no-touch / noTouch refuse).
- R-06: fail CI when a LIVE service/app has `owningSeat: UNASSIGNED`; extend NONE detector to `superseded` without a named executor.
- Markets probe for empressa-trading-cockpit and smart-markets still owed (not this worker).
