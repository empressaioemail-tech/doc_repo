# R-05 W4 scratch (Tier 2)

## GROUND-TRUTH 2026-08-21
- Snapshot P:/doc_repo main d254467788c795c6f8fa5a9256ad6a074859b615, seat integration.
- canon-divergence.mjs renderReport writes frontmatter `checks: ${args.checks}` as an absolute path. stampLastVerified rewrites repo_intents_checks.json via JSON.stringify when stamp=true and checks === DEFAULT_CHECKS.
- cited-untracked looksLikeRepoPath accepts `.git/` (regex allows `.` and `/`); existsSync(.git) true; isInGit false. Same class: `.git/objects/` in agent_workspace_hygiene.md backticks.
- ci-baseline IMPROVED (actual < baselineExit) does not fail the job; it tells you to tighten. Never raise baselineExit.

## LESSON 2026-08-21
- R-04 tracked diffs of canon_divergence.md and repo_intents_checks.json are the control writing itself, not census scope creep. Do not land: leaked P:/tmp/r04-controls/ into checks path.
- Blueprint ENFORCED count of 1 (BP-MEANING-01) is not a register ENFORCED. three-layer-audit.mjs is absent from tooling_register.json controls[] and is manual-only (DORMANT). Do not count DORMANT or STARVED as ENFORCED.
- `.git` prefix skip must be `.git/` or exact `.git`, never startsWith('.git'), which would swallow `.github/`.

## OPEN
- Planner applies cited-untracked patch, proves by violation on a CLEAN tree, then LOWERS baselineExit 2 to 0 and moves tier to BLOCKING. W4 did not apply.
- Re-run canon-divergence from P:/doc_repo with an out-of-tree --out if a fresh report is wanted; do not commit the r04-controls leak.
