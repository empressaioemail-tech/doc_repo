# Field-mapping pass — Lane 3 / P-58 / feasibility A3

## GROUND-TRUTH (2026-08-24)

Filed `_inbox/2026-08-24_field_mapping_pass.md`. Snapshot doc_repo `476cca2`. No store hop. No `--apply`. No product write.

Recommended single writer for Bastrop setbacks: **BDC ordinance via `resolveAuthoritativeSetbacks`** (gold 30/10/30). Leftover writer is atom-chain `setback-rule` via `deriveBakedCardModel` (gold 25/5/25).

ETJ and who-serves: dest named 2026-08-24 in `_inbox/2026-08-24_p73_dest_bind_etj_who_serves.md`. Cards in flight: P-76 city-limits, P-75 who-serves. Not adapters-designed on this pass; dest is enough to start Wave 1.

## OPEN

- Card bind still serves the leftover writer. Meaning-shaped check (atom-chain `front_ft` vs live-derive `front_ft`) would fail on gold. That fail is the desired gate, not this pass.
- Store hop (`cad_property`, `hauska_mcp.atoms`, setback-rule DID / layer-23 vs B3) stays unmeasured.

## LESSON

A3 is a mapping-completeness precondition, not an ingest wave. Naming the next card is the whole ETJ/who-serves job on this pass.
