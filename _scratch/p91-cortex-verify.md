# p91-cortex-verify (Tier 2)

LESSON: F-11's setback refusal is a prose `basis`, not a closed token, because Gate 8 C7 walks every served string and the retired provenance `road-class-setback-table` must not appear. The p563 vocabulary maps tokens. Putting the 70-character sentence in that table is the wrong shape and would starve until something forwards the field.

DEAD-END: Calling the token safe because a panel walk did not print it. `parcelDrawFromReads.boundaryInput` omits `setback`; `edgesFromDraw` drops it; `parcel-anchor.ts` reads only `cityLimitsFact.queryPoint`. That omit is the evidence.

GROUND-TRUTH 2026-08-31T12:52Z: `GET https://cortex-api-tds7av26va-uc.a.run.app/api/brokerage/v1/place/node/48021%3A34137/facets` 200, 11322 bytes. Lead setback `{state:"refused", basis:"refused: retired road-class derivation — a road class is not a setback"}`, no feet. `road-class-setback-table` absent from the body. `queryPoint` `{-97.31654, 30.10981}`. `48021:82112` is `atom-miss`. Five other named fixtures match the refused arm. Roles observed: front, side, rear, side_corner.

OPEN: live `research/brief` / `get_smart_site` JSON still gated. If a later cut forwards `setback`, property seat ships a closed `code` on each serve arm before any MCP vocab row.
