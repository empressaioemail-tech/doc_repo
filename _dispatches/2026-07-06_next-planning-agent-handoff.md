# Next planning agent — handoff (2026-07-06 close)

You are the planning agent continuing the three-lane program in `P:\doc_repo`. Read order: (1) this file top to bottom; (2) `_inbox/2026-07-06_three_lane_program_STATUS.md` — the live tracker and verification log, your state anchor; (3) `_sessions/2026-07-06_three_lane_autonomous_run_claude_code.md` for the narrative. Steering-doc claims go stale within hours: verify against live gh/npm/gcloud before acting on any of them. Concurrent sessions exist in this shared clone — `git log -3` + `git status` before every doc_repo commit, stage explicit paths only.

**Your FIRST job is to walk the operator through Section 1 below, item by item, before starting build work.** He asked for exactly that. Then proceed to Section 2 (queued stack) autonomously.

## 1. OPERATOR-GATED — walk Nick through these, in this order

1. **NPM_TOKEN paste (2 minutes, unlocks the most).** The token ldt CI uses is PROVEN to publish to the @empressaio scope (five packages shipped with it today). Nick pastes that same token as an Actions secret named `NPM_TOKEN` in TWO repos: `hauska-atom-contract` (new) and `hauska-sdk` (replace the stale 2026-04-05 one). Once done, YOU: add a tag-push publish workflow to atom-contract (mirror ldt's proven publish.yml pattern, idempotent version-exists skip), fix the SDK main build error (pre-existing tsc exit 2 right after adapters-storage-redis in the workflow build step), then publish by tag-push: `@empressaio/atom-contract@1.7.0` (main is ready and deliberately untagged — tag at publish moment to avoid tagged-unpublished divergence) and `@hauska-sdk/metering@0.1.1` (tag already exists; re-run its workflow after the build fix). This ends manual npm forever. If Nick prefers manual instead: `npm publish --access public` + OTP from a clean atom-contract main checkout (the 1.6.1 path).
2. **Title-slice verification spend (a real money decision).** Herbert has no Reeves deals, so verification is re-architected (activation decision step 5, amended): Nick approves purchasing 1-2 professional runsheets from a title plant/abstractor (CourthouseDirect-class, a few hundred dollars) as graded ground truth for the title slice. The cost deliberately feeds the commitment-3 checkpoint. Get a yes/no + budget ceiling; the slice's other two verification legs (dual-source consistency, operator cross-signals) need no approval.
3. **Herbert relay (optional, zero-block).** The Reeves allocation-vs-PSA ratio: RRC W-1 query at webapps2.rrc.texas.gov, County=Reeves, since 01/01/2022, Wellbore Completion Type = Allocation, then PSA, then blank for total; residual = pooled+standalone (resolvable only by our county ingest — which validates the ADR's negative-case rule). Herbert or Nick can run it in 5 minutes; OR it arrives automatically as the C3 adapter's first deliverable. Also relay to Herbert his surviving role: grading run-sheet METHOD (structure, chain handling, ratification gaps), which transfers across counties.
4. **og-twin + command center visual review (his quality bar is the exit gate).** https://og-twin.vercel.app (append `?source=synthetic` for Chris's original data; check the OPERATOR OVERLAY badges and the 3D drilling scene) and https://cmdcenter-blush.vercel.app (post-shell-dissolution). Collect his defect list; it drives the next UI rounds.
5. **T1 enforce checkpoint (FYI, grant already given).** You will deploy MCP with the #38 fix, soak log-mode, review gate_context_mismatch/product logs, then flip enforce and remove plain-header trust — autonomously, per his standing grant. Tell him when the soak review happens so he can watch if he wants; do not block on him.
6. **Standing items he owns, do NOT nag:** Cotality production keys (his, self-managed, never a blocker per ruling); extension key rotation + Upstash replacement (deferred until after QA by ruling); Stripe stays test-mode until he flips it; VS Code zombie installer `CodeSetup-stable-3.10.11` needs an elevated End-task (planner shell got access-denied).

## 2. QUEUED STACK (dependency order — proceed autonomously after the walk-through)

1. **MCP redeploy + T1 soak + enforce (FIRST, fresh context by design).** Deploy hauska-mcp-server main (has #38) via the recorded method: `cloudbuild-build-only.yaml` build+push, `gcloud run deploy --no-traffic --tag`, gate probes (anon→public, malformed→401, product-key matrix, 63 tools), shift. Full env/secret list is in the convergence tracker runbook (`_inbox/2026-07-04_convergence-program_STATUS.md`). Then log-mode soak with REAL keyed traffic, review signed-context product values (the #38 bug made everything "public" — verify map keys now stamp map), then GATE_CONTEXT_MODE=enforce on all three services + remove plain-header trust. Then T2 (tenant-private write) per the tenancy plan.
2. **A4 engine truth + revenue meter:** pysheds into the engine worker image (Hydrology degraded banner); IPMC2018P2 empty-body adapter fix; metering wire-up (layer2_call events at the MCP tool-call layer → the existing Stripe test meter) + a native revenue-meter panel in the command center. Metering is an ICC contract ACCEPTANCE CRITERION, not discretionary.
3. **ldt reviewer-queue endpoint** (spec in hauska-map repo: REVIEWER_QUEUE_SCOPING.md from A3): expose a reviewer-scoped engagements listing the proxy can reach; then the command center queue shows the ~31 real engagements; then the live compliance-run e2e probe (an IBC citation labeled "(ICC model code)" — the B1 wiring is deployed and waiting).
4. **ICC walkthrough assembly** (the 2026-07-06 operator answer in the convergence tracker): extension running cited briefs + command center plan-review space + revenue meter. Needs 2+3.
5. **C3 RRC adapters, Reeves-first** (hauska-engine, one merge queue — engine is shared): FIRST deliverable = the Reeves W-1 ratio report (allocation/PSA/total, County=Reeves, since 2022); then production (PDQ + EBCDIC, oil-at-lease/gas-at-well), H-10 injection/disposal, W-1/P-4/P-5/completions. Shapes from @empressaio/atom-contract@1.7.0 ./og (consume the vendor tarball pattern until published).
6. **Reeves mint (C6) + title slice (C7)** per the amended activation decision; C7 waits on gated item 2's spend approval; mint gate = non-vacuous curated eval + cost capture.
7. **M1 calibration run** (operator ruled RUN): the 06-25 drafted dispatches — engine edition-bundle ingest, then K2 retrodiction Austin+SA, then the M1 measure. Also correct `00d_portfolio_roadmap_reference.md`'s retracted arrow-two claim.
8. **Doc pass:** promote proof-of-record → slot 62 and certification scaffold → slot 63 (rename "Hauska Verified" per branding; keep the non-vacuousness floor; Reeves mint gate cites it); park siting memo as exploration; scrub-tracker additions for the branding rename (CLAUDE.md brand language, thesis docs).
9. **Hygiene:** close-or-land engine #75 (check for unlanded corpus work FIRST) and #76 (superseded by #83); extension PR #3 folds into B2; the B2/B3 extension catch-up + live QA loop lane itself; eval-scores curated queries; discoverability (llms.txt, MCP registry listing — the Phase-1 GTM exit); Upstash decision.

## 3. Roadmap after the stack

ICC walkthrough demo-ready → Phase-1 "own the layer" exit (spec published ✔, metering live, discoverability listed — exit test: an outside agent can discover, call, and pay for a Layer-2 tool) → Radar launch chain (Web Store, landing origin; Cotality-dependent legs on Nick's key) → tenancy T2 → Reeves mint flips og-twin from seeded to live behind the same TwinDataSource seam (zero view changes — that's the architecture's promise).

## 4. HOW to execute clean builds (the model that produced 11 merged PRs, 5 deploys, 0 rollbacks today)

**The loop: dispatch → verify → adversarially review → merge on green → deploy → LIVE-PROBE → tripwire.**

- **Dispatch:** fresh clone per task at `P:\tmp\lane-<name>` (never the operator's persistent clones). Write `CURSOR_TASK.md` into the clone: branch name, "push immediately after your FIRST commit, keep pushing", "delete CURSOR_TASK.md before final commit", "open a PR titled ...", context the agent cannot discover (live-probe evidence, exact file:line findings, ratified rulings VERBATIM — agents deviate from paraphrase), and EXIT-BOUNDED verification only ("pnpm build + pnpm test ONLY; NEVER pnpm dev/preview/watch" — a runner hung 8h on a dev server; see memory dispatch-no-nonexiting-commands). Launch: PowerShell, PATH refresh (`$env:PATH = "$([Environment]::GetEnvironmentVariable('PATH','Machine'));$([Environment]::GetEnvironmentVariable('PATH','User'))"`), then `cursor-agent --print --force --model sonnet-4.5 "Read CURSOR_TASK.md in this directory and execute it completely."` with run_in_background. If a runner shows 0-byte output but old pushed commits: find the hung child (`Get-CimInstance Win32_Process` filtered on the task dir) and kill the child; the agent resumes.
- **Verify (never delegate):** re-run the suite/build yourself in the clone; grep the agent's claims (they narrate fixes that don't exist); byte-math sanity checks (a 16KB bundle claiming to contain a 262KB app is a lie).
- **Adversarial review:** for substantial PRs, an independent subagent prompted to REFUTE with file:line evidence and its own runtime checks; expect HOLDs — today's two HOLDs caught unregistered prefixes, absent DID validation, and a freeze-critical core-type shape. Small diffs: planner line-review. ldt merges wait for green CI (memory: merge-only-on-green-ci). Merge style: `--merge` (repo convention).
- **Deploy + live-probe:** per-service recorded methods (cortex-api = ldt workflow_dispatch canary chain: bare dispatch = deploy-canary, then `-f action=shift-traffic`; MCP = cloudbuild + tag + probes; cmdcenter/og-twin = `vercel deploy --prod --yes` from repo root, projects cmdcenter/og-twin under empressaioemail-techs-projects). Watch workflows with background `gh run watch <id> --exit-status`. THEN PROBE PROD — today's probes caught three defects green tests missed. Windows curl needs `--ssl-no-revoke`; MCP probes need `Accept: application/json, text/event-stream`.
- **Tripwire:** every caught failure becomes a committed test (proxy contract test, nested-shell DOM-gone test, subsystem-presence text assertions). Agents repeat failure modes; tests don't forget.
- **Traps (today's scars):** PS5.1 `Out-File -Encoding utf8` writes a BOM that breaks vercel.json — write JSON via bash printf or the Write tool; GitHub "Base branch was modified" on merge is transient — retry, and NEVER chain checkout-main-and-deploy after an unverified merge in one command; npm registry propagation lags `npm view` by minutes — read the workflow log before theorizing; secrets referenced in deploy config may not EXIST in the deploy project (canary fails safe at 0% — create + IAM-grant + redeploy); a "coordinated-landing" PR list in a tracker may already be merged — check `gh pr view` states before planning around them.

## 5. Live state snapshot (verify before relying)

Prod: cmdcenter-blush.vercel.app (A3 shell + provider fix), og-twin.vercel.app (verbatim mockup + seeded Reeves), cortex-api-00293-hud (B1 ICC supplement, rollback 00290-qol), hauska-mcp-server-00010-zuq (four-gate; #38 fix MERGED but NOT deployed — that's your queue item 1), engine-api-00031-rap, retrieval-api-00008-ber (29,857 atoms incl. 8,731 icc-model-code). npm: @empressaio five packages live; @empressaio/atom-contract@1.7.0 on main UNPUBLISHED; @hauska/atom-contract latest stays 1.6.1. T1: log-mode live on all three services, signing key mounted, enforce pending queue item 1. Stripe: test-mode products + layer2_call meter exist. og-twin consumes the contract via committed vendor tarball until the npm publish lands.
