CANON-PREAMBLE v0f465c77

- COTALITY IS EXTINGUISHED — when code hits it (502/OAuth/fallthrough), re-route to county-gis/public-record, NEVER rotate the credential. Regrid also dead.
- DEPLOYS ARE PLANNER-OWNED — the agent deploys and fixes failed deploys; never escalate a deploy to the operator; "failed on X, fixing X".
- NO PRIVILEGED DATA — everything via uniform public-record; any path must work for a no-relationship jurisdiction.
- CTX / national HELD until Bastrop QA-done + operator go.
- CODE-DONE != CUSTOMER-DONE — a grade is a live probe on the deployed surface across multiple different-data parcels, never a merged PR.
- Full standing-decisions detail: `MEMORY.md` (auto-memory) + `_decisions/`.

AGENT-CONTRACT v4d5ddcd3 — you are bound by 90_runbooks/AGENT_CONTRACT.md in full (fan model,
interruption recovery, slot law + lease, heavy-scan serialization, verification rules, close schema).
Read it before any work; where this dispatch and the contract disagree, STOP and report.

PLAN-ROW: P-36 (90_operations/OPS-16_texas_market_plan_of_record.md)
repo: doc_repo

# Gate grading harness — DC-1..13 as a button

MISSION: build the GATE GRADING HARNESS — one runnable script that grades every done-card item
DC-1..DC-13 from _decisions/2026-08-11_texas_flush_launch_gate_amendment.md by its NAMED INSTRUMENT
and emits a pass/fail table. The gate close becomes a button, not a scramble. Hook-shaped controls
run 1-for-1 in this program; this is the gate's hook.
1. Read the amendment's GRADABLE DONE-CARD; implement each DC item's instrument EXACTLY as written
   (as amended: DC-11a graded on the MCP health endpoint per OPS-16 A-012). Ledger items grade via
   GET /api/county-ledger (now materialized, sub-second, computedAt-stamped — REQUIRE freshness:
   computedAt within 30 min or the item reports STALE-INSTRUMENT, never a silent pass) with the DB
   cross-checks the card names (SELECT COUNT(*) FROM county_rail for DC-1, etc.).
2. Output: _inbox/<date>_gate_grade.json + a human table — per DC item: PASS / FAIL / STALE with the
   verbatim instrument evidence (counts, run ids, header lines). DC-13 emits the verbatim summary
   snapshot automatically.
3. Idempotent, exit-bounded, read-only everywhere (SQL SELECTs, GETs, gh reads, file checks). Zero
   writes outside _inbox. Runnable by anyone: node scripts/gate-grade.mjs (doc_repo scripts/, like
   dispatch.mjs) or an ldt CLI — your CP1 call, justify placement.
4. Prove it by RUNNING it now and filing the honest current grade (expect FAILs — DC-3 mid-drain,
   DC-6 unpriced; the harness reporting those honestly IS the acceptance test, plus the DC items we
   believe pass: DC-1/4/5/10/12 must grade PASS mechanically).
CONSTRAINTS: read-only; no slot/lease; no doc edits beyond _inbox + the script itself (uncommitted,
planner commits).

CHECKPOINTS AND CLOSE (exact paths; machine-checkable per contract section 6):
  CP1: _inbox/2026-08-14_l23_cp1.json
  CP2: _inbox/2026-08-14_l23_cp2.json
  CLOSE: _inbox/2026-08-14_l23_close.json
