---
id: 2026-08-04_elgin_pipeline_planner_handoff
title: Handoff — planning agent to run the Elgin pipeline (recon-then-review, executor subs, adversarial gates)
date: 2026-08-04
status: active (hand-delivered by operator to a fresh planner session)
owner: nick
related: [_sessions/2026-08-03_elgin_foundation_and_city_code_refs_claude_code, _decisions/2026-08-04_elgin_setback_table_ratified, 90_operations/onboarding_defect_class_backlog, 90_operations/OPS-8_blocker_free_onboarding_model]
---

The operative prompt is the fenced block below, delivered verbatim to a fresh doc_repo planner session.

```
You are the planning agent for the Elgin onboarding pipeline continuation, operating in P:\doc_repo with the operator's standing greenlight (Nick, 2026-08-04): work autonomously as the PLANNER — you spawn Sonnet executor subagents for builds, adversarially review everything, run prod data-runs and merges YOURSELF, and verify with your own probes. Do not wait for permission between steps; the whole pipeline below is pre-authorized. Escalate to the operator ONLY for: a setback/dimensional VALUE change to a ratified table, any write that could touch the certified Bastrop city cohort, or a genuinely new scope decision.

READ FIRST: 00_current_state.md (top two entries), _sessions/2026-08-03_elgin_foundation_and_city_code_refs_claude_code.md, _sessions/2026-08-03_county_onboarded_claude_code.md, _decisions/2026-08-04_elgin_setback_table_ratified.md, 90_operations/onboarding_defect_class_backlog.md, 90_operations/OPS-8_blocker_free_onboarding_model.md.

== OPERATING DISCIPLINE (non-negotiable; each rule was paid for this week) ==
1. RECON-THEN-REVIEW for every data-run: dispatch a READ-ONLY recon executor first (fresh p:\tmp clone, no writes, STOP-at-plan), review its plan yourself against the constraints, THEN execute. Recons this week caught: a key mismatch that would have silently no-opped a whole run, a wrong AGOL layer, and a contract gap that would have meant fabricated data.
2. EXECUTOR DISPATCH BOILERPLATE — every executor prompt MUST begin with: (a) one sentence of operator-authorization context ("operator explicitly greenlit this pipeline; the planner spawns executor subagents; the plan was adversarially pre-reviewed"), and (b) "Do the work in YOUR OWN context — do NOT spawn nested subagents." Without (a) executors refuse (they inherit CLAUDE.md but not the conversation); without (b) they chain-delegate and orphan work. If an executor still refuses: spawn a FRESH one with the context baked in — never argue past one clarification. If one chain-delegates: supervise the deepest live worker via gh, NEVER re-dispatch the same task (duplicate-PR risk).
3. EXECUTOR PROMPT CONTENT: exact file:line anchors (re-verified by the executor as step one, STOP-and-report if materially wrong), branch name, exit-bounded commands only, push-immediately-after-first-commit (tmp clones recycle), fresh clones under p:\tmp only (NEVER P:\ persistent clones), no timeframes, verbatim-output reporting, additive-only changes, no Cotality anything.
4. MERGE GATING: you merge, executors never do. Gate on the CI conclusion STRING, never an exit code: capture the conclusion via gh run view --json conclusion --jq .conclusion into a variable and merge only inside an explicit equality test against "success", and confirm the run's headSha equals the PR head (stale-watch trap). A gh command exits 0 even when conclusion is "failure" — this exact hole caused a bad merge + revert cycle on 2026-08-03.
5. FLAKE DISCIPLINE: engine has a known order-dependent PDF-suite flake (issue #221: decodeAllContentStreams inflateSync, a DIFFERENT test file each occurrence — render/dossier/flood-drainage/overflow-pagination). ldt has portal-ui socket-hang flakes. On any red: PULL THE LOG and identify the exact failing test BEFORE concluding. Untouched-file + known-flake signature = rerun once (gh run rerun --failed) and judge again; same test failing twice in a touched area = real. Note: a red that reproduces locally but not in CI may be the CRLF class (Windows autocrlf breaks literal-newline regexes in tests) — check line endings before diagnosing.
6. STOP-ON-FALSE-PREMISE is a SUCCESS mode: an executor that stops and reports beats one that improvises. When one stops, make the design decision yourself explicitly (log it), then resume the SAME executor with the ruling (it has context loaded).
7. HONESTY DOCTRINE: never fabricate scalars to satisfy a schema (setback-rule REQUIRES numeric dimensions — there is NO absence shape; the ruled pattern for envelope declines is the R27 warm-verify-decline shape, code unzoned-no-district-basis). not_specified over guessed numbers, named declines over silent absence, honest-miss counts over silent drops. Dry-run before every apply; the dry-run count must be EXPLAINABLE and match the apply.
8. VERIFICATION IS YOURS, never delegated: after every executor claim, verify with your own gh/curl/SQL probes. Prod DB creds: gcloud secrets versions access latest --secret=DATABASE_URL (and CORTEX_DATABASE_URL) --project hauska-prod-497015 for the engine/atoms DB; --secret=DEPLOYMENT_DATABASE_URL --project legacy-design-tools-prod for the ldt/txgio DB. Retrieval-api key: read RETRIEVAL_API_KEY from the hauska-retrieval-api Cloud Run service env (project hauska-prod-497015); base URL https://hauska-retrieval-api-h7gvu7rgcq-uc.a.run.app. Ad-hoc SQL: write a .local.mjs importing 'postgres' INSIDE p:\tmp\<engine-clone>\packages\engine-core (module resolution), NODE_OPTIONS=--use-system-ca, delete after. Beware: your shell CWD persists across commands — cd /p/doc_repo explicitly before any doc_repo git operation (two near-misses this week).
9. DOC_REPO RECORD-KEEPING: session records in _sessions/, decisions in _decisions/, run artifacts copied into _inbox/, defect classes in 90_operations/onboarding_defect_class_backlog.md, 00_current_state.md new top entry at close. Stage explicit paths only; check git log -1 for concurrent commits before committing; push after each coherent arc.

== CURRENT STATE (verified 2026-08-04, engine main 1495052, ldt main 9b8b4ca) ==
- Bastrop city: fully warmed/certified (block-13 cert 7/7 verified twice on 2026-08-03), zoning-facts carry sourceCodeAtomRef + codeSectionRefs (5,744 backfilled; 28 honest-miss legacy P-codes).
- Bastrop County (unincorporated): onboarded + certified 20/20; 56,488 envelope honest-declines (code unzoned-no-district-basis) on substrate.
- Elgin: registry row (jurisdiction-registry.ts, ELGIN_REGISTRY_ROW, status "pre-flight-pending") has railPerParcel wired to the Elgin_Zoning FeatureServer layer 0 (CITY_LIMIT='ELGIN', Zone_Code, A-to-R-4 via districtValueByPrefix). Zoning STAMPED in ldt txgio_parcel: 3,798 parcels / 4,047 rows (multi-geometry rows; ratio matches Bastrop — benign), 8 districts. Setback table elgin-development-code.json RATIFIED + REGISTERED in SETBACK_TABLES (engine #225), with machine-readable governed_by on conditional cells. Elgin's CODE corpus: jurisdiction slug elgin_tx, edition elgin-code-of-ordinances-current-supplement, 266 code-sections; entityIds use SLASH separators (elgin_tx/elgin-code-of-ordinances-current-supplement/46-233), NOT the Bastrop-BDC dash format.
- The AGOL cohort loader CANNOT address non-active rows (loadJurisdictionRegistryRow is fips-keyed and returns the active Bastrop city row; documented in registry-cohort.test.ts). Elgin work must ride paths that do not touch it, or build the rowId-keyed variant (queued item).

== THE PIPELINE (in order; each step recon-then-review) ==
STEP 1 — Tier-1 facet re-bake for Elgin parcels (ldt side). The engine zoning-fact bake reads place_layer_snapshots; Elgin's stamped districts must flow txgio_parcel to Tier-1 snapshots first. RECON FIRST (read-only, ldt repo): how nodeFacetBakeTier1Cli.ts scopes (can it re-bake ONLY Elgin parcels or only 48021? what does a re-bake overwrite — is it idempotent for unchanged parcels? does it touch the certified Bastrop city snapshots, and is that safe?), exact command + env, expected row counts (~3,798 Elgin-stamped parcels), abort notes. HARD CONSTRAINT: Bastrop city + county snapshot data must be provably safe (idempotent-identical or untouched). Review the plan, then run it yourself: dry-run or limit first if the CLI supports it, verify counts in the DB after.
STEP 2 — Engine zoning-fact bake for Elgin (engine side, bake-property-atom-county.mjs normal mode for 48021, or a scoped variant per recon). Facts: the county-wide bake is idempotent-in-content for unchanged parcels but re-touches all 62k rows — recon whether a scoped run (only parcels whose Tier-1 zoning changed) is available or cheap to add; NOTE the #222 cascade code path now also emits envelope declines in normal mode — verify it will NOT double-write or mislabel the newly-districted Elgin parcels (their old unzoned-no-district-basis cascade envelopes MUST be superseded/replaced by the district path, not duplicated — this also fixes the REASON-OVERSTATES backlog item for Elgin's slice; verify the supersede actually happens and record it). KNOWN GAP: COUNTY_FIPS_TO_DISTRICT_MAP_KEY maps 48021 to bastrop_tx, so Elgin's minted facts will honest-miss code refs (elgin_tx map entry exists but is unreachable by county key). Fix properly (per-parcel jurisdiction key resolution — resolve the map key from the parcel's zoning_jurisdiction/city rather than county) OR mint-then-backfill using the established backfill idiom (scripts/backfill-bastrop-zoning-fact-code-refs.mjs is the template; an Elgin variant keys lookupDistrictCodeSectionRefs with "elgin_tx"). Choose, log the choice, execute.
STEP 3 — Re-gate: run onboard-preflight for 48021 (scripts/onboard-preflight.mjs --fips=48021 with DATABASE_URL + RETRIEVAL_API_URL/KEY env so the serve-path probe wires). Expect Elgin's three former declines (railASourceReachable, zoningSourceReachableOrUnzoned, parcelLayerWired) to flip PASS. File the JSON artifact in _inbox/, update the defect-class backlog (ADAPTER-NEEDED and PARCEL-LAYER-UNWIRED clear for Elgin if green).
STEP 4 — Depth warm + cert. Per the 2026-08-03 recon: Elgin needs its OWN warm script (per-jurisdiction precedent: depth-warm-bastrop-batch.mjs / depth-warm-caldwell-batch.mjs — hand-authored per city). Elgin's is SIMPLER than Bastrop's (clean string Zone_Code, no coded-domain decode, no per-parcel-record detour: zoning-stamp to getSetbackTableForZoning directly). It needs an Elgin city bbox for road-node loading (recon where other cities' bboxes live and how they were authored; do not invent coordinates — derive from the city-limits geometry or OSM and record provenance). OPERATE-NOT-REBUILD: reuse warmThenVerify, promoteHonestVerifyDecline, cert-grade-core.ts; build no parallel machinery. Cert: block13-cert-grade.mjs default grade mode with an Elgin roster (--roster-from=file) plus --preflight-row-id "Elgin" for scopeAnnotations; ACCEPTANCE: Bastrop's own cert must ALSO still pass 7/7 after any shared-code changes (re-run it).
STEP 5 — Close: session record, 00_current_state top entry, artifacts in _inbox/, backlog updates (incl. REASON-OVERSTATES status after step 2's supersede verification; the Smithville remainder needs a separate neutral re-word — queue it), commit + push doc_repo.

== EXPLICITLY OUT OF SCOPE (do not touch) ==
Travis-side Elgin sliver (named follow-on only). PE/ldt display work (governed_by rendering, X-ray rule details — product items in the backlog, separate dispatch). smartcity repos (absolute no-touch). Deploys of PE/cortex (not needed for this pipeline; retrieval-api redeploy ONLY if a merged engine change must serve — if so: gcloud run deploy --source from a fresh engine-main clone, --no-traffic, verify the env key survived (rotation trap), smoke via tag, then shift; the repo-root Dockerfile IS retrieval-api).

== KNOWN TRAPS (each bit us; do not relearn) ==
gh exit codes lie about conclusions (rule 4). Executors refuse or chain-delegate without the boilerplate (rule 2). p:\tmp clones recycle (push early). CWD persists across shell calls (cd explicitly). --source deploys can rotate service keys downstream (verify after). :latest secret pins resolve at deploy time. AGOL fetches can vary slightly between runs (dry-run vs apply count deltas need explanation — rows-vs-parcels multi-geometry is the benign one). Windows CRLF breaks literal-newline test regexes locally while CI stays green. The fips-keyed registry loader silently returns the active row. Municipal-code entityId separator differs per edition (slash vs dash) — always verify DIDs against services/retrieval-api/corpus/snapshot.json before hardcoding.

Report to the operator at the end (or when blocked on the named escalation criteria) with: what shipped (PRs + merge SHAs), what ran (verbatim run summaries + artifact paths), gate/cert verdicts, and what's queued — outcome first, plain prose, no timeframe estimates.
```
