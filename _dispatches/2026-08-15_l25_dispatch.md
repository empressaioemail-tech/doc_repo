CANON-PREAMBLE v0f465c77

- COTALITY IS EXTINGUISHED — when code hits it (502/OAuth/fallthrough), re-route to county-gis/public-record, NEVER rotate the credential. Regrid also dead.
- DEPLOYS ARE PLANNER-OWNED — the agent deploys and fixes failed deploys; never escalate a deploy to the operator; "failed on X, fixing X".
- NO PRIVILEGED DATA — everything via uniform public-record; any path must work for a no-relationship jurisdiction.
- CTX / national HELD until Bastrop QA-done + operator go.
- CODE-DONE != CUSTOMER-DONE — a grade is a live probe on the deployed surface across multiple different-data parcels, never a merged PR.
- Full standing-decisions detail: `MEMORY.md` (auto-memory) + `_decisions/`.

AGENT-CONTRACT v7b714e95 — you are bound by 90_runbooks/AGENT_CONTRACT.md in full (fan model,
interruption recovery, slot law + lease, heavy-scan serialization, verification rules, close schema).
Read it before any work; where this dispatch and the contract disagree, STOP and report.

DEV-PROCESS vbb19bd34 — you are bound by 90_runbooks/DEV_PROCESS.md in full. It governs how work
is SHAPED and how a result is JUDGED: coverage figures travel with their denominator, classes are
measured never subtracted, an instrument's exclusion set is part of its contract, gating indicators are
proven able to fire, paired controls need a divergence test, guardrails that do not survive a clone are
not guardrails. Every rule in it is traced to an incident. Read it before any work.

PLAN-ROW: P-07, P-08, P-09, P-11, P-12, P-17 (90_operations/OPS-16_texas_market_plan_of_record.md)
repo: hauska-engine

# Drain rearchitecture — fresh-eyes solutions lane

MISSION: REARCHITECT THE DRAIN. You are a fresh-eyes solutions lane with NO loyalty to the current
design. The operator's verdict after three days: the write path spends its wall-clock on stalls and
supervision, not writing, and that is an architecture problem. Your deliverable is a measured
diagnosis, a proposed architecture, and a PILOT that proves it — not a rewrite spree.

THE RECORD YOU INHERIT (verify at source; every claim below has an artifact):
- What works, measured: batched client-side drains hit 1,400-2,043 atoms/s when running
  (48021/Tarrant/Harris drain artifacts in P:/tmp/l1_sd_plans_20260812/). Plans pre-persist as
  NDJSON payloads (77 remainder + 5 metro flood plans banked in P:/tmp/plan_farm_20260813/). The
  DB-enforced writer lease works. The staging pattern works.
- What keeps failing: per-county child-process runners. Three multi-hour stalls in 36h, all in
  plan/read phases, all 0-CPU dead-waits (L16 Brazoria 7.5h silent; L16B Brazoria 8h CHATTY — its
  touch-loop kept liveness green while its own killAtMs was exceeded 3.3x and the kill never fired;
  Harris flood plan starved twice under concurrent parcel reads). Artifacts:
  _inbox/2026-08-14_l16b_pipelines_hang_postmortem.json, leg5_pipelines_progress.json (the skipped
  entry), _inbox/2026-08-14_l24_finding_cortex_contention.json.
- Chain state: legs 0-2 + mud scorer DONE (SD 253/253, rail 254/254, Harris parcel-node, ~30M atoms
  at 0 verifyFailures). Pipelines 20/254 (48039 skipped for diagnosis). Remaining: pipelines, wells,
  footprint, flood (plans banked), roads, CAD re-applies (manifests in _inbox), Ector.
- Hard constraints that STAND: one lease holder per atoms DB (the lease TABLE allows the holder to
  run parallel workers — the law is one CUSTODIAN, not one connection); heavy-scan serialization on
  deployment Neon; verify-by-PK on stored bytes; provenance/honesty rules per AGENT_CONTRACT +
  DEV_PROCESS. Everything else — per-county processes, serial legs, client-side writes — is
  INCIDENTAL and yours to kill.

HYPOTHESES TO EVALUATE (measure, do not assume; add your own):
H1. SET-BASED SERVER-SIDE DRAIN: plans are already fully-materialized NDJSON payloads. COPY a
    county's payload into a temp/staging table, then ONE INSERT...SELECT (ON CONFLICT) into atoms,
    verify-by-PK via a set-based readback count+digest — all server-side. If this holds, a county is
    seconds-to-minutes and the 1,400/s client ceiling is irrelevant. This is the biggest lever;
    pilot it FIRST on a mid-size county's banked flood plan.
H2. ONE SUPERVISOR, PARALLEL COUNTY WORKERS under the single lease: wall-time budgets enforced by
    the supervisor killing workers (prove the kill fires with a synthetic hung child — the L16B kill
    logged and never fired), work-based liveness (CPU/rows, never touch-loops), N counties in flight
    where DB throughput allows (measure saturation; W1 measured single-writer, nobody has measured
    2-4 concurrent).
H3. CONTENTION ISOLATION: plan/read scans on a Neon read replica or branch so plans never starve
    against drains (Harris flood plan is still owed and starves under any concurrent parcel reads).
    Evaluate cost/feasibility on the actual Neon plan.
H4. PLAN-SHAPE FIXES where reads hang: Brazoria-class 0-CPU dead-waits look like the eng #335
    TEMP+GiST class (that fix took Harris SD 5h->6.4min). Diagnose 48039's pipelines SELECT with
    EXPLAIN before any re-run.
DELIVER:
1. A wall-clock autopsy with counting rules: of the ~60h since the chain started, how much was
   writing, planning, stalled, idle-awaiting-humans. Artifacts exist for all of it.
2. The proposed architecture (likely H1+H2+H4; justify or refute each) with the lease/honesty
   constraints intact.
3. A PILOT on real data proving the core claim (e.g., one county via set-based drain, verified by
   PK + digest against the same county drained the old way), with measured rates.
4. The migration path for the REMAINING legs only — no re-doing finished work — and the projected
   completion at measured pilot rates.
CONSTRAINTS: build in isolated worktrees; no --apply beyond your pilot county without planner go;
the pilot county's lease interaction coordinated via _STATE.md custody note (L16B currently holds
the lease — your pilot uses a PLANNER-granted window, ask via close/CP artifacts); register every
long-running run in _catalog/watch_registry.json per contract; wall-time budgets on everything.

CHECKPOINTS AND CLOSE (exact paths; machine-checkable per contract section 6):
  CP1: _inbox/2026-08-15_l25_cp1.json
  CP2: _inbox/2026-08-15_l25_cp2.json
  CLOSE: _inbox/2026-08-15_l25_close.json
