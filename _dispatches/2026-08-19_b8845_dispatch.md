CANON-PREAMBLE v664d6256

- COTALITY IS EXTINGUISHED — when code hits it (502/OAuth/fallthrough), re-route to county-gis/public-record, NEVER rotate the credential. Regrid also dead.
- DEPLOYS ARE PLANNER-OWNED — the agent deploys and fixes failed deploys; never escalate a deploy to the operator; "failed on X, fixing X".
- NO PRIVILEGED DATA — everything via uniform public-record; any path must work for a no-relationship jurisdiction.
- CTX / national HELD until Bastrop QA-done + operator go.
- CODE-DONE != CUSTOMER-DONE — a grade is a live probe on the deployed surface across multiple different-data parcels, never a merged PR.
- SMARTCITY PRODUCT LINE THEN UI THEN ONE FEED — template Dashboards UI first, then one adapter/source onto `template-city`. Live Bastrop is an island, not the next card. Three identities: `template-city` demo, live `tenant_id=2` Bastrop, next onboarded city. Do not rewrite `tenant_id=2` in place. CitizenConnect is the citizen lens, not a SKU. Feeds are adapters that write records. Destination still `_decisions/2026-08-17_smartcity_product_line_then_bastrop_onboarding.md`. Next-card sequence `_decisions/2026-08-17_dashboards_ui_then_one_feed.md`. Gap map `_inbox/2026-08-17_dashboards_missing_pieces.md`.
- FEED ADAPTER CONTRACT (G-63 CLOSED) — kinds are a catalog; grants are per city pack. Write spine or files with provenance. Never a Dashboards vendor table. Never Pipedrive as a city feed. Samsara fleet copies are not G-24. Decision `_decisions/2026-08-17_g63_feed_adapter_contract.md`.
- G-11 CITY-PACK TENANCY (CLOSED 2026-08-17 as sequencing) — a city pack is the tenant. Identified caller is a Hauska product key whose `jurisdiction_tenant` equals `cityKey`. `DASHBOARDS_API_KEY` is not a tenant. Fixture pack `fixture-city`. Not sprint-54 done. Not live ingest. WDLL `_inbox/2026-08-17_g11_tenancy_WDLL.md`. Decision `_decisions/2026-08-17_g11_city_pack_tenancy.md`. Close `_inbox/2026-08-17_g11_close.json`.
- G-45 SMARTSITE STAFF MAP (CLOSED 2026-08-17) — Dashboards staff map is the SmartSite embed of gold `48021:34137`. GET `/` auto-loads it. Do not cut live Leaflet. Do not clone PE. WDLL `_inbox/2026-08-17_g45_smartsite_staff_map_WDLL.md`. Decision `_decisions/2026-08-17_g45_smartsite_staff_map.md`. Close `_inbox/2026-08-17_g45_close.json`.
- G-64 LANE C STAFF PATH (CLOSED 2026-08-17) — Dashboards development-services mounts plan-review-app. GET `/?lens=development-services` auto-loads it. GET `/` stays G-45 SmartSite. Do not cut live PermitFlow. Do not start G-52. WDLL `_inbox/2026-08-17_g64_lane_c_staff_path_WDLL.md`. Decision `_decisions/2026-08-17_g64_lane_c_staff_path.md`. Close `_inbox/2026-08-17_g64_close.json`. Serving Dashboards `00007-8sc`.
- G-65 PERMITFLOW KILL (CLOSED 2026-08-17) — PermitFlow dead as a Dashboards product. Live `/permitflow/*` uncut until a named island replacement. WDLL `_inbox/2026-08-17_g65_permitflow_kill_WDLL.md`. Decision `_decisions/2026-08-17_g65_permitflow_kill.md`. Close `_inbox/2026-08-17_g65_close.json`.
- COMPASS IS SHARED-ELEMENT SHEET CHROME — G-66 item. Top-bar source control, not a page, not a rail-only assistant. Answer engine is out of this wave. Old Compass is not the atom-render reference; SmartSite is. Decision `_decisions/2026-08-17_ux_implementation_sequence.md`.
- UX IMPLEMENTATION SEQUENCE (G-67 first) — kit copy, then G-66 / G-68 / G-69 in parallel. Those three CLOSED 2026-08-17. G-24 stays zero. Live Bastrop no-touch.
- FILES COMPOSE THEN ONE FEED (G-70 G-71 G-72 CLOSED 2026-08-17) — Work → Files mounts smart-files-app. G-71 wrote Bastrop municode meetings onto `template-city` files. That host is a HOLD (identity collapse), not a feed win. Decision `_decisions/2026-08-17_files_compose_then_one_feed.md`.
- SHELL BEFORE FEEDS (G-73 CLOSED 2026-08-17) — Every G-18 / live-Bastrop staff function has a named home on the Dashboards shell. Connections is 67 of 67 Homes-table rows. Assets honest-empty. Feeds still pause. Register `_inbox/2026-08-17_g18_shell_homes.md`. Decision `_decisions/2026-08-17_shell_before_feeds.md`. WDLL `_inbox/2026-08-17_g73_shell_homes_WDLL.md`. Close `_inbox/2026-08-17_b_g73_close.json`.
- TEMPLATE-CITY IDENTITY (G-74 CLOSED 2026-08-17) — municode grant pulled off template-city. Compose meetings empty with basis `no municode calendar grant on template-city`. Citizen has no Chestnut. Connections HTML has zero Bastrop. No clerk retarget. Decision `_decisions/2026-08-17_template_city_identity.md`. WDLL `_inbox/2026-08-17_g74_identity_leak_WDLL.md`. Close `_inbox/2026-08-17_b_g74_close.json`.
- DEMO-CITY CHROME (G-75 CLOSED 2026-08-17) — mounts fill the frame, one SmartSite iframe, Compass-class map motion from current rails, 30c screens honest-empty. Serving `00013-vkl`. Plan Review `embed=1` is Dashboards-side; host already had detection. Interruptibility partial. Register 67 of 67 plus 3 addenda. Note `_inbox/2026-08-17_g75_shell_mounts_motion.md`. WDLL `_inbox/2026-08-17_g75_shell_mounts_motion_WDLL.md`. Close `_inbox/2026-08-17_b_g75_close.json`. Handoff `_inbox/2026-08-17_demo_city_template_handoff.md`.
- SMARTCITY PRODUCT-LINE DESIGN SYSTEM — one Empressa kit governs Dashboards, Smart Files, Plan Review, and future Asset Management. Not a Dashboards-only theme. Not Hauska chrome. Decision `_decisions/2026-08-17_smartcity_product_line_design_system.md`.
- SMARTCITY VISUAL LAW (session 1, operator loved 2026-08-17) — quiet surfaces, loud exceptions, honest absence. Register not card deck. Sidebar. Inverted applicability (Pass quiet, Unchecked hatch). Inter + Plex Mono, 12px floor. Environment badge. Not-built nav. Provenance chip; no bare confidence. Code citation has no ICC body slot. Light `--sc-atom` `#177F78`, dark `#4CC9C0`. Kit extract `_inbox/2026-08-17_sc_kit.css`. Decisions `_decisions/2026-08-17_smartcity_visual_law.md` and `_decisions/2026-08-17_atom_accent_light_hex.md`.
- SMARTCITY DASHBOARDS HOUSING — one product repo `empressaioemail-tech/smartcity-dashboards`, cities as tenant packs. Live Bastrop stays `smartcity-os` until a named island replacement. Decision `_decisions/2026-08-17_smartcity_dashboards_housing.md`.
- Full standing-decisions detail: `MEMORY.md` (auto-memory) + `_decisions/`.

AGENT-CONTRACT v7b714e95 — you are bound by 90_runbooks/AGENT_CONTRACT.md in full (fan model,
interruption recovery, slot law + lease, heavy-scan serialization, verification rules, close schema).
Read it before any work; where this dispatch and the contract disagree, STOP and report.

DEV-PROCESS vbb19bd34 — you are bound by 90_runbooks/DEV_PROCESS.md in full. It governs how work
is SHAPED and how a result is JUDGED: coverage figures travel with their denominator, classes are
measured never subtracted, an instrument's exclusion set is part of its contract, gating indicators are
proven able to fire, paired controls need a divergence test, guardrails that do not survive a clone are
not guardrails. Every rule in it is traced to an incident. Read it before any work.

PLAN-ROW: G-88 (90_operations/OPS-17_govtech_stack_plan_of_record.md)
repo: smartcity-kit

# G-88 items 4 and 5, recount and wrap

## MISSION — G-88 items 4 and 5, recount and wrap

Housing: `empressaioemail-tech/smartcity-kit`. Your clone, and nobody else's: **`P:/tmp/b8845-kit`**.
Clone it fresh from `https://github.com/empressaioemail-tech/smartcity-kit.git`. You will also need a
READ-ONLY checkout of `smartcity-dashboards` to re-vendor from and to run vendor-parity arm B; clone that
to **`P:/tmp/b8845-dash`** and never write to it. Do not touch `P:/tmp/lb-kit-planner`, `P:/tmp/g82v`,
`P:/tmp/lb-dash-planner`, `P:/tmp/b89-dash` or any other clone; siblings are working concurrently.

Branch from kit `main` at `93f2e6b30a6c8350dc5ec0cf878e240827b15abf`. Branch name
`g88/recount-and-wrap-four-families`. **You open the PR. You do NOT merge it.**

**You do NOT run the DesignSync upload.** The re-sync half of item 5 is the planner's and runs after your
PR merges. Your job ends at a green PR that makes the bundle correct to build.

### What this is

G-88 item 2 shipped four CSS families into the product's `web/shell.css` and merged. The kit still vendors
the OLD `shell.css`, so its `SHIPPED.size === 109` pin still passes — it is passing on a stale copy, which
is the gate being lied to rather than the gate working. Re-vendor, recount, and wrap the new classes as
components so the coverage claim holds against the bigger denominator.

### Planner measurements — GIVEN, and the authority is your re-run, not this list

Reporting any of these wrong is a successful outcome of this dispatch. Measured 2026-08-19:

- `vendor/sc-kit.css`, `vendor/app.js`, `vendor/index.html` are byte-identical to the product by git blob
  hash. **Only `vendor/shell.css` is stale**: kit `72710a00b00f234d1bea1b21c70a0198c7702846` versus
  product `c0c656bb4ff9fd9cdce7d31114caab62d749e4e1`.
- Running the kit's own counting rule (`stylesheetClasses()` in `test/_lib.mjs`) over
  `vendor/sc-kit.css` + the product's current `web/shell.css` returns **139**. Thirty added, zero removed.
  The thirty, sorted: `atomchip basisline c cite conf corpus crit dead did f fact fbody fid finding fmeta
  ftitle lic meter model mx mx-fail mx-pass mx-unc mx-unchecked mxgroup mxrow sec sect txt web`.
- **139 is a simulation, not the acceptance value.** The acceptance value is what the rule returns on
  YOUR re-vendored tree. The three G-88 scoping investigations produced 117, 128 and 138 by three
  different methods and all three were wrong; do not add a fourth by trusting this one. Run it, quote it
  WITH its counting rule at the point of use, and if it is not 139 say so and reconcile the difference
  rather than rounding it off.

### Item 4 — the recount

`npm run refresh:vendor <path-to-dashboards-clone>` (read `scripts/refresh-vendor.mjs` first; it rewrites
`vendor/UPSTREAM.json` hashes and normalizes to LF). Rebuild. Update the pin at
`test/gate3-classes.test.mjs:50` to the number the rule RETURNS.

`README.md:180` publishes "**109 of 109**" with its denominator stated. Restate it against the recount.
A coverage figure that travels without its denominator does not ship (DEV_PROCESS 1.1).

### Item 5 — wrap the four families

`test/gate3-classes.test.mjs` carries a **coverage** assertion that every class in the shipped vocabulary
is emitted by some component. It will go red on all thirty the moment you re-vendor. That redness is the
work list; do not weaken the assertion, do not add an exclusion set, and do not park a class as
"structural". Every one of the thirty gets emitted by a real component.

The four families, and the CSS is fully commented in the product's `web/shell.css` from the
`/* ---------- code citation, evidence chip, applicability matrix, finding row ----------` banner to the
`/* ---------- responsive ---------- */` banner. **Read that block before designing anything.** It names
its own divergences (DIV-1 through DIV-5) and its own carve-outs, and it tells you what each class is FOR:

- **`cite`** — `cite`, `model`, `corpus`, `sect`. The licensed `.cite.model` form deliberately has NO
  slot for body copy. That absence is an enforcement mechanism, not a styling choice: licensed model-code
  text must not be able to leak through a component that does not exist. **Do not give your `Cite`
  component a children slot on the model variant.** If you do, you have built the leak the CSS was
  shaped to prevent, and it will pass every gate in this repo.
- **`atomchip`** — `atomchip`, `did`, `dead`, `web`. Markup contract comes from the platform IA document
  (ruling G2): the openable form is a `<button>` with `aria-expanded`; the unverified `.web` form is a
  plain `<span>`. Those are different elements, not a prop on one element. `.dead` is an unservable
  record.
- **`mx*`** — `mx`, `mxgroup`, `c`, `lic`, `mxrow`, `sec`, `txt`, `mx-pass`, `mx-fail`, `mx-unc`,
  `mx-unchecked`, and it reuses the existing `rail`. **Applicability is INVERTED**: pass is the quietest
  row on the page and declares no background and no rail colour; unchecked takes a diagonal hatch because
  unreviewed is more dangerous than failed. A component API that makes "pass" the loud state, or that
  defaults an unset row to pass, reverses the ruling this family exists to express.
- **`finding`** — `finding`, `crit`, `fid`, `fbody`, `ftitle`, `fmeta`, `fact`, plus `basisline`, `conf`,
  `meter`, `f`. `.meter i.f` is a class on an `<i>`, so `f` is emitted by the meter's filled segments.
  The whole point of the family is that a confidence value cannot render without the state, source count
  and timestamp that earn it — so a `Meter` or a confidence prop that can be composed WITHOUT its basis
  line is the same defect one layer up. Shape the API so the earned form is the easy one.

Kit conventions you are bound by, and `src/surfaces.tsx` (`Panel`/`PanelHead`/`PanelBody`, `Metric`/
`MetricStrip`) is the pattern to copy: components own no styling, declare no token, no colour, no radius,
no duration and no type step. Gate 4 scans the whole repo and will fail you for authoring CSS. Export
every new component from `src/index.ts` — gate 3 fails any export missing from the gallery, and any
gallery entry naming a non-export.

Then, in the same PR:

- **`examples/gallery.tsx`** — one `GALLERY` entry per new component with a real `node`, a `from`
  (which shipped screen it was taken from), and a `covers` list. Gate 3 asserts `covers` EQUALS what the
  entry renders, so a hand-written list that drifts fails. Use fixture content that is realistic; the
  existing entries take theirs from the product's own fixture pack.
- **`.design-sync/previews/<Name>.tsx`** — one per new component, ported from its GALLERY entry by
  splitting the JSX into named exports, one per card cell. Budget 2-3 cells each. Read
  `.design-sync/NOTES.md` "Preview authoring" first. There are 73 today; state the new count.
- **`.design-sync/conventions.md`** ships verbatim inside the uploaded README and, since G-87, IS
  scanned by the constraint gates. If you add the new families to it, do not name a vendor brand — that
  is exactly the violation G-87 found sitting green in this file.

### Every gate must be green, and one of them is not a test run

- `npm test` (build + `node --test test/*.test.mjs`) — all green, and state the count either side.
- `npm run typecheck` and `npm run typecheck:examples`.
- `npm run prove:gates` — **27 of 27 injected violations caught and named**, plus any case you add.
  This is the pass/fail instrument, not a passing suite. A passing suite proves nothing fired; the
  injection harness proves the gates can fire.
- `test/vendor-parity.test.mjs` arm B only runs with `SC_DASHBOARDS_DIR` pointed at a dashboards
  checkout. Run it that way and paste the output. Locally-unrun is the default and it is a trap.
- `test/markup-parity.test.mjs` — note that `test/_markup.mjs` normalises away `id`, `hidden` and every
  non-class attribute, so it is silent about the addressability layer BY CONSTRUCTION. Do not read a
  green parity run as evidence about anything except classes.

### Do NOT

Do not rebuild or touch `ds-bundle/`, `.ds-sync/` or `.render-check.json` — those are the planner's sync
leg and a rebuild wipes them. Do not upload anything to the Design project. Do not edit `vendor/sc-kit.css`
by hand — it is byte-identical across three repos and a token change there is a product-line decision, not
a kit PR. Do not merge your own PR. Do not touch `smartcity-dashboards` except to read it.

### Verification rules that apply to you

Every verification step is EXIT-BOUNDED: a build, a test run, a one-shot command. Never a watch, a tail,
or a non-exiting server. Merge gates read the CI check-run **conclusion string**, never a `gh` exit code.
Windows-local CRLF test failures are a known artifact of this machine and CI is authoritative — but say
so explicitly if you hit one rather than quietly ignoring the red.

### Deliverables

- Branch pushed, PR opened against kit `main`, PR number and head SHA reported. **Do not merge.**
- CI run to completion; report the check-run conclusion string.
- The recounted denominator, its counting rule quoted beside it, and the reconciliation if it is not 139.
- The new component count, the new preview count, and the coverage claim restated: N of N.
- CP1 before you write components: the re-vendor done, the recount measured, the coverage test watched
  going red on the thirty with the raw output pasted, and your component API sketch for all four families
  with the three inversion traps above addressed by name.
- CP2 at the pilot: one family fully wrapped, gallery entry, preview, gate 3 green for that family, with
  the raw output pasted.
- Close artifact at the exact path named below, carrying `missionPremise`, `completionPredicate` and
  `scopeBasis` as well as the accounting fields. Paste raw command output for every state claim.

CHECKPOINTS AND CLOSE (exact paths; machine-checkable per contract section 6):
  CP1: _inbox/2026-08-19_b8845_cp1.json
  CP2: _inbox/2026-08-19_b8845_cp2.json
  CLOSE: _inbox/2026-08-19_b8845_close.json
