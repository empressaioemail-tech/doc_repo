CANON-PREAMBLE v0f465c77

- COTALITY IS EXTINGUISHED — when code hits it (502/OAuth/fallthrough), re-route to county-gis/public-record, NEVER rotate the credential. Regrid also dead.
- DEPLOYS ARE PLANNER-OWNED — the agent deploys and fixes failed deploys; never escalate a deploy to the operator; "failed on X, fixing X".
- NO PRIVILEGED DATA — everything via uniform public-record; any path must work for a no-relationship jurisdiction.
- CTX / national HELD until Bastrop QA-done + operator go.
- CODE-DONE != CUSTOMER-DONE — a grade is a live probe on the deployed surface across multiple different-data parcels, never a merged PR.
- Full standing-decisions detail: `MEMORY.md` (auto-memory) + `_decisions/`.

AGENT-CONTRACT v4d5ddcd3 — you are bound by 90_runbooks/AGENT_CONTRACT.md in full (fan model,
interruption recovery, slot law + lease, heavy-scan serialization, verification rules, close schema).
Read it before any work; where this dispatch and the contract disagree, STOP and report.

PLAN-ROW: P-02, P-03, P-06, P-07, P-08, P-09, P-11, P-12, P-17 (90_operations/OPS-16_texas_market_plan_of_record.md)
repo: hauska-engine

# Drain custodian — lease-enforced slot chain (leg 0: SD residue)

MISSION: hold the atoms bulk-writer slot continuously through the entire remaining drain chain, with
the database-enforced slot lease as your first act. You are the DRAIN CUSTODIAN.

PRECONDITIONS (verify all three at source before taking anything): L1's close artifact exists and
records SD complete + slot released to you in _STATE.md; the L15 readiness manifest exists
(_inbox/2026-08-13_L15_plan_farm_manifest.json — note L15 closed HONESTLY_PARTIAL: flood-metro plans
may still be completing behind L1's heavy-scan window; consume what is READY, wait-or-skip-and-report
what is not); and a rogue sweep is clean (no process matching
special.district|rail_apply|solo_seq|a2_staged writing — kill on sight; the A2 runners are quarantined
exit-stubs but the sweep is still mandatory).

FIRST ACT — SLOT LEASE (OPS-16 A-012 ruling 0, build BEFORE any write): engine PR adding a
database-enforced writer lease at the shared bulk-write boundary (writePropertyAtomsBatch or its call
seam): single-row lease table (holder, taken_at, heartbeat, expires); the batch path validates +
heartbeats and FAILS CLOSED with a named error without the live lease. Unit test proving a writer
without the lease cannot write. Merge on CI conclusion STRING "success". Then TAKE the lease as L16
and record custody in _STATE.md.

SECOND PRE-SLOT BUILD: add --from-plan consumption to the flood writer (mirror eng #334's NDJSON plan
pattern) so L15's flood plans are DRAINED, not re-planned in-slot (~1.5h of slot otherwise wasted).
For any OTHER plan query you must run on a mega-county, adopt the eng #335/#336 shape: TEMP
once-detoast + GiST, district/zone-major batches, pool max:1 (TEMP is connection-local) — and
pre-register a SQL budget from a measured reference county with a halt-and-EXPLAIN trigger at 1.5x.

THE CHAIN, in order (per leg: manifest inputs check -> drain batch=5000 -> verify-by-PK -> scorer ->
ledger GET before/after appended to your log; a leg with no gain is data):
0. SD RESIDUE (L1 close, CLOSED_PARTIAL): 81 counties whose special-district-fact atoms still carry
   SF-6 "Parcel centroid" provenance or unknown lineage (roster + reasons:
   P:/tmp/l1_sd_plans_20260812/store_truth_reconcile_table.json — spot-verified at store 2026-08-13).
   Delete per county + plan/drain on the true-geom path (eng #322 machinery; #335/#336 TEMP+GiST shape
   for anything metro-sized). Zero-flip was proven INSUFFICIENT — provenance must read true-geom.
   Re-run L1's reconcile query after: 253/253 method-verified or named residue again.
1. RAIL-CORRIDOR REMEDIATION + STATEWIDE: roster from STORE TRUTH (DISTINCT left(entity_id,5) on
   rail-corridor-fact — the rogue's artifacts undercounted twice; final rogue product 204 counties /
   9,867,600 atoms). Delete per county (entity_id LIKE, composite index — NOT-ANY hangs), re-apply
   from L15's staged NARN under the FIXED classifier (post-#324). GATE: per-county present-rate must
   fall inside L15's pre-registered bands (present_rate_bands.json, 254 counties, halt if
   nearTrueRate > 0.50); outside band HALTS the leg — writer defect, not data. Donley 48129: no
   parcels — satisfied-absent path per the L7 sparse-rail doctrine, not a skip.
2. HARRIS 48201 PARCEL-NODE (~1.5M from txgio; OPS-16 A-009) + geometry rescore.
3. FLOOD METROS from L15 plans via your --from-plan path; sha-digest parity where provided.
4. FLOOD REMAINDER (L15 roster: union 77 = 66 missing + 42 logoff-class, store-truth counts) + full
   flood rescore on the fixed scorer (ldt #420).
5. RRC-PIPELINES from L15 partitions (254 files, 491,178 segments).
6. ECTOR 48135 re-key (~75k, L15 VERIFY.json stillReady) + geometry rescore (closes P-02).
7. FOOTPRINT: writer at/after 28b85a1 ONLY (verify the SHA in your checkout history); staged-only
   fail-closed semantics; one small-county dry first.
8. RRC-WELLS: staged tx_rrc_well ONLY (gis.hctx.net anywhere = HALT); consume L15's overlay-assigned
   county-null wells PLUS its named ~701-well exclusion set — an excluded well renders as honest
   unknown, never as absence on adjacent parcels; fail-closed SYMNUM spot-check on first county.
9. ROADS x254 from L15's PBF extracts (A-003 GO; extracts may still be splitting — consume as they
   land). Bastrop must hold at 19,907 (regression tripwire).
THEN under the same lease: L8's G2b 188-parcel re-promote; SD scorer AFTER leg 0 completes. MUD
DECLARATION CONFIRMATION: GRANTED 2026-08-13 by the planner — the declaration is already correct
(countyRailDimension.ts railKey "mud" = displayName "Special districts", family present, writer bound
since P1-4 2026-08-11; served live not-yet x254). No refresh or redeploy needed. SCORER SEMANTICS: the
rail's subject is special districts GENERALLY (MUD/WCID/MMD/FWSD are districtType body subcategories
per the declaration and OPS-16 A-002); score facet=mud on DETERMINATION coverage (present + provenanced
absent atoms over parcels, threshold 90) per the L7 sparse-rail absence doctrine
(_inbox/2026-08-12_L7_sparse_rail_absence_doctrine.md — read it before writing the scorer); a county
with ZERO TCEQ districts reaches satisfied-absent via the statewide-source positive determination.
Harris reading satisfied-present off county-spanning districts (FCD, Port of Houston — verified at
source) is CORRECT, not an anomaly.
Between legs: zero idle. A leg whose manifest entry is missing/stale is SKIPPED and reported — never
improvise acquisition inside the slot.

Per-leg close artifacts: _inbox/<date>_l16_leg<N>_<name>_close.json. Master close carries the lease PR
+ SHA + CI string, per-leg volumes/rates/verifyFailures, every ledger before/after pair, and slot +
lease release recorded in _STATE.md.

CHECKPOINTS AND CLOSE (exact paths; machine-checkable per contract section 6):
  CP1: _inbox/2026-08-13_l16_cp1.json
  CP2: _inbox/2026-08-13_l16_cp2.json
  CLOSE: _inbox/2026-08-13_l16_close.json
