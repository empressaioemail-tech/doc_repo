CANON-PREAMBLE v0f465c77

- COTALITY IS EXTINGUISHED — when code hits it (502/OAuth/fallthrough), re-route to county-gis/public-record, NEVER rotate the credential. Regrid also dead.
- DEPLOYS ARE PLANNER-OWNED — the agent deploys and fixes failed deploys; never escalate a deploy to the operator; "failed on X, fixing X".
- NO PRIVILEGED DATA — everything via uniform public-record; any path must work for a no-relationship jurisdiction.
- CTX / national HELD until Bastrop QA-done + operator go.
- CODE-DONE != CUSTOMER-DONE — a grade is a live probe on the deployed surface across multiple different-data parcels, never a merged PR.
- Full standing-decisions detail: `MEMORY.md` (auto-memory) + `_decisions/`.

AGENT-CONTRACT v4d5ddcd3 — you are bound by 90_runbooks/AGENT_CONTRACT.md in full (fan model,
interruption recovery, slot law + lease, heavy-scan serialization, verification rules, close schema).
Read it before any work; where this dispatch and the contract disagree, STOP and report.

PLAN-ROW: P-29 (90_operations/OPS-16_texas_market_plan_of_record.md)
repo: hauska-mcp-server

# DC-11b/c/d closeout — load test, capacity doc, domain

MISSION: close the last two small gate items in DC-11 — the load test (DC-11b) and the capacity doc
freshness (DC-11c) — plus the DC-11d domain check. These are P-29 residue named in the L13 close.

1. DC-11b LOAD TEST: a dated, artifact-producing load test against the MCP server surface (the
   service that carries the limiter: hauska-mcp-server, revision family 00063+). Bounded RPS tiers
   (e.g. 1/5/20 rps ramps, fixed duration), measuring p50/p95/p99, error rates, and PROVING the
   postgres rate limiter engages correctly under concurrency (429s at the right thresholds, no
   silent memory fallback — health must show primary=postgres throughout). CONSTRAINT: the atoms
   drain (L16) and cortex Neon are live — do NOT fan load into cortex-api or retrieval-api beyond
   incidental MCP dependencies; the target is MCP + its limiter, bounded and announced. File the
   artifact at the path 76j Workstream C names (or _inbox/ with a pointer).
2. DC-11c CAPACITY DOC: refresh the capacity doc named in 76j_smartsite_launch_readiness_program.md
   Workstream C with the new measurements + last_updated bump (doc_repo edit, uncommitted).
3. DC-11d: curl -sI https://smartsite.cloud — record status + redirect target verbatim. If not
   200/301-to-Vercel, report what IS; fixing DNS is out of scope this lane.
Exit-bounded everything. Live proofs verbatim in the close.

CHECKPOINTS AND CLOSE (exact paths; machine-checkable per contract section 6):
  CP1: _inbox/2026-08-14_l19_cp1.json
  CP2: _inbox/2026-08-14_l19_cp2.json
  CLOSE: _inbox/2026-08-14_l19_close.json
