CANON-PREAMBLE v0f465c77

- COTALITY IS EXTINGUISHED — when code hits it (502/OAuth/fallthrough), re-route to county-gis/public-record, NEVER rotate the credential. Regrid also dead.
- DEPLOYS ARE PLANNER-OWNED — the agent deploys and fixes failed deploys; never escalate a deploy to the operator; "failed on X, fixing X".
- NO PRIVILEGED DATA — everything via uniform public-record; any path must work for a no-relationship jurisdiction.
- CTX / national HELD until Bastrop QA-done + operator go.
- CODE-DONE != CUSTOMER-DONE — a grade is a live probe on the deployed surface across multiple different-data parcels, never a merged PR.
- Full standing-decisions detail: `MEMORY.md` (auto-memory) + `_decisions/`.

AGENT-CONTRACT vbcc3efb6 — you are bound by 90_runbooks/AGENT_CONTRACT.md in full (fan model,
interruption recovery, slot law + lease, heavy-scan serialization, verification rules, close schema).
Read it before any work; where this dispatch and the contract disagree, STOP and report.

DEV-PROCESS v48da8334 — you are bound by 90_runbooks/DEV_PROCESS.md in full. It governs how work
is SHAPED and how a result is JUDGED: coverage figures travel with their denominator, classes are
measured never subtracted, an instrument's exclusion set is part of its contract, gating indicators are
proven able to fire, paired controls need a divergence test, guardrails that do not survive a clone are
not guardrails. Every rule in it is traced to an incident. Read it before any work.

PLAN-ROW: P-02, P-07, P-08, P-09, P-11, P-12, P-17, P-25 (90_operations/OPS-16_texas_market_plan_of_record.md)
repo: hauska-engine

# Successor drain custodian — resume the chain, finish the slot work

MISSION: you are the SUCCESSOR DRAIN CUSTODIAN (L16B). The prior custodian (L16) hung at
2026-08-14T16:29Z mid-pipelines (19/254 landed, frozen progress, 0 CPU accumulation, heartbeats died
with it); its lease EXPIRED at 20:29Z and its hung workers were killed by the planner. You inherit a
clean, guarded store and a fully staged queue. The lease guard is live: nothing writes without your
lease.

TAKE-OVER SEQUENCE:
1. Verify state at source: P:/tmp/l16_drain_20260813/ progress files (legs 0-2 + mud scorer DONE and
   accepted; pipelines landed=19); the lease row (expired, holder L16); _STATE.md custody line.
   Rogue sweep per contract (planner already killed PIDs 54744/68436/6332; verify zero).
2. RE-TAKE the lease as L16B (same table, fresh expiry, heartbeat loop that OUTLIVES your chat: run
   it as a detached OS process writing to a log, so a Cursor stall cannot silently expire it again —
   the prior failure mode was heartbeat-dies-with-agent).
3. POST-MORTEM BEFORE RESUME (budgeted 30 min): the hung county context is pipe_48037.json + the
   tail of pipelines_apply.log. Determine the hang class if cheap (DB wait? fetch? loop?). Whether or
   not root-caused, your resume carries a PER-COUNTY BUDGET with halt-and-diagnose at 1.5x (the
   Harris lesson): a county exceeding budget gets killed, EXPLAINed, and named — never a 5-hour wait.
CHAIN (resume where L16 stopped; per leg: drain batch=5000 -> verify-by-PK -> scorer ->
countyLedgerMaterializeCli --apply -> ledger before/after in your log):
4. PIPELINES resume: landed-only skip (19 done), 48037 first with the budget armed.
5. ECTOR 48135 re-key + geometry rescore (closes P-02/DC-2).
6. WELLS: staged tx_rrc_well ONLY (gis.hctx.net = HALT); L15 overlay + the named 701 exclusion set;
   fail-closed SYMNUM spot-check on first county.
7. FOOTPRINT: writer >= 28b85a1; ONE pilot county with pre-registered budget + TEMP+GiST (#335/#336)
   fallback named BEFORE it runs.
8. FLOOD: --from-plan NDJSONs — remainder 77 READY at P:/tmp/plan_farm_20260813/flood_remainder/;
   metros landing NOW into flood_metros/ (planner is running L24's runner; consume as manifest
   entries flip READY). Digest adjudication precedent: post-#325 plans are correct where old L15
   summary digests disagree.
9. ROADS x254 from the split NDJSONs (P:/tmp/plan_farm_20260813/roads/ndjson/, 255 files). Bastrop
   holds at 19,907 (tripwire).
10. TAIL: Tarrant + Dallas owner/landuse/cad-roll re-applies per
    _inbox/2026-08-14_l21_drain_queue_manifest.json (engine parity #341/#342 merged; ldt #428/#429
    live — declared vintages agree, verify one sample read per county before applying); then G2b
    188-parcel re-promote.
11. MASTER CLOSE + lease release recorded in _STATE.md.
Per-leg closes as _inbox/<date>_l16b_leg<N>_<name>_close.json. SQL is ledger truth during drain
windows; the GET is materialized and stamped — trust computedAt.

CHECKPOINTS AND CLOSE (exact paths; machine-checkable per contract section 6):
  CP1: _inbox/2026-08-14_l16b_cp1.json
  CP2: _inbox/2026-08-14_l16b_cp2.json
  CLOSE: _inbox/2026-08-14_l16b_close.json
