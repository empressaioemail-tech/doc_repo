CANON-PREAMBLE v0f465c77

- COTALITY IS EXTINGUISHED — when code hits it (502/OAuth/fallthrough), re-route to county-gis/public-record, NEVER rotate the credential. Regrid also dead.
- DEPLOYS ARE PLANNER-OWNED — the agent deploys and fixes failed deploys; never escalate a deploy to the operator; "failed on X, fixing X".
- NO PRIVILEGED DATA — everything via uniform public-record; any path must work for a no-relationship jurisdiction.
- CTX / national HELD until Bastrop QA-done + operator go.
- CODE-DONE != CUSTOMER-DONE — a grade is a live probe on the deployed surface across multiple different-data parcels, never a merged PR.
- Full standing-decisions detail: `MEMORY.md` (auto-memory) + `_decisions/`.

AGENT-CONTRACT v4d5ddcd3 — you are bound by 90_runbooks/AGENT_CONTRACT.md in full (fan model,
interruption recovery, slot law + lease, heavy-scan serialization, verification rules, close schema).
Read it before any work; where this dispatch and the contract disagree, STOP and report.

PLAN-ROW: P-07, P-08 (90_operations/OPS-16_texas_market_plan_of_record.md)
repo: hauska-engine

# Flood plan pre-emission — protect the drain window

MISSION: pre-emit DRAINABLE flood plan artifacts (planned[] NDJSON, the eng #338 --from-plan format)
for the 6 metro counties + the 77-county remainder roster, OFF-SLOT, so the drain custodian (L16)
consumes plans instead of re-planning inside the atoms slot (~2h of slot protected). You inherit a
CLOSED lane's staging (L15, plan farm) — its context so you need none of its memory:

- Work root: P:/tmp/plan_farm_20260813/ (flood_metros/, flood_remainder/, progress.json).
- VALID summaries already exist for the metros (digest + counts, e.g. flood_metros/48113.summary.json
  — Dallas: 693,556 planned atoms, zonesIndexed 15,571, mode dry-run). They were falsely marked
  failed by a runner predicate whose '--apply' string check matched the writer's own hint text; the
  PLANS are good, the VERDICT function was not. Do not repeat that mistake: judge by the summary
  file contents, never by a stdout substring.
- Remainder roster: flood_remainder/roster.json (union 77 = 66 missing + 42 logoff-class per its own
  counting rules; one plan landed: 48261).
- Metros: 48201 Harris (expect ~31 min, the zone-major measured wall), 48453, 48439, 48029, 48113,
  48085 (expect 400-750s each).

WORK:
1. Run the flood writer's plan phase in the #338 NDJSON-emitting form (hauska-engine at/after
   e15f7cb; writer: write-flood-hazard-fact-county, --plan-backend=postgis, dry/plan only, NEVER
   --apply) per county, emitting planned[] artifacts L16's --from-plan can drain.
2. HEAVY-SCAN DISCIPLINE: these are heavy PostGIS scans on deployment Neon. ONE at a time, each
   announced before start and confirmed after in progress.json heavyScan (that file is the shared
   mutex; also READ it first — if another lane announced an active scan, wait). L16's atoms-store
   drain does NOT contend; only PostGIS on deployment Neon does.
3. DIGEST CROSS-CHECK: where a valid summary exists, the new plan's digest must match it —
   a mismatch is a FINDING to report (data changed or plan nondeterminism), never shrugged past.
4. Per-county budget: pre-register expected wall from the numbers above; halt-and-EXPLAIN at 1.5x
   (the Harris SD lesson: 74s of CPU over 5 hours is a query-shape defect, not a queue).
5. Flip each county's entry to READY in _inbox/2026-08-13_L15_plan_farm_manifest.json as its plan
   lands — L16 consumes ONLY manifest-READY entries; finished-but-unlisted is invisible.
CONSTRAINTS: zero --apply, zero atoms-store writes, zero slot/lease contact. Exit-bounded runs.
Do not touch P:/hauska-engine (pinned dirty tree) — use a worktree or the l9_full_loads checkout
pattern at origin/main.

CHECKPOINTS AND CLOSE (exact paths; machine-checkable per contract section 6):
  CP1: _inbox/2026-08-14_l24_cp1.json
  CP2: _inbox/2026-08-14_l24_cp2.json
  CLOSE: _inbox/2026-08-14_l24_close.json
