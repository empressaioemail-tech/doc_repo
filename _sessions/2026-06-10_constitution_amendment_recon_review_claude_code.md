---
id: 2026-06-10_constitution_amendment_recon_review_claude_code
date: 2026-06-10
topic: cross-repo recon + adversarial review + partnership-first constitutional amendment + ICC/SDK decision
agent: doc_repo planner (claude_code)
status: complete
related: [00_current_state, 03_structural_constitution_and_drift_guard, CLAUDE, _research/2026-06-09_cross_repo_recon, _decisions/2026-06-09_retire_partnership_first_amend_constitution, _decisions/2026-06-09_icc_nfpa_licensing_and_sdk_rail_survive, 57_national_code_warming_sprint, 04a_arrow_two_calibration_capture]
---

# Session summary — constitution amendment, recon, adversarial review

Ran in parallel with the code-warming / arrow-two build session (`2a0661c1`, summary `_sessions/2026-06-09_codewarm_arrow_two_tenant_keystone_claude_code.md`). This session was strategy, verification, and the constitutional change; that session was the build. The two coordinated through two handoff prompts and disjoint file ownership.

## The arc

**1. Cross-repo recon + doc catchup (commit `647c852`).** Verified live state against GitHub + gcloud + health endpoints because the doc set lagged reality. Findings: the build-out + Miami keystone arc (#141 to #156) was fully merged AND deployed (cortex-api `00140-dax` serving real anthropic findings; `#156` merged, not "held"); a new undocumented `api-server` prod service exists; the gate carries 57 tools and is wired (`00007-njc`, `/health` ok); the retrieval substrate-Neon DB is unwired (`#68`). The whole "deploy the build-out wave" linchpin was already closed. Filed [`_research/2026-06-09_cross_repo_recon.md`](../_research/2026-06-09_cross_repo_recon.md); added the `00_current_state` DEPLOYED+LIVE section that supersedes the stale deploy-pending/ships-mock blocks; corrected the `72a` capital-doc gap #1 (keystone proven, not pending).

**2. Adversarial design review of the code-warming + arrow-two plan (commit `dabe16e`, reviewed not authored here).** Reviewed the combined plan's four load-bearing mechanisms. Verdicts: Area 1 sovereignty split HOLD (undefined inclusion set + `tenant-shared` pooling backdoor + per-class cross-tenant pooling); Area 2 field contract HOLD on same-edition source-set drift silently carrying stale calibration (migration sequencing CLEAR, verified 0035 was live max); Area 3 attribution-coverage HOLD (generation-time `invalidCitationCount` does not cover attribution-time key-space misses); Area 4 placement CLEAR with a stale-gate-rationale flag. Handed all findings to the build session, which actioned them: the canonical atom-id key fix (#158) is the Area-3 resolution; source-set-drift invalidation landed in #157; the gate-lineage audit closed the arrow-one-only Codex path.

**3. Partnership-first constitutional amendment (commit `b046f48`).** Operator call: retire partnership-first sourcing as a structural commitment because the web-first reasoning-not-text pivot moved the moat from city operational data to the public-code calibrated reasoning corpus, leaving the commitment unexercised (a honesty liability) and a recurring source of friction. Pre-mortem GREEN (conditional on tenant-isolation re-founding landing in the same amendment; met). The change: structural commitment #2 becomes confidence-is-earned (calibration, I3); the sovereignty root (I1) survives, re-expressed as tenant data sovereignty (I5 re-grounded as an enterprise customer-trust floor); invariant I8 (cities are partners/licensors) retired; cities reframe to SmartCity OS customers and design partners (Bastrop). Root-cause fix: the premortem-check SKILL hard-coded partnership-first as load-bearing #2, which is why it "kept surfacing" — updated in the same amendment so the validator stops re-injecting it. Decision record with reversal criteria: [`_decisions/2026-06-09_retire_partnership_first_amend_constitution.md`](../_decisions/2026-06-09_retire_partnership_first_amend_constitution.md).

**4. Propagation sweep (commit `a671b4e`).** Reframed the cities-as-licensors language across ten canonical docs (72a, investor brief, commercialization vision, 73, 09, 04, 14, 18, 00d, 08): cities → customers/design partners; the value-returns-to-contributors thesis re-grounded on tenant sovereignty + calibration deposits. Discovered partnership-first bundled three things and the amendment retired only one; flagged the other two rather than silently deciding them.

**5. ICC/SDK decision (commit `c2fd9cd`).** Resolved the flagged sub-decision. Pre-mortem GREEN. ICC/NFPA standards-body licensing KEPT, reclassified as content/display IP licensing (Hauska is the payer/metered channel for copyrighted code text; reinforces sell-reasoning-not-data; not the retired Bastrop template; commercial structure flat-fee-vs-metered-share TBD in negotiation). The SDK source-actor revenue rail KEPT as designed-not-built dormant plumbing (not a structural commitment, rail-quiet I7; first use = ICC/NFPA routing if the deal carries it; no "substrate-enforced revenue share" headline until the routing layer ships). Decision record: [`_decisions/2026-06-09_icc_nfpa_licensing_and_sdk_rail_survive.md`](../_decisions/2026-06-09_icc_nfpa_licensing_and_sdk_rail_survive.md).

## Coordination with the build session

Two handoff prompts: first to fold arrow-two scope into its code-warming plan (one cohesive build on the shared `reasoning_atoms` confidence field), then to fold the partnership-first retirement + the three surviving review findings into that plan. It built the entire arrow-two + tenant leg on top: cold-warm harness (#157), key-space (#158), citation preservation (#159), gate-front seam + Phase 2 (#160), Phase 3 calibration overlay (#161, `a431e8e`), tenant isolation at the gate (#29), ADR-005 ratified. Arrow-two is now CLOSED end-to-end and invariant I3 is satisfied. File ownership stayed disjoint; no commit collisions.

## State at close

The constitution, its enforcement skill, the canonical doc set, and the active build are all aligned on the new four commitments (sell reasoning / confidence-is-earned / cost-per-jurisdiction / dual-interface) with tenant data sovereignty as the re-grounded I5 floor. Arrow-two (roadmap item 1, the one load-bearing build) is done. The forward frame is the GTM-readiness sprint ([`58`](../58_gtm_readiness_sprint.md)) and the moat feature set ([`59`](../59_spine_moat_and_high_value_features.md)). Residual, non-blocking: the encumbrance/county-recorder partnership rows in 73 (swept this close); finding-quality tuning; the three observability PRs (#68/#27/#26); Decision C GTM unpin (its gate is now met).
