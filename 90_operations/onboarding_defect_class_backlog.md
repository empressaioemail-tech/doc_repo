---
id: onboarding_defect_class_backlog
title: Onboarding defect-class backlog (OPS-8 dual-ledger, doc side)
date: 2026-08-05
status: active
owner: nick
related: [OPS-8_blocker_free_onboarding_model, _sessions/2026-08-03_trust_surface_wave1_execution_claude_code, _decisions/2026-08-03_cert_scope_annotation_ruling]
---

# Onboarding defect-class backlog

The class-grouped half of the OPS-8 dual defect ledger: every pre-flight decline and honest-decline event groups by DEFECT CLASS so one fix clears a whole class. The CC per-jurisdiction gaps column is the other half (not yet built). Source of truth for entries: `onboard-preflight` ledger-event JSON from real gate runs, planner-run with prod creds, verbatim artifacts kept alongside.

## First real gate run — fips 48021, 2026-08-03 (baseline, pre-D5 probes)

Run artifact: `_inbox/2026-08-03_preflight_48021_baseline.json`. Verdicts: Bastrop 5/8 PASS, Bastrop County (unincorporated) 5/8 PASS (unzoned regime correctly PASSES zoning as expected honest-absence), Elgin 2/8. Zero mid-run stalls; every gap named up front. The inversion works.

## FULL gate run — fips 48021, 2026-08-03 (all 8 checks live, post-#220)

Run artifact: `_inbox/2026-08-03_preflight_48021_full_gate.json`. **Bastrop 8/8. Bastrop County (unincorporated) 8/8 — EARNS THE RIGHT TO RUN** (first jurisdiction through the full gate; unzoned regime PASS, geometry parity holds on sample, serve-path healthy, cost $14.11 est < $200, superseded 0/5732 with a real denominator). Elgin 5/3, declining only its known ADAPTER-NEEDED + PARCEL-LAYER-UNWIRED classes. Annotations: the fips-wide sample checks (geometry/cost/superseded) are county-sample results for Elgin, not Elgin-specific (it has no parcel rail yet); the 0-superseded count carries a measure-vintage watch (live serve demonstrably honest-declines superseded parcels, so run-time honesty is unaffected). Cert machinery re-proven same run: post-refactor block13-cert-grade reproduces **7/7 CERT-RESTORE ELIGIBLE** (`_inbox/2026-08-03_block13_cert_post_refactor.log`). NEXT: the county warm is now a gate-earned run awaiting operator greenlight (run-what-passes: parcels/terrain/flood rails; zoning/setback honest-absence by regime).

## Class register

| Defect class | Members (rows) | Fix that clears the class | Status |
|---|---|---|---|
| PROBE-UNWIRED (geometry parity, serve-path, cost) | ~~all rows~~ | CLEARED by engine #220 (merged e680dc6 after a #218 revert cycle; src/scripts dependency inverted). Full-gate run 2026-08-03 proves it: geometry parity PASS on sample, serve-path PASS, cost $14.10-14.13 < $200 | CLEARED (`_inbox/2026-08-03_preflight_48021_full_gate.json`) |
| MEASURE-EMPTY-COHORT (superseded 0/0 false-PASS) | ~~Bastrop, Bastrop County~~ | CLEARED by #220 (query keyed countyFips which never exists on envelope bodies → parcelNodeId LIKE; DECLINE backstop added). Full-gate denominator now real: 0/5732. Residual watch: 0 superseded vs the known live superseded-decline parcels (e.g. 48021:141364) — measure-vintage semantics question, run-time honest-decline unaffected | CLEARED (watch note) |
| ADAPTER-NEEDED | ~~Elgin~~; eCode360 bucket (Smithville/Pflugerville/…) | Elgin CLEARED 2026-08-04 re-gate 8/8 (`_inbox/2026-08-04_preflight_48021_elgin_regate.json`). Class stays OPEN for the eCode360 bucket — ENGINEERING-track per the 2026-08-04 scrape ruling | CLEARED for Elgin; OPEN for eCode360 |
| PARCEL-LAYER-UNWIRED | ~~Elgin~~ | Elgin CLEARED 2026-08-04 re-gate 8/8 (railPerParcel wired to Elgin_Zoning/0) | CLEARED for Elgin (`_inbox/2026-08-04_preflight_48021_elgin_regate.json`) |
| SUPERSEDED-GT3PCT | none yet (first honest measures arrive post-D5) | R15 successor re-key (class-backlog per OPS-8, never a run blocker) | EMPTY |
| GEOMETRY-DIVERGE (real divergence, not probe-unwired) | none yet | fix engine before warming the affected cohort | EMPTY |
| SERVE-PATH-UNHEALTHY (real, e.g. retrieval 401) | none yet (the 2026-08-03 cortex key desync predates the gate and is FIXED) | key re-sync + fail-loud (shipped in wave 1) | EMPTY |
| COST-GATE | none yet | per commitment #3, engineering review on any breach | EMPTY |
| MIXED-VINTAGE | none yet (scan PASSed all rows) | enumerate + re-warm plan before running | EMPTY |
| REASON-OVERSTATES (planner-caught in own output, 2026-08-03) | ~~Elgin slice~~; Smithville-area remainder — CLAIMED 2026-08-04 by the OPS-9 planner session as the COUNTY-FAN PREREQUISITE (operator greenlit the fan; branch fix/cascade-decline-reason-city-aware makes the cascade city-aware + adds a re-word backfill mode; Elgin session should NOT build this) | Elgin slice CLEARED 2026-08-04 by zoning-fact bake supersede (stale `unzoned-no-district-basis` on Elgin district parcels = 0; cascade remaining 52,726 = 56,488 − 3,762). Smithville remainder still needs neutral re-word. **REWORD APPLY HELD 2026-08-04 (dry-run catch)**: the `--reword-city-parcels` dry-run reported scanned 52,726 / would-reword **38,026** / no-city-signal 14,700 / 0 errors — blast radius far beyond the Smithville intent, and the first samples are **McDade** (an UNINCORPORATED CDP). The mode's "city signal" is the situs-city tenant segment, NOT city-limits membership, so it would reword unincorporated-community parcels to "no district on record — jurisdiction not yet onboarded", which understates THEIR truth (the county is their jurisdiction, it IS onboarded, unzoned is correct). The dry-run-first discipline caught a 38k mis-wording before it happened. Fix before apply: scope the mode to an incorporated-city allowlist (e.g. `--city-segments=smithville`) or a city-limits membership check; the situs segment alone is too blunt. **CLEARED 2026-08-05**: engine #252 added the required `--city-segments` allowlist (blunt whole-county form now fail-loud-unrunnable); scoped dry-run 5,327 smithville-segment parcels (32,699 correctly not-allowlisted, counts sum exact); **apply 5,327 exact, 0 errors**; idempotency re-run reworded 0 with scan self-excluding (47,399 = 52,726 − 5,327); **block13 regression 7/7 blockPass held**, both POSTed to ledger | **CLEARED in full** — Elgin 2026-08-04, Smithville 2026-08-05 (engine #252 + scoped apply) |
| ELGIN-CERT-RESIDUAL (2026-08-04) | Elgin promoted cohort cert 2/10 on sample | (a) frontOrientation token match OSM vs CAD situs abbreviations; (b) served rear 0 vs descriptor 10 on some emit paths; (c) occasional per-edge role/index mismatch. Warm itself: 1,886/3,762 promoted. Bastrop block-13 still 7/7 | OPEN — (c) partially addressed for Bastrop **export path** by engine `fix/export-boundary-primitive-refresh` (R28 ring recompute + R30 relabel at export); (a)(b) and flag-lot side/rear judgment remain |

## COUNTY ONBOARDED — 2026-08-03 (the first gate-onboarded jurisdiction, end to end)

Full arc same-day: gate 8/8 → recon (zoning honest-absence layer already baked 2026-07-24; real gap = zero envelope/setback atoms for the 56,488-parcel unincorporated cohort) → contract-shape STOP honored (no fabricated setback-rule atoms; planner ruled envelope-decline-only via the R27 persisted-decline precedent, code `unzoned-no-district-basis`) → engine #222 merged green → cascade run against prod: **scanned 62,260, cascaded 56,488, errors 0** (dry-run predicted the exact count; city cohort untouched, verified live: exactly 5,732 non-cascade envelopes remain) → **county cert 20/20 PASS (all honest-decline, rings resolve), blockPass true** (`_inbox/2026-08-03_county_cert_20of20.json`). Every unincorporated parcel now carries: zoning-fact (named absence) + buildable-envelope (named decline, honest reason string) + terrain/flood serve-time rails; setback-rule legitimately absent by regime. KNOWN TOOLING ARTIFACT on the cert: one scopeAnnotation claims Rail A "not runnable" — the cert script's internal preflight lacks the HTTP probe wiring the standalone gate CLI has; the authoritative full-gate artifact shows Rail A PASS. Queued nit: wire probes into the cert-path preflight. Queued ADR: first-class absence variants for setback-rule/buildable-envelope in @empressaio/atom-contract (tonight used the R27 precedent instead of extending the contract).

## BASTROP CITY CODE-REFS BACKFILL — DONE 2026-08-03 (the zoning→code-text graph edge, retroactive)

Recon caught that a naive re-bake would silently no-op (descriptorForCounty key "breadth_48021" never matches the map's "bastrop_tx" seed — latent bug, FIXED in #223 for future bakes). Executed instead as the repo's established UPDATE-in-place backfill idiom (#223, script with --dry-run/--apply/--revert + drift-pin test): dry-run predicted exactly 5,744 patch / 28 honest-miss (legacy P-codes: P-5 6, P-3 10, P-2 9, P-1/P-4/P-EC 1 each — the 6 still-on-repealed-P-5 parcels are a watch item); apply landed exactly that, 0 errors. Live-verified: gold parcel's atom-chain serves `sourceCodeAtomRef → 14-02-003` + both codeSectionRefs. **Acceptance gate: block-13 cert re-run 7/7 CERT-RESTORE ELIGIBLE post-backfill** (`_inbox/2026-08-03_cert_post_refs_backfill_7of7.log`). The SF-1→district-text chip chain is live end to end (engine refs → atom-chain wire → cortex provenanceRefs → PE chips, all deployed earlier tonight). Operator visual check owed.

## PRODUCT WORK ITEMS FROM THE ELGIN RATIFICATION (2026-08-04, operator directives)

1. GOVERNED-BY RENDERING (PE inspect/X-ray + ldt serve): conditional setback cells now carry machine-readable `governed_by` (added at ratification). Display must show the governing district's values with citations (e.g. C-2 adjacent-to-residential → render C-1's F/S/R with both 46-391 and the C-1 row's citations), or at minimum a live citation to the governing table. Never a bare "not specified" where the governing rule is known.
2. X-RAY RULE DETAILS: the per-field provenance notes (one-vs-two-story side yards, corner-lot cases, formula rears) must render in the detail/X-ray surface. Scalar shows the modeled minimum; the X-ray tells the whole rule.
3. FORMAT REFINEMENT (queued): story-aware dual scalars (side_1story/side_2story) — would upgrade Bastrop's table too.

## EDGE-ROLE-MISJUDGED (new class, 2026-08-04, stakeholder-reported) + S4 CLAIM NOTE

| Defect class | Members | Fix that clears the class | Status |
|---|---|---|---|
| EDGE-ROLE-MISJUDGED | 48021:80578 (605 Mesquite St), 48021:80577 (607 Mesquite St) — flag lots where the served SIDE setback is in reality the REAR (backing orientation misread); reported by Valerie Thompson with screenshots (property-explorer-qna-issues, 2026-08-03); SAME class family as the Elgin cert residuals (edge-role/inset) — cross-jurisdiction pattern | Edge-role classification for flag/irregular lots needs judgment beyond the current geometry pass; likely rides the Elgin CERT-RESTORE residual work (orientation tokens / edge roles). Flag rail for more of these: OPS-10 | **PARTIAL 2026-08-05** — engine `fix/export-boundary-primitive-refresh`: export path now runs cert-grade R28+R30 gates + stale boundary setback refresh from authoritative setback-rule atom. **CLEARED for mid-block simple lots** (48021:31362 109 Higgins St verified live: front=south/Higgins, F25/S5/R25 sheet==card; artifact `_inbox/2026-08-05_31362_export_fix_verify.json`). **Mesquite flag-lot members 80577/80578 NOT CLEARED** — fresh `labelEdgesFromRoads` agrees with stored side/rear assignment; side-vs-rear misread is upstream labeling logic, not stale-primitive/export-index mismatch (artifact `_inbox/2026-08-05_mesquite_flag_lots_export_refresh_verify.json`). Remains OPEN for flag-lot orientation (OPS-10) |

SESSION-CLAIM NOTE (coordination between the two active planner sessions): the OPS-9 planner session CLAIMS the S4 items "rowId-keyed cohort loader" and "cert-path probe wiring" (executor dispatched 2026-08-04, branch feat/s4-rowid-loader-and-cert-probes) plus "cert-wrapper rowId attribution fix". The Elgin session should NOT build these. Elgin session retains: CERT-RESTORE residuals (orientation/rear/inset), Smithville REASON-OVERSTATES re-word, thin C-2/I/R-4 coverage, Travis sliver.

SESSION-CLAIM NOTE (2026-08-04): the **DFW planner session** CLAIMS onboarding for FIPS 48113, 48439, 48085, 48121, 48397, 48139, 48251, 48257, 48367 (county-unincorporated lane + Phase 4 city-source recon only). It DEPENDS ON (does NOT build) the OPS-9 county-fan prerequisites: cascade city-aware decline wording and `gradeUnzonedParcel` cadastral-URL parameterization. The OPS-9 session CLAIMS the Central-TX 9-county wave (Bell/Bexar/Caldwell/Comal/Guadalupe/Hays/McLennan/Travis/Williamson). Neither session touches 48021 certified cohorts without explicit operator escalation.

## WAVE-1 COUNTY CERTS — 2026-08-04 (Guadalupe + Caldwell CERTIFIED; three new classes from the Caldwell debug)

Guadalupe County (unincorporated), 48187: cascade apply scanned 93,728 / cascaded 63,115 / errors 0 (dry-run predicted exactly; the 30,613 delta vs the July recon = parcels now districted by the Seguin/Cibolo stamps, correctly skipped by the city-aware cascade). **Cert 20/20 PASS, blockPass true, POSTed to ledger.** Caldwell County (unincorporated), 48055: cert failed 0/20 twice, root-caused to TWO stacked registry/code defects (below), then **cert 20/20 PASS, blockPass true, POSTed** on roster3.

| Defect class | Members | Fix that clears the class | Status |
|---|---|---|---|
| CAD-LAYER-INDEX-UNVERIFIED | Caldwell row (layer 0 = "Municipal Utility Districts"; Parcels is layer 1) | Engine #245 (live-probed, comment carries the verification). RUNBOOK RULE: never freeze a row's cadastralQueryUrl without a live probe of layer name + fields + one roster-parcel query | CLEARED for Caldwell; rule added to runbook |
| CAD-PROPID-FIELD-CASING | Caldwell (layer echoes `Prop_ID`; parser read `properties.prop_id` → every ring dropped) | Engine #246: case-insensitive property-key match in `fetchBcadParcelRings`; both casings unit-tested; BCAD/Guadalupe byte-identical | CLEARED (structural — any cased-field county now works) |
| CAD-COHORT-VINTAGE-DRIFT | 48055:36263, 48055:131166, 48055:77228 (in the StratMap-sourced cohort + atom store, zero features in the live Caldwell CAD layer) | Roster policy: replace unresolvable parcels deterministically (next-in-cohort-order, CAD-probe-gated) and FILE them here — never silently swap. Class fix: parcel-currency pass against live CAD (successor/retire detection, R15 family) | OPEN (3 members; parcels still carry cascade atoms — serve-time honesty unaffected, ring-grade path unavailable for them) |
| WARDEN-MIXED-CITY-BLIND-SPOT (CONFIRMED by DB query, 2026-08-04) | Guadalupe sweep: 10 GEOMETRY-DIVERGE (`cascade-missing` on re-grade) + 3 SERVE-PATH-UNHEALTHY (`zoningFactPresent` served-vs-db mismatch), parcels 48187:106109…106132 | CONFIRMED not data defects — DB queried directly (48187:106109/106125/106126): each carries tenant `breadth_48187_seguin`/`_marion` (in-city), a zoning-fact absence row (`no-zoning-stamp`, district null), and a buildable-envelope with the CORRECT city-aware decline `no-district-on-record` (the #240 cascade wrote them properly). The Warden v1 comparator is wrong twice: crossStore expects the UNZONED cascade code on every sampled parcel (city-decline codes read as `cascade-missing`), and dbTruth counts only district-carrying zoning facts while the serve path correctly reports the absence fact as present. Warden v1.1: teach the comparator both decline codes + absence-fact semantics | **CLEARED 2026-08-05** (engine #253, Warden v1.1): planner verification sweeps Guadalupe 48187 **13 flag → 0 flag** (`_inbox/2026-08-05_warden11_sweep_48187_guadalupe.json`), McLennan 48309 **20 flag → 0 flag** (`_inbox/2026-08-05_warden11_sweep_48309_mclennan.json`). Comparator fix also introduces honest defect class **`CASCADE-STATE-MISMATCH`** for genuine cascade gaps (replacing mislabeled `GEOMETRY-DIVERGE` on cascade-missing). McLennan members appended same session — same fix clears them |
| MIXED-VINTAGE-NEIGHBOR (Bastrop, Warden first real findings) | 50 parcels, e.g. 48021:31131 district-null with 100% districted neighbors | TRIAGED 2026-08-04 (spatial containment vs `Administrative_BP/Bastrop_County_City_Limits`, method validated on 3 control points; artifact `P:/tmp/bastrop50-triage.json`): **48 REAL — inside Bastrop city limits with no district stamp** (first real Warden-discovered data-defect cohort; fix = stamp-roll pass over the 48 + root-cause why the original stamp missed them, dry-run first per stamp mechanics); 1 unincorporated (edge false positive, feeds Warden v1.1 boundary-aware neighbor check); 1 (48021:25436) prop_id ABSENT from live BCAD — filed under CAD-COHORT-VINTAGE-DRIFT. Note: parcel attribute fields (city/situs_city) are NULL on all 50 — attribute-based classification is a dead end on BCAD; spatial containment is the method. DISTRICT RECON (2026-08-04, `P:/tmp/bastrop48-district-recon.json`): **41 of 48 = stamp re-roll fix** — live `Parcels_One_Click/23` carries a populated ZoneTypeClass for them (SF-1 ×17, GC ×10, RR ×8, MU ×3, IND/PI/PDD ×1 each); the stamp missed parcels whose record exists. **7 of 48 = genuine zoning-coverage gaps** (prop_ids 51847, 49295, 35968, 45713, 32183, 32408, 32688 — ZoneTypeClass null AND no covering polygon in the Place-Types zoning layer; two geographic clusters near (-97.278, 30.097) and (-97.337, 30.116), possibly annexation/ETJ pockets — check for a newer draft-11-2025 layer before confirming). Fix path: stamp re-roll dry-run for the 41 (same-DB stamp CLI, MATCH CONTRACT rules), block13 7/7 regression gate before/after; the 7 route to a zoning-layer follow-up with the city | TRIAGED + RECONNED — 41 stamp re-roll OPEN, 7 coverage-gap follow-up OPEN |

MEASURE-EMPTY-COHORT (Warden `--cert-artifact` not supplied on wave-1 sweeps): operational nit, not a defect — the sweep invocation must pass the cert artifact so certFreshness/diff can run. Added to runbook. **certFreshness timestamp nit CLEARED 2026-08-05 (engine #253):** `cert-grade-and-report.mjs` now writes a timestamped artifact file on every successful run (`when` field); `warden-sweep.mjs` certFreshness reads `when`. Caldwell sweep with `--cert-artifact=P:/tmp/cert5-48055-artifact.json` produces zero flag findings including no "cert artifact carries no timestamp" decline (`_inbox/2026-08-05_warden11_sweep_48055_caldwell.json`).

## HONEST SAMPLING FALLOUT — 2026-08-04 (post-#247/#248 re-gates; vacuous-pass freebies removed, real state surfaced)

The #247 sample fix (unzoned-eligible sampling) plus its #248 correction (degenerate-only filter + dedupe; #247's numeric-only filter emptied Elgin's non-numeric CAD-ref cohort) removed the degenerate-id "freebies" that had let some probes pass vacuously (a degenerate id grades as an honest-decline PASS). Re-gates then surfaced:

| Item | Evidence | Disposition |
|---|---|---|
| BASTROP-CITY-PARITY-LATENT → ENVELOPE-BEHIND-STAMP (mechanism CONFIRMED by DB query same session) | 48021 re-gate: geometry parity 5/5 REAL city parcels diverge (48021:103255/103281/103387/104119/104120). DB state for all five: zoning-fact carries a REAL district (PDD/GC/SF-1 ×3, tenant `breadth_48021_bastrop-city-tx`) while the latest buildable-envelope is pre-stamp vintage (district null, NO decline code, older tenant `breadth_48021_bastrop`) — stamped-but-never-warmed. NOT in the 48 stamp-gap set (zero overlap — this is the complementary defect: stamp present, envelope behind) | MIXED-VINTAGE family. Fix pass: enumerate 48021 parcels with districted zoning-fact + district-null/code-null envelope (the inline lateral-join count query timed out at 2m — run as a batched script), re-warm that cohort through the city warm path, parity re-gate, block13 7/7 regression gate before/after. Post-#248 re-gate state: Elgin 0 declines (8/8 recovered), Bastrop County 0 declines, Bastrop city exactly this one decline | CONFIRMED mechanism; enumeration + re-warm OPEN |
| COUNTY-COHORT-LOADER-ZERO (Caldwell) | `loadRegistryDistrictCohortByRow("Caldwell County (unincorporated)")` returns count 0 (pre-existing; unrelated to #247) → parity/serve probes decline empty-sample | County certs run roster-from-file so cert lane is unaffected; the cohort loader's query shape vs Caldwell's layer needs its own fix. Comal/Bell/McLennan cohorts load fine |
| BELL-COST-GATE-BREACH | Bell 48027 re-gate: sample-cohort cost estimate $333.19 ≥ $200 hard-kill (cohortCount 163,519 — estimate extrapolates per-parcel cost × cohort) | **CLEARED 2026-08-05** — engine #250 recalibrated cost model to bake/warm Neon heuristic (0.25 CU × $0.16/hr + $0.000002/atom-write); removed stale $2/1k-calls term. Re-gate `_inbox/2026-08-05_costgate_48027.json`: **$1.15 PASS** (was $333.34 DECLINE). Regression 48187 $0.59 PASS, 48309 $0.69 PASS | CLEARED (`_inbox/2026-08-05_costgate_48027.json`, engine PR #250) |
| BELL-SUPERSEDED-MEASURE-EMPTY | Bell re-gate: "superseded measure returned empty cohort (query matched 0 parcels — measurement path broken, not zero superseded)" | **CLEARED 2026-08-05** — engine #250: pre-warm counties with 0 envelope atoms now PASS with named state "pre-warm county: no buildable-envelope atoms yet — superseded measure not yet applicable"; MEASURE-EMPTY-COHORT reserved for legacy countyFips cross-check hits. Re-gate artifact `_inbox/2026-08-05_costgate_48027.json` | CLEARED (`_inbox/2026-08-05_costgate_48027.json`, engine PR #250) |
| COMAL-PARITY-CLEARED | Comal 48091 re-gate post-#247: parity no longer declining (previous decline was the degenerate-id sample) | CLEARED — Comal's remaining decline is the shared county-row "parcel layer not wired" (districtField empty on county rows; run-what-passes) |

## STAMP-CENTROID-PRECISION — 2026-08-05 (program-level finding from the Bastrop-41 fix chain)

The zoning-stamp CLI's `representativePoint()` shoelace centroid suffered catastrophic cancellation at WGS84 magnitude (Texas longitude ~ -97): computed centroids landed tens of meters off (measured ~21m on 48021:31131), outside the parcel's own polygon, so the point-in-polygon stamp test missed. **This bug was live for every historical whole-county stamp run for every city in ZONING_LAYERS** — it is the probable root cause of the Warden's 48-parcel Bastrop catch and likely under-coverage in every stamped city (Elgin, Seguin, Cibolo, and the rest of the roster). Fix: local-origin shift before shoelace accumulation (ldt #386; scoped dry-run went 41/41 missed to 40/41 correct with districts matching the independent recon). Fetch pagination and SRS were investigated and ruled out with evidence (7125 fetched = layer count; coordinates identical between txgio_parcel and the live layer).

| Defect class | Members | Fix that clears the class | Status |
|---|---|---|---|
| STAMP-CENTROID-PRECISION | every pre-#386 whole-county stamp run (all ZONING_LAYERS cities) | ldt #386 (numerically stable centroid). VERIFICATION SWEEP REQUIRED: post-merge, re-run every city's stamp in dry-run and count would-stamp parcels — that number per city is the historical under-coverage; review then apply per city with block13-class regression gates | FIX GATING; roster-wide dry-run sweep QUEUED |
| PROPID-GEOMETRY-NONUNIQUE | 48021:29431 (two distinct geometries under one prop_id in txgio_parcel; likely resubdivision) | PLANNER RULING 2026-08-05: EXCLUDE from the 41-parcel apply (run the 40); do not guess the authoritative geometry. Class fix is parcel-currency/successor handling (R15 family): resolve which geometry is current against the live CAD record, then re-key | RULED — excluded from apply; R15-family follow-up OPEN |

## Operating rule

Fix by class, in isolation, off the critical path; re-run the gate after each class fix; a class row moves to CLEARED with the gate-run artifact that proves it. The run never waits on this ledger.
