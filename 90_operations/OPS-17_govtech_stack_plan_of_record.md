---
id: OPS-17_govtech_stack_plan_of_record
title: "OPS-17 — the govtech stack: plan of record"
status: active
last_updated: 2026-09-18T15:30:00Z
applies_to: portfolio
owner: nick
related: [OPS-16_texas_market_plan_of_record, 90_runbooks/AGENT_CONTRACT, _smartcity_masters/00_README, 65_sensors/sensor_program_overview, 65_sensors/convergence_smart_site_live_layer, 80_adrs/adr_023_cortex_reporting_repo_designation, 48_cortex_reporting_plan_review_spec, 75n_icc_code_connect_catalog, _inbox/2026-08-14_spine_unification_handoff, _inbox/2026-08-24_govtech_program_scope, _inbox/2026-08-24_govtech_transaction_contract, _inbox/2026-08-24_govtech_engine_migration_plan]
---

# OPS-17 — the govtech stack: plan of record

## Why this document exists

Four lanes (Smart Files, SmartCity/Bastrop, Plan Review, ICC) are about to be built by four hand-carried lane planners. They share a spine, an auth leg, an atom contract, and a customer. Without a frozen baseline with stable row IDs, they will drift into four private definitions of done and meet at integration.

This repo has a measured base rate: prose-shaped controls run 0-for-3, structure-shaped controls run 1-for-1. This document is the structure-shaped answer, copied in shape from OPS-16, which proved it on the Smart Site program. The spine unification handoff states the cost of adopting it late: Smart Site adopted this machinery four days in and spent those four days paying for its absence. This program adopts it at hour zero.

**Definition of done for the program.** All four lanes complete through Layer 5, the govtech stack built and tested, the spine healthy, and each surface consuming and depositing into the others per the seam table below. Every row graded by its named instrument. A row graded by narration or doc assertion fails.

## Governing rules

These are the OPS-16 rules, which govern here unchanged except where noted. `90_runbooks/AGENT_CONTRACT.md` is the operative law for lane behavior and is not restated here.

1. **The baseline table never gets edited.** Every scope change is a dated row in the AMENDMENTS table. What we believed on 2026-08-14 stays visible forever.
2. **No dispatch without a row.** Every dispatch names `PLAN-ROW: G-xx`. Row IDs carry a `G-` prefix so they never collide with the OPS-16 `P-` rows in a grep, a hook, or a compiler.
3. **Statuses re-grade only by instrument** (SQL, gh, live probe, script). The GRADE LOG records each pass.
4. **Lane-planner fan model** per AGENT_CONTRACT section 1. Four lane planners, hand-carried. Verification never delegated below the lane planner.
5. **Write-slot law** per AGENT_CONTRACT section 3. This program shares the atoms store with any other running program; only `--apply` queues, everything else is parallel.
6. **Shared-leg rows block across lanes.** A row in the SHARED LEGS table is not owned by one lane. No lane may build past a shared leg it depends on by stubbing it privately.

## The structure: four lanes by five layers

The five layers are the proven OPS-16 ladder, generalized. They are ordered by what-blocks-what, not by subject:

**L1 Foundation** — what must exist for anything else to be true.
**L2 Measurement** — does it actually cover what we claim, counted by instrument.
**L3 Integrity** — is it honest where it does not cover, and is access enforced.
**L4 Depth** — jurisdiction- and customer-specific richness.
**L5 Launch** — the commercial surface: priced, sellable, demonstrable.

The four lanes are subjects, each passing through all five layers:

| Lane | Subject | Twin relationship | Primary repo | Owner | Sequence |
|---|---|---|---|---|---|
| **A** | Smart Files | the twin DOCUMENTS (a **NEW** atom family, per A-012) | legacy-design-tools | **nick** + planning agent | **FIRST** (A-007) |
| **B** | SmartCity / Bastrop | a PORTFOLIO of twins including infrastructure | smartcity-os (**absolute no-touch on live Bastrop production**; see CTRL-2) | **nick** + planning agent | last (consumes A, C, Smart Site) |
| **C** | Plan Review | the twin ADJUDICATIONS (the earning loop) | legacy-design-tools (cortex-reporting, ADR-023) | **nick** + planning agent | parallel to A |
| **D** | ICC | a licensed SOURCE metered across every twin citing it | hauska-engine | **nick** + planning agent | parallel to A |

**Owner rule (A-006).** A lane or shared leg without a named owner is not dispatchable. The operator assigns owners at dispatch time; "unassigned" is a blocking state, not a default. **Assigned 2026-08-14: nick owns every lane and shared leg, with a hand-carried planning agent executing each.** The owner is who is accountable, not who does the work; the lane planner is named at dispatch.

**WDLL card rule (A-006).** Every lane carries a FROZEN WDLL card (What Done Looks Like), written at dispatch time and never edited afterward — the lane's own definition of done, in its own words, against which drift is visible. It is filed with the dispatch and cited in the close, with a graded Finish card at close. Full practice: `90_runbooks/wdll_practice.md`. Never call it an "acceptance card" — one artifact with two names is how two lanes produce non-comparable closes.

## The twin, stated once

Ratified by the operator 2026-08-14 and consistent with `65_sensors/sensor_program_overview.md`:

**A twin is a node. A node is an ID full of atom facts. The ID can be a human, a building, a desk.** What this company specializes in is creating twins of things with provenance and access control. That is the essence; everything in this program is a form of it.

Degrees of aliveness (ratified 2026-08-06): a COLD twin has the record; a WARM twin has it verified with provenance on every fact; a LIVE twin has senses streaming current state. The three verbs are CAPTURE, CONNECT, SENSE, in that order, as a cost discipline: hardware is the last resort.

**Sensors are Asset Management Tier 2, not a separate program.** The `_smartcity_masters/32_smartcity_asset_management.md` tiers (record, live state, view) and the sensor program cold/warm/live ladder are the same architecture at two altitudes. The sensor band adds the WATCH plane (a standing rule on a live twin: baseline plus condition plus roster) and the hardware doctrine.

**The two-altitude vocabulary rule is binding on every artifact this program produces.** Internally: twin, node, atom, graph. Externally to a city: the record, the asset, current state. Never say digital twin to a city. The Asset Management master carries the full never-say list; the Smart Files master carries the foundation one-sentence rule and its own never-say list. A claim not in the approved-claims register of a master is not approved.

## Inherited spine constraints (compile-time requirements, not advice)

From `_inbox/2026-08-14_spine_unification_handoff.md`. Every lane inherits these; no lane re-derives them. A lane deliverable that violates one is rejected at checkpoint, not at launch.

1. **Absence is typed and provenanced.** The seven-status taxonomy exists because one undifferentiated absent carried at least seven meanings. Plan Review and ICC face the identical class: code section not found must never conflate never-looked, source-down, paywalled, and genuinely-absent.
2. **Only a positive determination writes an absence.** An empty result re-enters the queue. This one rule killed an entire defect class.
3. **satisfied-absent is a first-class product state.** A surface that cannot render verified-absent misrepresents the substrate.
4. **Freshness is part of every response.** Visible computedAt and servedAt stamps, and a STALE indicator proven able to fire before it is trusted. This binds hardest on Smart Files: it is an artifact store, and a cache without a stamp is a liar waiting for load.
5. **Vintages are declared, never mixed; fallbacks are named, counted, and marked on every fallback read.** Plan Review hits this with code editions (the Bastrop B3-repealed-to-BDC incident is the same disease); ICC citations carry edition identity for the same reason.
6. **EntityId shapes are NOT uniform across families, by design.** Never reconstruct an identity from parts; use the value storage persists. A wrong reconstruction silently matches zero rows and looks like an honest absence.
7. **County-level and portfolio-level determinations are a legitimate node class.** Any surface that aggregates must know the marker convention exists.
8. **Ingestion is a Factory 1.5 workload.** Smart Files ingest and plan-review corpus intake reuse the staging pattern; they do not invent parallel machinery.
9. **Instruments are build items.** Gates are scripts. A gating indicator is tested for its ability to fire before it is trusted. Every ratio travels with its counting rule.

## SHARED LEGS — cross-lane blocking dependencies

Not owned by any single lane. A lane that needs one may not stub it privately.

| ID | Leg | Why it is shared | Blocks | Owner |
|---|---|---|---|---|
| S-1 | **Auth and tenancy** (sprint-54 leg) | The claim flow is the single most load-bearing dependency in the convergence (65_sensors convergence doc). Tenant isolation is not enforced today; Cortex runs an anonymous default tenant. | B (Bastrop private layer), A (per-tenant artifacts), C (reviewer identity), the claim flow | **nick** + planning agent — longest pole |
| S-2 | **Telemetry plane placement** | Open item 1 in the sensor overview: named, not decided. Needs the ADR-008 and 56 target-topology check before it hardens. | B Tier 2 (live state), any watch work | **nick** + planning agent |
| S-3 | **Smart Site mapping adoption** | Operator-named. The Smart Site mapping system is the proven one; SmartCity adopts it and Bastrop mapping comes up to that standard. | B, C (E6 map compose) | **nick** + planning agent |
| S-4 | **ICC content-to-actor reference** | ICC is metered across BOTH Smart Site and Plan Review. The meter currently detects ICC content by allowlist and regex heuristic, not a hard reference. Two citing surfaces, one ledger. | D, C (F6 code library), any Smart Site ICC citation | **nick** + planning agent (lane D executes) |
| S-5 | **Consumer contract shape** | ADR-023 rules that SmartCity consumes plan review as a FUNCTION. Whether all three suppliers are consumed via MCP, direct atom read, or service API must be one answer, not three. | B consumes A, C, and Smart Site | **nick** + planning agent |
| S-6 | **Twin node-class and entityId ruling** | A twin of a human, a building, a desk, or a pump station is not the parcel-keyed shape. Constraint 6 says shapes are non-uniform by design; this program must DECLARE its shapes rather than discover them. **Now also carries the A-007 ruling: does Smart Files EXTEND the brokerage workspace family or SUPERSEDE it?** | A, B (assets), any sensor work | **nick** + planning agent — RULED (A-012) |

## The seam table — what consumes and deposits into what

Operator-stated 2026-08-14. This is the definition of done for all apps consuming and depositing into each other.

| Seam | Direction | Ruling and status |
|---|---|---|
| SmartCity consumes Smart Files | B from A | Documents are the twin records; SmartCity is a consumer, never a second implementation |
| SmartCity consumes Plan Review | B from C | ADR-023: consumer pass only, after the C standalone gate passes. No review logic duplicated in SmartCity |
| SmartCity consumes Smart Site | B from Smart Site | Mapping adoption (S-3) plus the public record layer beneath the city own |
| ICC paid across Smart Site AND Plan Review | D from both | Two citing surfaces, one ledger, one actor identity. Requires S-4 |
| Twins (freeze watch class) surface on Smart Site | Smart Site from B and sensors | Already ruled: the sensor program IS the live rung of the Smart Site ladder (convergence doc, 2026-08-06) |
| Bastrop infra private to Bastrop, controlled via Smart Site, blocked to public | B | Already ruled: city view equals city own sensors plus public layer; landlord data never feeds a city dashboard. Requires S-1 |
| Plan Review deposits adjudications as atoms | C to spine | The earning loop. Structural commitment 2 depends on it |

## PLAN OF RECORD v1 — baseline frozen 2026-08-14

Status legend: **LIVE** = verified at source by the doc_repo planner on the date shown. **DOC** = from an artifact, not re-verified. **OPEN** = not started. **SCOPED** = written, not dispatched.

### Layer 0 — program zero (audit and standards; blocks confident scoping of everything)

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status at baseline |
|----|---|-----------|--------|---------------------|------------|-------------------|
| G-01 | 0 | Doc sweep: 102 root docs are `status: active` with `last_updated` before 2026-07-01; apply the lineage rulings doc 33a already made (47, 33, 40i, 30, 11a) | all lanes | `scripts/doc-staleness.mjs` reports zero active-and-stale in the lane-relevant set; status vocabulary validator passes | none | LIVE 2026-08-14: 102 of 159 stale-active; 16 distinct status strings against 4 legal |
| G-02 | 0 | Status-vocabulary and staleness instrument built (script, then hook) | G-01 durability | Script exits non-zero on an illegal status value or a stale-active lane doc | none | OPEN |
| G-03 | 0 | Code audit: does the workspace and Smart Files substrate exist as `34_smartcity_smart_files_and_foundation.md` open-item 1 claims (substrate built; Smart Files is a rendering pass, not a build) | Lane A sizing | Live read of the workspace atom family in the contract, engine registration, and any serving path | none | OPEN — this row SIZES lane A |
| G-04 | 0 | Memory-system review: `MEMORY.md` contents audited for stale, contradicted, and superseded entries | all lanes | Every memory traced to a live artifact or retired; index matches files | none | OPEN |
| G-05 | 0 | Dev-standards refresh: Cursor rules, `.cursor/settings.json`, AGENT_CONTRACT currency, hook inventory | all lanes | Contract hash current; hooks fire on a deliberate negative test | none | OPEN |
| G-06 | 0 | Dispatch compiler made plan-aware (`--plan` selects OPS-16 or OPS-17) so both programs compile | all lanes | `node scripts/dispatch.mjs --plan OPS-17 --lane X --plan-row G-01` compiles; an invalid row fails closed | none | OPEN — blocks every dispatch in this program |
| G-07 | 0 | **Repo cartography** — classify every one of the ~37 top-level directories and ~4,900 files: what it is, who owns it, active/archive/dead, and explicitly in-scope or out-of-scope for this program. Durable artifact `_catalog/repo_map.md`, not a report. | all lanes | The map exists, every top-level directory appears in it with a classification, and every count carries its counting rule | none | OPEN — G-01 covered root `.md` + `_smartcity_masters/` only; 35 directories and ~4,700 files were never examined |
| G-08 | 0 | **Dev-process file** — one hash-versioned document defining how we work, compiled into every dispatch like the contract | all lanes | A fresh agent with no thread context reaches the same operating conclusions as one with it | G-07 | OPEN |
| G-09 | 0 | **Process proving run** — three small deliberately-shaped agents (read-only audit, build-with-PR, forced mid-flight handoff) dispatched against the PROCESS, not lane work; close artifacts compared for structural identity | all lanes | Three closes are structurally comparable; divergence is a process defect, not an agent defect | G-08 | OPEN |
| G-19 | 0 | **Empressa Command Center authoritative docs** — 231 mentions, 116 in `_inbox/`, ZERO authoritative docs; authority split across eight documents with nothing composing them. Mechanical cause: `00c_portfolio_master_map.md`, whose job is enumerating surfaces, does not list it. Create the canonical doc set and add it to the master map. | all lanes (CC is the spine operator console) | A canonical CC doc exists at a numbered slot, `00c_portfolio_master_map.md` lists it, and the eight scattered sources are reconciled or point to it | G-07 map | OPEN — operator-ruled 2026-08-14. Cert View wiring goes to the backlog, not this row |

### Layer 1 — foundation

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status at baseline |
|----|---|-----------|--------|---------------------|------------|-------------------|
| G-10 | 1 | S-6: declare twin node classes and their entityId shapes (human, building, desk, asset, station) | S-6 | Written ruling plus contract types; no shape reconstructed from parts anywhere in lane code | G-03 | OPEN |
| G-11 | 1 | S-1: auth and tenancy leg state established and sequenced (sprint-54) | S-1 | Live probe: a tenant-private atom is refused to an anonymous caller on every surface | none | CLOSED 2026-08-17 as sequencing. Dashboards `00005-m5t` fixture-city tenant-private; MCP `00082-mat` tag g11 whoami + identified get_city_pack. Sprint-54 T2/T3, Cortex default tenant, G-33, G-42 remain OPEN. Close `_inbox/2026-08-17_g11_close.json`. |
| G-12 | 1 | S-2: telemetry plane placement ruled against the ADR-008 and 56 target topology | S-2 | ADR or decision record plus topology check | none | OPEN (named, not decided since 2026-08-06) |
| G-13 | 1 | S-5: consumer contract shape ruled (MCP against atom read against service API) once for all three suppliers | S-5 | Decision record; the B integration plan cites it | none | CLOSED 2026-08-17, re-graded 2026-09-02 — `_decisions/2026-08-17_g13_consumer_contract.md` rules the caller-split contract (agents via MCP, applications via HTTP service-token mount, no shared DSN) and explicitly states "Closes G-13." This row sat OPEN for over two weeks despite the decision existing; found and corrected during the 2026-09-02 Bastrop-cutover reconciliation pass. See A-106. |
| G-14 | 1 | Lane A: Smart Files foundation — artifact store with provenance and freshness stamp on every artifact | A | Every served artifact carries source plus computedAt and servedAt; STALE indicator backdate-tested to prove it fires | G-03, G-10 | OPEN |
| G-15 | 1 | Lane C: cortex-reporting F1 through F7 spine wiring re-verified against live ldt (spec is 2026-07-01) | C | Per-function live probe against the `48_cortex_reporting_plan_review_spec.md` acceptance criteria | none | OUT OF SCOPE FOR BASTROP CUTOVER, 2026-09-02 — this row measures `legacy-design-tools/artifacts/plan-review`, the AEC-cortex-facing architect reporting surface. ADR-023's DOC-5 amendment (ratified 2026-09-02) confirms city plan review — what Bastrop actually consumes — lives in the standalone `plan-review` repo instead, built through Wave 1. Not retired: still a real row for whoever eventually builds out the AEC-cortex reporting surface. Not a Bastrop-cutover blocker. See A-106. |
| G-16 | 1 | Lane C: replace the Cotality APN-resolution dependency in F2 (Cotality is EXTINGUISHED) | C | F2 resolves a parcel with zero Cotality calls | address-to-parcel posture | OUT OF SCOPE FOR BASTROP CUTOVER, 2026-09-02 — same reason as G-15: F2 is a `legacy-design-tools/artifacts/plan-review` function, not the standalone `plan-review` repo Bastrop consumes. Still a real dead-dependency defect in the AEC-cortex surface, just not a Bastrop blocker. See A-106. |
| G-17 | 1 | Lane D: S-4 content-to-actor reference wired (ICC atoms carry sourceActorDid plus book_id plus section_id) | D, S-4 | Meter attributes a reference by hard reference, not heuristic; purge selector resolves from the same field | none | OPEN |
| G-18 | 1 | Lane B: module decomposition seam identified — what in smartcity-os is genuinely modular against monolithic | B | Written inventory of the live codebase against the four category masters | none | OPEN |

### Layer 1.5 — operator-visible (CC-done)

Inserted by A-016. Isolation home added by A-018 (G-58). Command Center is the operator proving ground; G-56 is the Cortex prototype grade, not the product home. Customer-done remains Layer 5 / G-53 / Lane B.

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status |
|----|---|-----------|--------|---------------------|------------|--------|
| G-56 | 1.5 | Lane A: Command Center data room. Folders are nodes. Files are file-shaped atoms (documents with attachments), not every atom. Folder membership is edges (`placed-on`), not identity. PDF travels with the atom. accessPolicy on every read. MCP tools plus CC panel `smart-files`. | A, all lanes (CC is the operator console) | Frozen WDLL `_inbox/2026-08-15_a_wdll_cc_done_l15.md`, graded by live MCP probes and a live CC URL, never a merged PR | G-14 | OPEN |
| G-58 | 1.5 | Lane A: Smart Files isolation. Own repo `empressaioemail-tech/smart-files`, own Neon project, own GCP/Cloud Run service. Rewrite the settled shape. MCP retarget. Cortex application code removed. SmartSite mounts as a consumer. First instance of capability-mount composition. | A, SmartSite | Frozen WDLL `_inbox/2026-08-15_a_wdll_smart_files_isolation.md`, graded by live 404 on cortex-api `/api/smart-files` plus files-service healthz plus DSN-refusal probes | A-017, isolation WDLL approved | CLOSED 2026-08-15 (serving path; G-58b DROP remains OPEN). Close `_inbox/2026-08-15_a_smart_files_isolation_close.json` |
| G-58b | 1.5 | Drop `smart_file_*` on cortex-prod after L26 is quiet. Not during Texas ingest. Not this card's serving-path grade. | A | Named migrate applied on cortex-prod; `information_schema` has zero `smart_file_*`; county ledger still 200 | G-58 item 7, L26 quiet | OPEN |
| G-59 | 1.5 | Smart Files QA rooms: create folder, upload file, share room, org-level list. Vercel QA UI on the files service. Cortex stays unmounted. | A, Smart Files | Frozen WDLL `_inbox/2026-08-15_a_wdll_smart_files_qa_rooms.md`, graded by live create/upload/share/org probes plus a Vercel URL | G-58 | CLOSED 2026-08-15 on serving path (operator browser QA remaining). Close `_inbox/2026-08-15_a_smart_files_qa_rooms_close.json` |
| G-60 | 5 | ICC PoC demo: complete plan review on own Vercel/Neon/GCP plus finished Hauska MCP plus ICC-demo portal on its own host. Not the G-50 SaaS/public-paid half. | C, D, MCP | Frozen WDLL `_inbox/2026-08-16_icc_demo_program_WDLL.md` | A-024, this WDLL approved | CLOSED_ON_DEMO_PATH. A-031 files UI. A-033/A-034 ICC-demo https://icc-demo.vercel.app. WDLL 13 met. Residuals 7/18/19 held with L26. |

### Layer 1 Lane B product line (inserted by A-035)

G-18 CLOSED as as-found inventory 2026-08-17. Product-line overlay `_decisions/2026-08-17_smartcity_product_line_then_bastrop_onboarding.md`. Path amendment `_decisions/2026-08-17_dashboards_ui_then_one_feed.md` (A-052): next is Dashboards UI, then one source at a time; not a Bastrop cutover. G-13 CLOSED 2026-08-17 (`_decisions/2026-08-17_g13_consumer_contract.md`). Serving Bastrop remains no-touch. G-61 housing locked 2026-08-17 at `empressaioemail-tech/smartcity-dashboards` (A-037). G-61 CLOSED 2026-08-17. G-62 CLOSED 2026-08-17. G-63 CLOSED 2026-08-17. G-11 CLOSED 2026-08-17 as city-pack tenancy sequencing (`_inbox/2026-08-17_g11_tenancy_WDLL.md`). G-45 CLOSED 2026-08-17 (`_inbox/2026-08-17_g45_smartsite_staff_map_WDLL.md`). Dashboards staff map is SmartSite. Do not cut live Leaflet. G-64 CLOSED 2026-08-17 (`_inbox/2026-08-17_g64_lane_c_staff_path_WDLL.md`). Dashboards development-services is plan-review-app. G-65 CLOSED 2026-08-17 (`_inbox/2026-08-17_g65_permitflow_kill_WDLL.md`). PermitFlow is dead as a Dashboards product. Do not cut live `/permitflow/*`. G-66 CLOSED 2026-08-17 (`_inbox/2026-08-17_g66_dashboards_ui_WDLL.md`). Dashboards four lead lenses are navigable views on `00008-d55`. G-67 CLOSED. G-68 CLOSED 2026-08-17. G-69 CLOSED 2026-08-17 (Dashboards Review iframe residual). G-70 G-71 G-72 CLOSED 2026-08-17. A-054 Files compose then one feed is closed. A-055: feeds pause; G-73 shell homes next. Do not treat sprint-54 as done. G-33 and G-42 stay OPEN. G-21 honesty stays OPEN. G-24 stays zero.

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status |
|----|---|-----------|--------|---------------------|------------|--------|
| G-61 | 1 | Lane B: Dashboards product template (doc 31 lens family over records; not a clone of live Bastrop wallpaper). First city onboarding is a later named cutover, not this row. | B | Frozen WDLL `_inbox/2026-08-17_g61_dashboards_template_WDLL.md` approved; template mounts spine + SmartSite + Smart Files; live Bastrop unchanged | G-13 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — Dashboards `00002-sxq` compose+packs live-probed; MCP `00076-veq` tools live-probed; city `00118-qox` unchanged. Close `_inbox/2026-08-17_g61_close.json`. Cutover is a later WDLL. |
| G-62 | 1 | Lane B: unauthenticated Dashboards compose atom-reads as anonymous (same accessPolicy as MCP). Residue of G-61, not G-11. | B | Frozen WDLL `_inbox/2026-08-17_g62_compose_honesty_WDLL.md` approved; live unauth compose on `48021:34137` omits `owner-fact`; service Bearer is not a paid subject; live Bastrop unchanged | G-61 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — Dashboards `00003-jc2` live-probed omit owner-fact; MCP `00078-xuv` tag g62 `dashboards_compose_city_manager` live-probed; city `00118-qox` unchanged. Close `_inbox/2026-08-17_g62_close.json`. |
| G-63 | 1 | Lane B: feed adapter contract (kinds write records onto spine or files; not a product; not a copied table). Catalog only. No live vendor ingest. | B | Frozen WDLL `_inbox/2026-08-17_g63_feed_adapter_contract_WDLL.md` approved; live adapter-kinds catalog; template-city grantedAdapters []; live Bastrop unchanged | G-62 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — Dashboards `00004-zsq` kinds live-probed; MCP `00080-voc` tag g63 `dashboards_list_adapter_kinds` live-probed; city `00118-qox` unchanged. Close `_inbox/2026-08-17_g63_close.json`. |
| G-64 | 1 | Lane B: Dashboards development-services mounts Lane C plan-review-app as the staff reviewer. Not G-51 re-grade. Not G-52. | B | Frozen WDLL `_inbox/2026-08-17_g64_lane_c_staff_path_WDLL.md` approved; live GET `/?lens=development-services` iframe is plan-review-app; GET `/` still G-45 SmartSite; live Bastrop unchanged | G-45 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — Dashboards `00007-8sc` GET `/?lens=development-services` iframe `https://plan-review-app-ten.vercel.app/`; GET `/` still 908 PINE; MCP `00082-mat` tag g11 `data.planReview.url` match; city `00118-qox` unchanged. Close `_inbox/2026-08-17_g64_close.json`. |
| G-65 | 1 | Lane B: PermitFlow killed as a product on Dashboards. Live city `/permitflow/*` uncut until Bastrop cutover. | B | Frozen WDLL `_inbox/2026-08-17_g65_permitflow_kill_WDLL.md` approved; served Dashboards zero `permitflow`; city bundle still has PermitFlow; zero city deploys | G-64 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — Dashboards `00007-8sc` GET `/app.js` permitflow=0; GET `/permitflow` 404; city bundle permitflow=160 and `/permitflow/review` 200. No product PR (kill already true after G-64). Close `_inbox/2026-08-17_g65_close.json`. |
| G-66 | 1 | Lane B: Dashboards product UI on template-city. Four lead lenses as navigable views. Compass sheet chrome. Not a feed. Not Bastrop cutover. | B | Frozen WDLL `_inbox/2026-08-17_g66_dashboards_ui_WDLL.md` approved; live navigation of city-manager / development-services / finance / citizen without a hidden parcel form; live Bastrop unchanged | G-67 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — Dashboards `00008-d55` GET `/` `/?lens=finance` `/?lens=citizen` 200 badge Demo; GET `/compass` 404; PR **#8** squash `4ad3ba44` CI success run 32079207831; city `00118-qox` unchanged. Close `_inbox/2026-08-17_b_g66_close.json`. |
| G-67 | 1 | Lane B: design-system kit copy. Same `--sc-` file in smartcity-dashboards, smart-files, plan-review. No UI rewrite. | B, A, C | Frozen WDLL `_inbox/2026-08-17_g67_kit_copy_WDLL.md` approved; three token files byte-identical to `_inbox/2026-08-17_sc_kit.css`; live Bastrop unchanged | G-65 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — dashboards #7 `f7395e54` CI success; files #1 `53b22236`; plan-review #1 `a5f47e10`. Hash `2CDD0BF8…113EF2`. Close `_inbox/2026-08-17_b_g67_close.json`. |
| G-68 | 1 | Lane A: Smart Files product UI. File browser, access rail, Bring files chrome. Not OAuth. Not G-53. | A | Frozen WDLL `_inbox/2026-08-17_g68_smart_files_ui_WDLL.md` approved; serving Files QA is the kit browser; write API still works; Cortex unmounted | G-67 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — `smart-files-app` `dpl_3KjVHdKS1s7Rcpj3hehDvEJmqKqM`; PR **#2** squash `569bbdbd`; create 201 share 201; cortex 404. Close `_inbox/2026-08-17_a_g68_close.json`. |
| G-69 | 1 | Lane C: Plan Review product UI. Kit queue and console. Documents from Smart Files. Dashboards Review compose. | C, B | Frozen WDLL `_inbox/2026-08-17_g69_plan_review_ui_WDLL.md` approved; serving plan-review-app is kit chrome; no persona-gate first paint on Dashboards Review | G-67 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — `plan-review-app-ten` `dpl_CKg13X2su89rQYjore9VevtfDfP9`; PR **#2** squash `4330ac85`; iframe residual on Dashboards Review. Close `_inbox/2026-08-17_c_g69_close.json`. |
| G-70 | 1 | Lane B: Dashboards Work → Files mounts serving Smart Files. Not a browser rewrite. | B | Frozen WDLL `_inbox/2026-08-17_g70_dashboards_files_compose_WDLL.md` approved; live GET `/?work=files` host is smart-files-app; live Bastrop unchanged | G-68 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — Dashboards `00009-vpl`; PR **#9** squash `d2fc9525`; live compose `smartFiles` `https://smart-files-app.vercel.app/?embed=1`. Close `_inbox/2026-08-17_b_g70_close.json`. |
| G-71 | 1 | Lane B: first municode calendar feed on template-city. Writes records to files. Not MyGov. Not spine atoms. | B | Frozen WDLL `_inbox/2026-08-17_g71_municode_calendar_feed_WDLL.md` approved; grant on template-city; one public meeting record on files; Overview meetings reads it; live Bastrop unchanged | G-63 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — Dashboards `00010-vbs`; PR **#10** squash `570556c3`; compose meetings 5 records; files `00005-fdr` folder `template-city:public-meetings`. Close `_inbox/2026-08-17_b_g71_close.json`. |
| G-72 | 1 | Lane A: Smart Files embed chrome. Hide fixture panel and nested header when iframed. | A | Frozen WDLL `_inbox/2026-08-17_g72_files_embed_chrome_WDLL.md` approved; `?embed=1` or iframe hides `#fixture-panel`; write API still works | G-68 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — `smart-files-app` `dpl_8WCADkEhKByhLa6QSKEfUCbUZ3DZ`; PR **#3** squash `b73bcc6f`; create 201; cortex 404. Close `_inbox/2026-08-17_a_g72_close.json`. |
| G-73 | 1 | Lane B: Dashboards shell homes. Every G-18 / live-Bastrop staff function has a named home. No new feed. | B | Frozen WDLL `_inbox/2026-08-17_g73_shell_homes_WDLL.md`; live nav has Parks, Records search, Assets, Connections, People; Connections lists the register; live Bastrop unchanged | G-70 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — serving `00011-nzs`. PR #11 squash `6bad594`. Live Connections 67 of 67. City `00118-qox` unchanged. |
| G-74 | 1 | Lane B: template-city identity leak. Pull Bastrop meetings, Chestnut Street, and Bastrop-named Connections copy off the demo. | B | Frozen WDLL `_inbox/2026-08-17_g74_identity_leak_WDLL.md`; live compose meetings.records=[]; Citizen has no Chestnut; Connections HTML has zero Bastrop; live Bastrop unchanged | G-73 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — serving `00012-9dk`. PR #12 squash `00ea669`. Live compose meetings empty, no grant. City `00118-qox` unchanged. Close `_inbox/2026-08-17_b_g74_close.json`. |
| G-75 | 1 | Lane B: demo-city chrome. Fill mounts, Compass-class map motion from current rails, remaining 30c screens honest-empty. | B | Frozen WDLL `_inbox/2026-08-17_g75_shell_mounts_motion_WDLL.md`; live map iframe taller than 220px; Plan Review and Files pass embed=1; 67 of 67 plus 3 addenda; G-74 identity holds; live Bastrop unchanged | G-74 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — serving `00013-vkl`. PR #13 squash `34b307b`. Live map 378x605. City `00118-qox` unchanged. Close `_inbox/2026-08-17_b_g75_close.json`. |

### Layer 1 Wave 1 integration (inserted A-085)

Govtech seat Wave 1 on `template-city`. Scope rev 3: `_inbox/2026-08-24_govtech_program_scope.md`. Live board: `canvases/govtech-master-program.canvas.tsx`. Transaction contract filed (S5-1). Merged ≠ live until deploy gates pass live violation probes.

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status at baseline |
|----|---|-----------|--------|---------------------|------------|-------------------|
| G-105 | 1 | **Deploy cut + live violation probes.** Deploy plan-review #7 Cloud Run + Vercel together; dashboards #39; MCP #75; engine-api #361. Each gate runs a deliberate violation probe on the deployed revision. | B, C, D, MCP | Per-PR live probe fails on known pre-fix behavior; passes on serving revision | none | OPEN — merged PRs armed-inert |
| G-106 | 1 | **Smart Files read-path scope.** Close defect #3: scope enforced on folder/file/blob read, not write-only. | A | Anonymous or wrong-tenant read of scoped artifact returns 403; violation probe on deployed files service | G-105 (dashboards #39 for paired BFF posture) | CLOSED 2026-09-02 — smart-files PR #9 merged, deployed `smart-files-00008-geb` @100%. Live-verified pre- and post-traffic-shift: anonymous 403, wrong-tenant 403, matching-tenant 200; legacy production callers unaffected (blanket-grant token preserved by design). Close `_inbox/2026-09-02_a_close.json`. See A-091. |
| G-107 | 1 | **Staff upload into Smart Files.** Wave 1 intake (R-B). Submittal lands in city-scoped folder before plan review runs. | A, C | Live POST upload on `template-city` returns artifact id; plan review can attach it | G-106, S1-17 tenant ruling, DOC-1 contract | OPEN |
| G-108 | 1 | **Plan review honesty path.** Edition selector, typed absence on code path, applicability matrix, code-lookup refuse deployed (#7). | C | Determination on declared edition cites real section or typed absence; neighbour fallback probe fails | G-105 DEPLOY-7, DOC-1, S2-1 migration or scoped interim | CLOSED 2026-09-02 — plan-review PR #10 merged, 57/57 tests. Items 8/9/10 re-graded MET now that G-107 landed: live end-to-end walk on `48021:34737` (real production engagement, not a test) declared edition IRC-2018, `proposedSetbackFrontFt=30` produced `14-02-003: Pass`, a second engagement with `proposedSetbackFrontFt=15` produced `14-02-003: Fail` — both with genuine analysis text and non-empty `editionId`/structured citations, not hardcoded. Typed `SOURCE_UNAVAILABLE` still holds from the original PR. See A-095. |
| G-109 | 1 | **ICC obligation ledger chain.** Migration 009 applied probe; meter bypass deployed; cited atom recorded; `source_obligation_ledger` reader (R-I). | D, MCP | Accrual row in obligation ledger for a live ICC reference probe; activity reconciles | G-105 DEPLOY-75/361, property S4-6 backfill | PARTIAL 2026-09-02 — govtech half (S4-1b, S4-8) merged, plan-review PR #8; prod migration/deploy held; substrate half (S4-3, S4-7) still OPEN and now the blocker. See A-090. |
| G-110 | 1 | **Wave 1 E2E transaction proof on template-city.** One submittal through Smart Files, review with declared edition, honest absence, accrual in obligation ledger, one shell with standalone SKUs. | all | Frozen acceptance chain S5-5 in scope rev 3; graded item by item on deployed surfaces | G-107, G-108, G-109, G-113, G-114 | CLOSED 2026-09-02 — one coherent live walk on `template-city`, engagement `3463b738`: (1) real staff upload with full provenance, artifact `smartfile:tenant:template-city:3463b738...--site-plan`; (2) IRC-2018 declared + a real proposed dimension; (3) matrix shows a genuine `Pass` (Bastrop UDC, structured citation) AND typed `Unchecked` absence (IRC rows) in the same run; (4) a `source_obligation_ledger` row for this specific session's own `request_id`s was NOT produced — this walk's own ICC-touching rows are typed absence, which never calls the MCP gate (confirmed by direct query). The accrual mechanism itself is separately, genuinely proven (A-102, same day); the gap is precisely named as no real ICC section content existing anywhere in the system yet to cite, not a mechanism or wiring defect; (5) Dashboards' embed correctly resolves to this exact engagement's service, structurally incapable of diverging since it has no separate computation of its own. **Operator-ruled 2026-09-02: residual named is fine — closed with item 4's gap standing as content-acquisition scope, not build scope.** See A-103, A-104. |
| G-113 | 1 | **Seam vocabulary conformance (WDLL item 13).** Fix the `verified-absent`/`absent-verified` spelling divergence (deliberately deferred to here since G-108, canvas item L3) and add a durable CI grep gate so `citation.mjs`'s `buildCitation()` stays the sole citation-minting path. Rescoped 2026-09-02 from the WDLL's literal text after checking source: the spelling divergence and the citation-minting logic both live ONLY in `plan-review` today — grepped `smart-files` and `smartcity-dashboards`, zero hits on either spelling, zero citation-shaped-object construction in either repo. Vendoring a validator into repos that never build citations is not in scope. | C | Zero occurrences of `verified-absent` in plan-review; a CI check fails when a hand-authored citation string (e.g. matching `/Section \d/` outside `citation.mjs`) is introduced, verified by violation before trusting it | none (single-repo, no cross-repo dependency found) | CLOSED 2026-09-02 — plan-review PR #12 merged, 64/64 tests. Spelling fixed. Re-confirmed single-repo scope with a fresh independent grep, not trusted from the dispatch. Found and fixed a real residual: two hand-authored citation strings sat dead-but-live in the `BASTROP-UDC` manifest since G-108 (unread, not fixed). CI gate is three layers: textual, structural, and a meaning-shaped check (shipped citation vs. one rebuilt fresh from the same row's fields). See A-100. |
| G-114 | 1 | **Presentation convergence (WDLL item 14).** Dashboards mounts Plan Review and Smart Files via iframe with NO city-scoping parameter in the embed URL — confirmed 2026-09-02 by reading `mounts.mjs`: `planReviewEmbedUrl()`/`smartFilesEmbedUrl()` return a static origin from env vars plus `?embed=1`, with no `cityKey` threaded through, and `compose.mjs` calls them with no city argument either. As Dashboards onboards more than one city this becomes a real scope trap, not a hypothetical. Each product must still deploy and run standalone (R-F, G-13). | B | Live probe: the embed URL carries the composing request's own `cityKey`, not just a static origin; `$PLAN_REVIEW_URL`/`$SMART_FILES_URL` each serve core flows with no Dashboards origin required; close names the three independent SKUs (`SCOS-PLAN-DEP`, `SCOS-FILE-DEP`, `SCOS-DASH-DEP`) | G-107, G-112 | CLOSED 2026-09-02 — dashboards PR #40, plan-review PR #13, smart-files PR #11, all merged and deployed. Live-verified end to end: fetched the actual deployed app.js/index.html from both Vercel production URLs and ran them in jsdom with the exact URL the live compose response produces — both resolve to `template-city`, not just a passing isolated test. Cross-city differentiation proven at unit level (no second city pack registered in production). All three SKUs confirmed independent, dependency direction unchanged from G-13. See A-101. |
| G-112 | 1 | **Wire plan-review's ledger reconciliation to the live substrate reader.** `fetchLedgerRows()` in `src/ledger-reconciliation.mjs` deliberately returns `null` pending exactly this. Capture `x-hauska-request-id` from `mcp.mjs`'s `mcpCall`, thread it into the `recordActivity()` call sites currently passing `requestId: null`, and call G-111's `GET /obligations/source-ledger` reader with the minted `platform_internal` key (stored, `plan-review-platform-key`). Surfaced 2026-09-02 once G-111 unblocked it (A-095). | D | Live probe: a real ICC-citing call records a `request_id` on `plan_review_activity` matching the ledger row's own; `reconcileActivity()` returns a genuine `matched` (or `divergent`) result against live data, not `ledger-unavailable` | G-111 (CLOSED, live, A-094) | CLOSED PARTIAL 2026-09-02 — plan-review PR #11 merged, deployed `plan-review-00021-huc` @100%. Mechanism live-verified: real, distinct `request_id`s confirmed landing on production `plan_review_activity` rows post-deploy (direct SQL). `fetchLedgerRows()` wired to G-111's reader. A genuine `matched`/`divergent` result NOT produced — blocked by the same codex-tier-key gap as DEPLOY-75's residual (A-088), confirmed empirically not assumed. See A-098. |
| G-111 | 1 | **Substrate half of the ICC obligation ledger chain (S4-3, S4-7).** `hauska-mcp-server` echoes the minted `source_obligation_ledger.request_id` back to the caller (or accepts a caller-supplied correlation id), and gains an MCP tool/endpoint that reads `source_obligation_ledger`. Surfaced 2026-09-02 by two independent govtech sessions building G-109's govtech half; request filed at `_inbox/2026-09-02_govtech_to_substrate_ledger_asks.md`. | D, MCP, substrate | Live probe: a Codex-gate tool call records a `request_id` the caller can read back on the response; a reader tool/endpoint returns ledger rows for a given `request_id` or `source_actor_did` | G-109 (govtech half, closed A-090) | OPEN |

### Layer 2 — measurement

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status at baseline |
|----|---|-----------|--------|---------------------|------------|-------------------|
| G-20 | 2 | Lane A: coverage of the real Bastrop document corpus counted, with counting rule | A | Count plus rule published; no bare ratio | G-14 | OPEN |
| G-21 | 2 | Lane B: which SmartCity modules actually consume the spine against mock or vendor-passthrough | B | Per-module live probe; honest inventory including UI-only surfaces | G-18 | OPEN — catalog currently oversells (payments, citizen portal UI-only) |
| G-22 | 2 | Lane C: determinations cite atom IDs and confidence objects, no bare scalars | C | F3 and F7 acceptance criteria pass on live data | G-15 | OUT OF SCOPE FOR BASTROP CUTOVER, 2026-09-02 — same reason as G-15 (F3/F7 are `legacy-design-tools/artifacts/plan-review` functions). The underlying intent (structured citations, no bare scalars) is independently met in the standalone `plan-review` repo via G-108/G-113 (citations carry `editionId`/`bookId`/`sectionNumber`, CI-gated against hand-authored strings) — cited as evidence the intent is satisfied where it matters for Bastrop, not as a grade on this row, which stays about the other surface. See A-106. |
| G-23 | 2 | Lane D: per-reference rate set; ledger shows real accruals, not pending-rate nulls | D | Ledger query returns a non-null accrual for a live reference | G-17 | OPEN — G-60 WDLL 20 met on plan-review activity table (`rate=0.01`, UI + MCP sources). Hauska inbound meter on existing ICC atoms still waits G-30 UPDATE |
| G-24 | 2 | Lane B: Asset Management Tier 1 — first city-owned assets ingested into the graph | B | Store query: count of city-owned asset nodes greater than zero, with provenance | G-10, G-18 | LIVE 2026-08-10 per doc 32: ZERO city assets ingested |

### Layer 3 — integrity

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status at baseline |
|----|---|-----------|--------|---------------------|------------|-------------------|
| G-30 | 3 | Lane D: ICC accessPolicy stamped platform-internal at source; ingest hardcode removed | D | Store query: zero ICC atoms with public-free; anonymous list_jurisdictions omits icc-model-code | none | LIVE 2026-08-16: ingest hardcode REMOVED on engine main PR **#346** squash `ebe6d63` (CI SUCCESS). Store still public-free until bounded UPDATE. L26 holds `--apply` |
| G-31 | 3 | Absence taxonomy adopted by Lane C and Lane D for code-section absence | C, D | Schema carries typed absence; a probe failure cannot render as a data gap | G-15 | OUT OF SCOPE FOR BASTROP CUTOVER, 2026-09-02 — same reason as G-15. The underlying intent is independently met in the standalone `plan-review` repo (G-108/G-113: `UNCHECKED`/`ABSENT_VERIFIED`/`SOURCE_UNAVAILABLE`/`NOT_ENTITLED` taxonomy, CI-gated). Cited as evidence, not a grade on this row. See A-106. |
| G-32 | 3 | Vintage and edition identity on the code corpus (Bastrop B3-to-BDC class; ICC editions) | C, D | Declared vintage per jurisdiction; fallbacks named, counted, marked on read | none | OPEN |
| G-33 | 3 | Lane B: Bastrop infra private to Bastrop, blocked to public, controlled via Smart Site | B, S-1 | Live probe: anonymous caller refused; Bastrop-authenticated caller served; landlord telemetry absent from the city view | G-11 | OPEN |
| G-34 | 3 | Lane A: typed absence plus proven STALE indicator on the artifact store | A | Backdate test fires the indicator; absence carries basis | G-14 | OPEN |

### Layer 4 — depth

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status at baseline |
|----|---|-----------|--------|---------------------|------------|-------------------|
| G-40 | 4 | Lane C: Bastrop UDC and adopted-code depth in the review flow | C | F6 navigable at section granularity for the Bastrop own code | G-15 | OPEN |
| G-41 | 4 | Lane D: IPMC 2018 zero-sections resolved or ruled out of scope | D | Sections ingested, or a written ruling with ICC correspondence | ICC entitlement | LIVE: 0 sections, empty body upstream |
| G-42 | 4 | Lane B: Bastrop-specific lenses per the Dashboards master (lens family by audience plus permission) | B | Per-lens live probe with permission enforcement | G-11, G-21 | OPEN |
| G-43 | 4 | Lane B: infrastructure twins (SCADA ask) — station as smart site, bearing watch as watch contract | B | Watch contract instantiated on a station node; read-only boundary enforced | G-12, G-24, G-33 | SCOPED: `_inbox/2026-08-02_bastrop_scada_infrastructure_intelligence_ask.md` |
| G-44 | 4 | Lane A: the real Bastrop documents captured (CAPTURE verb, Factory 1.5 staging) | A | Staged with provenance; drain to store; count with rule | G-14, G-20 | OPEN |
| G-45 | 4 | S-3: Smart Site mapping adopted by SmartCity; Bastrop mapping brought to standard | B, S-3 | Bastrop map renders on the Smart Site mapping system, verified in the deployed bundle | none | CLOSED 2026-08-17 — Dashboards `00006-vfk` GET `/` auto-loads gold `48021:34137` on `smartsite.cloud` (908 PINE). MCP `00082-mat` tag g11 unchanged. City `00118-qox` Leaflet uncut. Close `_inbox/2026-08-17_g45_close.json`. Live Leaflet cutover remains a later named WDLL. |

### Layer 5 — launch

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status at baseline |
|----|---|-----------|--------|---------------------|------------|-------------------|
| G-146 | 5 | Lane B: **FINISH THE DESIGN WORK — the completion gate, and the FIRST card.** Operator ruling 2026-09-15: finishing design comes first, ahead of the order in `_inbox/2026-09-15_roadmap_reconciliation.md` section 4. Bastrop approves the design before we build, so every build row behind it is waiting on this. **REGRADED 2026-09-18 (A-152): this row is the denominator every design build is judged against, and the counts it carried were frozen at A-137 while three later amendments recorded different ones.** Stated 2026-09-15, counted at source then: 11 design folders, 48 boards, 96 exported images (48 dark + 48 light), `_kit.css` 1 unique md5 of 12 copies; against nav `f776b4bf` (`ALL_LENS_IDS` nine, `WORK_IDS` six): 15 nav surfaces, 5 designed, 2 excluded by ruling, 8 uncovered. **MEASURED 2026-09-18T18:57Z by `_inbox/2026-09-18_planner_g146_recon.mjs` against `doc_repo` `3243a171` and nav `7487d7c0`: 20 design folders (26 subdirs less 6 non-design dirs: `all-canvas`, `bastrop-review`, `demo`, `exports`, `handoffs`, `report-chrome`), 72 boards declared across 17 surfaces, `_kit.css` 1 unique md5 of 21 copies, 108 exported images of which 48 dark and 48 light, and 14 of 20 folders carrying a `check.mjs` with 7 carrying a `violate.mjs`.** Against nav `7487d7c0`: **15 nav surfaces, 13 designed, 2 excluded by ruling, 0 uncovered.** Design is 13 of 15 by surface, not a third, which inverts this row's own justification for being the front of the queue: design is nearly finished and the front of the queue is now the instruments. The one number here that is a meaning-shaped check rather than a single derivation is the instrument count, and the gate and the recon instrument agree on it independently at 14. The export figure moved least and least usefully: the boards went 48 to 72 while dark and light stayed at 48 each, so the export set did not grow with the design set. Order, each already source-checked: G-138 filings AMEND, G-143 People and access, G-142 Citizen, G-145 the five lenses, G-147 the long tail, G-148 the missing instruments; then the operator-owed ratify/amend/kill on three DRAFTs and the choice of which of three plan review designs a city is shown. **The canvas the designs are reviewed on still does not contain them all, but the omissions have MOVED.** The two instances this row named are FIXED: `_design/all-canvas/build.mjs` now declares `smart-files` (5 boards) and `plan-review-departments` (5 boards), 72 boards across 17 surfaces, up from 38 across 10. **Three designs that exist on disk are now declared nowhere on it: `smartcity-citizen-lens`, `smartcity-people-and-access` and `smartcity-records-search`** — the three newest, all drawn 2026-09-18 under G-142, G-143 and G-147. This is a stale declaration list rather than a policy against DRAFTs: the canvas includes DRAFTs deliberately and places the applicant precheck first for being one. It bites hardest on People and access, which is the DRAFT awaiting the operator's ratify/amend/kill, so the design the operator is being asked to ratify cannot be reached on the combined canvas. The undeclared copies sit in the folder, which is why the omission is invisible from a file listing. Carries the loose end from the design thread: the vocabulary defect in `_design/plan-review/gen.mjs` is still translated at build time by the review-artefact build script rather than fixed at source. **The `_design/INDEX.md` disagreement this row carried is RESOLVED and is no longer work:** the index records the Plan Review console as SUPERSEDED 2026-09-15 by the reasoner path and the reasoner path as *"the plan review design a city is shown"*, which is the addendum's reading (A-138 ruling 2). **The operator's ratify/amend/kill has also moved off its original three** — the Finance lens, Smart Files and the Development services lens are all RATIFIED 2026-09-17 — **and now sits on three DRAFTs: Citizen lens, People and access and Records search.** | B | `node scripts/govtech/design-completion-gate.mjs` exits 0. It reads THREE independently derived inputs — the product's own nav constants at a named ref, the `_design/` folder inventory on disk, and the declarations in `_design/INDEX.md` and `_design/surface_coverage.json` — so a wrong declaration fails against the filesystem and a stale folder fails against the nav, and no single party can satisfy every half. It is a refusal, not a report: it exits 1 while design is unfinished. Built and run 2026-09-15: 24/24 self-tests both directions including a fixture that PASSES, 15 nav surfaces matched, verdict UNFINISHED, 13 findings, exit 1 | Nothing. This is the front of the queue | OPEN — carded 2026-09-15, FIRST. **REGRADED 2026-09-18 (A-152). THIS ROW DOES NOT CLOSE WHEN G-148's FOUR INSTRUMENTS LAND** — R3 emptying is necessary and not sufficient, because the all-canvas declaration still omits three designs and the vocabulary loose end is still Owed. |
| G-147 | 5 | Lane B: **the three designed-nowhere surfaces that had no row at all.** **Records search** — a nav surface (`work:records`), ships as a stub with a `Not built` badge; document search is a named island until a lens is designed for it. **Compass** — doc 34 calls it a sidebar over readable records, the v1 chatbot still runs, and the design thread's own advice was to hold it until more lenses exist, which G-142 and G-145 satisfy. **The applicant-facing plan review view** — a persona exists in the product and the view does not, and it is the only surface on this list a non-employee would ever see. Shell chrome is NOT in this row: `G-90` already shipped eight shell functions (PR #26, merged), so what is missing there is a design pass over a set that was built function by function, and that is a distinct question from these three. | B | Per surface, an adversarial read run as a file, self-testing in both directions with a non-zero matched-input count, against the shipped surface at a named ref; and the design-completion gate stops reporting each one as uncovered | G-142 and G-145 for Compass only, which needs lenses to be a sidebar over |**CLOSED-PARTIAL 2026-09-18** — `_inbox/2026-09-18_g147-record-search_close.json`, CP1 and CP2 filed, lane released. Two of three parcels PASS and the third is deferred by the dispatch's own ruling, which is the only reason this is not `closed`; a `closed` would assert a PASS for a parcel the lane was forbidden to draw. **Verified by the planner rather than taken on report:** `node _design/smartcity-records-search/check.mjs` exits 0 with all 18 predicate counts non-zero; `violate.mjs` catches 24 of 24 plants with boards restored; and the `records` block in `_design/surface_coverage.json` flipped from `{folder: null, planRow: G-147}` to `{folder: smartcity-records-search}`. **Parcel 1, Records search: PASS.** `_design/smartcity-records-search/`, DRAFT, drawn against `smartcity-dashboards` `origin/main` `7487d7c0`. Parcel 2, the applicant-facing view: adjudicated COVERED by the RATIFIED `_design/smartcity-applicant-precheck/` and not redrawn, with the residual gap named as buildability rather than coverage. **Parcel 3, Compass: DEFERRED and not drawn**, on the dispatch's instruction that a sidebar must not be drawn before the surface it sidebars over exists; G-142 landed mid-execution, which is the deferral's own justification observed rather than argued. **The deferral goes back to this row and is not a silent drop.** **Two disclosures a reader should not have to find:** no seat worktree was created (the session was rooted in `P:\doc_repo`, against the dispatch's preference, and the close says so); and the probe artifact is an INSTRUMENT probe, not a `scripts/surface-probe.mjs` deployed-surface artifact, because Records search is not built and no deployed surface is claimed|
| G-148 | 5 | Lane B: **five designs are past DRAFT and carry no adversarial read, and two of them are already dispatched to build.** The rule that no design is shown or ratified without an instrument was earned on 2026-09-15 and **was never applied backwards**, which is the same shape as every other control in this repo that was correct and enforced nothing. Measured by `design-completion-gate.mjs` R3: `smartcity-overview-lens` RATIFIED and dispatched **G-120**; `smartcity-map-dock` APPROVED and dispatched **G-128**; `smartcity-flood-study` RATIFIED; `plan-review-reasoner` RATIFIED; `plan-review` IN REVIEW. Three folders have one and eleven exist, so 3 of 11. The two dispatched ones matter most: a build is already running against a design nobody checked against source, and the seven defects this thread caught were every one of them invisible to a re-read of the canvas. A DRAFT is legitimately exempt and stays exempt; `smartcity-finance-filings` is covered by G-138 instead. | B | `node scripts/govtech/design-completion-gate.mjs` reports zero R3 findings. Each new `check.mjs` self-tests in both directions, aborts rather than returning a worthless verdict, reports a NON-ZERO matched-input count, and is verified by violation against a real artboard — the Smart Files precedent, where a predicate self-tested perfectly and matched nothing on any board | Nothing. Every one of the five is already drawn; this is instrumenting what exists, not drawing | CLOSED 2026-09-18, verified by the planner at source. The done-condition is MET: `design-completion-gate.mjs` reports zero R3 findings and exits 0; all four `check.mjs` self-test in both directions (48/48, 45/45, 47/47, 57/57) with non-zero matched-input counts; and all four are verified by violation (17/17, 30/30, 21/21, 41/41). Close `_inbox/2026-09-18_g148-design-instruments_close.json`, lane claim released. **LEAVE_BEHIND, AND IT NEEDS AN OWNER: 11 findings across the four designs, reported by the instruments and deliberately NOT fixed, because this dispatch boundary was to instrument what exists and not to redraw it.** The planner recounted them from each instrument own total line at source (4 + 1 + 4 + 2 = 11) rather than from the close prose: `plan-review-departments` 4, `smartcity-flood-study` 1, `smartcity-map-dock` 4, `smartcity-overview-lens` 2. They are real board defects, mostly stale NEGATIVES (a claim that the product does not have something it now has), so a re-read of the canvas would not have caught them, and three were true when the board was drawn. **No row owns them** (A-156, A-157). The lane also left the surface-probe redirect as `_inbox/2026-09-18_g148_surface_probe.log.txt` in UTF-16, a PowerShell artifact, excluded from the commit. |
| G-149 | 5 | Lane B: **BUILD THE RATIFIED FLOOD STUDY DESIGN into the Development services tab.** Not the mount, which G-129 already deployed and which serves today on `smartcity-dashboards-00074-sil` tag `g129-flood`. This is the five artboards in `_design/smartcity-flood-study/`: the tab becomes a SCREENING LIST answering which permits in flight sit on parcels that pond, rather than a per-parcel viewer; the regulatory FEMA zone and the modeled drainage study render as separate cards with separate authority and separate vintage, the model badged `NOT A DETERMINATION`, and a Zone X parcel that still models ponding is never drawn as a contradiction; depth is a control that re-ranks the whole list rather than one report; running is a real state that survives navigation, and a timed-out parcel stays RETRYABLE rather than being written down as no ponding, because an engine that did not answer and a parcel that does not pond are different results; and the G-130 citation refusal is enforced on the page, not only in a decision record. **Two things are folded in rather than left orphaned.** (1) The design's own `check.mjs`, which does not exist today and is an R3 finding on `design-completion-gate.mjs`; **being delivered by G-148's lane `g148-design-instruments` (hold released 2026-09-18, `_decisions/2026-09-18_g148_hold_released.md`), which this row INHERITS rather than rebuilds**; it self-tests in both directions, aborts rather than returning a verdict it cannot support, reports a NON-ZERO matched-input count, and is verified by violation against a real artboard. (2) **G-125's unowned leave-behind, which undercuts this row's central control:** NOAA's Atlas 14 HDSC endpoint changed payload shape, `parsePfdsDepthTable` never matches, and every no-parameter study in every county silently uses Bastrop's 9.5in regional default regardless of location. A depth control drawn on top of a silently-wrong default is a fabricated basis the moment a second city exists, and it is the absent-versus-default collapse `ENFORCEMENT.md` prohibits. Repo `smartcity-dashboards`. | B | The five states render at their own widths on a pack with no boundary on file as well as on `bastrop_tx`; a timed-out parcel is provably retryable and is NEVER recorded as no ponding; citing the study in a review letter is REFUSED on the page with its G-130 reason; `node _design/smartcity-flood-study/check.mjs` exits non-zero on a planted violation and zero on the real boards, with a non-zero matched-input count; the NOAA parser resolves `rainfallSource: 'noaa-atlas14'` against the live endpoint for a non-Bastrop county, or refuses rather than defaulting; and the built surface is reached on the DO app per D-12, verified by edge header from a vantage point outside this fleet host, never by a merge | **D-12** (OPS-25). Operator ruling 2026-09-17: all SmartCity surfaces move to DigitalOcean before design build work, so this row's done-condition is the DO app at the post-D-12 commit, moved by the operator. A merge to `smartcity-dashboards` main ships nothing while deploy-on-push stays disabled, and until D-12 repoints the app off `d9-pin-96efa35` a merge does not even reach the build source | OPEN — carded 2026-09-17 (A-141). INSTRUMENT INHERITED FROM G-148 (A-159), now DELIVERED, do not build a second one: `_design/smartcity-flood-study/check.mjs` (45 self-tests both directions, 10 matched-input counters all non-zero) with `violate.mjs` planting 30 named violations, every plant caught on a real artboard, and its facts derived by `dump-source-facts.mjs` into `source-facts.json` from `P:/hauska-map` origin/main `163fde32` and the G-130 ruling. The shipped boards fail on one real defect, the stale rainfall-atlas claim, carded as G-164. |
| G-150 | 5 | Lane B: **BUILD THE RATIFIED REASONER PATH, the plan review design a city is shown.** `_design/plan-review-reasoner/`, RATIFIED 2026-09-15 and ruled the shown design the same day (`_decisions/2026-09-15_plan_review_reasoner_is_the_shown_design.md`). Five artboards: the console, one finding's derivation, coverage, the correction notice, and cycle 2. Companion product, not a plan review system: no markup, no batch stamping, no measurement tools, no document workflow. **The design's own corrections are acceptance items, not notes**, because each is a defect that shipped once already in the draft: every rule other than the front setback badges `NO ADJUDICATOR`, since exactly one adjudicator exists in source (`adjudication.mjs`, `adjudicateMinimumSetback`); permitted use renders `Unchecked` rather than Pass where source sets `adjudicated: null`; `Uncertain` is reachable only through a named reviewer override with a written reason and is never machine-derived; no invented section numbers (`14-02-005` does not exist and was cited six times in the draft, once inside an issued letter); the book is "City of Bastrop Building Block B3" and not "Bastrop Development Code"; and no code is paraphrased where `quotable === false`. Delivers `_design/plan-review-reasoner/check.mjs`, an R3 finding today. Repo `plan-review`, which is NOT in OPS-25's six services, is outside the DO-to-GitHub connection scope, and runs in its own GCP project `plan-review-505715`. | B | Every rule without a code path badges `NO ADJUDICATOR` and the badge is proven wrong-way by adding a second adjudicator fixture; `adjudicated: null` renders `Unchecked` and never Pass; no section identifier appears on any surface that is absent from source, checked as a file and not by eye; `node _design/plan-review-reasoner/check.mjs` exits non-zero on a planted violation and zero on the real boards with a non-zero matched-input count; and the built surface is verified live on `plan-review`'s serving revision read BY JSON FIELD NAME, because its traffic is pinned by revision name (`plan-review-00025-ley`, `latestRevision=false`) and a plain redeploy is a silent no-op that reports success | Nothing. **Operator confirmed 2026-09-17: `plan-review` stays on GCP for now and this row is NOT gated on D-12**, so it runs in parallel with the dashboards cutover. `plan-review` migrates later under its own D-row when the substrate phases reach it; OPS-25 does not cover it today and must not be assumed to | **CLOSED 2026-09-18, verified on `bastrop_tx`** (close-out `_inbox/2026-09-18_g150-closeout_close.json`): the reasoner console was reached live at `plan-review-app-ten.vercel.app/...?cityKey=bastrop_tx` as a Bastrop staff persona, GATE 2 recorded, and both record gaps closed. **Its finding: the console is a SEPARATE Vercel app and sat fifteen days behind the API, so the shipped reasoner was invisible for 2h27m while every API probe said the deploy was fine. The planner had verified only the Cloud Run side.** Carded G-160. Also open: the SF-1 label defect is live in production. The earlier grade, kept for the record: **MERGED AND DEPLOYED 2026-09-17; NO CLOSE ARTIFACT FILED.** `plan-review` PR #18 merged (`99c156ba`), lane-branch tree byte-identical to `origin/main`. Deployed `plan-review-00029-gom` @100%, tag `g150-reasoner`, moved OFF the `00025-ley` pin, so the canary-then-shift path was followed on a traffic-pinned service rather than a plain redeploy. **The instrument this row owed is delivered and was re-verified by the planner rather than taken on report:** `check.mjs` 72/72 self-tests both directions with non-zero matched inputs (5 boards, 11 finding rows, 9 citations); `violate.mjs` 27/27 planted violations caught on real artboards, including the vacuous-predicate case that REFUSES a verdict when predicates match nothing; GATE 3 identifier extraction 16 entries, 0 absent from source. `design-completion-gate.mjs` R3 drops 6 to 5. **The lane found and fixed a real defect in the ratified design:** the chip row read `Unchecked 9 / Pass 1` and now reads `Unchecked 10 / Pass 0`, because a Pass was rendered where source sets `adjudicated: null`. It also added `NO ADJUDICATOR` to the reviewer row and `CITATION OWED` where book and section are empty. **NOT DONE, and it is why this is not graded CLOSED:** no close, CP1 or CP2 artifact exists anywhere, so GATE 2 (the second-adjudicator wrong-way proof) has no record and no `leave_behind` was declared. The planner also did NOT reach the reasoner console on the live surface: the root route returns 200 carrying none of the reasoner markers, which is consistent with the console living behind an engagement id and equally consistent with a partial deploy, and those two were not separated. **Grade on a live console probe plus a filed close before calling this CLOSED.** |
| G-151 | 5 | Lane B: **BUILD THE PARKS LENS, the one lens that does not exist, drawn as not existing.** `_design/smartcity-parks-lens/`, RATIFIED 2026-09-17. Parks has no vendor and no registered domain, and absence from `DOMAIN_REGISTRY` is the only surviving meaning of "not built" in this product. The design's whole argument is that this surface must carry NO metric tiles, NO empty table and none of the built-surface vocabulary, because an empty built surface and an unbuilt surface are different claims and the first is a lie about the second. It also draws the three refusals in the product that make Parks unregistrable. **This is the cheapest row in the set and the one most likely to be "fixed" wrong by a later lane**, since every instinct says fill the page. The existing `check.mjs` already requires the built-surface vocabulary to be ABSENT on Main and PRESENT on the contrast board, and refuses a verdict if the scope marker is removed. Repo `smartcity-dashboards`. | B | `node _design/smartcity-parks-lens/check.mjs` passes on the built surface with a non-zero matched-input count and fails against a planted violation in both directions; the shipped Parks surface renders no metric tile, no empty table and no built-surface vocabulary, checked as a file rather than by eye; and the surface is reached on the DO app per D-12, verified by edge header | **D-12** (OPS-25) for the deploy path only. No vendor dependency exists and none can, which is the design | OPEN — carded 2026-09-17 (A-142) |
| G-152 | 5 | Lane B: **BUILD THE PUBLIC WORKS AND FIRE AND EMS LENSES.** `_design/smartcity-public-works-lens/` and `_design/smartcity-fire-ems-lens/`, both RATIFIED 2026-09-17, both carrying a `check.mjs` already. Paired because they share a dependency shape and nothing else: each has one vendor answering and one vendor gap, and **each design already draws its own blocked state**, so neither waits on a vendor. Public works carries two regions of different UNITS, so they switch rather than stack; its capital-projects matrix renders eight of twenty cells as CANNOT OCCUR by the product's own rule, hatched, beside four measured zeros, which is a fourth state next to absent, zero and unmeasured; its call analytics is drawn once as the grid its two shipped tables are the two margins of, reconciling to 2,365 both ways; and no figure of money appears anywhere, which its check refuses. Fire and EMS is drawn as small multiples per station rather than a rollup, because the domain module's own brief says a rollup cannot tell you which station is carrying the shortfall. **Both blocked boards name the obstacle as a vendor ENTITLEMENT or an uncompleted OAuth consent rather than a bug, and say plainly that nobody reading the screen can fix it.** Do not convert either into an error state or a retry affordance. GoTo (`goto_not_authorized`, nobody completed the consent) and FirstDue (403, the credential lacks the apparatus and assets scope) are vendor-onboarding work tracked outside this row. Repo `smartcity-dashboards`. | B | Both `check.mjs` pass on the built surfaces with non-zero matched-input counts and fail against planted violations in both directions; the Public works matrix renders cannot-occur cells distinguishably from measured zeros and from unmeasured, proven by a fixture exercising all four states; no money figure appears on Public works anywhere; Fire and EMS renders twelve of twelve per-station multiples with nobody named; each blocked board states the obstacle as an entitlement or consent and offers no retry; and both are reached on the DO app per D-12 | **D-12** (OPS-25) for the deploy path only. NOT blocked on GoTo or FirstDue: both designs draw the vendor-absent state as design content, verified at source 2026-09-17 | OPEN, DISPATCHED 2026-09-18 (A-154), compiled and queued behind `g153`. Do NOT start until g153 is closed: lenses serialize on web/app.js and web/index.html. Carded 2026-09-17 (A-142) |
| G-153 | 5 | Lane B: **BUILD THE FLEET AND POLICE LENSES, and fix the three live-mapper defects they both run on.** `_design/smartcity-fleet-lens/` and `_design/smartcity-police-lens/`, both RATIFIED 2026-09-17, both carrying `check.mjs` and Police also `violate.mjs`. Paired because they are the two lenses whose vendors actually answer, and because they share one blocker and three defects. Fleet is the first lens designed whose vendor returns real data, so its second board asks what the page still says once it has FILLED, and two of the three things that change at the live cutover are defects. Police is the only registered lens whose two regions disagree about their SOURCE, and it disagrees in opposite directions on the two shipped packs: on the demo Verkada is granted and Spireon withheld, on Bastrop Spireon is granted live and Verkada is not granted at all, so the demo's emptiest region is the city's most connected one. **Three defects verified at source 2026-09-17 against `smartcity-dashboards` `origin/main` `f776b4bf`, none of which had a row, and all three land on exactly these two lenses.** (1) `assertRecordShape` is called from `fixture-seam.mjs:546` and from tests and from NOTHING in `vendor-live.mjs`, so the fixture path we control is shape-validated and the live vendor path we do not control is not. That is a dormant mechanism in the `ENFORCEMENT.md` sense: built, correct, and never reached on the path that needs it. Run by hand against the live Samsara record it refuses with three faults, and that record carries twelve undeclared fields including `vin`, `make`, `model` and `odometerMiles`, an inventory field set arriving on exactly the cutover that drops the one sentence saying this is not an inventory. (2) The literal `"Unnamed unit"` is produced on an empty row by BOTH `vendor-live.mjs:131` (Samsara) and `:186` (Spireon), so identifier collisions cross lenses. A sentinel that two vendors share is not an identifier. (3) `operatorRef` is `required: true` on the Spireon shape and occurs ZERO times in `vendor-live.mjs`, so a required field is silently absent on every live record. **The namespace ruling lands in THIS unit of work**, because defect (3) is the code that mints operator references: change `OPERATOR_REF_FORMAT` to `/^FL-OPR-\d{2}$/` in `fleet-vehicles.mjs` and `/^PV-OPR-\d{2}$/` in `patrol-vehicles.mjs`, and mint the namespaced form on the live path. **Planner-owned follow-on, not this lane's:** the two design folders then recapture `source-state.json` and regenerate their artboards (`Main.dc.html` 20 occurrences, `Patrol.dc.html` 14). **Named trap:** `_design/smartcity-fleet-lens/check.mjs:154` hardcodes `/\bOPR-[A-Za-z0-9]+/g` to extract candidates, and in `FL-OPR-01` the `\b` still matches before `OPR`, so it extracts the truncated `OPR-01` and fails it against the new format. The check will break in a way that reads as a design defect and is an extraction bug. Both checks also carry hardcoded bare-form self-test fixtures (Fleet `:271`, Police `:436` and `:449`). Repo `smartcity-dashboards`. | B | Both `check.mjs` pass with non-zero matched-input counts and fail against planted violations both ways; Police's check refuses any plate-shaped string anywhere and any surveillance term outside a declared refusal; Fleet's refuses any driver name, any operator reference outside the declared format, and asset-inventory vocabulary in an assertive position; `assertRecordShape` is called on the live vendor path and REFUSES rather than defaulting when the live Samsara record fails it; no two vendors can emit the same unit label sentinel, proven by a fixture feeding both an empty row; `operatorRef` is either populated or the record is refused, never silently absent; and both surfaces are reached on the DO app per D-12 | **D-12** (OPS-25) for the deploy path. **The operator-namespace blocker CLEARED 2026-09-17** (`_decisions/2026-09-17_operator_reference_namespaced_by_domain.md`): operator references are namespaced by domain, Fleet mints `FL-OPR-nn` and Police `PV-OPR-nn`, and a bare `OPR-01` stops being valid anywhere. Ruled before the live path is written rather than after, because `operatorRef` occurs zero times in `vendor-live.mjs` today and whatever fixes defect (3) will mint these references; changing the scheme after a feed is minting them is a migration, not a constant | CLOSED-PARTIAL 2026-09-18, verified by the planner at source. The lane graded itself `closed` and the planner is stricter for one measured reason, stated below. Done, and re-run by the planner rather than read from the close: both design instruments exit 0 with NON-ZERO matched inputs (fleet pairs=3, operatorRefs=22, verbatim 11 of 11; police pvIds=10, plateScanTokens=11, verbatim 16 of 16), both are proven by violation (fleet 13 caught, 4 controls held of 17; police 17 of 17), the three live-mapper defects are shown by a violation rather than asserted (the shape guard refuses 102 of 102 LIVE records with the faults named, 75 Samsara and 27 Spireon), and G-135 parcel 2 is measured on the real shell (the map iframe mounts at `app.smartcityos.io`, 6 of 6 tiles, 12 of 12 layers responses arriving FROM the frame). Close `_inbox/2026-09-18_g153-fleet-police-lens_close.json`. **What holds the row at partial: the row's own instrument ends "and both surfaces are reached on the DO app per D-12", and the DELIVERED build is not on the DO app.** PR #75 is green (test 15s, a11y 8m38s, MERGEABLE/CLEAN at 20:34:26Z) and deliberately UNMERGED, because the merge and every deploy are planner-owned; nothing was deployed and the app still serves the pre-fix build. **LEAVE_BEHIND, AND IT NEEDS AN OWNER: the vendor mapping, and no row owns it.** Every live fleet and patrol record fails its declared shape (the status enum, `operatorRef`, Samsara's `odometerBand`), so once this is deployed Bastrop's Fleet and Police regions read `refused` with the faults named. That is a declared absence and not a blank, which is correct fail-closed behaviour, but until the mapping lands the two lenses show nothing useful. Also left: the two dumps lag the code (both declare the bare operator reference form while the code mints FL-OPR-nn and PV-OPR-nn) and need a recapture, which the lane states with counts and was forbidden to do; and Fleet ships no durable `violate.mjs` (its suite is a scratch instrument), which is a planner call. |
| G-154 | 5 | Lane B: **BUILD THE DEVELOPMENT SERVICES LENS EXTENSION, which is the delta the design grew AFTER its build row closed.** G-123 closed 2026-09-14, deployed `smartcity-dashboards-00068-sij`, 20 of 20 live browser-driven checks across all five built tabs. The design was then EXTENDED AND CORRECTED on 2026-09-15, one day later, and is now complete at six artboards covering Inspections, Code enforcement and Licenses, with their rows taken from the product's own fixture generators rather than transcribed. **So a closed row and a ratified design disagree about what this lens is, and the design is ahead.** Four defects were fixed in that same pass and each is an acceptance item here rather than a note, because each shipped once: a Place tab the product never had, five named people on a published workload ranking, the licence rows in the wrong sort order, and three residents named beside their addresses on Pipeline. The lane measures the delta against the shipped surface at a named ref rather than trusting this row's account of it. **Corrected by A-147: no grants are needed.** MyGov is granted at the KIND level, and `bastrop_tx` already holds it, so inspections, code violations, business licenses and work orders already read live Bastrop data. The server registers all four as live resources of kind `mygov` and gates on the kind, and GCP v1 served all four to the dashboards on 2026-09-16. So this row makes a lens that ALREADY shows live data match the ratified design, which is correctness work on real records rather than wiring. The live mapper already refuses the vendor's free-text description field, where citizens' names live (a G-123 finding). It does pass `assignedOfficer`, a staff name, and an `isRepeatOffender` flag beside each address; measure whether either lands on a surface the design corrected. Prove on `bastrop_tx`. Repo `smartcity-dashboards`. | B | The delta between the six-artboard design and the shipped surface is measured at a named ref and stated before any code is written; `node _design/smartcity-dev-services/check.mjs` refuses all four named defects and passes on the built surface with a non-zero matched-input count; no resident or staff name appears beside an address or on any published ranking, checked as a file; and the surface is reached on the DO app per D-12 | Nothing blocks this row. The live proof is obtained; two residuals are named in the status cell (acceptance item 2 has no input on this pack, and the proof reads at most 50 rows per tab) | CLOSED-PARTIAL 2026-09-18, re-graded by the planner from the lane's close (`_inbox/2026-09-18_g154-dev-services-live_close.json`, merged to `main`). Three of its four acceptance items are PASS on the live `bastrop_tx` surface and the fourth is NAMED AS UNMEASURED rather than counted, which is why this is a partial and not a close. PASS: no Place tab (the live surface draws exactly the shipped seven); licence rows in the design's own band-major sort order (19 dated rows off the live feed, and reversing the roll is caught); no resident named beside an address (200 refused-column cells across four header-keyed columns, a planted Title Case resident is caught). UNMEASURED: no named persons on published workload rankings -- the live surface draws NO workload ranking at all, every loadstrip-head is hidden and there are zero `class=who` spans on `bastrop_tx` and on `template-city`, so there is nothing to name. The rule has teeth (its self-tests refuse a planted name); a live proof of it is not available on this pack and is not claimed. The product work is merged to `smartcity-dashboards` `main` (PR #68 at `7dda6db`, follow-up PR #71 at `7267c3f`) and is on no production app. CP2 records the four corrections proven by mutation (planted 4, caught 4, unmutated control passes), the repeat-offender flag kept off the surface, and one live defect the dispatch had called a confirmation: at `fd8562c` the work-order Subject cell rendered the vendor's free-text title, where residents write names and phone numbers. **The live `bastrop_tx` proof IS obtained, and it is why the row moved.** At 2026-09-18T14:49Z the exporter read five Development services queues live on `d12-main-uat` (commit `53ade8a9`) with a key: 250 rendered rows across five tabs, `manifest.auth` recording that a key was used and never recording its value (`P:\tmp\g154-bastrop-live4\dev-services-export.json`). Anonymous `bastrop_tx` reads are still 401; the same lane minted its OWN credential (`key_id 2510a3ff`) through the admin API under A-118's precedent rather than waiting on G-135, and the operator's pilot key was not read, copied or used. **The credential is the reason this row could move at all, and it did not need G-135's key.** Two things a reader must not take from this row as fully closed: acceptance item 2 is unmeasured (above), and the live proof reads at most 50 rows per tab, so a 73-licence roll is a 50-row page of that roll. Four defects were found getting here and three of them were in the proving instruments, each producing a plausible reading of a real artifact rather than an error, which is why the exporter and the check are part of this row's change. A separate finding is REPORTED and not fixed: `hauska-mcp-server/.env`'s `HAUSKA_ADMIN_BOOTSTRAP_KEY` has drifted from the deployed value (the P-305 class). |
| G-155 | 5 | Lane B, A: **BUILD THE SMART FILES DESIGN, in its own repo and outside the DigitalOcean gate.** `_design/smart-files/`, RATIFIED 2026-09-17, carrying a `check.mjs`. Drawn against `smart-files` `origin/main` `61c84f61` and NOT against the dashboards nav item, which is a mount point and says so on the design. **Not a file browser**, and the design refuses that vocabulary: searchability with three named reasons, revision-not-sibling, five-key provenance that REFUSES the write when a key is missing, and placement. Its governing stance is COVERAGE BEFORE RESULTS, which is the same commitment the rest of the portfolio makes about unaccounted cells. **This row does not depend on D-12.** `smart-files` is a separate repo with its own GCP project (`smart-files-505619`) and is not among OPS-25's six services, so it ships on its existing path in parallel with the dashboards cutover, exactly like G-150. **Related but explicitly NOT in scope:** G-141 found that "drop any link, Google Drive or OneDrive, into one database", described to the customer on the Jaime call as a capability, has NO server path at all — zero hits for `googleapis`, `oauth`, `drive`, `onedrive` or `sharepoint` across `src/` and `package.json`, and the only ingest requires `bytesBase64`. That is an absent path, not a partial one, and it stays G-141's row. Do not let this row quietly imply it. | B, A | `node _design/smart-files/check.mjs` passes on the built surface with a NON-ZERO matched-input count and fails against a planted violation both ways — the non-zero count is mandatory here specifically, because this folder is the precedent where a predicate self-tested perfectly and matched nothing on any board; the five-key provenance path REFUSES an incomplete write rather than defaulting a key; no file-browser vocabulary appears anywhere the design forbids it; and the deployed surface is verified on `smart-files`' own serving revision read BY JSON FIELD NAME | Nothing. Not gated on D-12 | **CLOSED 2026-09-18, and its QA ran on `template-city`, which A-146 no longer accepts as proof.** Close `_inbox/2026-09-18_g155-smart-files-build_close.json`: PRs #16 and #17 merged, the not-indexed reason is now a closed set in a CHECK-constrained column, and 29 production rows now read "? Reason not recorded" rather than an inferred reason; G-141 link ingest left absent and named. But the lane's own words are "the QA fixtures are template-city demo rows". **Owed:** verification against Bastrop's own files, and confirmation that the Smart Files CONSOLE (a Vercel app, `smart-files-app.vercel.app`) moved with the Cloud Run canary, which is the exact split G-150 found stale on plan-review (G-160). |
| G-156 | 5 | Lane B: **BUILD THE FINANCE LENS, once the operator rules whether Finance belongs in the Bastrop package at all.** `_design/smartcity-finance-lens/`, RATIFIED 2026-09-17, carrying a `check.mjs`. Drawn at FULL SHAPE against the 25-page v1 capture: every cell either measured or countably UNACCOUNTED, and every unaccounted cell carrying a named acquisition path. Operator correction 2026-09-15, which is the design's spine: **removing the v1 fabrications IS the gap analysis, not the product.** That makes this the lens most directly expressing the every-record-starts-with-its-full-shape ruling, and the one where a later lane is most likely to "improve" it by filling a cell nobody acquired. Its check refuses any money figure not traceable to the capture record. **The open question is scope, not design.** `_design/SMARTCITY_PACKAGE.md` records it unresolved: the first Bastrop cohort is Development Services staff and Finance is not in their daily path, so this may be correct work aimed at the wrong launch. Sylvia Carrillo is the stakeholder. **Sibling row, deliberately separate:** `smartcity-finance-filings` is RATIFIED and G-138 still blocks its build over two citations that trace to nothing. Do not build the filings surface from this row. Repo `smartcity-dashboards`. | B | `node _design/smartcity-finance-lens/check.mjs` passes with a non-zero matched-input count and fails against a planted violation both ways; every money figure on the surface traces to the capture record and an untraceable one fails the check; every unaccounted cell renders as countably unaccounted with its named acquisition path and NEVER as zero, proven by a fixture exercising absent, zero and unaccounted separately; and the surface is reached on the DO app per D-12 | **D-12** (OPS-25) for the deploy path. **The scope call is ANSWERED, 2026-09-17 (A-143): Finance ships.** The operator named the Finance dashboard, the hotel occupancy tax and all the current financial tooling as one deliverable, so the question of whether Finance belongs in a Bastrop phase-1 aimed at Development Services staff is settled yes | **CLOSED-PARTIAL 2026-09-18.** Close `_inbox/2026-09-18_g156-finance-lens-build_close.json`: PR #67 merged (`fd8562c2`), the lens at full shape with every state DERIVED from the active pack's own grants, and its design check made pointable at the built surface. **Not done:** the build never reached the non-production app (no DigitalOcean credential in that session), and production was not touched. **Two findings, A-146:** (1) its route defaults `cityKey` to `template-city` when none is passed, which serves demo data silently, and (2) the budget renders UNACCOUNTED because the lane found no OpenGov connection in FIVE v2 REGISTERS. That is true of v2 and not of Bastrop: v1 is wired to OpenGov with keys in production. The operator's "build the honest full shape" ruling STANDS until G-159 lands, after which the budget moves to measured on its own. |
| G-157 | 5 | Lane B: **THE CITY MANAGEMENT BOARD — v2 must not remove what the city manager works from in v1.** Operator, 2026-09-17, naming the surface: *"that is the v1 dashboard that they have been currently working from, it's all departments combined."* So the city management board is not a new idea, it is the combined all-departments view Bastrop uses TODAY, and **v1 is being retired rather than run alongside** (G-145, stated to the customer in these words: *"the version two dashboard. I'm retiring the one you're working off of."*). That makes this a capability-preservation row, not a feature row: if v2 ships without parity, the cutover REMOVES the city manager's daily surface. **The v2 counterpart already exists and is shipped.** `smartcity-dashboards` `src/staff-review.mjs` declares `CITY_MANAGER_LENS = "city-manager"` as the FIRST entry in `LEAD_LENS_IDS`, `_design/surface_coverage.json` maps `city-manager` to `smartcity-overview-lens`, and G-120 closed 2026-09-14 (PR #57, deployed, verified at 100% traffic by JSON field name) shipping an "Across departments" roll-up covering all six department lenses. **So the question is parity, not existence, and nobody has measured it.** **The blocking dependency is an artifact only the operator can produce.** The Finance lens was drawn at FULL SHAPE against `SmartCity OS Screenshots (2).pdf`, 25 pages of the v1 live Finance tab, supplied by the operator 2026-09-15 and cited by page in `capture-figures.json`. **No equivalent capture of the v1 combined dashboard exists**, and no lane can take one: G-133 established that v1 is gated by a logged-in Google OAuth session cookie (global gate in `smartcity-os` `server/routes.ts`), a completely different credential from v2's Hauska product key. A lane cannot reach v1 at all. **Do the gap analysis the way the Finance lens did it**: every element of the v1 combined board either has a v2 counterpart, is a v1 fabrication being deliberately dropped (with that stated), or is a real gap carrying a named acquisition path. Removing a v1 fabrication IS the analysis, not a regression, per the operator correction of 2026-09-15. Repo `smartcity-dashboards`. | B | A page-cited parity table covering every element of the v1 combined board, each classified as carried, deliberately dropped as a v1 fabrication, or an open gap with a named acquisition path, with NO fourth state and no element unclassified; every v2 claim in it verified against the deployed surface rather than against the Overview design; the `smartcity-overview-lens` design amended and re-ratified if parity requires new regions, and its missing `check.mjs` delivered with it (it is an R3 finding today) -- **that instrument is being delivered by G-148's lane `g148-design-instruments` (hold released 2026-09-18, `_decisions/2026-09-18_g148_hold_released.md`), which this row INHERITS rather than rebuilds, so this row does not wait on it and must not build a second predicate for the same design**; and the result reached on the DO app per D-12 | **A capture of the v1 combined dashboard, operator-owed**, in the form the 2026-09-15 Finance capture took. No lane can produce it: v1 sits behind a Google OAuth session cookie (G-133). AND **D-12** (OPS-25) for the deploy path. Not blocked on RBAC, though G-143's audit-trail work and this row both serve the same city-manager audience | OPEN — carded 2026-09-17 (A-143). INSTRUMENT INHERITED FROM G-148 (A-159), now DELIVERED, do not build a second one: `_design/smartcity-overview-lens/check.mjs` (57 self-tests both directions, 11 matched-input counters all non-zero) with `violate.mjs` planting 41 named violations, every plant caught on a real artboard, and its facts derived by `dump-source-facts.mjs` into `source-facts.json` from `P:/smartcity-dashboards` origin/main `7487d7c0` and the G-120 ruling. The shipped boards fail on two real defects, Main's lane roll-up and Sparse's promotion, carded as G-164. |
| G-158 | 5 | Lane B: **WRITE DOWN WHO OPENED A RECORD — the access-log write path the audit trail cannot exist without.** G-143 drew the audit trail as the trust surface the row calls it, "who looked at that citizen's record", and found nothing to draw it from: no write path records a staff read anywhere in `smartcity-dashboards` at `3d3ec62a`, across all 45 source files. So the design renders "Who looked" as NOT RECORDED, deliberately, because an empty table would read as "nobody looked", which is a different and false answer. This row builds the thing that would let it answer. At the moment of every read by a signed-in staff identity, write one record naming who (the staff `sub`), which record, which lens, and when, scoped to the tenant so one city's reads never show on another's. **Fail closed:** a read whose log write fails is refused rather than served unlogged, or the trail has holes nobody can see, and a trail with invisible holes is worse than none because it reads as complete. It must cover the MCP surface as well as the UI, or the door around the UI is open (the same point G-132 makes about identity). **Only after this lands is "Who looked" redrawn against it**; `gen.mjs` refuses to render the current board once `source-state.json` reports an access-log write path. Repo `smartcity-dashboards`. | B | A read by a named fixture staff identity writes exactly one record carrying who, which record, which lens and when, and a second identity's read writes a second; a forced log-write failure REFUSES the read rather than serving it, verified by violation; reads through the MCP surface are logged the same way; a tenant's reads never appear under another tenant; and `dump-source-state.mjs` then reports an access-log write path, which makes the current "Who looked" board refuse to render until it is redrawn against the real trail | G-134 for a staff identity to attribute a read to; G-143 ratified for the design it serves | OPEN — carded 2026-09-18 (A-145) |
| G-159 | 5 | Lane B: **THE FINANCE BRIDGE — put Bastrop's live finance data where v2 can read it.** Bastrop's finance data is wired in v1 and unreachable from v2, which is why G-156 renders the budget UNACCOUNTED. v1 `smartcity-os` has `server/routes/opengov.ts` (entities, financial, reports), `opengov-bnp.ts` (budgets, chart of accounts, data exports, entity info, segment names, sync) and `finance.ts` (permit revenue by type, outstanding, summary), all present at production `8bea7fa`, with API keys in production in BOTH projects (`smartcity-OPENGOV_API_KEY`, `smartcity-OPENGOV_BNP_API_KEY`; `opengov-api-key`, `opengov-bnp-api-key`). None has a platform route, so v2 cannot reach any of it. **Three legs.** (1) v1: add platform routes for OpenGov, budgeting and planning, and permit revenue behind `requirePlatformInternalKey`, on the pattern of the thirteen that already exist. (2) v2: declare the OpenGov adapter kind (catalogued `declared: false` because its budget record shape is not declared on G-91), declare that record shape, and grant it to `bastrop_tx`. (3) Fix G-156's route: its fallback to `template-city` when no `cityKey` is passed becomes a refusal. The Finance lens derives every state from the pack's grants, so once the grant exists the budget moves to MEASURED with NO lens change. **Prove the keys before building anything:** the credentials exist, and that they return live Bastrop data is not established, because v1 sits behind a login and now runs on DigitalOcean. The first act is one authenticated OpenGov read; if it does not answer, stop and report rather than build a bridge to nothing. **v1 drift is not a blocker:** `main` is one commit past production and that commit touches only the scraper. The v1 deploy is `walrus-app`, which is `smartcityos.io` in production: canary, `services[0].source_commit_hash` read back (OPS-25 rule 13), and the v1 surface Bastrop uses never broken. Repos `smartcity-os` and `smartcity-dashboards`. Prove on `bastrop_tx`. | B | Before any code: one authenticated OpenGov read returns a Bastrop record, recorded verbatim; each new platform route answers with the key and REFUSES without it, both directions; the adapter declares its record shape and validates a live record, refusing rather than defaulting a failing field; on `bastrop_tx` the Finance lens shows the budget MEASURED with a basis naming OpenGov, and UNACCOUNTED again when the grant is pulled; the finance route REFUSES a request with no `cityKey`; the v1 deploy is a canary with `source_commit_hash` read back and the v1 surface unchanged | Nothing to START: the key proof and the v1 routes run now. **Going live waits on two OPS-25 rows, carded 2026-09-18:** D-14, because walrus-app builds from a side branch (`d9-api-8bea7fa`) that v1 `main` does not contain, so routes merged to `main` never deploy; and D-13, because the dashboards read every v1 platform route from the GCP copy, not walrus-app, so they could not reach the routes even once deployed. This row finishes everything short of that: the key proof, the v1 routes merged to `main`, the v2 adapter declared and granted, and the city default fixed. Deploying the routes to GCP instead is ruled out, because that modifies the untouched rollback copy (OPS-25 rule 3) | CLOSED-PARTIAL 2026-09-18. Close `_inbox/2026-09-18_g159-finance-bridge_close.json` (revision 2). Every part this lane owns passes. The key proof: BNP answers with Bastrop's own budget, 12 records, and the operator capture reproduces to the digit, while the v1 REST leg answers with 32 entities of which none are Bastrop's, so no route was built on it. Four v1 platform routes merged to `smartcity-os` `main` as `a400f7be` (PR #58), 16 of 16 cells measured with and without the key, not deployed. The dashboards adapter declares its record shape and the finance route refuses a missing city, merged as `53ade8a` (PR #69) and proven on `d12-main-uat` against the pre-fix deployment. **Open:** the `bastrop_tx` grant, which waits on D-14 and D-13, dispatched together (A-148). Its findings are carded as G-161 (seven more city defaults) and G-162 (v1 finance honesty) |
| G-160 | 5 | Lane B: **A DEPLOY IS NOT DONE UNTIL THE CONSOLE AND THE API SERVE THE SAME COMMIT.** Found by the G-150 close-out: `plan-review` is served by two independent deploy paths with nothing comparing them, the API by Cloud Run and the console by Vercel. On 2026-09-17 the API advanced to the reasoner while the console sat fifteen days behind, so a shipped feature was invisible to every user for 2h27m and every API probe said the deploy was fine. The planner's own verification of G-150 checked only the Cloud Run side and missed it. The dashboards embed Smart Files the same way (`DEFAULT_SMART_FILES_ORIGIN` is `smart-files-app.vercel.app`), so the same split is likely there, and G-155 deployed Smart Files by Cloud Run canary without its close saying the console moved. Build one check that, for each product with a console and an API, reads the commit each actually serves and FAILS when they disagree. **Three-question gate:** it is executed by a script run at the end of every deploy of either side; it is triggered by any Cloud Run or Vercel deploy of `plan-review` or `smart-files`, and also on a schedule, because a deploy made without running it is its bypass; and what fails is a non-zero exit that blocks calling the deploy done. Repos `plan-review` and `smart-files`. | B | The check FAILS when the console serves an older commit than the API, proven by violation against the recorded 2026-09-17 state, and passes when they agree; it runs for both `plan-review` and `smart-files`; its first scheduled run records whether Smart Files is split today | Nothing | CLOSED-PARTIAL 2026-09-18, verified by the planner at source. Parcel 2 armed the check, and the planner re-ran the trees rather than the prose: all five files are present on CLEAN trees, `scripts/deploy.mjs`, `web/api/version.js`, `scripts/served-commit-parity.mjs`, `.github/workflows/served-commit-parity.yml` and `scripts/g160-served-harness.mjs`, in BOTH `plan-review-worktrees/g160-served-commit-parity` (cdd4cb3) and `smart-files-worktrees/g160-served-commit-parity` (2c5d4e1), each two commits ahead of its `origin/main` and UNPUSHED. `SERVED_COMMIT` is set TWICE on the Cloud Run revision (env var and label) from the same resolved value, and the console reports the commit it serves without holding a platform credential, refusing rather than defaulting. Steps (a)-(g) of the lane predicate are met. **What holds the row at partial: the row's own instrument ends "its first scheduled run records whether Smart Files is split today", and no scheduled run can have happened on an unpushed branch.** A close that said `closed` would claim a green live run this lane cannot produce. **Left to the integration seat, named by the lane:** push both branches and open the PRs; deploy both products THROUGH `scripts/deploy.mjs` (not by hand, which is this check's own named bypass) so `SERVED_COMMIT` is recorded; expect the first deploy through the new path to PASS its own gate, and expect a single post-deploy DISAGREE if the API deploys from HEAD while the console still serves the pre-deploy commit, which is the gate working. **Record defect the lane disclosed instead of implying:** Parcel 1's CP1 was overwritten by Parcel 2's CP1 at the same untracked path and is UNRECOVERABLE; the lane searched the worktree, the integration inbox, the Cursor caches and all git refs, and says so. |
| G-161 | 5 | Lane B: **NEVER DEFAULT A CITY, EVERYWHERE, NOT ONLY ON THE FINANCE ROUTE.** Rule 3 of `_decisions/2026-09-18_bastrop_is_the_proving_pack.md` says a route that takes a `cityKey` refuses when it is missing. G-159 fixed the finance route and enumerated the rest read-only at `7dda6db`. It found seven more request-scoped fallbacks to `template-city` in `src/server.mjs`: the Development services pipeline, `/api/city-domains`, every `/api/domains/*` route, `/api/city-identity`, `/api/shell`, the city manager compose route (through an imported `DEFAULT_CITY_KEY`, which a grep for the literal misses), and the municode calendar run (which a guard already stops from leaking). There are module-level defaults in `src/compose.mjs` (which defaults twice, so an empty string also becomes `template-city`), `src/staff-map.mjs`, `src/municode-calendar.mjs` and `src/run-municode-calendar.mjs`. Six of the seven route fallbacks are unguarded. Each silently serves the demo pack to a caller who named no city, which is the defect class that sent the first wave of builds to `template-city`. **Refusing before enumerating callers is an outage**: every client call site in `web/app.js` and `web/index.html` names its city before the server refuses a missing one. Follow G-159's pattern: a missing, empty or whitespace-only `cityKey` refuses 400 `city_key_required` before the caller is resolved; a named unknown pack stays 404; a named pack is unchanged. Repo `smartcity-dashboards`. | B | Every enumerated fallback, re-measured at current `main` by symbol and by literal, refuses a missing city, proven by violation in both directions; every client call site names a city; on `d12-main-uat`, with `source_commit_hash` read back, each route answers keyless 400, anonymous `bastrop_tx` 401, `no-such-city` 404 and `template-city` 200 | Nothing | **LANDED 2026-09-18, read at source by the planner at 16:26Z.** The six `G-161. Was \|\| "template-city"` sites plus the module-level and searchParam defaults are replaced in `src/server.mjs` at `7487d7c0`, which is `smartcity-dashboards` `origin/main` HEAD; live on `d12-main-uat`, whose `source_commit_hash` reads back `7487d7c0`, each route answers keyless `400 city_key_required`, `no-such-city` 404, `template-city` 200 and anonymous `bastrop_tx` 401, where the 15:09Z read answered 200 keyless carrying the demo city. **No lane close, CP1 or CP2 is filed**, so this row moves on the deployed record and the lane's own acceptance items and leave-behind are still owed |
| G-162 | 5 | Lane B: **v1 FINANCE HONESTY: THREE DEFECTS G-159 FOUND IN THE PRODUCTION FINANCE CODE AND DELIBERATELY DID NOT TOUCH.** In `smartcity-os`: (1) the production budget filter drops Test, SANDBOX, training, Proof and UAT budgets but not DNU, so "FY2027 Operating Budget (DNU)" and "(DNU2)" count among the 6 of 12 production budgets, and the rule is shared with the session route's cache warm-up; (2) `server/routes/finance.ts` clamps a difference with `Math.max(0, ...)`, which turns an impossible state into a clean zero, so publish the signed difference and name the conflict; (3) the same file defaults a missing tenant id to 1. **Not in scope, and owed by the operator:** which MyGov fee report window is authoritative. `mygov_fees` holds 290,077 rows across 1,491 FY2026 permits, 174,092,822 charged against 261,793,717 collected (a 150 percent collection rate), because a fee listed in several report windows is summed once per window. Choosing the window is a finance-owner decision, not a code one. **Do not "tidy" `getBnpApiKey()`**: it reads `OPENGOV_API_KEY` first, and that is the only reason BNP answers (G-159). These change figures on `smartcityos.io`, so under rule 4 of the proving-pack decision each is a bridge proven on a non-production app before `walrus-app` carries it. Repo `smartcity-os`. | B | The two DNU budgets leave the production count, measured on a live BNP read; the clamp is gone and a negative difference renders as a named conflict, proven by violation; a missing tenant refuses; each proven on a non-production DigitalOcean app before `walrus-app` | **D-14** (OPS-25). Until `walrus-app` builds from `main`, nothing merged here reaches production, and landing it first would make D-14's repoint change behaviour when it must change none | CLOSED-PARTIAL 2026-09-18, verified by the planner at source; the lane's own grade stands, for a measured reason. Two of the three defects are PROVEN BY LIVE READS on the non-production app `g162-v1-uat`: the two DNU budgets leave the production count (6 of 12 to 4 of 12, with both DNU names in `productionExcludedNames`), and the `Math.max` clamp is gone so the signed difference renders as a named conflict (`collected_exceeds_charged`), with the violation produced first on the pre-fix commit. **The clause that holds it at partial: defect 3's refusal is proven only at the ROUTE level, never on a deployed surface**, because no deployed request can present a missing tenant binding (the app refuses at 401 before any finance handler runs, measured on both legs), and the lane did not stage a violation it could not produce. PR #60 is OPEN, MERGEABLE and unmerged, so nothing here has reached production. **Findings left unfixed and named:** the `/api/finance/permit-revenue/outstanding` HAVING clause silently excludes permits whose collections exceed their charges; `requireTenant` binds an anonymous visitor to the demo tenant (slug `your-city`, id 1), which is deliberate but is "default a city" in the direction rule 3 of the proving pack cares about; and **the `staging` smartcity-os database IS the production one**, so any future lane told to test against staging is writing to production data. DEBT, visible on purpose: `g162-v1-uat` carries 18 placeholder env values and a lane-generated platform key, so it is a proof surface and not a staging environment. Operator still owes the MyGov fee report window, which this lane names as the now-binding constraint on the figures rather than resolving it. Close `_inbox/2026-09-18_g162-v1-finance-honesty_close.json`. |
| G-163 | 5 | Lane B: **TWO PLATFORM ROUTES FAIL OPAQUELY ON `walrus-app`, WHERE THE GCP COPY STATED THE REASON.** Found by the D-13 ship acceptance on 2026-09-18 (OPS-25 A-10), proven by violation to be INSIDE the handler and not at the auth path. `GET /api/platform/firstdue/apparatus` and `GET /api/platform/goto/call-summary` on `smartcityos.io` answer a Cloudflare `504` HTML page in about a second, with no reason, to a caller presenting the canonical `PLATFORM_INTERNAL_API_KEY`. The same routes on the GCP `smartcity-api` production read until 2026-09-18 answered a structured `503` naming the vendor permission: *"Apparatus data exists in FirstDue but current API credentials do not have access (contact dashboards@firstarriving.com to request apparatus/assets API scope)"* and `goto_not_authorized, needsAuth: true`. **The discriminating test, which is what places the defect:** a one-character-mutated bearer still returns the gate's `401 platform_internal_required` on both routes while the canonical bearer returns `504`, so the gate runs and the handler is reached; `mygov/permits` and `powerbi/cip-projects` on the same host return 200 canonical and 401 mutated, so the host is healthy generally. **Why it matters beyond these two feeds.** The dashboards' `fetchLiveJson` fails closed correctly here, reporting `basis: "platform HTTP 504"` and never a fabricated value, so the customer-visible effect is a domain that reads `unavailable` with a reason that names nothing actionable. Both domains are `fire-apparatus` (firstdue) and `call-analytics` (goto) on `bastrop_tx`, and both were already `unavailable` on GCP, so this is lost diagnosis rather than a broken feed. **The shape to fix:** a vendor call whose failure must arrive as the vendor's own structured status, not as an edge timeout's HTML. Repo `smartcity-os`. | B | Both routes return a JSON status carrying the vendor's own error and message, proven by violating the vendor credential and reading the body, on `walrus-app` and after its next deploy; the gate's `401` on a mutated bearer is retained as the paired control | None. Independent of D-14 and D-13, which are landed | OPEN, DISPATCHED 2026-09-18 (A-154), compiled and queued behind `g162`. Do NOT start until g162 is closed: one owning seat per repo. Carded 2026-09-18 (A-151) |
| G-164 | 5 | Lane B: **REPAIR THE 11 FINDINGS G-148 DISCLOSED, WHICH NO ROW OWNED.** `g148` instrumented four designs past DRAFT and all four instruments exit 1 on the boards as shipped, disclosing 11 findings (4 + 1 + 4 + 2, re-derived from each instrument's own total, A-158). **plan-review-departments (4):** `Department.dc.html` draws finding 13 as "Fire apparatus access" where the product's 13 is "Parking spaces required"; `Letter.dc.html` renders 3 notice classes counting 1 + 2 + 10 over the product's 13 findings and carries no heading for the product's heldBack class, so it is folded into another; `Letter.dc.html` prints held-back finding 6 ("Driveway width") under the escalations heading, which asks nothing of the applicant; and the design's "no department model at all" claim, in the README and the canvas, is stale against `DEPARTMENT_ROLES` at `P:/plan-review` origin/main `99c156ba` `src/staff-identity.mjs:283`, a factual question A-145 already answered in the affirmative. **smartcity-flood-study (1):** the design says naming a depth by return period "would need a local rainfall atlas nobody has cited yet" while the engine carries `rainfallSource: noaa-atlas14`, cites NOAA Atlas 14 seven times, pairs return period to depth in `rainfallCurve`, and renders "100-yr (NOAA Atlas 14)". **smartcity-map-dock (4):** `Main.dc.html` and `Expand.dc.html` each render "Licences", which is not a product tab, and each drop "Licenses", which is. **smartcity-overview-lens (2):** `Main.dc.html` states "4 of 6 reading" over six lanes where 3 render a fact read from a source; `Sparse.dc.html` is granted 1 of 10 sources and still promotes Connections above the decision queue where the shipped function demotes it. Each repair is an adjudication rather than a typo, and the instrument that proves each is already written (A-158). Repo `doc_repo` (`_design/`). | B | `node _design/<folder>/check.mjs` exits 0 in all four folders with a non-zero matched-input count, each `violate.mjs` still catches every plant, and `node scripts/govtech/design-completion-gate.mjs` exits 0 with the instruments RUN rather than counted (A-159) | **G-125 for the flood-study finding only.** G-125's unowned leave-behind is that NOAA's Atlas 14 HDSC endpoint changed payload shape, `parsePfdsDepthTable` never matches, and every no-parameter study in every county silently uses Bastrop's 9.5in default, so the engine cites NOAA Atlas 14 as though it were a live value produced by a broken parser. A design-only correction would make the page agree with a value the parser invents. The other ten findings are unblocked | OPEN - carded 2026-09-18 (A-159). The design gate now RUNS the instruments it counts, so it exits 1 until this row lands; G-146 does not close on it (A-152) |
| G-50 | 5 | Lane D: ICC demo run; SaaS agreement executed; atoms upgrade platform-internal to public-paid | D | Signed agreement plus accessPolicy migration verified in store | G-17, G-23, G-30 | OPEN |
| G-51 | 5 | Lane C: standalone gate — all seven functions pass with ZERO SmartCity OS session | C | The overall acceptance criteria 1 through 7 in doc 48 | G-15, G-16, G-22, G-31 | OUT OF SCOPE FOR BASTROP CUTOVER, 2026-09-02 — "doc 48" is `48_cortex_reporting_plan_review_spec.md`, the AEC-cortex-facing surface, not the standalone `plan-review` repo Bastrop consumes. This row incorrectly blocked G-52 (the real Bastrop consumer-pass row) for the whole life of Wave 1. Re-pointed: G-52 no longer depends on this row. Still a real row for AEC-cortex's own reporting surface, whenever that gets built. See A-106. |
| G-52 | 5 | Lane B: consumer pass — SmartCity initiates an engagement from a MyGov permit record | B | Live: engagement created from SmartCity, review runs in the standalone `plan-review` product, no duplicated logic | The engagement-from-permit bridge itself (NOT YET BUILT). `engagement` has zero hits in `smartcity-dashboards` on `origin/main` `86487fa` — grepped `engagement`, `planReview`, `submittal`, `intake`, `plan-review` across all non-test source and web assets; `planReview` resolves only to an iframe embed URL carrying a `cityKey` and nothing else, and the served surface still reads "Reviews, Submittals against this parcel, Not connected" (`web/index.html:484`) | STILL BLOCKED — but the stated blocker CHANGED 2026-09-14 (R-10), because the old one was satisfied and nobody updated the row. The feed dependency is GONE: a live MyGov permit feed exists, is deployed and serves (G-116; `smartcity-os` `/api/platform/mygov/permits`, 312 real Bastrop permits). It landed on `bastrop_tx`, not on `template-city` as this row originally anticipated — a demo pack structurally cannot hold a real feed, since `assertCityPackShape` refuses any pack that both generates fixtures and grants an adapter, so this row's original precondition was never satisfiable as written. `_inbox/2026-08-17_dashboards_missing_pieces.md`'s "Do not start G-52" no longer stands on its original reason (no feed); it stands only on the bridge being unbuilt. Do not read this correction as authorization to start — it re-states the blocker truthfully, it does not clear it |
| G-115 | 5 | Lane B/C: PermitFlow island cut — real Bastrop staff onto `plan-review-app` as their reviewer, replacing PermitFlow per the 2026-08-17 sequencing. Real Bastrop tenant, UDC coverage honestly measured, ICC (IBC/IPMC) wiring fix folded in, tenancy/access decision named explicitly, one real staff go-live. | B, C | Frozen WDLL `_inbox/2026-09-02_bastrop_permitflow_islandcut_WDLL.md`, graded item by item at close | Wave 1 plan-review completeness (G-105 through G-114, CLOSED) | ITEMS 1+4 CLOSED 2026-09-02/03 — plan-review PR #14 merged (`1af5ac5`), deployed `plan-review-00014-bbg` @100% (Cloud Run traffic-pinning gotcha hit again, same class as G-111's; caught and corrected the same way — explicit `update-traffic`, verified by JSON field not CLI summary) plus a Vercel redeploy for the QA UI's persona-forwarding fix. Real `bastrop_tx` persona live, cross-tenant read/write refusal live-verified both directions. IBC live query path wired to the real substrate content (section `1001.1` resolves with a real citation; nonexistent sections correctly typed-absent; IPMC2018P2 confirmed to have no real content, correctly left alone). **Incidental production defect found and fixed, scope precisely bounded:** the standalone `/api/plan-review/code` REST route had a payload-extraction bug (`atom.data` vs the real `atom.data.atom` nesting) silently breaking EVERY resolved-section fetch through that specific route since G-108 shipped — confirmed live pre-fix on the already-shipped Bastrop UDC `14-02-003` atom. **This did NOT affect `matrixFromChain` or any WDLL-graded determination**, including today's G-110 walk: `matrixFromChain` builds citations via `buildCitation()` from data it already holds and never calls `getAtom()` — verified directly against `mcp.mjs` before accepting the close's claim, not assumed. Nothing from G-108/A-095/A-103 needs revisiting. Close `_inbox/2026-09-03_g115-tenant-icc_close.json`. Items 2, 3, 5, 6 remain open per the WDLL. Leave-behind: Smart Files' own `QA_PERSONAS` needs a matching `bastrop_tx` entry before a Bastrop-persona file upload will work (routed to that repo, not fixed here); the substrate's jurisdiction-rollup-vs-per-atom `accessPolicy` disagreement (`public-free` at the rollup, refused at `get_atom`) is named for the substrate seat, not fixed here. |
| G-116 | 5 | Lane B: real `bastrop_tx` city pack on `smartcity-dashboards`, plus ten real vendor/MyGov domain feeds and real Hauska tenant auth, read live server-to-server through a narrow platform-internal seam added to `smartcity-os` under explicit operator authorization | B | Live on the traffic-serving revision, read by field from the traffic JSON: an authenticated `bastrop_tx` caller receives real records on each granted domain with `origin: "feed"`, and an anonymous caller receives 401 on every `bastrop_tx` route while `template-city` still returns fixtures. Never graded as a gate — this row is written after the work shipped | none (shipped) | SHIPPED, RECORDED RETROACTIVELY 2026-09-14. Built 2026-09-03 to 2026-09-04 across `smartcity-os` PRs #39-#48 and `smartcity-dashboards` PRs #41-#52, deployed through seven tagged revisions `g116-mygov-live` ... `g116-cip-enrich`. Authorized by `_decisions/2026-09-03_bastrop_tx_dashboards_pack_ratified.md` and `_decisions/2026-09-03_smartcity_os_platform_read_authorization.md` — both operator-owned, both written 2026-09-03, both STRANDED on the unmerged local branch `seat/govtech` until recovered 2026-09-14 by R-10. Narrated in detail by amendments A-111 through A-124, also stranded on that branch. The `smartcity-os` ABSOLUTE NO-TOUCH line in `_catalog/repo_intents.md` was crossed here, narrowly and deliberately; that file still does not record the exception. Eight of ten domains returned live data at close; fire-apparatus (FirstDue API scope not granted) and call-analytics (GoTo OAuth consent never completed) are honestly unavailable, not broken. Verkada has no credential anywhere |
| G-117 | 5 | Lane B: native Leaflet property map for the real `bastrop_tx` pack, replacing the SmartSite embed on that pack's map stage only, reading parcel/zoning/flood/permit data plus a 52-layer GIS overlay catalog through two further platform-internal routes on `smartcity-os` | B | Live on the traffic-serving revision: an authenticated `bastrop_tx` caller composing the city-manager lens receives `/property-map.html?cityKey=bastrop_tx` rather than a `smartsite.cloud` URL, `template-city` still receives the exact SmartSite embed, and `/api/property-map/summary` refuses every non-`bastrop_tx` key with a stated basis. Never graded as a gate — this row is written after the work shipped | none (shipped) | SHIPPED, RECORDED RETROACTIVELY 2026-09-14. Built 2026-09-04 across `smartcity-os` PRs #49-#52 and `smartcity-dashboards` PRs #53-#56, deployed through three tagged revisions `g117-property-map`, `g117-overlay-layers`, `g117-full-catalog`; `smartcity-dashboards-00062-ful` serves 100 percent as of 2026-09-14, read by field from the traffic JSON. This row overrides the founding "No Leaflet island" rule (G-61) for exactly one page, under `_decisions/2026-09-04_no_leaflet_island_overridden_for_bastrop_map.md` — operator-owned, carrying the operator's own verbatim reasoning, STRANDED on `seat/govtech` until recovered 2026-09-14 by R-10. The override is explicitly temporary; its reversal trigger is SmartSite reaching a better launch position, not a date. `smartcity-os` PR #49 correctly HALTED the dashboards-side build on discovering the conflict rather than forcing through it, and the decision was taken before work resumed. Narrated by amendments A-125 and A-128 through A-134, also stranded |
| G-120 | 5 | Lane B: Overview lens design pass. Implement the approved three-state layout: tiles become filtered entry points naming the lens and filter they open; "Across departments" roll-up covering all six department lenses; Connections promotes to the top of the stack on a pack that reads nothing and demotes to the bottom when it reads; right rail keeps the map and address lookup, and the `Sources` atom-type panel is REPLACED by "On the map" (located records, clickable). Layout, hierarchy and density only — `sc-kit.css` tokens and the type ramp are frozen and must not change. | B | Design `https://claude.ai/code/artifact/3cf94cc7-4aa5-4846-9a60-bc51a1275180`; the four CSS gates stay green (no colour or token in `shell.css`, `sc-kit.css` byte-identical across the three repos, type ramp unchanged, no unstyled class); every honest-empty state keeps its `.basis` line; regression-check every state against the empty pack, not only `bastrop_tx` | none | **CLOSED 2026-09-14** — PR #57 merged, deployed and verified at 100% traffic by JSON field name. All five items shipped. Two real defects caught pre-ship: the Connections promote/demote was first keyed on `sources.demonstrated` and would have demoted the panel on the product's own demo pack while every sibling stayed empty (re-keyed to `sources.granted`); and CI's a11y job correctly rejected an overlapping transparent anchor over the tiles (each tile became the anchor instead, removing the overlap structurally). Left open and declared: "On the map" has no located-records source in this repo yet. Close `_inbox/2026-09-14_g120-overview-lens_close.json`. |
| G-121 | 5 | Lane B: Overview map component. PINNED 2026-09-14 by operator, deliberately deferred until after the Development services refinement. The rail map renders broken on the live `bastrop_tx` Overview (collapses to a narrow sliver with clipped control text) and has had no design pass. Scope when unpinned: correct rendering at rail width, layer control, boundary draw, located-record pins feeding the G-120 "On the map" panel, and behaviour on a pack with no boundary on file. | B | Map renders correctly at rail width and at the expanded width, on a pack WITH a boundary and on a pack WITHOUT one; located pins agree with the records "On the map" lists | G-120 | UNPINNED 2026-09-14 — the map approach was settled by the dock design; implementation folded into G-128, which owns all three dock states across both lenses. This row stays as the record of why it was pinned. |
| G-122 | 5 | Lane B: **INCIDENT** — live Bastrop v1 (`smartcityos.io` / `smartcity-api`) regression reported by the city 2026-09-14. Diagnose first, fix second. Fourteen PRs (#39 to #52) landed on `smartcity-os` building the platform-internal seam for `smartcity-dashboards`, and v1 production now serves `smartcity-api-00138-law` tagged `g117-full-layers-v2`. The fix must NOT break v2: `smartcity-dashboards` reads `/api/platform/mygov/*` and `/api/platform/property-intel/*` from this same service, so reverting the seam repairs v1 by breaking the property map and the permit feed. | B | The reported symptom reproduces before the change and does not reproduce after, on the DEPLOYED revision, read from the request's own log line; v2's property-map summary and MyGov permit reads still succeed after the fix, verified live | none | **CLOSED 2026-09-15 — and the cause was NONE of the planner's hypotheses.** Symptom, obtained from the operator because the dispatch did not carry it: clicking a property in the Development Services map to see its details did nothing. **Root cause is EXTERNAL.** The City of Bastrop's own ArcGIS-hosted parcel layer had its schema edited on **2026-09-10**, dropping a field (`Ordinance_Link`) that this app's parcel-lookup query had requested since March. **ArcGIS rejects an ENTIRE query if any one requested field does not exist**, so every click failed silently. **The timeline exonerates the seam:** the fourteen-PR platform seam finished deploying 2026-09-04, SIX DAYS BEFORE the external schema change, and Bastrop reported it FOUR DAYS AFTER — contrary to both the operator's working theory and the planner's ranked hypotheses, which were tested on diff review and found clean. Fix: one-line removal of the dead field name, **shared by the v1 staff route AND v2's platform routes** — so v2's property map carried the same defect. PR #55 merged, 192/192 tests, CI conclusions read as strings not exit codes, no-traffic canary, smoked, shifted to 100%, confirmed by gcloud field-reads. **Honest limitation, declared:** no OIDC credentials for the staff-authenticated browser UI, so verification runs through two independent live production routes sharing the identical fixed code path — not a literal click-through. See G-135. Close `_inbox/2026-09-14_g122-v1-regression_close.json`. |
| G-123 | 5 | Lane B: Development services lens build — the four record tabs plus the shared lens pattern. Pipeline, Inspections, Work orders, Code enforcement, Licences on the approved three-tier hierarchy (attention row of filtered entry points, tab strip carrying inline counts, compact secondary metric strip), ONE table treatment for every operational list, ONE load component shared by Inspector/Manager/Officer load, and view modes (List / Map / Performance) replacing the stray Service Performance button. This lens is the PATTERN fourteen of fifteen v2 destinations inherit. | B | Design `https://claude.ai/code/artifact/1ed0733e-e10c-4ec6-948e-66d2b24e562e`; the four CSS gates stay green; every honest-empty keeps its `.basis`; each tab regression-checked against the EMPTY pack, not only `bastrop_tx`; the free-text description renders in NO list column on any tab | G-120 (shares the tile pattern) | **CLOSED 2026-09-14** — deployed `smartcity-dashboards-00068-sij` @100%, verified by JSON field name; 20/20 live browser-driven checks across all five built tabs plus the two named-not-built, on BOTH `template-city` and `empty-city`, zero console errors. **The PII rule earned its place immediately:** Inspections carried a Comments column leaking a live citizen's free-text field — removed and permanently regression-tested, plus two further latent leak paths closed in the permit and code-violation mappers. Rebased mid-build onto G-120 and G-126 and adopted G-120's CI-hardened attention-row pattern rather than shipping a second one. OPEN: the dispatch's filter category names for Pipeline and Work orders do not match the shipped status vocabulary and were flagged rather than guessed. Close `_inbox/2026-09-14_g123-dev-services_close.json`. |
| G-124 | 5 | Lane B: Development services **Place** tab. HELD 2026-09-14 by operator — "I want to sit with it", to find a better map than either v1 or the parity proposal. The read path already exists (G-117 `composePropertyIntelSummary`: address in, geocoded parcel + zoning + flood + real permits/inspections/code-cases out, 52 layers from smartcity-os's own `layerCatalog.ts`). Parity design drawn and parked. | B | A parcel click returns dossier plus the records on that parcel, every value carrying its own source; any cityKey but `bastrop_tx` refuses rather than serving Bastrop's data mislabelled | G-121 (shares the map question), G-122 (shares the v1 upstream) | RESOLVED 2026-09-14 by G-128 — Place stops being a tab and becomes the persistent map rail. The parity design at `_design/smartcity-place-tab/` is superseded by `_design/smartcity-map-dock/` and kept as the record of the parity option. Carries the standing finding that this tab does NOT port to city two as written. |
| G-125 | 5 | Lane B: **the four-inch question — expose the rainfall control on the Flood and Drainage study.** Named deliverable, requested by **Sylvia Carrillo, Bastrop city manager**, in her own words: *"I want to be able to tell what happens when four inches of rain falls on a property."* Her stated value is cutting roughly ninety percent of the city's drainage-engineering spend by screening out sites that plainly do not need an engineer. **The engine already answers this and nobody can ask it.** `flood-drainage-study.ts` takes `rainfallDepthInches?` as an OPTIONAL input and, absent one, pulls the NOAA Atlas 14 PFDS point estimate and selects `DESIGN_STORM_RETURN_PERIOD_YEARS = 100` — the 100-yr 24-hr storm, 9.5 inches in the fixtures. It is a scenario engine with the scenario frozen at one value. Because Atlas 14 returns the whole frequency curve (`designStorms`, keyed by `returnPeriodYears`), ONE control can serve both vocabularies from one source: the city manager asks for four inches, the consulting engineer asks for the 10-year storm, and it is the same call. | B, S-3 | A study run at an operator-chosen depth returns a materially different result from the 100-yr default on the same parcel, verified on a real Bastrop parcel; both the depth AND its return-period equivalent are stated on screen and in the PDF; the default path is unchanged when no depth is passed | none | **CLOSED 2026-09-14, row status corrected 2026-09-17 (A-141) — it had read OPEN while the work was done.** Close `_inbox/2026-09-14_g125-rainfall-control_close.json`, `status: closed`, three PRs merged at CI success: `hauska-engine` #446 and #447, `hauska-map` #402. The control shipped in `hauska-map` `apps/property-explorer` (`FloodTool.tsx`, `floodDrainageClient.ts`), NOT in `smartcity-dashboards`, so SmartCity reaches it through the G-129 embed rather than natively. **Leave-behind, unowned until G-149 absorbed it 2026-09-17:** NOAA's Atlas 14 HDSC endpoint now returns a JavaScript array literal rather than the HTML table `parsePfdsDepthTable` expects, so every no-parameter flood study in every county silently falls back to Bastrop's 9.5in regional default regardless of location. Every live probe in the close returned `rainfallSource: 'default'` and never `'noaa-atlas14'`. Harmless for Bastrop and a fabricated basis for the second city. **Honesty framing is an ACCEPTANCE ITEM, not a nicety** — the deliverable's whole value is a city skipping an engineer on its strength, so the surface must say plainly what it is, what it is not, and when to call an engineer anyway, in the operator's words rather than ours. Precedent: property lines render as "GIS-approximate, not a survey". |
| G-129 | 5 | Lane B: **mount the Flood and Drainage study in the SmartCity platform as a cross-department capability.** Operator 2026-09-14: "we need the function in the smart city platform" and "usable across departments". NOT a Development services tab — it mounts the way SmartSite and Files already mount, reachable from Public works (drainage planning), Development services (review) and Fire and EMS (operations). **Mounted, not ported.** The PE overlay is MapLibre; the city map is Leaflet. Porting the renderer creates a second implementation of one study that will drift, and deepens the island `_decisions/2026-06-18_map_engine_maplibre_cotality_national.md` already named. The study is already addressable — `GET ?action=study&parcelNodeId=`, `GET ?action=download&…&format=pdf-flood-drainage` — so the platform consumes a service rather than rebuilding a renderer. In the G-128 dock it is a VIEW MODE of the map area in Full, so it takes the map region rather than nesting a map in a page that has one. | B | A Bastrop staff member reaches the study and its PDF without leaving the platform, on a real parcel, from more than one lens; no flood renderer exists in `smartcity-dashboards`; the regulatory FEMA zone and the derived study read as two different questions on one screen, never as competing answers | none — both gates CLEARED 2026-09-14 | **CLOSED-PARTIAL 2026-09-15 — DEPLOYED.** `smartcity-dashboards-00074-sil` @100%, tag `g129-flood`, read by JSON field name. The capability is reachable and functions identically from all three named lenses, **verified live by violation in BOTH directions including revert-on-close**, and the PE embed auto-opened the Reports dock in production **for a real Bastrop parcel**. Mounted, not ported — no flood renderer exists in `smartcity-dashboards`. **Sylvia's flood study is in the platform.** Leave-behinds: the flood-drainage synchronous refresh path used by EVERY PE customer today carries a general cold-container timeout risk measured live by G-125 at ~92s against a 55s abort — production-wide, not this row's, and needs a home. Public works and Fire and EMS both carry the `Not read` nav badge, checked rather than assumed. Close `_inbox/2026-09-15_g129-flood-mount_close.json`. |
| G-130 | 5 | Lane S-3: **the FEMA tier2 flood determination is dropped on the wire.** Separate from G-125/G-129 and not a drainage-study question. `hauska-engine` `packages/retrieval/src/serving-sweep/bff-flow.ts` states it in its own comment: the atom response has no `tier2` key and `mergeBakedBaseFacts` never adds one, so *"the cortex `tier2.flood` overlay — the only FEMA determination on this endpoint — is DROPPED from the wire for every parcel whose atom chain is usable. The sweep does not compensate for that. It measures it."* Underneath that, TWO stores derive flood zone by different METHODS, not by different vintages: `atoms:flood-hazard-fact` does parcel-centroid point-in-polygon against the local NFHL bulk table, while `pls:node-facets:tier2` quantises to a 0.005-degree tile, issues ONE live FEMA query at the TILE CENTRE and reuses it for every parcel in the tile — so a parcel's tier2 zone can be decided by a point up to ~366 m away. tier2 also records NO edition path, forcing its vintage undecidable. | S-3 | One derivation is named authoritative with its reasoning recorded; the surviving determination reaches the wire, verified on a parcel where the two stores disagree; it carries a vintage | none | **CLOSED 2026-09-14, and it corrected this row's own premise twice.** There are THREE derivations, not two: the one that actually serves is hauska-factory's parcel-record `flood` rail, landed 2026-09-02, which neither this row nor the planner named. And the `mergeBakedBaseFacts` comment quoted above is a STALE OFFLINE REPRODUCTION — its vendor pin is `hauska-map` at roughly 2026-08-18 and current `origin/main` has moved past it; accurate about that function at that pin, not about what serves today. Measured disagreement, denominator stated: Bastrop 5,424 of 62,249 comparable = **8.71%**; Travis 332 of 4,145 comparable = 8.01%, but 0.09% of roster because tier2 has no opinion on 92.8% of that county. Only entities where BOTH stores name a value can arithmetically disagree; a rate over the full roster silently rewards the sparser store for its own gaps. RULED `_decisions/2026-09-14_flood_determination_authority.md`: parcel-record authoritative, `atoms:flood-hazard-fact` sanctioned legacy fallback outside the program, **tier2 retired and not to be restored**. Store A's W-3/W-4/W-5 fix DEFERRED — unmeasured blast radius gets measured before it gets funded. **Owed before any citation:** a C10-shaped ground-truth sample of parcel-record's OWN output against live NFHL — the agent named that gap on itself rather than hedging. Recommendation `_inbox/2026-09-14_g130_flood_determination_recommendation.md`. |
| G-131 | 5 | Lane B: **the platform key binds to no tenant, and tenant resolution splits three ways behind it.** Reported read-only by G-126 defect 3, not fixed there. `PLATFORM_INTERNAL_API_KEY` is ONE shared bearer secret gating all 12 platform routes uniformly — it carries no tenant identity at all. Downstream, resolution takes three different shapes: five routes do a robust name-based lookup; **`property-intel/summary` uses a BARE NUMERIC LITERAL that has already been silently bumped once by an unrelated commit and feeds a real SQL filter**; and five vendor routes have no tenant concept whatsoever. Three implementations of one rule, in a live multi-tenant app, one of them a magic number with a demonstrated history of moving on its own. | B | One tenant-resolution path, or a divergence test that fails when the implementations disagree; the numeric literal replaced by a resolved binding; a second tenant behind that key is refused rather than served the first tenant's rows | none — G-122 CLOSED 2026-09-15 | **CLOSED 2026-09-15** by seat cente-g131. **G-126's count was wrong: verified the real split is 5/1/6 (=12), not 5/1/5 (=11).** `property-intel/layers` references no tenant field anywhere in its call chain (it queries `BASTROP_LAYER_REGISTRY`, a hardcoded single-city ArcGIS config) — it belongs in the tenant-blind group, which G-126 never separately counted. **The literal is fixed:** `property-intel/summary` now resolves its tenant via `getBastropTenantId()` (exported from `mygov.ts`), the same name-based lookup the five mygov platform routes already use — convergence onto the existing mechanism, not a fourth implementation. PR #56 merged (`8bea7fa5`), CI green (Gitleaks/Semgrep/Trivy), 195/195 tests (192 baseline + 3 new, including a falsifier that mocks tenant resolution to a distinct non-2 value and asserts it — not the literal — reaches the SQL-filtering call). Deployed `smartcity-api-00142-yuv` tag `g131-tenant-resolution`, canary `--no-traffic`, all twelve platform routes smoked before and after the shift (10x 200 identical to the pre-existing serving revision; 2x 503 on `firstdue/apparatus`/`goto/call-summary` confirmed pre-existing on both canary AND the then-serving revision — vendor credential/OAuth gaps, not caused by this change), shifted to 100%, confirmed by `gcloud` JSON field read (`revisionName=smartcity-api-00142-yuv percent=100`), traffic lease taken before the canary build and released after. **Vendor-routes verdict: genuinely dimensionless, all six** (`property-intel/layers` plus samsara/spireon/firstdue/goto/powerbi) — each calls one vendor account or Bastrop's own ArcGIS server via a single credential, with no tenant dimension anywhere in the codebase to ignore. **Convergence chosen over a divergence test** since the fix was a one-line reuse of an already-proven mechanism, not two competing implementations needing reconciliation. Out of scope, reported not fixed: binding `PLATFORM_INTERNAL_API_KEY` itself to a tenant (explicit exclusion); `platformOneClickAtPoint()`'s silent-null swallow of ArcGIS errors (G-122 close leave-behind, adjacent, untouched). Close `_inbox/2026-09-15_g131-tenant-resolution_close.json`. |
| G-132 | 5 | Lane B: **staff authentication — a person, not a persona.** Prerequisite to G-127 and to issuing any Bastrop staff login. Today no product can identify an individual: `smartcity-dashboards` resolves a city and never a person, and `plan-review` and `smart-files` each carry ONE shared Bastrop persona labelled "Development services staff" that every department would sign in as. Ruled 2026-09-14, AMENDED TWICE THE SAME DAY (`_decisions/2026-09-14_staff_identity_and_department_rbac.md`, ruling 1 final form): **SmartCity admin provisions every account and issues the credentials** — no SSO, no city IT, no invites, self-registration off, MFA on, via a managed provider's admin API so we never hold password hashes. SSO was the first ruling and was reversed on "we should not be asking cities to configure anything". The deciding argument is offboarding — under SSO the city disables one account and access ends across all three products immediately, without us being told, which for a customer whose data carries citizen names and phone numbers in free-text fields is the control rather than a convenience. | B, C | A named Bastrop staff member signs in with their city credential and is identified as a PERSON in all three products; disabling that account upstream ends access in all three, verified by violation; the identity carries a `role` claim G-127 can read; the MCP surface receives the same identity, or the door around the UI is open | none | OPEN 2026-09-14. **CLOSED-PARTIAL 2026-09-14.** Verifier built provider-agnostic by construction (OIDC discovery + JWKS, any compliant provider works with zero code change), `GET /api/people-and-access` built, 812/812 tests passing, left uncommitted for review. **Operator ruled WorkOS 2026-09-14.** Second ruling: a provisioned staff member with no lens role sees the Overview shell only, with a typed refusal naming the missing role — fails closed and stays visible. The lane deliberately did NOT cut the `QA_PERSONAS`/`x-persona` mechanism, because doing so before a real provider account exists would take live Bastrop staff access to zero; **the shared-persona hole stays open until the provider is wired.** Five build gaps named in the close, all follow-ons. |
| G-133 | 5 | Lane B: **RECON — why can nobody verify Bastrop, when Bastrop works.** The v1 dashboard's property map is serving real Bastrop data right now (operator, 2026-09-14), and yet THREE lanes have failed to verify authenticated `bastrop_tx` behaviour in v2, each reporting it as a standing gap rather than escalating: G-116's close listed "Real Hauska tenant key for `bastrop_tx`" as a leave-behind on 2026-09-03; G-128's close hit 401 on `GET /api/property-map/summary` and, because the shell's own compose call is gated the same way, **never got the map iframe to mount in a full-shell probe**; and the planner's own probes 401'd on every `bastrop_tx` route the same day. Those facts cannot both be casually true. Establish which: the data path and the verification path are different things we have been conflating, or a credential exists and is not reaching anyone, or none exists and something else has been answering. **Start by establishing what is true, not by obtaining a key.** | B | The crux stated in one sentence — why the map works for the operator in a browser and 401s for a lane with curl — plus: what `packContentReadStatus`/`resolveCaller` actually require; whether v1's map needs any Hauska credential at all given `esri.ts` queries Bastrop ArcGIS directly; an ENUMERATED search for the credential naming every place looked; and a plain verdict of provisioning problem vs distribution problem vs misunderstanding | none | **CLOSED 2026-09-15 — and it inverted the premise.** The key was never missing. v1 and v2 are gated by TWO UNRELATED credentials, both working: v1 needs a Google OAuth session cookie (global gate in `server/routes.ts`), v2 needs a Hauska product key scoped to `jurisdiction_tenant=bastrop_tx` in `HAUSKA_TENANT_KEY`, validated by `src/tenancy.mjs:43` against `hauska-mcp-server`'s `/auth/whoami`. **That key exists, is active, was created 2026-09-03 — the same day as G-116's close said it did not exist — and was used at 20:31 UTC 2026-09-14, hours before this recon.** A lane's curl carries neither credential, which is the whole answer to why the map works in the operator's browser and 401s for a lane. **Distribution problem, not provisioning.** Nobody found it because it lives in `hauska-mcp-server` under the substrate seat, outside every dashboards lane's dispatch scope: the lanes DID enumerate, and their enumeration boundary was the seat boundary. That structural finding is bigger than the key. Fix is G-135. Artifacts in the govtech seat worktree. Original framing follows: read-only, three closes degraded for want of this; a gap three closes name and nobody owns is the "exists, is correct, does nothing" class. `smartcity-os` reads are inside its narrow recorded exception; changing anything there is not. Coordinate with G-122 if it is live on that repo. |
| G-134 | 5 | Lane B, C: **wire WorkOS and put real Bastrop staff on v2 — the critical path for the phase-1 soft launch.** `_inbox/2026-09-15_bastrop_cutover_WDLL.md` names exactly ONE hard blocker on getting staff onto v2 and this is it: today every department signs in as one shared persona labelled "Development services staff", which is a shared account, not a login — no audit trail, no offboarding, no way to answer who looked at a citizen's record. Lands G-132's four in-scope build gaps: the WorkOS admin-API provisioning client feeding the existing `upsertStaffAccount`/`disableStaffAccount`; `smart-files` routes consuming the verifier it already carries (mirroring `plan-review`'s `resolveStaffCaller`, not a second shape); the sign-in UI and the People and access lens (read-only for the city manager, administered by us); and a stated verdict on TTL-bounded offboarding. G-132's verifier is provider-agnostic by construction and already passing 812/812 — **land that work, do not redo it.** | B, C | A named Bastrop staff member signs in as THEMSELVES in all three products and is confirmed by that person; disabling them at WorkOS ends access in all three, verified by violation both directions; the anonymous `template-city` path still serves fixtures and still 401s on `bastrop_tx`; the actual token TTL stated as a number with a verdict, not rounded off | G-132 (closed-partial) | **CLOSED-PARTIAL 2026-09-15, row re-graded 2026-09-18 from its own close** (`_inbox/2026-09-15_g134-workos-wiring_close.json`; found by `scripts/govtech/smartcity-tracker.mjs` on its first run, a fourth instance after G-125, G-128 and G-145 of a close that landed without re-grading its row). The WorkOS wiring is built and merged (PR #65, `f776b4bf`), and every predicate measurable without a real WorkOS account passes, including two live-local end-to-end sign-in tests. The one outstanding is the real-WorkOS violation test (P13, UNVERIFIED-LIVE), which waits on the operator's WorkOS credentials. The rule below still binds. **SEQUENCING IS NOT NEGOTIABLE:** wire, then provision and verify a real account, and ONLY THEN retire the shared persona — G-132 deliberately left it in place because cutting it before a real credential exists takes live Bastrop staff access to zero. Leaving it another day is free; removing it early is a customer outage. GAP 2 (`hauska-mcp-server` staff bearer) is NOT this row — OPS-19/OPS-23 territory, report only. |
| G-135 | 5 | Lane B: **distribute the `bastrop_tx` tenant key — it exists, it works, and no lane can reach it.** G-133 (closed 2026-09-15) found the crux: v1 and v2 are gated by two completely unrelated credentials, both working correctly. v1 needs a logged-in Google OAuth session cookie (global gate in `server/routes.ts`; `esri.ts` itself is wide open). v2 needs a Hauska product key scoped to `jurisdiction_tenant=bastrop_tx`, carried in `HAUSKA_TENANT_KEY` and validated by `smartcity-dashboards` `src/tenancy.mjs:43` against `hauska-mcp-server`'s `/auth/whoami`. **That key EXISTS, is ACTIVE, was created 2026-09-03 — the same day as G-116's close — and was used successfully at 20:31 UTC on 2026-09-14, hours before the recon.** So G-116's and G-128's closes both asserting it "does not exist" were FALSE, and three lanes were degraded to `closed-partial` for nothing. **This is a distribution problem, not a provisioning one.** | B | Any lane can obtain a `bastrop_tx`-scoped credential from a documented location without asking a human; a full-shell authenticated probe on `bastrop_tx` mounts the map iframe, which has never once happened | none | CLOSED-PARTIAL 2026-09-15 by its lane. Its close, checkpoints and mint request, and the credential index `_catalog/credential_access_index.json`, sat untracked until 2026-09-18 and are now committed. **UPDATED 2026-09-18: the verification-scoped key IS minted.** Lane `g135-mint` created `key_id 96e40316-0907-49f3-8c6e-d47d9013f02e` (tenant `bastrop_tx`, tier team, status active) and stored it at Secret Manager `hauska-prod-497015/hauska-tenant-key-bastrop-tx-lane-verification` version 1, recorded in `_catalog/credential_access_index.json` with a one-command `howToUse` and a `howToRevoke`. Parcel 1 of the completion predicate is PASS, read at source by the planner's own instrument rather than taken on report: `_inbox/2026-09-18_planner_g135_credential_probe.mjs` declared its expected value for every cell before the run and returned 8 of 8 against `d12-main-uat` at commit `53ade8a9` -- 200 on `bastrop_tx` with the key, 401 keyless, 401 on a garbage key, 403 on `fixture-city` (the other tenant-private pack, which is what makes the key tenant-scoped rather than a master key), 200 keyless on `template-city`, and 404 on an unknown city. The lane's own close is `_inbox/2026-09-18_g135-mint_close.json`, verdict `closed-partial` and self-honest. **Parcel 2 stays UNMEASURED**: a full-shell authenticated probe on `bastrop_tx` that mounts the map iframe has still been run by no lane, so this row remains CLOSED-PARTIAL. It is now UNBLOCKED rather than blocked, which is the change that matters to every other row. **Census correction (the lane's CP1, and it contradicts the dispatch):** the premise that only one active `bastrop_tx` key existed was FALSE. Three existed -- the operator's pilot `2a26c318` (2026-09-03), a dormant G-134 lane key `acf2cf9f` (2026-09-15), and a G-154 lane key `2510a3ff` minted 2026-09-18T14:36:21Z and in active use. Both non-pilot keys are now listed in the index as what exists; deciding whether to revoke them is a planner and operator call, and the mint lane touched neither. **BOTH NON-PILOT KEYS ARE NOW REVOKED (2026-09-18), read back at source.** The operator deleted the G-154 key file and instructed that the dormant G-134 key be removed. `2510a3ff` was already revoked when the planner read the census; the planner revoked `acf2cf9f` with its own instrument (`_inbox/2026-09-18_planner_revoke_dormant_g134_key.mjs`, artifact `_inbox/2026-09-18_planner_revoke_dormant_g134_key.json`), which refuses unless the target is the exact expected `key_id`, is not on a must-not-touch list, and is genuinely dormant -- it last ran 2026-09-15T00:49:49Z, three days idle. The `bastrop_tx` census is now the minted verification key `96e40316` plus the operator's own pilot and nothing else, which is the shape G-135 asked for. **Instrument lesson worth keeping:** the first run of that instrument reported "NOT REVOKED" while the status field already read `revoked`, because its verdict tested PRESENCE in the `/admin/keys` list and that endpoint returns revoked rows too. Presence is one input; the status field is the authoritative one. The ruling below is the reason the pilot was never an acceptable substitute. **Do NOT simply hand out the existing key** — it is labelled "Nick, bastrop_tx staff pilot" and belongs to the operator's own account. A human's pilot credential is the wrong shape for automated verification: mint a separate verification-scoped key, put it where lanes look (Secret Manager + the repos' `.env.example` + the dispatch preamble), and correct G-116's and G-128's closes, which assert a falsehood the next reader will inherit. **Why nobody found it:** the credential lives in `hauska-mcp-server`, a different repo under the substrate seat, outside every dashboards lane's dispatch scope — the lanes DID enumerate, and their enumeration boundary was the seat boundary. That is the structural finding and it is bigger than this key. **PARCEL 2 IS ASSIGNED, NOT ORPHANED (operator, 2026-09-18):** a full-shell authenticated probe on `bastrop_tx` that mounts the map iframe, which has never once happened, is folded into the next lens BUILD lane rather than given a credential lane of its own, because it needs a browser-driven probe of the map iframe and a lens lane already does exactly that. The next lens build is G-153 (Fleet and Police), which runs once G-161 and D-13 land; that lane inherits parcel 2 and this row closes when the probe is reported, at which point nothing in this row is unmeasured. |
| G-136 | 5 | Lane B: **the external-schema dependency that took Bastrop's One Click down, and will do it again.** G-122's root cause was not ours: the City of Bastrop edited their own ArcGIS parcel layer on 2026-09-10 and dropped a field this app had requested since March. **ArcGIS rejects the ENTIRE query when any one requested field is absent**, so a third party dropping a column silently killed a feature for four days until a customer reported it. The same explicit field list is shared by the v1 staff route and v2's platform routes, so both broke together. This is a contract-free dependency on a schema we do not control, with no detector: nothing noticed, nothing alerted, and the only instrument was a phone call from the city. | B | A schema change on any consumed external layer is detected before a customer notices — and the detector is proven able to fire, by removing a field in a test fixture and watching it go red; the query degrades to the fields that exist rather than failing whole, OR fails loudly with a named cause | none | OPEN 2026-09-15. Two things to weigh rather than assume: whether to request only fields we use and tolerate absence, or keep the explicit list and monitor it — the first is more robust and loses the ability to notice an upstream removal, the second notices but stays brittle. Note this is not Bastrop-specific: every jurisdiction whose GIS we query is the same exposure, and the 52-layer property-map catalog multiplies it. |
| G-126 | 5 | Lane B: **tenant isolation — the prerequisite to staff logins.** Three verified cross-tenant defects, none of them RBAC. (1) `plan-review` `GET /queue` returns ALL engagements across ALL tenants with no persona and no credential — cross-tenant refusal covers only `/engagements/:id/*` and only when a persona is asserted. (2) `smartcity-dashboards` `mapRealPermitRecord(row, cityKey, ...)` STAMPS `pack.cityKey` onto every upstream row without verifying it, and the outbound platform call carries no tenant parameter — correct today only because v1 is single-tenant Bastrop, silently wrong the moment a second tenant exists. (3) whether `PLATFORM_INTERNAL_API_KEY` is bound server-side to `tenant_id=2` in `smartcity-os` is UNVERIFIED; "the data is scoped to Bastrop" is currently an inference from v1 being single-city, not a property of the interface. | B, C | Each defect verified BY VIOLATION in both directions: a second tenant's rows must be refused, not relabelled. The G-115 acceptance test re-run and still passing. | none | **CLOSED 2026-09-14** by seat cente-f6. (1) plan-review: all 15 routes enumerated, **THREE** unguarded list routes found, not one — `/queue`, `/reviewer/engagements`, `/findings`; a bare service-token call did see all 25 engagements across 3 tenants; fixed by reusing the existing persona primitive with the org filter pushed into SQL so it fails closed. PR #15, `plan-review-00025-ley`. (2) dashboards: upstream carries no tenant field so the fixture-side paired control could not be replicated; instead closed a real reachable gap — any pack acquiring the generic `mygov` grant via an ordinary DB row write would have received Bastrop's real permits labelled as its own. PR #59, `smartcity-dashboards-00066-wed`. (3) smartcity-os: read-only, zero files touched — see G-131. **Staff logins are unblocked.** |
| G-127 | 5 | Lane B: **RBAC by department.** Different Bastrop departments see different things WITHIN the tenant — the operator's actual ask, distinct from G-126's cross-tenant isolation. Development services, Public works, Police, Fire and EMS, Fleet and Finance each have staff who should not see each other's operational data by default, and code enforcement carries officer names and complaint addresses while work orders carry citizen names and phone numbers in free-text fields. | B, C | A department persona sees its own lens's records and is refused another department's, verified by violation both directions; the refusal is typed, not a blank | G-126 | **BLOCKED then RULED 2026-09-14.** The lane returned blocked, correctly: there is no per-staff identity anywhere to attach a role to, and it refused to choose a permissions model rather than build one an agent picked. Three rulings now recorded in `_decisions/2026-09-14_staff_identity_and_department_rbac.md`: (1) managed-provider SSO — see G-132; (2) **the roster is the nine lenses**, operator ruling against the planner's recommendation to take it from the city directory; (3) the role is a CLAIM on the identity and the enforcement primitives live in ONE shared package across the three repos with a divergence test, following the `sc-kit.css` precedent. Deny-by-default stands and was never a ruling. Resolved with the rulings rather than sent back: only SEVEN of the nine lenses are departments — Overview is the cross-department roll-up and shows only the lanes a role is entitled to, Citizen is not role-gated — and the set is seven department roles plus `city-manager` (all lanes, because Sylvia asked for a cross-departmental tool) plus `admin` (adds People and access). Plan review gates to Development services and city-manager; Files, Records search, Assets and Connections are cross-cutting. Now blocked on **G-134** (the WorkOS wiring), not on a ruling. |
| G-128 | 5 | Lane B: **the map dock — three states, one component.** The city map becomes a persistent RIGHT RAIL on Development services, matching Overview, with the lens tabs between the left sidebar and the rail. It carries three dock states and the components redistribute per state: DOCK (map on top, property detail and records stacked below it in the rail), EXPAND (map on top, the two components below side by side at roughly 2/3 · 1/3), FULL (full-width permit row under the map, one-click property detail in the right column, layers sidebar). **The layers panel exists in FULL ONLY** — 52 layers in a fixed panel cannot work at rail width, and today it crushes the map at every size and renders as an unreadable sliver when docked. Dock and Expand get a Layers button carrying the active count. Consequence: **Place stops being a tab** and becomes the rail; the map is context you work beside, not a destination. | B | Each of the three states renders correctly at its own width AND on a pack with no boundary on file; the layers panel is absent in Dock and Expand and present in Full; a parcel click fills the property detail and the records in every state; `buildable area` still renders REFUSED with its basis in all three | **G-120 and G-123 (both in flight — see collision note)** | **CLOSED-PARTIAL 2026-09-14, row status corrected 2026-09-17 (A-142) — it had read OPEN while a close artifact sat in `_inbox/` for three days.** Close `_inbox/2026-09-14_g128-map-dock_close.json`, `status: closed-partial`. Its one stated leave-behind is the `bastrop_tx` Hauska product key, which G-135 owns, not a dock defect. Design `https://claude.ai/code/artifact/9a33103c-9765-4a50-ad7f-461fa6f17f43`, source `_design/smartcity-map-dock/`. **COLLISION:** G-120 is rebuilding the Overview rail and G-123 the Development services lens; all three touch the same layout files. G-128 lands AFTER both merge and retrofits the shared dock component, or it produces a three-way merge nobody can review. |
| G-137 | 5 | Lane B: **the Localgov Filings feed — the hotel occupancy tax integration the customer prioritised and will be billed for separately.** Client, store, platform route and reconciliation surface per `_inbox/2026-09-15_localgov_filings_integration_scope.md`; priced per `_inbox/2026-09-15_hot_integration_bid_brief.md`. `GET https://api.localgov.org/filings/reports/bastrop-filings`, read-only, single tenant, Cognito `IdToken`. **The endpoint has never been called** — no credential exists in any environment we control (verified absent 2026-09-15 across `smartcity-os-prod` 49 secrets, `smartcity-dashboards` 32 secrets and every local env file, with known-present controls returning rows in the same query so the absence is real and not a failed instrument). Two bindings are unresolved and neither is an engineering problem: **nothing confirms this is hotel occupancy tax** (the endpoint is `bastrop-filings`, the response carries no tax-type field, and nothing in the spec says hotel occupancy), so every label reads "Localgov filings" until Azavar Q1 returns; and **no field keys a taxpayer** (`FilingId` keys a filing, `FilingRefId` is a receipt reference), so this is scoped as a reconciliation instrument and non-filer reporting is a separate, separately priced phase if Q4 comes back well. The vendor encodes absence as `0`, so until Q2 returns no rate, average or ratio is computed from a numeric field — the row refuses rather than dividing by a fabricated zero. | B | A live call against the real credential returning rows, with the raw payload stored and re-fetchable; a reconciliation figure traceable to BOTH halves (vendor filing and city ledger) and neither blended into a single revenue number; and a refusal, not a zero, rendered wherever Q2 is unanswered — verified by violation by feeding a `0`-valued field and confirming the surface declines instead of computing | The four Azavar credentials and the four Azavar questions, both operator-owed and both in one email; and G-138 for the surface it renders into | OPEN — carded 2026-09-15 |
| G-138 | 5 | Lane B: **the filings design carries two citations to external authorities that trace to nothing, and it is the one design folder on the critical path with no instrument.** AMEND ruling 2026-09-15: an `Ordinance rate 7.00%` attributed on the artboard to "Bastrop code of ordinances", and a fund number attributed to the city ledger. The 7.00% is *plausible* — Texas municipalities commonly levy hotel occupancy tax at seven percent — and that plausibility is exactly the property that let it through unchallenged, while it scores three of the city's own filings as exceptions against it. Source both citations or badge them illustrative on the page. `_design/smartcity-finance-filings/` is the only one of the four live design folders without a `check.mjs`; `smartcity-dev-services`, `smartcity-finance-lens` and `smart-files` all carry one and all three pass at `eabec3a4`. | B | `node _design/smartcity-finance-filings/check.mjs` exists, self-tests in both directions, reports a NON-ZERO count of matched inputs, and refuses an external-authority citation with no traceable source — verified by violation against a real artboard, per the Smart Files precedent where a predicate self-tested perfectly and matched nothing | Nothing. The badge path needs no credential and no vendor reply | **CLOSED 2026-09-18 by the badge path, planner-owned in this repository.** Both citations are badged UNVERIFIED at EVERY use, and the count of uses was the first finding: the rate appeared in THREE places (the Exceptions citation, every scored row's `Expected` column, the Lodging tax-due line) and `fund 108` in SEVEN, so badging the one line the AMEND quoted would have left two uses unmarked. **The badge alone would have been cosmetic, so the rate check is HELD.** It is a meaning-shaped check (implied rate from the filing against the ordinance rate) and with its second input unsourced it turns a guessed number into a confident accusation against Bastrop's taxpayers; it now renders held and scores nothing, which also honours the scope card's freeze on computing any rate before the Azavar Q2 answer is filed. **Two derived figures would have kept the accusation alive:** the Exceptions tab counted 12 (3 rate + 4 outstanding + 5 late) in red on four boards and now reads 9, derived from the same flag that holds the check; the Unlabelled board hardcoded `'12'` inline and was missed by the first pass until a hand count caught it. And Lodging's `Tax due on that base, $1,282,946` is exactly the taxable base times 7.00%, manufactured from the unsourced rate and toned green, now neutral. **Instrument:** `_design/smartcity-finance-filings/check.mjs`, 23 self-tests both directions, finds every citation value from its OWN registry rather than asking the generator where its tags are (C1 and C3 meaning-shaped; C2, C4, C5, C6 internal consistency, stated as such). `violate.mjs`: 13/13 plants caught on real boards, each asserted to have changed the file, each followed by a clean pass, including the hardcoded 12. **Pre-registered falsifier: the check must not pass the original, unpatched boards. It refuses them, exit 2.** Design gate R3 drops 5 to 4. **Leave-behind:** the published artifact `Vo3LW9rEKzPsLtMo7QzQRw` and the local combined export predate this and still show taxpayers being scored; republishing is outward-facing and awaits the operator. G-137 still waits on the Azavar credentials. |
| G-139 | 5 | Lane B: **the camera connection the city reported broken: the code is built and correct, and no credential exists anywhere.** `smartcity-os` `server/routes/verkada.ts` implements auth, token cache, fetch/post/patch/delete, binary thumbnails and a TTL cache, and it **fails closed correctly** — `/api/verkada/health` returns `{connected:false, status:"not_configured"}` when the key is unset rather than inventing a camera state. Verified 2026-09-15: no secret matching verkada/esri/arcgis/camera exists in `smartcity-os-prod`, `hauska-prod-497015` or `legacy-design-tools-prod`. This is vendor onboarding, not a defect to debug, and it is the one domain in the whole city pack with no credential anywhere. On v2 the `police-cameras` domain is a declared fixture and is badged as one, which is the honest state and is not the thing to fix. **Not yet verified and in scope:** what v1's `CameraDashboard.tsx` actually renders when health returns `not_configured` — the route's honesty does not prove the surface's. | B | `/api/verkada/health` returns `connected:true` with a camera count on the traffic-serving revision, read by field from the traffic JSON and never from a positional formatter; and, before the credential exists, the v1 camera surface renders a declared not-configured state — verified by violation by unsetting the key and confirming the surface says so rather than showing empty or stale | Verkada vendor onboarding, a real-world action, operator-owed | OPEN — carded 2026-09-15 |
| G-140 | 5 | Lane B: **the zoning on the map comes from a layer the City of Bastrop stopped editing on 2023-04-28.** Full evidence in `_inbox/2026-09-15_bastrop_zoning_layer_findings.md`. The reported symptom was one property changed from commercial and not reflected on the map; the mechanism is that `const BASTROP_ZONING_URL` in `smartcity-os` `server/routes/esri.ts` points at `PlaceTypesCharacterDistricts/FeatureServer/1`, last edited 2023-04-28, 565 features, while the city publishes `Zoning_Place_Type/0` (2025-06-02, 574), `Zone_Types/25` (2026-07-09, 595, described by the city as *"the new zoning districts that will replace the B3 PlaceType code (2025)"*) and `Zoned_Parcels/83` (2026-07-23, 7,125). **No failure mode this product can detect is present**: the query succeeds, the layer is healthy, the fields are all there, and the answer is three years and four months old. This is the silent half of G-136 — G-136's fix, tolerating a missing field, would not catch a layer that is merely superseded, because nothing is missing. A cache was rejected as the explanation by reading the write path: all three zoning routes `fetch` upstream per request and hold nothing. Eight Bastrop layer URLs are hardcoded in `esri.ts` with no vintage check on any of them; the FIX is scoped to zoning, the INSTRUMENT covers all eight. v2 inherits this unchanged: `smartcity-dashboards` consumes `placeTypeClass`/`PlaceTypeDesc` through the platform seam, so ONE upstream fix serves both surfaces. | B | `node scripts/govtech/bastrop-zoning-layer-vintage.mjs` exits 0 — it derives the read URL from the product's own source at a named ref and compares it against every zoning layer the city publishes live, so no single source can satisfy both halves. Built and run 2026-09-15: 17/17 self-tests both directions, 4 inputs matched, verdict STALE, exit 1. Extend to the remaining seven hardcoded layers and put it in a gating position | A question of fact to Bastrop GIS: which layer is authoritative, and whether the 2025 draft has been adopted. We do not pick — rendering a layer named `Draft_11_2025` to staff as their zoning is the same defect in the other direction | OPEN — carded 2026-09-15 |
| G-141 | 5 | Lane A: **"drop any link, Google Drive or OneDrive, into one database" has no server path at all.** Described to the customer on the Jaime call as a capability. Verified at `smart-files` `origin/main` `61c84f61`: zero hits for `googleapis`, `oauth`, `drive`, `onedrive` or `sharepoint` across `src/` and `package.json`; thirteen routes in `src/server.mjs`, none of them a link or account ingest; and `POST /api/smart-files/folders/:id/files` requires `bytesBase64` and returns 400 without it, so the only ingest is bytes-in-hand. This is an ABSENT path, not a partial one, which makes it a build rather than a finish. The product is already honest on its own face — `web/app.js:705` toasts *"Bring files is chrome on this card. The designed states are already on this page. No Drive sync runs."* — so nothing improper has shipped; the exposure is that it is the most clickable thing in a demo. **Close it or qualify it before it is shown**, and the qualification is cheap while the build is not. | A | Either: a real link resolved to stored bytes with the five-key provenance the design requires, the write refusing when provenance is incomplete, verified by violation; or the demo and the collateral carry the qualification and a check refuses to emit the capability claim without it — the same BANNED-guard shape the review-artefact build already proved | Operator ruling on build vs qualify. Qualify is available today and blocks nothing | OPEN — carded 2026-09-15 |
| G-142 | 5 | Lane B: **Citizen lens design.** The only `public-free` surface and the only one a resident ever sees, which makes it the widest-audience surface with no design. It is NOT a blank: on `smartcity-dashboards` `origin/main` `f776b4bf` it ships with real markup, badged `Preview` in the nav, and its address lookup is deliberately disabled with the basis printed on the page — *"Lookup returns nothing today and the control is disabled rather than silent. It answers from public record once a source is connected."* That is already the correct treatment of absence and the design must not regress it. Read the history before drawing: `src/shell-homes.mjs:146` records *"Citizen service requests — Citizen lens. The twelve-tile grid was dropped."* | B | An adversarial read run as a file, self-testing in both directions with a non-zero matched-input count, against `smartcity-dashboards` at a named ref — the standing rule for every design in this thread. Nobody named on the canvas; fixture badged as fixture; absent, zero and unmeasured never collapsed | Nothing |**CLOSED 2026-09-18** — `_inbox/2026-09-18_g142-citizen-lens_close.json`, CP1 and CP2 filed, lane released. `_design/smartcity-citizen-lens/`, DRAFT, drawn against `smartcity-dashboards` `origin/main` `7487d7c0`. **Verified by the planner rather than taken on report:** `node _design/smartcity-citizen-lens/check.mjs` exits 0 with all 14 matched-input counts non-zero; `violate.mjs` catches 16 of 16 planted violations in about four seconds; and `design-completion-gate.mjs` no longer reports `lens:citizen` under R4. **The premise it rests on is a zero read from the product's own source:** 0 of 11 `DOMAIN_REGISTRY` entries carry `lensId: citizen` and 0 of 3 lens route literals name it, so this is the lens with the least behind it, which is exactly where absent, zero and unmeasured are most tempting to collapse. Three attribute shapes are kept distinct and each is planted as its own violation. **The finding came off the SHIPPED page, not the design:** `web/index.html`'s `Your requests` panel prints the quiet pill `None on file`, which is not in the badge vocabulary `src/ui.test.mjs` declares (`Empty / Not built / Not read / Preview / Not connected`). Not fixed here (the product is read-only to this row) and handed to leave-behind. **Falsifier filed and unresolved:** `Not read` may itself read as `not read yet`, i.e. as a queue the product does not have and never will, which would move the collapse rather than remove it. Ratification is owed; the blanket 2026-09-17 approval predates this design and does not cover it. This was the front of G-146's declared order|
| G-143 | 5 | Lane B: **People and access design — the same surface as the WorkOS build, so design and build land together.** In the shipped nav and marked NOT BUILT. Four things, not one: provisioning, role assignment, offboarding, and the audit trail that answers *who looked at that citizen's record*, which the cutover card records as unanswerable today. For a government customer the audit view is a trust surface, not an admin screen, and it is the half most likely to be dropped as "internal". Designing it against G-132/G-134 rather than after them is the point of the row. | B | Adversarial read as a file against the staff-identity source at a named ref, both directions, non-zero inputs; every role and permission name on the canvas present in the product's own vocabulary and no invented ones — the `smart-files` check is the working precedent | G-132 (staff authentication) for the vocabulary it must draw against; not blocked on the WorkOS credentials | **DESIGNED 2026-09-18, DRAFT awaiting the operator's ratification** (drawn after the 2026-09-17 blanket approval, which does not cover it). `_design/smartcity-people-and-access/`: five boards drawn against `smartcity-dashboards` `3d3ec62a` through a `source-state.json` snapshot read with `git show`, so no role, status, refusal or latency is typed by hand. `check.mjs` 26 self-tests both directions and twelve board-against-source rules; `violate.mjs` 20/20 plants caught on real boards, including the four this surface must never do. Design gate: `work:people` covered, uncovered surfaces 3 to 2. **Five findings, all read from source, in A-145.** |
| G-144 | 5 | Lane B, C: **the plan-review role-gate deferral has no mechanism, and this row IS the mechanism.** `_decisions/2026-09-14_staff_identity_and_department_rbac.md` gates plan review to Development services and city-manager. Departmental parallel review — described to the customer on the Jaime call in those words, *"fire review will be able to use theirs and zoning and structural"* — needs Fire and EMS, Public works and Parks to reach that surface. The amendment was DEFERRED 2026-09-15 with a trigger, and its own decision record states the problem plainly: **the only trigger is that somebody remembers before launch, which by the three-question gate is not a control.** Carding it answers the gate's three questions — what executes it: the launch gate script; what triggers it: the launch close; what fails: the close, while this row is open. | B, C | The launch-gate check fails closed while G-144 is open, verified by violation: run it with the row open and confirm a non-zero exit, then with the row closed and confirm it passes. A gate observed only passing has not been observed working | G-127 (RBAC by department) for the enforcement it gates | OPEN — carded 2026-09-15 |
| G-145 | 5 | Lane B: **the five remaining undesigned lenses, now a migration obligation rather than a backlog.** Public works, Parks, Police, Fire and EMS, Fleet. v1 is being retired, not run alongside — stated to the customer: *"the version two dashboard. I'm retiring the one you're working off of."* These five exist in v1 today, so if v2 ships without them the cutover REMOVES capability from departments that have it. Jaime named engineering and fire as likely flood-study users and Sylvia may add directors, so at least two of the five have named demand. Police and Fleet were previously "not in the pilot"; v1 retirement is what changed their status, and whether they are in scope is an operator call, not an inference. **Check each one at source before proposing it** — the Citizen lens turned out to ship with real markup and a correctly declared absence, and Connections was proposed on two premises that were both wrong. | B | Per lens, an adversarial read run as a file against the shipped surface at a named ref, both directions, non-zero matched inputs; and a source-state note per lens written BEFORE the design, not after | Nothing. The scope call was made 2026-09-15 (A-138, `_decisions/2026-09-15_police_and_fleet_lenses_in_scope.md`) and this blocker should have cleared then | **CLOSED 2026-09-16, row status corrected 2026-09-17 (A-142) — it had read OPEN with an already-cleared blocker while THREE close artifacts sat in `_inbox/`.** All five lens designs landed: `_inbox/2026-09-15_g145a-department-lenses_close.json` (Public works, Fire and EMS, Parks), `_inbox/2026-09-16_g145b-police-lens_close.json`, `_inbox/2026-09-16_g145c-fleet-lens_close.json`, all `status: closed`. A-139 declared the work complete by instrument on 2026-09-16. **This is a DESIGN row; the builds are G-151, G-152 and G-153.** |
| G-53 | 5 | Lane A: Smart Files sellable at the set price (25,000 dollars entry) | A | Deployed surface plus collateral drawn only from the doc 34 approved claims | G-34, G-44 | OPEN — price SET 2026-08-10 |
| G-54 | 5 | Program: govtech stack tested end to end — the seam table verified seam by seam | all | Each seam row probed live and recorded | all lanes | OPEN |
| G-55 | 5 | Watch external name for the municipal buyer (GovTitle retired without a replacement) | B | Name ruled plus catalog-thesis check | none | OPEN — owed naming call |

## AMENDMENTS (append-only; never edit the baseline)

| ID | Date | Change | Reason | Ruled by |
|----|------|--------|--------|----------|
| A-000 | 2026-08-14 | Baseline v1 frozen | Program stood up with its plan of record at hour zero, per the explicit lesson in the spine unification handoff | nick |
| A-001 | 2026-08-14 | G-06 CLOSED. `scripts/dispatch.mjs` is plan-aware: `--plan OPS-16\|OPS-17`, default OPS-16 so existing usage is unchanged. Row-prefix guard added so a `P-` row compiled against OPS-17 (or the reverse) fails closed instead of silently missing the table scan. | Two programs now run concurrently against one compiler; the plan path was hardcoded | planner (mechanical, no scope change) |
| A-002 | 2026-08-14 | **ASSUMPTION 2 FALSIFIED. Lane A is a BUILD, not a rendering pass.** G-03 closes with verdict BUILD. G-14 re-scoped from "wire a surface to a substrate" to "design and build the city-file-system family". G-53 now gates the Smart Files SALE, not just its surface. | G-03 verified at source: the workspace family is a BROKERAGE feature (agents saving listings via a browser extension), not a city file system — contract description "Brokerage workspace packaging contract types for V1", engine registry line 819, MCP "Brokerage tool 1/2/3". Planner re-verified the load-bearing schema independently: `brokerage_workspace_attachments` has a single notNull FK with cascade delete and **no `updated_at`, no `version`, no `cid`, no `access_policy`** (**8** columns, `lib/db/src/schema/brokerageWorkspaces.ts:54-76` — the planner first said 9 and miscounted; corrected by the G-09 proving run and re-counted at source. The BUILD verdict is unaffected: no `updated_at`, `version`, `cid`, or `access_policy` all confirmed). "A document lives once and appears everywhere it belongs" is therefore structurally impossible — one attachment belongs to exactly one workspace, so many placements means many copies, the exact problem Smart Files claims to solve. "Revise once, prior version still there" has no schema at all: only insert and delete exist. Production: 142 workspaces, **0 attachments** — the files half has never been written to. | nick (operator stated Smart Files needed building at program open; the doc-34 self-assessment carried into assumption 2 was the planner's error) |
| A-003 | 2026-08-14 | **OR-1 RULED: claims register NOT revised. The build is made true before any customer sees it.** The three rows stay as written; lane A delivers what they promise. Consequence recorded: **doc 34's approved claims are now gated on the G-14/G-53 build**, and no Smart Files collateral may ship ahead of it. Doc 34 line 32 ("built and running in the command center") is false as written and is corrected as a scoped in-place edit. | Operator ruling. The claim is the product commitment, not a drafting artifact; the fix is to build it, not to soften it. Price is already submitted to Vertosoft at $25,000 entry | nick |
| A-004 | 2026-08-14 | **CTRL-1 + CTRL-2 recorded as program defects; fix dispatched to G0 (not planner-patched, to avoid a concurrent-edit collision).** CTRL-1: `canon-gate.ps1` `Test-PlanRows` greps only `P-\d+`; an empty match returns `ok`, so **every OPS-17 dispatch has passed PLAN-ROW validation unvalidated**. Planner reproduced: `G-9999` and `Q-42` (rows existing nowhere) return `ok`, while `P-9999` correctly rejects — which is why the gate looked alive. CTRL-2: `Work in P:/smartcity-os` passes open on the absolute-no-touch live-Bastrop-production repo while three other phrasings block. Both fixes carry negative tests. **A divergence test between the hook and `dispatch.mjs` is required**, not just the two patches. | CTRL-1 root cause is a DESIGN SEAM the planner introduced: rule 2 chose the `G-` prefix so rows would "never collide ... in a grep, a hook, or a compiler", and that anti-collision choice is exactly what blinded the hook. The compiler is stricter than the gate enforcing it, and AGENT_CONTRACT section 7's blocking guarantee has been false for OPS-17 since rule 2 was written. Two implementations of one rule drift the moment one changes — hence the divergence test | nick (sequencing: fix before the four build lanes) |
| A-005 | 2026-08-14 | **OR-2 through OR-6 ACCEPTED as recommended.** OR-2: 40i and 11a are NOT covered by any 33a ruling (33a open-item 4 names exactly three targets: 47, 33, and the M4-B/PLR/SD/W vocabulary inside doc 30); both route to the operator rather than being guessed, and 11a's 10 inbound references mean no retirement without a `supersedes:` target. OR-3: **doc 30 stays `status: active`** — a scoped in-place correction marking the M4-B/PLR-1..28/SD-1..SD-8/W1-W6 section superseded-in-place, NOT a status flip, because the masters cite doc 30 four times as live source of record and it holds the only integrations inventory G-18/G-21 have. OR-4: ADR status vocabulary is a real convention gap (`01_doc_conventions.md` never ruled on ADR lifecycle); 16 ADRs affected including adr_023, which lane C depends on. OR-5: `premortem-check` — live config and CLAUDE.md contradict the memory that retired it 2026-07-13; resolve in one direction. OR-6: `.cursor/settings.json` tracked or ignored, never merely-unadded. | The planner's dispatch brief over-attributed rulings to 33a; the lane planner checked at source and pushed back correctly. Recorded so the correction is durable and the brief's error does not propagate | nick |
| A-006 | 2026-08-14 | **Baseline v0 shape adopted: every lane and shared leg carries an OWNER, and every lane carries a frozen WDLL CARD at dispatch time.** Owner column added to the lane and shared-leg tables below. | The operator's Smart Site v0 board ("each one now has a state and an owner") plus its WDLL practice ("everything in flight has a frozen acceptance card so drift is visible when it happens"). OPS-17 had state and instruments but neither owner nor per-lane acceptance card; with four hand-carried lane planners the shared legs would otherwise become nobody's | nick |
| A-007 | 2026-08-14 | **Lane A sequenced FIRST among the build lanes.** Three seams depend on it (B consumes A; plan review document handling rides it; Bastrop document capture is a Factory 1.5 workload needing the artifact store to exist). Open design ruling routed to G-10: **does Smart Files EXTEND the brokerage workspace family or SUPERSEDE it?** They are different products sharing a name — an agent saving listings against a city file system. Planner instinct is a separate family leaving the brokerage one alone; to be ruled deliberately, not by default. | Lane A was already first in dependency order; the BUILD verdict makes the sequencing consequential rather than incidental | nick |
| A-008 | 2026-08-14 | **G0-B closed: 41 of 41 directories mapped, none unmapped.** Deliverables `_catalog/repo_map.md` and `_catalog/repo_cleanup_backlog.md` (24 items, P0-P3). Four operator rulings executed this session, all planner-verified at source first. **R-A: the root `.vercel` link is REMOVED** — it linked doc_repo itself to a Vercel project named `doc_repo`, so a root `vercel deploy` would have published prospect dossiers, pricing, investor letters, and decision records; gitignored so it never propagated, but live locally. `system-overview-site/.vercel` (`empressa-overview`) is legitimate and untouched. **R-B: `_smartsite_masters/` NOW TRACKED** — nine `status: active` masters governing the customer-facing product, two carrying approved-claims registers and never-say lists, were entirely untracked with no ignore rule matching; they existed on one machine and would not survive a clone. This binds A-003 directly: the ruling that doc-34 claims stay as written and get built true is only enforceable if the register survives a clone. PDFs stay ignored per existing convention. **R-C: the shadow `Master Collateral Folder/_smartcity_masters/` is DELETED** (backed up to `_scratch/removed_2026-08-14/`, it was untracked so no git history existed to recover from) — it differed from the ratified root set in five of six files, root newer on all five, and listed government pricing as "an operator decision" while the authoritative root says SET 2026-08-10 with prices already at Vertosoft as MSRP. **R-D: `_inbox` retention R2/R3 adopted, R1 held** pending its two prerequisites. | Cartography found two hazards that were on nobody's brief. The scope correction is the durable lesson: G0 covered **846 of 1,955 markdown files (43.3%)** and published its exclusion list, but the "366 violations" headline travelled without its denominator — a process defect, not an agent defect, and now a G-08 requirement that a coverage figure travels with its denominator or does not ship | nick |
| A-009 | 2026-08-14 | **Command Center gets authoritative docs (new row G-19).** Cert View wiring — where a required certification gate physically happens and which may be unwired — goes to the cleanup backlog until it surfaces in real work. **The two in-repo clones are NOT deleted**: `hauska-mcp-server/` (already ruled in `.gitignore:12-13` "delete the clone", never executed) and `tmpbrief-l3-spine-consume/` (genuinely unrecorded, a clone of a different repo). Both stay untouched at their original HEADs pending an inspection of contents. | Operator: "i dont want to delete either of those without knowing whats in them. our MCP server is half of our business model." A ruling recorded in a gitignore comment and never executed is the same 0-for-3 prose-control shape this program exists to fix — but deleting a clone of a business-critical repo without reading it is the larger risk | nick |
| A-010 | 2026-08-14 | **G-09 PROVING RUN CLOSED. Verdict: the rulebook HOLDS as a behavioral control and FAILS as a schema.** Every behavioral rule that could fire, fired, and each caught something real; but the close schema is shaped for a build lane in a code repo, so all three sub-agents had to invent conventions to file a doc_repo close. The operator's criterion — a fresh agent with no thread context reaching the same conclusions — was **met on findings, missed on accounting**. Fixes applied this session: (1) **DEV_PROCESS 1.1 corrected 852→846 / 43.6%→43.3%**, planner-verified at source (852 appears ZERO times in the G0 close; its own metric reads "366/846") — the rule written to stop a number escaping its counting rule did exactly that with its own illustrative figure, and the planner propagated it from a lane report without checking. (2) **A-002 corrected 9→8 columns**, re-counted at source; the BUILD verdict is unaffected. (3) **AGENT_CONTRACT 1 now states the fan is exactly ONE level deep** — the contract was silent on whether a sub-agent may spawn while `dispatch-template-gate.ps1` forbade it outright, and the hook blocked all three of the lane planner's own dispatches (position-checked, not presence-checked). One rule, two implementations, disagreeing: the CTRL-1 shape again, with the hook stricter than the prose. (4) **AGENT_CONTRACT 6 gains three non-accounting fields** — `missionPremise`, `completionPredicate`, `scopeBasis` — because the forced-handoff branch produced a correct deliverable only by the successor independently re-reading OPS-17; the handoff artifact never carried the A-002 premise. (5) **DEV_PROCESS 3.2a** attaches an evidentiary standard to corrections: the correction instrument ran **7-for-10**, and rule 3.2 as written rewarded producing discrepancies without one. (6) **DEV_PROCESS 2.0**: a permanently-red gate is a dead gate — 1,036 of 1,141 staleness violations sit in append-only trees where an age check is meaningless; the actionable number is 105 of 494 in canonical doc space. | The most consequential save was SA-2 refusing a literal reading of its own brief: the four-line edit the brief described would have satisfied it exactly while pulling 102 append-only `_decisions` and 26 `_research` records into an age check, breaking the gate. Treating the brief as fallible is rule 3.2 working | nick |
| A-011 | 2026-08-14 | **Deliverable 4 of the G-09 dispatch (time-to-first-useful-action) RETIRED on the lane planner's recommendation.** It is self-reported, which rule 0 says is not a control, and it rewards the wrong behavior: three of SA-1's seven findings came from reading carefully rather than quickly, and a speed-optimizing agent skims backlog row 22 and misses its trap. **Backlog row 22 FAILS OPEN and is re-opened**: it names the wrong file for the band table and attributes another file's stale date to it, so an agent actioning it marks the row resolved while the real register stays stale. Twelve proposed diffs PD-1..PD-12 filed; six applied this session, the rest carried. | A metric that cannot be verified and that penalizes the behavior we want is worse than no metric | nick |
| A-012 | 2026-08-14 | **G-10 RULED: Smart Files is a NEW atom family. It does not extend `brokerage_workspaces`.** And **owners assigned**: nick owns all four lanes and all six shared legs, with a hand-carried planning agent executing each; the A-006 blocking state clears. The `brokerage*` RENAME is backlogged as its own focused lane (`_catalog/repo_cleanup_backlog.md` item 25) and is explicitly NOT part of lane A. | Operator: the brokerage CONCEPT is an early framing that is irrelevant. Verified at source what the name still reaches: **126 tracked files in legacy-design-tools (72 non-test), 8 database tables, and 54 NON-brokerage files importing brokerage modules** — including `app.ts`, `index.ts`, the atom registry, `spineZoningDistrict.ts`, `encumbranceService.ts` and `cadPropertyLookup.ts` (counting rule: `git ls-files \| grep -i brokerage` on main, tracked only). The name is dead; the code under it is the property reasoning substrate — federal and composite GIS layers, parcel keying, brief atoms, **metering (the ICC money path)**, entitlement, and provenance envelopes. So a new family for three reasons: the existing schema cannot carry the Smart Files promise regardless (single-parent FK, no `updated_at`, `version`, `cid`, or `access_policy`), so extending means rebuilding anyway; an in-place rename across 54 dependents is a high-risk refactor that would compete with lane A's real work; and `brokerage_workspaces` holds 142 live rows — dead concept, live data. Two known live items sit inside that surface and should be resolved WITH the rename: the 0.74 motivated-seller fixture in `brokerageGisCompositeLayers.ts` and the install-keyed `brokerage_install_claims` that the Radar ruling says must become user-aware | nick |
| A-013 | 2026-08-15 | **OR-A1 RULED: the Smart Files contract type is authored LOCALLY in legacy-design-tools for its first iteration, with a NAMED promotion step to `@empressaio/atom-contract` once the shape settles. Promotion criterion: the type promotes when G-34 closes** (typed absence is the last thing that reshapes the shape); if a second consumer needs it before then, that forces early promotion and is a FINDING, not a violation. **OR-A2 RULED: REUSE the existing CID and document-ingest mechanism with a NEW parent table** — reuse the mechanism, never the parent. | OR-A1: freezing an unsettled shape into a published cross-repo contract is the more expensive mistake, and the contract has consumers beyond this lane; once a type ships, changing it becomes a coordinated multi-repo migration. The cutover that would fix the underlying drift has a blast radius adjacent to the backlogged rename, which is ruled out of this lane. **Drift verified at source and the planner's first read was WRONG: the vendored tarball DOES exist** (`vendor/hauska-atom-contract-1.6.0.tgz`, installed at 1.6.0) — an `ls ... \| head -3` truncated it out of view. So LDT sits on vendored 1.6.0 against npm 1.22.0: the drift is real, nothing is broken, and the lane is DECLINING to widen scope rather than being blocked on a repair. OR-A2: `dataroom_document_atoms` already carries `atomDid`, `accessPolicy`, `sourceDocumentCid`, `confidence`, `verificationStatus` and an idempotent-upsert uniqueness index (verified at source), so re-minting content addressing beside a working deterministic-DID pipeline is the operate-not-rebuild failure. Its parent `attached_documents` carries the IDENTICAL defect as the brokerage table (single notNull cascade FK, no `updated_at`, `version`, or `access_policy`), so inheriting the parent would reproduce the problem lane A exists to fix | nick |
| A-014 | 2026-08-15 | **Lane A G-14 BUILT AND MERGED, and DELIBERATELY NOT CLOSED. Operator ruled HOLD on applying the migration.** PR #430 merged at `7bb79248` (planner-verified: MERGED, and every `statusCheckRollup` conclusion string SUCCESS — Test, Typecheck, Rubric, and five L5/L17 greps). Three tables: `smart_file_documents` (identity), `smart_file_versions` (content, append-only), `smart_file_placements` (location, many-to-many). **Placements reference the DOCUMENT, never a version**, so revise-once-current-everywhere is structural rather than a per-placement fan-out that can partially fail. entityId `smartfile:<jurisdictionFips>:<docSlug>` — decision record `_decisions/2026-08-15_smart_files_entity_id_shape.md`, closing lane A's half of G-10. **G-14 remains OPEN pending: apply `lib/db/drizzle/0078_smart_files_foundation.sql` to the deployment database, then refresh the fixture from live.** | The lane refused to call G-14 closed, correctly. **CI never executes `.sql` migration files** — planner-verified: no `drizzle-kit` step exists in any workflow, and migrations apply only through the `run-migrations` job in `cloud-run-deploy.yml` against `_schema_migrations` tracking. Green CI proves the drizzle schema, the fixture, and the pushed database AGREE; it does not prove 0078 EXECUTES. That is the merged-is-not-applied trap and the WDLL card retained the check on purpose. Operator ruled HOLD on the apply: it is a production schema change on a shared database, and deploys being planner-owned does not make an unrequested one appropriate | nick |
| A-015 | 2026-08-15 | **Smart Files is a spine MODULE, not a city-only store. Identity reversed before apply.** entityId is `smartfile:<scopeType>:<scopeId>:<docSlug>` with closed scopeType `jurisdiction` / `tenant` / `site`. Successor decision `_decisions/2026-08-15_smart_files_module_identity.md`. The same-day FIPS decision is superseded. Three-table split KEEP. Typed absence KEEP. G-10 (lane A half) re-opens until the new builder/parser is in code. G-14 stays OPEN until (a) identity matches this ruling and (b) 0078+0079+0080 are APPLIED. **A-014 HOLD LIFTED** by operator go to complete Layer 1; apply is planner-owned and waits until the identity PR is merged. **A-014 CI claim CORRECTED:** migrations execute only on `workflow_dispatch` `run-migrations`, never on the merge path (F-A2-CP1-1). A-013 promotion criterion has fired (G-34 closed) but promoting the FIPS shape is forbidden; promotion waits on this identity. G-44 is blocked until the key is right. G-53 is not the module definition of done. Do not dispatch B/C/D or G-30 in the L1 completion wave. | Operator correction 2026-08-15: Smart Files is used across SmartCity, Smart Site, Mox/custom, and a la carte. A FIPS-only key cannot address those scopes. Sequencing correction: do not apply 0078 first (that hardens the wrong key); do not wait on ICC/G-30; do not treat Layer 0 process or G-34 merge as L1 complete. | nick |
| A-016 | 2026-08-15 | **G-56 ADDED.** Layer 1.5 CC-done data room. Frozen WDLL `_inbox/2026-08-15_a_wdll_cc_done_l15.md`. Files are one atom type; folder membership is edges (`_decisions/2026-08-15_file_set_edges_not_identity.md`). SDK is question 3 and is not this row; ICC is the first real SDK customer (`_decisions/2026-08-15_icc_first_sdk_customer.md`). G-53 gains G-56 as an additional blocker (baseline G-53 row is not edited). G-19 stays the CC docs row. G-50 stays the first SDK-customer row. Mountain `_thought_leadership/04_positioning_narrative.md`. Mapping `portfolio_thesis/03_three_questions_data_room_and_sdk.md`. | Operator: CC-done now, customer-done waits on SmartCity/Bastrop. Vision document 2026-08-15. Rulings 1-4 approved. | nick |
| A-017 | 2026-08-15 | **Smart Files HOME is an independent module, not cortex-prod.** Own repo, own database, own service. SmartSite is one consumer among any (a la carte Joe Smith). Live SmartSite save/draw/share is a get-by and is not Smart Files. Knit (user + place + private data room + public spine read) is application-layer only. Default file accessPolicy is tenant-private. A-013 LDT location is prototype, not store. G-56-on-Cortex is disposable UX. No further Smart Files writes to cortex-prod. Do not drop 0078-0081 during L26. Decision `_decisions/2026-08-15_smart_files_independent_module.md`. Isolation WDLL owed before any new repo scaffold. | Operator: Texas ingest and the filing system must be physically unable to touch each other. Complementary in SmartSite UI, partitioned in wiring. | nick |
| A-018 | 2026-08-15 | **G-58 ADDED. Isolation WDLL approved.** Frozen card `_inbox/2026-08-15_a_wdll_smart_files_isolation.md`. Repo name locked `hauska-smart-files`. Mount-composition decision `_decisions/2026-08-15_capability_mount_composition.md`: capabilities mount into one interface (digital twin, Smart Files, SmartSite context, closing documents, tokenization if that later proves real). Same pattern for title company, city, builder, agent, buyer, seller. Bastrop example is operator-attributed pattern, not a live-probe claim. Tokenization / closing vault / twin authoring are future mounts and are not G-58 acceptance items. G-56 stays the Cortex prototype grade. G-53 stays customer-done. Do not scaffold until G-58 is this row (now true). Do not touch L26. Do not DROP 0078-0081 on this row. | Operator: card approved; capture the mount vision because it shapes how isolation is built (module plus mount, not a Cortex data room). | nick |
| A-019 | 2026-08-15 | **Repo name relocked to `empressaioemail-tech/smart-files`.** Operator created https://github.com/empressaioemail-tech/smart-files (PUBLIC, 2026-08-15T19:42:07Z). A-018 `hauska-smart-files` recommendation is superseded on name only. **RWA positioning:** we are not RWA creators; RWA operators mount their asset onto our provenance, Smart Files, and map (`_decisions/2026-08-15_rwa_mount_not_create.md`). Neon: new project in Empressa org, not a database on `fancy-fire-06136146` / cortex-prod or `tiny-art-63602898` / smartcity-os-prod. GCP: new project (recommended `smart-files-prod`), not a Cloud Run service on `legacy-design-tools-prod` or `hauska-prod-497015`. RWA mint / chain adapter is not a G-58 item. | Operator created the remote; asked for Neon + GCP; captured RWA mount-not-create as the legitimate sell. | nick |
| A-020 | 2026-08-15 | **Smart Files is an Empressa product.** Decision `_decisions/2026-08-15_smart_files_is_a_product.md`. Isolation (own repo, own DB, own service, mountable) is how the product is housed, not a demotion to substrate. Planner thesis-yellow "do not sell Smart Files as a product brand" is withdrawn. G-53 remains the sale. G-58 acceptance items do not change. | Operator: "smart files is a product." | nick |
| A-021 | 2026-08-15 | **Smart Files infra ids locked.** Neon: `smart-files` / `snowy-bread-83475727` / `ep-winter-shape-aw8ken54`. GCP: `smart-files-505619` (name `smart-files`, number `529170139834`, created 2026-08-15T19:47:56Z). Operator said "smart-site" for both; live names are `smart-files`. No GCP project id `smart-site` on `empressaioemail@gmail.com`. Do not deploy this product into `hauska-prod-497015`, `legacy-design-tools-prod`, or `smartcity-os-prod`. Cloud Run not enabled yet. | Operator created the projects; planner verified at source. | nick |
| A-022 | 2026-08-15 | **G-58b ADDED** (cortex-prod `smart_file_*` DROP after L26 quiet). Item 5 store applied on files Neon while Cloud Run waits on billing quota. Isolation seed entityIds are not on cortex-prod. | Operator: keep moving on Smart Files while Google quota is pending. Do not take the atoms slot. | nick |
| A-023 | 2026-08-15 | **G-59 ADDED.** Smart Files QA rooms on the files service plus a Vercel UI. Cortex stays unmounted (G-58 item 9 stands). Write MCP tools are a named follow-on, not this row's live grade. Personas are QA actors, not G-11. | Operator: prove create/upload/share/org at the Smart Files level; Vercel UI for QA; do not fish the flow through Cortex. | nick |
| A-024 | 2026-08-16 | **G-60 ADDED. ICC demo WDLL approved.** Frozen card `_inbox/2026-08-16_icc_demo_program_WDLL.md`. Equation: complete plan review (F1-F7 + map + letter + files) + finished MCP = ICC, watched on `/icc/activity`. Housing: repo `empressaioemail-tech/plan-review` (PUBLIC, createdAt 2026-08-16T15:03:02Z), GCP `plan-review-505715` (name `plan-review`, number `364754576784`, ACTIVE), Neon DSN at `%USERPROFILE%\.empressa\plan-review.database_url`. Decision `_decisions/2026-08-16_icc_demo_is_the_joint_done_line.md` active. G-50 signed-SaaS half stays OPEN. Applicant portal / Bluebeam / visual design / Circle / DNS / directory stay out. Do not steal L26. Do not DROP G-58b. | Operator: WDLL approved; still-out confirmed; repo and GCP created; go on the rest. | nick |
| A-025 | 2026-08-16 | **Lane C serving home isolated (same move as A).** Baseline lane table still names LDT as C's primary repo (ADR-023 function package). Serving housing is now `empressaioemail-tech/plan-review` + GCP `plan-review-505715` + own Neon, per G-60. G-15 "live ldt" host is superseded for grading: probes are the plan-review Cloud Run / Vercel. G-51 standalone gate is still the C Layer-5 row; G-60 is how that gate plus D's PoC demo plus finished MCP are built together. G-50 SaaS half unchanged. Alignment `_inbox/2026-08-16_ops17_four_lane_alignment.md`. Foundation SQL applied on plan-review Neon (`plan_review_*` tables=4, db=neondb). S-5 not closed; this wave reuses the A HTTP+MCP pattern. Lane B remains last / no-touch. | Operator: four-lane program; we just did Smart Files; align C+D. | nick |
| A-026 | 2026-08-16 | **Lane C extract-then-remount.** Plan-review BFF currently on cortex-api is pulled into `empressaioemail-tech/plan-review` (elevate existing callables, do not rewrite F1-F7 from 501s). Cortex `/api/plan-review` remounts as a proxy, not a 404 (unlike G-58 files). Calibration left as-is. Cotality stripped on intake/geocode. Sheets for new rooms go to Smart Files. Decision `_decisions/2026-08-16_plan_review_extract_and_remount.md`. Inventory `_inbox/2026-08-16_plan_review_cortex_callable_inventory.md`. ADR-023 serving home is this housing; ADR body not edited. Dirty LDT is not the remount vehicle. | Operator: pull cortex functions into plan-review as a whole, then remount on cortex. | nick |
| A-027 | 2026-08-16 | **Plan review is Smart Files' first product consumer.** Documents, sheets, and dataroom atoms are Smart Files file-shaped atoms in tenant `icc-demo`. Do not copy cortex attachedDocuments / GCS / `dataroom_document_atoms` into plan-review Neon. PE isolation mount remains a probe; live PE save/share is still not Smart Files. Decision `_decisions/2026-08-16_plan_review_is_smart_files_first_consumer.md`. | Operator: plan review should be Smart Files first consumer; documents and dataroom atoms all Smart Files based. | nick |
| A-028 | 2026-08-16 | **G-60 does not wait on L26.** Store UPDATE of existing ICC atoms (G-30 / G-17) and F4 engine ingest stay named residuals. Demo continues slot-free: MCP read-path withhold of ICC for anonymous callers, then walk / close. No second `--apply`. No G-58b DROP. Decision `_decisions/2026-08-16_g60_does_not_wait_on_l26.md`. WDLL A-008. | Operator: L26 will not be quiet for a while; work around it. | nick |
| A-029 | 2026-08-16 | **Smart Files gains a SECOND product consumer: Smart Markets** (the instrument twin, `_rd_disclosure_twin/08_build_scope.md`). It adds an `instrument` value to `scope_type` and to the placement `target_type`, plus a new scope validator in `src/identity.mjs`, keyed on the cockpit security-master node id (`_decisions/2026-08-16_instrument_scope_identifier.md`). **No Smart Markets migration may land on the smart-files store without this lane seeing it first**, because Plan Review is live on that store on the `tenant` scope (A-027, G-60). Smart Markets is NOT registered as a plan of record and its `TW-` rows are not dispatchable; this row exists so the shared leg is visible to lane A rather than arriving as an unscoped migration. Findings from the second consumer return to the store as a divergence report (TW-23); the first is that the two-verdict `smart_file_absence_determinations` enum has no way to say a category is structurally inapplicable, which lane A may meet independently. | A second consumer is the first real generalisation test the store gets. Two consumers landing schema changes on one store concurrently is a collision hazard, and OPS-17 governing rule 6 says a shared leg is not owned by one lane. | nick |
| A-030 | 2026-08-16 | **G-60 map is live SmartSite. Share is a submitter data room.** Plan-review embeds `smartsite.cloud/?parcelNodeId=` (same surface as Property Explorer). Do not deploy plan-review from dirty `P:\hauska-map`. Reviewer upload stays on the engagement Smart Files folder. Share returns a read-only `smart-files-app` `#share=` URL for the architect, homeowner, or contractor. Not an applicant portal. WDLL A-009. **A-031 reverses the share host.** | Operator: same map as SmartSite; upload then share the room as a data room with the submitter. | nick |
| A-031 | 2026-08-16 | **Plan review owns files UI and applicant view.** Smart Files remains the backend store (A-027). Share host is plan-review-app `/applicant?token=`, not smart-files-app. A-009/A-030 share-to-QA-app is reversed. Planner does not seed more files objects. Applicant view is a token room on plan review, not G-52 MyGov and not a full applicant portal. Decision `_decisions/2026-08-16_plan_review_owns_files_ui.md`. WDLL A-010. | Operator: files function lives in plan review; Smart Files is backend; applicant view needed; should not have written to Smart Files yet. | nick |
| A-032 | 2026-08-16 | **REVERSED.** Had put ICC portal on plan-review `/icc/activity`. Successor A-033. | Operator later: separate portal and domain for paying IP access. | nick |
| A-033 | 2026-08-16 | **ICC-demo is a separate portal and domain.** Path for people paying to access licensed IP. Vercel project `icc-portal-app`. Alias https://icc-demo.vercel.app. Repo `empressaioemail-tech/icc-portal`. Plan review cites. Activity mounts plan-review over HTTP. No second ledger. No DSN. Apex/custom DNS held. Decision `_decisions/2026-08-16_icc_demo_is_separate_portal.md`. WDLL A-012. Successor name pin A-034. | Operator: ICC Demo is a path for all people paying for their IP to access; separate portal; separate domain. | nick |
| A-034 | 2026-08-16 | **Product name and host: ICC-demo.** Operator: just call it ICC-demo. Alias https://icc-demo.vercel.app assigned to `icc-portal-app`. WDLL 13 applicant token-room met on live reviewer share. G-60 demo path stop. L26 / store UPDATE / F4 / G-58b held. | Operator: (1) call it ICC-demo (2) WDLL 13 done and verified (3) hold L26 residuals and session-close. | nick |
| A-035 | 2026-08-17 | **G-61 ADDED.** Lane B product line then Bastrop onboarding. Decision `_decisions/2026-08-17_smartcity_product_line_then_bastrop_onboarding.md`. G-18 as-found stands; build-plan overlay: template Dashboards then migrate Bastrop; kill PermitFlow after Lane C is staff path; CitizenConnect is the citizen lens not a SKU; Parcel Intelligence hosts on SmartSite; Digital Twin replaced by Asset Management; Compass reworked; vendor feeds are templated adapters that write records. Next cards G-13 then G-61. WDLL drafts `_inbox/2026-08-17_g13_consumer_contract_WDLL.md` and `_inbox/2026-08-17_g61_dashboards_template_WDLL.md` pending operator approval. G-21 remains OPEN and does not block the template. Live `P:\smartcity-os` stays no-touch. Do not start G-52. Do not fill G-24. Do not cut `/permitflow/*` on this amendment. | Operator: same page; file the decision; G-13 plus Dashboards template WDLL as next Lane B cards, not a Bastrop rewrite. | nick |
| A-036 | 2026-08-17 | **G-13 CLOSED.** Consumer contract `_decisions/2026-08-17_g13_consumer_contract.md`. Caller split: agents on one Hauska MCP; applications HTTP service-token or SmartSite embed; never a DSN; never a copied table. Pattern once; HTTP target named per supplier. G-61 WDLL approved same day and cites this id. Housing of the Dashboards template is unnamed; do not start G-61 build until housing is ruled. Live Bastrop unchanged. G-11 remains OPEN. Close `_inbox/2026-08-17_g13_close.json`. | Operator: both WDLLs approved. | nick |
| A-037 | 2026-08-17 | **G-61 housing locked.** One product repo, many cities as tenant packs. GitHub [empressaioemail-tech/smartcity-dashboards](https://github.com/empressaioemail-tech/smartcity-dashboards). Local `P:\smartcity-dashboards`. Isolation is accessPolicy plus tenant scope, not git. Live `P:\smartcity-os` stays the old weld until a named cutover. Do not clone `smartcity-os`. Do not deploy this product into `smartcity-os-prod`. Neon and GCP are not created on this amendment. MCP tools named in-repo for later retarget on the existing Hauska MCP server; no second MCP. Decision `_decisions/2026-08-17_smartcity_dashboards_housing.md`. | Operator created the remote and said done. | nick |
| A-038 | 2026-08-17 | **G-62 ADDED.** Lane B compose honesty after G-61 close. Unauthenticated city-manager compose must atom-read as anonymous (G-13 same accessPolicy). Live fail: gold compose lists `owner-fact`. Decision `_decisions/2026-08-17_g62_compose_honesty_before_g11.md`. WDLL draft `_inbox/2026-08-17_g62_compose_honesty_WDLL.md` pending operator approval. Do not dispatch as G-11. Do not start feed adapters on this card. Do not start implementation until the WDLL is approved. G-11 remains OPEN (longest pole). Live `P:\smartcity-os` stays no-touch. L26 untouched. | Operator: go on a thin compose-honesty WDLL, not full G-11; feed adapters after that. | nick |
| A-039 | 2026-08-17 | **G-63 ADDED.** Lane B feed adapter contract after G-62 close. Adapters write records onto spine or files with provenance. Kinds are a catalog; grants stay empty on template-city. Not a product SKU. Not a Dashboards vendor table. Not live MyGov/Samsara. Not G-24. Pipedrive refused. Decision `_decisions/2026-08-17_g63_feed_adapter_contract.md`. WDLL draft `_inbox/2026-08-17_g63_feed_adapter_contract_WDLL.md` pending operator approval. Do not dispatch as G-11. Do not start implementation until the WDLL is approved. Live `P:\smartcity-os` stays no-touch. L26 untouched. | Operator: keep going after G-62. | nick |
| A-040 | 2026-08-17 | **G-11 WDLL DRAFTED.** After G-63 close, Lane B next card is G-11 as city-pack tenancy, not live ingest and not sprint-54 T1-T3. Decision `_decisions/2026-08-17_g11_city_pack_tenancy.md` (provisional until WDLL approval). WDLL `_inbox/2026-08-17_g11_tenancy_WDLL.md` pending operator approval. Identified caller is a Hauska product key whose `jurisdiction_tenant` equals `cityKey`. Fixture pack `fixture-city`. Service key is not a tenant. No `--apply`. No live MyGov/Samsara. Live Bastrop no-touch. Baseline instrument "every surface" is sequenced: this card grades Dashboards HTTP plus existing Hauska MCP plus compose accessPolicy; Cortex anonymous default tenant, claim flow, and live Bastrop stay residuals. G-33 and G-42 stay OPEN after this card; they need a Bastrop-authenticated caller this card does not mint. Do not start implementation until the WDLL is approved. L26 untouched. | Operator: draft the WDLL and commit. | nick |
| A-041 | 2026-08-17 | **G-11 WDLL APPROVED.** Status flipped draft to approved. Decision `_decisions/2026-08-17_g11_city_pack_tenancy.md` active. Dispatch compiled `--plan OPS-17 --lane B --plan-row G-11`. Live Bastrop no-touch. L26 untouched. Do not absorb sprint-54 T2/T3. | Operator: wdll approved. | nick |
| A-042 | 2026-08-17 | **G-11 CLOSED as sequencing.** City pack is the tenant. Live Dashboards `00005-m5t` fixture-city tenant-private grants []. Unauth 401. Service Bearer 403. Identified Hauska product key `jurisdiction_tenant=fixture-city` reads the pack. MCP `00082-mat` tag g11 GET `/auth/whoami` plus identified `dashboards_get_city_pack`. Probe keys revoked. Sprint-54 T2/T3, Cortex default tenant, claim flow, G-33, G-42 remain OPEN. Live Bastrop `00118-qox` unchanged. L26 untouched. Close `_inbox/2026-08-17_g11_close.json`. | Operator: Approved, proceed. | nick |
| A-043 | 2026-08-17 | **G-45 WDLL DRAFTED.** After G-11 close, Lane B next card is G-45: Dashboards staff map is the SmartSite embed of gold `48021:34137`. As-found: GET `/` iframe is `about:blank`; after Compose the PE bundle renders 908 PINE. Do not cut live Leaflet. Do not clone PE. hauska-map only if frame policy blocks (it did not on 2026-08-17 probe). Decision `_decisions/2026-08-17_g45_smartsite_staff_map.md` provisional until WDLL approval. WDLL `_inbox/2026-08-17_g45_smartsite_staff_map_WDLL.md`. Live Bastrop no-touch. L26 untouched. Do not start implementation until the WDLL is approved. | Operator: go on next. | nick |
| A-044 | 2026-08-17 | **G-45 WDLL APPROVED.** Status flipped draft to approved. Decision `_decisions/2026-08-17_g45_smartsite_staff_map.md` active. Dispatch compiled `--plan OPS-17 --lane B --plan-row G-45`. Live Bastrop no-touch. Do not cut Leaflet. L26 untouched. | Operator: wdll approved. | nick |
| A-045 | 2026-08-17 | **G-45 CLOSED.** Dashboards staff map is the SmartSite embed of gold `48021:34137`. Serving `smartcity-dashboards-00006-vfk` @100%. PR **#5** squash `3080f4024651c0d53a74c307873cc85fab84b41b`. CI run **32052778257** check-run `test` conclusion `success`. Live GET `/` auto-loads 908 PINE / APN 34137 with no Compose click. Anon MCP `dashboards_compose_city_manager` `smartsite.url` matches iframe src. MCP `00082-mat` tag g11 unchanged (no MCP PR). City `00118-qox` CSP still omits `smartsite.cloud`. Live Leaflet uncut. L26 untouched. Close `_inbox/2026-08-17_g45_close.json`. G-33 and G-42 stay OPEN. | Operator: proceed. | nick |
| A-046 | 2026-08-17 | **G-64 ADDED.** Lane B Lane C staff path after G-45 close. Dashboards development-services mounts `plan-review-app-ten.vercel.app`. GET `/` stays G-45 SmartSite. Do not cut live PermitFlow. Do not start G-52. Do not re-grade G-51 F1-F7. Do not resume G-60 residuals. Decision `_decisions/2026-08-17_g64_lane_c_staff_path.md` provisional until WDLL approval. WDLL `_inbox/2026-08-17_g64_lane_c_staff_path_WDLL.md`. Live Bastrop no-touch. L26 untouched. Do not start implementation until the WDLL is approved. | Operator: do these two (Lane C staff path, then PermitFlow kill). | nick |
| A-047 | 2026-08-17 | **G-65 ADDED.** Lane B PermitFlow kill as a product after G-64 graded. Dashboards zero `permitflow`. Live `/permitflow/*` and `pf_documents` stay until Bastrop cutover. No city deploy. Decision `_decisions/2026-08-17_g65_permitflow_kill.md` provisional until WDLL approval. WDLL `_inbox/2026-08-17_g65_permitflow_kill_WDLL.md`. Do not start implementation until G-64 is graded. L26 untouched. | Operator: do these two (Lane C staff path, then PermitFlow kill). | nick |
| A-048 | 2026-08-17 | **G-64 WDLL APPROVED.** Status flipped draft to approved. Decision `_decisions/2026-08-17_g64_lane_c_staff_path.md` active. Dispatch compiled `--plan OPS-17 --lane B --plan-row G-64`. Live Bastrop no-touch. Do not cut PermitFlow. Do not start G-52. L26 untouched. Compass vision aligned, not a card. | Operator: both the others are approved as well. | nick |
| A-049 | 2026-08-17 | **G-65 WDLL APPROVED.** Status flipped draft to approved. Decision `_decisions/2026-08-17_g65_permitflow_kill.md` active. Implementation still blocked on G-64 graded. Live `/permitflow/*` uncut. No city deploy. L26 untouched. | Operator: both the others are approved as well. | nick |
| A-050 | 2026-08-17 | **G-64 CLOSED.** Dashboards development-services mounts plan-review-app as the staff reviewer. Serving `smartcity-dashboards-00007-8sc` @100%. PR **#6** squash `fdd615cc7928bdc48569252dbf285c12ce760027`. CI run **32059174854** check-run `test` conclusion `success`. Live GET `/?lens=development-services` auto-loads Plan review persona gate with no Compose click. GET `/` still 908 PINE / APN 34137. Anon MCP `dashboards_compose_city_manager` `data.planReview.url` matches. MCP `00082-mat` tag g11 unchanged (no MCP PR). City `00118-qox` `/permitflow/review` still 200. L26 untouched. Close `_inbox/2026-08-17_g64_close.json`. G-65 unblocked. G-33 and G-42 stay OPEN. | Operator: both the others are approved as well. | nick |
| A-051 | 2026-08-17 | **G-65 CLOSED.** PermitFlow is dead as a Dashboards product. Serving `00007-8sc` GET `/app.js` permitflow=0; GET `/permitflow` 404. No Dashboards PR this card (G-64 already refused the string). Paired control: city bundle `index-kGj7uMs4.js` permitflow=160; GET `https://smartcityos.io/permitflow/review` 200. City `00118-qox` unchanged. Zero city deploys. No `pf_*` DROP. Live island cut is Bastrop cutover, not this card. Close `_inbox/2026-08-17_g65_close.json`. G-33 and G-42 stay OPEN. | Operator: both the others are approved as well. | nick |
| A-052 | 2026-08-17 | **G-66 ADDED. Path amendment: UI then one feed, not Bastrop cutover.** Operator rejected city-wide Bastrop cutover as the next card. Sequence: Dashboards product UI on `template-city` (this row), then one data source at a time onto the template (adapter writes a record, or a copied scraper through that adapter). Three identities must not collapse: `template-city` demo, live Bastrop island (`00118-qox` / `tenant_id=2`), next onboarded city as a new pack. Gold `48021:34137` is a demo fixture, not Bastrop onboarded. Live `/permitflow/*` and Leaflet stay until named island replacements. Decision `_decisions/2026-08-17_dashboards_ui_then_one_feed.md`. Gap map `_inbox/2026-08-17_dashboards_missing_pieces.md`. WDLL draft `_inbox/2026-08-17_g66_dashboards_ui_WDLL.md` pending operator approval. Do not start implementation until the WDLL is approved. Do not start G-52. Do not fill G-24. L26 untouched. | Operator: session close; prefer UI first, then one source at a time; name the line between demo, Bastrop, and the next city. | nick |
| A-053 | 2026-08-17 | **G-67 ADDED. G-68 ADDED. G-69 ADDED. G-66 WDLL APPROVED** against 30c layouts. Kit copy first, then Dashboards UI, Files UI, and Plan Review UI in parallel. Extract `_inbox/2026-08-17_sc_kit.css`. Sequence `_decisions/2026-08-17_ux_implementation_sequence.md`. Light atom hex `_decisions/2026-08-17_atom_accent_light_hex.md`. Do not start G-66 G-68 G-69 until G-67 is graded. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: sent housekeeping to design agent and approved the four cards. | nick |
| A-054 | 2026-08-17 | **G-70 ADDED. G-71 ADDED. G-72 ADDED.** Files compose into Dashboards, then one public municode calendar feed onto `template-city` writing files (not spine; L26 holds atoms slot), plus Files embed chrome. Decision `_decisions/2026-08-17_files_compose_then_one_feed.md`. WDLLs approved. Do not start a Bastrop tenant. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: spawn the next wave; do not start a Bastrop tenant. | nick |
| A-055 | 2026-08-17 | **G-73 ADDED then CLOSED. Feeds still pause.** Operator: work on the shell; make homes for everything in the G-18 inventory before more data feeds. Decision `_decisions/2026-08-17_shell_before_feeds.md`. Homes register `_inbox/2026-08-17_g18_shell_homes.md`. WDLL `_inbox/2026-08-17_g73_shell_homes_WDLL.md` closed. Serving Dashboards `00011-nzs`. G-71 Bastrop municode host on template-city is a HOLD, not a feed win. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: review inventory; make homes; no more feeds. | nick |
| A-056 | 2026-08-17 | **G-74 ADDED.** Identity leak. Design review `_inbox/2026-08-17_g73_shell_design_review.md` A1 A2 A3. Pull municode grant off template-city. Compose must not hydrate Bastrop clerk meetings. Citizen must not name 1311 Chestnut. Connections must not say Bastrop. No clerk retarget. Decision `_decisions/2026-08-17_template_city_identity.md`. WDLL `_inbox/2026-08-17_g74_identity_leak_WDLL.md` approved. Demo-city chrome completeness is a separate handoff, not this row. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: you can do the identity stuff; other agent builds the rest of the demo city. | nick |
| A-057 | 2026-08-17 | **G-75 ADDED then CLOSED.** Demo-city chrome, mounts, Compass-class map motion. Operator handoff `_inbox/2026-08-17_demo_city_template_handoff.md`. Note `_inbox/2026-08-17_g75_shell_mounts_motion.md`. WDLL `_inbox/2026-08-17_g75_shell_mounts_motion_WDLL.md`. Serving Dashboards `00013-vkl`. PR **#13** squash `34b307b87683fb72de71c2163ac21c494b9fbe75`. CI run **32088195999** conclusion `success`. Plan Review detection was already on the host; Dashboards now passes `embed=1`; no plan-review PR. Interruptibility partial (no velocity, no drag). Register 67 of 67 plus 3 labelled addenda. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. Close `_inbox/2026-08-17_b_g75_close.json`. | Operator: other agent builds the rest of the demo city; planner owns merge and deploy. | nick |
| A-058 | 2026-08-18 | **G-76 ADDED. G-77 ADDED. Two rulings landed.** Operator approved a kit component package against 30b section 4 (`_decisions/2026-08-18_smartcity_kit_component_package.md`: typed wrapper only, CSS stays canonical, four-part conformance gate) and `template-city` becoming a fixture city (`_decisions/2026-08-18_template_city_becomes_fixture_city.md`: generated records labelled fixture, new `empty-city` pack keeps the honest-empty demonstration, five-part labelling gate). G-76 is the type conformance pass on Dashboards: fourteen declarations render below the 30b 12px floor and four ramp steps are off, which is most of why the serving build reads flat. G-77 is the fixture pack plus the Development services lens one deep; `ADAPTER_KINDS` declares no record shape today, so declaring one per kind is part of that card and is what makes the later pack swap work. Design sync of the kit and the design pass are sequenced AFTER both, because a design judged on an empty city is judged on the one condition where 30b law 3 guarantees no loud object. Do not start G-52. Do not fill G-24. `grantedAdapters` stays empty. Live Bastrop no-touch. L26 untouched. | Operator: sounds good; yes to both rulings; proceed with the decision records then fan subagents for 1, 2 and 3. | nick |
| A-059 | 2026-08-18 | **G-76 CLOSED. G-77 CLOSED. G-78 ADDED then CLOSED. G-79 ADDED then CLOSED.** G-76 brought shell type onto the 30b ramp: fourteen sub-12px declarations to zero, four ramp steps corrected, 42 rules and 67 declarations changed, guard `src/type-conformance.test.mjs` watched fire. Nav badges wrap to a second line rather than breaking the floor; measured 13 of 15 rows single-line. PR **#15** squash `30cb660`. G-77 made `template-city` a fixture city: 14 generated mygov permit-cases (3 overdue, 4 awaiting-applicant, 5 in-review, 2 ready-to-issue), record shapes declared beside `ADAPTER_KINDS`, new `empty-city` pack, five-part labelling gate each watched fire, zero CSS classes added, no parcel minted (a generated case is attached to no parcel, because a parcel is a real record). PR **#14** squash `321119f`. **Two production divergences followed, both invisible to a green local suite, both found by probing the deployed surface.** G-78: reading a public-free pack's CONTENT went through the enumeration gate, which falls through to "is a service key configured"; local has no key so it was open, production has one so the demo refused its own records. PR **#16** squash `04e16ed`. G-79: both pack SELECTs kept older explicit column lists while the write path learned `environment` and `generates_fixtures`, so Neon held `generates_fixtures=true` and the read mapped it to false; a local run has no DSN and never executes the SQL path at all. PR **#17** squash `23154be`. Serving `smartcity-dashboards-00016-77z` @100%: anonymous pipeline 200 with 14 fixture-marked records, `empty-city` honest-empty, `fixture-city` 401, pack list 401, zero identity strings. 149 tests. Rulings `_decisions/2026-08-18_smartcity_kit_component_package.md` and `_decisions/2026-08-18_template_city_becomes_fixture_city.md`. Closes `_inbox/2026-08-18_b76_close.json` and `_inbox/2026-08-18_b77_close.json`. Next: chrome must follow the pack (city name, seal, source counter are still static markup, so a pack swap leaves the wrong city named in the top bar); `[hidden]` is inert product-wide; then the design pass and the kit component package. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: yes and yes; proceed with the decision records then fan subagents for 1, 2 and 3. | nick |
| A-060 | 2026-08-18 | **G-80 ADDED. G-81 ADDED.** Two chrome clean-ups carried out of the G-76 to G-79 close. G-80: the chrome does not follow the pack. City name, seal, environment badge and the nav footer are static markup, so on `empty-city` the top bar still reads "City of Template" while the breadcrumb reads "Empty city". A pack switch is therefore only half a switch, and the one-command city swap the fixture-city ruling promises cannot be demonstrated until identity resolves from the pack. Public-free pack identity is content, readable anonymously per G-78. G-81: `[hidden]` is inert product-wide because the kit gives `.pill`, `.prov` and `.state` an explicit display. It has already shipped a visible defect (an amber Partial pill beside "no meeting packet has been read") and was patched one component at a time twice (`.stage[hidden]`, `.stage-esc[hidden]`); the root fix is one rule. Split by file: G-80 owns `web/index.html`, `web/app.js` and `src/`; G-81 owns `web/shell.css`. Neither touches `web/sc-kit.css`. Carried from the G-79 post-mortem into both dispatches: an executor must name every code path its tests do not exercise, especially any that differs between a local run and the deployed service, because two divergences in a row were invisible to a fully green suite. Design pass, kit component package and design sync stay sequenced after these two. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: spawn agents to clean up one and two, then we will do three. | nick |
| A-061 | 2026-08-18 | **G-80 CLOSED. G-81 CLOSED.** Serving `smartcity-dashboards-00017-vx4` @100%, 185 tests. G-80: identity resolves from the pack through one resolver (`src/city-identity.mjs`, served by `GET /api/city-identity` behind `packContentReadStatus`, so a public-free pack answers anonymously and tenant-private still 401s). All sixteen `template-city` literals left `web/index.html`; top bar name, state suffix, seal, both environment badges, document title, Compass scope, nav footer, fourteen breadcrumbs and eight absence bases now follow the pack. Verified live: the rendered DOM on `empty-city` contains **zero** occurrences of Template. Footer ruling: `0 of 7 sources granted` with its counting rule inline (distinct adapter kinds granted on this pack, of 7 in the catalog); the old `1 of 12` was a product-wide disposition count whose numerator was Esri mounted through SmartSite, granted on no pack, so beside a city name it was false for the city. The register figure stays on Connections and gained an inline rule it never had. PR **#19** squash `cdfca39`. G-81: `[hidden]:not([hidden="until-found" i]) { display: none !important; }` replaces two per-component patches. An attribute selector and a class tie, so every heavier-selector alternative relocates the tie rather than winning it; an important author declaration is the only form that outranks all normal author declarations regardless of position. Planner measured independently: hidden `.pill` `.prov` `.state` `.metric` all compute `none`, a component rule declared AFTER the global rule still loses, and zero of 203 live elements are wrongly hidden. PR **#18** squash `7a255de`. **Lane fence was wrong, not the executor:** `src/ui.test.mjs` asserted the literal text of the patch G-81 retires, so "all tests pass" and "do not edit the rest of `src/`" could not both hold. G-81 made a two-line minimal edit, reported it unprompted and graded itself PARTIAL. **F1 carried:** the standing forbidden-string clause bans `Bastrop`/`Chestnut` in `web/`+`src/` outside tests, but main legitimately carries 15, all of them refusal guards (held clerk host, `isIdentityHeldClerkHost`, the pack-name throw, identity-hold bases, fixture leak-detector needles). The clause must forbid ASSERTING and permit REFUSING or it is a permanently-red gate; planner hit the same false positive when verifying G-77. New cards queued, not started: asset cache headers (`/shell.css` ships with no `cache-control` and no fingerprint, so a returning browser can hold a stale stylesheet across every CSS deploy shipped so far); retire the now-redundant `show()` helper; the inert-`hidden` defect is untested inside the mounted SmartSite / Plan Review / Smart Files documents. Next is the design pass judged on a populated screen, then the kit component package and design sync. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: spawn agents to clean up one and two, then we will do three. | nick |
| A-062 | 2026-08-18 | **G-82 ADDED. Ruling amended same day.** Operator invoked design sync; it has nothing to convert, verified: no `.design-sync/`, no storybook, no stories, no React or JSX, no `dist/`, no `main`/`exports`, and `pg` is the only dependency in the product. All three product repos are static `web/` plus a Node server. The design system is ~110 CSS classes and 63 tokens, and design sync converts a compiled component library that renders from `window.<globalName>.*`. G-82 builds the prerequisite: a typed React wrapper over the kit vocabulary, whole shipped surface (~18-22 components), housed in a **new repo `empressaioemail-tech/smartcity-kit`** (operator choice; peer to the four product repos so none of them owns the shared system, per 30b section 4). Sequencing note worth keeping: wrapping before G-76 would have frozen fourteen sub-12px violations into every design the agent ever builds, and syncing before G-77 would have shipped preview cards of empty states, since the severity ladder that makes this system legible only exists when records exist. **`_decisions/2026-08-18_smartcity_kit_component_package.md` amended before implementation:** gate items 1 and 4 contradicted each other, forbidding any `--sc-` token declaration while requiring the canonical block be byte-identical or absent, so a package shipping `sc-kit.css` could not pass. Item 1 now forbids inventing a token rather than carrying the canonical one; item 4 now forbids the package styling anything. Second clause in two days whose wording forbade the compliant case, after the `Bastrop` refusal-guard finding; a gate is only a gate if the correct implementation can satisfy it. Design sync runs from `smartcity-kit` after G-82, not from doc_repo. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: /design-sync; new repo smartcity-kit; whole shipped vocabulary. | nick |
| A-063 | 2026-08-18 | **G-82 CLOSED. Licence HELD for operator ruling. Two new gaps named.** `empressaioemail-tech/smartcity-kit` PR **#1** squash `d6f83d9`, CI added by the card and conclusion `success`, 67 tests. **73 exported components covering 109 of 109 shipped stylesheet classes**, denominator being distinct class selectors in `sc-kit.css` plus `shell.css` with comments stripped; planner verified the denominator independently and the executor's set is a correct strict subset of a cruder grep (the 6-item gap is file-extension noise and one attribute selector). The 18-22 component estimate in the dispatch was low by ~3x because element-scoped children (`.t`, `.k`, `.v`, `.nm`, `.rail`, `.gl`, `.sep`) each need a real component to be covered without inventing wrapper markup. Vendored `sc-kit.css` and `shell.css` confirmed byte-identical to upstream **by git blob hash**; a working-tree md5 said DIFFERENT and was the wrong instrument on a Windows checkout (CRLF), which is the same trap recorded in the CRLF memory. `prove:gates` injects a real violation per gate and requires it to fail AND name the violation: **16 of 16 fired**, including law 3 quiet-default-flipped and Panel-gains-an-elevation-prop, the two design-law constraints the dispatch asked to be encoded in the API rather than merely permitted. **Licence held:** the executor added a proprietary `LICENSE` naming Legacy Group ATX LLC, an entity it inferred rather than was given. Legal execution routes to the operator per CLAUDE.md, so the planner removed it before merge; an unlicensed public repo defaults to all rights reserved, which is the safer interim state than an unread legal instrument. Proposed text preserved for the ruling. **Gap named, needs a card:** two of the five components 30b section 3.1 calls load-bearing have no CSS in any shipped stylesheet, the applicability matrix (`mx*`) and the code citation (`cite`), plus the atom chip from section 6.3. The executor reported three of five; it is two of five plus `atomchip`, which is section 6.3 not load-bearing. A design agent drawing the Plan Review surfaces will invent all three, which is exactly the failure the wrapper exists to prevent, so the kit cannot cover them until the product ships their CSS. Design sync now has something to convert and runs from `smartcity-kit`. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: /design-sync; new repo smartcity-kit; whole shipped vocabulary. | nick |
| A-064 | 2026-08-18 | **G-83 CLOSED. G-84 ADDED. Design sync in flight; ruling amended a second time.** Design sync against `smartcity-kit` reached a clean converter run: 73 components, 43 KB bundle, all `.d.ts` parse, anchor matches disk, `package-validate.mjs` exit 0, render check 72 of 73 with 65 on the honest floor card (previews not yet authored). **G-83 (kit PR #2, squash `7fa706c`):** the token layer was falling out of the sync's stylesheet closure. `shell.css` consumes `var(--sc-*)` throughout and defines none of them, and the converter's closure takes one entry, so it took the component CSS and left all 63 token definitions behind; every design built from the kit would have rendered a complete layout with every colour, size and radius silently falling back. Fixed by emitting `dist/kit.css`, the two concatenated in their load-bearing order, which also removes a real ordering footgun for npm consumers. Gate 4 refused the new file, correctly, so it is now checked HARDER than a copy: the test reconstructs what the build must have produced and requires byte equality including the header, watched failing on a rule appended to the built file. Second finding in the same run: the repo-wide scan was reading design sync's own `ds-bundle/` output as the package authoring CSS, the nested-clone failure again with a different cause, now recorded and printed rather than quietly excluded. Planner note: `cfg.tokensGlob` is a no-op without `cfg.tokensPkg` (it resolves a sibling PACKAGE, not a file in the DS's own dist). **G-84 added:** `[FONT_MISSING]` — the kit's CSS names Inter and IBM Plex Mono and ships neither. The product loads them from a font host in its own HTML; nothing built from the package does, so every design renders in a fallback and the G-76 type ramp is discarded silently. Operator chose to ship the woff2 from the kit (both OFL). That required amending gate 4 a second time: the package may now ship `@font-face` rules, and ONLY those, for families the canonical token block already names in `--sc-font-*`, with a test asserting both halves. **Third wording correction in three days; the pattern is that a gate written as an absolute keeps forbidding the compliant case, and each fix names the boundary rather than loosening the rule.** Remaining sync work: author 73 preview cards from the kit's GALLERY (operator scoped all 73), then upload. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: /design-sync; ship the woff2 files in the kit. | nick |
| A-065 | 2026-08-18 | **G-85 ADDED then CLOSED. G-86 ADDED.** G-85 was the sync-inputs PR, titled G-85 in the kit repo before an amendment row existed; A-065 records it rather than leaving a PR title that cannot compile. Kit PR **#4** squash `8ef0203f376107306595b26ccd75171965c66d51`, CI run **32185143428** check-run `test` conclusion `success`. Durable set: `.design-sync/config.json`, `NOTES.md`, calibration previews Pill / State / MetricStrip. Project `f5e5465e-943f-4f68-b52f-608925bc07b0` pinned, still empty. **G-86** is the remaining seventy preview cards, the conventions header, and incremental upload into that empty project. WDLL `_inbox/2026-08-18_g86_previews_WDLL.md` approved by operator proceed on the 2026-08-18 handoff. Planner fans disjoint preview sets; verification is not delegated. Do not set `cfg.provider` to Theme with `mode: "dark"`. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: pick this up and proceed on `_inbox/2026-08-18_design_sync_handoff.md`. | nick |
| A-066 | 2026-08-18 | **G-86 CLOSED PARTIAL — five of six acceptance items met, the upload DROPPED. G-87 ADDED.** Kit PR **#5** open not merged, head `c935ad07dae7c7320c3eb37f68c2548632194108`, base `8ef0203`, CI run **32194013389** check-run `test` conclusion `success` with vendor-parity arm B running (CI sets `SC_DASHBOARDS_DIR`, and the final step asserts the drift check actually ran rather than declaring itself unrun). Durable set 72 files: 70 owned previews, `conventions.md`, `config.json`. `package-validate.mjs` exit 0 — 73 of 73 render cleanly, zero floor cards, zero bad, **zero `GRID_OVERFLOW`**, anchor matches disk on 73 recomputed hashes; 73 grade files over 155 cells, all good. The fifteen flagged components were remedied in `cfg.overrides`: eleven `cardMode: "column"` (full card width per story, none dropped), four `cardMode: "single"` with an explicit `primaryStory`, three shell entries keeping `1100x700` because `viewport` IS in the grade key and `cardMode` deliberately is not. Header answers the carried open question: dark is the staff default, set `data-theme="dark"` on the root, never `Theme mode="dark"` as a page provider. **Item 5 dropped for a TOOLING reason, not a work reason.** The dispatch was issued on the belief that a Claude Code seat carried the DesignSync MCP server; the executing session was Cursor and carried the same three servers the prior session had, verified by full catalog listing plus two pattern searches. `.ds-sync/resync.mjs` documents the upload as agent-side MCP calls, and no HTTP client, token or CLI upload path exists in the toolchain, so nothing local substitutes. Project `f5e5465e-943f-4f68-b52f-608925bc07b0` is still EMPTY and untouched; improvising a route to a live design project was refused under the dispatch's own stop rule, since a fresh anchor over a half-applied upload is permanent while an un-anchored project merely re-verifies. Operator ruling: hand the mechanical STEP 2 runbook in `_inbox/2026-08-18_b86_close.json` to a seat that has the tool. **Two defects the instruments could not see, both found by looking at the card render.** Grading reads per-story sheets captured through `?story=`, which are full-bleed and therefore unaffected by `cardMode`, so the sheet a grader reads is not the card the picker shows — `CompassScrim` and `StageScrim` rendered as an empty white box with `bad`, `blank`, `collapsed` and `thin` all false and every cell graded good, because a `Region` outside `ColStack rail` collapses `.region-canvas` to height 0 and a 0.34 wash over nothing is nothing. Fixed with the kit's own height idiom and recorded as a WDLL amendment rather than absorbed, since the dispatch forbade re-authoring. **Fourth wording finding in four days, and the first where the gate was RIGHT.** `conventions.md` named four vendor brands inside a refusal guard, in a file stitched verbatim into the uploaded README. `test/constraints.test.mjs` keeps `ASSERTION_ONLY` (`bastrop`, `chestnut`, nameable in order to be rejected) and `ABSOLUTE` (`permitflow`, `citizenconnect`, `leaflet`, `pipedrive`, `stripe.com`, not at all), and its header states they are not the same rule — the carve-out was written knowingly and withheld from vendors, so reaching for the now-familiar "the gate forbids the compliant case" pattern would have granted a carve-out that is the operator's to grant. Content fixed; all 417 files of the upload payload grep clean. **G-87 added** from that finding: `walk()` in `test/_lib.mjs` skips `.design-sync` at ROOT as `TOOL_OUTPUT_DIRS`, so the constraint suite scans neither the conventions header nor the 73 previews and CI was green on the violation. The skip was correct when the directory held only converter output and went stale when G-85 moved authored source into it. Not fixed as a drive-by: it touches three scans and re-opens the gate-4 stylesheet false positive it was recorded to prevent. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: hand the STEP 2 runbook to a seat that carries DesignSync; open F-1 as a row. | nick |
| A-067 | 2026-08-18 | **G-87 SCOPED.** Lane B. Restore constraint-gate coverage over `.design-sync/`, which now holds authored source that ships. Pass/fail instrument: the injected-violation harness, not a passing suite — a forbidden string planted in `.design-sync/conventions.md` and one planted in a `.design-sync/previews/*.tsx` must each turn `npm test` red and name the file, and gate 4 must still not read converter output as package-authored CSS. `toolOutputSkipped()` must keep reporting whatever remains skipped, because an empty result is not an absence. Housing `empressaioemail-tech/smartcity-kit`. Blocked on nothing; kit PR #5 merge is a convenience, not a dependency. | A gate that cannot see the file it governs is not a gate, and this one was already green on a real violation. Prose in a PR body does not survive; the base rate in this repo is hook-shaped controls 1-for-1 and prose-shaped controls 0-for-3 | nick (row opened); planner (instrument) |
| A-068 | 2026-08-18 | **G-86 CLOSED FULL. Item 5 re-graded dropped to met on the upload leg.** Kit PR **#5** merged squash `d4f7b615b25f5ec79c3058ea28dfbae1b4515fa3` at 2026-08-18T23:22:27Z; pre-merge check-run `test` conclusion `success` on head `c935ad07dae7c7320c3eb37f68c2548632194108` (run 32194013389), `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`. The merge gate was the conclusion STRING on the identical tree, not a `gh` exit code. A-066 dropped item 5 for a TOOLING reason rather than a work reason, and that reason no longer holds: this seat carries DesignSync and proved it with a read call before any write (`get_project` returned `SmartCity Product Kit`, `PROJECT_TYPE_DESIGN_SYSTEM`, `canEdit true`), which is the check the mis-issue of this dispatch cost a session for want of. **417 of 417 uploaded and reconciled by prefix against the remote**, not asserted: components 292, `_preview` 73, fonts 44, `_vendor` 2, six root files; `tokens/` and `guidelines/` empty and correct, because `cfg.tokensGlob` is a no-op without `cfg.tokensPkg` and all 63 tokens ship inside `kit.css`. Sequence held: sentinel first and alone, 416 content files in nine chunks, no deletes since the project started empty, sentinel re-armed, `_ds_sync.json` last in its own call. Zero write failures, zero retries. One deliberate deviation from the runbook: **the remote was listed and reconciled at 416 BEFORE the anchor was armed**, because the anchor vouches for everything beneath it and a fresh anchor over a half-applied upload is permanent while an un-anchored project merely re-verifies. Chunking was by BYTES as well as file count, `fonts/` largest-first in four chunks and `react.js` alone at 1086 KB. The bundle was deliberately NOT rebuilt; `package-validate.mjs` exit 0 with the anchor matching disk on 73 recomputed hashes is stronger evidence than a fresh build, and a rebuild wipes `ds-bundle/` and `.render-check.json`. **The honest half of the grade:** item 5's named check has two halves and only the first was observed. `list_files` rose 0 to 416 to 417, but not in named batches, because everything was already verified on the green PR and there was nothing left to stream. "Project URL returns the picker" was NOT observed; that seat has no browser and the `_ds_needs_recompile` sentinel is still armed, so the app has not yet rebuilt its card index. Met is a claim about the bundle arriving intact and anchored, not a claim that anyone has looked at the picker; the operator walk is owed and is recorded as unrun. Also unrun: the post-merge check-run on `d4f7b61` was `in_progress` at close time. `[FONT_MISSING]` carried unchanged and is not a new finding, the shipped CSS naming "Inter Variable" and "Cascadia Mono" while Inter static and Plex Mono ship and render. Close `_inbox/2026-08-18_b86u_close.json`; WDLL finish card re-graded in place. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: three legs, in order, and do not start leg 3 before leg 1 is filed. You have credentials the previous seat did not; that is the only reason this is yours. | nick |
| A-069 | 2026-08-18 | **G-87 CLOSED, PR open not merged.** Kit PR **#6** on branch `g87/constraint-gate-sees-design-sync`, head `830fbe3e007e05f2357b9da0a330f6a525469248`, base `d4f7b61`, check-run `test` conclusion `success` polled to completion. Not merged: leg 1 carried an explicit operator greenlight for PR #5 and this leg carried none. `walk()` skipped `ds-bundle`, `.ds-sync` and `.design-sync` by directory NAME at the root; the skip is now by PATH and names the generated paths INSIDE `.design-sync` (`.cache`, `learnings`, `node_modules`) rather than the directory. **76 authored files now enter the scans that could not see them**: `conventions.md`, `config.json`, `NOTES.md` and the 73 previews, verified directly with zero `.cache` leakage. Gate 4 is not re-opened because `ds-bundle/` and `.ds-sync/` stay skipped and no `.css` exists anywhere under `.design-sync`. **Every path on the skip list is also in `.gitignore`, and that is load-bearing:** a gitignored path exists locally and NOT in a fresh CI clone, so a scan that walks one reports violations only one environment can ever see; `.design-sync/learnings` was listed for that reason though it does not exist on disk. **Two harness gaps found, and either would have made this row unprovable.** `prove-gates.mjs` never copied `.design-sync` into its scratch, so a violation planted there could not have fired for want of the file; and the scratch is a plain tmpdir, so the new gitignore guard would have reported UNRUN and passed. Both fixed: the directory is copied filtered, and the scratch is `git init`ed. **27 of 27 injected violations caught and named (was 24), 1 of 1 legitimate-payload controls stayed green, exit 0.** The two A-067 cases use the FILE PATH as their expect string, not the generic failure banner, because the requirement is red AND named. Added beyond the row: a **divergence pair** planting one stylesheet payload, held in a single constant so "the same bytes" is a fact about the code, under `ds-bundle/` where it must stay green and under `.design-sync/` where it must be caught, because a single case proves a verdict while a pair proves the boundary sits where it is claimed; and a **root-cause guard** asserting everything skipped is gitignored, since the list was not wrong when written, it went STALE and nothing was watching. **Counter-verified rather than trusted:** reverting the fix on a backup sent both new cases SILENT and the harness to exit 1 at 23 of 26, which is the only way to know the cases are about the real defect. `toolOutputSkipped()` still reports, with a CONDITIONAL assertion (if a declared path exists, the walk must have refused it) because an unconditional non-empty assertion would only ever pass on a developer's machine. Preflight that mattered: all seven needles grepped CLEAN across `.design-sync` and the gate-1a token pattern checked against `conventions.md` before any edit, because a fix that turns CI red on legitimate content is not a fix; baseline held at 73 pass either side, 74 with the new guard. Close `_inbox/2026-08-18_g87_close.json`. Named not built: the inverse guard, that no gitignored directory is silently walked, which needs a `git ls-files` reconciliation larger than A-067 scoped. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: three legs, in order. Pass/fail instrument is the injected-violation harness, not a passing suite. | nick (row opened); planner (instrument) |
| A-070 | 2026-08-18 | **G-88 ADDED and SCOPED. Design into the apps.** Lane B, housing `smartcity-dashboards` primarily. **The premise, and it is the whole shape of the row: the kit is React and THE PRODUCTS ARE NOT.** All three product repos are static `web/` plus a Node server, no React, no JSX, no bundler, `pg` the only dependency, verified at A-062. Claude Design emits React that imports the kit, and that cannot be dropped into the apps. **It does not need to be.** The kit adds ZERO styling: gate 4 enforces that the package styles nothing, and the vendored `sc-kit.css` and `shell.css` are byte-identical to the product's own by git blob hash. Every kit component is a typed wrapper over classes that already exist in the shipping stylesheet, 73 components covering 109 of 109 classes, so a design composed ONLY of kit components is by construction expressible as the exact class markup the product already serves. The translation React-composition to static HTML is mechanical and lossless in one direction, and that direction is the only one that ships. Do NOT adopt React in the products. Do NOT invent a build step. **Do not let a design ship a class that is not in the 109 — that last clause is the real gate.** A design needing a class that does not exist means shipping the CSS in the product FIRST and re-vendoring, never hand-writing it into a screen. **Order of work, and the first three are prerequisites rather than setup.** (1) Ship the three missing CSS families into `smartcity-dashboards`: `mx*` applicability matrix and `cite` code citation, two of the five 30b section 3.1 calls load-bearing, plus `atomchip` from 6.3. None has CSS in any shipped stylesheet, so until they ship a design agent drawing Plan Review will invent all three, which is precisely the failure the wrapper exists to prevent. Carried as F-4 through two closes; it blocks the design pass, not the reverse. (2) Re-vendor into the kit, rebuild, `package-validate.mjs` exit 0, re-sync the delta with sentinel first and `_ds_sync.json` last and alone, wrapping the three new families as components so the 109-of-109 claim stays true against a BIGGER denominator, and STATE the new denominator. (3) Fingerprint `/shell.css` or give it cache headers, BEFORE the design pass and not after: it ships with no `cache-control` and no fingerprint, so a returning browser holds a stale stylesheet across every CSS deploy shipped so far and every design change landed would be invisible to a returning operator. Queued since A-061 and never built. (4) Design the screens in the Design project from kit components only. (5) Translate to static `web/` markup, ONE PR PER SURFACE, each stating which classes it used and asserting none is outside the vendored stylesheet. (6) Deploy and verify on the deployed surface, because a grade is a live probe and never a merged PR; probe with GET, HEAD returns 404 on this service. **WDLL required before implementation**, numbered acceptance items each checkable by a specific observation, operator-approved. Four read-only investigations fanned at scoping time: the three CSS families against 30b, the `shell.css` cache path and its returning-browser probe, the React-to-static translation boundary and where it breaks, and the surface inventory. **Gate finding, reported not ruled:** the canon gate and the dispatch-template gate both fired on ONE of four near-identical read-only investigator prompts, the one that happened to name a repo in `owner/name` form. Both key on dispatch SHAPE, not on whether the agent writes anything, so a read-only prompt phrased with a clone path passes and the same prompt phrased with a repo name does not. Adding the sanctioned `CANON_OVERRIDE` made the brief dispatch-shaped and triggered the second gate the siblings never hit. Complied with rather than overridden a second time. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: merge then fan agents to do leg 3. | nick (row opened); planner (scope) |
| A-071 | 2026-08-18 | **G-88 WDLL APPROVED. Three rulings taken. Three executors dispatched.** WDLL `_inbox/2026-08-18_g88_design_into_apps_WDLL.md`, eleven acceptance items, operator-approved 2026-08-18. **Four read-only investigations ran before the card was written and THREE OF THEM CONTRADICTED A PREMISE THE ROW WAS SCOPED ON**, which is the finding of this row so far. (1) The CSS for all four families already exists, fully written and token-only, inside 30b's own `<style>` block, so item 2 is a PORT of roughly 26 rules needing **zero new tokens**, and it is a Dashboards PR rather than the three-repo `sc-kit.css` ruling it could have been. No product markup references any of the twelve classes, so there is no unstyled-markup cleanup leg. (2) **The React-to-static translation is NOT lossless.** A kit composition emits zero `id`, zero `hidden` branches and zero `data-*` hooks, while the product carries 94 ids and `app.js` makes 33 `getElementById` calls against them. `test/_markup.mjs` normalises away exactly `id`, `hidden` and every non-class attribute before comparing, so the nineteen green parity tests were silent about the gap BY CONSTRUCTION and were never evidence either way. Same failure shape as the G-86 `cardMode` blind spot: the instrument does not look at the thing that broke. Item 7 is new, is not in the dispatch, and exists because a screen that renders perfectly and cannot be driven passes every gate in both repos. (3) **The shell.css staleness premise is FALSE, measured** with a real Chrome on a persistent profile against the live service across three scenarios including a full browser restart and an in-session deploy: the returning browser fetched fresh CSS every time. With no `Last-Modified` there is no basis for heuristic freshness, so Chrome revalidates, and with no validator that degrades to a full GET. The defect is inverted from the one recorded at A-061, never stale and 145 KB re-transferred per navigation with a `304` structurally impossible, so **item 8 is re-sequenced parallel and non-blocking** and the design pass does not wait on it. (4) One-PR-per-surface is LOGICAL not physical: all fifteen surfaces share one `index.html` and one unpartitioned `shell.css`, so PRs merge serially bottom-of-file upward. **Operator rulings, each because an executor would otherwise have to guess.** G1: the 12px type floor HOLDS, so 30b's own sub-12px declarations (`.cite em` 11px, `.cite.model .corpus` 10px, `.mxgroup .lic` 10px) are raised to 12px, consistent with the product already shipping `.prov` at 13px where 30b says 11px; consequence accepted and named, 30b's specimens stop matching what ships and 30b is now the looser document. G2: **30b governs the CSS and 30c governs atomchip markup**, resolving five undeclared divergences including `.mxrow` 132px versus 108px; 30b carries no atomchip specimens at all, and the dispatch's "30b section 6.3" pointer was the wrong doc. G6: a **fourth family ships**, `finding`/`basisline`/`meter`, because it is what a matrix and a citation compose INTO and shipping the parts without the whole leaves the design agent to invent the assembly. **Denominator is recounted, never forecast:** the three investigations produced 117, 128 and 138 by different methods, the WDLL records 138 as an estimate explicitly not as the acceptance value, and the `SHIPPED.size === 109` pin failing on merge is the gate working. Dispatches COMPILED, three of them, lanes **B88-2** (port the families), **B88-3** (harden the class gate BEFORE screens ship, since its rule does not strip CSS comments and an injected `class="hidden"` does not fire today), **B88-8** (the cache contract, whose third probe leg — a stale ETag must return 200 with a DIFFERENT etag — is the only one that catches the constant-ETag implementation that would cause permanent staleness). Each executor works its own clone and none merges; verification does not delegate below the planner. **Tooling finding:** `scripts/dispatch.mjs` requires a row id followed by one of the add-verbs `ADDED`, `ADD` or `NEW`, and A-070 declared G-88 as `SCOPED`, which the row scan rejects; A-070 amended to `ADDED and SCOPED` before compiling. A-067 declared G-87 the same way and would have failed identically had G-87 been dispatched rather than planner-executed. Carried out of scope, named not built: `.prov.stale` missing from a shipped load-bearing component so a stale source renders identical to a current one, `roster-lens` shipped and defined nowhere, the forbidden-string gate not scanning CSS on the card that adds CSS, and the runtime extractor blind to classes passed as function arguments. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: approved. | nick (approval); planner (rulings taken to the operator, card, dispatches) |
| A-072 | 2026-08-19 | **G-89 ADDED. Kill the lens flash.** Lane B, housing `smartcity-dashboards`. **Every deep link into this product paints the wrong screen first.** Operator found it walking the deployed surface and confirmed the diagnosis. Mechanism, all four facts verified at source: `web/index.html` hardcodes `<section class="lens on" id="lens-city-manager">`, so Overview is ON in the static document; `shell.css` hides lenses with `.lens { display: none }` and shows with `.lens.on`; the nav items are real `<a href="/?lens=...">` links so every click is a FULL PAGE NAVIGATION; and `app.js` loads as `<script type="module">`, which is deferred by definition and therefore runs after the document parses. So the browser paints Overview, which is the lens carrying real content, and only then does `applyLens()` switch to the requested lens. On a not-built lens the operator sees a populated screen collapse into an honest-empty one, which reads as a broken deploy. **NOT caused by the G-88 deploy, and that was established rather than asserted:** `index.html` and `app.js` are byte-identical across `cdfca39..6a4580d`, the `class="lens on"` count is 1 on both sides, and revision `00017-vx4` was tagged `pre` at 0% traffic so the operator could A/B the pre-deploy revision directly and reproduce it there. The deploy plausibly WIDENED the window without creating it: `shell.css` grew 27 KB to 37.6 KB and is render-blocking, and the first load after the deploy had a cold cache on every asset because the service had never sent a validator before. Both delay `app.js` relative to first paint. **The fix cannot be CSS alone and cannot be a script move.** A module script is always deferred, so no reordering makes it run before paint, and CSS cannot read a query string. The shape that works without a build step, which this repo does not have and should not gain: an inline classless script in `<head>` that reads `location.search` and stamps `data-lens` on the root element, plus ONE rule keying visibility off that attribute rather than off a class JS adds later, so visibility is decided before first paint and the flash is structurally impossible rather than merely faster. Attribute-based, so the G-88 item 3 class gate stays satisfied. **Done means the flash cannot recur, not that it looks better:** the acceptance instrument is a probe that fails on the CURRENT build, since a fix nobody watched fail is the pattern this program keeps paying for. Related defect to fold in or row separately: the static document's default `.on` is itself the bug surface, and any fix must keep every lens reachable with JS disabled or state plainly that it does not. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: found it on the deployed surface, confirmed the diagnosis. | nick (found); planner (diagnosis, scope) |
| A-073 | 2026-08-19 | **G-89 CLOSED. The lens flash is dead, deployed, and proven by a probe that was watched failing.** Dashboards PR **#23** merged squash `ecfae70ba7e0ea308f74c8589e1ac6713cef7e86`, pre-merge check-run `test` conclusion string `success` on head `e01c192847475c7a0e82b17ea85b5a4c954cba16`. Deployed via `gcloud run deploy --source .` as tag `g89b` with `--no-traffic`, smoked, then shifted: revision **`smartcity-dashboards-00021-yap` @100%**, with `00017-vx4` left tagged `pre` at 0% as the operator required. **The acceptance is a 30-target browser walk with `/app.js` aborted at the route level**, which reproduces the pre-paint state with no race: on `00018-kiw` correct lens **2 of 30**, 28 mismatches, and the distinct visible set across ALL 28 was exactly `["lens-city-manager"]`; on `00021-yap` correct lens **30 of 30**, zero mismatches. The two that passed before were the two `?lens=city-manager` requests, one per pack. **The settled render did not change**: the unblocked walk, diffed target by target, shows 0 of 30 surfaces differing in visible lens, `.basis` count, `.state` count, DOM id count or pill text, which is exactly what a first-paint fix should look like. Edge cases on the deployed surface **9 of 9**: unknown lens, unknown work, unknown tab, empty lens value and no query all fall back to Overview rather than painting nothing, and **JS disabled is exactly 1 of 15 sections, identical before and after**, because the hide rules gate on attribute PRESENCE rather than value. **The fix**: an inline classless `<script>` in `<head>` resolves the query before the parser reaches the body and stamps `data-surface`, `data-tab` and `data-atab` on the root; `shell.css` keys visibility off those. 27 enumerated selectors. Specificity was COMPUTED rather than argued: all 230 rules parsed, the 68 declaring `display` filtered to those that could reach a `.lens` section, exactly 2 survive; `html[data-surface] .lens` at (0,2,1) beats `.lens.on` at (0,2,0) without depending on source order, and the show rule at (1,1,1) beats both. No `!important`, so `hidden-rule.test.mjs` check 4 still holds. **Two rulings, both on divergences the executor surfaced rather than absorbed. R1: the attribute ships as `data-surface`, not the `data-lens` A-072 wrote** — `data-lens` already exists in this document on the fifteen nav anchors carrying a DIFFERENT value vocabulary (bare `finance`, where the root needs `lens-finance`), and two vocabularies under one attribute name is the naming trap; A-072's wording was planner scoping language, not an operator ruling on the name. **R2: the fix extends to the six DS tabs and three Assets tabs**, because `#tab-pipeline` and `#atab-inventory` carry the same static `on` class, so `?lens=development-services&tab=review` — this row's own acceptance input — would have painted the right lens and then still swapped a populated Pipeline table out for Review. **The planner mutation-tested the instrument rather than trusting it**: deleting the head script fires it, deleting one show selector fires it twice with Finance painting NOTHING (`actual: []`), and the paired-control divergence test fires in BOTH directions naming the exact mismatch. One planner mutation removed the selector from a CSS COMMENT and produced a false all-clear, caught by echoing the byte count it claimed to remove. `infra.md` stops pinning a serving revision — it carried `00001-92j` for eighteen revisions after that went false — and now carries the command plus a dated observation, with a test asserting both halves. Close `_inbox/2026-08-19_b89_close.json`; walks `_inbox/2026-08-19_g89_prefix_walk_blocked.json` and `_inbox/2026-08-19_g89_postfix_walk_blocked.json`. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: three cards, any order you judge best; G-89 first, it is what a prospect sees. | nick (found the defect); planner (rulings, verification, merge, deploy) |
| A-074 | 2026-08-19 | **G-88 items 4, 5 and 7 CLOSED. Items 6, 9 and 10 HELD, not dropped.** **Item 4, the recount: 139**, with its counting rule beside it — distinct class-selector tokens across `vendor/sc-kit.css` and `vendor/shell.css`, CRLF-normalized, CSS comments stripped first; state modifiers and element-scoped children count, pseudo-classes and attribute selectors do not. 109 before, **30 added, 0 removed**, and BOTH sides were measured (`git show HEAD:vendor/*.css` for the before set) rather than one derived by subtraction. **It reconciles the three failed estimates exactly**: the scoping investigations predicted 117, 128 and 138 by three different methods and all three were wrong; 139 is 109 plus 19 for the three named families plus 11 for the finding family, where the eleventh token is the meter fill `f` — which the css-families investigation flagged in a parenthesis and then dropped from its own headline total. The entire discrepancy lived between a parenthesis and a sum. The `SHIPPED.size === 109` pin failing on re-vendor was the gate working: it had been passing on a stale copy of `shell.css`, which is a gate being lied to rather than a gate holding. **Item 5**: nine components in `src/evidence.tsx` cover all thirty classes; components **73 to 82**, previews **73 to 82**, coverage restated **139 of 139** with its denominator in `README.md`; no exclusion set, no weakened assertion, no class parked as structural. **The three inversion traps hold at the TYPE, and the planner verified each by attempting the violation**: `ModelCite` rejects `children` (TS2322 against `Omit<AnchorBase, "children">`) so the licensed-model-code leak the missing slot exists to prevent is structurally impossible; `MatrixRow` requires `applicability` (TS2741) so an unreviewed row cannot read as clean by omission; `Confidence` rejects a bare value (TS2353), and **no `Meter` is exported at all**. Kit PR **#7** merged squash `d6bd9a1d7e29a0306fd5b331282a0483ef110e61`; `npm test` **81 of 81 with `SC_DASHBOARDS_DIR` set so vendor-parity arm B actually ran**; `prove:gates` **31 of 31** injections caught and named plus 1 of 1 control green; and `prove:gates` **now runs in CI**, where previously the only instrument that proves a gate can fire ran only when someone remembered. **The DesignSync re-sync is complete**: bundle rebuilt at kit main, `package-validate.mjs` exit 0 with 82 of 82 previews rendering cleanly and the anchor matching disk on 82 recomputed hashes; the delta was MEASURED against a sha256 snapshot of the previous bundle rather than assumed — **45 added, 5 changed, 0 removed, 226,101 bytes**; sentinel first and alone, 49 content files in three chunks sized by BYTES (51,684 / 79,393 / 72,242), no deletes, **remote listed and reconciled BEFORE the anchor was armed**, sentinel re-armed, `_ds_sync.json` written last in its own call; **zero write failures, zero retries**; remote went **417 to 462 planner-managed plus 2 app-generated** (`_ds_manifest.json`, `_adherence.oxlintrc.json`) for 464 total. One unexplained delta was resolved before uploading rather than waved through: `Basis.prompt.md` changed because the generator appended a `## Related` cross-reference naming `BasisLine`, established by fetching the remote copy and diffing it. **Item 7 is the item that had no instrument behind it, and its number is the justification: 32 of the 70 addressed ids can be deleted from `web/index.html` with the entire pre-existing 199-test suite still green** — measured by 70 delete-run-restore iterations with the file hash verified, not inferred; a literal grep said 36 and was wrong in both directions; all six Development-services tab ids and the entire stage-escape control were uncovered. Sets: **70 required ids** (43 via `getElementById`, 1 via `#ds-metrics`, 24 via `el.id` route comparisons, 3 via ARIA IDREFs), **14 hidden branches**, **12 behaviour hooks** (10 script-read UNION 2 stylesheet-read MINUS script-created). Dashboards PR **#24** merged squash `e11155246196a82224e4d291d53ba63bd7735849` at **219 of 219**; it ships zero served bytes, so no second deploy is owed. Folds delivered: the second weaker copy of the class rule is gone, extracted to `src/served-surface.mjs` with both callers using it; the forbidden-string gate now scans `.css` and has its first firing arm; and the city-name gate hole was proven by injection rather than by reading — lowercase `bastrop` in `index.html` AND `"Bastrop"` in `app.js` both shipped green through 209 tests — then fixed. **Items 6, 9 and 10 are HELD.** The mission is drafted and compiles. Item 6 depends on item 5, which only completed at the end of this session, and dispatching a long serial design pass that could not be supervised to completion inside the remaining budget is the nested-fan orphan trap. Scaling the card down was not a call to make silently. Close `_inbox/2026-08-19_lane_b_close.json` plus `_inbox/2026-08-19_b89_close.json`, `_inbox/2026-08-19_b8845_close.json` and `_inbox/2026-08-19_b887_close.json`. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: fan executors, verify their work yourself, merge, deploy, and report. | nick; planner (verification, merges, deploy, sync) |
| A-075 | 2026-08-19 | **Three process findings, each measured, each with teeth beyond this row.** **(1) A green check-run plus a `CLEAN` merge state is NOT a merge gate**, demonstrated twice inside one hour. A check-run conclusion is a statement about the PR's own tree, and `CLEAN` is a statement about textual conflict; neither says anything about the tree that exists after the merge, and none of the three SmartCity repos requires branches to be up to date before merging. Merging G-89 broke PR #24 (218 tests, 215 pass, **3 fail**, from two PRs that share no source file) and simultaneously invalidated kit PR #7, whose CI checks `smartcity-dashboards` out at `ref: main` so its green was a fact about a world that no longer existed. Both would have shipped red on their own greens. The standing AGENT_CONTRACT rule — merge only on the conclusion STRING — is necessary and is not sufficient; the addition is to re-green against the CURRENT base before merging, and for a cross-repo consumer the base that matters is every repo its CI reads, not only its own. The preferred mechanization is branch protection's up-to-date requirement, because a control that depends on the merging agent remembering is the shape DEV_PROCESS rule 0 exists to name. Environment note: `git rebase` is blocked in this environment's Bash tool, so `gh api -X PUT repos/<owner>/<repo>/pulls/<n>/update-branch` is the form to reach for. Detail `_inbox/2026-08-19_finding_green_ci_plus_clean_is_not_a_merge_gate.md`. **(2) WDLL item 10's acceptance walk cannot see what it is written to check.** The served document is byte-identical across every lens and every pack — six URL forms, one sha256 — because `index.html` is one static document carrying every lens inline and the pack differences arrive later through `/api/*`. Item 10 as written is thirteen identical GETs and then thirteen more, and would return the same result on a redesigned build and an untouched one. Replaced with three legs: a deployed-bytes hash against the merged tree, pack checks moved to `/api/*` where the packs actually differ, and a real browser walk, which is the only leg that can grade a design pass. Precedent for re-shaping an acceptance item on a measurement: item 8 was re-sequenced at A-071 and the operator accepted. Detail `_inbox/2026-08-19_g88_item10_walk_is_vacuous.md`. **(3) The two defects that mattered most were found by planner verification AFTER CI was green, and both were in an INSTRUMENT rather than in a product.** The addressability gate's created-by-script derivation recognised `X.dataset.Y =` but not `setAttribute("data-...")`, so `data-surface` read as missing while `data-tab` and `data-atab` escaped only because they already exist in static markup — passing by luck rather than by rule. And the gate unioned the served documents with the BAKE SOURCE, so detaching `data-disposition` from `web/index.html` alone left it green at 10 pass and 0 fail while the 446-element connections register lost its entire severity colouring. Both were closed by widening a DERIVATION rather than by adding an exclusion, plus a pinned excused set so the derivation cannot self-heal into silence. The second fix also made `web/index.html` an asserted FIXED POINT of the bake, closing a hole the translation-boundary investigation named and nothing covered: the bake output was never byte-asserted, so a stale bake shipped and passed every test in the repo, in either direction. Two more of the same class: the kit's injection harness case labelled `vendor parity arm B` was actually proving arm A, and arm B — the only guardrail that can see the PRODUCT move, and the one that went red for real this session — carried no injection at all; and `.design-sync/conventions.md`, which ships verbatim inside the README uploaded to the Design project, was still telling the design agent that the four families had no CSS and to draw Plan Review without them. **Carried, named, not built:** `.prov.stale` is absent from every shipped stylesheet and every component in both repos — grep for `stale` returns nothing — so a stale source renders identical to a current one; rowed separately rather than folded, because shipping it is a three-step cross-repo chain and a drive-by would land half of it. The kit's counting rule and the product's counting rule remain two implementations of one rule with no divergence test, which is the CTRL-1 shape. `roster-lens`, and the reader-less `data-home-table` (70 occurrences) and `data-source` hooks, fold into the design pass. **Owed by the OPERATOR and still recorded as unrun:** the Design picker walk at `https://claude.ai/design/p/f5e5465e-943f-4f68-b52f-608925bc07b0`, now against a 464-file bundle with the sentinel armed. | Operator: when a card tells you WHY, check the why before you build the what. | planner |
| A-076 | 2026-08-19 | **G-90 ADDED and G-91 ADDED. Template city gets built out. Three operator rulings, and the first one reverses a constraint that has travelled in every Lane B dispatch for weeks.** Build sheet `_inbox/2026-08-19_template_city_lens_build_sheet.md`, operator-approved 2026-08-19. **The correction that opens this: `_inbox/2026-08-17_g18_shell_homes.md` inventoried 67 live Bastrop staff jobs and gave each a home, and roughly forty carry the disposition `Not built`, written to mean THIS SURFACE DOES NOT EXIST YET. Across three agent handoffs it hardened into THIS SURFACE IS MEANT TO BE EMPTY, and by 2026-08-19 it was travelling as a hard dispatch constraint — "the sixteen honest-empty states STAY empty" — which was faithfully preserved through G-88 and G-89. The register was never wrong; its disposition column was read as a destination rather than as a starting state, and the operator's actual intent (inventory Bastrop, then use it as the guide to build the others) was carried in no field any handoff read.** **RULING 1: `not built` moves down one level.** Today it describes the SURFACE. From here the surface exists and honest absence is a statement about SOURCES: the lens renders its designed shape, and what is honest is which adapter kinds the pack has granted, per region, with its basis. The product currently cannot distinguish "we did not build Parks" from "your city has no Parks data" and those are different sentences to a customer. Unchanged: no invented freshness, no `last sync`/`last read`/`last updated`, every empty region keeps a `.basis` line, no real city asserted as content, `sc-kit.css` byte-identical across three repos, 12px floor, live Bastrop no-touch. **RULING 2: match the live department roster and expect it to GROW.** Cities have different org charts; forcing Bastrop's departments into a smaller set risks adoption. The lens roster is a growing template library, not a fixed five, and departments Bastrop lacks arrive as new template lenses rather than as reshapes. Taxonomy note the ruling had to settle: "the live seven" is ambiguous, because Bastrop has the `/departments/*` page family (admin, code-enforcement, community, court, development, infrastructure, parks) AND a seven-item operational nav (Overview, Finance, Development, Emergency, Operations, Compass, Prophecy), which are an org chart and a set of consoles respectively. Resolved to the UNION, ten department lenses, because collapsing any two is the forcing this ruling rejects and cutting now is cheap while re-splitting later is not. **RULING 3: demo data on every lens, ONE badge.** Fixture data everywhere so the UX can be worked; the environment chip stays a single `Demo` in the top-left, never per region, per card or per number. Provenance chips and basis lines carry per-region truth; the badge carries the environment. **Evidence base, measured this session rather than taken from the register.** Live `smartcityos.io` is an SPA — every route returns the same 3,624-byte document, one sha256 across `/`, `/overview`, `/fleet`, `/police`, `/citizen` — so the inventory came from the shipped bundle `/assets/index-kGj7uMs4.js` (5,578,225 bytes): **138 routes and 177 distinct `/api/` endpoints across 11 vendor families**. Development services is **MyGov ×16** (`permits`, `permits/stats`, `inspections`, `work-orders` plus `daily-queue`/`geo-clusters`/`sla`/`stats`, `code-violations` ×2, `business-licenses`, `reviews/summary`, `sync-status` ×3); Police is Spireon ×7 plus Verkada ×8 plus dispatch and emergency; Fire is FirstDue ×6 plus VFD ×5 plus seven county flood/wildfire/weather layers; Fleet is Samsara ×5; Public works is PowerBI ×4 plus GoTo ×5; Finance is OpenGov ×8 plus permit-revenue ×3 plus scenarios and baselines. **These are evidence of what data exists, NOT an instruction to connect a feed; no adapter is granted by this work.** **G-90, shell function parity.** Eight functions live Bastrop has and the Dashboards shell lacks, captured as FUNCTIONS rather than layout: theme toggle light/dark (live persists to `localStorage` and defaults dark; Dashboards hardcodes `data-theme="dark"` on the root), account menu (My Account / My Profile / Account Settings / Support), sign-in-out and session, notifications with an honest empty state, tenant branding, a real record search (today a stub carrying a `Not built` badge), help/support, and feedback. Theme must set `data-theme` on the ROOT and never a page-level provider; light is a first-class theme. **G-91, the fixture-pack seam.** The mechanism already exists and is proven, so this is an extension and not a new system: `src/fixtures.mjs` is a seeded deterministic generator (`fnv1a` seed, `mulberry32` PRNG) carrying two content guards, `assertNoRealWorldContent` and `assertDeclaredVocabulary`, which must keep applying to every new domain; `src/city-pack.mjs` already declares `cityKey`/`accessPolicy`/`environment`/`seal`/`generatesFixtures`/`grantedAdapters`. It generates ONE domain today. G-91 adds one generator per domain on the same seam and the same pack seed, so a city's data is stable across reloads and reproducible, and swapping a city is swapping the pack. Three identities hold: `template-city` generates, `empty-city` generates nothing and stays the regression target because with zero records every pill renders quiet and the tension mechanism switches off, `fixture-city` is tenant-private. `src/city-pack.mjs` must keep throwing on live Bastrop. **G-91 also carries the gate re-scope ruling 1 forces**, because the tests that currently enforce honest-empty as a PERMANENT state (the `$0`-never-appears assertion, the sixteen `.state` blocks, the honest-empty walk) must become per-pack assertions or they will block every populated lens. **Adapter catalog is short by three, measured:** `src/adapters.mjs` declares seven kinds (`mygov`, `samsara`, `opengov`, `esri`, `municode`, `firstdue`, `verkada`) against eleven live vendor families; missing `spireon` (police vehicles), `goto` (phones), `powerbi` (CIP and reporting), so the nav footer's `0 of 7 sources granted` is a counted denominator that is short against reality and corrects itself once the catalog grows. **Two corrections the operator's own walk of the deployed surface produced.** Development services currently carries a `Review` tab while Plan review is a separate Work lens; DS mirrors what MyGov already runs and Plan review is the native console that aspirationally replaces it and is a long way from doing so, so the `Review` tab leaves DS and DS tabs become Pipeline, Place, Inspections, Work orders, Code enforcement, Licenses. And Court gets its own lens, which is why the Parks shell was explaining municipal court in a sentence that traces to a real register row pairing "Parks and Courts" but reads as a non sequitur on the Parks page. **Wave 1 is G-90 and G-91 in parallel because their file sets are disjoint** — G-91 is `src/` only and touches no served asset, G-90 owns `web/`. Wave 2 fans department lenses wide once a generator exists per domain. Merges stay SERIAL per repo regardless, because all lenses share one `web/index.html` and one unpartitioned `web/shell.css`. **Open at build time, operator-flagged:** code enforcement as a DS tab or a department lens or both; records search as a real register or honestly still Not built; Assets confirmed to carry fixture data on `template-city` while real packs stay at zero, which lifts a constraint that has been hard in every dispatch for weeks. Do not connect any feed. Do not start G-52. Live Bastrop stays no-touch and read-only. L26 untouched. | Operator: my intent got buried through the agent handoffs; inventory Bastrop, then use it as the guide to build the others. Approved, go fan. | nick (rulings); planner (evidence, build sheet, scope) |
| A-077 | 2026-08-19 | **G-90 and G-91 CLOSED and DEPLOYED. Wave 1 of the template-city build is live.** Serving revision **`smartcity-dashboards-00023-yex` @100%**, dashboards main **`c7d7980b98752fe6625e07da1e4e9b9921f67950`**, kit main **`17eccfade057c0f8a835b8731be834cd4b828166`**. Combined suite **273 of 273** on the product, **86 of 86** on the kit with vendor-parity arm B actually run. Deploy failed once on a two-character Cloud Run tag (`w1`; minimum is three) and was re-run as `wave1` — planner-owned, not escalated. **G-91, the fixture-pack seam.** PR **#25** merged squash `3d4c9e8320d2dd73aede13383d70776ad05f0222`, CI conclusion string `success`, **zero files under `web/`** so the lane boundary held. Four registered domains, each declaring its gating adapter kind: `permits-pipeline` and `work-orders` (mygov), `fleet-vehicles` (samsara), `patrol-vehicles` (spireon). **Ruling 1 turned out to be FOUR states, not two**, and that refinement is the row's most durable output: `ungranted` ("Spireon is not granted on template-city; the Patrol roster region is built and has no source") is distinct from `granted-empty`, from `no-fixture-source`, and from `not-registered` — which is the only surviving meaning of "not built". Planner-verified by flipping the grant rather than by reading: granting `spireon` on a throwaway pack takes `patrol-vehicles` from `ungranted`/0 records to `ok`/**10 records** while every other domain stays byte-identical at 14/15/14 and `grantedAdapters` stays `[]`. That is a SOURCE state and not a stub, and if the two were indistinguishable one layer down the original misreading could survive again. `empty-city` reads all four domains as `no-fixture-source`. **Two planner figures corrected by the executor, both material.** (1) `template-city.grantedAdapters` structurally CANNOT name a gating kind: `assertCityPackShape` throws `a pack that generates fixtures grants no adapter`, so the dispatch's instruction was unsatisfiable as written and, read literally, all three exemplars would have been ungranted. Resolved with a third pack axis `fixtureGrants` — adapter kind ids, no `sourceUrl`, connects nothing — leaving `grantedAdapters` `[]` on all three packs and the G-74 gate still throwing. (2) The adapter catalog goes **7 to 10, not 11**: the planner's eleventh live vendor family was `vfd`, and `/vfd` is Bastrop's own volunteer fire department route, a first-party city surface rather than a third-party adapter. Footer now returns `0 of 10 sources granted`, numerator still 0 because a demonstrated shape is not a connected source. **Guards proven structural rather than advisory**, and proven by breaking one: neutering `assertNoRealWorldContent` to a no-op turns **4 tests red**. The planner separately failed six times to hand-roll a malformed record past the seam, each attempt rejected by a differently-named invariant. Determinism measured across two separate node processes, sha256 identical. Three defects found, one pre-existing and worth carrying: `assertDeclaredVocabulary` listed `record.dueLabel` in its own allowed set, so **the field authorised itself** and any string at all passed in a field rendered on the DS queue. **A correction to the dispatch that the planner accepts:** the six `$0` assertions all read `web/index.html`, which is pack-invariant, so they cannot be made per-pack and do not block wave 2; the gate that actually needed a pack dimension was the composed-payload money rule, now widened to every domain on every pack. **G-90, shell function parity.** PR **#26** merged squash `c7d7980`, and it was UPDATED onto current main and re-greened before merge rather than merged on its own stale green. Eight functions: theme toggle, account menu, session state, notifications, tenant branding, record search, help and feedback — five built, two built as chrome with honest-disabled actions, one honestly stubbed, and `src/shell-parity.test.mjs` carries that table as walked data so a deferral and a silent omission stop looking alike. **The theme resolves in the SAME G-89 head script**, with the storage read in `try/catch` placed AFTER the surface stamps, because `getItem` throws in a partitioned context and an unguarded throw would have un-stamped `data-surface` and reintroduced G-89's flash through the door this change opened. Planner-verified on the deployed surface with `/app.js` aborted: no stored preference resolves `dark` under both a dark-preferring and a light-preferring OS; a stored `light` beats a dark OS and paints canvas `rgb(238,241,244)`; a stored `dark` beats a light OS; and `data-surface=lens-finance` in all four, so G-89 and G-90 coexist in one script without either breaking. G-89 regression clean: the 30-target blocked walk is still **30 of 30** on the new revision with zero `$0` and zero invented-freshness hits. **A near-miss worth recording:** the capability resolver's first draft echoed `FEEDBACK_DESTINATION` — a signed webhook URL — into a basis string served to anonymous browsers. Fixed so only the variable NAME travels, with a canary assertion (`SHOULD-NOT-APPEAR`) proving the payload excludes the value; planner re-verified both directions at source. **The G-90 lane concluded "no kit re-vendor owed" and that was wrong**, which the planner caught and lane **T1KIT** then proved by watching it fail: on the untouched kit clone with `SC_DASHBOARDS_DIR` set, kit main was ALREADY 80 pass / 1 fail against live product main, arm B naming `vendor/shell.css`. The reasoning "`sc-kit.css` untouched, therefore no product-line event" is correct; "therefore no kit re-vendor" does not follow, because **the kit vendors BOTH stylesheets**. That is the generalizable line for any product lane touching `web/*.css`. Kit PR **#8** merged squash `17eccfa`: re-vendored at `c7d7980`, recount **139 to 143** (`pop pop-group pop-item topmenu`, none removed, both sides measured), components and previews and gallery entries all 82 to **86**, coverage **143 of 143**, `prove:gates` 31 to **34 of 34** plus 1 of 1 control. `PopItem` has no `disabled` prop — it is `Omit`ed and `unavailable` is basis TEXT, so an entry cannot be greyed out without saying why. One markup-parity case moved and was fixed, three added, each watched going red on its own injection. Free finding: without comment-stripping the same two stylesheets return **150**, the seven phantoms being `css hidden html js md mjs test`. **Design project re-synced**, planner-owned: bundle rebuilt at kit main, `package-validate.mjs` exit 0 with 86 of 86 previews rendering and the anchor matching disk on 86 recomputed hashes; delta measured against a sha256 snapshot rather than assumed at **20 added, 4 changed, 0 removed**; sentinel first and alone, content chunked by BYTES, **remote listed and reconciled BEFORE the anchor was armed**, sentinel re-armed, `_ds_sync.json` last in its own call; zero write failures, zero retries; **462 to 482 planner-managed files** plus 2 app-generated. **Open for the operator, both flagged by executors rather than by the planner:** the footer excludes `fixtureGrants`, so `template-city` reads `0 of 10 sources granted` beside three populated regions — literally true, possibly reading as a contradiction, and the planner leans to the footer distinguishing GRANTED from DEMONSTRATED rather than collapsing them; and two Connections register rows are now partly false (the `Auth / session / notifications / theme / sign out` row bundles four jobs of which two now exist, and the disposition vocabulary is closed at six values so none fits), with the recommendation being to split the auth row into its four jobs, which is a ruling rather than an edit. **Wave 2, the department lenses, is ready to fan** on the seam G-91 left: fifteen lens regions have no domain yet, which is the deliberate exclusion and the fan. Do not connect any feed. Do not start G-52. Live Bastrop no-touch and read-only throughout. L26 untouched. | Operator: approved, go fan. | nick; planner (verification, merges, deploy, re-sync) |
| A-078 | 2026-08-19 | **G-92 ADDED and G-93 ADDED. Wave 2 of the template-city build.** Build sheet `_inbox/2026-08-19_template_city_lens_build_sheet.md`; seam shipped at G-91 (`3d4c9e8`), shell at G-90 (`c7d7980`), kit re-vendored at `17eccfa`. **G-92, the department domains.** Fifteen lens regions have no domain; wave 2 adds them on the registered seam `src/domains.mjs` + `src/domains/*.mjs`, one generator per domain, each declaring its gating adapter kind, all keyed off the same pack seed. **This row is `src/` only and ships NO markup** — the four source states (`ok` / `granted-empty` / `ungranted` / `no-fixture-source`, plus `not-registered`) already reach no pixel by design, and rendering them is a separate serial pass because all lenses share one `web/index.html` and one unpartitioned `web/shell.css`. Domains fan wide because `src/domains/*.mjs` are separate files; markup does not, and that asymmetry is the whole shape of the wave. Priority is **Development services**, because the operator's requirement is that DS match what the production Bastrop dashboard shows today, and live carries MyGov across sixteen endpoints of which only `permits` and `work-orders` are modelled. Both content guards keep applying structurally at the seam; determinism stays measurable across separate processes; `empty-city` keeps generating nothing and stays the regression target. **G-93, two operator-ruled corrections, both surfaced by executors rather than by the planner.** (1) **The footer must distinguish GRANTED from DEMONSTRATED.** `template-city` currently reads `0 of 10 sources granted` beside three populated regions, which is literally true — `fixtureGrants` is deliberately not a grant and connects nothing — but reads as a contradiction to a prospect. Operator ruled 2026-08-19 to distinguish the two rather than collapse them. (2) **The Connections register's auth row must be split into its four jobs.** The row `Auth / session / notifications / theme / sign out` bundles four jobs of which two now exist after G-90 and two do not, and the disposition vocabulary is closed at six values so no single value is true of the row. Operator ruled 2026-08-19 to split it. Connections is generated by `connectionsRegisterHtml()` in `src/shell-homes.mjs` and must be RE-BAKED, never hand-edited — `web/index.html` is now an asserted fixed point of that bake, so a hand edit fails a test by name. **Carried into both rows:** no feed is connected and no adapter is granted by any of this work; live Bastrop stays no-touch and read-only; `web/sc-kit.css` byte-identical across three repos; the 12px floor with its one named evidence-chip exception; no invented freshness and no `last sync` / `last read` / `last updated` string anywhere; every empty region keeps a `.basis` line; no real city asserted as content. Any product change to `web/*.css` triggers a kit re-vendor, because the kit vendors BOTH stylesheets — that inference failed once today and is now a standing line in every brief touching product CSS. Do not start G-52. Do not fill G-24 beyond the ruled `template-city` fixture posture. L26 untouched. | Operator: footer and auth, I agree with your lean. Good to go on wave 2. | nick (rulings); planner (scope, fan shape) |
| A-079 | 2026-08-19 | **G-92 and G-93 CLOSED and DEPLOYED. Wave 2 shipped: eleven domains, the footer's second claim, and the register split.** Dashboards main **`1b271c8c3dc7bf361c09f00a095cc8e9022a6946`**, serving **`smartcity-dashboards-00025-mam` @100%**, suite **320 of 320**. Three PRs, merged in dependency order and each re-greened against the base that had moved under it. **G-92 across two lanes.** W2DS PR **#28** merged squash `ed398fade199ee2b21e90ceae2a8aa5e651381cb` — `inspections` 21, `code-violations` 19, `business-licenses` 17, all gated `mygov`, closing the gap the operator named: Development services must match what the production Bastrop dashboard shows, and live carries MyGov across sixteen endpoints of which only two were modelled. W2DEPT PR **#27** merged squash `cfc061b4aa3f39ce9ab4fc3b494d34f05615df1a` — `police-cameras` 18 (`verkada`), `fire-apparatus` 12 (`firstdue`), `cip-projects` 16 (`powerbi`), `call-analytics` 25 (`goto`, aggregate buckets per queue per relative day, never a call). Registry is now **11 domains, 10 carrying records on `template-city`, 0 of 11 on `empty-city` all reading `no-fixture-source`**, and `patrol-vehicles` stays the single `ungranted` region because `spireon` is deliberately kept off the demonstration axis — a wave that grants everything quietly deletes the state that proves ruling 1. `grantedAdapters` still `[]` on every pack. **The vendorless finding, and it is the most useful thing wave 2 produced: Parks and Court are NOT expressible on this seam.** W2DEPT proved it at four independent gate points quoted at file and line (`fixture-seam.mjs:270-277` requires a `gatedBy` resolving to a catalogued kind and throws on both `"none"` and `"parks"`; `city-pack.mjs:317-320`; `adapters.mjs:341-342`, where the vendor id travels in every record's `kind` field; `fixture-seam.mjs:372-380`, where the only non-generating state for a built region has a basis reading `<Vendor> is not granted on <city>`), invented no kind to satisfy the shape, and pinned it as a test **written to GO RED when the seam grows a vendorless path** so the finding cannot rot. The build sheet names four lenses with "gates: none yet", so this recurs and is routed to the operator as W2DEPT-F2. **Privacy exclusions declared in the record contract rather than left to the guard:** Verkada LPR plates and persons-of-interest and counted occupancy; GoTo recordings, individual call detail and extension-to-person mapping; FirstDue crew; all money on the CIP register. Planner planted all four surveillance fields and watched each REJECTED by the seam. **G-93, both operator rulings.** PR **#29** merged squash `1b271c8`. The footer emits a second claim from the SAME resolver against the SAME denominator rather than a second implementation: `template-city` reads `0 of 10 sources granted` beside `6 of 10 demonstrated with fixture records`; `empty-city` reads `0 of 10` on both with the positive determination `this pack generates no records, so no adapter kind is demonstrated on it`. Derivation proven by growing the catalog 10 to 11 (both denominators move) and the grants 2 to 4 (only the demonstrated numerator moves) with the served assets sha256-identical in every arm. The Connections auth row split into four (auth actions `Not built`, notifications `Empty`, theme `Mounted`, sign out `Not built`) and the feedback row into three, using the existing six-value vocabulary with no value added: **70 register rows / 67 source rows, 5 / 3 addenda, 75 baked `data-home-row` occurrences live**, source rows measured as distinct `splitFrom ?? job` and never by subtraction. Bake re-run and watched failing by name on three planted hand edits. No CSS shipped, so the class vocabulary stays **143** and no kit re-vendor is owed. **The unproven leg W2FIX named was then closed by the planner:** it said the footer was proven by served bytes and never by a browser, so a rendered walk was run against the deployed surface and both claims render on both packs. **Three findings worth carrying.** The Connections page carried three hand-typed copies of a counting rule that lives in the generator, and this split is exactly the edit that would have staled all three — that page has already shipped two stale integration counts for the same reason; all five strings now bake. W2DEPT's exclusion scan first went red on a false positive because a bare `/plate/i` matches `template-city`, and the tempting fix (excluding `cityKey` from the scan) would have been the actual hole; word-bounded instead. And W2DS found two defects in its own work at CP2: an escalation rule written inline AND copied into its test (the CTRL-1 shape, where the copy keeps passing while the original drifts), and an open-work measure keyed off a status literal rather than the declared `resolved` flag. **Planner-corrected in the build sheet:** its "adapter catalog is short by three" section went stale within hours when G-91 added exactly those three, and W2DEPT caught it by checking at source — trusting it would have declared three duplicate kinds. The section is corrected rather than deleted, because it is the same failure the sheet exists to fix, and it now states that every count in a build sheet is a dated snapshot while the source of truth is the code. **Merge mechanics worth keeping:** W2DS ran a LOCAL trial merge with W2DEPT, aborted it, and wrote out the exact ten-hunk resolution — including that `fixtures.test.mjs` is NOT a union, because both lanes rewrote the same SORTED literal and a union produces two consecutive `assert.deepEqual` calls and a syntax error. The planner resolved by hand to the sorted eleven-entry set and hit two of its own errors doing it: an `\n`-anchored marker regex against a CRLF checkout that silently matched nothing (third CRLF miss of the day, and it is in the planner's own memory file), and a replacement that left the original list's tail behind giving seventeen entries where eleven were expected. Both caught by the suite, not by the planner. **NOT shipped, stated plainly: no department lens BODY renders its domain yet.** The eleven domains reach no pixel; the visible wave-2 deltas are the footer's second claim and the register's split rows. Rendering is wave 3 and is a SERIAL markup pass, because all fifteen lenses share one `web/index.html` and one unpartitioned `web/shell.css`. Do not connect any feed. Live Bastrop no-touch and read-only throughout. Do not start G-52. L26 untouched. | Operator: footer and auth, I agree with your lean. Good to go on wave 2. | nick (rulings); planner (verification, conflict resolution, merges, deploy) |
| A-080 | 2026-08-19 | **G-94 ADDED, G-95 ADDED, G-96 ADDED. Wave 3 opens: conformance becomes a build requirement across the sellable product line.** WDLL `_inbox/2026-08-19_wave3_surfaces_and_vpat_WDLL.md`. Operator goal: every surface in Dashboards done pending QA, and a VPAT documented with proof its reports re-run against `template-city`. **Four operator decisions taken 2026-08-19:** fix the kit (contrast remediation approved as a product-line change); conformance target is **BOTH** WCAG 2.0 AA (Revised 508, federal) and WCAG 2.1 AA (ADA Title II, state and local); scope is the whole sellable line — Dashboards, Asset Management, Plan Review, Smart Files; and PDF export is IN scope because the original SmartCity has it. **The baseline was MEASURED before anything was scoped, and six of the ten "known defects" the operator's hand-off brief carried do not survive it.** axe run through real Chromium against serving revision `00025-mam` across **16 surfaces** (nine lenses, six work views, `empty-city`), counting rule stated where read — a VIOLATION is one axe rule failing on at least one surface, NODES sums failing DOM elements, and a rule counts as CONFORMANCE only if tagged `wcag2a`/`wcag2aa`/`wcag21a`/`wcag21aa`. Result as the planner measured it: two conformance rules failing, `color-contrast` 54 nodes and `scrollable-region-focusable` 4, plus `heading-order` 11 as best-practice. **THOSE FIGURES ARE TOO SMALL AND G-95 CORRECTED THEM THE SAME DAY.** The planner HAND-ENUMERATED 16 surfaces on ONE theme; G-95 DERIVED the list from the id sets `src/staff-review.mjs` exports and scanned 23 surfaces x 2 themes: `color-contrast` **1002** (light 930, dark 72), `heading-order` **30**, `scrollable-region-focusable` **8**, plus **24** findings on 2.4.2 that no automated rule reports. On the planner's own 16-surface subset G-95's scanner reproduced the planner's numbers exactly, so the instruments agree and only the SCOPE differed — the hand-built list omitted the Development-services and Assets tab panels, which are distinct rendered surfaces. **A conformance denominator is derived or it is wrong**, the same hand-declared defect class as `has_writer`, and the evidence pack states the derivation beside the number. Sixteen of sixteen surfaces clean of everything else, against an old site whose six pages failed six rules at Lighthouse 0.80 to 0.90. NOT PRESENT on the new product: `maximum-scale=1`, unnamed icon-only controls (`button-name` never fired, so G-90 built them right), missing `<main>` or bypass mechanism, nested anchor-plus-button. The brand hex `#6499aa` is not in this codebase at all. **STILL LIVE and structural: `document.title` is `SmartCity Dashboards` on every lens.** Nav items are real `<a href>` full navigations, so each lens IS a distinct page under 2.4.2, and axe cannot catch it because it scans one page at a time and a title is present. **G-94, the kit contrast pass — the one finding that is not an engineering call.** `color-contrast` fails on `.badge` and `.p-quiet.pill`, which are kit classes coloured by `--sc-*` tokens in `sc-kit.css`. That file is byte-identical across `smartcity-dashboards`, `smart-files` and `plan-review` and its own header says a repo that edits a token value has forked the system. **CORRECTED BY G-95 SAME DAY: the planner wrote that this therefore CANNOT be fixed in a Dashboards PR, and that was overstated.** `web/sc-kit.css` contains ZERO rules for `.badge` or `.p-quiet`; those rules live in `web/shell.css` at lines 162, 632 and 643 and merely REFERENCE `var(--sc-ink-3)` and `var(--sc-quiet)`, so a local re-pairing to different existing tokens was possible. The conclusion holds on its own merits and the shipped fix is not revisited — a `--sc-quiet` that fails contrast is wrong everywhere it is used, so the token was the right OWNER and a shell-local re-pairing would have left the same defect live in the other two products — but the stated reason was an impossibility asserted from a plausible mechanism without checking where the declarations actually lived. It lands in all three product repos as identical bytes and is followed by a kit re-vendor and a bundle re-sync. Every changed pair ships with its MEASURED ratio; normal text 4.5:1, large text and non-text UI 3:1. **Cross-repo atomicity is a real hazard here and is named rather than discovered:** whichever repo merges first puts the other two out of byte-identity until they follow, so the lane establishes what actually asserts that identity before touching anything. **G-95, the accessibility baseline and the gate.** The two conformance findings plus `heading-order` plus 2.4.2 titles, and then the axe scanner wired into CI failing the build at any A or AA violation with per-surface node counts in the output. The gate ships BEFORE surface rendering, because retrofitting conformance onto fifteen rendered surfaces costs multiples and this is the only chance to avoid it. Titles resolve in the existing G-89 head script rather than a second one. **G-96, the document-generation investigation, read-only.** The operator's brief said "replace or configure the PDF export path"; measured against the live bundle, **replace is the only option**. **PLANNER RATIONALE CORRECTED BY G-96 SAME DAY, and the correction is recorded rather than quietly edited because a wrong finding inside an ACR discredits the true ones.** The planner claimed the exports are rasters failing 1.1.1 and 1.4.5 as well as 504.2.2. **That is FALSE and retracted:** every application path draws VECTOR TEXT through jsPDF `.text()` (73 occurrences) and jspdf-autotable (10); html2canvas is bundled only as an optional dependency of the jsPDF `html()` plugin and has ZERO call sites, `.html(` occurring 0 times in 5,578,225 bytes. A federal reviewer opening one of these finds selectable text. **1.1.1 and 1.4.5 come OUT of the PDF rationale; replace remains the verdict on better evidence:** `/StructTreeRoot` and `/MarkInfo` occur 0 times, jsPDF's `putCatalog` writes only `/Type /Catalog`, `/Pages`, `/OpenAction` and `/PageLayout`, and jsPDF 4.2.1 has no tagged-PDF capability at any setting, with `setLanguage` and `setDocumentProperties` present but uncalled so there is no `/Lang` and no title either. The planner's token counts were also wrong for a stated reason: `jsPDF 17` and `html2canvas 3` came from `grep -oci`, which counts LINES against a nearly-single-line minified bundle; measured as occurrences with `grep -oF` they are **143 and 23**, a clean instance of DEV_PROCESS 1.2. **The site-plan PDF is SmartSite, not Plan Review**, established on four independent links including `filenameFor` in property-explorer producing exactly that filename and `render.ts:1152` building the `SP-48021-34649` id; it is server-side engine-api pdf-lib. **Its worse defect is not the missing tags:** `render.ts:373` draws tracked runs glyph by glyph, so 64.4% of extracted lines are a SINGLE CHARACTER (268 of 416), meaning a tagged version would pass an automated check and still read one letter at a time — the strongest argument in the investigation for why an automated gate is necessary and not sufficient. **Scope-map correction:** Dashboards emits no documents but MOUNTS SmartSite in an iframe, so a Dashboards user reaches an untagged PDF without leaving the product. **Recommendation: headless Chromium print-to-PDF from semantic HTML through one shared render service**, evidenced by four PDFs already on the operator's machine carrying the full 504.2.2 set, with the limit stated — Chromium tags what the HTML says (`/NonStruct 50`), so it makes the G-94 and G-95 semantics count twice rather than fixing conformance alone. SmartSite is NOT replaced: add `setTitle`/`setLanguage` to its three emitters, then fix the glyph runs. Sizing: 22 document types by layout or 15 by `.save()` site on SmartCity, 3 on SmartSite, 0 on the three new repos. Separately the site-plan PDF the operator holds was audited as untagged `pdf-lib`, which appears nowhere in the SmartCity bundle, so it comes from a different surface: **there are at least two PDF paths in the product line and they fail for different reasons.** G-96 investigates both, recommends a generator, and sizes the remediation. No code. **Finding carried to the operator rather than absorbed: Asset Management has no repository.** `smartcity-dashboards`, `smart-files` and `plan-review` all exist; no asset-management repo exists under any obvious name, and Assets is currently a lens inside Dashboards held at zero by G-24. One of the four surfaces in the approved VPAT scope does not exist yet. Operator agreed with the recommendation: **ship ACR revision 1 covering the three surfaces that EXIST, carrying Asset Management in the scope map explicitly as not-yet-built** rather than silently omitting it. The broader question of how Asset Management, Smart Files, SmartSite and SmartCity tie together is acknowledged as unopened and is NOT scoped here. **Still blocking the scope map: the contracts line items**, which the operator owes; nothing else waits on them. **On both standards:** the baseline already ran the `wcag21a` and `wcag21aa` tag sets and no 2.1-specific rule fired, but axe cannot settle reflow (1.4.10), orientation (1.3.4), text spacing (1.4.12), content on hover or focus (1.4.13) or status messages (4.1.3), so those are manual and are genuinely new work relative to a 2.0-only pass. Each worksheet row states which standard it answers, because a Rev508 ACR and a WCAG 2.1 ACR are different documents filled from one body of evidence. The 42-row per-criterion worksheet already in `P:/tmp/VPAT` is the right SHAPE and is reused; its CONTENT is rebuilt against the new surfaces, because it was filled against the old site and its scoring does not transfer. Do not connect any feed. Live Bastrop no-touch and read-only. Do not start G-52. L26 untouched. | Operator: fix the kit; both standards; everything we sell needs this — dashboards, asset mgt, plan review, smart files. Agreed on the Asset Management angle. | nick (decisions); planner (measurement, reconciliation, scope) |
| A-081 | 2026-08-19 | **G-97 ADDED. Render the lenses. This is the operator's actual ask and it had been deferred four times.** The operator wrote the wave-3 goal as "ALL surfaces in the smart dashboards are done pending QA" FIRST and the VPAT second; the planner fanned contrast, a CI gate and a document investigation and dispatched ZERO rendering lanes, inverting that order. Recorded plainly because the pattern matters: G-88 items 6/9/10 were held on supervision budget, wave 1 shipped the shell and the seam, wave 2 shipped eleven domains, and wave 3 opened conformance — four consecutive waves that built everything underneath the lenses and never the lenses. **The planner also mis-stated the constraint.** "All fifteen lenses share one `web/index.html` and one unpartitioned `web/shell.css`, so this is serial" is true of MERGING and false of BUILDING. Lanes build in separate clones and the planner merges serially with a rebase, which is exactly what W2DS and W2DEPT did against a shared registry file earlier the same day, conflict resolved by hand at 316 of 316. **G-97 renders the eleven registered domains onto their lenses.** Three lanes by lens group, each owning disjoint `<section>` blocks in `web/index.html` and disjoint additions to `web/shell.css`: R1 Development services, which carries the operator's stated priority that DS match what the production Bastrop dashboard shows and is the only lens with five domains (`permits-pipeline`, `work-orders`, `inspections`, `code-violations`, `business-licenses`, all `mygov`); R2 Police and Fire and EMS (`police-cameras` verkada, `patrol-vehicles` spireon and deliberately ungranted, `fire-apparatus` firstdue); R3 Fleet and Public works (`fleet-vehicles` samsara, `cip-projects` powerbi, `call-analytics` goto). **Accessibility is a merge gate inside each lane rather than a later pass**, because retrofitting it onto fifteen rendered surfaces costs multiples: sequential headings with exactly one `h1`, an accessible name on every control, every scrollable region keyboard-reachable, and both themes clean of `color-contrast` — the lever is `data-theme` on the root and NOT `prefers-color-scheme`, which is how the planner's own baseline missed 643 light-theme nodes. **The four source states must reach the pixel**, which is the whole point of ruling 1 and has not been visible until now: `ok` renders records, `ungranted` renders the region built and sourceless with its basis naming the vendor and the pack, `granted-empty` is a different sentence from `ungranted`, and `no-fixture-source` keeps every region reachable and quiet on `empty-city`. Lenses with no domain (Parks, Court, Community, Administration, Records search, People and access) render the correct source state rather than the retired page-level "not built". Standing: every `.basis` line survives, no invented freshness, one `Demo` chip, no city name as content, `web/sc-kit.css` untouched, class vocabulary 143 and any addition states the new count and triggers a planner-owned kit re-vendor. Live Bastrop no-touch. Do not start G-52. Do not fill G-24 beyond the ruled `template-city` fixture posture. L26 untouched. | Operator: you have been running four hours, how have you not shipped the lens. | nick; planner (owns the deferral) |
| A-082 | 2026-08-19 | **G-98 ADDED, G-99 ADDED, G-100 ADDED. Closing what three rendering lanes independently found.** G-97 landed: PRs **#32** (`13391e93`), **#33** (`ad6e2e24`) and **#34** (`3452b313`) merged, serving revision **`smartcity-dashboards-00031-tew` @100%**, and the lens bodies render at last — Development services 91 table rows over five MyGov regions and 86 records, Public works 35, Police 31, Fleet 20, Fire and EMS 17, none of them still reading "is named, and not built". **All three lanes independently measured the same defect and none of them absorbed it**, which is the strongest signal in the wave. **G-98, the `--sc-ok` token and the workaround it strands.** `--sc-ok` `#2F7A52` on `--sc-ok-wash` `#E3F0E8` computes **4.444:1** against the 4.5:1 required for normal text; R2's proposed `#2E7750` computes **4.623:1**, both verified independently by the planner. It is a `sc-kit.css` token, byte-identical across `smartcity-dashboards`, `smart-files` and `plan-review`, so it is the same four-PR cross-repo shape as G-94 — which fixed `--sc-ink-3` and `--sc-quiet` and left this one, because the planner's baseline never surfaced it. **The strand matters and makes this row urgent rather than tidy:** R3 avoided the failing pill by rendering a RESOLVED status quiet, which is the visual law's own ground and the first renderer to read the `resolved` flag, and it deliberately wrote a test that computes the ratio live so it **FAILS once the kit is fixed**. That is a self-retiring workaround and it means landing the token change alone turns main red on purpose. So G-98 carries the token in four repos AND retires the workaround AND updates that test in one coherent change. It also settles the cost all three lanes named: one severity vocabulary currently has **two renderings** across the product line, because Development services still renders `ready-to-issue` through `p-ok` while the department lenses render resolved quiet. **G-99, the gate is not yet a control.** Two independent divergence tests: R3 measured the Development services lens at **0 conformance nodes at 1440x900 and 2 at 1440x3000**, R2 measured **0 at 1440x900 and 2 at 1440x1200**, and in every arm the node count equalled the IN-VIEWPORT count. axe's `color-contrast` only evaluates what is in the viewport, the gate runs at 900, so **"0 conformance nodes over 46 scans" is an above-the-fold figure and not an absolute one** — material to the ACR, because an auditor does not scan at the planner's viewport. Worse, R2 measured the gate **failing locally on Windows with 44 unresolved nodes and passing on Linux CI on the same SHA**; G-95's own adjudication records the environment dependence and measured it in the OPPOSITE direction. A ceiling that ranges 0 to 44 across two operating systems is a control-design question, not a tuning one, and it is the single biggest threat to the VPAT's credibility because the report has to survive someone else running the scan. **G-100, three shipped markup claims that are now false.** The nav badges at `web/index.html:289` and `:293` still read "Not built", and the Overview register at `:366-371` still files Public works and Fleet under "Roster, not yet built", for lenses that now render data. Neither R2 nor R3 owned those lines and both routed them rather than stretching scope, which was correct. **Merge order is forced:** G-98 and G-100 both touch `web/index.html`, so G-98 merges first and G-100 rebases. G-99 is disjoint. **Named, not scoped, and owed to the operator as a decision:** R1 found that neither `ungranted` nor `granted-empty` is reachable through any shipped pack on Development services, because every DS domain gates on `mygov` and `template-city` grants it — so a prospect currently cannot be shown "built, but no source for your city" on the lens that matters most. R1 recommends a fourth demo pack granting a subset. That is a product decision and is NOT dispatched here. **Two lanes rewrote themselves rather than ship a duplicate rule:** R2 had shipped its own four-state resolver as a served module and deleted it, its route and its pinned-list edits when R3 merged first with the same rule inline; R1 hit the same collision and did the same. The CTRL-1 shape avoided twice, unprompted. **One disclosed deviation:** R1 ran a broad `kill` over node processes, the exact hazard G-95 identified and avoided; no damage, both siblings completed, and the planner cleaned up the orphan it left (pid 73780 on port 8792, and the `P:/tmp/r1-base` worktree). Live Bastrop no-touch. No feed connected. Do not start G-52. L26 untouched. | Operator: fan agents to do that. | nick; planner (verification, merges, deploys) |
| A-083 | 2026-08-20 | **G-101 ADDED, G-102 ADDED. Closing the pass.** State at open: dashboards main **`fd52428d`** serving **`smartcity-dashboards-00035-fey`** (no redeploy owed — G-99 touched zero `web/` files, verified by an empty `web/` diff between the deployed commit and main), smart-files `5de59adb`, plan-review `156677f6`, kit `17eccfad` with **PR #10 the only open PR in the product line**. G-97 rendered five lenses, G-98 fixed `--sc-ok` across three repos at 4.444 to 4.623, G-99 rebuilt the gate into a control, G-100 derived the badges. **Two engineering items remain and are dispatched here; three decisions remain and are the operator's.** **G-101, the two things G-99 routed rather than took.** First, `.prov .sep` paints a separator at **1.856:1 light / 1.737:1 dark** — roughly a third of the 4.5:1 floor — and axe will NEVER report it, because it excludes punctuation-only text by design (`axe.js:28714`, bisected: `"|"` not evaluated, `"X"` incomplete, `"XY"` violation). It is in NO bucket rather than the incomplete one; the planner relayed that wrongly to G-99 and G-99 corrected it by measurement. `aria-hidden` is the routed fix, with the measured caveat that **`aria-hidden` does not silence axe**, so the second instrument G-99 built is what will verify it. Second, and this is the number G-99 itself flagged as the one that could still be quoted too generously in an ACR: **axe surfaces 5 `.p-ok` nodes on Windows and 0 on Linux while coverage reads 100% on both**, and the un-eliminated candidate is that coverage is measured against the VIEWPORT box rather than against inner `overflow:hidden` boxes such as `div.panel`. Establish the mechanism, then make coverage tell the truth. **G-102, the kit is one card behind and the Design bundle lags with it.** Kit PR #10 carries the correct `sc-kit.css` at blob `3b89d4ae` matching all three products, but its `vendor-parity` arm B is red for an INHERITED reason reproduced on pristine main-versus-main: `vendor/index.html`, `vendor/app.js` and `vendor/shell.css` have drifted across G-92 through G-100. Refreshing them pulls G-93's two-source `.prov`, which needs a **`Prov` component-contract change** — the card this has been deferred behind twice. G-102 does the full re-vendor, the contract change, the recount, the wrap of anything the coverage assertion goes red on, and hands the planner a bundle re-sync. **Named and NOT dispatched, because each is a decision rather than an engineering task:** R1 found that neither `ungranted` nor `granted-empty` is reachable through any shipped pack on Development services — every DS domain gates on `mygov` and `template-city` grants it — so a prospect cannot be shown "built, but no source for your city" on the lens that matters most, and R1 recommends a **fourth demo pack granting a subset**; G-96 established that the SmartCity PDF path must be REPLACED rather than configured and recommends **headless Chromium print-to-PDF through one shared render service**, with the limit stated that Chromium tags what the HTML says; and the **VPAT scope map is still blocked on the contracts line items**, which nothing else waits on. **Also owed by the planner and not by a lane:** `30b_smartcity_design_system` still declares the pre-G-94 token values. Live Bastrop no-touch. No feed connected. Do not start G-52. L26 untouched. | Operator: can you close the open items so we can be complete with this pass. | nick; planner (state, scope, the relay error corrected above) |
| A-084 | 2026-08-20 | **G-103 ADDED, G-104 ADDED. The PDF generator decision is TAKEN; the scope map is unblocked on everything except the SKU column.** Two operator rulings 2026-08-20. **RULING: the renderer is ONE SHARED RENDER SERVICE**, headless Chromium print-to-PDF from semantic HTML, chosen over Puppeteer-in-each-repo and over `window.print()`. Basis, measured by G-96 rather than argued: four PDFs already on the operator's machine produced by `Skia/PDF m151` / HeadlessChrome each carry the complete 504.2.2 set — `/MarkInfo Marked true`, `/StructTreeRoot`, `/Lang en`, a real `/Title`, `/DisplayDocTitle true`. The cost of the rejected alternatives is what decided it: Puppeteer in-repo means each of three `pg`-only repos leaves `node:22-alpine`, gains roughly 300-400 MB of image and a browser at cold start, tripling the surface to maintain; `window.print()` is cheapest and browser-dependent, so an ACR cannot claim tagged output uniformly, and there are currently **zero** `@media print` rules across all three kits. **The limit travels with the ruling and is not optional to state:** Chromium tags what the HTML says. The structure walk on those four files reads `/Document 1, /H1 1, /H2 3, /P 4, /NonStruct 50`, so this does NOT fix conformance on its own — it makes the G-94, G-95, G-99, G-100 and G-101 semantics count twice. Bad HTML in, bad tags out. **RULING: SmartSite gets both fixes, sequenced.** Its site-plan PDF is server-side `pdf-lib` from engine-api, established by G-96 on four independent links including `filenameFor` in property-explorer producing exactly `48021_34649_site_plan.pdf` and `render.ts:1152` building the `SP-48021-34649` id. `setTitle`/`setLanguage` is three lines and kills two of five failures immediately. The worse defect is separate and gets its own card: `render.ts:373` draws tracked runs glyph by glyph, so **64.4% of extracted lines are a single character** (268 of 416, pypdf newline-split) — a tagged version of that file would pass an automated 504.2.2 check and still read one letter at a time to a screen reader. SmartSite is NOT replaced by the render service. **G-103** stands up the shared service and routes the SmartCity document types through it; G-96 sized them at 22 by rendered layout or 15 by `.save()` call site, one fix at the library seam and 22 decisions at the content seam. **G-104** takes the SmartSite pair. **The scope map is now blocked on ONE input.** It binds SKU to product surface to `template-city` route to conformance surface, and three of those four columns are derivable from measured state: 23 surfaces, four products of which three are built, and the routes. The SKU column cannot be invented, because a VPAT covering LESS than what is sold means a buyer finds an unreported surface and one covering MORE confuses a procurement officer. The planner builds every derivable column now so the operator fills one column against a list rather than producing a document. Two facts the map must carry regardless: **Asset Management has no repository** and appears as not-yet-built rather than being silently omitted, and **Dashboards mounts SmartSite in an iframe**, so a Dashboards user reaches an untagged pdf-lib PDF without leaving the product and the scope cannot stop at "Dashboards emits nothing". **State at open:** product line CLEAR, zero open PRs across all four repos. Dashboards main `238e0234` serving `smartcity-dashboards-00037-ced` with 20 of 20 separators carrying `aria-hidden`; kit main `54efe8c5` current with the product on all four vendored blobs; smart-files `5de59adb`; plan-review `156677f6`. Half A of wave 3 is shipped and **half B, the evidence pack and the ACR, is NOT STARTED** — that is the honest state of the VPAT and it is recorded here rather than implied. Live Bastrop no-touch. Do not start G-52. L26 untouched. | Operator: lets unblock the scope map and the pdf generator. what do you need from me. | nick (both rulings); planner (evidence, scope) |
| A-085 | 2026-08-24 | **G-105 ADDED through G-110 ADDED. Wave 1 integration program rows for the govtech seat.** Operator ruling 2026-08-24 moved four product repos to govtech; scope rev 3 (`_inbox/2026-08-24_govtech_program_scope.md`) and adversarial CP1 (`_inbox/2026-08-24_govtech_plan_adversarial_cp1.json`) define the dependency graph. **G-105** deploy gates for merged #7+#39+#75+#361 with live violation probes (S5-2a). **G-106** Smart Files read-path scope (S3-1, defect #3). **G-107** staff upload path (S2-6, R-B). **G-108** plan review honesty path including edition selector and typed absence (S2-7/8/9). **G-109** ICC obligation ledger chain through R-I authoritative ledger (S4-0 through S4-8). **G-110** Wave 1 E2E proof on `template-city` (S5-5). S2-1 engine migration remains blocked on ADR-023 amendment (DOC-5); dispatch as a separate row when amendment is ratified. Canvas: `canvases/govtech-master-program.canvas.tsx`. Do not conflate merged with live. Live Bastrop no-touch. L26 untouched. | Operator: govtech thread scope and Wave 1 critical path from 2026-08-24 handoff; CP1 PASS WITH AMENDMENTS once these rows exist | nick |
| A-086 | 2026-09-02 | **Wave 1 regrouped after eight days untouched; G-105 scope corrected by live digest, not narration.** WDLL (`_inbox/2026-08-25_govtech_wave1_WDLL.md`) had sat draft/pending since 2026-08-25 with zero sessions touching G-105 through G-110 in between. Regroup filed at `_inbox/2026-09-02_govtech_wave1_regroup.md`. **Finding: two of the four "merged, not deployed" PRs in STATE.md are actually live**, carried in by unrelated property-program redeploys — hauska-mcp-server `#75` (`c81f1c3`) is an ancestor of the serving image `1ae9f28` on `hauska-mcp-server-00084-mof` (`gh api compare`, ahead_by 4 / behind_by 0); hauska-engine `#361` (`cfa18bc`) matches `hauska-engine-api-00176-keb`, confirmed live and cross-checked against the 2026-08-27 memory record of the same close. **plan-review `#7` and dashboards `#39` are confirmed NOT deployed**: plan-review Cloud Run still serves `plan-review-00012-pen` tag `g60g` from 2026-08-16 (predates the PR by 8 days), its Vercel front redeployed at 13:55 CDT 2026-08-24, ~4.5h before the PR merged at 18:33 CDT; dashboards still serves `smartcity-dashboards-00037-ced` from 2026-08-20. **G-105 is therefore a two-item deploy (plan-review, dashboards) plus two probe-graded confirmations (MCP, engine-api), not a four-item deploy.** DOC-5 re-checked: ADR-023 amendment draft is still `status: draft`, unratified. No baseline row edited; no WDLL edited. | Operator: regroup the 8-day-old plan before resuming execution; live digests pulled this session as the basis | nick; planner (digest pulls, corrected scope) |

| A-087 | 2026-09-02 | **DEPLOY-7 and DEPLOY-39 CLOSED.** plan-review Cloud Run `plan-review-00014-zot` @100% + Vercel `dpl_D8satDguP8ZhRKFjXwf2aW9jV3po` (`plan-review-app-ten.vercel.app`), paired in one cut. Live probe (correcting the WDLL's assumed route — real route is `GET /api/plan-review/code`, Bearer-gated by the already-live PR #6): pre-deploy returned HTTP 200 with fabricated `R311.7` for a nonsense section; post-deploy returns typed absence. Dashboards Cloud Run `smartcity-dashboards-00039-cid` @100%. Its live probe was inconclusive — the compose endpoint doesn't expose per-atom `accessPolicy`, so grading rests on code reading: `git diff` confirms the fail-open line (`raw == null → return true`) removed and replaced with an enum-allowlist resolver defaulting to `false`. Full evidence and the deploy-violation-probes.mjs defects found along the way (wrong route on gate deploy-7, no defect-triggering params on gate deploy-39 — both gave false passes when run live) are in `_inbox/2026-09-02_govtech_wave1_regroup.md`. `_state/govtech/STATE.md` NOT updated (SEAT-01: not writable from the integration checkout) — still needs a refresh by the govtech seat. | Operator: "do the two deploys" | nick; planner (deploy, probes, code-diff verification) |

| A-088 | 2026-09-02 | **DEPLOY-75 and DEPLOY-361 formally graded.** Neither redeployed — both were already live (A-086). **DEPLOY-75**: migration 009 confirmed applied (`source_obligation_ledger` exists, 12 historical rows, all pre-#75). The PR shipped its own self-test (`scripts/violation-probes.mjs`, run this session from a fresh temp copy): all three defect classes (S1 accrual-gating, S2 tenant/adapter/actor detector unification, S3 revenue reporting) pass on the correct implementation and fail on injected-defect and refuse-all variants — confirmed non-vacuous. Live replay of a known pre-fix false positive (`did:hauska:setback-rule:48021:34737`, Bastrop's own ordinance, wrongly ICC-billed in the 2026-07-25 rows) now correctly produces zero accrual. **Named gap: the true-positive live path (real ICC content via a `codex`-tier key) could not be tested — anonymous `get_atom` on the ICC corpus atom was refused by accessPolicy, and no product key was available this session.** Closed with that residual named, not closed clean. **DEPLOY-361**: closed by code reading only, deliberately — no live malformed-write probe was run against the shared atoms store, because a concurrent-write collision on that same store was reported and resolved earlier this session (doc-repo-c0, unrelated to this program, but evidence the store is contended right now). `git diff` on `packages/corpus/src/atomization/index.ts` confirms the exact fail-open default removed (`options.accessPolicy ?? "public-free"` deleted) and `accessPolicy` promoted from optional to required across five atom-instance interfaces in `packages/atoms/src/instances.ts` — a structural (type-level) fix, not just a runtime check. | Operator: "yes" (continuing from the deploy request) | nick; planner (grading, self-test run, code diff, live replay) |

| A-089 | 2026-09-02 | **Three Wave 1 dispatches COMPILED for operator hand-carry (not yet sent).** `node scripts/dispatch.mjs --plan OPS-17` for lane A (`G-106,G-107`, repo smart-files — read-path scope then staff upload), lane C (`G-108`, repo plan-review — edition selector, typed absence, applicability matrix), lane D (`G-109`, repo plan-review — govtech half of the ICC ledger: S4-1b reconciliation + S4-8 reader only; S4-3/S4-7 explicitly routed to the substrate seat, not built here). Missions at `_inbox/2026-09-02_govtech_track_{a,c,d}_mission.md`; compiled output at `_dispatches/2026-09-02_{a,c,d}_dispatch.md`. Each mission carries source-verified corrections found this session: smart-files' primary checkout is dirty/stale and its seat worktree doesn't exist yet; plan-review's edition/absence code is genuinely unbuilt past `store.mjs`, not just ungraded; and the obligation ledger's real schema has no `book_id`/`section_id`/`edition_id`/`reference_kind` columns, contradicting the WDLL's own item 12 check — S4-1b/S4-8 must be built against the real schema. S5-2c and S5-3 (seam vocabulary, presentation convergence) are deliberately NOT dispatched yet — held until these three tracks produce real output to converge on. | Operator: "yes" (continuing the biteoff-the-work plan) | nick; planner (verification, mission drafting, compile) |

| A-090 | 2026-09-02 | **G-109 govtech half CLOSED PARTIAL.** `plan-review` PR **#8** merged (squash, head `bfb4559` / merge `1dd20d1`, CI conclusion string `success` on both), branch `govtech/g-109-ledger-reconciliation`. S4-1b (reconciliation, `request_id`-matched, typed `ledger-unavailable` not fabricated-zero) and S4-8 (reader folded into existing `GET /api/icc/activity`) built and tested (39/39, dedup_key uniqueness verified by violation against a live scratch Postgres). Production migration (`sql/003`, `sql/004`) and Cloud Run deploy **deliberately held**, operator-ruled: the feature is structurally inert until substrate closes the gaps below, so nothing is gained deploying early. Full artifact `_inbox/2026-09-02_d_close.json`. **Two NEW substrate-seat blockers surfaced, independently confirmed by two sessions:** (1) `hauska-mcp-server` mints `source_obligation_ledger.request_id` server-side (`randomUUID()`, `src/index.ts:289`) and never echoes it to the caller — exact reconciliation cannot produce one real match until this is fixed. (2) No MCP tool reads `source_obligation_ledger` at all (`grep tools.ts` for "ledger"/"obligation" — zero matches) — even with (1) fixed, plan-review has no substrate-side reader to consume. Both are beyond S4-3/S4-7 as originally scoped; a substrate-seat request is being filed separately. WDLL item 11 stands met; item 12 is explicitly NOT graded met — its literal check names columns (`reference_kind`, `book_id`, `section_id`, `edition_id`) that don't exist on the real table (A-088 already flagged this; S4-7 would need to add them). This close is the product of the A-085/A-089 duplicate-dispatch incident: the same compiled `_dispatches/2026-09-02_d_dispatch.md` reached three executors in one shared, branch-unpinned worktree; the collision was caught before any commit, the two independent builds turned out complementary (not conflicting), and the standing-down session preserved its own diff non-destructively (copied to its own scratchpad rather than `git stash`, to avoid clobbering the other session's in-flight edit). Worth a standing fix: hand-carry a dispatch to exactly one executor, or give plan-review the same per-lane worktree isolation smartcity-dashboards and smart-files already have. | Operator: "yes relay" (commit/PR/merge go, prod deploy hold) | nick; doc-repo-1f (build, close); doc-repo-33 (original design, stood down cleanly); doc-repo-1d (collision brokering, this amendment) |

| A-091 | 2026-09-02 | **G-106 CLOSED. G-111 ADDED.** `smart-files` PR **#9** merged (`d7b3e71`), deployed `smart-files-00008-geb` @100% (prior `smart-files-00006-xwp`), tagged canary smoke-tested at 0% traffic before the shift. Live-verified both pre- and post-shift: anonymous read 403, wrong-tenant token 403, matching-tenant token 200; the existing production `SMART_FILES_SERVICE_TOKEN` kept a blanket read grant for backward compatibility (real callers — QA UI, Dashboards, plan-review — all still use one shared token today), two new narrowly-scoped tokens minted for future migration, none printed to chat or left in a scratch file. 25/25 unit, 46/46 existing round-trip probe, 17/17 new read-scope probe, 2/2 CI checks — all read via the literal conclusion string, not display text. G-107 (tenant identity, staff upload) explicitly NOT attempted this pass — correctly scoped out once the executor read `uploadFileToFolder` and found it needs plan-review UI work too, not just smart-files. Close `_inbox/2026-09-02_a_close.json`. **G-111 ADDED**: the substrate half of the ICC obligation ledger chain (S4-3 request_id echo-back, S4-7 a ledger reader tool), surfaced by two independent sessions building G-109 and filed at `_inbox/2026-09-02_govtech_to_substrate_ledger_asks.md`. Given its own row rather than folded into the closed G-109, since G-109 was explicitly scoped and closed as the govtech half only. | Operator: "add G-111 to OPS-17, compile a real dispatch for it" | nick; doc-repo-25 (G-106 build); doc-repo-1d (this amendment, G-111 scoping) |

| A-092 | 2026-09-02 | **G-108 CLOSED PARTIAL. Planner error in the Track C mission corrected.** `plan-review` PR **#10** merged (`f27c60f`), 57/57 tests (18 new). Real mechanism, not narration: edition selector, a citation-minting module with a hard refuse absent `editionId`/`bookId`/`sectionNumber`, an adjudicator that reaches genuine Pass and Fail (live-verified: the 12 findings rows already in production had been 100% Uncertain/Unchecked since migration 001 despite a 4-value CHECK constraint existing the whole time — a starved mechanism this PR actually feeds), typed `SOURCE_UNAVAILABLE` distinct from a genuinely-empty slot. Items 8/9/10 graded PARTIAL, correctly — item 8's own WDLL text blocks it on item 7 (G-107), confirmed still unbuilt by checking live `smart-files` PRs rather than assumed. **Planner error:** the Track C mission (`_inbox/2026-09-02_govtech_track_c_mission.md`) told the executor "spelling is `verified-absent`," which is backwards — the WDLL (item 9) and the transaction contract both specify `absent-verified`; `verified-absent` is the divergent one, already tracked as canvas item L3, scoped to `S5-2c` (a govtech-wide seam-vocabulary pass across all repos at once, not a one-repo fix). The mission's operational instruction ("do not import the L3 fix here") was still right despite the wrong stated reason — fixing spelling in one repo ahead of the others would create a second divergence, not close the first. The executor caught the contradiction by reading both WDLL and contract rather than trusting the mission text, did not silently pick one, and documented the conflict in its close — exactly the right response to a wrong instruction. **Real gap found, not caused:** `plan-review`'s PR #8 code (G-109) requires `plan_review_activity.dedup_key`; migration 004 was never applied to the production Neon. Currently harmless only because deploy is also held (A-090) — production still serves pre-#8 code. **Binding note for whoever lifts the G-109 deploy hold: migration 004 must apply before that deploy, or every `recordActivity()` call breaks.** Close `_inbox/2026-09-02_c_close.json`. | Operator: none this row (planner self-correction, surfaced by the executor) | doc-repo-33 (build, close, catch); doc-repo-1d (mission error, this correction) |

| A-093 | 2026-09-02 | **Production engagement-creation break FIXED.** Root cause was not a code defect: `plan-review-00016-buh` (deployed by the G-107 session) was built via `gcloud run deploy --source .` from a local checkout that had G-107's own merge but had not fetched G-108's concurrently-landed merge (git commit order on `origin/main` is #7 → #8/G-109 → #9/G-107 → #10/G-108 — G-107 merged before G-108, and the deploy ran from whatever the local working directory held at that moment, not the true tip at deploy time). Meanwhile G-108's migration 005 (the `plan_review_findings_absence_required_check` CHECK constraint) WAS applied to the production schema (doc-repo-33 verified it live while building G-108). Net effect: new-code-missing-but-new-constraint-present, so every `seedMatrix` insert for a freshly created engagement violated the constraint. Confirmed live by the planner: reproduced on the broken revision (`POST /api/plan-review/engagements` → `violates check constraint`), then diagnosed by deploying a fresh build from a clean `git checkout --detach origin/main` with temporary row-level logging (`console.error` before each `upsertFinding`, on a 0%-traffic canary tag `diag-g110`) — every row logged a correctly non-null `absence` for its `Unchecked` determination, proving the merged code was already correct and the live revision was simply stale. Diagnostic logging stripped (`git diff --stat` against `origin/main` confirmed byte-identical before redeploy), rebuilt clean, smoke-tested on a second 0%-traffic tag (`g110-fix`: real parcel 201, malformed parcel 201, DEPLOY-7 fabrication check still holds, G-108 editions route 200), then shifted to 100% (`plan-review-00019-puj`) and reconfirmed on the production URL. **A real, separate bug also found and left in place, not fixed here:** `mcpCall`'s `textPayload()` (`src/mcp.mjs`) only checks the JSON-RPC-level `rpc.error`, never the tool-level `result.isError` — an MCP tool call that fails at the tool layer (e.g., `get_property_atom_chain` given a malformed `parcel_node_id`) returns `{text: "MCP error ..."}` rather than throwing, so `matrixFromChain`'s `sourceUnreachable = Boolean(chain?.error)` never fires for this failure class. Currently harmless only because every downstream row-builder still produces a correctly-typed `Unchecked`+absence regardless of which branch it takes — but the SOURCE_UNAVAILABLE absence kind, which exists specifically to distinguish "corpus unreachable" from "corpus reached, nothing there," never fires when it should. Named as a residual, not dispatched. **Operational lesson for the standing decisions:** `gcloud run deploy --source .` builds from local working-directory contents, not the git tip at deploy time — a deploy run immediately after a merge, from a checkout that has not re-fetched a concurrently-landed second merge, silently ships stale code even though the operator/agent believes they just deployed "current main." Worth a `git fetch && git log -1 --format=%H HEAD..origin/main` (expect empty) check before every `--source .` deploy, not just before the first checkout of a session. | Operator: "fix" | nick; doc-repo-1d (diagnosis, fix, deploy, verify) |

| A-094 | 2026-09-02 | **G-111 DEPLOYED.** Per the coordination check with the property/Factory program (clean repo/store separation confirmed; two cautions given), used `hauska-mcp-server`'s documented pipeline (`gcloud builds submit --config=cloudbuild-mcp.yaml --substitutions=_TAG=40e48d4`), not a raw `--source` deploy — the known-risky path per the 2026-08-03 cortex-api key-rotation outage this exact caution named. Built from a clean `git checkout --detach origin/main` in the registered substrate worktree (`P:/seat-worktrees/substrate/hauska-mcp-icc-meter`; four CRLF-phantom-modified files present per `git status` but confirmed empty by `git diff --stat`, matching G-111's own prior finding — left untouched, not committed). **Real gotcha found:** this service's traffic is pinned to explicit revision names rather than "latest," so the plain `gcloud run deploy` inside the pipeline created a healthy new revision (`hauska-mcp-server-00055-8pz`) but did NOT route traffic to it — Cloud Run auto-retired it at 0% traffic within seconds. Caught by revision-list (not the `services describe` traffic block, which stayed on the old revision even after the build reported SUCCESS), tagged it back in at 0%, smoke-tested, then explicitly shifted. **Post-deploy verification, per the caution — an existing consumer path, not just the new tools:** secret bindings byte-identical between old and new revisions (same names, same `:latest` refs, diffed field by field); `get_property_atom_chain` (an existing, heavily-used tool) still serves real data; anonymous and wrong-header calls still correctly resolve to `product:"public"`, unchanged. Both G-111 features confirmed live: `x-hauska-request-id` header present on every tool-call response; the reader (`GET /obligations/source-ledger`, not an MCP tool — a plain HTTP route, absent from `tools/list`) correctly refuses with `platform_internal_required` to an unscoped/unkeyed caller. **Side effect, named not hidden:** `--to-revisions=X=100` replaces the ENTIRE traffic block, not just the percentage split — it silently dropped roughly eighteen historical 0%-traffic named tags (`fourgate`, `t1fix`, `a4b` through `g63`, `g11`, `p89-1ae9f28`, etc.) that existed before this deploy. The underlying revisions are untouched and still exist; only their tag aliases are gone. Likely harmless (these were one-off historical smoke-test URLs, not live dependencies) but not verified harmless — flagged as a residual. G-111's remaining leave_behind (plan-review needs a `platform_internal` key to call the reader; `seat_register.json`'s stale worktree-removal note) still stands. | Operator: "lets proceed" (after reviewing the coordination check) | nick; doc-repo-1d (deploy, diagnosis of the traffic-pinning gotcha, verification) |

| A-095 | 2026-09-02 | **G-108 re-graded CLOSED (from PARTIAL).** Live walk, not a re-read of old evidence: created a real production engagement on `48021:34737`, declared `IRC-2018`, submitted `proposedSetbackFrontFt=30` (above the 25ft Bastrop UDC minimum) — `14-02-003` returned `Pass` with real analysis text. A second engagement with `proposedSetbackFrontFt=15` returned `Fail`. Both citations carried non-empty `editionId` and a structured `bookId`/`sectionNumber` shape. Items 8, 9, 10 of the frozen WDLL are all genuinely met. **Minted a `platform_internal`/`codex`/`team` key** (`hauska-mcp-server` `POST /admin/keys`, key_id `19c13d1f-...`) for plan-review to call G-111's reader, stored as GCP secret `plan-review-platform-key` (project `plan-review-505715`, IAM-granted to the plan-review runtime service account, raw value never printed to chat or left on disk). **Not done, and named as real remaining work, not a quick follow-up:** nothing in plan-review actually CALLS the new reader yet. `src/ledger-reconciliation.mjs`'s `fetchLedgerRows()` still deliberately returns `null` (correctly — its own docstring names exactly this as the one seam to change once a read path exists, which now it does). Wiring it in touches `src/mcp.mjs`'s shared `mcpCall`/`textPayload` plumbing (used by every MCP-calling route in the service, not just this one) to capture the `x-hauska-request-id` response header, which `mcpCall` currently discards entirely along with all other response headers — a genuine multi-file change needing its own dispatch, not something to improvise inline. The secret is provisioned and waiting; nothing consumes it yet. | Operator: "how do we do that" | nick; doc-repo-1d (verification walk, key mint, secret provisioning) |

| A-096 | 2026-09-02 | **G-112 ADDED.** Wire plan-review's `fetchLedgerRows()` to G-111's live reader, closing the loop A-095 named as real remaining work. Not folded into the closed G-109 (govtech half) or G-111 (substrate half) — a third, distinct unit of work consuming both. Dispatch to follow. | Operator: "yes" (compile the dispatch) | nick |

| A-097 | 2026-09-02 | **A-093's root cause CORRECTED, not just restated.** doc-repo-25 (who ran the G-107 deploy A-093 described as "stale") pushed back with a specific, checkable claim: it fetched and checked out `origin/main` immediately before deploying, and per A-093's own commit order (G-107 merged before G-108), a correct fetch at that moment would have captured only G-107. Checked the actual evidence rather than picking a side: the broken revision's Cloud Run build-source upload timestamp is `2026-09-02T15:48:30.820Z`. G-108 (#10) merged `2026-09-02 10:48:28 -0500` = `15:48:28Z`. G-107 (#9) merged `15:47:02Z`. **The gap between G-108 merging and the source upload is 2.8 seconds.** `gcloud run deploy --source .` tars and uploads the working directory as a step, taking real wall-clock time after the checkout completes — so doc-repo-25's fetch+checkout almost certainly happened at or before the instant G-108 merged, not carelessly stale. **A-093's "didn't re-fetch" framing was wrong; the corrected lesson: a deploy pipeline has a non-zero window between reading source and the build completing, and a concurrent merge landing inside that window is missed regardless of fetch discipline immediately prior — this is a property of the pipeline, not a lapse by the operator running it.** The practical fix is verifying what's actually running post-deploy (a commit/build marker, or exactly the diagnostic-canary technique A-093 used), not trusting a pre-deploy fetch as sufficient when merges are landing within the same ~90 seconds. doc-repo-25's separate hypothesis — that G-108's schema constraint was applied to production out-of-band, ahead of G-108's own code merge — is plausible and would also explain the break, but is **not verified**: no direct timestamp evidence for when migration 005 actually landed on the schema was checked by either party. Left as an open, named hypothesis, not asserted as fact. The associated cross-session memory (`gcloud-source-deploy-stale-local-checkout.md`) is corrected to match. | doc-repo-25 (pushback, verification); nick (none — planner self-correction) | nick; doc-repo-1d (corrected timeline, re-verification) |

| A-098 | 2026-09-02 | **G-112 CLOSED PARTIAL.** `plan-review` PR **#11** merged (head `99516af` / merge `1f2d6ba`, CI `success` on both), deployed `plan-review-00021-huc` @100% with the `plan-review-platform-key` secret bound. 60/60 tests (5 new). Live-verified via direct SQL, not the diff: two real, distinct `request_id`s landed on production `plan_review_activity` rows post-deploy (one `/code` call, one `/matrix` call), where every pre-deploy row carried an empty string. `fetchLedgerRows()` now calls G-111's live reader. **Acceptance item 2 (a genuine matched/divergent row) could not be produced — a real, external reason, not a defect in this row:** the one ICC-tagged fixture atom built to prove this refuses an anonymous read (`"not readable under the caller's accessPolicy"`), the identical gap DEPLOY-75 already named in A-088. **The codex-tier-key gap is now a single blocker across two rows** (A-088's true-positive verification and G-112's genuine-match verification) rather than two separate ones — getting one key resolves both. A diagnostic activity row (`id 47fc24c3-...`, `bookId=G112-DIAGNOSTIC-PROBE`) was deliberately left in production, clearly labeled, as the record of the verification attempt rather than deleted. Caught and fixed its own tooling bug mid-session: a `Monitor` script that declared a CI check done before the check had started, caught by checking `gh api` directly rather than trusting the first report. Close `_inbox/2026-09-02_g112-reconciliation-wiring_close.json`. | doc-repo-1f (build, close) | nick; doc-repo-1d (recording) |

| A-099 | 2026-09-02 | **G-113 and G-114 ADDED** (WDLL items 13, 14 — the last two blockers before G-110's E2E proof is even attemptable). Both rescoped from the WDLL's literal 9-day-old text after checking source, not assumed: G-113's "vendor a citation validator into plan-review, smart-files, and smartcity-dashboards" narrows to plan-review only — neither other repo constructs a citation-shaped object today (grepped, zero hits), and `citation.mjs`'s `buildCitation()` (built inside G-108) already refuses construction without `editionId`/`bookId`/`sectionNumber`, so the real remaining gap is the spelling fix plus a durable CI gate, not new validator code. G-114 is grounded in a real, confirmed defect, not a hypothetical: `smartcity-dashboards/src/mounts.mjs`'s `planReviewEmbedUrl()`/`smartFilesEmbedUrl()` return a static env-configured origin with no `cityKey` threaded through, and `compose.mjs` calls them with no city argument — a genuine scope trap the moment Dashboards serves more than one city. Missions and dispatches to follow. | Operator: "yes please" (continuing phase 2/3, defer Bastrop/polish) | nick |

| A-100 | 2026-09-02 | **G-113 CLOSED.** `plan-review` PR **#12** merged (`3f426bb`), 64/64 tests (4 new, 2 rewritten). `ABSENCE_KINDS.VERIFIED_ABSENT` renamed `absent-verified` everywhere live (two remaining hits are historical prose in comments, explicitly named and excluded, not silently satisfied by a lenient grep). Re-confirmed single-repo scope with an independent fresh grep against both other repos' current `origin/main`, not trusted from the dispatch's own claim. **Real residual found and fixed, not assumed clean:** two hand-authored citation strings had gone dead-but-still-present in `CODE_BOOKS["BASTROP-UDC"].sections` since G-108 switched the route to `buildCitation()` — unread, so invisible to any live probe, but exactly the kind of defect that resurrects itself the next time someone reads that field back. Removed. The new CI gate is three layers, not one: a textual check (no `citation:` literal outside `citation.mjs`), a structural check (no `CODE_BOOKS` entry carries one), and a genuine meaning-shaped check — `matrixFromChain`'s shipped citation on a row must equal one rebuilt fresh from that row's own `editionId`/`bookId`/`sectionId`, two independently derived values required to agree. Violation-verified: planted the exact historical pattern in a file with no legitimate reason to touch citations, confirmed the gate caught it and named the file, restored, reran green. No separate CP1/CP2 files — close artifact carries everything, a deliberate choice over fabricating empty checkpoint files to satisfy a path check. Close `_inbox/2026-09-02_g113-seam-vocabulary_close.json`. | doc-repo-33 (build, close) | nick; doc-repo-1d (recording) |

| A-101 | 2026-09-02 | **G-114 CLOSED. WDLL items 1 through 14 now all met — G-110 is the only row left.** Three PRs merged and deployed: `smartcity-dashboards` #40 (`03250b3`, `smartcity-dashboards-00041-naf` @100%), `plan-review` #13 (`67b852c`, Vercel front only — Cloud Run backend correctly not redeployed, the change touched only `web/app.js`), `smart-files` #11 (`c38c20f`, Vercel front only, same reasoning). Live-verified past the point of "each half looks right": fetched the actual deployed `app.js`/`index.html` from both Vercel production URLs and ran them in `jsdom` with the exact URL the live compose response produces — `plan-review` resolves `localStorage pr_persona` to `template-city/staff`, `smart-files` resolves its persona select to `template-city/g71-calendar`, both against the real deployed bundle, not a local copy. This is the actual thing a presentation-convergence card has to prove: not that Dashboards' compose response looks right and not that the downstream apps' logic looks right in isolation, but that the two halves compose correctly in production. Cross-city differentiation proven at the unit level only (no second city pack exists in production to test against live — onboarding one was explicitly out of scope). All three SKUs (`SCOS-PLAN-DEP`, `SCOS-FILE-DEP`, `SCOS-DASH-DEP`) confirmed independent; the pre-existing G-13 dependency direction (Dashboards embeds the other two, never the reverse) is unchanged — this card fixed what goes in the iframe `src`, not whether the iframe relationship exists. **Named residual, correctly left alone:** `staff-review.mjs` carries dormant, never-called iframe-URL functions with the identical pre-fix static-URL shape — investigated, confirmed inert, deliberately not "fixed" for a defect they cannot currently cause. Flagged for whoever next touches that file, not fixed here (no scope creep for an unreachable code path). Close `_inbox/2026-09-02_g114-presentation-convergence_close.json`. | doc-repo-25 (build, close) | nick; doc-repo-1d (recording) |

| A-102 | 2026-09-02 | **DEPLOY-75's true-positive residual (A-088) CLOSED.** The `platform_internal`/`codex`/`team` key minted for G-112's reader turns out to also satisfy this — tested directly: `get_atom` on `did:hauska:jurisdiction-corpus:icc-model-code` through `hauska-mcp-server` succeeded (previously refused anonymous), and a live query of `source_obligation_ledger` immediately after shows the real row it produced: `source_actor_did=did:hauska:actor:org:icc`, `tool=get_atom`, `product=codex`, `tier=team`. Genuine accrual for genuine ICC content, not a fixture replay. **Named precisely, not overclaimed:** this proves the corpus-level accrual mechanism; it does not mean plan-review's own production code-lookup route can cite real ICC *sections* — tested that too, same key, `plan_review_get_code` against `IBC2018P6`/`R311.7` still correctly returns typed absence, because plan-review's local manifest has zero ingested sections (unchanged) and, per this same check, the substrate itself has no real per-section ICC content either (only the corpus-level atom exists). That's a data-acquisition gap, not an access or mechanism defect, and it directly bears on G-110 below. | Operator: "keep the pipeline going" | nick; doc-repo-1d (found, tested, closed) |

| A-103 | 2026-09-02 | **G-110 walked, CLOSED PARTIAL — the honest state of Wave 1's capstone row.** Full evidence above and in this row's own table entry. Four of five WDLL-item-15 observations are genuinely met on one coherent live session, not stitched from separate probes across the day. The fifth (a `source_obligation_ledger` row for THIS session) cannot be produced through plan-review's own production input surface today, for a reason established precisely, not assumed: no real per-section ICC content exists anywhere in the system — not in plan-review's local manifests (already known, G-108/G-113) and, newly confirmed this session, not in the substrate either (only the corpus-level `jurisdiction-corpus` atom is real; `plan_review_get_code` against `IBC2018P6`/`R311.7` through the same codex-tier key still correctly returns typed absence). This is a content-acquisition gap, not a build gap — Wave 1 built the mechanism; it was never scoped to also ingest real ICC section content. **Operator call owed:** does Wave 1 count as done with this residual named (the same pattern G-105/G-109/G-112 all used), or does closing G-110 fully require an ICC content-ingestion effort that was never part of this wave's scope? Not decided here. | Operator: "keep the pipeline going" | nick; doc-repo-1d (walk, evidence, this amendment) |

| A-104 | 2026-09-02 | **G-110 RE-GRADED CLOSED (from PARTIAL). Operator ruling: "residual named is fine." WDLL items 1 through 15 are now all met — Wave 1 is complete.** The ruling answers A-103's owed call directly: the missing session-scoped ledger row stands as a named content-acquisition gap, not a build defect, and does not block the row from closing. **Scope note, not a status change:** G-112's own open acceptance item (a genuine matched/divergent ledger row, A-098) shares this exact root cause — plan-review has no path to an ICC-citing call while no real per-section ICC content exists to cite. G-112's row is left at CLOSED PARTIAL here rather than silently upgraded under this ruling; it wasn't the row this ruling was asked about, and re-grading it deserves its own explicit ask rather than inheriting one by inference. **Real ICC section-content ingestion remains genuinely unscoped anywhere in this program** — named here so it isn't lost now that its two visible symptoms (G-110, G-112) are both resolved by residual-naming rather than by the content actually landing. | Operator: "residual named is fine" | nick |

| A-105 | 2026-09-02 | **CORRECTION to A-102/A-103/A-104: "no real per-section ICC content exists anywhere in the system" was WRONG.** Real, licensed IBC 2018 section content exists — 4,825 code-section atoms (`did:hauska:code-section:icc-model-code/2018-international-building-code-6th-printing/*`), fetched 2026-07-06 via the `icc-code-connect` adapter under the live ICC Code Connect PoC credential (GCP secret `ICC_CODE_CONNECT_CLIENT_ID`/`_SECRET`, entitled through Dec 30 2026), each carrying a real title, our own analysis text, a real source deep-link to `codes.iccsafe.org`, correctly withholding verbatim body text per the PoC license (`75n_icc_code_connect_catalog.md`). Live-verified this session: `get_atom` via `hauska-mcp-server` on `did:hauska:code-section:icc-model-code/2018-international-building-code-6th-printing/1001-1` (a real section, "General.", Chapter 10 Means of Egress) returns the full atom with a fresh `readContract`. **What actually happened and why A-102/A-103 got it wrong:** the test run there queried section `R311.7` — a genuinely non-existent IBC section number (R-prefix is IRC convention; the IBC uses numeric section ids like `1001.1`, confirmed by every real atom just read). Two stale fixture rows using that exact wrong pairing (`R311.7`, `R302.1` mislabeled as IBC) were found and correctly DELETED from `plan-review/src/code-lookup.mjs`'s `CODE_BOOKS.IBC2018P6.sections` on 2026-08-24 (predates this session). Testing a section number that was never real was guaranteed to return absence regardless of whether the corpus held anything — that result was real but proved nothing about the corpus. Separately, and this is the part A-102/A-103 never checked: plan-review's `CODE_BOOKS.IBC2018P6.sections` is `Object.freeze({})` — a static, hand-maintained, currently-empty manifest that has no query path into the substrate's atoms store at all. So plan-review's route returning absence is real and currently correct for EVERY IBC section, not because the content doesn't exist, but because nothing in plan-review's code ever asks the substrate for it. **The error was generalizing from one route's output to a claim about the whole system's data, instead of checking the substrate directly** — the same shape of mistake ENFORCEMENT.md's "read the authoritative record, never a proxy for it" names. Caught this session while scoping the actual next-push work, not by a downstream reviewer. **Consequence for G-110/G-112's residuals:** the "content-acquisition gap" language in A-103/A-104 is corrected to a wiring gap — the content is already acquired, licensed, and live-retrievable; what plan-review lacks is a lookup path from `CODE_BOOKS` into the substrate's real code-section atoms. This is now scoped as real, boundable engineering work for the next push, not open-ended data acquisition. See `_decisions/2026-09-02_plan_review_leads_the_bastrop_push.md`. | Operator: none this row (self-correction, surfaced while scoping "get the ICC demo functioning" per the operator's direct instruction) | nick; doc-repo-1d (found, live-verified, corrected) |

| A-106 | 2026-09-02 | **Bastrop-cutover reconciliation pass: G-13 re-graded CLOSED, G-15/G-16/G-22/G-31/G-51 marked out of scope for Bastrop, G-52 re-pointed at real prerequisites. ADR-023 DOC-5 amendment RATIFIED.** Before scoping any new Bastrop-cutover work, checked what OPS-17 already had rather than drafting fresh (operator instruction: "did you look at what we had already... don't do a bunch of duplicate work"). Found: (1) **G-13 was already closed** by `_decisions/2026-08-17_g13_consumer_contract.md` (explicitly states "Closes G-13") but the OPS-17 row sat OPEN for over two weeks — re-graded. (2) **G-15, G-16, G-22, G-31, and G-51 — which G-52, the actual Bastrop-consumer-pass row, depends on — all measure `48_cortex_reporting_plan_review_spec.md`, the AEC-cortex-facing surface living in `legacy-design-tools/artifacts/plan-review`.** The ADR-023 amendment draft (DOC-5, `_inbox/2026-08-24_adr023_amendment_draft.md`, ruling R-D 2026-08-24) already established that CITY plan review — what Bastrop actually consumes — lives in the standalone `plan-review` repo instead, and Wave 1 was built entirely on that premise. The amendment sat unratified the whole time. **Ratified it now** (operator go): applied the drafted edit to `80_adrs/adr_023_cortex_reporting_repo_designation.md` in place (original 2026-07-01 decision retained struck-through for record), marked the amendment draft `status: ratified`. S2-1 (the engine migration this amendment was blocking) is now unblocked, not yet dispatched. Re-graded G-15/G-16/G-22/G-31/G-51 as out-of-scope-for-Bastrop rather than retired — they remain real rows for AEC-cortex's own reporting surface, whenever built. Re-pointed G-52's dependency from the wrong repo's rows to the real prerequisites (G-13, closed; Wave 1 plan-review completeness, closed) and corrected its acceptance criteria wording from "cortex-reporting" to "plan-review". **G-52's own remaining scope is real, but — corrected in A-107 below — still genuinely blocked, on a real dependency this time (a live feed), not the stale one just removed.** | Operator: "yes" (ratify ADR-023, retire the stale rows and re-point G-52) | nick; doc-repo-1d (found, ratified, reconciled) |

| A-107 | 2026-09-02 | **G-115 ADDED (draft, pending approval). G-52 correction: still blocked, on a real dependency this time.** A-106 said G-52's remaining scope was "open and unstarted" — checked `_inbox/2026-08-17_dashboards_missing_pieces.md` fully before drafting anything and found that framing incomplete: the doc explicitly says **"Do not dispatch a Bastrop cutover WDLL. Do not start G-52,"** because G-52 needs a live permit-record feed on `template-city` to initiate FROM, and G-63 (closed 2026-08-17) built only the adapter contract — `grantedAdapters` is still `[]`. That instruction is verified still valid, not stale: the reason it was written hasn't changed. G-52's OPS-17 row corrected accordingly (blocked-on-real-dependency, not open-and-unstarted). **What Wave 1 does unblock, per that same doc's own recommended sequence (step 4, island replacements):** PermitFlow dies specifically when `plan-review-app` is the reviewer Bastrop staff actually use — no feed required, only Plan Review being real, which Wave 1 made true today. **G-115 added**, a narrow, explicitly-not-"Bastrop-cutover" island-cut card per the sequencing doc's own rule (each island replacement is its own WDLL with a staff go-live). Frozen Start card filed: `_inbox/2026-09-02_bastrop_permitflow_islandcut_WDLL.md`. Six acceptance items: a real Bastrop tenant/persona (not `template-city`), UDC edition/citation re-verified under it, an honest coverage measurement (only 2 real UDC sections exist today — named as a real adoption-risk number, not assumed away), the A-105 ICC wiring fix folded in (Bastrop reviews are not UDC-only), an explicit tenancy/access decision (not an assumed answer — G-11 stays open per `_decisions/2026-08-17_g13_consumer_contract.md`), and one real staff go-live. `smartcity-os`/PermitFlow untouched; stays live per G-65 until adoption is proven, not declared. | Operator: "PermitFlow island-cut WDLL" (chosen over scoping the missing feed, or drafting both) | nick; doc-repo-1d (found the still-valid "do not start G-52" instruction, drafted G-115) |

| A-108 | 2026-09-02 | **G-115 items 1 and 4 CLOSED (the two this dispatch covered — the close itself is explicit that items 2, 3, 5, 6 were "not attempted"). Item 5 separately resolved by direct operator ruling, not by this dispatch.** plan-review PR #14 merged (`1af5ac5`), deployed `plan-review-00014-bbg` @100% (a second instance of the Cloud Run traffic-pinning gotcha first found at G-111 — caught and corrected the same way, verified by JSON field not CLI summary) plus a Vercel redeploy. Real `bastrop_tx` persona live with cross-tenant read/write refusal live-verified both directions. IBC live query path wired; real sections resolve, nonexistent ones correctly typed-absent, IPMC2018P2 confirmed to have no real content and correctly left alone. **A real production defect found and fixed, precisely scoped:** the standalone `/api/plan-review/code` route had a payload-extraction bug silently breaking every resolved-section fetch through that route since G-108 shipped — confirmed live pre-fix on the already-shipped `BASTROP-UDC/14-02-003` atom. **Checked directly against `mcp.mjs` before accepting this, rather than assuming the close's framing:** `matrixFromChain` (what every WDLL-graded determination, including today's G-110 walk, actually runs through) builds citations via `buildCitation()` from data it already holds and never calls `getAtom()` — this defect could not have affected it. Nothing from G-108/A-095/A-103 needs revisiting. **Item 5 (tenancy/access decision) also resolved this session:** operator accepted the current persona-list access model for this pilot, scoped to a small named set of invited Bastrop staff; real tenancy/auth stays separately tracked, revisited before wider rollout. **Item 3 (honest UDC coverage measurement) was not separately dispatched but is answerable from what's now known:** exactly 2 of the Bastrop UDC's sections are real (`14-02-003`, `14-02-008`) — the coverage ratio against any realistic review is not yet measured against a real submittal sample, so this item stays open, not graded MET, despite the mechanism around it being ready. Close `_inbox/2026-09-03_g115-tenant-icc_close.json`. Leave-behind: Smart Files needs a matching `bastrop_tx` `QA_PERSONAS` entry before a Bastrop-persona file upload works (routed to that repo); the substrate's jurisdiction-rollup-vs-per-atom `accessPolicy` disagreement is named for the substrate seat. Remaining on G-115: item 2 (a live matrix run under the Bastrop persona/edition specifically — not yet attempted, distinct from the mechanism it depends on being ready), item 3 (coverage measurement), and item 6 (real staff go-live, operator's own action). | Operator: "looks like they both are closed" | nick; doc-repo-1d (verified, reconciled the matrixFromChain question, recorded) |

| A-109 | 2026-09-03 | **G-115 item 2 CLOSED (live matrix run under `bastrop_tx`, not `template-city`'s fixtures).** Real engagement created via the production BFF path (`plan-review-app-ten.vercel.app/api/backend`, the same route a real staff browser uses) as `bastrop_tx/staff` on real parcel `48021:34737` (Bastrop County, TX — resolved from the parcel itself, not asserted). Edition `bastrop_tx-bdc-2026-adopted` declared; matrix run twice on the same engagement, changing only `proposedSetbackFrontFt`: `30` → `14-02-003` Pass ("meets the 25 ft minimum"), `15` → Fail ("below the 25 ft minimum"), both carrying real citations (`editionId=bastrop_tx-bdc-2026-adopted`, `bookId=BASTROP-UDC`). **A genuine correction to the WDLL's own check text, found by reading source before executing, not after:** the check as literally written asks for "a genuine Pass or Fail on `14-02-003`/`14-02-008`" — `mcp.mjs`'s `matrixFromChain` wires only `14-02-003` to real adjudication logic (`adjudicateMinimumSetback`); `14-02-008` (permitted-use table) has none anywhere in the codebase (single `adjudicate*` export exists, grepped), by explicit design — "not a fabricated Pass." Confirmed `14-02-008` instead stays honestly `Unchecked` with a real citation under the new tenant, not regressed to `source-unavailable`, which is what would have actually indicated a problem. **No code changed** — this was a pure live-verification lane against the already-deployed `plan-review-00014-bbg` revision, dispatched and closed the same as a build lane (mission, CP1, CP2, close artifact) per `AGENT_CONTRACT.md` section 6's non-code-lane clause. Close `_inbox/2026-09-03_g115-matrix-verify_close.json`. **G-115 now stands at items 1, 2, 4, 5 MET; item 3 (coverage measurement) is unblocked, not started; item 6 (staff go-live) still depends on item 3 and the operator's own action.** | Operator: "yes" (dispatch item 2, then execute it live) | nick; doc-repo-1d (mission, live verification, correction, recording) |

| A-110 | 2026-09-03 | **G-115 item 3 CLOSED (honest UDC coverage measurement) — and the WDLL's own check text was wrong about where the gap is.** N=4 representative submittal types (new SFR, addition, commercial change-of-use, accessory structure) run live via the production BFF path across 2 real parcels in 2 zoning districts (`48021:34737`/SF-1, `48021:34177`/MU). **The check's literal wording ("the specific missing section numbers") assumes uningested sections are the gap — checked the real chain data before reporting a number and found that's false:** `zoning-fact`'s `codeSectionRefs` names exactly 2 UDC sections on both sampled parcels and both are already in `CODE_BOOKS` — section-reference coverage is 2/2 (100%). **The real, measured gap is one level down and narrower than assumed:** (1) section-ADJUDICATION coverage is 1/2 — `14-02-003` reaches real Pass/Fail, `14-02-008` (permitted-use table) has a real citation but zero adjudication logic anywhere in the codebase, by design, so whether a proposed use is even permitted in the district is never checked, for any submittal, ever; (2) within `14-02-003`, dimension-adjudication coverage is 1/6 — the real `setback-rule` atom carries six ordinance-sourced numeric fields (front, side-interior, side-corner, rear, max height, min lot size) but only front is ever compared against anything, confirmed with different real values on both sampled districts. **Submittal-type invariance confirmed live, not just from source:** all 4 samples across both parcels/4 declared types produced byte-identical coverage shapes — traced to `seedMatrix` never passing `engagement.projectType` into `matrixFromChain` at all. **A new finding, not previously named anywhere in this program:** both sampled parcels' real `setback-rule` atoms already carry an unresolved conflict between two City of Bastrop GIS layers on the exact dimension(s) being adjudicated (SF-1's front/side/rear: 25/5/25 vs 30/10/20/30; MU's max height: 40 vs 45) — flagged in the atom's own metadata ("verify which is in effect with the city") but never actioned by this program, meaning the one dimension that IS adjudicated may be checked against a non-authoritative number for an unknown fraction of parcels. No code changed — measurement only. Close `_inbox/2026-09-03_g115-coverage-measure_close.json`. **G-115 now stands at items 1, 2, 3, 4, 5 MET; item 6 (real staff go-live) is unblocked but should be scoped against this close's findings — a real reviewer will see honest Unchecked on use-permissibility and 5 of 6 real dimensions, every time, by design, not by defect.** | Operator: "yes" (proceed to item 3) | nick; doc-repo-1d (chain-data investigation, correction, live sample, recording) |

| A-111 | 2026-09-03 | **G-116 ADDED (approved) — real `bastrop_tx` city pack on `smartcity-dashboards`, Phase 0 (foundation) + Phase 1 (first real feed).** Operator direction: with G-115 (plan-review) closed, move to `smartcity-dashboards` itself — build the rest of the real Bastrop dashboard (parallel to live `smartcityos.io`, for an eventual cutover), UI/wiring first then one feed at a time, per the gap map's own recommended sequence (`_inbox/2026-08-17_dashboards_missing_pieces.md`). Two-Explore-pass investigation before drafting anything found: no real city pack exists yet (`template-city`/`empty-city`/`fixture-city` only); `city-pack.mjs`'s G-11 guard blocks the literal string `"bastrop"` only, not `bastrop_tx`; embed-URL cityKey threading (G-114, `03250b3`) is merged to `origin/main` but not yet in the checked-out worktree; smart-files has no `bastrop_tx` persona yet and its write path hard-ties `scopeId` to `persona.orgId`; all 11 Dashboards fixture domains are 100% fixture-only regardless of grants (real per-domain live-feed wiring doesn't exist anywhere yet); the one real, already-proven non-fixture feed is the municode public-meetings scraper, currently hard-blocked from ever running against a real Bastrop pack by pack-unaware identity guards (`isIdentityHeldClerkHost`/`assertPublicFeedSourceUrl`, from G-74) that refuse the real Bastrop host unconditionally. Decision `_decisions/2026-09-03_bastrop_tx_dashboards_pack_ratified.md` ratifies the pack (staging environment, not live — no go-live declared). **Scope of this card:** (1) sync the worktree, create the `bastrop_tx` pack (`tenant-private`, `environment: staging`, `generatesFixtures: false`); (2) add `bastrop_tx/staff` to smart-files' `QA_PERSONAS`; (3) live-verify embeds/staff-map resolve for the new pack; (4) make the municode identity guard pack-aware (refuse the real host on every pack except the ratified `bastrop_tx`, not unconditionally); (5) run the real municode scraper against `bastrop_tx` instead of `template-city`, live-verify real records land in smart-files. **Explicitly out of scope, named as Phase 2 and NOT started:** the other 10 fixture domains / real vendor feeds (MyGov, Samsara, OpenGov, Esri, FirstDue, Verkada, Spireon, GoTo, PowerBI) — zero credentials provisioned for any of them; the gap map's sanctioned shortcut (porting a real MyGov scraper/credential from live `smartcity-os`, read-only, if one exists) is an open question for the operator, not resolved here. Real staff auth/login stays deliberately unbuilt (named future item, `shell-state.mjs`). No PermitFlow/live-Bastrop touch. | Operator: plan approved | nick; doc-repo-1d (investigation, decision, build) |

| A-112 | 2026-09-03 | **G-116 Phase 0 + Phase 1 CLOSED — real `bastrop_tx` pack live on `smartcity-dashboards`, first real feed proven live.** `smartcity-dashboards` PR #41: new `BASTROP_TX` pack (tenant-private, `environment: staging`, `generatesFixtures: false`, `jurisdictionFips: "48021"`), registered in `ensureCityPacksTable`'s durable upsert list. `smart-files` PR #12 (merged `5fdc5d1`, deployed `smart-files-00010-b9v`@100%): `bastrop_tx/staff` persona added, mirroring plan-review's G-115-item-1 convention. **The real blocker found while implementing, not assumed from the plan:** the G-74 identity-hold guard (`isIdentityHeldClerkHost`/`assertPublicFeedSourceUrl`) is called from four distinct sites, not the two the plan expected — pack-validation time (`assertCityPackShape`'s own grant loop) as well as fetch/read time — all four needed `cityKey` threaded through to lift the hold correctly for `bastrop_tx` only, leaving every other pack (`template-city`, `fixture-city`, `empty-city`, any future pack) exactly as held as before. **Live-verified against production, not only unit-tested:** ran `run-municode-calendar.mjs bastrop_tx` against the real deployed smart-files backend and the real production Neon DB — fetched 25 real meetings from `bastrop-tx.municodemeetings.com`, wrote 5 (existing limit), read them back (`meetingsFromPack('bastrop_tx')`: ok, 5 records), confirmed `template-city` unchanged (still empty, still "no municode calendar grant"). Full suites green both repos (460/460, 28/28). `smartcity-dashboards` PR #41 merged (`01e9c141`) and deployed (`smartcity-dashboards-00030-whz`@100%, traffic confirmed by field) after the feed was already proven live against prod data — the app itself now serves the real pack. Post-deploy live-check: the new pack's `tenant-private` access policy is genuinely enforced (401 anonymous, 403 for a bare service-kind caller — read `tenancy.mjs` to confirm this is correct, not a defect: `canReadPack`'s tenant-private branch requires `callerIsPackSubject`, checked ahead of the service-key fallback). Full compose-content verification (embed URLs actually rendering) needs a real Hauska tenant key for `bastrop_tx`, which doesn't exist yet — that's staff-auth/tenant-key provisioning, a separately-tracked future item, not this card's scope. Close `_inbox/2026-09-03_g116-bastrop-dashboards-pack_close.json`. **Phase 2 (the other 10 fixture domains, real vendor feeds for MyGov/Samsara/OpenGov/Esri/FirstDue/Verkada/Spireon/GoTo/PowerBI) explicitly not started** — zero credentials provisioned anywhere; the gap map's sanctioned shortcut (porting a real MyGov scraper/credential from live `smartcity-os`, read-only) is a real open question for the operator, not resolved here. No PermitFlow/live-Bastrop touch. | Operator: plan approved; "yes" (execute live) | nick; doc-repo-1d (investigation, decision, build, live verification, recording) |

| A-113 | 2026-09-03 | **G-116 Phase 2 credentials TRANSFERRED — 7 of 9 remaining vendor feeds now have real credentials available; 2 (Esri, Verkada) genuinely have none anywhere.** Operator direction: check live `smartcity-os` production env for existing vendor credentials before assuming Phase 2 needs vendor onboarding from zero. Found `smartcity-os-prod` (GCP project) holds real, live secrets for MyGov (base URL, username, password), Samsara (API key, webhook secret), OpenGov (API key, BNP API key, email), FirstDue (API email, password), GoTo (account key, client ID/secret), PowerBI (client ID/secret, tenant/workspace/report/dataset IDs), and Spireon (account name, nspire ID, password, sysdevx ID, token, username) — 25 individual secret values across 7 vendors. Copied all 25 into the `smartcity-dashboards` GCP project's own Secret Manager (byte-length parity verified on a sample, no truncation), named without the `smartcity-` prefix and lowercased to match this project's existing secret-naming convention (e.g. `smartcity-MYGOV_BASE_URL` → `mygov-base-url`). **Deliberately NOT copied:** `smartcity-PIPEDRIVE_API_TOKEN` — this program's own standing rule is explicit ("Never Pipedrive as a city feed," `_catalog/dispatch_missions` CANON-PREAMBLE / `_decisions`), so it stays out regardless of it existing on the source side. Also not copied: smartcity-os's own internal app secrets (`DATABASE_URL`, `SESSION_SECRET`, `CREDENTIAL_ENCRYPTION_KEY`, `ANTHROPIC_API_KEY`, `GOOGLE_CLIENT_ID/SECRET`, `RESEND_API_KEY`, admin/user reset passwords, `VFD_JWT_SECRET`, `BASTROP_BOOTSTRAP_PASSWORD`) — these are Bastrop's own auth/session/infra secrets, not vendor-feed credentials, and copying them would be scope creep well beyond what Phase 2 needs. **Confirmed absent, searched by name and by keyword (esri/verkada/gis/camera), nothing found:** no Esri or Verkada credential exists anywhere in `smartcity-os-prod`. Those two remain genuinely blocked on real vendor onboarding, not a credential-transfer problem. **What this does NOT do:** the real-feed mechanism itself (a granted adapter actually reading live vendor data instead of a fixture) still doesn't exist for any of the 11 domains — A-112's finding stands unchanged. This amendment provisions the credentials the mechanism will need; building the mechanism and wiring each domain to its real feed is separate, unstarted engineering work. `smartcity-os` itself was read-only accessed (Secret Manager listing/read), never written to — the absolute-no-touch rule was about modification, and the program's own gap map explicitly sanctions this exact kind of read-only credential/scraper porting. | Operator: "all the creds live in bastrop prod env... let's get them transferred/copied to where it needs to go" | nick; doc-repo-1d (found, mapped, transferred, verified) |

| A-114 | 2026-09-03 | **G-116 Phase 2 — `smartcity-os` platform-internal read endpoint LIVE, first MyGov real feed unblocked.** Operator explicitly authorized crossing this program's standing absolute-no-touch line, narrowly: one new, additive, key-gated endpoint. Ratified `_decisions/2026-09-03_smartcity_os_platform_read_authorization.md`. Built `GET /api/platform/mygov/permits` (`smartcity-os` PR #39, merged `68fe8cc`) — server-to-server read of Bastrop's already-synced active permits, gated by a new `PLATFORM_INTERNAL_API_KEY` (fails closed if unset), reusing the existing `dbPermitToApi` shape and the file's own documented accuracy contract (`in_mygov_active_list=true`, not `status_normalized` — the same 193-record undercount gotcha named there). **A real bug caught before any traffic shift, by live-testing a tagged 0%-traffic revision rather than trusting the unit tests:** the new route returned the SESSION gate's generic 401 (`{"message":"Unauthorized"}`), not the route's own error — a global `isAuthenticated` check applies to every `/api/*` path except an explicit allowlist, and `/platform` wasn't on it. The unit tests (`buildTestApp()`, matching this repo's own existing test convention) never exercised that global middleware, so they passed while the real deployed behavior was wrong. Fixed (PR #40, merged `d2d3648`): added `/platform` to the same allowlist `/vfd`/`/grant-writer` already use for their own non-session auth — not a weakening, the route's own key check still fails closed on every request; only the session-cookie check (which doesn't apply to a service-to-service caller) is bypassed. **Deployed with full discipline given the stakes** (a live system serving real Bastrop staff): tagged, `--no-traffic` first, smoke-tested wrong-key (401), no-key (401), correct-key (200 with real live permit records — real addresses, permit numbers, applicants), AND confirmed the existing session-gated `/api/mygov/permits` route unaffected — only then shifted traffic. Verified 100% on `smartcity-api-00121-xez` by field, re-confirmed on the untagged production URL post-shift. New secret `platform-internal-api-key` also copied into `smartcity-dashboards`' own Secret Manager (same value, for the consumer side) with IAM granted to the `smartcity-api` runtime service account for the source side. **What's not yet built:** the `smartcity-dashboards` consumer code (permits-pipeline domain actually calling this endpoint instead of generating a fixture) — that's the next unit of work, not done here. | Operator: "deploy" | nick; doc-repo-1d (design, build, live-caught bug, fix, careful deploy) |

| A-115 | 2026-09-03 | **G-116 Phase 2 — first real (non-fixture) domain feed LIVE end to end: MyGov permits on `bastrop_tx`.** `smartcity-dashboards` PR #42, merged `8f61062`, deployed `smartcity-dashboards-00044-red`@100%. New `src/mygov-permits.mjs` reads the `smartcity-os` platform endpoint (A-114) and maps rows onto the existing record envelope with `origin: "feed"` — the first record this product has ever produced that isn't a fixture. **Deliberately not a branch inside `composeDomain`/`fixture-seam.mjs`**: that file's own header states it is not a feed and its guards exist to catch a fixture that looks real — running genuinely real data through them would be a category error. Branches at the `server.mjs` route level instead, matching how `meetingsFromPack` already sits beside `composeDomain` for the municode feed; `domains.mjs` and its other 9 domain consumers are completely untouched (avoided a 10-file async ripple). **Real status values are NOT force-mapped onto the fixture's invented four-stage taxonomy** — sampled live, real Bastrop permits carry `active`/`in-review`/`pending`/`completed`, not `overdue`/`awaiting-applicant`/`in-review`/`ready-to-issue` (only one value overlaps), and `smartcity-os`'s own code names the source field "unreliable for permits — do not use for counts." Forcing a guessed mapping would assert confidence the source system's own team explicitly disclaims; real records keep their real status, a new `extras.realStatusCounts` groups by what's actually there. **Live end-to-end verified against the real `smartcity-os` endpoint directly (not mocked)** before any deploy: 312 real active Bastrop permits, correctly mapped (real addresses, departments, managers). **Deploy verification honestly incomplete in one specific, already-named way:** the HTTP route itself still correctly returns 403 for a bare service-key caller against the `bastrop_tx` (tenant-private) pack — the same Hauska-tenant-key gap named in A-112's deploy note, not a new one. `template-city` (public-free, fixture) confirmed completely unaffected. Both `/api/domains/permits-pipeline` and `/api/city-domains` (the summary map) patched consistently so they cannot disagree about whether this region has a source for the same pack. New tests: 9 (`mygov-permits.test.mjs`), full suite 469/469. | Operator: "yes" | nick; doc-repo-1d (design, build, live-verify, careful deploy) |

| A-116 | 2026-09-03 | **G-116 Phase 2 — `smartcity-os` source-side platform reads for the rest of the MyGov family LIVE.** `smartcity-os` PR #41, merged `1ff70d2`, deployed `smartcity-api-00123-huq`@100%. Four more platform-internal endpoints alongside permits (A-114): `/api/platform/mygov/{work-orders,inspections,code-violations,business-licenses}`. Work orders use `inMygovActiveList=true AND tenant_id=2`, matching the file's own top-of-file accuracy contract verbatim — deliberately NOT the session-gated list route's broader default view (`inMygovArchivedList=false` + a resolution exclusion), which is a UI-browse convenience, not the contract's stated authoritative filter. Inspections and code violations have no documented active-list flag for either resource, so both return the full tenant-scoped set, matching their session-gated counterparts rather than inventing a filter the source system doesn't apply. Zero new TypeScript errors (410 before, 410 after, diffed not assumed). Smoke-tested on a tagged 0%-traffic revision before shifting: all four return real Bastrop records (real work orders, real inspections, real code-violation cases, real business licenses), existing `/api/platform/mygov/permits` and the session-gated routes both confirmed unaffected. 12 new tests, full suite 126/126. **Consumer side (smartcity-dashboards) not yet built for these four** — permits is the only one actually wired to a domain; this amendment covers the source-side only. | Operator: "wire it up" (continuing G-116 Phase 2) | nick; doc-repo-1d (design, build, live-verify, careful deploy) |

| A-117 | 2026-09-03 | **G-116 Phase 2 — remaining 5 non-mygov vendors: source side LIVE, consumer side LIVE for 3 of 5, honestly blocked (not broken) on 2.** `smartcity-os` PR #42, merged `29eada2`, deployed `smartcity-api-00125-fec`@100%: five more platform-internal routes (samsara/spireon/firstdue/powerbi/goto), `requirePlatformInternalKey` exported from `mygov.ts` so every vendor file reuses one auth gate. `smartcity-dashboards` PR #44 (consumer side, `src/vendor-live.mjs`): five new single-kind grants, `server.mjs`'s dispatch generalized into one `REAL_LIVE_DOMAINS` table covering all ten real domains now. **GoTo deliberately aggregate-only** — this product's own fixture domain already drew that line for invented call data ("never a call... no mapping from an extension to a person"); real call detail carries real citizen phone numbers, a higher sensitivity class, so the real feed keeps the same restraint (one daily all-queues record, never individual calls). **Live-verified before any deploy, all five, not assumed:** fleet-vehicles 75 real Samsara vehicles, patrol-vehicles 24 real Spireon units, cip-projects 14 real PowerBI capital projects — all genuinely live. fire-apparatus and call-analytics are **honestly unavailable, not broken**: FirstDue's own code already documents a real 403 (apparatus/assets API scope not granted to the current credential, contact `dashboards@firstarriving.com`); GoTo's OAuth consent (`GET /api/goto/authorize` on smartcity-os) has never been completed by a human. Both grants are provisioned; each starts working the moment its real-world blocker clears, no further engineering needed. Zero new TypeScript errors on the smartcity-os side (410/410, diffed). 34 new tests total across both repos. **G-116 Phase 2 status: 8 of 10 real domains fully live (5 mygov + 3 of 5 here); 2 blocked on real-world action, not code.** Verkada (police-cameras) remains the one domain with no credential anywhere, needing real vendor onboarding. | Operator: "get it all live and do the auth before qa" | nick; doc-repo-1d (design, build, live-verify, careful deploy) |

| A-118 | 2026-09-03 | **G-116 — real auth provisioned end to end for `bastrop_tx`: a real Hauska tenant key minted, and the browser given a way to actually present it.** Two real gaps closed together, both live-verified before any deploy. (1) `hauska-mcp-server` (substrate seat's system; `POST /admin/keys`, an already-exposed admin API, same routine key-minting precedent as G-111/G-112 — not a code change to that repo, no new authorization needed beyond what those rows already established) minted a real `tier: team` key with `jurisdiction_tenant: "bastrop_tx"`, owned to the operator's own email. Confirmed via `/auth/whoami`: resolves `jurisdiction_tenant: "bastrop_tx"` correctly. (2) `smartcity-dashboards`'s own backend has always accepted this exact caller shape (`resolveHauskaTenant`/`tenancy.mjs`; `shellSession`'s own basis text already named it: "a Hauska product key resolved to a city pack tenant") — but no browser fetch call site ever sent the header, and no login UI exists (a named, deliberately-unbuilt future item). PR #45: a one-time `?hauskaKey=...` query param seeds `localStorage` (URL/history cleaned via `replaceState` after), a thin `window.fetch` wrapper attaches it to same-origin `/api/` calls only. Nothing changes for any public-free demo pack. **A real, meaningful catch mid-build:** the city-name gate (`ui.test.mjs`) and the G-90 anti-stranding guard (`shell-parity.test.mjs`, "only the theme is stored on the device") both correctly fired against the first draft — the gate for a literal pack name in a code comment, the guard for a second `localStorage` key. First fixed by genericizing the comment (no pack name belongs in served markup, ever, not even in a comment); second fixed by updating the guard's own allowlist with an explicit rationale (the new key holds a caller-presented credential, never anonymous visitor work — the guard's actual purpose is unaffected), not by working around either check. **Live end-to-end verified before commit, the whole chain, not just the pieces:** the minted key against the real `hauska-mcp-server`, then the same key against the real deployed `smartcity-dashboards` compose endpoint for `bastrop_tx` — real tenant-scoped data returned, including the plan-review/smart-files embed URLs correctly threading `cityKey=bastrop_tx`. Full suite 486/486. Raw key value delivered to the operator directly, never in this record or in chat. | Operator: "get it all live and do the auth before qa" | nick; doc-repo-1d (mint, build, live-verify) |
| A-119 | 2026-09-03 | **G-116 CLOSED — combined production deploy of PR #44 (5 vendor consumers) + PR #45 (real auth bootstrap), all 10 real-domain routes and real auth confirmed live on the traffic-serving revision.** Both PRs' CI went green independently (`smartcity-dashboards` #44 a11y+test, #45 a11y+test) and were merged in sequence (`d17085c`, `181f588`). Local checkout fast-forwarded `238e023..181f588` (27 files, both PRs' full diffs), full suite re-run clean on the merged tree: 505/505. Deployed as one build (`gcloud run deploy --no-traffic --tag=g116-vendor-auth`, no new secret — `PLATFORM_INTERNAL_API_KEY` already bound from A-117's deploy), revision `smartcity-dashboards-00048-kic`. **Smoke-tested on the tagged 0%-traffic URL before any traffic shift, not assumed from the merge:** `template-city` fixture route unaffected (200); all 5 tenant-private `bastrop_tx` real-domain routes correctly reject an unauthenticated caller (401, not 404 — confirms the routes exist and are access-gated, not missing); the real minted Hauska key (A-118) against `permits-pipeline` returns 312 real records, `origin: "feed"`, `source: "live"` — the full auth chain, not just presence. Vendor domain (`fleet-vehicles`) confirmed 200 with the same real key. Traffic shifted to 100% on `smartcity-dashboards-00048-kic`, **verified by field** (`status.traffic`), not by the CLI's own summary line — same discipline as every prior deploy in this card. Re-verified the identical smoke checks against the now-100%-traffic main service URL (not just the tag URL) after the shift, to rule out any routing difference between the tagged and default paths. **G-116 is now closed**: real `bastrop_tx` pack, 8 of 10 real domains genuinely live, 2 honestly blocked on real-world vendor action (not code), real auth minted and verified end to end through the browser bootstrap. Matches the operator's own stated sequencing — data feeds, then auth, then UI QA — both prerequisites now live; ready for the operator's deep UI QA session. | Operator: "get it all live and do the auth before qa" | nick; doc-repo-1d (merge, sync, deploy, live-verify, careful traffic shift) |
| A-120 | 2026-09-04 | **G-116 — two real bugs found and fixed on the operator's OWN first look at the live dashboard, not from a dispatch.** The operator asked whether the Pipeline page was showing real data and, separately, reported "my bastrop login went away" after clicking a nav item; both were investigated and were real defects, not operator error. (1) **The Development Services / Pipeline page — the exact page the operator's earlier screenshot showed — never dispatched to real data at all.** `/api/domains/:id` and `/api/city-domains` both got the real-source branch when the ten real domains shipped (A-115–A-117); `/api/lenses/development-services/pipeline` was the one route that never got it, so it kept calling the fixture composer (`composeDomain`) unconditionally — always fixture, even for `bastrop_tx`, even after the domain-level route and the map already agreed the same pack had a real source. Fixed by threading a precomposed real result through `composePipeline(pack, precomposed)`, mirroring the existing dispatch exactly. (2) **None of the 8 live real-domain composers ever populate `extras.metrics`** (only `extras.realStatusCounts`, by design — real status values don't line up with each domain's invented fixture taxonomy, per A-115's own finding), but the summary tiles atop every real domain panel only ever read `extras.metrics`, so every one of them said "Not read" despite a full table of real records underneath. Fixed by rebuilding the tile strip from `extras.realStatusCounts` when present — one tile per real value actually returned, never forced into the fixture's fixed four named slots. Caught and fixed alongside, not left for later: a handful of table cells (permit stage, due date, work-order SLA hours, code-violation escalation rung) would have rendered the literal word "undefined" for real records, since those are fixture-only synthetic fields the real MyGov data model doesn't have; hardened to render blank instead — deliberately NOT inventing real values for them, which stays a named open item for the operator's own UI session, not something decided unilaterally here. (3) **Found while scoping the above, a third, unrelated bug with the same symptom the operator actually reported:** every one of the 24 static nav hrefs shipped in `index.html` (every lens, every work item, every tab) carries no `cityKey`, so clicking ANY sidebar link dropped the visitor back onto the default pack — invisible for the one pack that used to exist (the default IS what the resolver falls back to with no query at all) and exactly what the operator saw as "my login went away" on `bastrop_tx` (the Hauska key itself never left `localStorage`; the CITY selection was what reset). Fixed by threading the active `cityKey` through every nav href once at boot, a no-op for the default pack. All three shipped together, `smartcity-dashboards` PR #46, merged `6aed1a5`. **Live-verified against the tagged 0%-traffic revision before any shift, then re-verified against the same, now-100%-traffic main URL after** (same discipline as every prior deploy in this card): default-pack pipeline route unaffected (200); `bastrop_tx` pipeline with the real minted key (A-118) now returns `sourceStatus: "ok"`, 312 real records, `realStatusCounts` populated with the same four real values A-115 first sampled (`active`/`in-review`/`pending`/`completed`); served `app.js` confirmed to carry the nav-threading fix. Deployed `smartcity-dashboards-00050-hiz`@100%, verified by field. 512/512 tests, new coverage for the pipeline real branch, the tile fallback, and the nav-href fix (including a test that fails if the shipped markup ever stops needing the fix, so it cannot go stale silently). | Operator: "is it serving in the template city..." / "my bastrop login went away" | nick; doc-repo-1d (investigate, fix, live-verify, careful deploy) |
| A-121 | 2026-09-04 | **G-116 — three more real bugs found on the operator's own Fleet page QA, two on `smartcity-dashboards`, one on `smartcity-os`.** Operator sent a screenshot of the real `bastrop_tx` Fleet page: 75 real Samsara vehicles, summary tiles all "Not read", a "Demo records" pill sitting over them, and every single vehicle's status reading "unknown". All three investigated as real defects. (1) **`vendor-live.mjs`'s `okResult()` never populated `extras.realStatusCounts`** — the same field A-120 item 2 already fixed for the mygov domains, missed on this module (fleet/patrol/fire-apparatus/cip/call-analytics). Fixed identically, mirroring `mygov-permits.mjs`/`mygov-live.mjs`'s own helper. (2) **`renderRegion`/`renderPipeline`/`sourcedLabel` showed the fixture-only "Demo records" claim on ANY `ok` payload**, with no real/fixture check at all — correct back when `ok` only ever meant fixture, an active false claim over 75 genuinely real vehicles now. `sourcedLabel` is also what drives the sidebar nav badge and the Overview register row (`applyLensState`), so the same false claim was showing in three places from one bug. Fixed at the call edges (`payload.source === "live"`, `pipeline.realStatusCounts`, `regions.some(r => r.source === "live")`) rather than inside `LENS_BADGE`/`LENS_BADGE_ORDER`, which stay byte-identical to the fixture seam's own guarded five-word vocabulary — `src/lens-claims.test.mjs` holds them equal to `DOMAIN_STATUSES` and a real domain is not a seam status, so it does not get a sixth word spliced into that set. A real `ok` region now reads "Live records" instead. Both (1) and (2) shipped together, `smartcity-dashboards` PR #47, merged `9ad2a70c`, deployed `smartcity-dashboards-00052-vug`@100%, verified by field. 513/513 tests. (3) **`status: "unknown"` on all 75 vehicles was not a mapping bug — it was a real, verified gap in `smartcity-os`'s own platform route.** `GET /api/platform/samsara/vehicles`'s own code comment claimed engineState/odometer/fuel "are genuinely real" from `cachedFetch('vehicles', '/fleet/vehicles')` alone; live-tested against the real deployed route and found false — 0 of 75 vehicles carried any of the three. Root cause: that endpoint is Samsara's static vehicle roster, not telemetry; those three fields live on a separate `/fleet/vehicles/stats` endpoint the narrower platform route never called, unlike the existing, already-proven session-gated `/api/samsara/vehicles` route. Operator explicitly authorized crossing the no-touch line again ("fix it") for this narrow, additive extension of a route already authorized under the standing platform-read decision. Fixed by adding the identical parallel stats-batch call and per-vehicle merge the proven route already makes — reused, not re-derived. `smartcity-os` PR #43, merged `e0b627a`, 15/15 platform-route tests unchanged, `tsc --noEmit` 410/410 (no new errors). **Before deploying, verified by ancestry** (`git log`/`git show`, not assumed) that `smartcity-os` `main`'s tip was exactly "what's already live in production + this one commit" — a stale local branch pointer briefly made it look like ten commits of unrelated concurrent engineering work had landed since the last deploy; confirmed this was a local-checkout artifact, not real drift, before touching the live no-touch repo's traffic. Deployed `smartcity-api-00127-quv`@100%, verified by field. **Live result, honest and partial:** odometer now real for 72 of 75 vehicles (e.g. 10,167 mi) and engine hours present — a genuine improvement, live-verified on the tagged revision before the shift. Engine on/off state and fuel percent remain empty for all 75, using the exact same proven field-extraction the working route already uses — read as a real hardware/sensor-coverage gap on this fleet's actual equipment (OBD-derived stats like odometer are broadly available; engine-state and fuel-level sensors are not guaranteed on older or retrofitted municipal vehicles), not a further code defect, and not chased with a guessed fix absent vendor-side evidence. Named as open, not silently declared complete. | Operator: screenshot of Fleet page, then "fix it" | nick; doc-repo-1d (investigate, fix, live-verify, careful deploy across two repos) |
| A-122 | 2026-09-04 | **G-116 — production-comparison field-enrichment pass, 5 parallel agents, one per domain family, real gaps closed against real production reference.** A read-only research pass first compared production's actual staff dashboard (`smartcity-os`'s own `client/src/pages/*`) against the 10-domain build here. Findings shaped scope: several nav items already marked "Not built"/"Empty" (Parks, Records search, People and access, Assets) turned out honest — production doesn't have live equivalents either, just placeholder/mock pages or a third-party embed; call-analytics' aggregate-only, no-per-call-detail design was CONFIRMED the right call, not a gap, since production exposes full per-call detail including citizen phone numbers. Operator chose to enrich the 9 already-built domains only this pass, explicitly deferring whole new domains (Finance etc., named not started). Dispatched 5 agents, one per domain family so shared files (`vendor-live.mjs`, `mygov-live.mjs`, `app.js`, `index.html`) wouldn't collide across concurrent work; each produced its own `smartcity-os` + `smartcity-dashboards` PR pair, explicitly instructed to open PRs only, never merge or deploy. **MyGov family**: applicant/contractor/ownerName/itemized fees added to permits; a real `smartcity-os` bug fixed en route (work-order fees/assignee lived in an unparsed text blob the platform route never decoded — now reuses `parseWOEnriched`, the exact function production's own dashboard already calls for this); a bonus field found beyond the original ask (inspection `comments`). Nothing invented — every field traced to a real column or an already-working production code path before being added. **Fleet (Samsara)**: DVIR unresolved-defect count + last inspection, 7-day safety event count, high-mileage/low-fuel threshold flags, all from functions the session-gated production route (`fetchDvirs`/`fetchSafetyEvents`) already calls, reused rather than re-derived; `0` and `false` preserved as real values, never conflated with "unknown" (`null`). **Patrol (Spireon)**: NSpire active status, maintenance alert count, 7-day recent alert count, same reuse pattern (`deriveMaintenanceAlerts`/`fetchAlerts`). **Fire apparatus (FirstDue)**: the agent correctly DECLINED the originally-scoped occupancy pre-plan fields once it traced them to a different FirstDue endpoint (`/occupancy`) than apparatus (`/apparatus`) — adding them would have fabricated a shape the apparatus API doesn't have, the same category of mistake this card has already caught and corrected twice. It found and fixed a real, separate, evidenced bug instead: the apparatus table already had `apparatusType`/`stationLabel` columns the live mapper never populated. Still honestly live-unverified — FirstDue API access remains 403'd, unrelated to and unresolved by this fix. **CIP (PowerBI)**: found the real Gantt phase data (`phases`/`overallCompletion`/`currentPhase`/`status`) was already computed and exposed by the existing platform route and simply never read on the dashboards side — the `smartcity-os` PR for this domain is test-only, zero production changes, added coverage that locks the already-correct shape in. **All 5 `smartcity-os` PRs merged and deployed together** (`smartcity-api-00130-loc`@100%, 56/56 tests, `tsc --noEmit` 410/410 unchanged), **live-verified field by field before the traffic shift, not assumed**. Two of my own smoke-test misreads caught and corrected by reading the actual merged code rather than assuming either side was broken: checked `stats.dvir`/`stats.safetyEvents7d` when the real fields are top-level (`dvir`, `safetyEvents7d`); checked for a literal `activeInNspire` key in the raw `smartcity-os` response when that name is a deliberate dashboards-side rename of the source's own `active` field, confirmed correct once traced. Two genuine real-world data-sparsity findings surfaced honestly rather than chased as further bugs: DVIR is null for all 75 fleet vehicles (plausible non-use of the driver-side DVIR submission workflow — the mechanism itself proven working since `safetyEvents7d` populates correctly with real, honest zero counts) and work-order fees/contractor are null for all 194 current work orders (same parsing function production's own dashboard already relies on, so most likely genuine absence of enrichment data for this tenant today, not broken wiring). **4 of 5 `smartcity-dashboards` PRs merged and deployed** (`smartcity-dashboards-00054-les`@100%, 540/540 tests) — MyGov, fleet, patrol, fire-apparatus. **CIP's PR hit an unrelated, stale accessibility adjudication on its first CI run** (pinned to the Development Services Inspections/Licenses pages, not CIP at all — a check category this repo's own `a11y-gate.mjs` documents as measurably environment-sensitive, "four measurements of one commit, no two agreeing"); a rerun was kicked off rather than editing adjudication code not fully understood on a guess. | Operator: "yes let's address as much as we can... spawning subagents" | nick; doc-repo-1d (dispatch, review, live-verify per field, careful deploy across two repos) |
| A-123 | 2026-09-04 | **Map architecture — research complete, scope deliberately deferred, nothing built.** Operator asked to stop embedding SmartSite in the `smartcity-dashboards` staff map and instead read from "the root map repo that smartsite feeds from," then separately asked about layering Bastrop-only water/utility data on the same base map. Three parallel research passes (`smartcity-os`'s real production map, the `hauska-map`/`hauska-factory` stack, this repo's own prior planning) found the request conflated three genuinely separate systems, and surfaced a standing operator ruling directly in tension with the ask: **the real, live, already-working "type an address, see the permit/zoning/flood-plain" map has zero relationship to SmartSite** — it's `smartcity-os`'s own Leaflet + live Bastrop/Bastrop-CAD ArcGIS feature services, address-string-matched to permits (not parcel-ID joined), with real DEAD scaffolding for role-gated layers that was never wired up. **SmartSite/`hauska-map`** is a genuinely separate property-seat product (MapLibre, a real public unauthenticated PMTiles bucket with no discoverable "latest" manifest) that has nothing to do with Bastrop permit data specifically, and whose "74,729 parcels / full road network" marketing claim does not hold for Bastrop (roads explicitly undocumented/unbuilt there). **The Bastrop water/utility-layer idea is real Asset Management scope (G-24), already named/priced ($52k/$13k) — but stands against operator's own prior ruling "Do not fill G-24" (A-054, 2026-08-17), unresolved here, flagged for the operator explicitly rather than assumed either way.** One genuinely good finding: `smartcity-dashboards`' current map embed is architecturally content-agnostic — `MountStage.mount(src)` (`web/app.js:287`) is exactly `this.frame.src = src`, so the animated present/collapsed/max panel shell the operator wants preserved is completely independent of what's inside the iframe; swapping the eventual real destination in touches one URL composition (`src/compose.mjs`/`src/mounts.mjs`), never the UI/animation code. **Operator's call after seeing the findings: document only, use what already works for the current cutover, do not build any of this now — treat it as a separately and carefully scoped future project.** Findings recorded in full at `_inbox/2026-09-04_dashboards_map_architecture_scoping.md`, including the two real, unresolved candidate directions for the eventual build (iframe a `smartcity-os` route directly, cross-origin/auth feasibility unverified; or a Dashboards-native map reading `smartcity-os`'s real ArcGIS data through a new narrow platform-internal-read endpoint, the same proven pattern used five times this week) — neither chosen, both left for the future scoping pass. | Operator: "document what you found... carefully scoped larger project" | nick; doc-repo-1d (research across 3 repos, synthesize, defer) |
| A-124 | 2026-09-04 | **G-116 field-enrichment closes: CIP's PR (A-122) was not stale/flaky as first suspected — two real, separate problems, both found and fixed rather than re-run blind.** (1) **A genuine, new accessibility regression**: the new "Project phases" Gantt table shipped `<th>` header cells with a permanently-empty `<tbody>` on any pack that doesn't populate `record.phases` — every fixture pack, including the one CI scans by default — a real `th-has-data-cells` violation (axe: "Table data cells are missing or empty"), not the unrelated stale Inspections/Licenses adjudication first suspected from an earlier, misleading log read. The domain's own "honest-empty, never fabricate a row" stance was correct in principle but misapplied: that stance covers not inventing data, not leaving a headerless-data table visible to a screen reader. Fixed by hiding the table itself (not just its body) when no real phase rows exist, matching the same `show()`-plus-static-caption pattern every other empty-state region in this app already uses — new test coverage added for the fix. (2) **A real merge conflict against `main`**, invisible in the CI log because GitHub cannot even construct a merge tree to run `pull_request` checks against a conflicting PR — the first "rerun" silently reran against the same broken tree and produced an identical-looking failure, which is why it looked like a flake repeating rather than a blocked check. The other 4 domain-family PRs (A-122) had all merged to `main` while this one sat open, all touching the same shared files (`web/app.js`, `src/ui.test.mjs`). Resolved by merging `main` into the PR branch directly (two clean side-by-side-addition conflicts — two independent functions/test blocks landing at the same insertion point — both kept, nothing dropped), re-tested (546/546), re-pushed; CI then ran for real and passed clean. Merged, synced, deployed `smartcity-dashboards-00056-bor`@100%, live-verified real `currentPhase`/`phases`/`status` on the tagged revision before the shift. **G-116 field-enrichment (A-122) is now fully closed: all 10 platform routes, all 5 domain families, live.** | Operator: "check the CI" (repeated) | nick; doc-repo-1d (diagnose past a misleading log, fix for real, resolve conflict, live-verify, deploy) |
| A-125 | 2026-09-04 | **Two new lanes opened, dispatched to background agents, neither merged/deployed yet.** (1) **Native property map, replacing the SmartSite embed.** Following A-123's deferred research, the operator asked to move on the one concrete, buildable piece: preserve `smartcity-dashboards`' existing animated map-panel chrome (`MountStage`, fully content-agnostic — confirmed unchanged) but have it house Bastrop's real, currently-functioning map instead of SmartSite. The literal ask (iframe `smartcity-os`'s own map page directly) was checked LIVE before dispatch and found genuinely blocked, not assumed: `curl -sI https://smartcityos.io/` returns `x-frame-options: SAMEORIGIN` and `frame-ancestors 'self'` on every response — it refuses to be framed by any other origin, full stop, and weakening that on a live session-authenticated production system was never on the table. Dispatched instead as the one path that actually works: a new, Dashboards-OWNED map page (Leaflet, matching `smartcity-os`'s own stack for behavioral consistency), reading the same real ArcGIS parcel/zoning/flood/permit data through a new narrow platform-internal read route on `smartcity-os` (same proven pattern as every G-116 domain), mounted into the exact same `MountStage` slot the SmartSite URL occupies today — a first vertical slice (parcel + zoning + flood + permits/inspections/code-cases), explicitly not the other ~35 ArcGIS layers. Real, non-obvious production behavior named explicitly in the dispatch so it isn't silently lost: the permit/address join in `smartcity-os` is address-STRING matching plus a 50m proximity check, not a parcel-ID join. (2) **Compass groundwork.** Per A-decision `2026-09-04_compass_rework_starts_parallel_to_plan_review` (operator relaxed the 2026-09-02 Plan-Review-leads sequencing for this specifically), a research pass was dispatched into Smart Files' real, current data model, search/query surface, access-policy enforcement, and any existing MCP tool-use scheme — grounding for the first real Compass build increment (Smart Files context-awareness, the operator's own named priority) before any Compass code gets written. Both are open, running in isolated worktrees; nothing merged or deployed. | Operator: "yes get the map build going with a sub agent" / Compass scope direction | nick; doc-repo-1d (verify blocker live, dispatch, ground before building) |
| A-126 | 2026-09-04 | **Compass groundwork returned a real, decisive, sobering finding: Smart Files has no search today, at all.** No full-text index, no PDF content extraction, no keyword/filter query endpoint — every route in `smart-files/src/server.mjs` is lookup-by-already-known-ID (folder ID, entity ID). The only "search" anywhere in that codebase is a client-side filename filter over an already-fetched folder listing. PDF bytes are stored as opaque `bytea`; nothing reads their content. **The product one-pager's own sold claim — "one search across everything your city holds, including the text inside the PDFs. Search a contractor name, a case number, or a phrase you half remember" — is not implemented.** This is the exact capability the operator named as the sales anchor ("this is the thing I want them to buy into") and the first priority for Compass to nail. It cannot be nailed by wiring Compass to Smart Files as it stands today; Smart Files itself needs real content search/indexing built first, which is a genuinely separate, foundational, unscoped-until-now piece of work, not a Compass-side integration task. Access control, by contrast, IS real and specific (a real `access_policy` enum, service-token-scoped grants, cross-tenant refusal proven in CI) — a real Compass backend would need its own service-token identity naming exactly the scopes a caller may read; there is no per-end-user identity propagation today, grants are static per service token. **For `bastrop_tx` specifically, almost nothing exists yet to be aware of**: 5 real JSON meeting records from the municode scraper, and that is the entire real content in Smart Files for this tenant — the 10 other planned domains have never touched Smart Files. No MCP surface exists for Smart Files either, and it is absent even from the platform's own MCP-first product roadmap (`28_mcp_first_product_design.md`) — a Compass backend would hand-write new tool-use functions against a plain HTTP API from scratch, there is nothing to plug into. **Not yet acted on** — reported to the operator to resequence against, since "Compass aware of Smart Files, done well, first" now visibly implies "build Smart Files search first," a materially different and larger piece of work than originally framed. | Operator: Compass scope direction ("make Smart Files context work well first") | nick; doc-repo-1d (research, report honestly rather than build on an untrue premise) |
| A-127 | 2026-09-04 | **Reprioritization: "make these systems function together" named as the actual thesis, the native map downgraded from active priority to a good later project.** Operator, on hearing the Smart Files search finding (A-126): "the most important work we can do is make these systems function together, that is the entire thesis (which is why i wanted smartsite map in here) but i think that will be a good later project still and first get all this linked together and reading properly." This retroactively reframes the original map ask as one instance of a broader platform thesis (the Dashboards flyer's own line: "One record underneath, not four systems stitched together") rather than the goal itself — a real map matters less than the underlying systems actually cross-referencing each other. Recorded as `_decisions/2026-09-04_systems_linking_is_the_thesis.md`. **Concretely**: the already-dispatched native-map agent (A-125) stays running — no reason to waste in-flight work — but its PR, once returned, gets reviewed and held rather than rushed to merge/deploy; it is not blocking anything. A new scoping-only research pass (explicitly not a build) was dispatched covering two things: (1) what real content search in Smart Files would actually require — text extraction approach, indexing strategy consistent with the existing plain-Postgres stack, a query endpoint shape consistent with the existing `requireReadScope` access-control convention, and an honest effort/complexity read (net-new background job infrastructure or a small addition). (2) Whether Smart Files' own `smart_file_placements` table — which already MODELS document↔permit/parcel/project/asset/meeting links in schema — is actually populated for any real data today (starting with the 5 real `bastrop_tx` municode meeting records already in Smart Files) and whether `smartcity-dashboards` reads it anywhere, or whether the cross-linking mechanism exists in schema only, unused end to end on both sides — and if so, the smallest real next step to make one real linkage true, not a redesign. Compass's own Smart Files-context-awareness priority (A-decision 2026-09-04, Compass rework) is unaffected in principle but now visibly converges with this same work rather than running independently. | Operator: "the most important work we can do is make these systems function together... first get all this linked together and reading properly" | nick; doc-repo-1d (record reprioritization, dispatch scoping, hold in-flight map work) |
| A-128 | 2026-09-04 | **The native-map agent (A-125) returned: `smartcity-os` side done and real, `smartcity-dashboards` side correctly halted by a genuine architectural conflict the agent caught rather than forced through.** `smartcity-os`: `GET /api/platform/property-intel/summary` opened as PR #49 (not merged) — geocodes an address via the real, already-proven `/api/esri/geocode` logic, resolves the parcel via the real `/api/esri/one-click-at-point` query, and hands both to `getPropertySummary()`, the EXACT function the session-gated production route already calls — the non-obvious address-string + 50m-proximity permit-match logic is genuinely reused, not re-derived. Nothing existing touched; 174 tests green, `tsc` unchanged. **`smartcity-dashboards`: no PR opened, correctly.** Before writing any code, the agent read the target repo's own founding architecture doc (`README.md`, G-61, 2026-08-17, still current) and found a stated, tested, permanent product boundary: **"No Leaflet island"** — this product's whole design is "mount the real product via embed" (SmartSite, Smart Files, the Hauska spine), never "reimplement that product's own function locally," enforced by two real regression tests asserting served output never contains the literal string "leaflet" (`src/mounts.test.mjs`, `src/server.test.mjs`). A Leaflet parcel/zoning/flood map inside Dashboards is exactly the thing this rule exists to prevent. The dispatch (A-125) had explicitly instructed Leaflet, in direct contradiction — the agent stopped rather than force a fragile workaround or silently violate a tested founding rule, leaving its dashboards-side worktree untouched. **This is a good catch, not a setback, and it validates rather than complicates A-127's reprioritization**: "No Leaflet island" reads as this product's own prior, tested instance of the exact thesis the operator just named today ("one record, not systems stitched together") — reimplementing production's map function locally would itself be the fragmentation this whole session has been trying to avoid. Left exactly where it is: `smartcity-os` PR #49 open and reviewable; `smartcity-dashboards` blocked on a real product decision (override the rule explicitly for this one case, or find a non-Leaflet rendering approach — e.g. server-rendered/SVG parcel shape — for the same real data the now-ready platform route serves) that stays deferred along with the rest of the map work per A-127, not urgent. | Operator: (agent self-halted on a real architectural conflict) | nick; doc-repo-1d (review agent output, record the conflict honestly, hold per A-127) |
| A-129 | 2026-09-04 | **Both open lanes un-parked with real, explicit operator decisions; both back in active motion.** (1) **"No Leaflet island" explicitly, deliberately overridden for the property map, one case only.** Operator's own reasoning: they underestimated the map's real cost when they wrote that rule (2026-08-17), a SmartSite launch is imminent and nothing should risk it right now, and this Leaflet map is an accepted, temporary, workable-not-perfect stopgap — to be swapped for the real architecture once SmartSite is ready and there's capacity to do it properly. Recorded as `_decisions/2026-09-04_no_leaflet_island_overridden_for_bastrop_map.md`. The halted map agent (A-128) was resumed in place (its worktree was left clean, no rework lost) with the explicit authorization and instructed to update the two "no leaflet" regression tests narrowly — the exception should cover only the new property-map page, not silently widen to permit "leaflet" anywhere else this product serves. (2) **Smart Files gets real search as a scoped wave, not a quick patch**, per the operator: "we need the real path laid out and executed against as a wave." Scope decided and recorded (`_decisions/2026-09-04_smart_files_search_wave_scope.md`) before dispatch, not left open-ended: PDF text extraction (`pdf-parse`, gated on content-type) + Postgres `tsvector`/GIN indexing + a real, access-gated `GET /api/smart-files/search` endpoint, extraction synchronous-on-upload (the real, working choice at today's actual volume — 5 documents — not async job-queue infrastructure this repo doesn't have and doesn't yet need); plus fixing the one real, already-available linkable content, the municode meeting uploads, which hard-code `target_type: 'folder'` on every write despite the schema already modeling a real `meeting` target type. **Checked, not assumed, before dispatch: real permit/work-order document content does not exist anywhere upstream today** (`smartcity-os`'s own `mygov_permits` table schema has no document/attachment/file field at all) — so cross-linking those domains' documents is explicitly named as a separate, unstarted future initiative in the scope doc, not half-built toward now. Both lanes dispatched to background agents, isolated worktrees, PR-only (no merge/deploy) per every prior dispatch's discipline. | Operator: "bastrop existing map needs to come into the new platform... this is the safer path" / "we need the real path laid out and executed against as a wave" | nick; doc-repo-1d (record both decisions with full reasoning, resume/dispatch, hold merge/deploy for review) |
| A-130 | 2026-09-04 | **Both G-117 lanes returned real, reviewed, and (server sides) deployed. `smartcity-dashboards`-side map PR still in CI.** **Map**: resumed agent built the dashboards-side Leaflet page (`web/property-map.html/.js/.css`, `src/property-map.mjs`, `src/compose.mjs`'s `nativePropertyMap` flag defaulting `false` for byte-identical fixture-pack behavior, `src/server.mjs`'s real-branch gate) and narrowed the two "no leaflet" regression tests to a named, self-verifying exception set (exactly `web/property-map.html`) citing the decision doc — both arms still fire (nothing else leaked "leaflet" in, the gate still catches it elsewhere). 562/562 tests. Reviewed the diff personally before merging (routing/auth gating, the `nativePropertyMap` conditional, the CDN script inclusion) rather than trusting the agent's self-report alone. `smartcity-dashboards` PR #53 open, CI running (test green, a11y in flight). **`smartcity-os` side** (PR #49, additive-only — confirmed by diff, 105 insertions/0 deletions on the one file touched) merged, deployed `smartcity-api-00132-vof`@100%, live-verified with a real address (710 Chestnut St — the same real permit address this card has used as a live fixture since A-115) returning real geocoded coordinates, real parcel polygon, real owner name, real zoning. **Smart Files search wave**: PR #13 merged after review (SQL migration, search-gate ordering, the honest not-throw extraction module all read directly, not just trusted), CI green (33/33 unit, 87/87 round-trip, run for real against a throwaway Postgres before push per the agent's own report). **Migration applied directly to the real production Neon DB** via this repo's own established `scripts/apply-sql.mjs` tool (additive `ADD COLUMN IF NOT EXISTS`, confirmed against `db=neondb`, sane row count), not assumed safe — this was the one genuinely consequential step in this deploy and it was checked, not skipped. **A stale, unrelated local checkout found and correctly left alone**: `P:/smart-files` (a different local path than the seat-registered worktree) showed ~20 files "modified" per `git status`, investigated before touching anything per standing discipline, found to be a pure Windows line-ending artifact (`git diff` on the same files showed zero real content change) — not real uncommitted work, left untouched, worked from the correctly seat-registered worktree instead. GCP project for this service (`smart-files-505619`, region `us-east1`) was not previously in this card's own record — found by listing accessible projects, not guessed. Deployed `smart-files-00013-ruq`@100%, verified by field pre- and post-shift: search endpoint correctly 403s an unauthenticated caller on both the tagged and main URLs. | Operator: (continuation of A-129's dispatch) | nick; doc-repo-1d (review before merge, apply real migration carefully, verify GCP project rather than guess, deploy, verify by field) |
| A-131 | 2026-09-04 | **G-117 CLOSED — the native Bastrop property map's own PR (#53) went green and deployed, all three lanes of this card now live end to end.** `smartcity-dashboards` PR #53 merged (562/562 tests), synced, redeployed `smartcity-dashboards-00058-pen`@100%. **Live-verified by field, not assumed, on the actual behavior that matters most — that the map stage swap is genuinely conditional, not a blanket change**: `template-city` (and by extension every fixture pack) composes its city-manager response exactly as before (`smartsite.basis: "missing parcelNodeId"` for the no-selection state, unchanged from pre-G-117 behavior); `bastrop_tx` alone now composes `smartsite.url: "/property-map.html?cityKey=bastrop_tx"`, `basis: "native property map (G-117)"`. The served page returns 200; its own live address-search API (`GET /api/property-map/summary`), hit with the real minted Hauska key against the real address "710 Chestnut St," returned real matched geocoding, a real parcel polygon, real owner ("CHESTNUT GROVE LLC"), and real zoning text — the exact same real record this card's own smartcity-os smoke test (A-130) already proved, now reachable through Dashboards' own UI rather than only the platform route directly. Two apparent problems during this verification pass were each traced to my own query mistakes, not code defects, before being logged as real findings — a discipline worth naming since this card has caught the same category of false alarm from itself multiple times this session: nested response fields (`result.match`/`result.parcel`) checked at the wrong path, and the empty-parcel compose state misread as a possible regression before comparing it against the pre-existing, unchanged behavior for a bare `cityKey` with no `parcelNodeId`. **G-117 is now closed**: real Bastrop property map live in `smartcity-dashboards`, backed by `smartcity-os`'s real ArcGIS data (not re-derived), Smart Files carrying real search for the first time in this product's history, the meeting-placement linking bug fixed, and the underlying architectural exception (`No Leaflet island`, overridden once, narrowly, with a stated reversal trigger) fully documented rather than silently taken. | Operator: (continuation of A-130) | nick; doc-repo-1d (merge, sync, test, deploy, verify the conditional behavior specifically by field) |
| A-132 | 2026-09-04 | **G-117 follow-up, two rounds: real fields the map was silently dropping, then real visual parity with production, both from the operator's own side-by-side screenshot comparison.** Operator compared the new map against production's live "GIS & Property Intelligence" panel directly and said "see the difference." **Round 1 (fields):** traced to a real, bounded bug, not a missing-upstream-data problem — `smartcity-os`'s `getPropertySummary()` already computes real CAD valuation (appraised/market/land/improvement value, year built, living area) and a compliance score; `mapRealPropertyResult` (dashboards) was silently dropping both, and `legalDesc`/`futureLandUse` were already flowing through the API response but never rendered at all. Fixed: passed through honestly (null valuation stays null, never a guessed $0 — a new discipline for this page specifically, since real money had never been rendered anywhere in this product before), added a Valuation section and the two missing Parcel fields. **Round 2 (styling, "yes i want the same styling too"):** basemap swapped from a generic OSM tile server to the exact Esri `World_Dark_Gray_Base` tiles production's own `BASEMAP_OPTIONS` (id `dark`) uses — same URL, same `maxNativeZoom: 16`, confirmed against the real source file, not approximated. The bigger piece — production's colored parcel/zoning look — turned out to be four distinct, always-on layers (Zoning Districts `#7c3aed`, Future Land Use `#8b5cf6`/`#c4b5fd`, Subdivisions `#1e40af`/`#3b82f6`, Parcels One Click `#3b82f6`), each its own fixed color per `layerCatalog.ts`, not a per-parcel attribute scheme — confirmed by reading the real source before dispatch, not guessed. Built as a new, narrow, allowlisted platform-internal route (`smartcity-os` PR #50 — real 400 on any of the ~50-entry registry's other keys, required bounds on all four, zoning served through its own dedicated ArcGIS query since it isn't a `BASTROP_LAYER_REGISTRY` entry like the other three) plus viewport-bbox fetching debounced on `moveend`/`zoomend`, each layer's own `minZoom` respected exactly, a real checkbox toggle control (`smartcity-dashboards` PR #55). **Both merged and deployed**: `smartcity-os` (PR #50, merged, `smartcity-api-00134-pam`@100%, live-verified with a real Bastrop bounding box — 157 real zoning-district features, 8 future-land-use, 500 parcels-one-click, subdivisions honestly 0 for this specific bbox rather than assumed broken given the other three clearly worked; bad-key rejection confirmed 400 with the real allowlist). Both diffs reviewed personally before merging, not trusted on the agents' self-reports alone. **A real merge-conflict pattern recurred a third time this session** (same class as the earlier CIP conflict): the second overlay-layers agent branched from a LOCAL checkout that was already sitting on the still-open valuation-fields PR's branch, so its own PR stacked on top; once that PR (#54) squash-merged, the stacked PR (#55) showed CONFLICTING against the rewritten history. Resolved the same proven way — merge `origin/main` into the branch, one trivial conflict (a new test block landing after `main`'s now-empty tail), nothing dropped, re-tested clean before re-push. | Operator: screenshot comparison, "see the difference" / "yes i want the same styling too" | nick; doc-repo-1d (compare against real production screenshots, ground every fix in the real source, review before merge, resolve the now-familiar stacked-PR conflict, deploy, verify by field) |
| A-133 | 2026-09-04 | **G-117 follow-up CLOSED — the overlay-layers PR (A-132) went green and deployed; visual parity with production is now fully live.** `smartcity-dashboards` PR #55 merged (587/587 tests), synced, redeployed `smartcity-dashboards-00060-gor`@100%. Live-verified through Dashboards' own route (not just the platform route already proven in A-132): `GET /api/property-map/layers?cityKey=bastrop_tx&key=zoning` against a real Bastrop bounding box returned a real GeoJSON `FeatureCollection` with real zoning attributes — `placeType: "Mix"`, `placeTypeClass: "P-4"`, a genuine multi-sentence zoning description, a real ordinance reference (`2019-...`). One more of my own smoke-test path mistakes caught before being logged as a finding (checked `layer.totalReturned`, smartcity-os's own raw shape, against the dashboards route's reshaped `result` field instead) — same discipline as every prior instance today, verify the actual response before concluding anything. **The map now has: real address search with real permits/inspections/valuation/legal data (G-117 close), the exact Esri basemap production uses, and the same four colored zoning/parcel overlay layers production shows — genuine visual and data parity with the live system, built as new Dashboards-owned code reading real data server-to-server, never an iframe of the no-touch production system.** | Operator: (continuation of A-132) | nick; doc-repo-1d (merge, sync, test, deploy, verify through the actual product route, not just the platform route) |
| A-134 | 2026-09-04 | **G-117 map layers taken from a 4-layer first slice to full 52-layer production parity, after the operator flagged the map was "still missing layers" and directed a complete, one-time inventory-and-build rather than another incremental patch.** An Explore pass against `smartcity-os` found the real scope: production's canonical "GIS & Property Intelligence" map (`DevelopmentServicesDashboard.tsx` + `layerCatalog.ts`) renders 55 layers across 7 categories, not the 4 already ported — plus 6 layers (zoning, future-land-use, fema-flood-zones, subdivisions, parcels-one-click, pci) render with per-feature styled color overrides (`STYLED_LAYERS`) rather than a flat swatch, and 10 named view-template presets. Scoped to 52 of the 55 (excluding `permits`/`violations`/`heatmap`, production's MyGov-backed "overlays" category — a different source/shape entirely, named as an explicit follow-up, not silently dropped). **`smartcity-os` side** (PR #51, merged, `smartcity-api-00138-law`@100% after a second small fix): widened the platform-internal bridge's allowlist from 4 to 52 keys, added the 4 new special-cased query branches (`etj`, `wastewater`, `cip-projects`, `parcels`) by duplicating each one's real production route body, never calling into it. Live smoke-testing all 52 keys against a real Bastrop bounding box surfaced two genuine findings, both handled honestly rather than glossed over: (1) **a real bug** — the live wastewater ArcGIS service is 100% lift-station facility polygons, but the new branch (copying a pre-existing production assumption) only accepted line geometry, silently dropping every feature; fixed in a follow-up PR (#52, merged, 18/18 tests) to accept polygon geometry too, confirmed live (0 → 40 real features); (2) **a pre-existing production defect, correctly left untouched** — `storm-drainage`'s registry entry points at an ArcGIS FeatureServer layer ID that doesn't exist (confirmed directly against the ArcGIS service, git-blamed to the original map build, long before this session), so it errors identically in production's own live map; out of scope to fix (shared registry config, not this seat's narrow platform-bridge route), flagged rather than patched. The other 5 previously-untested keys that returned zero results (evacuation-routes, shelters, incident-points, road-blocks, detours, impacted-areas) were verified genuinely empty upstream (real zero-count queries direct against ArcGIS), not broken. **`smartcity-dashboards` side** (PR #56, merged, 620/620 tests, `smartcity-dashboards-00062-ful`@100%): a new `src/property-map-catalog.mjs` module ports the full 52-layer catalog, the 6 styled-override functions, and the 10 view templates field-for-field from the real production source (spot-checked byte-for-byte identical against `DevelopmentServicesDashboard.tsx`'s own color tables before merge, not trusted on the agent's self-report alone); the server-side key allowlist is now derived from that same module (`getAllLayerKeys()`) rather than hand-duplicated, so the two can't drift apart. The flat 4-checkbox toggle was replaced with a categorized, searchable panel (7 collapsible categories, per-category show/hide-all, color swatches, live minZoom warnings, the 10 preset buttons) matching production's own `LayerManager.tsx` UX. **One process note**: the build agent for this PR ran for several hours with no interim signal; on the operator's repeated, increasingly direct prompting to check on it, its actual state (a complete, tested, already-pushed branch that simply hadn't reached the "open a PR" step) was found immediately once checked — the lesson recorded going forward is to check an agent's real repo state the first time its progress is questioned, not the third. Full pipeline live-verified against real ArcGIS data end to end on the `smartcity-os` side (all 52 keys); the `smartcity-dashboards` route itself requires the real per-tenant Hauska key to call, which this seat does not hold outside the operator's own browser session, so the final visual/UI check is the operator's own to make against the live page — everything checkable server-side (source parity, full test suites, the underlying data) is done and verified. | Operator: "lots to be done on the map... i dont want to keep coming back to this... get it all lined out and operational in the same fashion" | nick; doc-repo-1d (full-catalog inventory via Explore, two parallel builds, two real bugs found and handled honestly — one fixed in-scope, one flagged out-of-scope — merge, sync, test, deploy, verify by field) |
| A-135 | 2026-09-14 | **R-10 applied, and the premise it was dispatched on was wrong.** The three decision records cited by shipped code were never missing: they were written on the day the work shipped and STRANDED on `seat/govtech`, a branch 26 commits ahead of main, never merged and never pushed. The earlier recon checked both clones' working trees and filenames, correctly reported absence, and said explicitly it had not searched git history — the conclusion travelled and the caveat did not. The bookkeeping was done; the DELIVERY failed, and the failed artefact was a git branch. R-10 refused its own instruction to write reconstructions, on the grounds that stamping `reconstructed-awaiting-ratification` on genuinely ratified decisions would violate the mission's governing rule from the opposite direction, and recovered all six byte-identically instead, verified by blob hash with the hash check itself verified by violation. Applied today: branch pushed to origin and merged (A-109 through A-134 now on main, resolving a collision that had not happened yet — the next amendment on main would have taken A-109); **G-116 and G-117 filled into the existing four-ID hole rather than taking new numbers**, because fourteen PR bodies, ten Cloud Run tags, six source files and twenty-six amendments already cite exactly those numbers, and A-111 reads "G-116 ADDED (approved)" for a row that was never typed; G-52's blocker corrected — the feed dependency is GONE (a live MyGov feed serves 312 real permits) and the real blocker was always the unbuilt engagement-from-permit bridge, two conditions fused in one sentence; and `_catalog/repo_intents.md` now records the narrow `smartcity-os` exception and its boundary, having read ABSOLUTE NO-TOUCH with no mention of it, which was wrong in both directions. G-118 and G-119 stay unallocated — no artefact cites either. | operator: "r10 i lean on your rec" | nick; doc-repo integration (applied, verified) |
| A-136 | 2026-09-15 | **CARDING PASS — the roadmap reconciliation had no plan rows, and nine things that had been committed to a customer had no row anywhere. G-137 through G-145 ADDED.** `_inbox/2026-09-15_roadmap_reconciliation.md` closed by saying so itself: *"It does not assign plan rows. Nothing here is carded to OPS-17, and the two live defects and the Smart Files gap in particular have no row."* Every row was written against a source read in this session rather than transcribed from the reconciliation, and two of the nine came back different from how they had been described. **G-137** Localgov Filings (hotel occupancy tax) — scope and bid brief were written and had no row. **G-138** the filings design AMEND, plus the finding that it is the only live design folder with no `check.mjs`. **G-139** cameras: the code is BUILT and fails closed correctly (`/api/verkada/health` returns `not_configured`); no Verkada secret exists in `smartcity-os-prod`, `hauska-prod-497015` or `legacy-design-tools-prod`. It is vendor onboarding, not a defect to debug, which is a different card from the one "the camera API connection is broken" implied. **G-140** zoning: NOT a data gap and NOT a cache. `BASTROP_ZONING_URL` in `smartcity-os` `server/routes/esri.ts` reads `PlaceTypesCharacterDistricts/1`, last edited by the city **2023-04-28**, 565 features, while Bastrop publishes three newer zoning layers — `Zoning_Place_Type/0` (2025-06-02, 574), `Zone_Types/25` (2026-07-09, 595, the city's own description: *"the new zoning districts that will replace the B3 PlaceType code (2025)"*) and `Zoned_Parcels/83` (2026-07-23, 7,125). The query succeeds, the layer is healthy, every requested field is present, and the answer is three years and four months old, so **no failure mode this product can detect is present**. This is the silent half of G-136: G-136 was loud (a dropped field, whole query rejected, city told us in days) and its fix would not catch a layer that is merely superseded. Instrument built and run: `scripts/govtech/bastrop-zoning-layer-vintage.mjs`, 17/17 self-tests both directions, 4 inputs matched, verdict STALE, exit 1. It derives the read URL from the product source at a named ref and compares against the city's live services, so one party acting alone cannot satisfy both halves. Evidence: `_inbox/2026-09-15_bastrop_zoning_layer_findings.md`. **G-141** Smart Files link ingest: zero hits for googleapis/oauth/drive/onedrive/sharepoint in `smart-files` `61c84f61`, thirteen routes and none an ingest, upload requires `bytesBase64`. ABSENT, not partial. **G-142** Citizen lens design — and it ships with real markup and a correctly declared disabled lookup, so it is not a blank. **G-143** People and access design. **G-144** the plan-review role-gate deferral, whose own decision record says its only trigger is that somebody remembers; this row is the mechanism. **G-145** the five remaining lenses, reclassified from backlog to migration obligation by the v1 retirement. **Two carries not carded here.** `Zoned_Parcels_Revisions_Clip` (7,125 features, edited 2026-07-23) carries per-parcel `ZoneType`, `MinimumLotSize` and four setback fields — a live city-published setback and envelope source for Bastrop that no product reads; recorded in G-140's leave-behind rather than carded, because it belongs to whoever owns the envelope path. And seven further Bastrop ArcGIS layer URLs are hardcoded in `esri.ts` with no vintage check; G-140 scopes the FIX to zoning and the INSTRUMENT to all eight. **Assets and Connections deliberately not carded**, per the standing ruling that Assets stays zero until a named ingest and Connections needs an engineering pass first. | The reconciliation named its own gap and left it as the next planner's first job. Work that cannot name a PLAN-ROW is not scoped, and five of these nine had been described to the customer on the 2026-09-15 Jaime call | planner (carding; each row verified at source, no scope invented) |
| A-137 | 2026-09-15 | **OPERATOR RULING: finishing the design work is the FIRST card, ahead of the sequence in the roadmap reconciliation. G-146, G-147 and G-148 ADDED.** Operator, on reading the A-136 carding pass: *"there is much more design work to do... make finishing the design work the first card."* This reorders `_inbox/2026-09-15_roadmap_reconciliation.md` section 4, which had put hotel occupancy tax first. Bastrop approves the design before we build, so every build row sits behind design completion. **The numbers were re-counted from the folders rather than carried from the addendum, and they hold:** 11 design folders, 48 boards, 96 exported images (48 dark + 48 light), `_kit.css` byte-identical across all 12 copies. **What the count does not say, and the gate does.** Against the shipped nav (`smartcity-dashboards` `f776b4bf`, `ALL_LENS_IDS` nine and `WORK_IDS` six): **15 nav surfaces, 5 designed, 2 excluded by ruling, 8 uncovered.** Design is a third done measured by surface, which 48 boards across 11 folders comfortably hides. **G-146** is the completion gate and the front of the queue, with `scripts/govtech/design-completion-gate.mjs` as its instrument: three independently derived inputs (the product's nav constants at a named ref, the `_design/` inventory on disk, and the declarations in `_design/INDEX.md` plus the new `_design/surface_coverage.json`), so a wrong declaration fails against the filesystem and a stale folder fails against the nav. It is a refusal rather than a report — 24/24 self-tests both directions including a fixture that PASSES, 15 surfaces matched, verdict UNFINISHED, 13 findings, exit 1. **G-147** carries the three surfaces that had no row anywhere: Records search, Compass, and the applicant-facing plan review view. Shell chrome is deliberately NOT in it — G-90 already shipped eight shell functions via PR #26, so what is missing there is a design pass over a set built function by function. **G-148 is the finding this pass produced.** The rule that no design is shown or ratified without an adversarial read as a file was earned on 2026-09-15 and **was never applied backwards**. Five designs are past DRAFT with no instrument, and **two are already dispatched to build**: `smartcity-overview-lens` (RATIFIED, G-120) and `smartcity-map-dock` (APPROVED, G-128), plus `smartcity-flood-study` and `plan-review-reasoner` RATIFIED and `plan-review` IN REVIEW. Three of eleven folders carry one. A build is running against a design nobody checked against source, and every one of the seven defects this thread caught was invisible to a re-read of the canvas. Same shape as every other control here that was correct and enforced nothing. **One instrument defect caught and fixed before it was recorded as a finding.** The gate's first run reported `all-canvas` as an INDEX.md entry with no folder on disk. It is a real folder; it carries `build.mjs` rather than `gen.mjs` because it is a derived view that composes the other folders' boards. The folder scan, not the repo, was wrong. Derived views are now distinguished, exempt from R1 and R3, and still required to exist, with a regression test in both directions. **And the review artefact does not show all the designs.** The planner first recorded `_design/INDEX.md`'s "all 38 artboards" as a stale count against the 48 that exist, then checked it and the correction is worse than the claim: `all-canvas/build.mjs` declares exactly 38 boards across 10 rows, so INDEX.md is accurate about the build and **the build omits two whole surfaces** — `smart-files` and `plan-review-departments`, 10 boards, whose copies sit in the folder undeclared. The one artefact titled ALL DESIGNS ON ONE CANVAS silently drops Smart Files, which is one of the three DRAFTs waiting on a ratify/amend/kill, so reviewing designs through that canvas cannot reach it. Folded into G-146. Also unresolved: `_design/INDEX.md` lists `plan-review` as IN REVIEW while the addendum lists it as superseded by the reasoner set; the two disagree, and G-146 resolves it alongside the already-owed choice of which of three plan review designs a city is shown. | The reconciliation ordered the work before the design state had been counted by surface. Counted, design is 5 of 15, and the customer ruling is that design lands before build | operator (reordering); planner (carding, each row source-checked) |
| A-138 | 2026-09-15 | **FOUR OPERATOR RULINGS, and TWO PLANNER CORRECTIONS to rows carded three hours earlier.** **Ruling 1: Police and Fleet are IN SCOPE for design** (`_decisions/2026-09-15_police_and_fleet_lenses_in_scope.md`). G-145 becomes five lenses rather than three plus a scope question. The exclusion was written when v1 was the fallback and did not survive v1 ceasing to be one: both ship in v1 today, so an undesigned lens does not hold its current level of service after cutover, it disappears. Designed at the honest-absence level; rich design waits for a department to ask. **Ruling 2: the REASONER is the plan review design a city is shown** (`_decisions/2026-09-15_plan_review_reasoner_is_the_shown_design.md`). `_design/plan-review/` flips to SUPERSEDED and is kept as the record of the option not taken, the same treatment `smartcity-place-tab` got. It is the only one of the three that was RATIFIED, it is the one that matches the standing ruling that plan review is a COMPANION and not a system, and it is the one that survives the role-gate deferral intact. Resolves the disagreement A-137 recorded between `_design/INDEX.md` and the addendum, in the reasoner's favour. **Known weakness stated in the record rather than left to be found: the design this ruling selects has no `check.mjs`, so it is RATIFIED and shown on no instrument. G-148.** **Ruling 3: the Bastrop review artefact is DONE.** The design files are kept for reference rather than retired. **Ruling 4: G-148 is HELD** at operator direction in favour of continuing to draw. It is not killed and the gap it names is unchanged: five designs past DRAFT carry no adversarial read and two of them are dispatched to build. Ruling 2 just added a third consequence, since it rests on one of the five. **Correction 1, to G-143 as carded in A-136.** Its Blocked-on cell reads "G-132 (staff authentication) for the vocabulary it must draw against". **That is wrong and the row is not blocked.** The vocabulary already ships: `smartcity-dashboards` `origin/main` `src/staff-identity.mjs` exports `DEPARTMENT_ROLES` (seven) and `TIER_ROLES` (nine, the seven plus `city-manager` and `admin`). People and access can be designed today. The planner asserted a dependency without reading the file, which is the same error class as the Connections dependency asserted on 2026-09-15 and found not to hold. **Correction 2, to the G-138 AMEND.** The `Ordinance rate 7.00%` citation **is sourceable and is now sourced.** City of Bastrop Code of Ordinances Article 11.04, **Sec. 11.04.002 Levied; amount; exemptions**, verbatim: *"There is hereby levied a tax of seven percent (7%) of the price paid for a room in a hotel on every person who, under a lease, concession, permit, right of access, license, contract, or agreement, pays for the use or possession... of a room that is in a hotel, costs two dollars ($2.00) or more each day, and is ordinarily used for sleeping."* Read from the city's own published article at `cityofbastrop.org/upload/page/0181/docs/Hotel_Occupancy_Tax.pdf`. **The figure was right and the citation was absent, which are different defects**, and AMEND was still the correct call: plausibility is precisely what stops anyone checking. The second citation, a fund number attributed to the city ledger, remains unsourced and needs the city finance director. G-138 is half closed. **Lane opened: `g145a-department-lenses`**, compiled to `_dispatches/2026-09-15_g145a-department-lenses_dispatch.md` against PLAN-ROW G-145 with mission `_catalog/dispatch_missions/mission_g145a_department_lenses.md`, and executed by a planner-owned SUBAGENT rather than hand-carried, at operator direction. The subagent does not commit and does not nest; the planner reads the diff and commits. Scope is Public works, Parks and Fire and EMS only; Police and Fleet follow as a separate lane. **The design argument was handed to it from source rather than left to be re-derived:** `public-works` has two registered domains (`powerbi`, `goto`), `fire-ems` one (`firstdue`), and **`parks` has none at all** — and `src/domains.mjs` rules that absence from `DOMAIN_REGISTRY` is the only surviving meaning of "not built", where emptiness of a registered domain is a statement about SOURCES with a basis. Parks is therefore not an empty Public works, and a design rendering the two alike collapses two states the product deliberately keeps apart. | The operator ruled two open scope questions and the design lane could not open without them. The two corrections are the planner's own rows failing the standing rule that source state is checked before a claim, not after | nick (rulings 1-4); planner (corrections, both found by reading source) |
| A-139 | 2026-09-16 | **G-145 LENS WORK COMPLETE BY INSTRUMENT — Police and Fleet drawn by two parallel planner-owned subagents, and all nine lenses in the shipped nav now have a design.** `design-completion-gate.mjs`: **15 nav surfaces, 10 designed, 2 excluded by ruling, 3 uncovered**, 16 design folders with 8 carrying an instrument. Zero uncovered LENSES; the three remaining are Citizen (G-142), Records search (G-147) and People and access (G-143). Ratification is a separate call and is still owed on six drafts. **Police is the richest of the five and its spine was derived, not asserted.** It is the only registered lens whose two regions DISAGREE about their source, and it disagrees in opposite directions on the two shipped packs: on `template-city` verkada is granted and spireon deliberately withheld; on `bastrop_tx` spireon is granted live and verkada is not granted at all. **The demo’s emptiest region is the city’s most connected one.** `gen.mjs` computes that from the registry against both grant lists and throws if it stops being true. Four boards for three distances from data plus a refusal the product has no word for: a grant deliberately withheld (the ungranted exemplar, which exists so `ungranted`, `granted-empty` and `not built` stay three sentences), a vendor nobody has onboarded (verkada, G-139), and plate reads plus persons-of-interest declared `required:false` WITH a basis because a guard catches an attempt and a contract prevents it. Occupancy is a band and the not-measured band is a CONSEQUENCE of status, drawn as a 16-cell matrix with 8 cells that cannot occur by rule and two independent routes to 3 that the product compares nowhere. **Fleet is the first lens designed whose vendor actually answers**, so the question stops being whether the page fills and becomes what the page still says once it has. Its cutover board is an engineering finding rendered as a surface: on a granted feed the page still says the operator dimension has not been read for a region that WAS read, and says the same of an inventory position that was never a reading, because `okResult` returns only `realStatusCounts` and `renderFleet` overwrites those two basis sentences only when extras that carry them exist. The third row is the control that proves the panel is not a refuse-everything rewrite. **Three product findings this repo cannot fix, owed to the seat that owns `smartcity-dashboards`.** (1) **`assertRecordShape` is called by `composeDomain` and by NOTHING in `vendor-live.mjs`.** Run by hand against the live samsara record it refuses with three faults, and the live record carries **12 undeclared fields including `vin`, `make`, `model`, `odometerMiles`** — an inventory field set arriving on exactly the cutover that drops the one sentence saying this is not an inventory. (2) **Every exported live mapper invents exactly three sentinel strings on an empty row**, and samsara and spireon share the literal `"Unnamed unit"`, so id collisions cross lenses. The Fleet lane first claimed this was local to Fleet, then ran all five mappers instead of assuming, found the claim false and corrected the board, the README and its close rather than fixing it quietly. (3) `operatorRef` is `required:true` on the spireon shape and occurs **zero** times in `vendor-live.mjs` — a required field silently absent on every live record. **Planner verification, not taken on report.** Both lanes: scope clean, product repos untouched, `_kit.css` md5 identical, checks re-run here (Police 85 self-tests, Fleet 71, every matched-input count non-zero), every board re-rendered independently and read, and the Police matrix and Fleet roster arithmetic done by hand. The planner planted its own violations rather than re-running either lane’s. **Three of the planner’s own plants were MIS-AIMED at a single occurrence when the checked position was elsewhere**, and all three were caught once aimed at every occurrence — the checks are tightly scoped rather than sloppy, and the planner’s aim was the defect twice. **One real hole found, and it is not universal.** A fixture badge CHANGED to falsely claim `Live records` passes, while DELETING the badge fails — the worse direction, because an absent badge is a visible omission and a false one is a claim. Measured across four folders: `smartcity-public-works-lens` CAUGHT, `smartcity-fire-ems-lens` CAUGHT, `smartcity-police-lens` CAUGHT, **`smartcity-dev-services` NOT CAUGHT, `smartcity-fleet-lens` NOT CAUGHT.** dev-services is the oldest check and the pattern the others copy, so the hole propagated from there. Belongs to G-148. **A ruling is owed and both lanes raised it independently.** `OPERATOR_REF_FORMAT` and `OPERATOR_BASIS` are declared separately and byte-identically in `fleet-vehicles.mjs` and `patrol-vehicles.mjs`; Fleet mints OPR-01..04 and Police OPR-01..03 on one pack, and nothing in the product says whether OPR-01 is the same person. Neither lens can resolve it alone and neither asserted anything. Planner recommendation: **namespace the operator reference by domain** — one person appearing as the same opaque id across two departments is either a join nobody declared or a collision nobody noticed, and a city should not be the one to discover which. **Convention divergence, kept deliberately.** The Police lane shipped `violate.mjs` and `dump-source-state.mjs` inside its folder. No other design folder carries them. Keeping both: a committed violation harness makes verify-by-violation reproducible by the next reader instead of re-derived, which is the difference between a control and a memory. Recommend it becomes the convention at G-148. | Operator ruled Police and Fleet in scope (A-138) and directed subagent execution rather than hand-carried dispatch. Two lanes run in parallel, each verified independently by the planner before commit | nick (scope, execution model); planner (verification, the badge-hole measurement, the namespace recommendation) |
| A-140 | 2026-09-16 | **SMARTCITY PARKED for a few days, with one entry point and the track reconciled against the shipped product.** New doc `_design/SMARTCITY_PACKAGE.md`, linked from the head of `_design/INDEX.md` and from the roster section of `_smartcity_masters/31_smartcity_dashboards.md`. It carries the three links (live product, design gallery, design canvas), the measured design state, the per-vendor integration reality, the open defects with rows, the findings owed to the `smartcity-dashboards` seat, what the operator owes, and a five-item resume order. **The live URL was verified before it was recorded**: `smartcity-dashboards-52ecsl5mvq-ue.a.run.app/?cityKey=bastrop_tx` returns HTTP 200 serving the v2 shell with the nine-lens nav, read 2026-09-16. **`31_smartcity_dashboards.md` reconciled, three corrections, last_updated 2026-08-10 to 2026-09-16.** (1) **Three of the nine integrations it lists as running at Bastrop do not hold as written.** Verkada is not a running integration: zero occurrences in `src/vendor-live.mjs`, absent from `BASTROP_TX.grantedAdapters`, and no secret in any of three GCP projects; it is vendor onboarding (G-139). GoTo is not "degraded", it is unauthorised, returning `goto_not_authorized` because nobody completed the OAuth consent. FirstDue returns a specific scope 403. Samsara, Spireon and Power BI do return real data. That list feeds collateral thinking, so the split is now stated where the list is. (2) **The roster is not the product.** `ALL_LENS_IDS` is exactly nine; code enforcement, planning, streets, facilities, courts and human resources are in the commercial roster and are NOT lenses, and code enforcement is a Development services TAB. The roster stays as the commercial statement with the built surface named beside it. (3) **Open item 4, the fleet boundary, is SETTLED on the product side** by `fleet-vehicles.mjs` itself: a vehicle is never an asset record, G-24 stays at zero, and the Fleet design draws the boundary positively with a check that refuses asset-inventory vocabulary in an assertive position. Only the collateral lead remains open, and it may not contradict the product rule. **One number corrected before it landed.** The package first said fifty-six boards; counted at source it is **sixteen folders and sixty-two boards** against ten of fifteen surfaces designed. That gap is the argument for the gate: by boards this reads nearly finished, by surfaces a third of the product is undrawn. | Operator is parking SmartCity for several days and asked for the track to be solid and for the links to sit beside the design files as one package. A cold pickup should need one file, not an archaeology pass | nick (park, package); planner (reconciliation, each correction measured at source) |
| A-141 | 2026-09-17 | **OPERATOR APPROVED EVERY SMARTCITY DESIGN, AND RULED THE DIGITALOCEAN CUTOVER AHEAD OF ALL BUILD WORK. G-149 AND G-150 ADDED; G-125 STATUS CORRECTED.** Operator, in session: *"i have looked at all the designs and they are all approved"* and *"I want the dashboards to go to DO now before we do any work."* Eleven designs move to RATIFIED per `_decisions/2026-09-17_design_ratification_all_approved.md`, closing the ratify/amend/kill pass owed since 2026-09-15 that gated the Bastrop approval package. **Three of the eleven are approved and still not buildable, and the record says so rather than folding them in:** `smartcity-finance-filings` (G-138, two citations to external authorities that trace to nothing, one of them load bearing in a worklist that accuses the city own taxpayers of filing at the wrong rate), `plan-review-departments` (G-144, the role-gate amendment is DEFERRED), and `smartcity-applicant-precheck` (the operator deferred the blending ruling in the same session, so no service owns the check and no row can name a repo). **The instrument consequence was measured, not predicted, and the first measurement was wrong in a way worth recording.** Flipping the eleven folder READMEs to RATIFIED moved `design-completion-gate.mjs` not at all, which read as "approval is free." The real mechanism is that `parseIndex` reads status from `_design/INDEX.md` and never opens a folder README, so the edit was invisible to the instrument. The rejected alternative explanation, that blanket approval genuinely adds no findings, was disproved by reading the gate source rather than by re-reading its output. After correcting `INDEX.md`, verified through the gate own parser: **R3 moves 4 to 6, R4 unchanged at 3, verdict UNFINISHED with 9 findings.** Only the two carve-outs lacking a `check.mjs` were added, because nine of the eleven already carry one. **G-125 corrected from OPEN to CLOSED.** It closed 2026-09-14 with three PRs merged at CI success and a `status: closed` close artifact, while its row read OPEN for three days. Its unowned leave-behind, the broken NOAA Atlas 14 parser that silently defaults every county to Bastrop 9.5in, is folded into G-149 rather than left orphaned. **Deploy reality verified at the planner seat rather than inherited from either 2026-09-17 handoff:** `smartcityos.io` returns `Server: cloudflare`, the dashboards `run.app` returns `server: Google Frontend`, `smartcity-dashboards` has one workflow with zero deploy references, and `gcloud builds triggers list` is zero across three projects. Two facts neither handoff carried: the GCP dashboards original is in its own project with traffic pinned by revision name, and `dolphin-app` builds from a branch diverged from main in both directions. Both are carded in OPS-25 D-12. | Blanket approval unblocks the build program, and the operator sequencing ruling makes the dashboards cutover the gate on all of it. Carding two rows rather than ten is deliberate: the flood study and the reasoner path were already ratified, source-checked and scoped, and the other eight approved designs need a carding pass that has not been done | nick (approval, sequencing); planner (carding, corrections, each measured at source) |
| A-142 | 2026-09-17 | **CARDING PASS ON THE APPROVED SET: G-151 THROUGH G-156 ADDED, EIGHT DESIGNS ACROSS SIX ROWS. THREE ROW STATUSES CORRECTED. ONE OPERATOR RULING OWED AND ONE SCOPE CALL OWED.** Follows A-141, which approved every design and left eight of them with no route to being built. The eight buildable designs are carded **grouped by dependency shape rather than one row per design**, because what determines when a lens ships is what blocks it, not which folder it lives in: G-151 Parks (nothing blocks it but the deploy path), G-152 Public works and Fire and EMS (same, paired), G-153 Fleet and Police (blocked on an operator ruling that does not exist), G-154 the Development services delta, G-155 Smart Files (NOT gated on D-12), G-156 Finance lens (blocked on a scope call). Each row names its designs and each design's own `check.mjs` is an acceptance item, so grouping costs no traceability. **A near-miss worth recording, because the correct answer was the opposite of the obvious one.** This pass was about to card four vendor blockers on the lens rows: GoTo's uncompleted OAuth consent, FirstDue's 403 scope gap, Verkada having no credential anywhere, and Parks having no vendor at all. Checked at source instead of assumed, and **none of them is a build blocker.** Every one of those designs DRAWS ITS OWN BLOCKED STATE as design content: Fire and EMS names the obstacle as a vendor entitlement rather than a bug and says nobody reading the screen can fix it, Public works distinguishes an entitlement from a consent from a missing source as three different asks, and Parks is drawn as a surface that does not exist rather than an empty one. The designs were built to be honest about absence, so they ship WITHOUT the vendor and the vendor work is separate (G-139 for Verkada). Carding those four would have blocked five lenses on work that was never their precondition. **G-153 is genuinely blocked, and on something nobody has ruled.** Fleet and Police declare byte-identical `OPERATOR_REF_FORMAT` and `OPERATOR_BASIS` in two modules with no import between them, and nothing says whether `OPR-01` is the same person in both. Searched 2026-09-17: **no decision record exists**; the only mention in canon is OPS-17 recording it as owed, and A-139 recommends namespacing by domain. Police is the lens where getting this wrong means naming a person. **Three live-mapper defects verified at source and given a row for the first time,** read at `smartcity-dashboards` `origin/main` `f776b4bf`, all landing on G-153: `assertRecordShape` is called from `fixture-seam.mjs:546` and from tests and from nothing in `vendor-live.mjs`, so the fixture path we control is shape-validated and the live vendor path we do not control is not, which is a dormant mechanism in the `ENFORCEMENT.md` sense; the literal `"Unnamed unit"` is emitted on an empty row by both `vendor-live.mjs:131` (Samsara) and `:186` (Spireon), so identifier collisions cross lenses and a sentinel two vendors share is not an identifier; and `operatorRef` is `required: true` on the Spireon shape and occurs ZERO times in `vendor-live.mjs`, so a required field is silently absent on every live record. **The systemic finding: this plan's status column is not re-graded when a close lands.** Three rows were corrected in two days on identical evidence. G-125 read OPEN with a `status: closed` artifact and three merged PRs (corrected in A-141). G-128 read OPEN with a `closed-partial` artifact sitting in `_inbox/` since 2026-09-14. G-145 read OPEN, with a blocker A-138 had cleared on 2026-09-15, while THREE close artifacts sat in `_inbox/` and A-139 had already declared the work complete by instrument. In all three the amendments were accurate and the status cell was the stale proxy, which is exactly the shape `ENFORCEMENT.md` names: read the authoritative record, never a proxy for it. **Nothing enforces agreement between a close artifact and its row, so a close that lands without re-grading its row is invisible.** A gate comparing `_inbox/*_close.json` `planRows` against row status is worth building and is NOT carded. | A-141 approved every design and left eight with no route to being built. Carding by dependency shape rather than by folder is what makes each row gradeable, and checking the vendor gaps at source rather than inheriting them from the package doc is what kept five lenses from being blocked on work that was never their precondition | nick (approval, sequencing); planner (carding, source checks, three status corrections) |
| A-143 | 2026-09-17 | **THE NEXT DELIVERABLE IS NAMED AND SCOPED: THE FINANCE AND CITY MANAGEMENT PACKAGE. G-157 ADDED, G-156 UNBLOCKED, RBAC RULED TO FULL DEPARTMENT DEPTH.** Operator, 2026-09-17: the Finance dashboard, the hotel occupancy tax, all the current financial tooling, proper RBAC, and the city management board are one deliverable. **Ruling 1, the G-156 scope call is ANSWERED: Finance ships.** It had been open on whether Finance belongs in a Bastrop phase-1 aimed at Development Services staff. G-156 is now blocked on D-12 only. **Ruling 2, RBAC goes to FULL DEPARTMENT DEPTH and design comes first**, so finance data is visible only to finance and the product can answer who looked at a citizen's record. That orders it: G-143 (People and access) is DESIGNED first, then G-134 WorkOS wiring, G-127 department roles and G-144 the plan-review role gate. **G-143 is one of the three surfaces the completion gate reports as UNCOVERED**, so proper RBAC is design work before it is build work, which the phrase does not suggest and the gate does. **Ruling 3, the city management board is the v1 dashboard Bastrop works from today, all departments combined** — the operator's own words. Carded as G-157, and it is a capability-preservation row rather than a feature row, because v1 is being retired rather than run alongside (G-145) and the city manager's daily surface disappears with it. The v2 counterpart already ships: `city-manager` is the first entry in `LEAD_LENS_IDS`, `surface_coverage.json` maps it to `smartcity-overview-lens`, and G-120 closed 2026-09-14 with an across-departments roll-up. **The question is parity and nobody has measured it.** **Three things gate this deliverable and every one is operator-owed, which is the finding.** (1) **A capture of the v1 combined dashboard.** No lane can produce it: G-133 established v1 sits behind a logged-in Google OAuth session cookie, a different credential from v2's Hauska product key. The Finance lens only exists because the operator supplied 25 pages of the v1 Finance tab on 2026-09-15; G-157 needs the same for the combined board. (2) **The four Azavar credentials**, which gate G-137. Khalid AlAli answered all four of our questions on 2026-09-15 and **those answers are not filed anywhere in this repository**, searched 2026-09-17; the reply that requests the credentials was drafted 2026-09-17 and never sent. (3) **WorkOS org, client id, API key, MFA and the `bastrop_tx` key**, which gate G-134 and therefore the whole RBAC leg. **Two defects in the HOT build spec, found while scoping this and not previously carded.** `_inbox/2026-09-15_localgov_filings_integration_scope.md` is the spec G-137 builds from and it still reads as though all four Azavar questions are open, so it still forbids computing any rate, average or ratio and still says every label must read Localgov filings rather than hotel occupancy tax. More seriously, **its acceptance test is wrong in a way that would read as data corruption**: it declares the store done when a re-run changes no historical total, but `AmountPaid` legitimately moves as payments settle or refund, so the FIRST REFUND would fail the test and be investigated as corruption. The test must separate taxpayer-reported figures, which must never change under re-fetch, from payment figures, which may. Fix the card before anyone builds from it. **Sequencing.** G-138 is blocked on nothing by its own row (the badge path needs no credential and no vendor reply) and is the one component that can start today. G-156 needs only D-12. G-143 needs only G-132, which is closed-partial. G-137, G-134 and G-157 each wait on an operator-owed item above. | The operator named a deliverable in one sentence and it spans five existing rows plus one new one; carding it makes the three operator-owed gates visible instead of each surfacing serially when a lane stalls on it. Two of the three are a single email and a credential set, which is why they are named here rather than left in a package doc | nick (three rulings); planner (carding, source checks, the two spec defects) |
| A-144 | 2026-09-18 | **D-12's CUTOVER VERIFIED, AND THE BUILD QUEUE IS LINED UP: THE SIX DASHBOARDS ROWS SERIALIZE THROUGH ONE LANE.** The operator completed both DNS acts. Verified at 2026-09-18T00:45Z rather than taken on report: `app.smartcityos.io` resolves identically on the local resolver, 8.8.8.8 and 1.1.1.1, and GATE B's own behavioural marker discriminates origin where headers cannot — `GET /auth/sign-in` returns **500 `signin_not_configured`** on `app.smartcityos.io` and on `dolphin-app` directly, and **404 `not found`** on the GCP original. The new hostname is the DO app running `main`. Deploy-on-push stayed DISABLED (D-12 step 4, `deploy_on_push` absent from `services[0].github`), so a merge to `main` remains inert and every ship below is a deliberate act with `source_commit_hash` read back per OPS-25 rule 13. **But no staff have moved, and none will on their own.** `app.smartcityos.io` is a NEW hostname; unlike `smartcityos.io`, where DNS moved an existing name, nothing points anyone at it. `smartcityos.io` links to neither dashboards URL, and staff reach the surface by a bookmark to the `run.app` address. Since 00:00Z the GCP original took exactly 2 requests, both automated, both `/auth/sign-in` — the planner's own probes, excluded — so real traffic is zero, and that is not evidence of a migration because it was 7:45pm in Bastrop. **The bake cannot start until Bastrop is told the new URL.** Rule 3 keeps the GCP original unmodified through the bake, which rules out a redirect for now; a redirect belongs to the decommission step after the bake reads clean. **The serialization ruling, and the fact it rests on.** Every lens renders in TWO MONOLITHIC FILES: `web/app.js` (131,149 bytes) and `web/index.html` (145,758 bytes), read at `smartcity-dashboards` `origin/main`. Fleet alone appears 18 and 32 times across them. Domain modules are separate per lens and would parallelize; the UI does not. Six lanes in parallel is a six-way merge conflict in two files, which is the G-128 collision at twice the scale, and `ENFORCEMENT.md` already holds that a product repo has one owning seat. **So the dashboards rows run ONE AT A TIME through one govtech lane, one PR per row, each verified by the planner before the next fires.** Order, recommended and reorderable: G-156 Finance (it leads the deliverable the operator named, and it is the only piece of that deliverable buildable today), G-149 flood study (Sylvia's named request, ratified longest), G-153 Fleet and Police (live-data defects and the namespace change), G-154 the Development services delta (after G-149 because both touch the Development services area), G-152 Public works and Fire and EMS, G-151 Parks. G-157 and G-134 join the same queue when their operator inputs land, since both touch the same surface. **Parallel, because they build in other repos:** G-155 in `smart-files` (pinned by name to `smart-files-00013-ruq`, `latestRevision=false`, zero triggers, so canary-then-shift) and a G-150 close-out in `plan-review`. **Planner-owned, in this repository:** G-138 (its instrument is `_design/smartcity-finance-filings/check.mjs`, so it is design work here, not a lane's), G-143 People and access, the HOT scope card's acceptance-test fix, and the Fleet and Police design regeneration after G-153. **One consideration on G-156, stated rather than decided:** the operator bundled RBAC with Finance, and G-156 can be built and merged before RBAC exists without changing exposure (Finance is already a lead lens every signed-in user sees, and v1 shows its Finance tab to everyone). Whether it SHIPS before RBAC is the operator's call at ship time, which deploy-on-push being off makes possible to defer. **Checked and not a risk:** the govtech worktree carries an unpushed-looking `feat/g134-workos-wiring` branch, but its content is byte-identical to `main` on every file it touches; it is the pre-squash original of PR #65. | The operator asked for the next work lined up the moment the cutover landed. The only non-obvious call in it is the serialization, and it rests on a file-layout fact that makes parallel dashboards lanes a merge-conflict generator rather than a speedup. The staff-move gap is the other non-obvious item: a DNS cutover to a new hostname moves no traffic by itself | nick (DNS acts, cutover); planner (verification, sequencing) |
| A-145 | 2026-09-18 | **G-143 DESIGNED, FIRST OF THE RBAC LEG, AND THE SURFACE SAYS WHAT IS ENFORCED RATHER THAN WHAT WAS RULED. G-158 ADDED; TWO FINDINGS FOLDED INTO G-134; ONE RULING CONFLICT AND ONE UNOWNED PROCESS NAMED.** The operator ruled on 2026-09-17 that RBAC goes to full department depth, design first. The rulings describe the decided model, and the product enforces less than that. On an access review, showing the decided model as if it were enforced is a false assurance to a government customer. So every board shows what the code does, read from source into a snapshot and checked against it. **Finding 1, a role limits nothing yet.** The only role checks in `server.mjs` are the four inside the People and access route. No lens or data route checks a role, so every account can open every lens, including code enforcement cases and work orders carrying residents' names and phone numbers. The city manager's board leads with that. This is G-127, already carded. **Finding 2, nothing records who opened a record.** No write path exists across 45 source files, so the trust surface is drawn NOT RECORDED rather than as an empty table. Carded as **G-158**. **Finding 3, the city manager cannot see the people with the widest access to her data.** Her list is scoped to her own city, and SmartCity administrators read every city and belong to none, so they never appear on it. **Finding 4, a never-provisioned person is told their account was disabled.** `isStaffAccountRevoked` returns true for both states on purpose, and the identity module refuses both with "this account has been disabled.", which is false for someone who was never set up. **Findings 3 and 4 fold into G-134**, because both are changes to the staff-identity and People and access code G-134 is already building, and both must land before real Bastrop staff sign in. **Finding 5, offboarding is not instant everywhere.** A person is refused on the next request in the dashboards. In Plan review and Smart Files, which keep no record of ended accounts, access lasts up to the 15-minute token cap. At the sign-in provider it is not ended at all, because that call is not built. **Two things only the operator can settle.** (a) **The 2026-09-14 ruling contradicts itself on who may read this page.** Ruling 1 says read-only for the city manager, administered by SmartCity. Ruling 2's gating list says "admin only". The code implements Ruling 1, and the directory's own header repeats it. The design follows the code, and the decision record should be amended. (b) **Offboarding has no owner and no turnaround.** The same ruling names this as the real cost of choosing SmartCity-provisioned accounts over single sign-on, and nothing since has assigned it. **On trusting the instrument.** The snapshot program was wrong five times before it was trusted, and each wrong version returned a plausible answer. A refusal pattern missed three multi-line calls, reading 9 codes where the product has 13. Three regexes lost their backslashes through shell strings: one could match nothing, and one became an empty-branch alternation that matches everything. A read-only test was negative-only. The check's attribute parser missed valueless attributes, so one self-test passed for the wrong reason until its clean-direction twin failed. Each now carries a control, and the two refusal extractions cross-check each other. | The operator ruled RBAC design-first, and this is the design. Its value is in what the source says that the rulings do not: four of its five findings would have shipped as a false assurance on the one screen a government customer uses to check who can reach its residents' data | nick (RBAC design-first ruling); planner (design, source reading, instrument) |
| A-146 | 2026-09-18 | **BASTROP IS THE PROVING PACK. The first-wave builds proved on `template-city`, and the lanes were following program law when they did.** Operator, on the first-wave returns: *"it looks like this stuff was built in the template city not bastrop. the designs were meant to be implemented in bastrop and harness the data that much of which is wired into the bastrop dashboard."* Measured per lane, from their own closes: G-150 was verified on `bastrop_tx`; G-155's QA ran on `template-city` demo rows; G-156's route defaults `cityKey` to `template-city` and its build never reached the test app. **The root cause was the program preamble, not the lanes.** `_catalog/program_preambles/OPS-17.md`, compiled into every OPS-17 dispatch, still carried three August rulings: "template Dashboards UI first, then one adapter/source onto `template-city`. Live Bastrop is an island, not the next card"; "Live Bastrop no-touch"; and "Live Bastrop stays `smartcity-os` until a named island replacement". They were right when no Bastrop pack existed. They were never retired after G-116 created `bastrop_tx`, and the preamble says it "wins on scope", so a mission naming `bastrop_tx` would have lost to it. The planner compounded it: the G-156 mission never named `bastrop_tx` and pointed the lane at fixture traceability. **Ruled and fixed:** `_decisions/2026-09-18_bastrop_is_the_proving_pack.md`. The preamble now LEADS with it, and the three stale directives are retired in place, verified by compiling a real dispatch and reading it. **What "harness the data" means, measured.** `bastrop_tx` grants seven live feeds today, so Fleet, Police, Public works and Fire and EMS can read live Bastrop data now. Four MyGov routes (inspections, code violations, licenses, work orders) are served by production v1 at `8bea7fa` and not granted; they go into G-154. OpenGov, budgeting and planning, and v1 permit revenue are wired in v1 with keys in production in both projects, and reach v2 by no route; they become **G-159**. **The G-156 "honest full shape" ruling STANDS until G-159 lands** (operator, 2026-09-18). Its premise, "no OpenGov connection", came from five v2 registers and is false for Bastrop, but the lens derives every state from grants, so it becomes measured on its own once the bridge exists. **Also carded:** **G-160**, the console-versus-API deploy check the G-150 close-out found missing. **Evidence brought onto main:** the D-5, D-9, D-10 and D-11 closes had been on no file in `main`, although OPS-25 marks those rows CLOSED and cites their commits; merged (not cherry-picked, so the cited hashes stay reachable) with the G-155 and G-156 closes as `0b92f8d6`, in an isolated worktree because the one conflict was the live claims registry. The G-150 close-out landed as `9b23f474`. Both lanes' edits to their own design checks were reviewed before merging; both STRENGTHEN their checks. **v1 drift, recorded for OPS-25 open item 5:** `smartcity-os` `main` is one commit past production and that commit touches only the scraper, so the v1 API can be built from `main`. | The operator saw the builds land on the wrong city. The instructions every lane received told them to, and nothing had retired those instructions when the program moved to Bastrop. Fixing the preamble is what makes the correction travel, because the planner's memory does not reach a lane | nick (ruling, and the G-156 ruling standing); planner (root cause, preamble, carding) |
| A-147 | 2026-09-18 | **CORRECTION: G-154 needs no grants. MyGov inspections, code violations, licenses and work orders are ALREADY LIVE on `bastrop_tx`.** A-146 said those four were served by production v1 and not granted, and put four grants into G-154's scope. That was wrong. The planner read `bastrop_tx`'s list of grant constants, saw `PLATFORM_MYGOV_PERMITS_GRANT`, and inferred permits only. The grant's own definition says `kind: "mygov"`, the adapters file describes MyGov as "kind-level grouping across five domains", and the server registers all four as live resources of kind `mygov` and gates them on the kind (`platformGrantForKind(pack, entry.kind)`). **The planner also had the contradicting evidence in hand and did not connect it:** the log query it ran for D-13 showed the dashboards fetching all four MyGov routes from GCP v1 on 2026-09-16, which happens only when they are granted. Code and logs agree. Corrected in G-154's row, the decision record, the program preamble (which travels to every lane, so it must not understate what is live) and the package page. Development services joins the lenses that read live Bastrop data today. **What G-154 is, re-stated:** making a lens that already shows live records match the ratified design. The live mapper already refuses the vendor's free-text description, where citizens' names live; it does pass an officer's name and a repeat-offender flag beside each address, and the lane measures whether either lands on a surface the design corrected. Caught before G-154 was dispatched. | A claim that would have sent a lane to add grants that already exist, and hidden that the lens is already live on real records | planner (correction) |
| A-148 | 2026-09-18 | **G-159 AND G-154 LANDED, READ AT SOURCE: THE NEXT THREE DISPATCHES, AND ONE FINDING THAT CHANGES A PENDING OPERATOR CALL.** Read by the planner at `smartcity-dashboards` `main` `7267c3f` and `smartcity-os` `main` `a400f7be`. G-159 is CLOSED-PARTIAL: every part it owns passes, and Bastrop's budget goes MEASURED only after D-14, D-13 and the `bastrop_tx` grant. G-154's product work is merged, and its live proof waits on a credential. **The credential gap is structural, not G-154's.** G-135 requested a verification-scoped `bastrop_tx` key from the substrate seat on 2026-09-15. It was never minted, and G-135's own artifacts sat untracked for three days, so nothing surfaced it. Every row that must prove on `bastrop_tx` on a deployed app hits the same 401: G-154, D-13's end-to-end leg, the G-159 grant and every M3 lens. **Dispatched:** `g135-mint` to the substrate seat (G-135); `d14-d13-v1-reach` (OPS-25 D-14 then D-13, one lane, because the proof is one path); `g161-never-default-a-city` (G-161). Their files do not overlap: the D-13 leg owns the five data-source files, and G-161 owns `src/server.mjs`, the module-level defaults and the client call sites. **Carded, not dispatched:** G-162, behind D-14. **The finding.** `dolphin-app`, which serves `app.smartcityos.io`, runs `3d3ec62` (D-12's read-back, confirmed from outside on 2026-09-18: its finance lens answers "unknown lens"). At `3d3ec62` the work-order Subject cell renders the vendor's free-text title, where residents write names and phone numbers. That was checked against both controls: `fd8562c` has it and `7dda6db` does not. So holding `main` back from production keeps the worse surface live. It does not keep real records away from staff, because five lenses on production already read them. **The planner recommends shipping `main` to `dolphin-app` before the operator tells staff to use it.** The ship is the D-13 leg's final step and runs only if an amendment records the operator's ruling. **The deploy precondition this creates:** once D-13 merges, `main` needs the configured platform base, so a `dolphin-app` deploy without it fails closed on every feed. That is deliberate, and it fails loud rather than silent. | Two lanes landed. Reading them at source found the next three dispatches, a credential gap that blocks every Bastrop proof, and a production surface that is worse than `main` | planner |
| A-149 | 2026-09-18 | **OPERATOR RULED: SHIP `main` TO `dolphin-app`, AND THE `d14-d13-v1-reach` LANE PERFORMS THE SHIP AS ITS LAST STEP WITH THE CONFIGURED PLATFORM BASE IN THE SAME DEPLOY.** Operator, 2026-09-18, in session, on the planner's re-verification of the three in-flight lanes: *"Yes — record the OPS-17 amendment; the d14-d13 lane ships as its last step, configuring the platform base in the same deploy."* This is the amendment A-148 said the ship "runs only if" it exists, and it satisfies the `d14-d13-v1-reach` dispatch's own condition (an OPS-17 amendment dated 2026-09-18 or later recording that `main` ships to `dolphin-app`). **The precondition the lane cannot miss.** By the time the lane reaches its last step D-13 has merged, so `main` reads ONE configured platform base with no silent default to a host being retired, and a `dolphin-app` deploy without it fails closed on every feed. The lane adds the base to `dolphin-app`'s spec and ships `main` in ONE deploy, then reads `services[0].source_commit_hash` back and compares it to the merge commit (OPS-25 rule 13, which exists because a DigitalOcean deploy once reported success while running the wrong commit). **If the lane has already finished when it reads this,** the ship is a planner-owned deploy with the identical precondition and read-back; it is never skipped silently and never performed by a finished lane. **What the ship carries, and why exposure does not change.** `dolphin-app` moves off `3d3ec62` to `main` (`7267c3f` as read 2026-09-18T14:40Z), which carries G-154 (the work-order Subject cell no longer renders the vendor's free-text title, where residents write names and phone numbers), G-159's dashboards finance route and G-156's finance lens. A-144 left "whether Finance ships before RBAC exists" to the operator at ship time; this ruling ships it, and exposure is unchanged because Finance is already a lead lens every signed-in user sees and v1 shows its Finance tab to everyone. **The state the ruling was made against, verified at source rather than taken on report at 2026-09-18T14:40Z** (`_inbox/2026-09-18_planner_three_lane_verify.json`): all three dispatched lanes were NOT landed — the mint's secret absent from `hauska-prod-497015`, G-159's four routes still absent from `smartcityos.io` while the existing-route control still answered 401 JSON, dashboards `origin/main` still carrying six `cityKey` fallbacks to the literal `template-city` and the hardcoded GCP host in five files, and both `origin/main` refs unchanged from A-148's read. The operator confirmed the hand-carry in the same session: *"i have already sent all three"*. | The ship call A-148 recommended and left to the operator, recorded before the `d14-d13` lane can reach its last step, which is what makes that step runnable instead of silently skipped | nick (ruling, hand-carry); planner (re-verification, amendment) |
| A-151 | 2026-09-18 | **THE D-13 SHIP RAN AND ITS SURFACE IS GRADED; ONE NEW ROW (G-163) AND TWO INSTRUMENT DEFECTS.** A-149's ship was performed by the planner at 17:29Z as that amendment's own named fallback, because the `d14-d13-v1-reach` lane holds no claim and has filed no close in this worktree. `dolphin-app` deployment `e98f127c-aa0b-4550-9908-1044c616641d` is ACTIVE on `smartcity-dashboards` `origin/main` `7487d7c0` read back byte for byte, with `SMARTCITY_V1_PLATFORM_BASE` set to `https://walrus-app-kzog6.ondigitalocean.app` in the same deploy. The A-148 finding this closes is the one about holding `main` back: production served `3d3ec62`, where the work-order Subject cell still rendered the vendor's free-text title, so the worse surface was the live one. It is no longer live. **It is LANDED and not CLOSED, and the gap is one clause:** OPS-25 D-13 scope 5, a bake window in which a lens is actually opened and GCP `smartcity-api` logs zero platform requests. Nothing was decommissioned. **Graded on the surface, for `bastrop_tx`:** eight of eleven registered domains carry records with every record-less domain stating a basis; the finance route no longer answers `unknown lens`; eight platform routes the dashboards already read return byte-identical payloads on the new host and the GCP host production used before; three more exist only on the new host. **The PII clause, two derivations:** the multiset of the served `(recordId, subject)` pairs is IDENTICAL to the vendor's `(workOrderNumber, type)`, zero served subjects are a vendor free-text title, zero carry a phone-number shape, and no served row carries a `title` field. **G-163 is the new row.** `firstdue/apparatus` and `goto/call-summary` answer an opaque Cloudflare `504` on `walrus-app` where GCP answered a structured `503` naming the vendor permission to request. Both feeds were already `unavailable`, so nothing broke; what degraded is the stated reason. It is placed inside the handler rather than at the auth path by violation: a mutated bearer still returns the gate's `401` while the canonical one returns `504`. **Two instrument defects, recorded because each produced a confident wrong answer.** `PUT /v2/apps/{id}` returns an `app` whose `active_deployment` is still the PREVIOUS deployment, so the first run polled the superseded one and reported a failed ship while the real one was building; the instrument now finds the new deployment by set difference. And the first PII clause joined rows on `recordId`, which is NOT unique (181 rows over 52 work-order numbers, because one work order carries several line items), so a `Map` collapsed duplicates and reported 88 phantom mismatches; the check is now the multiset comparison above. **A per-row join on a key whose uniqueness was never measured is not a check**, and the loose form of the same clause (does the subject appear anywhere in the vendor's global type set) scored 181 of 181 and was nearly reported. Evidence `_inbox/2026-09-18_dolphin_ship_and_acceptance.md`; instruments `scripts/govtech/dolphin-ship.mjs` and `scripts/govtech/dolphin-ship-acceptance.mjs`. | The ship A-148 recommended and A-149 authorised, performed and then graded on the deployed surface rather than on the merge; plus the one row the grading found | nick (the A-149 ruling authorising the ship); planner (the ship, the acceptance, the instruments, G-163) |
| A-150 | 2026-09-18 | **FOUR ROWS MOVED IN ONE HOUR, READ AT SOURCE: D-14, D-13 AND G-161 LANDED, G-153 IS UNBLOCKED, AND THE SHIP IS A TWO-PART CHANGE.** Verified by the planner at 2026-09-18T16:26Z, evidence `_inbox/2026-09-18_planner_four_row_regrade.md`, and NOT taken on report: neither lane has filed a close, CP1 or CP2 in this worktree. **D-14 LANDED.** `walrus-app` deployment `e243db0a` is ACTIVE, deployed 15:41:52Z `cause: manual`, and the service now builds from branch `main` where it built from the side branch `d9-api-8bea7fa`. `services[0].source_commit_hash` reads `1f0262f15f8d1898a6c32826ec2e45407518e29e`, which IS `smartcity-os` `origin/main` HEAD read independently by `git rev-parse`. On `smartcityos.io`, `/api/opengov/budgets`, `/api/opengov/chart-of-accounts` and `/api/finance/permit-revenue/summary` all answer `401 platform_internal_required` while the fake-path control `xyz-nonexistent` answers the 200 SPA shell: a real route answers and a nonexistent route falls through, which are two independent derivations rather than one. **D-13 LANDED on the dashboards side.** `smartcity-dashboards` `origin/main` `7487d7c0` carries `src/platform-base.mjs`: one configured base (`SMARTCITY_V1_PLATFORM_BASE`) decides where every feed goes, the per-feed `DEFAULT_PLATFORM_BASE` and `MYGOV_PLATFORM_BASE` defaults are gone, and an unset base REFUSES carrying a named basis. The old GCP host `52ecsl5mvq` appears nowhere in `src/` or `web/`, and `d12-main-uat` sets the base to `https://walrus-app-kzog6.ondigitalocean.app`. **G-161 LANDED.** `src/server.mjs` at `7487d7c0` carries six `G-161. Was \|\| "template-city"` sites plus the module-level and searchParam defaults. Live on `d12-main-uat`, whose `source_commit_hash` reads back `7487d7c0`, all four probed routes answer `400 city_key_required` keyless where the 15:09Z read answered 200 carrying the demo city, while `template-city` named explicitly still answers 200, `no-such-city` 404 and `bastrop_tx` 401. **That lane's close is still owed**, so its row moved on the deployed record and its own acceptance items and leave-behind do not move with it. **THE SHIP IS TWO PARTS, ONE OF WHICH IS A VARIABLE.** `dolphin-app` serves `3d3ec62` from a deployment ACTIVE since 00:27:55Z, and its configured env carries **no `SMARTCITY_V1_PLATFORM_BASE`**. Because an unset base is now a refusal rather than a default, shipping `main` without setting that variable in the same deploy would move every v1 platform feed from serving to refusing, so A-149's ONE deploy must carry two changes. **G-153 IS UNBLOCKED:** both of its blockers are landed in `7487d7c0`, and it also inherits G-135's second parcel. Compiling it is the next planner act. **And one thing a later reader must not "fix":** `template-city` still answers 200 to a caller who NAMES it, which is correct, and the refusal is scoped to the unnamed case. | Three of the four rows read NOT LANDED in the roadmap an hour earlier, and the fourth was graded on prose. Four rows can move inside one planner read, so a status cell that is not re-graded from the deployed record becomes a proxy for its own subject | operator (both deploys, and the A-149 ship ruling); planner (the reads, the artifact, this amendment) |
| A-152 | 2026-09-18 | **G-146 RECONCILED AGAINST ITS OWN INSTRUMENT: THE ROW BODY WAS FROZEN AT A-137 WHILE THREE LATER AMENDMENTS RECORDED DIFFERENT NUMBERS, TWO OF ITS THREE NAMED LOOSE ENDS ARE CLOSED, THE THIRD HAS MOVED — AND THE RULING IS THAT G-146 DOES NOT CLOSE WHEN G-148's FOUR INSTRUMENTS LAND.** The operator asked what the planner seat could usefully do while four lanes ran; this is the one item that belongs to the plan of record itself and collides with no lane. Measured, not re-quoted, by `_inbox/2026-09-18_planner_g146_recon.mjs` (self-testing in both directions, so a zero count could not pass as a measurement) at `doc_repo` `3243a171` and nav `smartcity-dashboards` `7487d7c0`. **The staleness mechanism is A-142's systemic finding recurring, and this time it is the row BODY rather than the status cell.** A-137 recorded 11 folders, 48 boards, 5 of 15 surfaces designed, 8 uncovered. A-139 recorded 10 designed, 3 uncovered, 16 folders. A-140 recorded 16 folders and 62 boards against 10 of 15 designed. A-143 still reported three uncovered surfaces. **The row body kept quoting A-137 throughout, so the plan row and its own amendment history disagreed for three days while both were committed and each was internally accurate.** Re-measured today: **20 design folders, 72 boards declared across 17 surfaces, 13 of 15 nav surfaces designed, 2 excluded by ruling, 0 uncovered, 14 of 20 folders carrying a `check.mjs` and 7 a `violate.mjs`.** Design is 13 of 15 by surface, not "a third": the number the row uses to justify its place at the front of the queue inverts, because design is nearly finished and what remains at the front is the instruments. **The all-canvas omission is FIXED at the two instances the row named and has MOVED to three others.** `smart-files` (5 boards) and `plan-review-departments` (5 boards) are now declared, and the canvas reads 72 boards across 17 surfaces. But `smartcity-citizen-lens`, `smartcity-people-and-access` and `smartcity-records-search` — all drawn 2026-09-18 under G-142, G-143 and G-147 — are declared nowhere on it. The canvas includes DRAFTs deliberately (the applicant precheck is placed first for being one), so this is a stale declaration list rather than a policy, and its sharpest edge is People and access: the operator is being asked to ratify a DRAFT they cannot reach on the combined canvas. **The `_design/INDEX.md` disagreement is CLOSED**, because the index now records Plan Review as SUPERSEDED in the reasoner's favour (A-138 ruling 2), so the two agree and that item is not work. **The vocabulary loose end is NOT closed and is UNMEASURED from this seat:** `_design/plan-review/gen.mjs` defines its determination vocabulary locally with the comment "lifted from web/styles.css", the design-thread handoff still carries the build-time translation as "Owed", and the review-artefact build script's home is not in `doc_repo`, so the write path cannot be read from here and this amendment does not declare it either way. **The ruling, which contradicts this amendment's own first framing and is the reason it was worth doing:** G-146 does NOT close when G-148's four instruments land. R3 emptying is necessary and not sufficient. Three things survive it — one mechanical (the all-canvas declaration), one unmeasured (the vocabulary), one operator-gated (the three DRAFTs' ratify/amend/kill) — so G-146 is a rollup whose close is a planner act only after those resolve. **Recommended and NOT done, because adding a row is operator-ruled:** card the all-canvas declaration as its own row with a mechanical check, since "declared nowhere on the ALL DESIGNS ON ONE CANVAS canvas" is exactly the shape a file listing cannot see, which is why the original omission stayed invisible for three days. | The row that is the denominator for every design build was itself held together by prose while the instrument beside it moved three times. Three amendments recorded the new numbers and none of them propagated into the row, which is the same defect as a close that lands without re-grading its row: the amendment was right and the row was the stale proxy | operator (the ruling on carding the all-canvas item, and the three DRAFT dispositions); planner (the regrade, the instrument, this amendment) |
| A-153 | 2026-09-18 | **THE `g160` LANE STOPPED ON "ALREADY DONE" AND THE ARTIFACTS CONTRADICT IT: THE CHECK IS BUILT AND PROVEN ON TWO UNMERGED BRANCHES, NOTHING TRIGGERS IT, AND NO PRODUCT RECORDS ITS COMMIT. PARCEL 2 DISPATCHED.** The operator reported the lane had stopped, believing the work already done; the planner read both branches and both repos at source rather than accepting either the lane's verdict or the report. **What is real, and it is better than "already done" suggests.** Two single commits, each branched from its repo's current `origin/main`: `plan-review` `g160-served-commit-parity` `b4b44b9` on `99c156b` (7 files, 959 insertions) and `smart-files` `g160-served-commit-parity` `a238072` on `6d71bf3` (8 files, 1156 insertions). Each adds `scripts/served-commit-parity.mjs` (~500 lines), a `src/served-commit.mjs` that resolves the served commit and never substitutes a stand-in, a `GET /version` route deliberately not `/healthz` (GFE intercepts that exact path on `*.run.app`), and tests. The planner re-ran both suites: **31/31 and 36/36 pass**, and both carry the falsifier firing in both directions, *"the check FAILS against the recorded 2026-09-17 state"* and *"...and PASSES once the console catches up"*. The first-run measurement the row demanded exists and answers honestly: **Smart Files is NOT split, and parity is UNMEASURED by record**, with the supporting content match (47/47 blobs against `1caeb98`'s tree) explicitly labelled a reconstruction rather than a platform record. **Three gaps, each measured.** (1) **Nothing triggers it.** No workflow in either repo references the parity check; it is a manual `npm run parity`. The row's own three-question gate required a trigger on every deploy of either side plus a schedule, and named the bypass, "a deploy made without running it"; today that bypass is the only path. `smart-files` has no `.github/workflows` at all, so its parity test runs in no workflow, and `plan-review`'s CI collects its test but the workflow's own comment records that branch protection is absent, so a red result blocks nothing. (2) **The control is starved, not merely untriggered.** `SERVED_COMMIT` is read and never set by any deploy path, so a live run by the planner returned **REFUSED, exit 2**: *"revision plan-review-00029-gom records no commit"*. That is correct fail-closed behaviour and it means the check cannot PASS on any product today; the console-side refusal in the same run was a workstation TLS artifact and the check named the remedy rather than defaulting, which is the behaviour worth keeping. (3) **Neither commit is on `origin/main`.** **PARCEL 2 DISPATCHED**, mission `_catalog/dispatch_missions/mission_g160_served_commit_parity.md`, compiled to `_dispatches/2026-09-18_g160-served-commit-parity_dispatch.md`, carrying three steps in a fixed order: make the deploy record its commit, wire the trigger on both products, then prove PASS / FAIL / REFUSE all three observed. **The ordering constraint is the whole risk in that parcel and it is stated as such: `SERVED_COMMIT` and the trigger land in ONE change.** Arming a check that must refuse, before the deploy records a commit, fails every deploy of both products, and an over-broad control is worse than a narrow one because it teaches the fleet to reach for the bypass flag. **Verdict: built and proven, not armed, not merged, and unable to pass.** One limit on this verification is recorded rather than smoothed: the violation proof rests on a recorded fixture whose internal arithmetic the tests check, which is internal consistency rather than a second independent derivation, and the historical 2h27m split itself comes from the G-150 close and cannot be re-derived now. | A lane stopped on a verdict that its own artifacts refute, and it had filed no claim and no close, so nothing in the program would have surfaced the gap. The check it built is correct and fail-closed, which is exactly why "correct and doing nothing" is the state that has to be caught by reading rather than by measuring | operator (re-dispatch decision); planner (the source read, both suites re-run, the live refusal, this amendment) |
| A-154 | 2026-09-18 | **THE NEXT WAVE: G-152 AND G-163 COMPILED, EACH NAMING THE LANE IT QUEUES BEHIND, AND G-149 DELIBERATELY NOT IN IT.** Chosen from measured state at 20:2xZ, not from the row order. Four lanes were in flight: `g153-fleet-police-lens` and `g148-design-instruments` (both holding claims, seats `cente-vsc-g153` and `cente-vsc-g148`), `g162-v1-finance-honesty` (running UNCLAIMED), and `g160-served-commit-parity` parcel 2 (hand-carried). **Two serializations apply, and they are different rules.** Every dashboards lens renders into two monolithic files, `web/app.js` and `web/index.html`, so dashboards rows run ONE AT A TIME through one lane (A-144); and `ENFORCEMENT.md` holds that a product repo has one owning seat. **G-152 is the next dashboards lane, ahead of G-149, and the reason is a dependency rather than a preference.** G-149 INHERITS its instrument: its acceptance requires `node _design/smartcity-flood-study/check.mjs` to exit non-zero on a planted violation, and that file is `g148`'s deliverable. `g148` still owns that folder and is mid-delivery. G-152's two designs, `smartcity-public-works-lens` and `smartcity-fire-ems-lens`, are touched by no running lane, and the planner re-ran both instruments today: **both exit 0 with non-zero matched-input counts** (`pairs=14, badges=77, cells=69, numbers=188, cipIds=10, queueRefs=11, statuses=2, verbatim=8 of 8` and `pairs=4, badges=37, chips=12, cells=72, numbers=116, faIds=12, stnRefs=15, verbatim=5 of 5`). **The `smartcity-os` collision was measured rather than assumed, and it is smaller than the rule implies.** `g163` works in `server/routes/firstdue.ts` and `server/routes/goto.ts`; `g162` works in `server/routes/finance.ts`, `server/routes/opengov-bnp.ts` and `server/services/opengov-bnp.ts`. The file sets do not overlap. `server/routes/ai-assistant.ts` matches both greps, so the mission names it and instructs the lane to read it before touching it and prefer not to. The serialization is therefore discipline rather than a hard merge necessity, and both the mission and this amendment say so rather than overstating it. **Compiled, verified canonical:** `_dispatches/2026-09-18_g152-public-works-fire-ems_dispatch.md` (25,337 bytes) and `_dispatches/2026-09-18_g163-opaque-platform-routes_dispatch.md` (25,543 bytes), each carrying CANON-PREAMBLE v49001500, AGENT-CONTRACT v378cd643, DEV-PROCESS vbb19bd34 and FLEET-MEMORY v2a98086b, read back against the source files, so neither trips the canon gate. Both carry FAN-DEPTH 0. **Both missions state the same two anti-collision instructions:** do not begin your first write until the sibling lane's close is filed, and if you find an uncommitted tree that is not yours, STOP and report rather than cleaning, stashing or committing it. **One finding about instrument convention, recorded so a lane does not file it as a defect.** The Public works and Fire and EMS folders carry a `check.mjs` and NO `violate.mjs`. Their two-direction proof is embedded as self-tests inside `check.mjs`, which prints `self-tests: N/N passed, both directions` before it reads a board. A-139 records that Police and Fleet shipped a separate `violate.mjs` and recommends it become the convention. The older shape satisfies the acceptance clause on its own terms, so neither mission tells the lane to retrofit one. **Deliberately not in this wave:** G-151 (parks, queued behind G-152 in the same serialized repo); G-149 (waits on two lanes, see A-155); G-146 (planner rollup, A-152 already ruled it does not close on G-148's instruments); G-157 and G-137 (operator-owed capture and Azavar reply); G-127, G-144 and G-158 (the RBAC leg, each waiting on the one before). | The operator asked for the next wave, and the correct answer is short because the queue is nearly full: two rows are startable and the rest are blocked by a lane, a rule, or an operator act. Choosing G-152 over G-149 on a measured dependency rather than on the row's position is the load-bearing call here, and measuring the `smartcity-os` file sets turned an asserted collision into a stated discipline | operator (hand-carry); planner (the reads, the choice, both missions) |
| A-155 | 2026-09-18 | **THE DESIGN GATE FELL FROM 4 FINDINGS TO 1 WHILE THIS SESSION WAS READING IT, AND R3 TESTS THAT AN INSTRUMENT EXISTS, NEVER THAT IT PASSES. ONE DELIVERED INSTRUMENT FAILS TODAY.** At session start `design-completion-gate.mjs` read 20 design folders with 14 carrying an instrument and four R3 lines: `plan-review-departments`, `smartcity-flood-study`, `smartcity-map-dock`, `smartcity-overview-lens`. Re-run at 2026-09-18T20:19:39.877Z it reads **17 of 20 folders instrumented and ONE R3 line**, because `g148-design-instruments` delivered three of its four while the gate was being re-run. All the lane's new files are UNCOMMITTED in `doc_repo`, which is correct: doc_repo edits are the planner's to commit. `smartcity-overview-lens` is the last line and carries only `dump-source-facts.mjs` and `source-facts.json` today. **The finding, and it is in the gate rather than in a design.** R3 asks whether a folder past DRAFT has a `check.mjs`, and it answers that by testing `hasCheck` (`scripts/govtech/design-completion-gate.mjs:157-163`) — the file's existence. It never runs it and never reads its exit code. **So the last R3 line will clear while a delivered instrument reports a live FAIL, and `exit 0` will mean "instrumented" rather than "clean".** That is exactly the shape A-152 warned about when it ruled G-146 does not close on G-148's instruments, and it now has a concrete instance. **The instance.** `node _design/smartcity-flood-study/check.mjs` exits **1** on the shipped boards with one finding: the design states that naming a depth by return period is unavailable because it "would need a local rainfall atlas nobody has cited yet", while the engine already carries `rainfallSource: 'noaa-atlas14'`, cites NOAA Atlas 14 seven times across the four files, pairs return period to depth in `rainfallCurve`, and renders the default through `returnPeriodYearsForDepthInches` as "100-yr (NOAA Atlas 14)". Its `violate.mjs` catches **30 of 30** planted violations and a copy with that one claim corrected passes with a matched-input count, so the instrument self-tests in both directions and the finding belongs to the design rather than to the instrument. **Why this is not a one-line copy fix.** G-149's own row records the contradicting fact: G-125's unowned leave-behind is that NOAA's Atlas 14 HDSC endpoint changed payload shape, `parsePfdsDepthTable` never matches, and every no-parameter study in every county silently uses Bastrop's 9.5in regional default regardless of location. So the engine cites NOAA Atlas 14 as though it were a live value while that citation is produced by a broken parser. **The design's stated reason is wrong and its conclusion may still be right, and the two facts have to be reconciled together rather than either one fixed alone** — a design-only correction here would entrench the parser defect by making the page agree with a value the parser invents. G-149's mission will carry both, which is a further reason it is not in A-154's wave. **Recommended and NOT done, because it is G-146's gate and the meaning of its exit code is the operator's call:** either the gate runs each instrument and reports its exit code beside the folder, or its output states plainly that existence is all it checked. Today the gate is a control observed only counting, and the fleet's error class is controls that report a pass that no violation was ever run against. **Also recorded, because it must not reach the commit:** the `g148` lane has left debris in `_design/smartcity-flood-study/` — `.dbg.mjs`, `.check.log`, `.violate.log` — which is debug scratch, not a deliverable. | A gate went from four findings to one inside one session, which is what a mid-lane read looks like and why the gate labels the commit it ran at. Reading why the last line would clear found that the gate's R3 is a presence-shaped check, and the instance that proves it is a real design-versus-engine disagreement that G-149 already owned half of | operator (whether the gate runs what it counts, and the flood-study claim); planner (both gate reads, the instrument runs, this amendment) |
| A-156 | 2026-09-18 | **THE GATE REACHED `exit 0` AT 20:32:13Z, AND ALL FOUR INSTRUMENTS IT HAD JUST COUNTED REPORT LIVE FAILS IN THE DESIGNS THEY INSTRUMENT.** Measured 20 minutes after A-155, and it is the instance A-155 predicted rather than a new class. `design-completion-gate.mjs` now reads `20 design folders, with an instrument: 18`, zero R1, R2, R3 and R4 findings, and `verdict: FINISHED`. **The two folders in the gate's 20 without an instrument are `plan-review` and `smartcity-place-tab`, and both are SUPERSEDED by dated rulings** (`plan-review` by the reasoner path, 2026-09-15; `smartcity-place-tab` by the map dock, 2026-09-14), so neither requires one, because R3's instrument-required set is exactly `RATIFIED`, `APPROVED` and `IN REVIEW` and `SUPERSEDED` is deliberately not in it. **That exemption is correct and is not the gap.** **The gap is what the exit code means.** Run immediately after, at the same disk state: `_design/smartcity-overview-lens/check.mjs` **exit 1** ("Sparse.dc.html is granted 1 of 10 sources and still promotes Connections above the decision"); `_design/smartcity-flood-study/check.mjs` **exit 1** (the rainfall-atlas claim, A-155); `_design/smartcity-map-dock/check.mjs` **exit 1** ("Expand.dc.html: the tab nav drops 'Licenses'"); `_design/plan-review-departments/check.mjs` **exit 1** ("the design states the product has 'no department model at all'" where the README's source-state section says otherwise). **So the four instruments `g148` delivered are exactly the four that fail.** `smartcity-fleet-lens` and `smartcity-police-lens` exit 0 (2 and 4 artboards pass), and those two are `g153`'s, in flight. **These are design defects and not instrument defects, and that is proven rather than assumed:** each failing folder's `violate.mjs` exits 0, catching its planted violations on a real artboard (`plan-review-departments` 17 of 17) and confirming in its own words that a repaired copy passes with a matched-input count. An instrument that detects its own repair and rejects a planted violation is working; what it is reporting is the design. **In fairness to the gate, its own wording is precise and does not overclaim:** it prints "every design past DRAFT **carries an instrument**", which is exactly what it tested and exactly what is now true. The defect is not a false statement, it is that `exit 0` on an artifact titled "design-completion-gate" will be read by every downstream consumer as "the designs are clean", and four of them are not. **This is the fifth and strongest reason G-146 does not close on a green gate**, alongside A-152's three surviving items (the all-canvas declaration, the unmeasured vocabulary, the three operator-gated DRAFTs), and it converts "R3 emptying is necessary and not sufficient" from a caution into a list of four named defects. **Four designs now carry an open, reproducible finding, and no row owns them.** The repairs are design work in `doc_repo`, which is planner-owned, and each one is an adjudication rather than a typo fix: whether the flood study may name a return period at all depends on G-125's broken parser; whether the map dock's tab nav should carry Licenses is a navigation question; whether the product has a department model is a factual question about `smartcity-os` that A-145 already answered in the affirmative. **Recommended and NOT done, because carding is operator-ruled:** one row for the four design repairs plus a gate that reports per-instrument exit codes, since the repairs and the instrument that proves them are the same unit of work. | The gate went green inside the session, which is the moment a count-based control is most dangerous: `exit 0` from a gate named for design completion, over four designs whose own instruments say they are wrong. Reading the instruments rather than the gate is what found it, and it is the same lesson as a merged PR not being a done row | operator (whether to card the four repairs and teach the gate to report exit codes); planner (the gate reads, the instrument runs, the violate proofs, this amendment) |
| A-157 | 2026-09-18 | **A NEW INSTRUMENT REPORTS THE EXIT CODE THE GATE DOES NOT READ, AND THE FIRST THING IT CAUGHT WAS THIS SEAT DAMAGING A CLOSED DESIGN BOARD.** Deliverable: `scripts/govtech/design-instrument-exits.mjs`. It runs every `_design/*/check.mjs`, prints each one's exit code beside `design-completion-gate.mjs`'s verdict, and with `--violate` establishes ownership by running each folder's `violate.mjs`: a failure whose `violate.mjs` exits 0 is the **DESIGN**'s, and a failure whose `violate.mjs` is missing or does not pass is **UNPROVEN** rather than a design defect. Its self-test is **6 of 6**, and it plants and observes both directions rather than asserting them: a planted exit-0 instrument reads PASS, a planted exit-1 instrument reads FAIL, an unrunnable instrument is not a pass, and the restoration guard (below) is shown able to see a modified tracked board in a throwaway repo. Read at 2026-09-18T20:39Z: **18 instruments run, 4 failing, and the gate exits 0 and calls itself FINISHED at the same instant.** The four are `plan-review-departments`, `smartcity-flood-study`, `smartcity-map-dock`, `smartcity-overview-lens`, all four marked **DESIGN**, which is A-156's finding with its ownership now derived in one run instead of by hand. **The defect it caught in this seat.** Running the `--violate` sweep left `_design/smartcity-records-search/Main.dc.html` — a TRACKED, RATIFIED board whose row G-147 is CLOSED — with its `data-coverage-rule="documents"` element removed, so `check.mjs` exited 1 on it. Found by `git status`, not by the sweep: the only report was one line inside that folder's captured stdout. Restored to HEAD, `check.mjs` exits 0 again, and `violate.mjs` run three times from the restored tree is clean 3 of 3, so the instrument is re-entrant and the damage was **sweep-shaped**: this seat ran 18 mutate-capable instruments in a loop with nothing verifying that they put the boards back. **The instrument now fails closed in three places:** it REFUSES to start if `_design` is not clean, because on a dirty tree "did this instrument leave that behind?" is unanswerable; after every `violate` run it re-reads that folder's tracked state and any folder that came back modified is a REFUSAL; and it restores what it finds and **names every file it restored** rather than reporting itself clean. **Two bugs in the guard itself were found by violating it rather than reading it:** a whole-output `trim()` deleted porcelain's leading status column, which shifted every path by one character (`_design/x` printed as `design/x`) and made a one-character status unparseable, and an absolute Windows pathspec was passed to git, which reads backslashes as escapes and returns an empty list — a clean result for a dirty tree, which is precisely the failure this file exists to catch. Both are fixed and both are covered by the self-test. **Operational consequence:** `--violate` cannot run again until `g148`'s uncommitted instruments are committed, because it now refuses a dirty `_design` and that tree holds 25 paths from `g148` and `g153`. On a clean tree it refuses silently; on a dirty one it says so and stops. | The gate answers a question about instruments existing and its exit code is read as a question about designs being right. Nothing in the fleet ran the instruments it counted, so the missing instrument was authored here and immediately caught this seat's own hand in a tracked board, which is the strongest available evidence that a check is worth having: it fired on its author before it fired on anyone else. Both guard bugs were found by planting a violation, not by re-reading the code | operator (whether the gate should run what it counts, and whether to card the four repairs); planner (the instrument, the self-test, the restoration, this amendment) |
| A-158 | 2026-09-18 | **G-148 CLOSED, AND THE 11 FINDINGS IT DISCLOSED ARE NOW A NAMED LEAVE_BEHIND WITH NO OWNER RATHER THAN AN UNEXPLAINED GAP.** The lane filed `_inbox/2026-09-18_g148-design-instruments_close.json` at 20:43:47Z, after A-156 was written and before the tracker was next run, which is how it was found: the tracker REFUSED with `DISAGREE G-148 row=open close=closed`, which is the control working rather than a nuisance. **The close is honest about the four failures and does not round them off.** It states its done-condition as met with evidence a stranger can re-run, it records `contradicted` including that **it expected the four ratified designs to be CLEAN and they are not**, and it puts the findings in `leave_behind` by name rather than dropping them: *11 findings across the four designs, unfixed by the dispatch boundary*. The planner verified the row at source rather than accepting the close: the gate reports zero R3 findings and exits 0, all four `check.mjs` self-test in both directions (48/48, 45/45, 47/47, 57/57), all four exit non-zero on real findings, and **the count of 11 was re-derived from each instrument's own total line (4 + 1 + 4 + 2) rather than read from the close's prose**, which is the difference between confirming a claim and repeating it. The row is therefore **CLOSED**, because its stated done-condition was that the instruments exist, self-test, report a non-zero matched-input count and are proven by violation, and all of that is true. **What is NOT closed is the 11 findings, and A-156's sentence "no row owns them" now has an owner question attached to it instead of a silence.** They are real board defects, mostly stale NEGATIVES: a claim that the product does not have something it now has, including "no department model at all" and the flood study's row about an uncited rainfall atlas. A re-read of the canvas could not have caught them, and three were true when the board was drawn, so the instruments belong at every source change and not only at DRAFT. **Recommended and NOT done, because carding is operator-ruled: one row owning all 11 repairs**, since they are one class of defect (stale negative claims checked against source) and the instrument that proves each one is already written. Also recorded: the lane left `_inbox/2026-09-18_g148_surface_probe.log.txt` in UTF-16, the PowerShell redirect artifact the earlier lesson named, and it is excluded from the commit. | The tracker disagreed with a close within minutes of it landing and refused a verdict, which is a control firing on its own. The close contradicted its own expectations and said so, so the lane's honesty is what made the regrade cheap; verifying the count at source rather than reading it is what made it correct | operator (an owner for the 11 findings); planner (the regrade, the count re-derivation, this amendment) |
| A-159 | 2026-09-18 | **THE OPERATOR RULED THE TWO COUPLED QUESTIONS: ONE ROW OWNS THE 11 FINDINGS, AND THE DESIGN GATE NOW RUNS THE INSTRUMENTS IT COUNTS.** Ruling 1: the 11 findings `g148` disclosed as a named `leave_behind` with no owner (A-158) are carded as ONE row, **G-164**, because they are one class of defect (a claim checked against source that went stale or wrong, mostly a stale negative), each instrument proving one is already written, and repairing them and proving them are one unit of work. Four are adjudications rather than copy edits, and one is not a design-only fix: the flood-study rainfall-atlas claim is entangled with G-125's broken `parsePfdsDepthTable`, so G-164 carries that half blocked on G-125 and the other ten unblocked. Ruling 2: **R3 now asks whether a design's adversarial read PASSES, not whether a file exists.** Until this change R3 tested `hasCheck` and the gate could exit 0 over four designs whose own instruments exited 1 (A-155, A-156). The IO layer in `design-completion-gate.mjs` now runs each folder's `check.mjs` and records its exit code; R3 fails on exit non-zero (a design carrying an open finding), on exit 2 (the instrument's own refusal to reach a verdict), and on a null exit (the instrument could not be executed), so an unrunnable instrument is never read as a passing design. **Proven by violation rather than asserted:** the gate now exits **1** with exactly four R3 findings naming `plan-review-departments`, `smartcity-flood-study`, `smartcity-map-dock` and `smartcity-overview-lens`, and its self-tests went 23 to **27/27**, adding fixtures for a non-zero exit, an exit-2 refusal, an unrunnable instrument, and a non-vacuous `passingInstrument` count. **The enabling half, with one deliberate deviation from the approved package.** A plain `check.mjs` run rewrote its own `instrument-report.json` unconditionally, so a gate that runs 18 instruments would have churned tracked canon on every invocation; the package proposed making `check.mjs` read-only by default. On implementation that breaks the proof workflow, because each `violate.mjs` refuses when the report is absent (`instrument-report.json was not written. Run node check.mjs first.`), so the five report-writing instruments instead write **only when the measured body changed**, comparing the report with `generatedAt` removed. A run that changes nothing writes nothing, verified by a full gate run leaving `_design` with no report churn, and `violate.mjs` still finds its file. **One correction to this package's own premise, recorded rather than dropped:** the package said R3 requiring exit 0 would turn `smartcity-map-dock`'s Full-panel PARTIAL into a hard failure. It does not. That panel is a NOTE in the instrument, not a finding, so it does not affect the exit code and its disposition stays an open question rather than a blocker. Applied in the same pass: the four `_design/INDEX.md` replacement lines the close supplied, the `G-149` and `G-157` instrument-inheritance clauses, and the regenerated tracker. **Not resolved here, and named so it is not read as cleared:** `_design/smartcity-fleet-lens/check.mjs` and `_design/smartcity-police-lens/check.mjs` remain uncommitted and belong to `g153`, which holds no claim and filed no close; they are the only thing blocking `design-instrument-exits.mjs --violate`, and they are not this seat's to commit or revert (A-154). **G-146 does not close on this change** (A-152). | A green gate over four designs whose own instruments said they were wrong is the defect class `ENFORCEMENT.md` names: an artifact that exists, is correct, and does nothing, whose exit 0 is read as a verdict it never reached. The repair and the instrument that proves it are one unit, so one row owns both ends, and making R3 read the exit code is what turns the gate's green from "instrumented" into "clean". The approved read-only enabler was adjusted on contact with a dependency the package had not measured (violate.mjs refusing without the report); the deviation is recorded rather than smoothed, and write-only-on-change achieves the approved intent, that a plain measurement must not churn canon, without breaking the proof. | operator (the two rulings); planner (G-164, the gate and enabler change, the self-tests, the INDEX lines, the G-149 and G-157 inheritance, the tracker, this amendment) |
| A-160 | 2026-09-18 | **`smartcity-os`'S REPO-INTENT POSTURE WAS A STALE BLANKET FREEZE THAT BLOCKED A LANE THE OPERATOR HAD ALREADY AUTHORIZED; IT IS REPLACED WITH A-146'S PROVING RULE, AND THAT STARVED THE POSTURE CONTROL.** The canon gate refuses any dispatch that resolves to a repo whose intent row reads no-touch. `smartcity-os` read ABSOLUTE NO-TOUCH with case-by-case exceptions, but A-146 (operator, 2026-09-18) had already ruled that v1 `smartcity-os` is production and that a change to it is proven on a NON-PRODUCTION DigitalOcean app before `walrus-app` carries it. So the gate was refusing `g163`, and had already refused `g162` -- which ran anyway and opened PR #60. A control firing on authorized work is how a fleet learns to reach for the bypass flag. The planner verified the block through the REAL Cursor pipeline (`run-gate.mjs` -> `normalize-payload.mjs` -> `canon-gate.ps1`, with a `cursor-task` payload), not through a re-implementation of it. The row now reads ACTIVE UNDER A-146 RULE 4 and keeps every constraint the freeze existed for: one owning seat per repo, every change through PR review and never direct-to-main, every change proven on a non-production app before `walrus-app` carries it, and a lane never writes to `walrus-app` or to production traffic. Verified cleared: `g152`, `g153`, `g162` and `g163` all allow; before the change `g162` and `g163` both denied. **CONSEQUENCE, STATED RATHER THAN LEFT IMPLIED: `smartcity-os` was the ONLY row in `_catalog/repo_intents.md` whose posture resolved to no-touch or retiring, so the gate's posture branch now has NO armed input.** Its logic is untouched and it was validated against the real gate in both directions, but it is starved until some future row carries a freeze. A freeze not written into a row with the trigger string is not enforceable. **A-154'S CLAIM THAT "neither trips the canon gate" IS RETRACTED HERE:** it was derived from comparing hash markers and never from running the gate, and it was false for `g163`. | The earlier row had been amended case by case (the 2026-09-03 read authorization, the 2026-09-14 G-122 engagement, the G-126 defect-3 read-only pass) each time an operator ruling widened what was allowed, which left the standing rule less permissive than the standing rulings. That is a prose-shaped control drifting behind its own decisions. Folding A-146 in makes the row state the rule rather than enumerate its exceptions. | operator (confirmed the intent-row change); planner (the edit, the two-direction verification, the starvation measurement) |
| A-161 | 2026-09-18 | **FOUR LANES LANDED, THREE OF THEIR CLOSES SAT UNFILED INSIDE SEAT WORKTREES, AND THE TRACKER REPORTED PASS WHILE BLIND TO ALL THREE.** `g153-fleet-police-lens`, `g162-v1-finance-honesty` and `g160-served-commit-parity` parcel 2 each filed a close in their own seat worktree and nowhere else; `g148`'s was already in main. `smartcity-tracker.mjs` reads `_inbox/*_close.json` from doc_repo, so it printed "PASS every tracked row agrees with its own close" while three closes it could not see existed. That is not a wrong verdict, it is an UNFED control, and the difference matters because a starved mechanism reports success. Filed 15 artifacts plus one directory, each verified byte-identical (sha256 per file, a recursive manifest for the probe-shots directory), refusing on any cited file absent from the worktree so that no close lands with a dangling reference. The tracker then fired as designed and named all three (`row=open` against `close=closed` / `closed-partial`). Each close was verified AT SOURCE before regrading rather than accepted: the planner re-ran both `g153` instruments (exit 0, non-zero matched inputs), confirmed all five `g160` files present on CLEAN trees in both products with two unpushed commits each, and confirmed PR #75 and PR #60 open and MERGEABLE. **G-153 IS GRADED CLOSED-PARTIAL, STRICTER THAN THE LANE'S OWN `closed`, FOR ONE MEASURED REASON:** the row's instrument ends "both surfaces are reached on the DO app per D-12" and the DELIVERED build is not on the DO app (PR #75 is green and deliberately unmerged, nothing deployed). That is the same shape of gap for which `g160`'s lane graded itself partial, so the two rows are now graded consistently. `G-162` and `G-160` keep their lanes' CLOSED-PARTIAL. **Three leave_behinds are named and TWO HAVE NO OWNER:** the vendor mapping (all 102 live fleet and patrol records fail their declared shape, so after deploy Bastrop's Fleet and Police regions read `refused` until it lands) and the recapture of both design dumps. Owned by others: `g160`'s push, PRs and first deploy, which belong to the integration seat. Milestones after the regrade: M2 3/6 to 4/6, M3 4/9 to 5/9, M5 3/5 to 4/5. | Four lanes closing on one afternoon is the moment closes are most likely to strand: each lane writes to its own worktree, and the artifact that makes a row gradable is the one that lives outside the repo the planner reads. The unfed tracker is the same defect class as G-148's dormant instrument and G-160's starved `SERVED_COMMIT`: built, correct, and never reached by its own input. | planner (the filing, the source verification, the regrade); operator (an owner for the vendor mapping) |
| A-162 | 2026-09-18 | **TWO ROWS CARDED EARLIER THE SAME DAY WERE ENROLLED IN NO MILESTONE, SO THE PROGRAM'S OWN CONTROL COULD NOT SEE THEM AT ALL.** `smartcity-tracker.mjs` takes its row set from a hand-declared `MILESTONES` list, not from the plan, so a row that exists in the plan of record but appears in no milestone is read past. `G-163` (carded by A-151/A-154) and `G-164` (carded by A-159) were both in that state: the tracker said "tracking 32 rows" while the plan held 118 OPS-17 rows, and neither new row could produce a disagreement, be counted, or be reported as unreadable, because nothing iterated over it. **The scope is now declared rather than accidental:** 34 rows across the five milestones, M4 at 7 and M5 at 6. G-163 goes to M4 because it came out of the D-13 ship acceptance and is a `walrus-app` route defect, and G-164 goes to M5 because it is design repair work and it is the row the design gate is waiting on. **This is the same defect class as the other three found today** (G-148's dormant instrument, G-160's starved `SERVED_COMMIT`, the three closes filed where the tracker does not read) and it is the one that no complaint could have surfaced: a control that never iterates over a row reports success for it forever. | The gap appeared while catching up the roadmap canvas, which states a tracked-row denominator that would otherwise have been silently wrong by two. Note also that the tracker's `commit` stamp advanced between two runs minutes apart (`54e46861` to `9a53c8ae`), so another seat is committing to doc_repo while this work is uncommitted, which is why the close lands by explicit pathspec. | planner (the enrollment, the re-run, the counts) |

## Assumptions carried (each reversible by one amendment)

1. **The rebuild named in `34_smartcity_smart_files_and_foundation.md` open-item 1 IS this program.** The operator was uncertain. If it names a different effort, amend the G-03 and G-14 scope.
2. ~~**Smart Files is a rendering pass, not a build.**~~ **FALSIFIED 2026-08-14 by G-03; see A-002.** Smart Files is a BUILD. Retained struck-through rather than deleted: the assumption was written to be tested, the test ran, and the mechanism working is the record worth keeping. Doc 34 asserted it, the schema refuted it.
3. **Sensors are Asset Management Tier 2, not a fifth lane.** Ruled from the doc 32 Tier 2 section plus the sensor overview; both describe telemetry identically. UNTESTED — no row currently falsifies or confirms it.

**On carrying assumptions.** A-002 is the argument for this section existing. The planner carried doc 34's self-assessment into a program assumption; a read-only audit falsified it in one day and the cost was one amendment instead of a lane built on sand. Every future assumption added here is written to be falsifiable and named in a row that tests it.

## GRADE LOG (one row per grading pass; statuses re-grade by instrument only)

| Date | Pass | Rows graded | Result | Instrument notes |
|------|------|-------------|--------|------------------|
| 2026-08-14 | baseline | G-01, G-24, G-30, G-41 | recorded as LIVE at source | G-01 by frontmatter sweep; G-24 from the verified doc 32 statement; G-30 by grep on engine main; G-41 from adapter code plus commit history |
| 2026-08-14 | G-06 close | G-06 | PASS | Five-case negative test on true exit codes (not pipe exits): valid G row 0; valid P row on default plan 0; wrong-prefix row 1; unknown row 1; unknown plan 1. The gate was proven able to fire before being trusted, per inherited constraint 9. |
| 2026-08-14 | G0 close | G-01..G-05 | G-02 PASS; G-03 PASS (verdict BUILD); G-01/G-04/G-05 findings filed, 6 rulings owed | Close `_inbox/2026-08-14_g0_close.json` plus four sub-agent artifacts. G-02 `scripts/doc-staleness.mjs` built by the lane planner directly (not delegated) and proven on five cases; repo-wide it found **366 violations, not the 16 in the brief** — the brief's figure was root-only — and the largest class is **129 docs with no `status` field at all**, an absence class nobody had named. G-03 verdict BUILD, planner-re-verified at the schema. G-05 found CTRL-1 and CTRL-2, both reproduced. |
| 2026-08-14 | planner verify of G0 | G-03 schema claim, CTRL-1, doc-34 register | all three CONFIRMED at source | Verification not taken on report, per the rule that verification never rises on trust alone: `brokerageWorkspaces.ts:54-76` read directly (9 columns, single notNull FK cascade, no updated_at/version/cid/access_policy); CTRL-1 reproduced in PowerShell (`G-9999` → no `P-\d+` match → `ok` = fail open; `P-9999` → matched → rejected = looked alive); doc 34 register rows read at source. |
| 2026-08-15 | A3 L1 close | G-10 (lane A half), G-14 | G-10 MET; G-14 CLOSED foundation | Identity `smartfile:<scopeType>:<scopeId>:<docSlug>` on main `015b15d6` (PR #432, 8/8 conclusion SUCCESS). Live apply 0078/0079/0080 on cortex-prod neondb `_schema_migrations` 2026-08-15T14:27Z (run 31889907009). information_schema confirms four tables + nullable jurisdiction_fips + NOT NULL scope columns. Not customer-done. Close `_inbox/2026-08-15_a3_close.json`. |
| 2026-08-17 | G-13 close | G-13 | PASS | Decision `_decisions/2026-08-17_g13_consumer_contract.md`. Caller-split contract; three-supplier table; G-61 WDLL cites the id. Close `_inbox/2026-08-17_g13_close.json`. Live city unchanged. G-11 remains OPEN. |
| 2026-08-17 | G-61 close | G-61 | PASS | Dashboards `00002-sxq` compose+packs live-probed. MCP `00076-veq` tools live-probed. City `00118-qox` unchanged. Close `_inbox/2026-08-17_g61_close.json`. |
| 2026-08-17 | G-62 close | G-62 | PASS | Dashboards `00003-jc2` unauth compose omits owner-fact. MCP `00078-xuv` tag g62 `dashboards_compose_city_manager` omits owner-fact. City `00118-qox` unchanged. Close `_inbox/2026-08-17_g62_close.json`. |
| 2026-08-17 | G-63 close | G-63 | PASS | Dashboards `00004-zsq` GET `/api/adapter-kinds` seven kinds, samsara writesTo files, template-city grantedAdapters []. MCP `00080-voc` tag g63 `dashboards_list_adapter_kinds` anon. City `00118-qox` unchanged. Close `_inbox/2026-08-17_g63_close.json`. |
| 2026-08-17 | G-11 close | G-11 | PASS (sequencing, not sprint-54 done) | Dashboards `00005-m5t` fixture-city tenant-private grants []. Unauth 401. Bearer 403. Identified pack 200. MCP `00082-mat` tag g11 whoami plus identified get_city_pack. City `00118-qox` unchanged. G-33 and G-42 stay OPEN. Close `_inbox/2026-08-17_g11_close.json`. |
| 2026-08-17 | G-45 close | G-45 | PASS (staff map on Dashboards; live Leaflet uncut) | Dashboards `00006-vfk` GET `/` auto-loads `48021:34137` iframe `https://smartsite.cloud/?parcelNodeId=48021%3A34137`. Browser 908 PINE / APN 34137. MCP `00082-mat` tag g11 `smartsite.url` match. City `00118-qox` CSP omits smartsite.cloud. Close `_inbox/2026-08-17_g45_close.json`. |
| 2026-08-17 | G-64 close | G-64 | PASS (staff reviewer on Dashboards; live PermitFlow uncut) | Dashboards `00007-8sc` GET `/?lens=development-services` iframe `https://plan-review-app-ten.vercel.app/`. GET `/` still 908 PINE. MCP `00082-mat` tag g11 `data.planReview.url` match. City `00118-qox` `/permitflow/review` 200. Close `_inbox/2026-08-17_g64_close.json`. |
| 2026-08-17 | G-65 close | G-65 | PASS (product kill on Dashboards; live island uncut) | Dashboards `00007-8sc` GET `/app.js` permitflow=0; GET `/permitflow` 404. City bundle permitflow=160; `/permitflow/review` 200. No product PR. Close `_inbox/2026-08-17_g65_close.json`. |
