---
id: OPS-17_govtech_stack_plan_of_record
title: "OPS-17 — the govtech stack: plan of record"
status: active
last_updated: 2026-09-02
applies_to: portfolio
owner: nick
related: [OPS-16_texas_market_plan_of_record, 90_runbooks/AGENT_CONTRACT, _smartcity_masters/00_README, 65_sensors/sensor_program_overview, 65_sensors/convergence_smart_site_live_layer, 80_adrs/adr_023_cortex_reporting_repo_designation, 48_cortex_reporting_plan_review_spec, 75n_icc_code_connect_catalog, _inbox/2026-08-14_spine_unification_handoff, _inbox/2026-08-24_govtech_program_scope, _inbox/2026-08-24_govtech_transaction_contract, _inbox/2026-08-24_govtech_engine_migration_plan]
---

# OPS-17 — the govtech stack: plan of record

## Why this document exists

Four lanes (Smart Files, SmartCity/Bastrop, Plan Review, ICC) are about to be built by four hand-carried lane planners. They share a spine, an auth leg, an atom contract, and a customer. Without a frozen baseline with stable row IDs, they will drift into four private definitions of done and meet at integration.

This repo has a measured base rate: prose-shaped controls run 0-for-3, structure-shaped controls run 1-for-1. This document is the structure-shaped answer, copied in shape from OPS-16, which proved it on the Smart Site program. The spine unification handoff states the cost of adopting it late: Smart Site adopted this machinery four days in and spent those four days paying for its absence. This program adopts it at hour zero.

**Definition of done for the program.** All four lanes complete through Layer 5, the govtech stack built and tested, the spine healthy, and each surface consuming and depositing into the others per the seam table below. Every row graded by its named instrument. A row graded by narration or doc assertion fails.

## Governing rules

These are the OPS-16 rules, which govern here unchanged except where noted. `90_runbooks/AGENT_CONTRACT.md` is the operative law for lane behavior and is not restated here.

1. **The baseline table never gets edited.** Every scope change is a dated row in the AMENDMENTS table. What we believed on 2026-08-14 stays visible forever.
2. **No dispatch without a row.** Every dispatch names `PLAN-ROW: G-xx`. Row IDs carry a `G-` prefix so they never collide with the OPS-16 `P-` rows in a grep, a hook, or a compiler.
3. **Statuses re-grade only by instrument** (SQL, gh, live probe, script). The GRADE LOG records each pass.
4. **Lane-planner fan model** per AGENT_CONTRACT section 1. Four lane planners, hand-carried. Verification never delegated below the lane planner.
5. **Write-slot law** per AGENT_CONTRACT section 3. This program shares the atoms store with any other running program; only `--apply` queues, everything else is parallel.
6. **Shared-leg rows block across lanes.** A row in the SHARED LEGS table is not owned by one lane. No lane may build past a shared leg it depends on by stubbing it privately.

## The structure: four lanes by five layers

The five layers are the proven OPS-16 ladder, generalized. They are ordered by what-blocks-what, not by subject:

**L1 Foundation** — what must exist for anything else to be true.
**L2 Measurement** — does it actually cover what we claim, counted by instrument.
**L3 Integrity** — is it honest where it does not cover, and is access enforced.
**L4 Depth** — jurisdiction- and customer-specific richness.
**L5 Launch** — the commercial surface: priced, sellable, demonstrable.

The four lanes are subjects, each passing through all five layers:

| Lane | Subject | Twin relationship | Primary repo | Owner | Sequence |
|---|---|---|---|---|---|
| **A** | Smart Files | the twin DOCUMENTS (a **NEW** atom family, per A-012) | legacy-design-tools | **nick** + planning agent | **FIRST** (A-007) |
| **B** | SmartCity / Bastrop | a PORTFOLIO of twins including infrastructure | smartcity-os (**absolute no-touch on live Bastrop production**; see CTRL-2) | **nick** + planning agent | last (consumes A, C, Smart Site) |
| **C** | Plan Review | the twin ADJUDICATIONS (the earning loop) | legacy-design-tools (cortex-reporting, ADR-023) | **nick** + planning agent | parallel to A |
| **D** | ICC | a licensed SOURCE metered across every twin citing it | hauska-engine | **nick** + planning agent | parallel to A |

**Owner rule (A-006).** A lane or shared leg without a named owner is not dispatchable. The operator assigns owners at dispatch time; "unassigned" is a blocking state, not a default. **Assigned 2026-08-14: nick owns every lane and shared leg, with a hand-carried planning agent executing each.** The owner is who is accountable, not who does the work; the lane planner is named at dispatch.

**WDLL card rule (A-006).** Every lane carries a FROZEN WDLL card (What Done Looks Like), written at dispatch time and never edited afterward — the lane's own definition of done, in its own words, against which drift is visible. It is filed with the dispatch and cited in the close, with a graded Finish card at close. Full practice: `90_runbooks/wdll_practice.md`. Never call it an "acceptance card" — one artifact with two names is how two lanes produce non-comparable closes.

## The twin, stated once

Ratified by the operator 2026-08-14 and consistent with `65_sensors/sensor_program_overview.md`:

**A twin is a node. A node is an ID full of atom facts. The ID can be a human, a building, a desk.** What this company specializes in is creating twins of things with provenance and access control. That is the essence; everything in this program is a form of it.

Degrees of aliveness (ratified 2026-08-06): a COLD twin has the record; a WARM twin has it verified with provenance on every fact; a LIVE twin has senses streaming current state. The three verbs are CAPTURE, CONNECT, SENSE, in that order, as a cost discipline: hardware is the last resort.

**Sensors are Asset Management Tier 2, not a separate program.** The `_smartcity_masters/32_smartcity_asset_management.md` tiers (record, live state, view) and the sensor program cold/warm/live ladder are the same architecture at two altitudes. The sensor band adds the WATCH plane (a standing rule on a live twin: baseline plus condition plus roster) and the hardware doctrine.

**The two-altitude vocabulary rule is binding on every artifact this program produces.** Internally: twin, node, atom, graph. Externally to a city: the record, the asset, current state. Never say digital twin to a city. The Asset Management master carries the full never-say list; the Smart Files master carries the foundation one-sentence rule and its own never-say list. A claim not in the approved-claims register of a master is not approved.

## Inherited spine constraints (compile-time requirements, not advice)

From `_inbox/2026-08-14_spine_unification_handoff.md`. Every lane inherits these; no lane re-derives them. A lane deliverable that violates one is rejected at checkpoint, not at launch.

1. **Absence is typed and provenanced.** The seven-status taxonomy exists because one undifferentiated absent carried at least seven meanings. Plan Review and ICC face the identical class: code section not found must never conflate never-looked, source-down, paywalled, and genuinely-absent.
2. **Only a positive determination writes an absence.** An empty result re-enters the queue. This one rule killed an entire defect class.
3. **satisfied-absent is a first-class product state.** A surface that cannot render verified-absent misrepresents the substrate.
4. **Freshness is part of every response.** Visible computedAt and servedAt stamps, and a STALE indicator proven able to fire before it is trusted. This binds hardest on Smart Files: it is an artifact store, and a cache without a stamp is a liar waiting for load.
5. **Vintages are declared, never mixed; fallbacks are named, counted, and marked on every fallback read.** Plan Review hits this with code editions (the Bastrop B3-repealed-to-BDC incident is the same disease); ICC citations carry edition identity for the same reason.
6. **EntityId shapes are NOT uniform across families, by design.** Never reconstruct an identity from parts; use the value storage persists. A wrong reconstruction silently matches zero rows and looks like an honest absence.
7. **County-level and portfolio-level determinations are a legitimate node class.** Any surface that aggregates must know the marker convention exists.
8. **Ingestion is a Factory 1.5 workload.** Smart Files ingest and plan-review corpus intake reuse the staging pattern; they do not invent parallel machinery.
9. **Instruments are build items.** Gates are scripts. A gating indicator is tested for its ability to fire before it is trusted. Every ratio travels with its counting rule.

## SHARED LEGS — cross-lane blocking dependencies

Not owned by any single lane. A lane that needs one may not stub it privately.

| ID | Leg | Why it is shared | Blocks | Owner |
|---|---|---|---|---|
| S-1 | **Auth and tenancy** (sprint-54 leg) | The claim flow is the single most load-bearing dependency in the convergence (65_sensors convergence doc). Tenant isolation is not enforced today; Cortex runs an anonymous default tenant. | B (Bastrop private layer), A (per-tenant artifacts), C (reviewer identity), the claim flow | **nick** + planning agent — longest pole |
| S-2 | **Telemetry plane placement** | Open item 1 in the sensor overview: named, not decided. Needs the ADR-008 and 56 target-topology check before it hardens. | B Tier 2 (live state), any watch work | **nick** + planning agent |
| S-3 | **Smart Site mapping adoption** | Operator-named. The Smart Site mapping system is the proven one; SmartCity adopts it and Bastrop mapping comes up to that standard. | B, C (E6 map compose) | **nick** + planning agent |
| S-4 | **ICC content-to-actor reference** | ICC is metered across BOTH Smart Site and Plan Review. The meter currently detects ICC content by allowlist and regex heuristic, not a hard reference. Two citing surfaces, one ledger. | D, C (F6 code library), any Smart Site ICC citation | **nick** + planning agent (lane D executes) |
| S-5 | **Consumer contract shape** | ADR-023 rules that SmartCity consumes plan review as a FUNCTION. Whether all three suppliers are consumed via MCP, direct atom read, or service API must be one answer, not three. | B consumes A, C, and Smart Site | **nick** + planning agent |
| S-6 | **Twin node-class and entityId ruling** | A twin of a human, a building, a desk, or a pump station is not the parcel-keyed shape. Constraint 6 says shapes are non-uniform by design; this program must DECLARE its shapes rather than discover them. **Now also carries the A-007 ruling: does Smart Files EXTEND the brokerage workspace family or SUPERSEDE it?** | A, B (assets), any sensor work | **nick** + planning agent — RULED (A-012) |

## The seam table — what consumes and deposits into what

Operator-stated 2026-08-14. This is the definition of done for all apps consuming and depositing into each other.

| Seam | Direction | Ruling and status |
|---|---|---|
| SmartCity consumes Smart Files | B from A | Documents are the twin records; SmartCity is a consumer, never a second implementation |
| SmartCity consumes Plan Review | B from C | ADR-023: consumer pass only, after the C standalone gate passes. No review logic duplicated in SmartCity |
| SmartCity consumes Smart Site | B from Smart Site | Mapping adoption (S-3) plus the public record layer beneath the city own |
| ICC paid across Smart Site AND Plan Review | D from both | Two citing surfaces, one ledger, one actor identity. Requires S-4 |
| Twins (freeze watch class) surface on Smart Site | Smart Site from B and sensors | Already ruled: the sensor program IS the live rung of the Smart Site ladder (convergence doc, 2026-08-06) |
| Bastrop infra private to Bastrop, controlled via Smart Site, blocked to public | B | Already ruled: city view equals city own sensors plus public layer; landlord data never feeds a city dashboard. Requires S-1 |
| Plan Review deposits adjudications as atoms | C to spine | The earning loop. Structural commitment 2 depends on it |

## PLAN OF RECORD v1 — baseline frozen 2026-08-14

Status legend: **LIVE** = verified at source by the doc_repo planner on the date shown. **DOC** = from an artifact, not re-verified. **OPEN** = not started. **SCOPED** = written, not dispatched.

### Layer 0 — program zero (audit and standards; blocks confident scoping of everything)

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status at baseline |
|----|---|-----------|--------|---------------------|------------|-------------------|
| G-01 | 0 | Doc sweep: 102 root docs are `status: active` with `last_updated` before 2026-07-01; apply the lineage rulings doc 33a already made (47, 33, 40i, 30, 11a) | all lanes | `scripts/doc-staleness.mjs` reports zero active-and-stale in the lane-relevant set; status vocabulary validator passes | none | LIVE 2026-08-14: 102 of 159 stale-active; 16 distinct status strings against 4 legal |
| G-02 | 0 | Status-vocabulary and staleness instrument built (script, then hook) | G-01 durability | Script exits non-zero on an illegal status value or a stale-active lane doc | none | OPEN |
| G-03 | 0 | Code audit: does the workspace and Smart Files substrate exist as `34_smartcity_smart_files_and_foundation.md` open-item 1 claims (substrate built; Smart Files is a rendering pass, not a build) | Lane A sizing | Live read of the workspace atom family in the contract, engine registration, and any serving path | none | OPEN — this row SIZES lane A |
| G-04 | 0 | Memory-system review: `MEMORY.md` contents audited for stale, contradicted, and superseded entries | all lanes | Every memory traced to a live artifact or retired; index matches files | none | OPEN |
| G-05 | 0 | Dev-standards refresh: Cursor rules, `.cursor/settings.json`, AGENT_CONTRACT currency, hook inventory | all lanes | Contract hash current; hooks fire on a deliberate negative test | none | OPEN |
| G-06 | 0 | Dispatch compiler made plan-aware (`--plan` selects OPS-16 or OPS-17) so both programs compile | all lanes | `node scripts/dispatch.mjs --plan OPS-17 --lane X --plan-row G-01` compiles; an invalid row fails closed | none | OPEN — blocks every dispatch in this program |
| G-07 | 0 | **Repo cartography** — classify every one of the ~37 top-level directories and ~4,900 files: what it is, who owns it, active/archive/dead, and explicitly in-scope or out-of-scope for this program. Durable artifact `_catalog/repo_map.md`, not a report. | all lanes | The map exists, every top-level directory appears in it with a classification, and every count carries its counting rule | none | OPEN — G-01 covered root `.md` + `_smartcity_masters/` only; 35 directories and ~4,700 files were never examined |
| G-08 | 0 | **Dev-process file** — one hash-versioned document defining how we work, compiled into every dispatch like the contract | all lanes | A fresh agent with no thread context reaches the same operating conclusions as one with it | G-07 | OPEN |
| G-09 | 0 | **Process proving run** — three small deliberately-shaped agents (read-only audit, build-with-PR, forced mid-flight handoff) dispatched against the PROCESS, not lane work; close artifacts compared for structural identity | all lanes | Three closes are structurally comparable; divergence is a process defect, not an agent defect | G-08 | OPEN |
| G-19 | 0 | **Empressa Command Center authoritative docs** — 231 mentions, 116 in `_inbox/`, ZERO authoritative docs; authority split across eight documents with nothing composing them. Mechanical cause: `00c_portfolio_master_map.md`, whose job is enumerating surfaces, does not list it. Create the canonical doc set and add it to the master map. | all lanes (CC is the spine operator console) | A canonical CC doc exists at a numbered slot, `00c_portfolio_master_map.md` lists it, and the eight scattered sources are reconciled or point to it | G-07 map | OPEN — operator-ruled 2026-08-14. Cert View wiring goes to the backlog, not this row |

### Layer 1 — foundation

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status at baseline |
|----|---|-----------|--------|---------------------|------------|-------------------|
| G-10 | 1 | S-6: declare twin node classes and their entityId shapes (human, building, desk, asset, station) | S-6 | Written ruling plus contract types; no shape reconstructed from parts anywhere in lane code | G-03 | OPEN |
| G-11 | 1 | S-1: auth and tenancy leg state established and sequenced (sprint-54) | S-1 | Live probe: a tenant-private atom is refused to an anonymous caller on every surface | none | CLOSED 2026-08-17 as sequencing. Dashboards `00005-m5t` fixture-city tenant-private; MCP `00082-mat` tag g11 whoami + identified get_city_pack. Sprint-54 T2/T3, Cortex default tenant, G-33, G-42 remain OPEN. Close `_inbox/2026-08-17_g11_close.json`. |
| G-12 | 1 | S-2: telemetry plane placement ruled against the ADR-008 and 56 target topology | S-2 | ADR or decision record plus topology check | none | OPEN (named, not decided since 2026-08-06) |
| G-13 | 1 | S-5: consumer contract shape ruled (MCP against atom read against service API) once for all three suppliers | S-5 | Decision record; the B integration plan cites it | none | OPEN |
| G-14 | 1 | Lane A: Smart Files foundation — artifact store with provenance and freshness stamp on every artifact | A | Every served artifact carries source plus computedAt and servedAt; STALE indicator backdate-tested to prove it fires | G-03, G-10 | OPEN |
| G-15 | 1 | Lane C: cortex-reporting F1 through F7 spine wiring re-verified against live ldt (spec is 2026-07-01) | C | Per-function live probe against the `48_cortex_reporting_plan_review_spec.md` acceptance criteria | none | DOC: spec written, live state unverified |
| G-16 | 1 | Lane C: replace the Cotality APN-resolution dependency in F2 (Cotality is EXTINGUISHED) | C | F2 resolves a parcel with zero Cotality calls | address-to-parcel posture | OPEN — spec carries a dead dependency |
| G-17 | 1 | Lane D: S-4 content-to-actor reference wired (ICC atoms carry sourceActorDid plus book_id plus section_id) | D, S-4 | Meter attributes a reference by hard reference, not heuristic; purge selector resolves from the same field | none | OPEN |
| G-18 | 1 | Lane B: module decomposition seam identified — what in smartcity-os is genuinely modular against monolithic | B | Written inventory of the live codebase against the four category masters | none | OPEN |

### Layer 1.5 — operator-visible (CC-done)

Inserted by A-016. Isolation home added by A-018 (G-58). Command Center is the operator proving ground; G-56 is the Cortex prototype grade, not the product home. Customer-done remains Layer 5 / G-53 / Lane B.

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status |
|----|---|-----------|--------|---------------------|------------|--------|
| G-56 | 1.5 | Lane A: Command Center data room. Folders are nodes. Files are file-shaped atoms (documents with attachments), not every atom. Folder membership is edges (`placed-on`), not identity. PDF travels with the atom. accessPolicy on every read. MCP tools plus CC panel `smart-files`. | A, all lanes (CC is the operator console) | Frozen WDLL `_inbox/2026-08-15_a_wdll_cc_done_l15.md`, graded by live MCP probes and a live CC URL, never a merged PR | G-14 | OPEN |
| G-58 | 1.5 | Lane A: Smart Files isolation. Own repo `empressaioemail-tech/smart-files`, own Neon project, own GCP/Cloud Run service. Rewrite the settled shape. MCP retarget. Cortex application code removed. SmartSite mounts as a consumer. First instance of capability-mount composition. | A, SmartSite | Frozen WDLL `_inbox/2026-08-15_a_wdll_smart_files_isolation.md`, graded by live 404 on cortex-api `/api/smart-files` plus files-service healthz plus DSN-refusal probes | A-017, isolation WDLL approved | CLOSED 2026-08-15 (serving path; G-58b DROP remains OPEN). Close `_inbox/2026-08-15_a_smart_files_isolation_close.json` |
| G-58b | 1.5 | Drop `smart_file_*` on cortex-prod after L26 is quiet. Not during Texas ingest. Not this card's serving-path grade. | A | Named migrate applied on cortex-prod; `information_schema` has zero `smart_file_*`; county ledger still 200 | G-58 item 7, L26 quiet | OPEN |
| G-59 | 1.5 | Smart Files QA rooms: create folder, upload file, share room, org-level list. Vercel QA UI on the files service. Cortex stays unmounted. | A, Smart Files | Frozen WDLL `_inbox/2026-08-15_a_wdll_smart_files_qa_rooms.md`, graded by live create/upload/share/org probes plus a Vercel URL | G-58 | CLOSED 2026-08-15 on serving path (operator browser QA remaining). Close `_inbox/2026-08-15_a_smart_files_qa_rooms_close.json` |
| G-60 | 5 | ICC PoC demo: complete plan review on own Vercel/Neon/GCP plus finished Hauska MCP plus ICC-demo portal on its own host. Not the G-50 SaaS/public-paid half. | C, D, MCP | Frozen WDLL `_inbox/2026-08-16_icc_demo_program_WDLL.md` | A-024, this WDLL approved | CLOSED_ON_DEMO_PATH. A-031 files UI. A-033/A-034 ICC-demo https://icc-demo.vercel.app. WDLL 13 met. Residuals 7/18/19 held with L26. |

### Layer 1 Lane B product line (inserted by A-035)

G-18 CLOSED as as-found inventory 2026-08-17. Product-line overlay `_decisions/2026-08-17_smartcity_product_line_then_bastrop_onboarding.md`. Path amendment `_decisions/2026-08-17_dashboards_ui_then_one_feed.md` (A-052): next is Dashboards UI, then one source at a time; not a Bastrop cutover. G-13 CLOSED 2026-08-17 (`_decisions/2026-08-17_g13_consumer_contract.md`). Serving Bastrop remains no-touch. G-61 housing locked 2026-08-17 at `empressaioemail-tech/smartcity-dashboards` (A-037). G-61 CLOSED 2026-08-17. G-62 CLOSED 2026-08-17. G-63 CLOSED 2026-08-17. G-11 CLOSED 2026-08-17 as city-pack tenancy sequencing (`_inbox/2026-08-17_g11_tenancy_WDLL.md`). G-45 CLOSED 2026-08-17 (`_inbox/2026-08-17_g45_smartsite_staff_map_WDLL.md`). Dashboards staff map is SmartSite. Do not cut live Leaflet. G-64 CLOSED 2026-08-17 (`_inbox/2026-08-17_g64_lane_c_staff_path_WDLL.md`). Dashboards development-services is plan-review-app. G-65 CLOSED 2026-08-17 (`_inbox/2026-08-17_g65_permitflow_kill_WDLL.md`). PermitFlow is dead as a Dashboards product. Do not cut live `/permitflow/*`. G-66 CLOSED 2026-08-17 (`_inbox/2026-08-17_g66_dashboards_ui_WDLL.md`). Dashboards four lead lenses are navigable views on `00008-d55`. G-67 CLOSED. G-68 CLOSED 2026-08-17. G-69 CLOSED 2026-08-17 (Dashboards Review iframe residual). G-70 G-71 G-72 CLOSED 2026-08-17. A-054 Files compose then one feed is closed. A-055: feeds pause; G-73 shell homes next. Do not treat sprint-54 as done. G-33 and G-42 stay OPEN. G-21 honesty stays OPEN. G-24 stays zero.

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status |
|----|---|-----------|--------|---------------------|------------|--------|
| G-61 | 1 | Lane B: Dashboards product template (doc 31 lens family over records; not a clone of live Bastrop wallpaper). First city onboarding is a later named cutover, not this row. | B | Frozen WDLL `_inbox/2026-08-17_g61_dashboards_template_WDLL.md` approved; template mounts spine + SmartSite + Smart Files; live Bastrop unchanged | G-13 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — Dashboards `00002-sxq` compose+packs live-probed; MCP `00076-veq` tools live-probed; city `00118-qox` unchanged. Close `_inbox/2026-08-17_g61_close.json`. Cutover is a later WDLL. |
| G-62 | 1 | Lane B: unauthenticated Dashboards compose atom-reads as anonymous (same accessPolicy as MCP). Residue of G-61, not G-11. | B | Frozen WDLL `_inbox/2026-08-17_g62_compose_honesty_WDLL.md` approved; live unauth compose on `48021:34137` omits `owner-fact`; service Bearer is not a paid subject; live Bastrop unchanged | G-61 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — Dashboards `00003-jc2` live-probed omit owner-fact; MCP `00078-xuv` tag g62 `dashboards_compose_city_manager` live-probed; city `00118-qox` unchanged. Close `_inbox/2026-08-17_g62_close.json`. |
| G-63 | 1 | Lane B: feed adapter contract (kinds write records onto spine or files; not a product; not a copied table). Catalog only. No live vendor ingest. | B | Frozen WDLL `_inbox/2026-08-17_g63_feed_adapter_contract_WDLL.md` approved; live adapter-kinds catalog; template-city grantedAdapters []; live Bastrop unchanged | G-62 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — Dashboards `00004-zsq` kinds live-probed; MCP `00080-voc` tag g63 `dashboards_list_adapter_kinds` live-probed; city `00118-qox` unchanged. Close `_inbox/2026-08-17_g63_close.json`. |
| G-64 | 1 | Lane B: Dashboards development-services mounts Lane C plan-review-app as the staff reviewer. Not G-51 re-grade. Not G-52. | B | Frozen WDLL `_inbox/2026-08-17_g64_lane_c_staff_path_WDLL.md` approved; live GET `/?lens=development-services` iframe is plan-review-app; GET `/` still G-45 SmartSite; live Bastrop unchanged | G-45 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — Dashboards `00007-8sc` GET `/?lens=development-services` iframe `https://plan-review-app-ten.vercel.app/`; GET `/` still 908 PINE; MCP `00082-mat` tag g11 `data.planReview.url` match; city `00118-qox` unchanged. Close `_inbox/2026-08-17_g64_close.json`. |
| G-65 | 1 | Lane B: PermitFlow killed as a product on Dashboards. Live city `/permitflow/*` uncut until Bastrop cutover. | B | Frozen WDLL `_inbox/2026-08-17_g65_permitflow_kill_WDLL.md` approved; served Dashboards zero `permitflow`; city bundle still has PermitFlow; zero city deploys | G-64 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — Dashboards `00007-8sc` GET `/app.js` permitflow=0; GET `/permitflow` 404; city bundle permitflow=160 and `/permitflow/review` 200. No product PR (kill already true after G-64). Close `_inbox/2026-08-17_g65_close.json`. |
| G-66 | 1 | Lane B: Dashboards product UI on template-city. Four lead lenses as navigable views. Compass sheet chrome. Not a feed. Not Bastrop cutover. | B | Frozen WDLL `_inbox/2026-08-17_g66_dashboards_ui_WDLL.md` approved; live navigation of city-manager / development-services / finance / citizen without a hidden parcel form; live Bastrop unchanged | G-67 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — Dashboards `00008-d55` GET `/` `/?lens=finance` `/?lens=citizen` 200 badge Demo; GET `/compass` 404; PR **#8** squash `4ad3ba44` CI success run 32079207831; city `00118-qox` unchanged. Close `_inbox/2026-08-17_b_g66_close.json`. |
| G-67 | 1 | Lane B: design-system kit copy. Same `--sc-` file in smartcity-dashboards, smart-files, plan-review. No UI rewrite. | B, A, C | Frozen WDLL `_inbox/2026-08-17_g67_kit_copy_WDLL.md` approved; three token files byte-identical to `_inbox/2026-08-17_sc_kit.css`; live Bastrop unchanged | G-65 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — dashboards #7 `f7395e54` CI success; files #1 `53b22236`; plan-review #1 `a5f47e10`. Hash `2CDD0BF8…113EF2`. Close `_inbox/2026-08-17_b_g67_close.json`. |
| G-68 | 1 | Lane A: Smart Files product UI. File browser, access rail, Bring files chrome. Not OAuth. Not G-53. | A | Frozen WDLL `_inbox/2026-08-17_g68_smart_files_ui_WDLL.md` approved; serving Files QA is the kit browser; write API still works; Cortex unmounted | G-67 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — `smart-files-app` `dpl_3KjVHdKS1s7Rcpj3hehDvEJmqKqM`; PR **#2** squash `569bbdbd`; create 201 share 201; cortex 404. Close `_inbox/2026-08-17_a_g68_close.json`. |
| G-69 | 1 | Lane C: Plan Review product UI. Kit queue and console. Documents from Smart Files. Dashboards Review compose. | C, B | Frozen WDLL `_inbox/2026-08-17_g69_plan_review_ui_WDLL.md` approved; serving plan-review-app is kit chrome; no persona-gate first paint on Dashboards Review | G-67 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — `plan-review-app-ten` `dpl_CKg13X2su89rQYjore9VevtfDfP9`; PR **#2** squash `4330ac85`; iframe residual on Dashboards Review. Close `_inbox/2026-08-17_c_g69_close.json`. |
| G-70 | 1 | Lane B: Dashboards Work → Files mounts serving Smart Files. Not a browser rewrite. | B | Frozen WDLL `_inbox/2026-08-17_g70_dashboards_files_compose_WDLL.md` approved; live GET `/?work=files` host is smart-files-app; live Bastrop unchanged | G-68 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — Dashboards `00009-vpl`; PR **#9** squash `d2fc9525`; live compose `smartFiles` `https://smart-files-app.vercel.app/?embed=1`. Close `_inbox/2026-08-17_b_g70_close.json`. |
| G-71 | 1 | Lane B: first municode calendar feed on template-city. Writes records to files. Not MyGov. Not spine atoms. | B | Frozen WDLL `_inbox/2026-08-17_g71_municode_calendar_feed_WDLL.md` approved; grant on template-city; one public meeting record on files; Overview meetings reads it; live Bastrop unchanged | G-63 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — Dashboards `00010-vbs`; PR **#10** squash `570556c3`; compose meetings 5 records; files `00005-fdr` folder `template-city:public-meetings`. Close `_inbox/2026-08-17_b_g71_close.json`. |
| G-72 | 1 | Lane A: Smart Files embed chrome. Hide fixture panel and nested header when iframed. | A | Frozen WDLL `_inbox/2026-08-17_g72_files_embed_chrome_WDLL.md` approved; `?embed=1` or iframe hides `#fixture-panel`; write API still works | G-68 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — `smart-files-app` `dpl_8WCADkEhKByhLa6QSKEfUCbUZ3DZ`; PR **#3** squash `b73bcc6f`; create 201; cortex 404. Close `_inbox/2026-08-17_a_g72_close.json`. |
| G-73 | 1 | Lane B: Dashboards shell homes. Every G-18 / live-Bastrop staff function has a named home. No new feed. | B | Frozen WDLL `_inbox/2026-08-17_g73_shell_homes_WDLL.md`; live nav has Parks, Records search, Assets, Connections, People; Connections lists the register; live Bastrop unchanged | G-70 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — serving `00011-nzs`. PR #11 squash `6bad594`. Live Connections 67 of 67. City `00118-qox` unchanged. |
| G-74 | 1 | Lane B: template-city identity leak. Pull Bastrop meetings, Chestnut Street, and Bastrop-named Connections copy off the demo. | B | Frozen WDLL `_inbox/2026-08-17_g74_identity_leak_WDLL.md`; live compose meetings.records=[]; Citizen has no Chestnut; Connections HTML has zero Bastrop; live Bastrop unchanged | G-73 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — serving `00012-9dk`. PR #12 squash `00ea669`. Live compose meetings empty, no grant. City `00118-qox` unchanged. Close `_inbox/2026-08-17_b_g74_close.json`. |
| G-75 | 1 | Lane B: demo-city chrome. Fill mounts, Compass-class map motion from current rails, remaining 30c screens honest-empty. | B | Frozen WDLL `_inbox/2026-08-17_g75_shell_mounts_motion_WDLL.md`; live map iframe taller than 220px; Plan Review and Files pass embed=1; 67 of 67 plus 3 addenda; G-74 identity holds; live Bastrop unchanged | G-74 (CLOSED 2026-08-17) | CLOSED 2026-08-17 — serving `00013-vkl`. PR #13 squash `34b307b`. Live map 378x605. City `00118-qox` unchanged. Close `_inbox/2026-08-17_b_g75_close.json`. |

### Layer 1 Wave 1 integration (inserted A-085)

Govtech seat Wave 1 on `template-city`. Scope rev 3: `_inbox/2026-08-24_govtech_program_scope.md`. Live board: `canvases/govtech-master-program.canvas.tsx`. Transaction contract filed (S5-1). Merged ≠ live until deploy gates pass live violation probes.

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status at baseline |
|----|---|-----------|--------|---------------------|------------|-------------------|
| G-105 | 1 | **Deploy cut + live violation probes.** Deploy plan-review #7 Cloud Run + Vercel together; dashboards #39; MCP #75; engine-api #361. Each gate runs a deliberate violation probe on the deployed revision. | B, C, D, MCP | Per-PR live probe fails on known pre-fix behavior; passes on serving revision | none | OPEN — merged PRs armed-inert |
| G-106 | 1 | **Smart Files read-path scope.** Close defect #3: scope enforced on folder/file/blob read, not write-only. | A | Anonymous or wrong-tenant read of scoped artifact returns 403; violation probe on deployed files service | G-105 (dashboards #39 for paired BFF posture) | CLOSED 2026-09-02 — smart-files PR #9 merged, deployed `smart-files-00008-geb` @100%. Live-verified pre- and post-traffic-shift: anonymous 403, wrong-tenant 403, matching-tenant 200; legacy production callers unaffected (blanket-grant token preserved by design). Close `_inbox/2026-09-02_a_close.json`. See A-091. |
| G-107 | 1 | **Staff upload into Smart Files.** Wave 1 intake (R-B). Submittal lands in city-scoped folder before plan review runs. | A, C | Live POST upload on `template-city` returns artifact id; plan review can attach it | G-106, S1-17 tenant ruling, DOC-1 contract | OPEN |
| G-108 | 1 | **Plan review honesty path.** Edition selector, typed absence on code path, applicability matrix, code-lookup refuse deployed (#7). | C | Determination on declared edition cites real section or typed absence; neighbour fallback probe fails | G-105 DEPLOY-7, DOC-1, S2-1 migration or scoped interim | CLOSED 2026-09-02 — plan-review PR #10 merged, 57/57 tests. Items 8/9/10 re-graded MET now that G-107 landed: live end-to-end walk on `48021:34737` (real production engagement, not a test) declared edition IRC-2018, `proposedSetbackFrontFt=30` produced `14-02-003: Pass`, a second engagement with `proposedSetbackFrontFt=15` produced `14-02-003: Fail` — both with genuine analysis text and non-empty `editionId`/structured citations, not hardcoded. Typed `SOURCE_UNAVAILABLE` still holds from the original PR. See A-095. |
| G-109 | 1 | **ICC obligation ledger chain.** Migration 009 applied probe; meter bypass deployed; cited atom recorded; `source_obligation_ledger` reader (R-I). | D, MCP | Accrual row in obligation ledger for a live ICC reference probe; activity reconciles | G-105 DEPLOY-75/361, property S4-6 backfill | PARTIAL 2026-09-02 — govtech half (S4-1b, S4-8) merged, plan-review PR #8; prod migration/deploy held; substrate half (S4-3, S4-7) still OPEN and now the blocker. See A-090. |
| G-110 | 1 | **Wave 1 E2E transaction proof on template-city.** One submittal through Smart Files, review with declared edition, honest absence, accrual in obligation ledger, one shell with standalone SKUs. | all | Frozen acceptance chain S5-5 in scope rev 3; graded item by item on deployed surfaces | G-107, G-108, G-109, G-113, G-114 | CLOSED 2026-09-02 — one coherent live walk on `template-city`, engagement `3463b738`: (1) real staff upload with full provenance, artifact `smartfile:tenant:template-city:3463b738...--site-plan`; (2) IRC-2018 declared + a real proposed dimension; (3) matrix shows a genuine `Pass` (Bastrop UDC, structured citation) AND typed `Unchecked` absence (IRC rows) in the same run; (4) a `source_obligation_ledger` row for this specific session's own `request_id`s was NOT produced — this walk's own ICC-touching rows are typed absence, which never calls the MCP gate (confirmed by direct query). The accrual mechanism itself is separately, genuinely proven (A-102, same day); the gap is precisely named as no real ICC section content existing anywhere in the system yet to cite, not a mechanism or wiring defect; (5) Dashboards' embed correctly resolves to this exact engagement's service, structurally incapable of diverging since it has no separate computation of its own. **Operator-ruled 2026-09-02: residual named is fine — closed with item 4's gap standing as content-acquisition scope, not build scope.** See A-103, A-104. |
| G-113 | 1 | **Seam vocabulary conformance (WDLL item 13).** Fix the `verified-absent`/`absent-verified` spelling divergence (deliberately deferred to here since G-108, canvas item L3) and add a durable CI grep gate so `citation.mjs`'s `buildCitation()` stays the sole citation-minting path. Rescoped 2026-09-02 from the WDLL's literal text after checking source: the spelling divergence and the citation-minting logic both live ONLY in `plan-review` today — grepped `smart-files` and `smartcity-dashboards`, zero hits on either spelling, zero citation-shaped-object construction in either repo. Vendoring a validator into repos that never build citations is not in scope. | C | Zero occurrences of `verified-absent` in plan-review; a CI check fails when a hand-authored citation string (e.g. matching `/Section \d/` outside `citation.mjs`) is introduced, verified by violation before trusting it | none (single-repo, no cross-repo dependency found) | CLOSED 2026-09-02 — plan-review PR #12 merged, 64/64 tests. Spelling fixed. Re-confirmed single-repo scope with a fresh independent grep, not trusted from the dispatch. Found and fixed a real residual: two hand-authored citation strings sat dead-but-live in the `BASTROP-UDC` manifest since G-108 (unread, not fixed). CI gate is three layers: textual, structural, and a meaning-shaped check (shipped citation vs. one rebuilt fresh from the same row's fields). See A-100. |
| G-114 | 1 | **Presentation convergence (WDLL item 14).** Dashboards mounts Plan Review and Smart Files via iframe with NO city-scoping parameter in the embed URL — confirmed 2026-09-02 by reading `mounts.mjs`: `planReviewEmbedUrl()`/`smartFilesEmbedUrl()` return a static origin from env vars plus `?embed=1`, with no `cityKey` threaded through, and `compose.mjs` calls them with no city argument either. As Dashboards onboards more than one city this becomes a real scope trap, not a hypothetical. Each product must still deploy and run standalone (R-F, G-13). | B | Live probe: the embed URL carries the composing request's own `cityKey`, not just a static origin; `$PLAN_REVIEW_URL`/`$SMART_FILES_URL` each serve core flows with no Dashboards origin required; close names the three independent SKUs (`SCOS-PLAN-DEP`, `SCOS-FILE-DEP`, `SCOS-DASH-DEP`) | G-107, G-112 | CLOSED 2026-09-02 — dashboards PR #40, plan-review PR #13, smart-files PR #11, all merged and deployed. Live-verified end to end: fetched the actual deployed app.js/index.html from both Vercel production URLs and ran them in jsdom with the exact URL the live compose response produces — both resolve to `template-city`, not just a passing isolated test. Cross-city differentiation proven at unit level (no second city pack registered in production). All three SKUs confirmed independent, dependency direction unchanged from G-13. See A-101. |
| G-112 | 1 | **Wire plan-review's ledger reconciliation to the live substrate reader.** `fetchLedgerRows()` in `src/ledger-reconciliation.mjs` deliberately returns `null` pending exactly this. Capture `x-hauska-request-id` from `mcp.mjs`'s `mcpCall`, thread it into the `recordActivity()` call sites currently passing `requestId: null`, and call G-111's `GET /obligations/source-ledger` reader with the minted `platform_internal` key (stored, `plan-review-platform-key`). Surfaced 2026-09-02 once G-111 unblocked it (A-095). | D | Live probe: a real ICC-citing call records a `request_id` on `plan_review_activity` matching the ledger row's own; `reconcileActivity()` returns a genuine `matched` (or `divergent`) result against live data, not `ledger-unavailable` | G-111 (CLOSED, live, A-094) | CLOSED PARTIAL 2026-09-02 — plan-review PR #11 merged, deployed `plan-review-00021-huc` @100%. Mechanism live-verified: real, distinct `request_id`s confirmed landing on production `plan_review_activity` rows post-deploy (direct SQL). `fetchLedgerRows()` wired to G-111's reader. A genuine `matched`/`divergent` result NOT produced — blocked by the same codex-tier-key gap as DEPLOY-75's residual (A-088), confirmed empirically not assumed. See A-098. |
| G-111 | 1 | **Substrate half of the ICC obligation ledger chain (S4-3, S4-7).** `hauska-mcp-server` echoes the minted `source_obligation_ledger.request_id` back to the caller (or accepts a caller-supplied correlation id), and gains an MCP tool/endpoint that reads `source_obligation_ledger`. Surfaced 2026-09-02 by two independent govtech sessions building G-109's govtech half; request filed at `_inbox/2026-09-02_govtech_to_substrate_ledger_asks.md`. | D, MCP, substrate | Live probe: a Codex-gate tool call records a `request_id` the caller can read back on the response; a reader tool/endpoint returns ledger rows for a given `request_id` or `source_actor_did` | G-109 (govtech half, closed A-090) | OPEN |

### Layer 2 — measurement

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status at baseline |
|----|---|-----------|--------|---------------------|------------|-------------------|
| G-20 | 2 | Lane A: coverage of the real Bastrop document corpus counted, with counting rule | A | Count plus rule published; no bare ratio | G-14 | OPEN |
| G-21 | 2 | Lane B: which SmartCity modules actually consume the spine against mock or vendor-passthrough | B | Per-module live probe; honest inventory including UI-only surfaces | G-18 | OPEN — catalog currently oversells (payments, citizen portal UI-only) |
| G-22 | 2 | Lane C: determinations cite atom IDs and confidence objects, no bare scalars | C | F3 and F7 acceptance criteria pass on live data | G-15 | OPEN — G-60 WDLL 6 and 10 met 2026-08-16 on plan-review `00006-duj` (R302.1 Uncertain + 15-step briefing). Re-grade this row only against its instrument, not the WDLL close |
| G-23 | 2 | Lane D: per-reference rate set; ledger shows real accruals, not pending-rate nulls | D | Ledger query returns a non-null accrual for a live reference | G-17 | OPEN — G-60 WDLL 20 met on plan-review activity table (`rate=0.01`, UI + MCP sources). Hauska inbound meter on existing ICC atoms still waits G-30 UPDATE |
| G-24 | 2 | Lane B: Asset Management Tier 1 — first city-owned assets ingested into the graph | B | Store query: count of city-owned asset nodes greater than zero, with provenance | G-10, G-18 | LIVE 2026-08-10 per doc 32: ZERO city assets ingested |

### Layer 3 — integrity

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status at baseline |
|----|---|-----------|--------|---------------------|------------|-------------------|
| G-30 | 3 | Lane D: ICC accessPolicy stamped platform-internal at source; ingest hardcode removed | D | Store query: zero ICC atoms with public-free; anonymous list_jurisdictions omits icc-model-code | none | LIVE 2026-08-16: ingest hardcode REMOVED on engine main PR **#346** squash `ebe6d63` (CI SUCCESS). Store still public-free until bounded UPDATE. L26 holds `--apply` |
| G-31 | 3 | Absence taxonomy adopted by Lane C and Lane D for code-section absence | C, D | Schema carries typed absence; a probe failure cannot render as a data gap | G-15 | OPEN |
| G-32 | 3 | Vintage and edition identity on the code corpus (Bastrop B3-to-BDC class; ICC editions) | C, D | Declared vintage per jurisdiction; fallbacks named, counted, marked on read | none | OPEN |
| G-33 | 3 | Lane B: Bastrop infra private to Bastrop, blocked to public, controlled via Smart Site | B, S-1 | Live probe: anonymous caller refused; Bastrop-authenticated caller served; landlord telemetry absent from the city view | G-11 | OPEN |
| G-34 | 3 | Lane A: typed absence plus proven STALE indicator on the artifact store | A | Backdate test fires the indicator; absence carries basis | G-14 | OPEN |

### Layer 4 — depth

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status at baseline |
|----|---|-----------|--------|---------------------|------------|-------------------|
| G-40 | 4 | Lane C: Bastrop UDC and adopted-code depth in the review flow | C | F6 navigable at section granularity for the Bastrop own code | G-15 | OPEN |
| G-41 | 4 | Lane D: IPMC 2018 zero-sections resolved or ruled out of scope | D | Sections ingested, or a written ruling with ICC correspondence | ICC entitlement | LIVE: 0 sections, empty body upstream |
| G-42 | 4 | Lane B: Bastrop-specific lenses per the Dashboards master (lens family by audience plus permission) | B | Per-lens live probe with permission enforcement | G-11, G-21 | OPEN |
| G-43 | 4 | Lane B: infrastructure twins (SCADA ask) — station as smart site, bearing watch as watch contract | B | Watch contract instantiated on a station node; read-only boundary enforced | G-12, G-24, G-33 | SCOPED: `_inbox/2026-08-02_bastrop_scada_infrastructure_intelligence_ask.md` |
| G-44 | 4 | Lane A: the real Bastrop documents captured (CAPTURE verb, Factory 1.5 staging) | A | Staged with provenance; drain to store; count with rule | G-14, G-20 | OPEN |
| G-45 | 4 | S-3: Smart Site mapping adopted by SmartCity; Bastrop mapping brought to standard | B, S-3 | Bastrop map renders on the Smart Site mapping system, verified in the deployed bundle | none | CLOSED 2026-08-17 — Dashboards `00006-vfk` GET `/` auto-loads gold `48021:34137` on `smartsite.cloud` (908 PINE). MCP `00082-mat` tag g11 unchanged. City `00118-qox` Leaflet uncut. Close `_inbox/2026-08-17_g45_close.json`. Live Leaflet cutover remains a later named WDLL. |

### Layer 5 — launch

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status at baseline |
|----|---|-----------|--------|---------------------|------------|-------------------|
| G-50 | 5 | Lane D: ICC demo run; SaaS agreement executed; atoms upgrade platform-internal to public-paid | D | Signed agreement plus accessPolicy migration verified in store | G-17, G-23, G-30 | OPEN |
| G-51 | 5 | Lane C: standalone gate — all seven functions pass with ZERO SmartCity OS session | C | The overall acceptance criteria 1 through 7 in doc 48 | G-15, G-16, G-22, G-31 | OPEN — this row unblocks the B consumer pass |
| G-52 | 5 | Lane B: consumer pass — SmartCity initiates an engagement from a MyGov permit record | B | Live: engagement created from SmartCity, review runs in cortex-reporting, no duplicated logic | G-51, G-13 | OPEN |
| G-53 | 5 | Lane A: Smart Files sellable at the set price (25,000 dollars entry) | A | Deployed surface plus collateral drawn only from the doc 34 approved claims | G-34, G-44 | OPEN — price SET 2026-08-10 |
| G-54 | 5 | Program: govtech stack tested end to end — the seam table verified seam by seam | all | Each seam row probed live and recorded | all lanes | OPEN |
| G-55 | 5 | Watch external name for the municipal buyer (GovTitle retired without a replacement) | B | Name ruled plus catalog-thesis check | none | OPEN — owed naming call |

## AMENDMENTS (append-only; never edit the baseline)

| ID | Date | Change | Reason | Ruled by |
|----|------|--------|--------|----------|
| A-000 | 2026-08-14 | Baseline v1 frozen | Program stood up with its plan of record at hour zero, per the explicit lesson in the spine unification handoff | nick |
| A-001 | 2026-08-14 | G-06 CLOSED. `scripts/dispatch.mjs` is plan-aware: `--plan OPS-16\|OPS-17`, default OPS-16 so existing usage is unchanged. Row-prefix guard added so a `P-` row compiled against OPS-17 (or the reverse) fails closed instead of silently missing the table scan. | Two programs now run concurrently against one compiler; the plan path was hardcoded | planner (mechanical, no scope change) |
| A-002 | 2026-08-14 | **ASSUMPTION 2 FALSIFIED. Lane A is a BUILD, not a rendering pass.** G-03 closes with verdict BUILD. G-14 re-scoped from "wire a surface to a substrate" to "design and build the city-file-system family". G-53 now gates the Smart Files SALE, not just its surface. | G-03 verified at source: the workspace family is a BROKERAGE feature (agents saving listings via a browser extension), not a city file system — contract description "Brokerage workspace packaging contract types for V1", engine registry line 819, MCP "Brokerage tool 1/2/3". Planner re-verified the load-bearing schema independently: `brokerage_workspace_attachments` has a single notNull FK with cascade delete and **no `updated_at`, no `version`, no `cid`, no `access_policy`** (**8** columns, `lib/db/src/schema/brokerageWorkspaces.ts:54-76` — the planner first said 9 and miscounted; corrected by the G-09 proving run and re-counted at source. The BUILD verdict is unaffected: no `updated_at`, `version`, `cid`, or `access_policy` all confirmed). "A document lives once and appears everywhere it belongs" is therefore structurally impossible — one attachment belongs to exactly one workspace, so many placements means many copies, the exact problem Smart Files claims to solve. "Revise once, prior version still there" has no schema at all: only insert and delete exist. Production: 142 workspaces, **0 attachments** — the files half has never been written to. | nick (operator stated Smart Files needed building at program open; the doc-34 self-assessment carried into assumption 2 was the planner's error) |
| A-003 | 2026-08-14 | **OR-1 RULED: claims register NOT revised. The build is made true before any customer sees it.** The three rows stay as written; lane A delivers what they promise. Consequence recorded: **doc 34's approved claims are now gated on the G-14/G-53 build**, and no Smart Files collateral may ship ahead of it. Doc 34 line 32 ("built and running in the command center") is false as written and is corrected as a scoped in-place edit. | Operator ruling. The claim is the product commitment, not a drafting artifact; the fix is to build it, not to soften it. Price is already submitted to Vertosoft at $25,000 entry | nick |
| A-004 | 2026-08-14 | **CTRL-1 + CTRL-2 recorded as program defects; fix dispatched to G0 (not planner-patched, to avoid a concurrent-edit collision).** CTRL-1: `canon-gate.ps1` `Test-PlanRows` greps only `P-\d+`; an empty match returns `ok`, so **every OPS-17 dispatch has passed PLAN-ROW validation unvalidated**. Planner reproduced: `G-9999` and `Q-42` (rows existing nowhere) return `ok`, while `P-9999` correctly rejects — which is why the gate looked alive. CTRL-2: `Work in P:/smartcity-os` passes open on the absolute-no-touch live-Bastrop-production repo while three other phrasings block. Both fixes carry negative tests. **A divergence test between the hook and `dispatch.mjs` is required**, not just the two patches. | CTRL-1 root cause is a DESIGN SEAM the planner introduced: rule 2 chose the `G-` prefix so rows would "never collide ... in a grep, a hook, or a compiler", and that anti-collision choice is exactly what blinded the hook. The compiler is stricter than the gate enforcing it, and AGENT_CONTRACT section 7's blocking guarantee has been false for OPS-17 since rule 2 was written. Two implementations of one rule drift the moment one changes — hence the divergence test | nick (sequencing: fix before the four build lanes) |
| A-005 | 2026-08-14 | **OR-2 through OR-6 ACCEPTED as recommended.** OR-2: 40i and 11a are NOT covered by any 33a ruling (33a open-item 4 names exactly three targets: 47, 33, and the M4-B/PLR/SD/W vocabulary inside doc 30); both route to the operator rather than being guessed, and 11a's 10 inbound references mean no retirement without a `supersedes:` target. OR-3: **doc 30 stays `status: active`** — a scoped in-place correction marking the M4-B/PLR-1..28/SD-1..SD-8/W1-W6 section superseded-in-place, NOT a status flip, because the masters cite doc 30 four times as live source of record and it holds the only integrations inventory G-18/G-21 have. OR-4: ADR status vocabulary is a real convention gap (`01_doc_conventions.md` never ruled on ADR lifecycle); 16 ADRs affected including adr_023, which lane C depends on. OR-5: `premortem-check` — live config and CLAUDE.md contradict the memory that retired it 2026-07-13; resolve in one direction. OR-6: `.cursor/settings.json` tracked or ignored, never merely-unadded. | The planner's dispatch brief over-attributed rulings to 33a; the lane planner checked at source and pushed back correctly. Recorded so the correction is durable and the brief's error does not propagate | nick |
| A-006 | 2026-08-14 | **Baseline v0 shape adopted: every lane and shared leg carries an OWNER, and every lane carries a frozen WDLL CARD at dispatch time.** Owner column added to the lane and shared-leg tables below. | The operator's Smart Site v0 board ("each one now has a state and an owner") plus its WDLL practice ("everything in flight has a frozen acceptance card so drift is visible when it happens"). OPS-17 had state and instruments but neither owner nor per-lane acceptance card; with four hand-carried lane planners the shared legs would otherwise become nobody's | nick |
| A-007 | 2026-08-14 | **Lane A sequenced FIRST among the build lanes.** Three seams depend on it (B consumes A; plan review document handling rides it; Bastrop document capture is a Factory 1.5 workload needing the artifact store to exist). Open design ruling routed to G-10: **does Smart Files EXTEND the brokerage workspace family or SUPERSEDE it?** They are different products sharing a name — an agent saving listings against a city file system. Planner instinct is a separate family leaving the brokerage one alone; to be ruled deliberately, not by default. | Lane A was already first in dependency order; the BUILD verdict makes the sequencing consequential rather than incidental | nick |
| A-008 | 2026-08-14 | **G0-B closed: 41 of 41 directories mapped, none unmapped.** Deliverables `_catalog/repo_map.md` and `_catalog/repo_cleanup_backlog.md` (24 items, P0-P3). Four operator rulings executed this session, all planner-verified at source first. **R-A: the root `.vercel` link is REMOVED** — it linked doc_repo itself to a Vercel project named `doc_repo`, so a root `vercel deploy` would have published prospect dossiers, pricing, investor letters, and decision records; gitignored so it never propagated, but live locally. `system-overview-site/.vercel` (`empressa-overview`) is legitimate and untouched. **R-B: `_smartsite_masters/` NOW TRACKED** — nine `status: active` masters governing the customer-facing product, two carrying approved-claims registers and never-say lists, were entirely untracked with no ignore rule matching; they existed on one machine and would not survive a clone. This binds A-003 directly: the ruling that doc-34 claims stay as written and get built true is only enforceable if the register survives a clone. PDFs stay ignored per existing convention. **R-C: the shadow `Master Collateral Folder/_smartcity_masters/` is DELETED** (backed up to `_scratch/removed_2026-08-14/`, it was untracked so no git history existed to recover from) — it differed from the ratified root set in five of six files, root newer on all five, and listed government pricing as "an operator decision" while the authoritative root says SET 2026-08-10 with prices already at Vertosoft as MSRP. **R-D: `_inbox` retention R2/R3 adopted, R1 held** pending its two prerequisites. | Cartography found two hazards that were on nobody's brief. The scope correction is the durable lesson: G0 covered **846 of 1,955 markdown files (43.3%)** and published its exclusion list, but the "366 violations" headline travelled without its denominator — a process defect, not an agent defect, and now a G-08 requirement that a coverage figure travels with its denominator or does not ship | nick |
| A-009 | 2026-08-14 | **Command Center gets authoritative docs (new row G-19).** Cert View wiring — where a required certification gate physically happens and which may be unwired — goes to the cleanup backlog until it surfaces in real work. **The two in-repo clones are NOT deleted**: `hauska-mcp-server/` (already ruled in `.gitignore:12-13` "delete the clone", never executed) and `tmpbrief-l3-spine-consume/` (genuinely unrecorded, a clone of a different repo). Both stay untouched at their original HEADs pending an inspection of contents. | Operator: "i dont want to delete either of those without knowing whats in them. our MCP server is half of our business model." A ruling recorded in a gitignore comment and never executed is the same 0-for-3 prose-control shape this program exists to fix — but deleting a clone of a business-critical repo without reading it is the larger risk | nick |
| A-010 | 2026-08-14 | **G-09 PROVING RUN CLOSED. Verdict: the rulebook HOLDS as a behavioral control and FAILS as a schema.** Every behavioral rule that could fire, fired, and each caught something real; but the close schema is shaped for a build lane in a code repo, so all three sub-agents had to invent conventions to file a doc_repo close. The operator's criterion — a fresh agent with no thread context reaching the same conclusions — was **met on findings, missed on accounting**. Fixes applied this session: (1) **DEV_PROCESS 1.1 corrected 852→846 / 43.6%→43.3%**, planner-verified at source (852 appears ZERO times in the G0 close; its own metric reads "366/846") — the rule written to stop a number escaping its counting rule did exactly that with its own illustrative figure, and the planner propagated it from a lane report without checking. (2) **A-002 corrected 9→8 columns**, re-counted at source; the BUILD verdict is unaffected. (3) **AGENT_CONTRACT 1 now states the fan is exactly ONE level deep** — the contract was silent on whether a sub-agent may spawn while `dispatch-template-gate.ps1` forbade it outright, and the hook blocked all three of the lane planner's own dispatches (position-checked, not presence-checked). One rule, two implementations, disagreeing: the CTRL-1 shape again, with the hook stricter than the prose. (4) **AGENT_CONTRACT 6 gains three non-accounting fields** — `missionPremise`, `completionPredicate`, `scopeBasis` — because the forced-handoff branch produced a correct deliverable only by the successor independently re-reading OPS-17; the handoff artifact never carried the A-002 premise. (5) **DEV_PROCESS 3.2a** attaches an evidentiary standard to corrections: the correction instrument ran **7-for-10**, and rule 3.2 as written rewarded producing discrepancies without one. (6) **DEV_PROCESS 2.0**: a permanently-red gate is a dead gate — 1,036 of 1,141 staleness violations sit in append-only trees where an age check is meaningless; the actionable number is 105 of 494 in canonical doc space. | The most consequential save was SA-2 refusing a literal reading of its own brief: the four-line edit the brief described would have satisfied it exactly while pulling 102 append-only `_decisions` and 26 `_research` records into an age check, breaking the gate. Treating the brief as fallible is rule 3.2 working | nick |
| A-011 | 2026-08-14 | **Deliverable 4 of the G-09 dispatch (time-to-first-useful-action) RETIRED on the lane planner's recommendation.** It is self-reported, which rule 0 says is not a control, and it rewards the wrong behavior: three of SA-1's seven findings came from reading carefully rather than quickly, and a speed-optimizing agent skims backlog row 22 and misses its trap. **Backlog row 22 FAILS OPEN and is re-opened**: it names the wrong file for the band table and attributes another file's stale date to it, so an agent actioning it marks the row resolved while the real register stays stale. Twelve proposed diffs PD-1..PD-12 filed; six applied this session, the rest carried. | A metric that cannot be verified and that penalizes the behavior we want is worse than no metric | nick |
| A-012 | 2026-08-14 | **G-10 RULED: Smart Files is a NEW atom family. It does not extend `brokerage_workspaces`.** And **owners assigned**: nick owns all four lanes and all six shared legs, with a hand-carried planning agent executing each; the A-006 blocking state clears. The `brokerage*` RENAME is backlogged as its own focused lane (`_catalog/repo_cleanup_backlog.md` item 25) and is explicitly NOT part of lane A. | Operator: the brokerage CONCEPT is an early framing that is irrelevant. Verified at source what the name still reaches: **126 tracked files in legacy-design-tools (72 non-test), 8 database tables, and 54 NON-brokerage files importing brokerage modules** — including `app.ts`, `index.ts`, the atom registry, `spineZoningDistrict.ts`, `encumbranceService.ts` and `cadPropertyLookup.ts` (counting rule: `git ls-files \| grep -i brokerage` on main, tracked only). The name is dead; the code under it is the property reasoning substrate — federal and composite GIS layers, parcel keying, brief atoms, **metering (the ICC money path)**, entitlement, and provenance envelopes. So a new family for three reasons: the existing schema cannot carry the Smart Files promise regardless (single-parent FK, no `updated_at`, `version`, `cid`, or `access_policy`), so extending means rebuilding anyway; an in-place rename across 54 dependents is a high-risk refactor that would compete with lane A's real work; and `brokerage_workspaces` holds 142 live rows — dead concept, live data. Two known live items sit inside that surface and should be resolved WITH the rename: the 0.74 motivated-seller fixture in `brokerageGisCompositeLayers.ts` and the install-keyed `brokerage_install_claims` that the Radar ruling says must become user-aware | nick |
| A-013 | 2026-08-15 | **OR-A1 RULED: the Smart Files contract type is authored LOCALLY in legacy-design-tools for its first iteration, with a NAMED promotion step to `@empressaio/atom-contract` once the shape settles. Promotion criterion: the type promotes when G-34 closes** (typed absence is the last thing that reshapes the shape); if a second consumer needs it before then, that forces early promotion and is a FINDING, not a violation. **OR-A2 RULED: REUSE the existing CID and document-ingest mechanism with a NEW parent table** — reuse the mechanism, never the parent. | OR-A1: freezing an unsettled shape into a published cross-repo contract is the more expensive mistake, and the contract has consumers beyond this lane; once a type ships, changing it becomes a coordinated multi-repo migration. The cutover that would fix the underlying drift has a blast radius adjacent to the backlogged rename, which is ruled out of this lane. **Drift verified at source and the planner's first read was WRONG: the vendored tarball DOES exist** (`vendor/hauska-atom-contract-1.6.0.tgz`, installed at 1.6.0) — an `ls ... \| head -3` truncated it out of view. So LDT sits on vendored 1.6.0 against npm 1.22.0: the drift is real, nothing is broken, and the lane is DECLINING to widen scope rather than being blocked on a repair. OR-A2: `dataroom_document_atoms` already carries `atomDid`, `accessPolicy`, `sourceDocumentCid`, `confidence`, `verificationStatus` and an idempotent-upsert uniqueness index (verified at source), so re-minting content addressing beside a working deterministic-DID pipeline is the operate-not-rebuild failure. Its parent `attached_documents` carries the IDENTICAL defect as the brokerage table (single notNull cascade FK, no `updated_at`, `version`, or `access_policy`), so inheriting the parent would reproduce the problem lane A exists to fix | nick |
| A-014 | 2026-08-15 | **Lane A G-14 BUILT AND MERGED, and DELIBERATELY NOT CLOSED. Operator ruled HOLD on applying the migration.** PR #430 merged at `7bb79248` (planner-verified: MERGED, and every `statusCheckRollup` conclusion string SUCCESS — Test, Typecheck, Rubric, and five L5/L17 greps). Three tables: `smart_file_documents` (identity), `smart_file_versions` (content, append-only), `smart_file_placements` (location, many-to-many). **Placements reference the DOCUMENT, never a version**, so revise-once-current-everywhere is structural rather than a per-placement fan-out that can partially fail. entityId `smartfile:<jurisdictionFips>:<docSlug>` — decision record `_decisions/2026-08-15_smart_files_entity_id_shape.md`, closing lane A's half of G-10. **G-14 remains OPEN pending: apply `lib/db/drizzle/0078_smart_files_foundation.sql` to the deployment database, then refresh the fixture from live.** | The lane refused to call G-14 closed, correctly. **CI never executes `.sql` migration files** — planner-verified: no `drizzle-kit` step exists in any workflow, and migrations apply only through the `run-migrations` job in `cloud-run-deploy.yml` against `_schema_migrations` tracking. Green CI proves the drizzle schema, the fixture, and the pushed database AGREE; it does not prove 0078 EXECUTES. That is the merged-is-not-applied trap and the WDLL card retained the check on purpose. Operator ruled HOLD on the apply: it is a production schema change on a shared database, and deploys being planner-owned does not make an unrequested one appropriate | nick |
| A-015 | 2026-08-15 | **Smart Files is a spine MODULE, not a city-only store. Identity reversed before apply.** entityId is `smartfile:<scopeType>:<scopeId>:<docSlug>` with closed scopeType `jurisdiction` / `tenant` / `site`. Successor decision `_decisions/2026-08-15_smart_files_module_identity.md`. The same-day FIPS decision is superseded. Three-table split KEEP. Typed absence KEEP. G-10 (lane A half) re-opens until the new builder/parser is in code. G-14 stays OPEN until (a) identity matches this ruling and (b) 0078+0079+0080 are APPLIED. **A-014 HOLD LIFTED** by operator go to complete Layer 1; apply is planner-owned and waits until the identity PR is merged. **A-014 CI claim CORRECTED:** migrations execute only on `workflow_dispatch` `run-migrations`, never on the merge path (F-A2-CP1-1). A-013 promotion criterion has fired (G-34 closed) but promoting the FIPS shape is forbidden; promotion waits on this identity. G-44 is blocked until the key is right. G-53 is not the module definition of done. Do not dispatch B/C/D or G-30 in the L1 completion wave. | Operator correction 2026-08-15: Smart Files is used across SmartCity, Smart Site, Mox/custom, and a la carte. A FIPS-only key cannot address those scopes. Sequencing correction: do not apply 0078 first (that hardens the wrong key); do not wait on ICC/G-30; do not treat Layer 0 process or G-34 merge as L1 complete. | nick |
| A-016 | 2026-08-15 | **G-56 ADDED.** Layer 1.5 CC-done data room. Frozen WDLL `_inbox/2026-08-15_a_wdll_cc_done_l15.md`. Files are one atom type; folder membership is edges (`_decisions/2026-08-15_file_set_edges_not_identity.md`). SDK is question 3 and is not this row; ICC is the first real SDK customer (`_decisions/2026-08-15_icc_first_sdk_customer.md`). G-53 gains G-56 as an additional blocker (baseline G-53 row is not edited). G-19 stays the CC docs row. G-50 stays the first SDK-customer row. Mountain `_thought_leadership/04_positioning_narrative.md`. Mapping `portfolio_thesis/03_three_questions_data_room_and_sdk.md`. | Operator: CC-done now, customer-done waits on SmartCity/Bastrop. Vision document 2026-08-15. Rulings 1-4 approved. | nick |
| A-017 | 2026-08-15 | **Smart Files HOME is an independent module, not cortex-prod.** Own repo, own database, own service. SmartSite is one consumer among any (a la carte Joe Smith). Live SmartSite save/draw/share is a get-by and is not Smart Files. Knit (user + place + private data room + public spine read) is application-layer only. Default file accessPolicy is tenant-private. A-013 LDT location is prototype, not store. G-56-on-Cortex is disposable UX. No further Smart Files writes to cortex-prod. Do not drop 0078-0081 during L26. Decision `_decisions/2026-08-15_smart_files_independent_module.md`. Isolation WDLL owed before any new repo scaffold. | Operator: Texas ingest and the filing system must be physically unable to touch each other. Complementary in SmartSite UI, partitioned in wiring. | nick |
| A-018 | 2026-08-15 | **G-58 ADDED. Isolation WDLL approved.** Frozen card `_inbox/2026-08-15_a_wdll_smart_files_isolation.md`. Repo name locked `hauska-smart-files`. Mount-composition decision `_decisions/2026-08-15_capability_mount_composition.md`: capabilities mount into one interface (digital twin, Smart Files, SmartSite context, closing documents, tokenization if that later proves real). Same pattern for title company, city, builder, agent, buyer, seller. Bastrop example is operator-attributed pattern, not a live-probe claim. Tokenization / closing vault / twin authoring are future mounts and are not G-58 acceptance items. G-56 stays the Cortex prototype grade. G-53 stays customer-done. Do not scaffold until G-58 is this row (now true). Do not touch L26. Do not DROP 0078-0081 on this row. | Operator: card approved; capture the mount vision because it shapes how isolation is built (module plus mount, not a Cortex data room). | nick |
| A-019 | 2026-08-15 | **Repo name relocked to `empressaioemail-tech/smart-files`.** Operator created https://github.com/empressaioemail-tech/smart-files (PUBLIC, 2026-08-15T19:42:07Z). A-018 `hauska-smart-files` recommendation is superseded on name only. **RWA positioning:** we are not RWA creators; RWA operators mount their asset onto our provenance, Smart Files, and map (`_decisions/2026-08-15_rwa_mount_not_create.md`). Neon: new project in Empressa org, not a database on `fancy-fire-06136146` / cortex-prod or `tiny-art-63602898` / smartcity-os-prod. GCP: new project (recommended `smart-files-prod`), not a Cloud Run service on `legacy-design-tools-prod` or `hauska-prod-497015`. RWA mint / chain adapter is not a G-58 item. | Operator created the remote; asked for Neon + GCP; captured RWA mount-not-create as the legitimate sell. | nick |
| A-020 | 2026-08-15 | **Smart Files is an Empressa product.** Decision `_decisions/2026-08-15_smart_files_is_a_product.md`. Isolation (own repo, own DB, own service, mountable) is how the product is housed, not a demotion to substrate. Planner thesis-yellow "do not sell Smart Files as a product brand" is withdrawn. G-53 remains the sale. G-58 acceptance items do not change. | Operator: "smart files is a product." | nick |
| A-021 | 2026-08-15 | **Smart Files infra ids locked.** Neon: `smart-files` / `snowy-bread-83475727` / `ep-winter-shape-aw8ken54`. GCP: `smart-files-505619` (name `smart-files`, number `529170139834`, created 2026-08-15T19:47:56Z). Operator said "smart-site" for both; live names are `smart-files`. No GCP project id `smart-site` on `empressaioemail@gmail.com`. Do not deploy this product into `hauska-prod-497015`, `legacy-design-tools-prod`, or `smartcity-os-prod`. Cloud Run not enabled yet. | Operator created the projects; planner verified at source. | nick |
| A-022 | 2026-08-15 | **G-58b ADDED** (cortex-prod `smart_file_*` DROP after L26 quiet). Item 5 store applied on files Neon while Cloud Run waits on billing quota. Isolation seed entityIds are not on cortex-prod. | Operator: keep moving on Smart Files while Google quota is pending. Do not take the atoms slot. | nick |
| A-023 | 2026-08-15 | **G-59 ADDED.** Smart Files QA rooms on the files service plus a Vercel UI. Cortex stays unmounted (G-58 item 9 stands). Write MCP tools are a named follow-on, not this row's live grade. Personas are QA actors, not G-11. | Operator: prove create/upload/share/org at the Smart Files level; Vercel UI for QA; do not fish the flow through Cortex. | nick |
| A-024 | 2026-08-16 | **G-60 ADDED. ICC demo WDLL approved.** Frozen card `_inbox/2026-08-16_icc_demo_program_WDLL.md`. Equation: complete plan review (F1-F7 + map + letter + files) + finished MCP = ICC, watched on `/icc/activity`. Housing: repo `empressaioemail-tech/plan-review` (PUBLIC, createdAt 2026-08-16T15:03:02Z), GCP `plan-review-505715` (name `plan-review`, number `364754576784`, ACTIVE), Neon DSN at `%USERPROFILE%\.empressa\plan-review.database_url`. Decision `_decisions/2026-08-16_icc_demo_is_the_joint_done_line.md` active. G-50 signed-SaaS half stays OPEN. Applicant portal / Bluebeam / visual design / Circle / DNS / directory stay out. Do not steal L26. Do not DROP G-58b. | Operator: WDLL approved; still-out confirmed; repo and GCP created; go on the rest. | nick |
| A-025 | 2026-08-16 | **Lane C serving home isolated (same move as A).** Baseline lane table still names LDT as C's primary repo (ADR-023 function package). Serving housing is now `empressaioemail-tech/plan-review` + GCP `plan-review-505715` + own Neon, per G-60. G-15 "live ldt" host is superseded for grading: probes are the plan-review Cloud Run / Vercel. G-51 standalone gate is still the C Layer-5 row; G-60 is how that gate plus D's PoC demo plus finished MCP are built together. G-50 SaaS half unchanged. Alignment `_inbox/2026-08-16_ops17_four_lane_alignment.md`. Foundation SQL applied on plan-review Neon (`plan_review_*` tables=4, db=neondb). S-5 not closed; this wave reuses the A HTTP+MCP pattern. Lane B remains last / no-touch. | Operator: four-lane program; we just did Smart Files; align C+D. | nick |
| A-026 | 2026-08-16 | **Lane C extract-then-remount.** Plan-review BFF currently on cortex-api is pulled into `empressaioemail-tech/plan-review` (elevate existing callables, do not rewrite F1-F7 from 501s). Cortex `/api/plan-review` remounts as a proxy, not a 404 (unlike G-58 files). Calibration left as-is. Cotality stripped on intake/geocode. Sheets for new rooms go to Smart Files. Decision `_decisions/2026-08-16_plan_review_extract_and_remount.md`. Inventory `_inbox/2026-08-16_plan_review_cortex_callable_inventory.md`. ADR-023 serving home is this housing; ADR body not edited. Dirty LDT is not the remount vehicle. | Operator: pull cortex functions into plan-review as a whole, then remount on cortex. | nick |
| A-027 | 2026-08-16 | **Plan review is Smart Files' first product consumer.** Documents, sheets, and dataroom atoms are Smart Files file-shaped atoms in tenant `icc-demo`. Do not copy cortex attachedDocuments / GCS / `dataroom_document_atoms` into plan-review Neon. PE isolation mount remains a probe; live PE save/share is still not Smart Files. Decision `_decisions/2026-08-16_plan_review_is_smart_files_first_consumer.md`. | Operator: plan review should be Smart Files first consumer; documents and dataroom atoms all Smart Files based. | nick |
| A-028 | 2026-08-16 | **G-60 does not wait on L26.** Store UPDATE of existing ICC atoms (G-30 / G-17) and F4 engine ingest stay named residuals. Demo continues slot-free: MCP read-path withhold of ICC for anonymous callers, then walk / close. No second `--apply`. No G-58b DROP. Decision `_decisions/2026-08-16_g60_does_not_wait_on_l26.md`. WDLL A-008. | Operator: L26 will not be quiet for a while; work around it. | nick |
| A-029 | 2026-08-16 | **Smart Files gains a SECOND product consumer: Smart Markets** (the instrument twin, `_rd_disclosure_twin/08_build_scope.md`). It adds an `instrument` value to `scope_type` and to the placement `target_type`, plus a new scope validator in `src/identity.mjs`, keyed on the cockpit security-master node id (`_decisions/2026-08-16_instrument_scope_identifier.md`). **No Smart Markets migration may land on the smart-files store without this lane seeing it first**, because Plan Review is live on that store on the `tenant` scope (A-027, G-60). Smart Markets is NOT registered as a plan of record and its `TW-` rows are not dispatchable; this row exists so the shared leg is visible to lane A rather than arriving as an unscoped migration. Findings from the second consumer return to the store as a divergence report (TW-23); the first is that the two-verdict `smart_file_absence_determinations` enum has no way to say a category is structurally inapplicable, which lane A may meet independently. | A second consumer is the first real generalisation test the store gets. Two consumers landing schema changes on one store concurrently is a collision hazard, and OPS-17 governing rule 6 says a shared leg is not owned by one lane. | nick |
| A-030 | 2026-08-16 | **G-60 map is live SmartSite. Share is a submitter data room.** Plan-review embeds `smartsite.cloud/?parcelNodeId=` (same surface as Property Explorer). Do not deploy plan-review from dirty `P:\hauska-map`. Reviewer upload stays on the engagement Smart Files folder. Share returns a read-only `smart-files-app` `#share=` URL for the architect, homeowner, or contractor. Not an applicant portal. WDLL A-009. **A-031 reverses the share host.** | Operator: same map as SmartSite; upload then share the room as a data room with the submitter. | nick |
| A-031 | 2026-08-16 | **Plan review owns files UI and applicant view.** Smart Files remains the backend store (A-027). Share host is plan-review-app `/applicant?token=`, not smart-files-app. A-009/A-030 share-to-QA-app is reversed. Planner does not seed more files objects. Applicant view is a token room on plan review, not G-52 MyGov and not a full applicant portal. Decision `_decisions/2026-08-16_plan_review_owns_files_ui.md`. WDLL A-010. | Operator: files function lives in plan review; Smart Files is backend; applicant view needed; should not have written to Smart Files yet. | nick |
| A-032 | 2026-08-16 | **REVERSED.** Had put ICC portal on plan-review `/icc/activity`. Successor A-033. | Operator later: separate portal and domain for paying IP access. | nick |
| A-033 | 2026-08-16 | **ICC-demo is a separate portal and domain.** Path for people paying to access licensed IP. Vercel project `icc-portal-app`. Alias https://icc-demo.vercel.app. Repo `empressaioemail-tech/icc-portal`. Plan review cites. Activity mounts plan-review over HTTP. No second ledger. No DSN. Apex/custom DNS held. Decision `_decisions/2026-08-16_icc_demo_is_separate_portal.md`. WDLL A-012. Successor name pin A-034. | Operator: ICC Demo is a path for all people paying for their IP to access; separate portal; separate domain. | nick |
| A-034 | 2026-08-16 | **Product name and host: ICC-demo.** Operator: just call it ICC-demo. Alias https://icc-demo.vercel.app assigned to `icc-portal-app`. WDLL 13 applicant token-room met on live reviewer share. G-60 demo path stop. L26 / store UPDATE / F4 / G-58b held. | Operator: (1) call it ICC-demo (2) WDLL 13 done and verified (3) hold L26 residuals and session-close. | nick |
| A-035 | 2026-08-17 | **G-61 ADDED.** Lane B product line then Bastrop onboarding. Decision `_decisions/2026-08-17_smartcity_product_line_then_bastrop_onboarding.md`. G-18 as-found stands; build-plan overlay: template Dashboards then migrate Bastrop; kill PermitFlow after Lane C is staff path; CitizenConnect is the citizen lens not a SKU; Parcel Intelligence hosts on SmartSite; Digital Twin replaced by Asset Management; Compass reworked; vendor feeds are templated adapters that write records. Next cards G-13 then G-61. WDLL drafts `_inbox/2026-08-17_g13_consumer_contract_WDLL.md` and `_inbox/2026-08-17_g61_dashboards_template_WDLL.md` pending operator approval. G-21 remains OPEN and does not block the template. Live `P:\smartcity-os` stays no-touch. Do not start G-52. Do not fill G-24. Do not cut `/permitflow/*` on this amendment. | Operator: same page; file the decision; G-13 plus Dashboards template WDLL as next Lane B cards, not a Bastrop rewrite. | nick |
| A-036 | 2026-08-17 | **G-13 CLOSED.** Consumer contract `_decisions/2026-08-17_g13_consumer_contract.md`. Caller split: agents on one Hauska MCP; applications HTTP service-token or SmartSite embed; never a DSN; never a copied table. Pattern once; HTTP target named per supplier. G-61 WDLL approved same day and cites this id. Housing of the Dashboards template is unnamed; do not start G-61 build until housing is ruled. Live Bastrop unchanged. G-11 remains OPEN. Close `_inbox/2026-08-17_g13_close.json`. | Operator: both WDLLs approved. | nick |
| A-037 | 2026-08-17 | **G-61 housing locked.** One product repo, many cities as tenant packs. GitHub [empressaioemail-tech/smartcity-dashboards](https://github.com/empressaioemail-tech/smartcity-dashboards). Local `P:\smartcity-dashboards`. Isolation is accessPolicy plus tenant scope, not git. Live `P:\smartcity-os` stays the old weld until a named cutover. Do not clone `smartcity-os`. Do not deploy this product into `smartcity-os-prod`. Neon and GCP are not created on this amendment. MCP tools named in-repo for later retarget on the existing Hauska MCP server; no second MCP. Decision `_decisions/2026-08-17_smartcity_dashboards_housing.md`. | Operator created the remote and said done. | nick |
| A-038 | 2026-08-17 | **G-62 ADDED.** Lane B compose honesty after G-61 close. Unauthenticated city-manager compose must atom-read as anonymous (G-13 same accessPolicy). Live fail: gold compose lists `owner-fact`. Decision `_decisions/2026-08-17_g62_compose_honesty_before_g11.md`. WDLL draft `_inbox/2026-08-17_g62_compose_honesty_WDLL.md` pending operator approval. Do not dispatch as G-11. Do not start feed adapters on this card. Do not start implementation until the WDLL is approved. G-11 remains OPEN (longest pole). Live `P:\smartcity-os` stays no-touch. L26 untouched. | Operator: go on a thin compose-honesty WDLL, not full G-11; feed adapters after that. | nick |
| A-039 | 2026-08-17 | **G-63 ADDED.** Lane B feed adapter contract after G-62 close. Adapters write records onto spine or files with provenance. Kinds are a catalog; grants stay empty on template-city. Not a product SKU. Not a Dashboards vendor table. Not live MyGov/Samsara. Not G-24. Pipedrive refused. Decision `_decisions/2026-08-17_g63_feed_adapter_contract.md`. WDLL draft `_inbox/2026-08-17_g63_feed_adapter_contract_WDLL.md` pending operator approval. Do not dispatch as G-11. Do not start implementation until the WDLL is approved. Live `P:\smartcity-os` stays no-touch. L26 untouched. | Operator: keep going after G-62. | nick |
| A-040 | 2026-08-17 | **G-11 WDLL DRAFTED.** After G-63 close, Lane B next card is G-11 as city-pack tenancy, not live ingest and not sprint-54 T1-T3. Decision `_decisions/2026-08-17_g11_city_pack_tenancy.md` (provisional until WDLL approval). WDLL `_inbox/2026-08-17_g11_tenancy_WDLL.md` pending operator approval. Identified caller is a Hauska product key whose `jurisdiction_tenant` equals `cityKey`. Fixture pack `fixture-city`. Service key is not a tenant. No `--apply`. No live MyGov/Samsara. Live Bastrop no-touch. Baseline instrument "every surface" is sequenced: this card grades Dashboards HTTP plus existing Hauska MCP plus compose accessPolicy; Cortex anonymous default tenant, claim flow, and live Bastrop stay residuals. G-33 and G-42 stay OPEN after this card; they need a Bastrop-authenticated caller this card does not mint. Do not start implementation until the WDLL is approved. L26 untouched. | Operator: draft the WDLL and commit. | nick |
| A-041 | 2026-08-17 | **G-11 WDLL APPROVED.** Status flipped draft to approved. Decision `_decisions/2026-08-17_g11_city_pack_tenancy.md` active. Dispatch compiled `--plan OPS-17 --lane B --plan-row G-11`. Live Bastrop no-touch. L26 untouched. Do not absorb sprint-54 T2/T3. | Operator: wdll approved. | nick |
| A-042 | 2026-08-17 | **G-11 CLOSED as sequencing.** City pack is the tenant. Live Dashboards `00005-m5t` fixture-city tenant-private grants []. Unauth 401. Service Bearer 403. Identified Hauska product key `jurisdiction_tenant=fixture-city` reads the pack. MCP `00082-mat` tag g11 GET `/auth/whoami` plus identified `dashboards_get_city_pack`. Probe keys revoked. Sprint-54 T2/T3, Cortex default tenant, claim flow, G-33, G-42 remain OPEN. Live Bastrop `00118-qox` unchanged. L26 untouched. Close `_inbox/2026-08-17_g11_close.json`. | Operator: Approved, proceed. | nick |
| A-043 | 2026-08-17 | **G-45 WDLL DRAFTED.** After G-11 close, Lane B next card is G-45: Dashboards staff map is the SmartSite embed of gold `48021:34137`. As-found: GET `/` iframe is `about:blank`; after Compose the PE bundle renders 908 PINE. Do not cut live Leaflet. Do not clone PE. hauska-map only if frame policy blocks (it did not on 2026-08-17 probe). Decision `_decisions/2026-08-17_g45_smartsite_staff_map.md` provisional until WDLL approval. WDLL `_inbox/2026-08-17_g45_smartsite_staff_map_WDLL.md`. Live Bastrop no-touch. L26 untouched. Do not start implementation until the WDLL is approved. | Operator: go on next. | nick |
| A-044 | 2026-08-17 | **G-45 WDLL APPROVED.** Status flipped draft to approved. Decision `_decisions/2026-08-17_g45_smartsite_staff_map.md` active. Dispatch compiled `--plan OPS-17 --lane B --plan-row G-45`. Live Bastrop no-touch. Do not cut Leaflet. L26 untouched. | Operator: wdll approved. | nick |
| A-045 | 2026-08-17 | **G-45 CLOSED.** Dashboards staff map is the SmartSite embed of gold `48021:34137`. Serving `smartcity-dashboards-00006-vfk` @100%. PR **#5** squash `3080f4024651c0d53a74c307873cc85fab84b41b`. CI run **32052778257** check-run `test` conclusion `success`. Live GET `/` auto-loads 908 PINE / APN 34137 with no Compose click. Anon MCP `dashboards_compose_city_manager` `smartsite.url` matches iframe src. MCP `00082-mat` tag g11 unchanged (no MCP PR). City `00118-qox` CSP still omits `smartsite.cloud`. Live Leaflet uncut. L26 untouched. Close `_inbox/2026-08-17_g45_close.json`. G-33 and G-42 stay OPEN. | Operator: proceed. | nick |
| A-046 | 2026-08-17 | **G-64 ADDED.** Lane B Lane C staff path after G-45 close. Dashboards development-services mounts `plan-review-app-ten.vercel.app`. GET `/` stays G-45 SmartSite. Do not cut live PermitFlow. Do not start G-52. Do not re-grade G-51 F1-F7. Do not resume G-60 residuals. Decision `_decisions/2026-08-17_g64_lane_c_staff_path.md` provisional until WDLL approval. WDLL `_inbox/2026-08-17_g64_lane_c_staff_path_WDLL.md`. Live Bastrop no-touch. L26 untouched. Do not start implementation until the WDLL is approved. | Operator: do these two (Lane C staff path, then PermitFlow kill). | nick |
| A-047 | 2026-08-17 | **G-65 ADDED.** Lane B PermitFlow kill as a product after G-64 graded. Dashboards zero `permitflow`. Live `/permitflow/*` and `pf_documents` stay until Bastrop cutover. No city deploy. Decision `_decisions/2026-08-17_g65_permitflow_kill.md` provisional until WDLL approval. WDLL `_inbox/2026-08-17_g65_permitflow_kill_WDLL.md`. Do not start implementation until G-64 is graded. L26 untouched. | Operator: do these two (Lane C staff path, then PermitFlow kill). | nick |
| A-048 | 2026-08-17 | **G-64 WDLL APPROVED.** Status flipped draft to approved. Decision `_decisions/2026-08-17_g64_lane_c_staff_path.md` active. Dispatch compiled `--plan OPS-17 --lane B --plan-row G-64`. Live Bastrop no-touch. Do not cut PermitFlow. Do not start G-52. L26 untouched. Compass vision aligned, not a card. | Operator: both the others are approved as well. | nick |
| A-049 | 2026-08-17 | **G-65 WDLL APPROVED.** Status flipped draft to approved. Decision `_decisions/2026-08-17_g65_permitflow_kill.md` active. Implementation still blocked on G-64 graded. Live `/permitflow/*` uncut. No city deploy. L26 untouched. | Operator: both the others are approved as well. | nick |
| A-050 | 2026-08-17 | **G-64 CLOSED.** Dashboards development-services mounts plan-review-app as the staff reviewer. Serving `smartcity-dashboards-00007-8sc` @100%. PR **#6** squash `fdd615cc7928bdc48569252dbf285c12ce760027`. CI run **32059174854** check-run `test` conclusion `success`. Live GET `/?lens=development-services` auto-loads Plan review persona gate with no Compose click. GET `/` still 908 PINE / APN 34137. Anon MCP `dashboards_compose_city_manager` `data.planReview.url` matches. MCP `00082-mat` tag g11 unchanged (no MCP PR). City `00118-qox` `/permitflow/review` still 200. L26 untouched. Close `_inbox/2026-08-17_g64_close.json`. G-65 unblocked. G-33 and G-42 stay OPEN. | Operator: both the others are approved as well. | nick |
| A-051 | 2026-08-17 | **G-65 CLOSED.** PermitFlow is dead as a Dashboards product. Serving `00007-8sc` GET `/app.js` permitflow=0; GET `/permitflow` 404. No Dashboards PR this card (G-64 already refused the string). Paired control: city bundle `index-kGj7uMs4.js` permitflow=160; GET `https://smartcityos.io/permitflow/review` 200. City `00118-qox` unchanged. Zero city deploys. No `pf_*` DROP. Live island cut is Bastrop cutover, not this card. Close `_inbox/2026-08-17_g65_close.json`. G-33 and G-42 stay OPEN. | Operator: both the others are approved as well. | nick |
| A-052 | 2026-08-17 | **G-66 ADDED. Path amendment: UI then one feed, not Bastrop cutover.** Operator rejected city-wide Bastrop cutover as the next card. Sequence: Dashboards product UI on `template-city` (this row), then one data source at a time onto the template (adapter writes a record, or a copied scraper through that adapter). Three identities must not collapse: `template-city` demo, live Bastrop island (`00118-qox` / `tenant_id=2`), next onboarded city as a new pack. Gold `48021:34137` is a demo fixture, not Bastrop onboarded. Live `/permitflow/*` and Leaflet stay until named island replacements. Decision `_decisions/2026-08-17_dashboards_ui_then_one_feed.md`. Gap map `_inbox/2026-08-17_dashboards_missing_pieces.md`. WDLL draft `_inbox/2026-08-17_g66_dashboards_ui_WDLL.md` pending operator approval. Do not start implementation until the WDLL is approved. Do not start G-52. Do not fill G-24. L26 untouched. | Operator: session close; prefer UI first, then one source at a time; name the line between demo, Bastrop, and the next city. | nick |
| A-053 | 2026-08-17 | **G-67 ADDED. G-68 ADDED. G-69 ADDED. G-66 WDLL APPROVED** against 30c layouts. Kit copy first, then Dashboards UI, Files UI, and Plan Review UI in parallel. Extract `_inbox/2026-08-17_sc_kit.css`. Sequence `_decisions/2026-08-17_ux_implementation_sequence.md`. Light atom hex `_decisions/2026-08-17_atom_accent_light_hex.md`. Do not start G-66 G-68 G-69 until G-67 is graded. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: sent housekeeping to design agent and approved the four cards. | nick |
| A-054 | 2026-08-17 | **G-70 ADDED. G-71 ADDED. G-72 ADDED.** Files compose into Dashboards, then one public municode calendar feed onto `template-city` writing files (not spine; L26 holds atoms slot), plus Files embed chrome. Decision `_decisions/2026-08-17_files_compose_then_one_feed.md`. WDLLs approved. Do not start a Bastrop tenant. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: spawn the next wave; do not start a Bastrop tenant. | nick |
| A-055 | 2026-08-17 | **G-73 ADDED then CLOSED. Feeds still pause.** Operator: work on the shell; make homes for everything in the G-18 inventory before more data feeds. Decision `_decisions/2026-08-17_shell_before_feeds.md`. Homes register `_inbox/2026-08-17_g18_shell_homes.md`. WDLL `_inbox/2026-08-17_g73_shell_homes_WDLL.md` closed. Serving Dashboards `00011-nzs`. G-71 Bastrop municode host on template-city is a HOLD, not a feed win. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: review inventory; make homes; no more feeds. | nick |
| A-056 | 2026-08-17 | **G-74 ADDED.** Identity leak. Design review `_inbox/2026-08-17_g73_shell_design_review.md` A1 A2 A3. Pull municode grant off template-city. Compose must not hydrate Bastrop clerk meetings. Citizen must not name 1311 Chestnut. Connections must not say Bastrop. No clerk retarget. Decision `_decisions/2026-08-17_template_city_identity.md`. WDLL `_inbox/2026-08-17_g74_identity_leak_WDLL.md` approved. Demo-city chrome completeness is a separate handoff, not this row. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: you can do the identity stuff; other agent builds the rest of the demo city. | nick |
| A-057 | 2026-08-17 | **G-75 ADDED then CLOSED.** Demo-city chrome, mounts, Compass-class map motion. Operator handoff `_inbox/2026-08-17_demo_city_template_handoff.md`. Note `_inbox/2026-08-17_g75_shell_mounts_motion.md`. WDLL `_inbox/2026-08-17_g75_shell_mounts_motion_WDLL.md`. Serving Dashboards `00013-vkl`. PR **#13** squash `34b307b87683fb72de71c2163ac21c494b9fbe75`. CI run **32088195999** conclusion `success`. Plan Review detection was already on the host; Dashboards now passes `embed=1`; no plan-review PR. Interruptibility partial (no velocity, no drag). Register 67 of 67 plus 3 labelled addenda. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. Close `_inbox/2026-08-17_b_g75_close.json`. | Operator: other agent builds the rest of the demo city; planner owns merge and deploy. | nick |
| A-058 | 2026-08-18 | **G-76 ADDED. G-77 ADDED. Two rulings landed.** Operator approved a kit component package against 30b section 4 (`_decisions/2026-08-18_smartcity_kit_component_package.md`: typed wrapper only, CSS stays canonical, four-part conformance gate) and `template-city` becoming a fixture city (`_decisions/2026-08-18_template_city_becomes_fixture_city.md`: generated records labelled fixture, new `empty-city` pack keeps the honest-empty demonstration, five-part labelling gate). G-76 is the type conformance pass on Dashboards: fourteen declarations render below the 30b 12px floor and four ramp steps are off, which is most of why the serving build reads flat. G-77 is the fixture pack plus the Development services lens one deep; `ADAPTER_KINDS` declares no record shape today, so declaring one per kind is part of that card and is what makes the later pack swap work. Design sync of the kit and the design pass are sequenced AFTER both, because a design judged on an empty city is judged on the one condition where 30b law 3 guarantees no loud object. Do not start G-52. Do not fill G-24. `grantedAdapters` stays empty. Live Bastrop no-touch. L26 untouched. | Operator: sounds good; yes to both rulings; proceed with the decision records then fan subagents for 1, 2 and 3. | nick |
| A-059 | 2026-08-18 | **G-76 CLOSED. G-77 CLOSED. G-78 ADDED then CLOSED. G-79 ADDED then CLOSED.** G-76 brought shell type onto the 30b ramp: fourteen sub-12px declarations to zero, four ramp steps corrected, 42 rules and 67 declarations changed, guard `src/type-conformance.test.mjs` watched fire. Nav badges wrap to a second line rather than breaking the floor; measured 13 of 15 rows single-line. PR **#15** squash `30cb660`. G-77 made `template-city` a fixture city: 14 generated mygov permit-cases (3 overdue, 4 awaiting-applicant, 5 in-review, 2 ready-to-issue), record shapes declared beside `ADAPTER_KINDS`, new `empty-city` pack, five-part labelling gate each watched fire, zero CSS classes added, no parcel minted (a generated case is attached to no parcel, because a parcel is a real record). PR **#14** squash `321119f`. **Two production divergences followed, both invisible to a green local suite, both found by probing the deployed surface.** G-78: reading a public-free pack's CONTENT went through the enumeration gate, which falls through to "is a service key configured"; local has no key so it was open, production has one so the demo refused its own records. PR **#16** squash `04e16ed`. G-79: both pack SELECTs kept older explicit column lists while the write path learned `environment` and `generates_fixtures`, so Neon held `generates_fixtures=true` and the read mapped it to false; a local run has no DSN and never executes the SQL path at all. PR **#17** squash `23154be`. Serving `smartcity-dashboards-00016-77z` @100%: anonymous pipeline 200 with 14 fixture-marked records, `empty-city` honest-empty, `fixture-city` 401, pack list 401, zero identity strings. 149 tests. Rulings `_decisions/2026-08-18_smartcity_kit_component_package.md` and `_decisions/2026-08-18_template_city_becomes_fixture_city.md`. Closes `_inbox/2026-08-18_b76_close.json` and `_inbox/2026-08-18_b77_close.json`. Next: chrome must follow the pack (city name, seal, source counter are still static markup, so a pack swap leaves the wrong city named in the top bar); `[hidden]` is inert product-wide; then the design pass and the kit component package. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: yes and yes; proceed with the decision records then fan subagents for 1, 2 and 3. | nick |
| A-060 | 2026-08-18 | **G-80 ADDED. G-81 ADDED.** Two chrome clean-ups carried out of the G-76 to G-79 close. G-80: the chrome does not follow the pack. City name, seal, environment badge and the nav footer are static markup, so on `empty-city` the top bar still reads "City of Template" while the breadcrumb reads "Empty city". A pack switch is therefore only half a switch, and the one-command city swap the fixture-city ruling promises cannot be demonstrated until identity resolves from the pack. Public-free pack identity is content, readable anonymously per G-78. G-81: `[hidden]` is inert product-wide because the kit gives `.pill`, `.prov` and `.state` an explicit display. It has already shipped a visible defect (an amber Partial pill beside "no meeting packet has been read") and was patched one component at a time twice (`.stage[hidden]`, `.stage-esc[hidden]`); the root fix is one rule. Split by file: G-80 owns `web/index.html`, `web/app.js` and `src/`; G-81 owns `web/shell.css`. Neither touches `web/sc-kit.css`. Carried from the G-79 post-mortem into both dispatches: an executor must name every code path its tests do not exercise, especially any that differs between a local run and the deployed service, because two divergences in a row were invisible to a fully green suite. Design pass, kit component package and design sync stay sequenced after these two. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: spawn agents to clean up one and two, then we will do three. | nick |
| A-061 | 2026-08-18 | **G-80 CLOSED. G-81 CLOSED.** Serving `smartcity-dashboards-00017-vx4` @100%, 185 tests. G-80: identity resolves from the pack through one resolver (`src/city-identity.mjs`, served by `GET /api/city-identity` behind `packContentReadStatus`, so a public-free pack answers anonymously and tenant-private still 401s). All sixteen `template-city` literals left `web/index.html`; top bar name, state suffix, seal, both environment badges, document title, Compass scope, nav footer, fourteen breadcrumbs and eight absence bases now follow the pack. Verified live: the rendered DOM on `empty-city` contains **zero** occurrences of Template. Footer ruling: `0 of 7 sources granted` with its counting rule inline (distinct adapter kinds granted on this pack, of 7 in the catalog); the old `1 of 12` was a product-wide disposition count whose numerator was Esri mounted through SmartSite, granted on no pack, so beside a city name it was false for the city. The register figure stays on Connections and gained an inline rule it never had. PR **#19** squash `cdfca39`. G-81: `[hidden]:not([hidden="until-found" i]) { display: none !important; }` replaces two per-component patches. An attribute selector and a class tie, so every heavier-selector alternative relocates the tie rather than winning it; an important author declaration is the only form that outranks all normal author declarations regardless of position. Planner measured independently: hidden `.pill` `.prov` `.state` `.metric` all compute `none`, a component rule declared AFTER the global rule still loses, and zero of 203 live elements are wrongly hidden. PR **#18** squash `7a255de`. **Lane fence was wrong, not the executor:** `src/ui.test.mjs` asserted the literal text of the patch G-81 retires, so "all tests pass" and "do not edit the rest of `src/`" could not both hold. G-81 made a two-line minimal edit, reported it unprompted and graded itself PARTIAL. **F1 carried:** the standing forbidden-string clause bans `Bastrop`/`Chestnut` in `web/`+`src/` outside tests, but main legitimately carries 15, all of them refusal guards (held clerk host, `isIdentityHeldClerkHost`, the pack-name throw, identity-hold bases, fixture leak-detector needles). The clause must forbid ASSERTING and permit REFUSING or it is a permanently-red gate; planner hit the same false positive when verifying G-77. New cards queued, not started: asset cache headers (`/shell.css` ships with no `cache-control` and no fingerprint, so a returning browser can hold a stale stylesheet across every CSS deploy shipped so far); retire the now-redundant `show()` helper; the inert-`hidden` defect is untested inside the mounted SmartSite / Plan Review / Smart Files documents. Next is the design pass judged on a populated screen, then the kit component package and design sync. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: spawn agents to clean up one and two, then we will do three. | nick |
| A-062 | 2026-08-18 | **G-82 ADDED. Ruling amended same day.** Operator invoked design sync; it has nothing to convert, verified: no `.design-sync/`, no storybook, no stories, no React or JSX, no `dist/`, no `main`/`exports`, and `pg` is the only dependency in the product. All three product repos are static `web/` plus a Node server. The design system is ~110 CSS classes and 63 tokens, and design sync converts a compiled component library that renders from `window.<globalName>.*`. G-82 builds the prerequisite: a typed React wrapper over the kit vocabulary, whole shipped surface (~18-22 components), housed in a **new repo `empressaioemail-tech/smartcity-kit`** (operator choice; peer to the four product repos so none of them owns the shared system, per 30b section 4). Sequencing note worth keeping: wrapping before G-76 would have frozen fourteen sub-12px violations into every design the agent ever builds, and syncing before G-77 would have shipped preview cards of empty states, since the severity ladder that makes this system legible only exists when records exist. **`_decisions/2026-08-18_smartcity_kit_component_package.md` amended before implementation:** gate items 1 and 4 contradicted each other, forbidding any `--sc-` token declaration while requiring the canonical block be byte-identical or absent, so a package shipping `sc-kit.css` could not pass. Item 1 now forbids inventing a token rather than carrying the canonical one; item 4 now forbids the package styling anything. Second clause in two days whose wording forbade the compliant case, after the `Bastrop` refusal-guard finding; a gate is only a gate if the correct implementation can satisfy it. Design sync runs from `smartcity-kit` after G-82, not from doc_repo. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: /design-sync; new repo smartcity-kit; whole shipped vocabulary. | nick |
| A-063 | 2026-08-18 | **G-82 CLOSED. Licence HELD for operator ruling. Two new gaps named.** `empressaioemail-tech/smartcity-kit` PR **#1** squash `d6f83d9`, CI added by the card and conclusion `success`, 67 tests. **73 exported components covering 109 of 109 shipped stylesheet classes**, denominator being distinct class selectors in `sc-kit.css` plus `shell.css` with comments stripped; planner verified the denominator independently and the executor's set is a correct strict subset of a cruder grep (the 6-item gap is file-extension noise and one attribute selector). The 18-22 component estimate in the dispatch was low by ~3x because element-scoped children (`.t`, `.k`, `.v`, `.nm`, `.rail`, `.gl`, `.sep`) each need a real component to be covered without inventing wrapper markup. Vendored `sc-kit.css` and `shell.css` confirmed byte-identical to upstream **by git blob hash**; a working-tree md5 said DIFFERENT and was the wrong instrument on a Windows checkout (CRLF), which is the same trap recorded in the CRLF memory. `prove:gates` injects a real violation per gate and requires it to fail AND name the violation: **16 of 16 fired**, including law 3 quiet-default-flipped and Panel-gains-an-elevation-prop, the two design-law constraints the dispatch asked to be encoded in the API rather than merely permitted. **Licence held:** the executor added a proprietary `LICENSE` naming Legacy Group ATX LLC, an entity it inferred rather than was given. Legal execution routes to the operator per CLAUDE.md, so the planner removed it before merge; an unlicensed public repo defaults to all rights reserved, which is the safer interim state than an unread legal instrument. Proposed text preserved for the ruling. **Gap named, needs a card:** two of the five components 30b section 3.1 calls load-bearing have no CSS in any shipped stylesheet, the applicability matrix (`mx*`) and the code citation (`cite`), plus the atom chip from section 6.3. The executor reported three of five; it is two of five plus `atomchip`, which is section 6.3 not load-bearing. A design agent drawing the Plan Review surfaces will invent all three, which is exactly the failure the wrapper exists to prevent, so the kit cannot cover them until the product ships their CSS. Design sync now has something to convert and runs from `smartcity-kit`. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: /design-sync; new repo smartcity-kit; whole shipped vocabulary. | nick |
| A-064 | 2026-08-18 | **G-83 CLOSED. G-84 ADDED. Design sync in flight; ruling amended a second time.** Design sync against `smartcity-kit` reached a clean converter run: 73 components, 43 KB bundle, all `.d.ts` parse, anchor matches disk, `package-validate.mjs` exit 0, render check 72 of 73 with 65 on the honest floor card (previews not yet authored). **G-83 (kit PR #2, squash `7fa706c`):** the token layer was falling out of the sync's stylesheet closure. `shell.css` consumes `var(--sc-*)` throughout and defines none of them, and the converter's closure takes one entry, so it took the component CSS and left all 63 token definitions behind; every design built from the kit would have rendered a complete layout with every colour, size and radius silently falling back. Fixed by emitting `dist/kit.css`, the two concatenated in their load-bearing order, which also removes a real ordering footgun for npm consumers. Gate 4 refused the new file, correctly, so it is now checked HARDER than a copy: the test reconstructs what the build must have produced and requires byte equality including the header, watched failing on a rule appended to the built file. Second finding in the same run: the repo-wide scan was reading design sync's own `ds-bundle/` output as the package authoring CSS, the nested-clone failure again with a different cause, now recorded and printed rather than quietly excluded. Planner note: `cfg.tokensGlob` is a no-op without `cfg.tokensPkg` (it resolves a sibling PACKAGE, not a file in the DS's own dist). **G-84 added:** `[FONT_MISSING]` — the kit's CSS names Inter and IBM Plex Mono and ships neither. The product loads them from a font host in its own HTML; nothing built from the package does, so every design renders in a fallback and the G-76 type ramp is discarded silently. Operator chose to ship the woff2 from the kit (both OFL). That required amending gate 4 a second time: the package may now ship `@font-face` rules, and ONLY those, for families the canonical token block already names in `--sc-font-*`, with a test asserting both halves. **Third wording correction in three days; the pattern is that a gate written as an absolute keeps forbidding the compliant case, and each fix names the boundary rather than loosening the rule.** Remaining sync work: author 73 preview cards from the kit's GALLERY (operator scoped all 73), then upload. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: /design-sync; ship the woff2 files in the kit. | nick |
| A-065 | 2026-08-18 | **G-85 ADDED then CLOSED. G-86 ADDED.** G-85 was the sync-inputs PR, titled G-85 in the kit repo before an amendment row existed; A-065 records it rather than leaving a PR title that cannot compile. Kit PR **#4** squash `8ef0203f376107306595b26ccd75171965c66d51`, CI run **32185143428** check-run `test` conclusion `success`. Durable set: `.design-sync/config.json`, `NOTES.md`, calibration previews Pill / State / MetricStrip. Project `f5e5465e-943f-4f68-b52f-608925bc07b0` pinned, still empty. **G-86** is the remaining seventy preview cards, the conventions header, and incremental upload into that empty project. WDLL `_inbox/2026-08-18_g86_previews_WDLL.md` approved by operator proceed on the 2026-08-18 handoff. Planner fans disjoint preview sets; verification is not delegated. Do not set `cfg.provider` to Theme with `mode: "dark"`. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: pick this up and proceed on `_inbox/2026-08-18_design_sync_handoff.md`. | nick |
| A-066 | 2026-08-18 | **G-86 CLOSED PARTIAL — five of six acceptance items met, the upload DROPPED. G-87 ADDED.** Kit PR **#5** open not merged, head `c935ad07dae7c7320c3eb37f68c2548632194108`, base `8ef0203`, CI run **32194013389** check-run `test` conclusion `success` with vendor-parity arm B running (CI sets `SC_DASHBOARDS_DIR`, and the final step asserts the drift check actually ran rather than declaring itself unrun). Durable set 72 files: 70 owned previews, `conventions.md`, `config.json`. `package-validate.mjs` exit 0 — 73 of 73 render cleanly, zero floor cards, zero bad, **zero `GRID_OVERFLOW`**, anchor matches disk on 73 recomputed hashes; 73 grade files over 155 cells, all good. The fifteen flagged components were remedied in `cfg.overrides`: eleven `cardMode: "column"` (full card width per story, none dropped), four `cardMode: "single"` with an explicit `primaryStory`, three shell entries keeping `1100x700` because `viewport` IS in the grade key and `cardMode` deliberately is not. Header answers the carried open question: dark is the staff default, set `data-theme="dark"` on the root, never `Theme mode="dark"` as a page provider. **Item 5 dropped for a TOOLING reason, not a work reason.** The dispatch was issued on the belief that a Claude Code seat carried the DesignSync MCP server; the executing session was Cursor and carried the same three servers the prior session had, verified by full catalog listing plus two pattern searches. `.ds-sync/resync.mjs` documents the upload as agent-side MCP calls, and no HTTP client, token or CLI upload path exists in the toolchain, so nothing local substitutes. Project `f5e5465e-943f-4f68-b52f-608925bc07b0` is still EMPTY and untouched; improvising a route to a live design project was refused under the dispatch's own stop rule, since a fresh anchor over a half-applied upload is permanent while an un-anchored project merely re-verifies. Operator ruling: hand the mechanical STEP 2 runbook in `_inbox/2026-08-18_b86_close.json` to a seat that has the tool. **Two defects the instruments could not see, both found by looking at the card render.** Grading reads per-story sheets captured through `?story=`, which are full-bleed and therefore unaffected by `cardMode`, so the sheet a grader reads is not the card the picker shows — `CompassScrim` and `StageScrim` rendered as an empty white box with `bad`, `blank`, `collapsed` and `thin` all false and every cell graded good, because a `Region` outside `ColStack rail` collapses `.region-canvas` to height 0 and a 0.34 wash over nothing is nothing. Fixed with the kit's own height idiom and recorded as a WDLL amendment rather than absorbed, since the dispatch forbade re-authoring. **Fourth wording finding in four days, and the first where the gate was RIGHT.** `conventions.md` named four vendor brands inside a refusal guard, in a file stitched verbatim into the uploaded README. `test/constraints.test.mjs` keeps `ASSERTION_ONLY` (`bastrop`, `chestnut`, nameable in order to be rejected) and `ABSOLUTE` (`permitflow`, `citizenconnect`, `leaflet`, `pipedrive`, `stripe.com`, not at all), and its header states they are not the same rule — the carve-out was written knowingly and withheld from vendors, so reaching for the now-familiar "the gate forbids the compliant case" pattern would have granted a carve-out that is the operator's to grant. Content fixed; all 417 files of the upload payload grep clean. **G-87 added** from that finding: `walk()` in `test/_lib.mjs` skips `.design-sync` at ROOT as `TOOL_OUTPUT_DIRS`, so the constraint suite scans neither the conventions header nor the 73 previews and CI was green on the violation. The skip was correct when the directory held only converter output and went stale when G-85 moved authored source into it. Not fixed as a drive-by: it touches three scans and re-opens the gate-4 stylesheet false positive it was recorded to prevent. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: hand the STEP 2 runbook to a seat that carries DesignSync; open F-1 as a row. | nick |
| A-067 | 2026-08-18 | **G-87 SCOPED.** Lane B. Restore constraint-gate coverage over `.design-sync/`, which now holds authored source that ships. Pass/fail instrument: the injected-violation harness, not a passing suite — a forbidden string planted in `.design-sync/conventions.md` and one planted in a `.design-sync/previews/*.tsx` must each turn `npm test` red and name the file, and gate 4 must still not read converter output as package-authored CSS. `toolOutputSkipped()` must keep reporting whatever remains skipped, because an empty result is not an absence. Housing `empressaioemail-tech/smartcity-kit`. Blocked on nothing; kit PR #5 merge is a convenience, not a dependency. | A gate that cannot see the file it governs is not a gate, and this one was already green on a real violation. Prose in a PR body does not survive; the base rate in this repo is hook-shaped controls 1-for-1 and prose-shaped controls 0-for-3 | nick (row opened); planner (instrument) |
| A-068 | 2026-08-18 | **G-86 CLOSED FULL. Item 5 re-graded dropped to met on the upload leg.** Kit PR **#5** merged squash `d4f7b615b25f5ec79c3058ea28dfbae1b4515fa3` at 2026-08-18T23:22:27Z; pre-merge check-run `test` conclusion `success` on head `c935ad07dae7c7320c3eb37f68c2548632194108` (run 32194013389), `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`. The merge gate was the conclusion STRING on the identical tree, not a `gh` exit code. A-066 dropped item 5 for a TOOLING reason rather than a work reason, and that reason no longer holds: this seat carries DesignSync and proved it with a read call before any write (`get_project` returned `SmartCity Product Kit`, `PROJECT_TYPE_DESIGN_SYSTEM`, `canEdit true`), which is the check the mis-issue of this dispatch cost a session for want of. **417 of 417 uploaded and reconciled by prefix against the remote**, not asserted: components 292, `_preview` 73, fonts 44, `_vendor` 2, six root files; `tokens/` and `guidelines/` empty and correct, because `cfg.tokensGlob` is a no-op without `cfg.tokensPkg` and all 63 tokens ship inside `kit.css`. Sequence held: sentinel first and alone, 416 content files in nine chunks, no deletes since the project started empty, sentinel re-armed, `_ds_sync.json` last in its own call. Zero write failures, zero retries. One deliberate deviation from the runbook: **the remote was listed and reconciled at 416 BEFORE the anchor was armed**, because the anchor vouches for everything beneath it and a fresh anchor over a half-applied upload is permanent while an un-anchored project merely re-verifies. Chunking was by BYTES as well as file count, `fonts/` largest-first in four chunks and `react.js` alone at 1086 KB. The bundle was deliberately NOT rebuilt; `package-validate.mjs` exit 0 with the anchor matching disk on 73 recomputed hashes is stronger evidence than a fresh build, and a rebuild wipes `ds-bundle/` and `.render-check.json`. **The honest half of the grade:** item 5's named check has two halves and only the first was observed. `list_files` rose 0 to 416 to 417, but not in named batches, because everything was already verified on the green PR and there was nothing left to stream. "Project URL returns the picker" was NOT observed; that seat has no browser and the `_ds_needs_recompile` sentinel is still armed, so the app has not yet rebuilt its card index. Met is a claim about the bundle arriving intact and anchored, not a claim that anyone has looked at the picker; the operator walk is owed and is recorded as unrun. Also unrun: the post-merge check-run on `d4f7b61` was `in_progress` at close time. `[FONT_MISSING]` carried unchanged and is not a new finding, the shipped CSS naming "Inter Variable" and "Cascadia Mono" while Inter static and Plex Mono ship and render. Close `_inbox/2026-08-18_b86u_close.json`; WDLL finish card re-graded in place. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: three legs, in order, and do not start leg 3 before leg 1 is filed. You have credentials the previous seat did not; that is the only reason this is yours. | nick |
| A-069 | 2026-08-18 | **G-87 CLOSED, PR open not merged.** Kit PR **#6** on branch `g87/constraint-gate-sees-design-sync`, head `830fbe3e007e05f2357b9da0a330f6a525469248`, base `d4f7b61`, check-run `test` conclusion `success` polled to completion. Not merged: leg 1 carried an explicit operator greenlight for PR #5 and this leg carried none. `walk()` skipped `ds-bundle`, `.ds-sync` and `.design-sync` by directory NAME at the root; the skip is now by PATH and names the generated paths INSIDE `.design-sync` (`.cache`, `learnings`, `node_modules`) rather than the directory. **76 authored files now enter the scans that could not see them**: `conventions.md`, `config.json`, `NOTES.md` and the 73 previews, verified directly with zero `.cache` leakage. Gate 4 is not re-opened because `ds-bundle/` and `.ds-sync/` stay skipped and no `.css` exists anywhere under `.design-sync`. **Every path on the skip list is also in `.gitignore`, and that is load-bearing:** a gitignored path exists locally and NOT in a fresh CI clone, so a scan that walks one reports violations only one environment can ever see; `.design-sync/learnings` was listed for that reason though it does not exist on disk. **Two harness gaps found, and either would have made this row unprovable.** `prove-gates.mjs` never copied `.design-sync` into its scratch, so a violation planted there could not have fired for want of the file; and the scratch is a plain tmpdir, so the new gitignore guard would have reported UNRUN and passed. Both fixed: the directory is copied filtered, and the scratch is `git init`ed. **27 of 27 injected violations caught and named (was 24), 1 of 1 legitimate-payload controls stayed green, exit 0.** The two A-067 cases use the FILE PATH as their expect string, not the generic failure banner, because the requirement is red AND named. Added beyond the row: a **divergence pair** planting one stylesheet payload, held in a single constant so "the same bytes" is a fact about the code, under `ds-bundle/` where it must stay green and under `.design-sync/` where it must be caught, because a single case proves a verdict while a pair proves the boundary sits where it is claimed; and a **root-cause guard** asserting everything skipped is gitignored, since the list was not wrong when written, it went STALE and nothing was watching. **Counter-verified rather than trusted:** reverting the fix on a backup sent both new cases SILENT and the harness to exit 1 at 23 of 26, which is the only way to know the cases are about the real defect. `toolOutputSkipped()` still reports, with a CONDITIONAL assertion (if a declared path exists, the walk must have refused it) because an unconditional non-empty assertion would only ever pass on a developer's machine. Preflight that mattered: all seven needles grepped CLEAN across `.design-sync` and the gate-1a token pattern checked against `conventions.md` before any edit, because a fix that turns CI red on legitimate content is not a fix; baseline held at 73 pass either side, 74 with the new guard. Close `_inbox/2026-08-18_g87_close.json`. Named not built: the inverse guard, that no gitignored directory is silently walked, which needs a `git ls-files` reconciliation larger than A-067 scoped. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: three legs, in order. Pass/fail instrument is the injected-violation harness, not a passing suite. | nick (row opened); planner (instrument) |
| A-070 | 2026-08-18 | **G-88 ADDED and SCOPED. Design into the apps.** Lane B, housing `smartcity-dashboards` primarily. **The premise, and it is the whole shape of the row: the kit is React and THE PRODUCTS ARE NOT.** All three product repos are static `web/` plus a Node server, no React, no JSX, no bundler, `pg` the only dependency, verified at A-062. Claude Design emits React that imports the kit, and that cannot be dropped into the apps. **It does not need to be.** The kit adds ZERO styling: gate 4 enforces that the package styles nothing, and the vendored `sc-kit.css` and `shell.css` are byte-identical to the product's own by git blob hash. Every kit component is a typed wrapper over classes that already exist in the shipping stylesheet, 73 components covering 109 of 109 classes, so a design composed ONLY of kit components is by construction expressible as the exact class markup the product already serves. The translation React-composition to static HTML is mechanical and lossless in one direction, and that direction is the only one that ships. Do NOT adopt React in the products. Do NOT invent a build step. **Do not let a design ship a class that is not in the 109 — that last clause is the real gate.** A design needing a class that does not exist means shipping the CSS in the product FIRST and re-vendoring, never hand-writing it into a screen. **Order of work, and the first three are prerequisites rather than setup.** (1) Ship the three missing CSS families into `smartcity-dashboards`: `mx*` applicability matrix and `cite` code citation, two of the five 30b section 3.1 calls load-bearing, plus `atomchip` from 6.3. None has CSS in any shipped stylesheet, so until they ship a design agent drawing Plan Review will invent all three, which is precisely the failure the wrapper exists to prevent. Carried as F-4 through two closes; it blocks the design pass, not the reverse. (2) Re-vendor into the kit, rebuild, `package-validate.mjs` exit 0, re-sync the delta with sentinel first and `_ds_sync.json` last and alone, wrapping the three new families as components so the 109-of-109 claim stays true against a BIGGER denominator, and STATE the new denominator. (3) Fingerprint `/shell.css` or give it cache headers, BEFORE the design pass and not after: it ships with no `cache-control` and no fingerprint, so a returning browser holds a stale stylesheet across every CSS deploy shipped so far and every design change landed would be invisible to a returning operator. Queued since A-061 and never built. (4) Design the screens in the Design project from kit components only. (5) Translate to static `web/` markup, ONE PR PER SURFACE, each stating which classes it used and asserting none is outside the vendored stylesheet. (6) Deploy and verify on the deployed surface, because a grade is a live probe and never a merged PR; probe with GET, HEAD returns 404 on this service. **WDLL required before implementation**, numbered acceptance items each checkable by a specific observation, operator-approved. Four read-only investigations fanned at scoping time: the three CSS families against 30b, the `shell.css` cache path and its returning-browser probe, the React-to-static translation boundary and where it breaks, and the surface inventory. **Gate finding, reported not ruled:** the canon gate and the dispatch-template gate both fired on ONE of four near-identical read-only investigator prompts, the one that happened to name a repo in `owner/name` form. Both key on dispatch SHAPE, not on whether the agent writes anything, so a read-only prompt phrased with a clone path passes and the same prompt phrased with a repo name does not. Adding the sanctioned `CANON_OVERRIDE` made the brief dispatch-shaped and triggered the second gate the siblings never hit. Complied with rather than overridden a second time. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: merge then fan agents to do leg 3. | nick (row opened); planner (scope) |
| A-071 | 2026-08-18 | **G-88 WDLL APPROVED. Three rulings taken. Three executors dispatched.** WDLL `_inbox/2026-08-18_g88_design_into_apps_WDLL.md`, eleven acceptance items, operator-approved 2026-08-18. **Four read-only investigations ran before the card was written and THREE OF THEM CONTRADICTED A PREMISE THE ROW WAS SCOPED ON**, which is the finding of this row so far. (1) The CSS for all four families already exists, fully written and token-only, inside 30b's own `<style>` block, so item 2 is a PORT of roughly 26 rules needing **zero new tokens**, and it is a Dashboards PR rather than the three-repo `sc-kit.css` ruling it could have been. No product markup references any of the twelve classes, so there is no unstyled-markup cleanup leg. (2) **The React-to-static translation is NOT lossless.** A kit composition emits zero `id`, zero `hidden` branches and zero `data-*` hooks, while the product carries 94 ids and `app.js` makes 33 `getElementById` calls against them. `test/_markup.mjs` normalises away exactly `id`, `hidden` and every non-class attribute before comparing, so the nineteen green parity tests were silent about the gap BY CONSTRUCTION and were never evidence either way. Same failure shape as the G-86 `cardMode` blind spot: the instrument does not look at the thing that broke. Item 7 is new, is not in the dispatch, and exists because a screen that renders perfectly and cannot be driven passes every gate in both repos. (3) **The shell.css staleness premise is FALSE, measured** with a real Chrome on a persistent profile against the live service across three scenarios including a full browser restart and an in-session deploy: the returning browser fetched fresh CSS every time. With no `Last-Modified` there is no basis for heuristic freshness, so Chrome revalidates, and with no validator that degrades to a full GET. The defect is inverted from the one recorded at A-061, never stale and 145 KB re-transferred per navigation with a `304` structurally impossible, so **item 8 is re-sequenced parallel and non-blocking** and the design pass does not wait on it. (4) One-PR-per-surface is LOGICAL not physical: all fifteen surfaces share one `index.html` and one unpartitioned `shell.css`, so PRs merge serially bottom-of-file upward. **Operator rulings, each because an executor would otherwise have to guess.** G1: the 12px type floor HOLDS, so 30b's own sub-12px declarations (`.cite em` 11px, `.cite.model .corpus` 10px, `.mxgroup .lic` 10px) are raised to 12px, consistent with the product already shipping `.prov` at 13px where 30b says 11px; consequence accepted and named, 30b's specimens stop matching what ships and 30b is now the looser document. G2: **30b governs the CSS and 30c governs atomchip markup**, resolving five undeclared divergences including `.mxrow` 132px versus 108px; 30b carries no atomchip specimens at all, and the dispatch's "30b section 6.3" pointer was the wrong doc. G6: a **fourth family ships**, `finding`/`basisline`/`meter`, because it is what a matrix and a citation compose INTO and shipping the parts without the whole leaves the design agent to invent the assembly. **Denominator is recounted, never forecast:** the three investigations produced 117, 128 and 138 by different methods, the WDLL records 138 as an estimate explicitly not as the acceptance value, and the `SHIPPED.size === 109` pin failing on merge is the gate working. Dispatches COMPILED, three of them, lanes **B88-2** (port the families), **B88-3** (harden the class gate BEFORE screens ship, since its rule does not strip CSS comments and an injected `class="hidden"` does not fire today), **B88-8** (the cache contract, whose third probe leg — a stale ETag must return 200 with a DIFFERENT etag — is the only one that catches the constant-ETag implementation that would cause permanent staleness). Each executor works its own clone and none merges; verification does not delegate below the planner. **Tooling finding:** `scripts/dispatch.mjs` requires a row id followed by one of the add-verbs `ADDED`, `ADD` or `NEW`, and A-070 declared G-88 as `SCOPED`, which the row scan rejects; A-070 amended to `ADDED and SCOPED` before compiling. A-067 declared G-87 the same way and would have failed identically had G-87 been dispatched rather than planner-executed. Carried out of scope, named not built: `.prov.stale` missing from a shipped load-bearing component so a stale source renders identical to a current one, `roster-lens` shipped and defined nowhere, the forbidden-string gate not scanning CSS on the card that adds CSS, and the runtime extractor blind to classes passed as function arguments. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: approved. | nick (approval); planner (rulings taken to the operator, card, dispatches) |
| A-072 | 2026-08-19 | **G-89 ADDED. Kill the lens flash.** Lane B, housing `smartcity-dashboards`. **Every deep link into this product paints the wrong screen first.** Operator found it walking the deployed surface and confirmed the diagnosis. Mechanism, all four facts verified at source: `web/index.html` hardcodes `<section class="lens on" id="lens-city-manager">`, so Overview is ON in the static document; `shell.css` hides lenses with `.lens { display: none }` and shows with `.lens.on`; the nav items are real `<a href="/?lens=...">` links so every click is a FULL PAGE NAVIGATION; and `app.js` loads as `<script type="module">`, which is deferred by definition and therefore runs after the document parses. So the browser paints Overview, which is the lens carrying real content, and only then does `applyLens()` switch to the requested lens. On a not-built lens the operator sees a populated screen collapse into an honest-empty one, which reads as a broken deploy. **NOT caused by the G-88 deploy, and that was established rather than asserted:** `index.html` and `app.js` are byte-identical across `cdfca39..6a4580d`, the `class="lens on"` count is 1 on both sides, and revision `00017-vx4` was tagged `pre` at 0% traffic so the operator could A/B the pre-deploy revision directly and reproduce it there. The deploy plausibly WIDENED the window without creating it: `shell.css` grew 27 KB to 37.6 KB and is render-blocking, and the first load after the deploy had a cold cache on every asset because the service had never sent a validator before. Both delay `app.js` relative to first paint. **The fix cannot be CSS alone and cannot be a script move.** A module script is always deferred, so no reordering makes it run before paint, and CSS cannot read a query string. The shape that works without a build step, which this repo does not have and should not gain: an inline classless script in `<head>` that reads `location.search` and stamps `data-lens` on the root element, plus ONE rule keying visibility off that attribute rather than off a class JS adds later, so visibility is decided before first paint and the flash is structurally impossible rather than merely faster. Attribute-based, so the G-88 item 3 class gate stays satisfied. **Done means the flash cannot recur, not that it looks better:** the acceptance instrument is a probe that fails on the CURRENT build, since a fix nobody watched fail is the pattern this program keeps paying for. Related defect to fold in or row separately: the static document's default `.on` is itself the bug surface, and any fix must keep every lens reachable with JS disabled or state plainly that it does not. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: found it on the deployed surface, confirmed the diagnosis. | nick (found); planner (diagnosis, scope) |
| A-073 | 2026-08-19 | **G-89 CLOSED. The lens flash is dead, deployed, and proven by a probe that was watched failing.** Dashboards PR **#23** merged squash `ecfae70ba7e0ea308f74c8589e1ac6713cef7e86`, pre-merge check-run `test` conclusion string `success` on head `e01c192847475c7a0e82b17ea85b5a4c954cba16`. Deployed via `gcloud run deploy --source .` as tag `g89b` with `--no-traffic`, smoked, then shifted: revision **`smartcity-dashboards-00021-yap` @100%**, with `00017-vx4` left tagged `pre` at 0% as the operator required. **The acceptance is a 30-target browser walk with `/app.js` aborted at the route level**, which reproduces the pre-paint state with no race: on `00018-kiw` correct lens **2 of 30**, 28 mismatches, and the distinct visible set across ALL 28 was exactly `["lens-city-manager"]`; on `00021-yap` correct lens **30 of 30**, zero mismatches. The two that passed before were the two `?lens=city-manager` requests, one per pack. **The settled render did not change**: the unblocked walk, diffed target by target, shows 0 of 30 surfaces differing in visible lens, `.basis` count, `.state` count, DOM id count or pill text, which is exactly what a first-paint fix should look like. Edge cases on the deployed surface **9 of 9**: unknown lens, unknown work, unknown tab, empty lens value and no query all fall back to Overview rather than painting nothing, and **JS disabled is exactly 1 of 15 sections, identical before and after**, because the hide rules gate on attribute PRESENCE rather than value. **The fix**: an inline classless `<script>` in `<head>` resolves the query before the parser reaches the body and stamps `data-surface`, `data-tab` and `data-atab` on the root; `shell.css` keys visibility off those. 27 enumerated selectors. Specificity was COMPUTED rather than argued: all 230 rules parsed, the 68 declaring `display` filtered to those that could reach a `.lens` section, exactly 2 survive; `html[data-surface] .lens` at (0,2,1) beats `.lens.on` at (0,2,0) without depending on source order, and the show rule at (1,1,1) beats both. No `!important`, so `hidden-rule.test.mjs` check 4 still holds. **Two rulings, both on divergences the executor surfaced rather than absorbed. R1: the attribute ships as `data-surface`, not the `data-lens` A-072 wrote** — `data-lens` already exists in this document on the fifteen nav anchors carrying a DIFFERENT value vocabulary (bare `finance`, where the root needs `lens-finance`), and two vocabularies under one attribute name is the naming trap; A-072's wording was planner scoping language, not an operator ruling on the name. **R2: the fix extends to the six DS tabs and three Assets tabs**, because `#tab-pipeline` and `#atab-inventory` carry the same static `on` class, so `?lens=development-services&tab=review` — this row's own acceptance input — would have painted the right lens and then still swapped a populated Pipeline table out for Review. **The planner mutation-tested the instrument rather than trusting it**: deleting the head script fires it, deleting one show selector fires it twice with Finance painting NOTHING (`actual: []`), and the paired-control divergence test fires in BOTH directions naming the exact mismatch. One planner mutation removed the selector from a CSS COMMENT and produced a false all-clear, caught by echoing the byte count it claimed to remove. `infra.md` stops pinning a serving revision — it carried `00001-92j` for eighteen revisions after that went false — and now carries the command plus a dated observation, with a test asserting both halves. Close `_inbox/2026-08-19_b89_close.json`; walks `_inbox/2026-08-19_g89_prefix_walk_blocked.json` and `_inbox/2026-08-19_g89_postfix_walk_blocked.json`. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: three cards, any order you judge best; G-89 first, it is what a prospect sees. | nick (found the defect); planner (rulings, verification, merge, deploy) |
| A-074 | 2026-08-19 | **G-88 items 4, 5 and 7 CLOSED. Items 6, 9 and 10 HELD, not dropped.** **Item 4, the recount: 139**, with its counting rule beside it — distinct class-selector tokens across `vendor/sc-kit.css` and `vendor/shell.css`, CRLF-normalized, CSS comments stripped first; state modifiers and element-scoped children count, pseudo-classes and attribute selectors do not. 109 before, **30 added, 0 removed**, and BOTH sides were measured (`git show HEAD:vendor/*.css` for the before set) rather than one derived by subtraction. **It reconciles the three failed estimates exactly**: the scoping investigations predicted 117, 128 and 138 by three different methods and all three were wrong; 139 is 109 plus 19 for the three named families plus 11 for the finding family, where the eleventh token is the meter fill `f` — which the css-families investigation flagged in a parenthesis and then dropped from its own headline total. The entire discrepancy lived between a parenthesis and a sum. The `SHIPPED.size === 109` pin failing on re-vendor was the gate working: it had been passing on a stale copy of `shell.css`, which is a gate being lied to rather than a gate holding. **Item 5**: nine components in `src/evidence.tsx` cover all thirty classes; components **73 to 82**, previews **73 to 82**, coverage restated **139 of 139** with its denominator in `README.md`; no exclusion set, no weakened assertion, no class parked as structural. **The three inversion traps hold at the TYPE, and the planner verified each by attempting the violation**: `ModelCite` rejects `children` (TS2322 against `Omit<AnchorBase, "children">`) so the licensed-model-code leak the missing slot exists to prevent is structurally impossible; `MatrixRow` requires `applicability` (TS2741) so an unreviewed row cannot read as clean by omission; `Confidence` rejects a bare value (TS2353), and **no `Meter` is exported at all**. Kit PR **#7** merged squash `d6bd9a1d7e29a0306fd5b331282a0483ef110e61`; `npm test` **81 of 81 with `SC_DASHBOARDS_DIR` set so vendor-parity arm B actually ran**; `prove:gates` **31 of 31** injections caught and named plus 1 of 1 control green; and `prove:gates` **now runs in CI**, where previously the only instrument that proves a gate can fire ran only when someone remembered. **The DesignSync re-sync is complete**: bundle rebuilt at kit main, `package-validate.mjs` exit 0 with 82 of 82 previews rendering cleanly and the anchor matching disk on 82 recomputed hashes; the delta was MEASURED against a sha256 snapshot of the previous bundle rather than assumed — **45 added, 5 changed, 0 removed, 226,101 bytes**; sentinel first and alone, 49 content files in three chunks sized by BYTES (51,684 / 79,393 / 72,242), no deletes, **remote listed and reconciled BEFORE the anchor was armed**, sentinel re-armed, `_ds_sync.json` written last in its own call; **zero write failures, zero retries**; remote went **417 to 462 planner-managed plus 2 app-generated** (`_ds_manifest.json`, `_adherence.oxlintrc.json`) for 464 total. One unexplained delta was resolved before uploading rather than waved through: `Basis.prompt.md` changed because the generator appended a `## Related` cross-reference naming `BasisLine`, established by fetching the remote copy and diffing it. **Item 7 is the item that had no instrument behind it, and its number is the justification: 32 of the 70 addressed ids can be deleted from `web/index.html` with the entire pre-existing 199-test suite still green** — measured by 70 delete-run-restore iterations with the file hash verified, not inferred; a literal grep said 36 and was wrong in both directions; all six Development-services tab ids and the entire stage-escape control were uncovered. Sets: **70 required ids** (43 via `getElementById`, 1 via `#ds-metrics`, 24 via `el.id` route comparisons, 3 via ARIA IDREFs), **14 hidden branches**, **12 behaviour hooks** (10 script-read UNION 2 stylesheet-read MINUS script-created). Dashboards PR **#24** merged squash `e11155246196a82224e4d291d53ba63bd7735849` at **219 of 219**; it ships zero served bytes, so no second deploy is owed. Folds delivered: the second weaker copy of the class rule is gone, extracted to `src/served-surface.mjs` with both callers using it; the forbidden-string gate now scans `.css` and has its first firing arm; and the city-name gate hole was proven by injection rather than by reading — lowercase `bastrop` in `index.html` AND `"Bastrop"` in `app.js` both shipped green through 209 tests — then fixed. **Items 6, 9 and 10 are HELD.** The mission is drafted and compiles. Item 6 depends on item 5, which only completed at the end of this session, and dispatching a long serial design pass that could not be supervised to completion inside the remaining budget is the nested-fan orphan trap. Scaling the card down was not a call to make silently. Close `_inbox/2026-08-19_lane_b_close.json` plus `_inbox/2026-08-19_b89_close.json`, `_inbox/2026-08-19_b8845_close.json` and `_inbox/2026-08-19_b887_close.json`. Do not start G-52. Do not fill G-24. Live Bastrop no-touch. L26 untouched. | Operator: fan executors, verify their work yourself, merge, deploy, and report. | nick; planner (verification, merges, deploy, sync) |
| A-075 | 2026-08-19 | **Three process findings, each measured, each with teeth beyond this row.** **(1) A green check-run plus a `CLEAN` merge state is NOT a merge gate**, demonstrated twice inside one hour. A check-run conclusion is a statement about the PR's own tree, and `CLEAN` is a statement about textual conflict; neither says anything about the tree that exists after the merge, and none of the three SmartCity repos requires branches to be up to date before merging. Merging G-89 broke PR #24 (218 tests, 215 pass, **3 fail**, from two PRs that share no source file) and simultaneously invalidated kit PR #7, whose CI checks `smartcity-dashboards` out at `ref: main` so its green was a fact about a world that no longer existed. Both would have shipped red on their own greens. The standing AGENT_CONTRACT rule — merge only on the conclusion STRING — is necessary and is not sufficient; the addition is to re-green against the CURRENT base before merging, and for a cross-repo consumer the base that matters is every repo its CI reads, not only its own. The preferred mechanization is branch protection's up-to-date requirement, because a control that depends on the merging agent remembering is the shape DEV_PROCESS rule 0 exists to name. Environment note: `git rebase` is blocked in this environment's Bash tool, so `gh api -X PUT repos/<owner>/<repo>/pulls/<n>/update-branch` is the form to reach for. Detail `_inbox/2026-08-19_finding_green_ci_plus_clean_is_not_a_merge_gate.md`. **(2) WDLL item 10's acceptance walk cannot see what it is written to check.** The served document is byte-identical across every lens and every pack — six URL forms, one sha256 — because `index.html` is one static document carrying every lens inline and the pack differences arrive later through `/api/*`. Item 10 as written is thirteen identical GETs and then thirteen more, and would return the same result on a redesigned build and an untouched one. Replaced with three legs: a deployed-bytes hash against the merged tree, pack checks moved to `/api/*` where the packs actually differ, and a real browser walk, which is the only leg that can grade a design pass. Precedent for re-shaping an acceptance item on a measurement: item 8 was re-sequenced at A-071 and the operator accepted. Detail `_inbox/2026-08-19_g88_item10_walk_is_vacuous.md`. **(3) The two defects that mattered most were found by planner verification AFTER CI was green, and both were in an INSTRUMENT rather than in a product.** The addressability gate's created-by-script derivation recognised `X.dataset.Y =` but not `setAttribute("data-...")`, so `data-surface` read as missing while `data-tab` and `data-atab` escaped only because they already exist in static markup — passing by luck rather than by rule. And the gate unioned the served documents with the BAKE SOURCE, so detaching `data-disposition` from `web/index.html` alone left it green at 10 pass and 0 fail while the 446-element connections register lost its entire severity colouring. Both were closed by widening a DERIVATION rather than by adding an exclusion, plus a pinned excused set so the derivation cannot self-heal into silence. The second fix also made `web/index.html` an asserted FIXED POINT of the bake, closing a hole the translation-boundary investigation named and nothing covered: the bake output was never byte-asserted, so a stale bake shipped and passed every test in the repo, in either direction. Two more of the same class: the kit's injection harness case labelled `vendor parity arm B` was actually proving arm A, and arm B — the only guardrail that can see the PRODUCT move, and the one that went red for real this session — carried no injection at all; and `.design-sync/conventions.md`, which ships verbatim inside the README uploaded to the Design project, was still telling the design agent that the four families had no CSS and to draw Plan Review without them. **Carried, named, not built:** `.prov.stale` is absent from every shipped stylesheet and every component in both repos — grep for `stale` returns nothing — so a stale source renders identical to a current one; rowed separately rather than folded, because shipping it is a three-step cross-repo chain and a drive-by would land half of it. The kit's counting rule and the product's counting rule remain two implementations of one rule with no divergence test, which is the CTRL-1 shape. `roster-lens`, and the reader-less `data-home-table` (70 occurrences) and `data-source` hooks, fold into the design pass. **Owed by the OPERATOR and still recorded as unrun:** the Design picker walk at `https://claude.ai/design/p/f5e5465e-943f-4f68-b52f-608925bc07b0`, now against a 464-file bundle with the sentinel armed. | Operator: when a card tells you WHY, check the why before you build the what. | planner |
| A-076 | 2026-08-19 | **G-90 ADDED and G-91 ADDED. Template city gets built out. Three operator rulings, and the first one reverses a constraint that has travelled in every Lane B dispatch for weeks.** Build sheet `_inbox/2026-08-19_template_city_lens_build_sheet.md`, operator-approved 2026-08-19. **The correction that opens this: `_inbox/2026-08-17_g18_shell_homes.md` inventoried 67 live Bastrop staff jobs and gave each a home, and roughly forty carry the disposition `Not built`, written to mean THIS SURFACE DOES NOT EXIST YET. Across three agent handoffs it hardened into THIS SURFACE IS MEANT TO BE EMPTY, and by 2026-08-19 it was travelling as a hard dispatch constraint — "the sixteen honest-empty states STAY empty" — which was faithfully preserved through G-88 and G-89. The register was never wrong; its disposition column was read as a destination rather than as a starting state, and the operator's actual intent (inventory Bastrop, then use it as the guide to build the others) was carried in no field any handoff read.** **RULING 1: `not built` moves down one level.** Today it describes the SURFACE. From here the surface exists and honest absence is a statement about SOURCES: the lens renders its designed shape, and what is honest is which adapter kinds the pack has granted, per region, with its basis. The product currently cannot distinguish "we did not build Parks" from "your city has no Parks data" and those are different sentences to a customer. Unchanged: no invented freshness, no `last sync`/`last read`/`last updated`, every empty region keeps a `.basis` line, no real city asserted as content, `sc-kit.css` byte-identical across three repos, 12px floor, live Bastrop no-touch. **RULING 2: match the live department roster and expect it to GROW.** Cities have different org charts; forcing Bastrop's departments into a smaller set risks adoption. The lens roster is a growing template library, not a fixed five, and departments Bastrop lacks arrive as new template lenses rather than as reshapes. Taxonomy note the ruling had to settle: "the live seven" is ambiguous, because Bastrop has the `/departments/*` page family (admin, code-enforcement, community, court, development, infrastructure, parks) AND a seven-item operational nav (Overview, Finance, Development, Emergency, Operations, Compass, Prophecy), which are an org chart and a set of consoles respectively. Resolved to the UNION, ten department lenses, because collapsing any two is the forcing this ruling rejects and cutting now is cheap while re-splitting later is not. **RULING 3: demo data on every lens, ONE badge.** Fixture data everywhere so the UX can be worked; the environment chip stays a single `Demo` in the top-left, never per region, per card or per number. Provenance chips and basis lines carry per-region truth; the badge carries the environment. **Evidence base, measured this session rather than taken from the register.** Live `smartcityos.io` is an SPA — every route returns the same 3,624-byte document, one sha256 across `/`, `/overview`, `/fleet`, `/police`, `/citizen` — so the inventory came from the shipped bundle `/assets/index-kGj7uMs4.js` (5,578,225 bytes): **138 routes and 177 distinct `/api/` endpoints across 11 vendor families**. Development services is **MyGov ×16** (`permits`, `permits/stats`, `inspections`, `work-orders` plus `daily-queue`/`geo-clusters`/`sla`/`stats`, `code-violations` ×2, `business-licenses`, `reviews/summary`, `sync-status` ×3); Police is Spireon ×7 plus Verkada ×8 plus dispatch and emergency; Fire is FirstDue ×6 plus VFD ×5 plus seven county flood/wildfire/weather layers; Fleet is Samsara ×5; Public works is PowerBI ×4 plus GoTo ×5; Finance is OpenGov ×8 plus permit-revenue ×3 plus scenarios and baselines. **These are evidence of what data exists, NOT an instruction to connect a feed; no adapter is granted by this work.** **G-90, shell function parity.** Eight functions live Bastrop has and the Dashboards shell lacks, captured as FUNCTIONS rather than layout: theme toggle light/dark (live persists to `localStorage` and defaults dark; Dashboards hardcodes `data-theme="dark"` on the root), account menu (My Account / My Profile / Account Settings / Support), sign-in-out and session, notifications with an honest empty state, tenant branding, a real record search (today a stub carrying a `Not built` badge), help/support, and feedback. Theme must set `data-theme` on the ROOT and never a page-level provider; light is a first-class theme. **G-91, the fixture-pack seam.** The mechanism already exists and is proven, so this is an extension and not a new system: `src/fixtures.mjs` is a seeded deterministic generator (`fnv1a` seed, `mulberry32` PRNG) carrying two content guards, `assertNoRealWorldContent` and `assertDeclaredVocabulary`, which must keep applying to every new domain; `src/city-pack.mjs` already declares `cityKey`/`accessPolicy`/`environment`/`seal`/`generatesFixtures`/`grantedAdapters`. It generates ONE domain today. G-91 adds one generator per domain on the same seam and the same pack seed, so a city's data is stable across reloads and reproducible, and swapping a city is swapping the pack. Three identities hold: `template-city` generates, `empty-city` generates nothing and stays the regression target because with zero records every pill renders quiet and the tension mechanism switches off, `fixture-city` is tenant-private. `src/city-pack.mjs` must keep throwing on live Bastrop. **G-91 also carries the gate re-scope ruling 1 forces**, because the tests that currently enforce honest-empty as a PERMANENT state (the `$0`-never-appears assertion, the sixteen `.state` blocks, the honest-empty walk) must become per-pack assertions or they will block every populated lens. **Adapter catalog is short by three, measured:** `src/adapters.mjs` declares seven kinds (`mygov`, `samsara`, `opengov`, `esri`, `municode`, `firstdue`, `verkada`) against eleven live vendor families; missing `spireon` (police vehicles), `goto` (phones), `powerbi` (CIP and reporting), so the nav footer's `0 of 7 sources granted` is a counted denominator that is short against reality and corrects itself once the catalog grows. **Two corrections the operator's own walk of the deployed surface produced.** Development services currently carries a `Review` tab while Plan review is a separate Work lens; DS mirrors what MyGov already runs and Plan review is the native console that aspirationally replaces it and is a long way from doing so, so the `Review` tab leaves DS and DS tabs become Pipeline, Place, Inspections, Work orders, Code enforcement, Licenses. And Court gets its own lens, which is why the Parks shell was explaining municipal court in a sentence that traces to a real register row pairing "Parks and Courts" but reads as a non sequitur on the Parks page. **Wave 1 is G-90 and G-91 in parallel because their file sets are disjoint** — G-91 is `src/` only and touches no served asset, G-90 owns `web/`. Wave 2 fans department lenses wide once a generator exists per domain. Merges stay SERIAL per repo regardless, because all lenses share one `web/index.html` and one unpartitioned `web/shell.css`. **Open at build time, operator-flagged:** code enforcement as a DS tab or a department lens or both; records search as a real register or honestly still Not built; Assets confirmed to carry fixture data on `template-city` while real packs stay at zero, which lifts a constraint that has been hard in every dispatch for weeks. Do not connect any feed. Do not start G-52. Live Bastrop stays no-touch and read-only. L26 untouched. | Operator: my intent got buried through the agent handoffs; inventory Bastrop, then use it as the guide to build the others. Approved, go fan. | nick (rulings); planner (evidence, build sheet, scope) |
| A-077 | 2026-08-19 | **G-90 and G-91 CLOSED and DEPLOYED. Wave 1 of the template-city build is live.** Serving revision **`smartcity-dashboards-00023-yex` @100%**, dashboards main **`c7d7980b98752fe6625e07da1e4e9b9921f67950`**, kit main **`17eccfade057c0f8a835b8731be834cd4b828166`**. Combined suite **273 of 273** on the product, **86 of 86** on the kit with vendor-parity arm B actually run. Deploy failed once on a two-character Cloud Run tag (`w1`; minimum is three) and was re-run as `wave1` — planner-owned, not escalated. **G-91, the fixture-pack seam.** PR **#25** merged squash `3d4c9e8320d2dd73aede13383d70776ad05f0222`, CI conclusion string `success`, **zero files under `web/`** so the lane boundary held. Four registered domains, each declaring its gating adapter kind: `permits-pipeline` and `work-orders` (mygov), `fleet-vehicles` (samsara), `patrol-vehicles` (spireon). **Ruling 1 turned out to be FOUR states, not two**, and that refinement is the row's most durable output: `ungranted` ("Spireon is not granted on template-city; the Patrol roster region is built and has no source") is distinct from `granted-empty`, from `no-fixture-source`, and from `not-registered` — which is the only surviving meaning of "not built". Planner-verified by flipping the grant rather than by reading: granting `spireon` on a throwaway pack takes `patrol-vehicles` from `ungranted`/0 records to `ok`/**10 records** while every other domain stays byte-identical at 14/15/14 and `grantedAdapters` stays `[]`. That is a SOURCE state and not a stub, and if the two were indistinguishable one layer down the original misreading could survive again. `empty-city` reads all four domains as `no-fixture-source`. **Two planner figures corrected by the executor, both material.** (1) `template-city.grantedAdapters` structurally CANNOT name a gating kind: `assertCityPackShape` throws `a pack that generates fixtures grants no adapter`, so the dispatch's instruction was unsatisfiable as written and, read literally, all three exemplars would have been ungranted. Resolved with a third pack axis `fixtureGrants` — adapter kind ids, no `sourceUrl`, connects nothing — leaving `grantedAdapters` `[]` on all three packs and the G-74 gate still throwing. (2) The adapter catalog goes **7 to 10, not 11**: the planner's eleventh live vendor family was `vfd`, and `/vfd` is Bastrop's own volunteer fire department route, a first-party city surface rather than a third-party adapter. Footer now returns `0 of 10 sources granted`, numerator still 0 because a demonstrated shape is not a connected source. **Guards proven structural rather than advisory**, and proven by breaking one: neutering `assertNoRealWorldContent` to a no-op turns **4 tests red**. The planner separately failed six times to hand-roll a malformed record past the seam, each attempt rejected by a differently-named invariant. Determinism measured across two separate node processes, sha256 identical. Three defects found, one pre-existing and worth carrying: `assertDeclaredVocabulary` listed `record.dueLabel` in its own allowed set, so **the field authorised itself** and any string at all passed in a field rendered on the DS queue. **A correction to the dispatch that the planner accepts:** the six `$0` assertions all read `web/index.html`, which is pack-invariant, so they cannot be made per-pack and do not block wave 2; the gate that actually needed a pack dimension was the composed-payload money rule, now widened to every domain on every pack. **G-90, shell function parity.** PR **#26** merged squash `c7d7980`, and it was UPDATED onto current main and re-greened before merge rather than merged on its own stale green. Eight functions: theme toggle, account menu, session state, notifications, tenant branding, record search, help and feedback — five built, two built as chrome with honest-disabled actions, one honestly stubbed, and `src/shell-parity.test.mjs` carries that table as walked data so a deferral and a silent omission stop looking alike. **The theme resolves in the SAME G-89 head script**, with the storage read in `try/catch` placed AFTER the surface stamps, because `getItem` throws in a partitioned context and an unguarded throw would have un-stamped `data-surface` and reintroduced G-89's flash through the door this change opened. Planner-verified on the deployed surface with `/app.js` aborted: no stored preference resolves `dark` under both a dark-preferring and a light-preferring OS; a stored `light` beats a dark OS and paints canvas `rgb(238,241,244)`; a stored `dark` beats a light OS; and `data-surface=lens-finance` in all four, so G-89 and G-90 coexist in one script without either breaking. G-89 regression clean: the 30-target blocked walk is still **30 of 30** on the new revision with zero `$0` and zero invented-freshness hits. **A near-miss worth recording:** the capability resolver's first draft echoed `FEEDBACK_DESTINATION` — a signed webhook URL — into a basis string served to anonymous browsers. Fixed so only the variable NAME travels, with a canary assertion (`SHOULD-NOT-APPEAR`) proving the payload excludes the value; planner re-verified both directions at source. **The G-90 lane concluded "no kit re-vendor owed" and that was wrong**, which the planner caught and lane **T1KIT** then proved by watching it fail: on the untouched kit clone with `SC_DASHBOARDS_DIR` set, kit main was ALREADY 80 pass / 1 fail against live product main, arm B naming `vendor/shell.css`. The reasoning "`sc-kit.css` untouched, therefore no product-line event" is correct; "therefore no kit re-vendor" does not follow, because **the kit vendors BOTH stylesheets**. That is the generalizable line for any product lane touching `web/*.css`. Kit PR **#8** merged squash `17eccfa`: re-vendored at `c7d7980`, recount **139 to 143** (`pop pop-group pop-item topmenu`, none removed, both sides measured), components and previews and gallery entries all 82 to **86**, coverage **143 of 143**, `prove:gates` 31 to **34 of 34** plus 1 of 1 control. `PopItem` has no `disabled` prop — it is `Omit`ed and `unavailable` is basis TEXT, so an entry cannot be greyed out without saying why. One markup-parity case moved and was fixed, three added, each watched going red on its own injection. Free finding: without comment-stripping the same two stylesheets return **150**, the seven phantoms being `css hidden html js md mjs test`. **Design project re-synced**, planner-owned: bundle rebuilt at kit main, `package-validate.mjs` exit 0 with 86 of 86 previews rendering and the anchor matching disk on 86 recomputed hashes; delta measured against a sha256 snapshot rather than assumed at **20 added, 4 changed, 0 removed**; sentinel first and alone, content chunked by BYTES, **remote listed and reconciled BEFORE the anchor was armed**, sentinel re-armed, `_ds_sync.json` last in its own call; zero write failures, zero retries; **462 to 482 planner-managed files** plus 2 app-generated. **Open for the operator, both flagged by executors rather than by the planner:** the footer excludes `fixtureGrants`, so `template-city` reads `0 of 10 sources granted` beside three populated regions — literally true, possibly reading as a contradiction, and the planner leans to the footer distinguishing GRANTED from DEMONSTRATED rather than collapsing them; and two Connections register rows are now partly false (the `Auth / session / notifications / theme / sign out` row bundles four jobs of which two now exist, and the disposition vocabulary is closed at six values so none fits), with the recommendation being to split the auth row into its four jobs, which is a ruling rather than an edit. **Wave 2, the department lenses, is ready to fan** on the seam G-91 left: fifteen lens regions have no domain yet, which is the deliberate exclusion and the fan. Do not connect any feed. Do not start G-52. Live Bastrop no-touch and read-only throughout. L26 untouched. | Operator: approved, go fan. | nick; planner (verification, merges, deploy, re-sync) |
| A-078 | 2026-08-19 | **G-92 ADDED and G-93 ADDED. Wave 2 of the template-city build.** Build sheet `_inbox/2026-08-19_template_city_lens_build_sheet.md`; seam shipped at G-91 (`3d4c9e8`), shell at G-90 (`c7d7980`), kit re-vendored at `17eccfa`. **G-92, the department domains.** Fifteen lens regions have no domain; wave 2 adds them on the registered seam `src/domains.mjs` + `src/domains/*.mjs`, one generator per domain, each declaring its gating adapter kind, all keyed off the same pack seed. **This row is `src/` only and ships NO markup** — the four source states (`ok` / `granted-empty` / `ungranted` / `no-fixture-source`, plus `not-registered`) already reach no pixel by design, and rendering them is a separate serial pass because all lenses share one `web/index.html` and one unpartitioned `web/shell.css`. Domains fan wide because `src/domains/*.mjs` are separate files; markup does not, and that asymmetry is the whole shape of the wave. Priority is **Development services**, because the operator's requirement is that DS match what the production Bastrop dashboard shows today, and live carries MyGov across sixteen endpoints of which only `permits` and `work-orders` are modelled. Both content guards keep applying structurally at the seam; determinism stays measurable across separate processes; `empty-city` keeps generating nothing and stays the regression target. **G-93, two operator-ruled corrections, both surfaced by executors rather than by the planner.** (1) **The footer must distinguish GRANTED from DEMONSTRATED.** `template-city` currently reads `0 of 10 sources granted` beside three populated regions, which is literally true — `fixtureGrants` is deliberately not a grant and connects nothing — but reads as a contradiction to a prospect. Operator ruled 2026-08-19 to distinguish the two rather than collapse them. (2) **The Connections register's auth row must be split into its four jobs.** The row `Auth / session / notifications / theme / sign out` bundles four jobs of which two now exist after G-90 and two do not, and the disposition vocabulary is closed at six values so no single value is true of the row. Operator ruled 2026-08-19 to split it. Connections is generated by `connectionsRegisterHtml()` in `src/shell-homes.mjs` and must be RE-BAKED, never hand-edited — `web/index.html` is now an asserted fixed point of that bake, so a hand edit fails a test by name. **Carried into both rows:** no feed is connected and no adapter is granted by any of this work; live Bastrop stays no-touch and read-only; `web/sc-kit.css` byte-identical across three repos; the 12px floor with its one named evidence-chip exception; no invented freshness and no `last sync` / `last read` / `last updated` string anywhere; every empty region keeps a `.basis` line; no real city asserted as content. Any product change to `web/*.css` triggers a kit re-vendor, because the kit vendors BOTH stylesheets — that inference failed once today and is now a standing line in every brief touching product CSS. Do not start G-52. Do not fill G-24 beyond the ruled `template-city` fixture posture. L26 untouched. | Operator: footer and auth, I agree with your lean. Good to go on wave 2. | nick (rulings); planner (scope, fan shape) |
| A-079 | 2026-08-19 | **G-92 and G-93 CLOSED and DEPLOYED. Wave 2 shipped: eleven domains, the footer's second claim, and the register split.** Dashboards main **`1b271c8c3dc7bf361c09f00a095cc8e9022a6946`**, serving **`smartcity-dashboards-00025-mam` @100%**, suite **320 of 320**. Three PRs, merged in dependency order and each re-greened against the base that had moved under it. **G-92 across two lanes.** W2DS PR **#28** merged squash `ed398fade199ee2b21e90ceae2a8aa5e651381cb` — `inspections` 21, `code-violations` 19, `business-licenses` 17, all gated `mygov`, closing the gap the operator named: Development services must match what the production Bastrop dashboard shows, and live carries MyGov across sixteen endpoints of which only two were modelled. W2DEPT PR **#27** merged squash `cfc061b4aa3f39ce9ab4fc3b494d34f05615df1a` — `police-cameras` 18 (`verkada`), `fire-apparatus` 12 (`firstdue`), `cip-projects` 16 (`powerbi`), `call-analytics` 25 (`goto`, aggregate buckets per queue per relative day, never a call). Registry is now **11 domains, 10 carrying records on `template-city`, 0 of 11 on `empty-city` all reading `no-fixture-source`**, and `patrol-vehicles` stays the single `ungranted` region because `spireon` is deliberately kept off the demonstration axis — a wave that grants everything quietly deletes the state that proves ruling 1. `grantedAdapters` still `[]` on every pack. **The vendorless finding, and it is the most useful thing wave 2 produced: Parks and Court are NOT expressible on this seam.** W2DEPT proved it at four independent gate points quoted at file and line (`fixture-seam.mjs:270-277` requires a `gatedBy` resolving to a catalogued kind and throws on both `"none"` and `"parks"`; `city-pack.mjs:317-320`; `adapters.mjs:341-342`, where the vendor id travels in every record's `kind` field; `fixture-seam.mjs:372-380`, where the only non-generating state for a built region has a basis reading `<Vendor> is not granted on <city>`), invented no kind to satisfy the shape, and pinned it as a test **written to GO RED when the seam grows a vendorless path** so the finding cannot rot. The build sheet names four lenses with "gates: none yet", so this recurs and is routed to the operator as W2DEPT-F2. **Privacy exclusions declared in the record contract rather than left to the guard:** Verkada LPR plates and persons-of-interest and counted occupancy; GoTo recordings, individual call detail and extension-to-person mapping; FirstDue crew; all money on the CIP register. Planner planted all four surveillance fields and watched each REJECTED by the seam. **G-93, both operator rulings.** PR **#29** merged squash `1b271c8`. The footer emits a second claim from the SAME resolver against the SAME denominator rather than a second implementation: `template-city` reads `0 of 10 sources granted` beside `6 of 10 demonstrated with fixture records`; `empty-city` reads `0 of 10` on both with the positive determination `this pack generates no records, so no adapter kind is demonstrated on it`. Derivation proven by growing the catalog 10 to 11 (both denominators move) and the grants 2 to 4 (only the demonstrated numerator moves) with the served assets sha256-identical in every arm. The Connections auth row split into four (auth actions `Not built`, notifications `Empty`, theme `Mounted`, sign out `Not built`) and the feedback row into three, using the existing six-value vocabulary with no value added: **70 register rows / 67 source rows, 5 / 3 addenda, 75 baked `data-home-row` occurrences live**, source rows measured as distinct `splitFrom ?? job` and never by subtraction. Bake re-run and watched failing by name on three planted hand edits. No CSS shipped, so the class vocabulary stays **143** and no kit re-vendor is owed. **The unproven leg W2FIX named was then closed by the planner:** it said the footer was proven by served bytes and never by a browser, so a rendered walk was run against the deployed surface and both claims render on both packs. **Three findings worth carrying.** The Connections page carried three hand-typed copies of a counting rule that lives in the generator, and this split is exactly the edit that would have staled all three — that page has already shipped two stale integration counts for the same reason; all five strings now bake. W2DEPT's exclusion scan first went red on a false positive because a bare `/plate/i` matches `template-city`, and the tempting fix (excluding `cityKey` from the scan) would have been the actual hole; word-bounded instead. And W2DS found two defects in its own work at CP2: an escalation rule written inline AND copied into its test (the CTRL-1 shape, where the copy keeps passing while the original drifts), and an open-work measure keyed off a status literal rather than the declared `resolved` flag. **Planner-corrected in the build sheet:** its "adapter catalog is short by three" section went stale within hours when G-91 added exactly those three, and W2DEPT caught it by checking at source — trusting it would have declared three duplicate kinds. The section is corrected rather than deleted, because it is the same failure the sheet exists to fix, and it now states that every count in a build sheet is a dated snapshot while the source of truth is the code. **Merge mechanics worth keeping:** W2DS ran a LOCAL trial merge with W2DEPT, aborted it, and wrote out the exact ten-hunk resolution — including that `fixtures.test.mjs` is NOT a union, because both lanes rewrote the same SORTED literal and a union produces two consecutive `assert.deepEqual` calls and a syntax error. The planner resolved by hand to the sorted eleven-entry set and hit two of its own errors doing it: an `\n`-anchored marker regex against a CRLF checkout that silently matched nothing (third CRLF miss of the day, and it is in the planner's own memory file), and a replacement that left the original list's tail behind giving seventeen entries where eleven were expected. Both caught by the suite, not by the planner. **NOT shipped, stated plainly: no department lens BODY renders its domain yet.** The eleven domains reach no pixel; the visible wave-2 deltas are the footer's second claim and the register's split rows. Rendering is wave 3 and is a SERIAL markup pass, because all fifteen lenses share one `web/index.html` and one unpartitioned `web/shell.css`. Do not connect any feed. Live Bastrop no-touch and read-only throughout. Do not start G-52. L26 untouched. | Operator: footer and auth, I agree with your lean. Good to go on wave 2. | nick (rulings); planner (verification, conflict resolution, merges, deploy) |
| A-080 | 2026-08-19 | **G-94 ADDED, G-95 ADDED, G-96 ADDED. Wave 3 opens: conformance becomes a build requirement across the sellable product line.** WDLL `_inbox/2026-08-19_wave3_surfaces_and_vpat_WDLL.md`. Operator goal: every surface in Dashboards done pending QA, and a VPAT documented with proof its reports re-run against `template-city`. **Four operator decisions taken 2026-08-19:** fix the kit (contrast remediation approved as a product-line change); conformance target is **BOTH** WCAG 2.0 AA (Revised 508, federal) and WCAG 2.1 AA (ADA Title II, state and local); scope is the whole sellable line — Dashboards, Asset Management, Plan Review, Smart Files; and PDF export is IN scope because the original SmartCity has it. **The baseline was MEASURED before anything was scoped, and six of the ten "known defects" the operator's hand-off brief carried do not survive it.** axe run through real Chromium against serving revision `00025-mam` across **16 surfaces** (nine lenses, six work views, `empty-city`), counting rule stated where read — a VIOLATION is one axe rule failing on at least one surface, NODES sums failing DOM elements, and a rule counts as CONFORMANCE only if tagged `wcag2a`/`wcag2aa`/`wcag21a`/`wcag21aa`. Result as the planner measured it: two conformance rules failing, `color-contrast` 54 nodes and `scrollable-region-focusable` 4, plus `heading-order` 11 as best-practice. **THOSE FIGURES ARE TOO SMALL AND G-95 CORRECTED THEM THE SAME DAY.** The planner HAND-ENUMERATED 16 surfaces on ONE theme; G-95 DERIVED the list from the id sets `src/staff-review.mjs` exports and scanned 23 surfaces x 2 themes: `color-contrast` **1002** (light 930, dark 72), `heading-order` **30**, `scrollable-region-focusable` **8**, plus **24** findings on 2.4.2 that no automated rule reports. On the planner's own 16-surface subset G-95's scanner reproduced the planner's numbers exactly, so the instruments agree and only the SCOPE differed — the hand-built list omitted the Development-services and Assets tab panels, which are distinct rendered surfaces. **A conformance denominator is derived or it is wrong**, the same hand-declared defect class as `has_writer`, and the evidence pack states the derivation beside the number. Sixteen of sixteen surfaces clean of everything else, against an old site whose six pages failed six rules at Lighthouse 0.80 to 0.90. NOT PRESENT on the new product: `maximum-scale=1`, unnamed icon-only controls (`button-name` never fired, so G-90 built them right), missing `<main>` or bypass mechanism, nested anchor-plus-button. The brand hex `#6499aa` is not in this codebase at all. **STILL LIVE and structural: `document.title` is `SmartCity Dashboards` on every lens.** Nav items are real `<a href>` full navigations, so each lens IS a distinct page under 2.4.2, and axe cannot catch it because it scans one page at a time and a title is present. **G-94, the kit contrast pass — the one finding that is not an engineering call.** `color-contrast` fails on `.badge` and `.p-quiet.pill`, which are kit classes coloured by `--sc-*` tokens in `sc-kit.css`. That file is byte-identical across `smartcity-dashboards`, `smart-files` and `plan-review` and its own header says a repo that edits a token value has forked the system. **CORRECTED BY G-95 SAME DAY: the planner wrote that this therefore CANNOT be fixed in a Dashboards PR, and that was overstated.** `web/sc-kit.css` contains ZERO rules for `.badge` or `.p-quiet`; those rules live in `web/shell.css` at lines 162, 632 and 643 and merely REFERENCE `var(--sc-ink-3)` and `var(--sc-quiet)`, so a local re-pairing to different existing tokens was possible. The conclusion holds on its own merits and the shipped fix is not revisited — a `--sc-quiet` that fails contrast is wrong everywhere it is used, so the token was the right OWNER and a shell-local re-pairing would have left the same defect live in the other two products — but the stated reason was an impossibility asserted from a plausible mechanism without checking where the declarations actually lived. It lands in all three product repos as identical bytes and is followed by a kit re-vendor and a bundle re-sync. Every changed pair ships with its MEASURED ratio; normal text 4.5:1, large text and non-text UI 3:1. **Cross-repo atomicity is a real hazard here and is named rather than discovered:** whichever repo merges first puts the other two out of byte-identity until they follow, so the lane establishes what actually asserts that identity before touching anything. **G-95, the accessibility baseline and the gate.** The two conformance findings plus `heading-order` plus 2.4.2 titles, and then the axe scanner wired into CI failing the build at any A or AA violation with per-surface node counts in the output. The gate ships BEFORE surface rendering, because retrofitting conformance onto fifteen rendered surfaces costs multiples and this is the only chance to avoid it. Titles resolve in the existing G-89 head script rather than a second one. **G-96, the document-generation investigation, read-only.** The operator's brief said "replace or configure the PDF export path"; measured against the live bundle, **replace is the only option**. **PLANNER RATIONALE CORRECTED BY G-96 SAME DAY, and the correction is recorded rather than quietly edited because a wrong finding inside an ACR discredits the true ones.** The planner claimed the exports are rasters failing 1.1.1 and 1.4.5 as well as 504.2.2. **That is FALSE and retracted:** every application path draws VECTOR TEXT through jsPDF `.text()` (73 occurrences) and jspdf-autotable (10); html2canvas is bundled only as an optional dependency of the jsPDF `html()` plugin and has ZERO call sites, `.html(` occurring 0 times in 5,578,225 bytes. A federal reviewer opening one of these finds selectable text. **1.1.1 and 1.4.5 come OUT of the PDF rationale; replace remains the verdict on better evidence:** `/StructTreeRoot` and `/MarkInfo` occur 0 times, jsPDF's `putCatalog` writes only `/Type /Catalog`, `/Pages`, `/OpenAction` and `/PageLayout`, and jsPDF 4.2.1 has no tagged-PDF capability at any setting, with `setLanguage` and `setDocumentProperties` present but uncalled so there is no `/Lang` and no title either. The planner's token counts were also wrong for a stated reason: `jsPDF 17` and `html2canvas 3` came from `grep -oci`, which counts LINES against a nearly-single-line minified bundle; measured as occurrences with `grep -oF` they are **143 and 23**, a clean instance of DEV_PROCESS 1.2. **The site-plan PDF is SmartSite, not Plan Review**, established on four independent links including `filenameFor` in property-explorer producing exactly that filename and `render.ts:1152` building the `SP-48021-34649` id; it is server-side engine-api pdf-lib. **Its worse defect is not the missing tags:** `render.ts:373` draws tracked runs glyph by glyph, so 64.4% of extracted lines are a SINGLE CHARACTER (268 of 416), meaning a tagged version would pass an automated check and still read one letter at a time — the strongest argument in the investigation for why an automated gate is necessary and not sufficient. **Scope-map correction:** Dashboards emits no documents but MOUNTS SmartSite in an iframe, so a Dashboards user reaches an untagged PDF without leaving the product. **Recommendation: headless Chromium print-to-PDF from semantic HTML through one shared render service**, evidenced by four PDFs already on the operator's machine carrying the full 504.2.2 set, with the limit stated — Chromium tags what the HTML says (`/NonStruct 50`), so it makes the G-94 and G-95 semantics count twice rather than fixing conformance alone. SmartSite is NOT replaced: add `setTitle`/`setLanguage` to its three emitters, then fix the glyph runs. Sizing: 22 document types by layout or 15 by `.save()` site on SmartCity, 3 on SmartSite, 0 on the three new repos. Separately the site-plan PDF the operator holds was audited as untagged `pdf-lib`, which appears nowhere in the SmartCity bundle, so it comes from a different surface: **there are at least two PDF paths in the product line and they fail for different reasons.** G-96 investigates both, recommends a generator, and sizes the remediation. No code. **Finding carried to the operator rather than absorbed: Asset Management has no repository.** `smartcity-dashboards`, `smart-files` and `plan-review` all exist; no asset-management repo exists under any obvious name, and Assets is currently a lens inside Dashboards held at zero by G-24. One of the four surfaces in the approved VPAT scope does not exist yet. Operator agreed with the recommendation: **ship ACR revision 1 covering the three surfaces that EXIST, carrying Asset Management in the scope map explicitly as not-yet-built** rather than silently omitting it. The broader question of how Asset Management, Smart Files, SmartSite and SmartCity tie together is acknowledged as unopened and is NOT scoped here. **Still blocking the scope map: the contracts line items**, which the operator owes; nothing else waits on them. **On both standards:** the baseline already ran the `wcag21a` and `wcag21aa` tag sets and no 2.1-specific rule fired, but axe cannot settle reflow (1.4.10), orientation (1.3.4), text spacing (1.4.12), content on hover or focus (1.4.13) or status messages (4.1.3), so those are manual and are genuinely new work relative to a 2.0-only pass. Each worksheet row states which standard it answers, because a Rev508 ACR and a WCAG 2.1 ACR are different documents filled from one body of evidence. The 42-row per-criterion worksheet already in `P:/tmp/VPAT` is the right SHAPE and is reused; its CONTENT is rebuilt against the new surfaces, because it was filled against the old site and its scoring does not transfer. Do not connect any feed. Live Bastrop no-touch and read-only. Do not start G-52. L26 untouched. | Operator: fix the kit; both standards; everything we sell needs this — dashboards, asset mgt, plan review, smart files. Agreed on the Asset Management angle. | nick (decisions); planner (measurement, reconciliation, scope) |
| A-081 | 2026-08-19 | **G-97 ADDED. Render the lenses. This is the operator's actual ask and it had been deferred four times.** The operator wrote the wave-3 goal as "ALL surfaces in the smart dashboards are done pending QA" FIRST and the VPAT second; the planner fanned contrast, a CI gate and a document investigation and dispatched ZERO rendering lanes, inverting that order. Recorded plainly because the pattern matters: G-88 items 6/9/10 were held on supervision budget, wave 1 shipped the shell and the seam, wave 2 shipped eleven domains, and wave 3 opened conformance — four consecutive waves that built everything underneath the lenses and never the lenses. **The planner also mis-stated the constraint.** "All fifteen lenses share one `web/index.html` and one unpartitioned `web/shell.css`, so this is serial" is true of MERGING and false of BUILDING. Lanes build in separate clones and the planner merges serially with a rebase, which is exactly what W2DS and W2DEPT did against a shared registry file earlier the same day, conflict resolved by hand at 316 of 316. **G-97 renders the eleven registered domains onto their lenses.** Three lanes by lens group, each owning disjoint `<section>` blocks in `web/index.html` and disjoint additions to `web/shell.css`: R1 Development services, which carries the operator's stated priority that DS match what the production Bastrop dashboard shows and is the only lens with five domains (`permits-pipeline`, `work-orders`, `inspections`, `code-violations`, `business-licenses`, all `mygov`); R2 Police and Fire and EMS (`police-cameras` verkada, `patrol-vehicles` spireon and deliberately ungranted, `fire-apparatus` firstdue); R3 Fleet and Public works (`fleet-vehicles` samsara, `cip-projects` powerbi, `call-analytics` goto). **Accessibility is a merge gate inside each lane rather than a later pass**, because retrofitting it onto fifteen rendered surfaces costs multiples: sequential headings with exactly one `h1`, an accessible name on every control, every scrollable region keyboard-reachable, and both themes clean of `color-contrast` — the lever is `data-theme` on the root and NOT `prefers-color-scheme`, which is how the planner's own baseline missed 643 light-theme nodes. **The four source states must reach the pixel**, which is the whole point of ruling 1 and has not been visible until now: `ok` renders records, `ungranted` renders the region built and sourceless with its basis naming the vendor and the pack, `granted-empty` is a different sentence from `ungranted`, and `no-fixture-source` keeps every region reachable and quiet on `empty-city`. Lenses with no domain (Parks, Court, Community, Administration, Records search, People and access) render the correct source state rather than the retired page-level "not built". Standing: every `.basis` line survives, no invented freshness, one `Demo` chip, no city name as content, `web/sc-kit.css` untouched, class vocabulary 143 and any addition states the new count and triggers a planner-owned kit re-vendor. Live Bastrop no-touch. Do not start G-52. Do not fill G-24 beyond the ruled `template-city` fixture posture. L26 untouched. | Operator: you have been running four hours, how have you not shipped the lens. | nick; planner (owns the deferral) |
| A-082 | 2026-08-19 | **G-98 ADDED, G-99 ADDED, G-100 ADDED. Closing what three rendering lanes independently found.** G-97 landed: PRs **#32** (`13391e93`), **#33** (`ad6e2e24`) and **#34** (`3452b313`) merged, serving revision **`smartcity-dashboards-00031-tew` @100%**, and the lens bodies render at last — Development services 91 table rows over five MyGov regions and 86 records, Public works 35, Police 31, Fleet 20, Fire and EMS 17, none of them still reading "is named, and not built". **All three lanes independently measured the same defect and none of them absorbed it**, which is the strongest signal in the wave. **G-98, the `--sc-ok` token and the workaround it strands.** `--sc-ok` `#2F7A52` on `--sc-ok-wash` `#E3F0E8` computes **4.444:1** against the 4.5:1 required for normal text; R2's proposed `#2E7750` computes **4.623:1**, both verified independently by the planner. It is a `sc-kit.css` token, byte-identical across `smartcity-dashboards`, `smart-files` and `plan-review`, so it is the same four-PR cross-repo shape as G-94 — which fixed `--sc-ink-3` and `--sc-quiet` and left this one, because the planner's baseline never surfaced it. **The strand matters and makes this row urgent rather than tidy:** R3 avoided the failing pill by rendering a RESOLVED status quiet, which is the visual law's own ground and the first renderer to read the `resolved` flag, and it deliberately wrote a test that computes the ratio live so it **FAILS once the kit is fixed**. That is a self-retiring workaround and it means landing the token change alone turns main red on purpose. So G-98 carries the token in four repos AND retires the workaround AND updates that test in one coherent change. It also settles the cost all three lanes named: one severity vocabulary currently has **two renderings** across the product line, because Development services still renders `ready-to-issue` through `p-ok` while the department lenses render resolved quiet. **G-99, the gate is not yet a control.** Two independent divergence tests: R3 measured the Development services lens at **0 conformance nodes at 1440x900 and 2 at 1440x3000**, R2 measured **0 at 1440x900 and 2 at 1440x1200**, and in every arm the node count equalled the IN-VIEWPORT count. axe's `color-contrast` only evaluates what is in the viewport, the gate runs at 900, so **"0 conformance nodes over 46 scans" is an above-the-fold figure and not an absolute one** — material to the ACR, because an auditor does not scan at the planner's viewport. Worse, R2 measured the gate **failing locally on Windows with 44 unresolved nodes and passing on Linux CI on the same SHA**; G-95's own adjudication records the environment dependence and measured it in the OPPOSITE direction. A ceiling that ranges 0 to 44 across two operating systems is a control-design question, not a tuning one, and it is the single biggest threat to the VPAT's credibility because the report has to survive someone else running the scan. **G-100, three shipped markup claims that are now false.** The nav badges at `web/index.html:289` and `:293` still read "Not built", and the Overview register at `:366-371` still files Public works and Fleet under "Roster, not yet built", for lenses that now render data. Neither R2 nor R3 owned those lines and both routed them rather than stretching scope, which was correct. **Merge order is forced:** G-98 and G-100 both touch `web/index.html`, so G-98 merges first and G-100 rebases. G-99 is disjoint. **Named, not scoped, and owed to the operator as a decision:** R1 found that neither `ungranted` nor `granted-empty` is reachable through any shipped pack on Development services, because every DS domain gates on `mygov` and `template-city` grants it — so a prospect currently cannot be shown "built, but no source for your city" on the lens that matters most. R1 recommends a fourth demo pack granting a subset. That is a product decision and is NOT dispatched here. **Two lanes rewrote themselves rather than ship a duplicate rule:** R2 had shipped its own four-state resolver as a served module and deleted it, its route and its pinned-list edits when R3 merged first with the same rule inline; R1 hit the same collision and did the same. The CTRL-1 shape avoided twice, unprompted. **One disclosed deviation:** R1 ran a broad `kill` over node processes, the exact hazard G-95 identified and avoided; no damage, both siblings completed, and the planner cleaned up the orphan it left (pid 73780 on port 8792, and the `P:/tmp/r1-base` worktree). Live Bastrop no-touch. No feed connected. Do not start G-52. L26 untouched. | Operator: fan agents to do that. | nick; planner (verification, merges, deploys) |
| A-083 | 2026-08-20 | **G-101 ADDED, G-102 ADDED. Closing the pass.** State at open: dashboards main **`fd52428d`** serving **`smartcity-dashboards-00035-fey`** (no redeploy owed — G-99 touched zero `web/` files, verified by an empty `web/` diff between the deployed commit and main), smart-files `5de59adb`, plan-review `156677f6`, kit `17eccfad` with **PR #10 the only open PR in the product line**. G-97 rendered five lenses, G-98 fixed `--sc-ok` across three repos at 4.444 to 4.623, G-99 rebuilt the gate into a control, G-100 derived the badges. **Two engineering items remain and are dispatched here; three decisions remain and are the operator's.** **G-101, the two things G-99 routed rather than took.** First, `.prov .sep` paints a separator at **1.856:1 light / 1.737:1 dark** — roughly a third of the 4.5:1 floor — and axe will NEVER report it, because it excludes punctuation-only text by design (`axe.js:28714`, bisected: `"|"` not evaluated, `"X"` incomplete, `"XY"` violation). It is in NO bucket rather than the incomplete one; the planner relayed that wrongly to G-99 and G-99 corrected it by measurement. `aria-hidden` is the routed fix, with the measured caveat that **`aria-hidden` does not silence axe**, so the second instrument G-99 built is what will verify it. Second, and this is the number G-99 itself flagged as the one that could still be quoted too generously in an ACR: **axe surfaces 5 `.p-ok` nodes on Windows and 0 on Linux while coverage reads 100% on both**, and the un-eliminated candidate is that coverage is measured against the VIEWPORT box rather than against inner `overflow:hidden` boxes such as `div.panel`. Establish the mechanism, then make coverage tell the truth. **G-102, the kit is one card behind and the Design bundle lags with it.** Kit PR #10 carries the correct `sc-kit.css` at blob `3b89d4ae` matching all three products, but its `vendor-parity` arm B is red for an INHERITED reason reproduced on pristine main-versus-main: `vendor/index.html`, `vendor/app.js` and `vendor/shell.css` have drifted across G-92 through G-100. Refreshing them pulls G-93's two-source `.prov`, which needs a **`Prov` component-contract change** — the card this has been deferred behind twice. G-102 does the full re-vendor, the contract change, the recount, the wrap of anything the coverage assertion goes red on, and hands the planner a bundle re-sync. **Named and NOT dispatched, because each is a decision rather than an engineering task:** R1 found that neither `ungranted` nor `granted-empty` is reachable through any shipped pack on Development services — every DS domain gates on `mygov` and `template-city` grants it — so a prospect cannot be shown "built, but no source for your city" on the lens that matters most, and R1 recommends a **fourth demo pack granting a subset**; G-96 established that the SmartCity PDF path must be REPLACED rather than configured and recommends **headless Chromium print-to-PDF through one shared render service**, with the limit stated that Chromium tags what the HTML says; and the **VPAT scope map is still blocked on the contracts line items**, which nothing else waits on. **Also owed by the planner and not by a lane:** `30b_smartcity_design_system` still declares the pre-G-94 token values. Live Bastrop no-touch. No feed connected. Do not start G-52. L26 untouched. | Operator: can you close the open items so we can be complete with this pass. | nick; planner (state, scope, the relay error corrected above) |
| A-084 | 2026-08-20 | **G-103 ADDED, G-104 ADDED. The PDF generator decision is TAKEN; the scope map is unblocked on everything except the SKU column.** Two operator rulings 2026-08-20. **RULING: the renderer is ONE SHARED RENDER SERVICE**, headless Chromium print-to-PDF from semantic HTML, chosen over Puppeteer-in-each-repo and over `window.print()`. Basis, measured by G-96 rather than argued: four PDFs already on the operator's machine produced by `Skia/PDF m151` / HeadlessChrome each carry the complete 504.2.2 set — `/MarkInfo Marked true`, `/StructTreeRoot`, `/Lang en`, a real `/Title`, `/DisplayDocTitle true`. The cost of the rejected alternatives is what decided it: Puppeteer in-repo means each of three `pg`-only repos leaves `node:22-alpine`, gains roughly 300-400 MB of image and a browser at cold start, tripling the surface to maintain; `window.print()` is cheapest and browser-dependent, so an ACR cannot claim tagged output uniformly, and there are currently **zero** `@media print` rules across all three kits. **The limit travels with the ruling and is not optional to state:** Chromium tags what the HTML says. The structure walk on those four files reads `/Document 1, /H1 1, /H2 3, /P 4, /NonStruct 50`, so this does NOT fix conformance on its own — it makes the G-94, G-95, G-99, G-100 and G-101 semantics count twice. Bad HTML in, bad tags out. **RULING: SmartSite gets both fixes, sequenced.** Its site-plan PDF is server-side `pdf-lib` from engine-api, established by G-96 on four independent links including `filenameFor` in property-explorer producing exactly `48021_34649_site_plan.pdf` and `render.ts:1152` building the `SP-48021-34649` id. `setTitle`/`setLanguage` is three lines and kills two of five failures immediately. The worse defect is separate and gets its own card: `render.ts:373` draws tracked runs glyph by glyph, so **64.4% of extracted lines are a single character** (268 of 416, pypdf newline-split) — a tagged version of that file would pass an automated 504.2.2 check and still read one letter at a time to a screen reader. SmartSite is NOT replaced by the render service. **G-103** stands up the shared service and routes the SmartCity document types through it; G-96 sized them at 22 by rendered layout or 15 by `.save()` call site, one fix at the library seam and 22 decisions at the content seam. **G-104** takes the SmartSite pair. **The scope map is now blocked on ONE input.** It binds SKU to product surface to `template-city` route to conformance surface, and three of those four columns are derivable from measured state: 23 surfaces, four products of which three are built, and the routes. The SKU column cannot be invented, because a VPAT covering LESS than what is sold means a buyer finds an unreported surface and one covering MORE confuses a procurement officer. The planner builds every derivable column now so the operator fills one column against a list rather than producing a document. Two facts the map must carry regardless: **Asset Management has no repository** and appears as not-yet-built rather than being silently omitted, and **Dashboards mounts SmartSite in an iframe**, so a Dashboards user reaches an untagged pdf-lib PDF without leaving the product and the scope cannot stop at "Dashboards emits nothing". **State at open:** product line CLEAR, zero open PRs across all four repos. Dashboards main `238e0234` serving `smartcity-dashboards-00037-ced` with 20 of 20 separators carrying `aria-hidden`; kit main `54efe8c5` current with the product on all four vendored blobs; smart-files `5de59adb`; plan-review `156677f6`. Half A of wave 3 is shipped and **half B, the evidence pack and the ACR, is NOT STARTED** — that is the honest state of the VPAT and it is recorded here rather than implied. Live Bastrop no-touch. Do not start G-52. L26 untouched. | Operator: lets unblock the scope map and the pdf generator. what do you need from me. | nick (both rulings); planner (evidence, scope) |
| A-085 | 2026-08-24 | **G-105 ADDED through G-110 ADDED. Wave 1 integration program rows for the govtech seat.** Operator ruling 2026-08-24 moved four product repos to govtech; scope rev 3 (`_inbox/2026-08-24_govtech_program_scope.md`) and adversarial CP1 (`_inbox/2026-08-24_govtech_plan_adversarial_cp1.json`) define the dependency graph. **G-105** deploy gates for merged #7+#39+#75+#361 with live violation probes (S5-2a). **G-106** Smart Files read-path scope (S3-1, defect #3). **G-107** staff upload path (S2-6, R-B). **G-108** plan review honesty path including edition selector and typed absence (S2-7/8/9). **G-109** ICC obligation ledger chain through R-I authoritative ledger (S4-0 through S4-8). **G-110** Wave 1 E2E proof on `template-city` (S5-5). S2-1 engine migration remains blocked on ADR-023 amendment (DOC-5); dispatch as a separate row when amendment is ratified. Canvas: `canvases/govtech-master-program.canvas.tsx`. Do not conflate merged with live. Live Bastrop no-touch. L26 untouched. | Operator: govtech thread scope and Wave 1 critical path from 2026-08-24 handoff; CP1 PASS WITH AMENDMENTS once these rows exist | nick |
| A-086 | 2026-09-02 | **Wave 1 regrouped after eight days untouched; G-105 scope corrected by live digest, not narration.** WDLL (`_inbox/2026-08-25_govtech_wave1_WDLL.md`) had sat draft/pending since 2026-08-25 with zero sessions touching G-105 through G-110 in between. Regroup filed at `_inbox/2026-09-02_govtech_wave1_regroup.md`. **Finding: two of the four "merged, not deployed" PRs in STATE.md are actually live**, carried in by unrelated property-program redeploys — hauska-mcp-server `#75` (`c81f1c3`) is an ancestor of the serving image `1ae9f28` on `hauska-mcp-server-00084-mof` (`gh api compare`, ahead_by 4 / behind_by 0); hauska-engine `#361` (`cfa18bc`) matches `hauska-engine-api-00176-keb`, confirmed live and cross-checked against the 2026-08-27 memory record of the same close. **plan-review `#7` and dashboards `#39` are confirmed NOT deployed**: plan-review Cloud Run still serves `plan-review-00012-pen` tag `g60g` from 2026-08-16 (predates the PR by 8 days), its Vercel front redeployed at 13:55 CDT 2026-08-24, ~4.5h before the PR merged at 18:33 CDT; dashboards still serves `smartcity-dashboards-00037-ced` from 2026-08-20. **G-105 is therefore a two-item deploy (plan-review, dashboards) plus two probe-graded confirmations (MCP, engine-api), not a four-item deploy.** DOC-5 re-checked: ADR-023 amendment draft is still `status: draft`, unratified. No baseline row edited; no WDLL edited. | Operator: regroup the 8-day-old plan before resuming execution; live digests pulled this session as the basis | nick; planner (digest pulls, corrected scope) |

| A-087 | 2026-09-02 | **DEPLOY-7 and DEPLOY-39 CLOSED.** plan-review Cloud Run `plan-review-00014-zot` @100% + Vercel `dpl_D8satDguP8ZhRKFjXwf2aW9jV3po` (`plan-review-app-ten.vercel.app`), paired in one cut. Live probe (correcting the WDLL's assumed route — real route is `GET /api/plan-review/code`, Bearer-gated by the already-live PR #6): pre-deploy returned HTTP 200 with fabricated `R311.7` for a nonsense section; post-deploy returns typed absence. Dashboards Cloud Run `smartcity-dashboards-00039-cid` @100%. Its live probe was inconclusive — the compose endpoint doesn't expose per-atom `accessPolicy`, so grading rests on code reading: `git diff` confirms the fail-open line (`raw == null → return true`) removed and replaced with an enum-allowlist resolver defaulting to `false`. Full evidence and the deploy-violation-probes.mjs defects found along the way (wrong route on gate deploy-7, no defect-triggering params on gate deploy-39 — both gave false passes when run live) are in `_inbox/2026-09-02_govtech_wave1_regroup.md`. `_state/govtech/STATE.md` NOT updated (SEAT-01: not writable from the integration checkout) — still needs a refresh by the govtech seat. | Operator: "do the two deploys" | nick; planner (deploy, probes, code-diff verification) |

| A-088 | 2026-09-02 | **DEPLOY-75 and DEPLOY-361 formally graded.** Neither redeployed — both were already live (A-086). **DEPLOY-75**: migration 009 confirmed applied (`source_obligation_ledger` exists, 12 historical rows, all pre-#75). The PR shipped its own self-test (`scripts/violation-probes.mjs`, run this session from a fresh temp copy): all three defect classes (S1 accrual-gating, S2 tenant/adapter/actor detector unification, S3 revenue reporting) pass on the correct implementation and fail on injected-defect and refuse-all variants — confirmed non-vacuous. Live replay of a known pre-fix false positive (`did:hauska:setback-rule:48021:34737`, Bastrop's own ordinance, wrongly ICC-billed in the 2026-07-25 rows) now correctly produces zero accrual. **Named gap: the true-positive live path (real ICC content via a `codex`-tier key) could not be tested — anonymous `get_atom` on the ICC corpus atom was refused by accessPolicy, and no product key was available this session.** Closed with that residual named, not closed clean. **DEPLOY-361**: closed by code reading only, deliberately — no live malformed-write probe was run against the shared atoms store, because a concurrent-write collision on that same store was reported and resolved earlier this session (doc-repo-c0, unrelated to this program, but evidence the store is contended right now). `git diff` on `packages/corpus/src/atomization/index.ts` confirms the exact fail-open default removed (`options.accessPolicy ?? "public-free"` deleted) and `accessPolicy` promoted from optional to required across five atom-instance interfaces in `packages/atoms/src/instances.ts` — a structural (type-level) fix, not just a runtime check. | Operator: "yes" (continuing from the deploy request) | nick; planner (grading, self-test run, code diff, live replay) |

| A-089 | 2026-09-02 | **Three Wave 1 dispatches COMPILED for operator hand-carry (not yet sent).** `node scripts/dispatch.mjs --plan OPS-17` for lane A (`G-106,G-107`, repo smart-files — read-path scope then staff upload), lane C (`G-108`, repo plan-review — edition selector, typed absence, applicability matrix), lane D (`G-109`, repo plan-review — govtech half of the ICC ledger: S4-1b reconciliation + S4-8 reader only; S4-3/S4-7 explicitly routed to the substrate seat, not built here). Missions at `_inbox/2026-09-02_govtech_track_{a,c,d}_mission.md`; compiled output at `_dispatches/2026-09-02_{a,c,d}_dispatch.md`. Each mission carries source-verified corrections found this session: smart-files' primary checkout is dirty/stale and its seat worktree doesn't exist yet; plan-review's edition/absence code is genuinely unbuilt past `store.mjs`, not just ungraded; and the obligation ledger's real schema has no `book_id`/`section_id`/`edition_id`/`reference_kind` columns, contradicting the WDLL's own item 12 check — S4-1b/S4-8 must be built against the real schema. S5-2c and S5-3 (seam vocabulary, presentation convergence) are deliberately NOT dispatched yet — held until these three tracks produce real output to converge on. | Operator: "yes" (continuing the biteoff-the-work plan) | nick; planner (verification, mission drafting, compile) |

| A-090 | 2026-09-02 | **G-109 govtech half CLOSED PARTIAL.** `plan-review` PR **#8** merged (squash, head `bfb4559` / merge `1dd20d1`, CI conclusion string `success` on both), branch `govtech/g-109-ledger-reconciliation`. S4-1b (reconciliation, `request_id`-matched, typed `ledger-unavailable` not fabricated-zero) and S4-8 (reader folded into existing `GET /api/icc/activity`) built and tested (39/39, dedup_key uniqueness verified by violation against a live scratch Postgres). Production migration (`sql/003`, `sql/004`) and Cloud Run deploy **deliberately held**, operator-ruled: the feature is structurally inert until substrate closes the gaps below, so nothing is gained deploying early. Full artifact `_inbox/2026-09-02_d_close.json`. **Two NEW substrate-seat blockers surfaced, independently confirmed by two sessions:** (1) `hauska-mcp-server` mints `source_obligation_ledger.request_id` server-side (`randomUUID()`, `src/index.ts:289`) and never echoes it to the caller — exact reconciliation cannot produce one real match until this is fixed. (2) No MCP tool reads `source_obligation_ledger` at all (`grep tools.ts` for "ledger"/"obligation" — zero matches) — even with (1) fixed, plan-review has no substrate-side reader to consume. Both are beyond S4-3/S4-7 as originally scoped; a substrate-seat request is being filed separately. WDLL item 11 stands met; item 12 is explicitly NOT graded met — its literal check names columns (`reference_kind`, `book_id`, `section_id`, `edition_id`) that don't exist on the real table (A-088 already flagged this; S4-7 would need to add them). This close is the product of the A-085/A-089 duplicate-dispatch incident: the same compiled `_dispatches/2026-09-02_d_dispatch.md` reached three executors in one shared, branch-unpinned worktree; the collision was caught before any commit, the two independent builds turned out complementary (not conflicting), and the standing-down session preserved its own diff non-destructively (copied to its own scratchpad rather than `git stash`, to avoid clobbering the other session's in-flight edit). Worth a standing fix: hand-carry a dispatch to exactly one executor, or give plan-review the same per-lane worktree isolation smartcity-dashboards and smart-files already have. | Operator: "yes relay" (commit/PR/merge go, prod deploy hold) | nick; doc-repo-1f (build, close); doc-repo-33 (original design, stood down cleanly); doc-repo-1d (collision brokering, this amendment) |

| A-091 | 2026-09-02 | **G-106 CLOSED. G-111 ADDED.** `smart-files` PR **#9** merged (`d7b3e71`), deployed `smart-files-00008-geb` @100% (prior `smart-files-00006-xwp`), tagged canary smoke-tested at 0% traffic before the shift. Live-verified both pre- and post-shift: anonymous read 403, wrong-tenant token 403, matching-tenant token 200; the existing production `SMART_FILES_SERVICE_TOKEN` kept a blanket read grant for backward compatibility (real callers — QA UI, Dashboards, plan-review — all still use one shared token today), two new narrowly-scoped tokens minted for future migration, none printed to chat or left in a scratch file. 25/25 unit, 46/46 existing round-trip probe, 17/17 new read-scope probe, 2/2 CI checks — all read via the literal conclusion string, not display text. G-107 (tenant identity, staff upload) explicitly NOT attempted this pass — correctly scoped out once the executor read `uploadFileToFolder` and found it needs plan-review UI work too, not just smart-files. Close `_inbox/2026-09-02_a_close.json`. **G-111 ADDED**: the substrate half of the ICC obligation ledger chain (S4-3 request_id echo-back, S4-7 a ledger reader tool), surfaced by two independent sessions building G-109 and filed at `_inbox/2026-09-02_govtech_to_substrate_ledger_asks.md`. Given its own row rather than folded into the closed G-109, since G-109 was explicitly scoped and closed as the govtech half only. | Operator: "add G-111 to OPS-17, compile a real dispatch for it" | nick; doc-repo-25 (G-106 build); doc-repo-1d (this amendment, G-111 scoping) |

| A-092 | 2026-09-02 | **G-108 CLOSED PARTIAL. Planner error in the Track C mission corrected.** `plan-review` PR **#10** merged (`f27c60f`), 57/57 tests (18 new). Real mechanism, not narration: edition selector, a citation-minting module with a hard refuse absent `editionId`/`bookId`/`sectionNumber`, an adjudicator that reaches genuine Pass and Fail (live-verified: the 12 findings rows already in production had been 100% Uncertain/Unchecked since migration 001 despite a 4-value CHECK constraint existing the whole time — a starved mechanism this PR actually feeds), typed `SOURCE_UNAVAILABLE` distinct from a genuinely-empty slot. Items 8/9/10 graded PARTIAL, correctly — item 8's own WDLL text blocks it on item 7 (G-107), confirmed still unbuilt by checking live `smart-files` PRs rather than assumed. **Planner error:** the Track C mission (`_inbox/2026-09-02_govtech_track_c_mission.md`) told the executor "spelling is `verified-absent`," which is backwards — the WDLL (item 9) and the transaction contract both specify `absent-verified`; `verified-absent` is the divergent one, already tracked as canvas item L3, scoped to `S5-2c` (a govtech-wide seam-vocabulary pass across all repos at once, not a one-repo fix). The mission's operational instruction ("do not import the L3 fix here") was still right despite the wrong stated reason — fixing spelling in one repo ahead of the others would create a second divergence, not close the first. The executor caught the contradiction by reading both WDLL and contract rather than trusting the mission text, did not silently pick one, and documented the conflict in its close — exactly the right response to a wrong instruction. **Real gap found, not caused:** `plan-review`'s PR #8 code (G-109) requires `plan_review_activity.dedup_key`; migration 004 was never applied to the production Neon. Currently harmless only because deploy is also held (A-090) — production still serves pre-#8 code. **Binding note for whoever lifts the G-109 deploy hold: migration 004 must apply before that deploy, or every `recordActivity()` call breaks.** Close `_inbox/2026-09-02_c_close.json`. | Operator: none this row (planner self-correction, surfaced by the executor) | doc-repo-33 (build, close, catch); doc-repo-1d (mission error, this correction) |

| A-093 | 2026-09-02 | **Production engagement-creation break FIXED.** Root cause was not a code defect: `plan-review-00016-buh` (deployed by the G-107 session) was built via `gcloud run deploy --source .` from a local checkout that had G-107's own merge but had not fetched G-108's concurrently-landed merge (git commit order on `origin/main` is #7 → #8/G-109 → #9/G-107 → #10/G-108 — G-107 merged before G-108, and the deploy ran from whatever the local working directory held at that moment, not the true tip at deploy time). Meanwhile G-108's migration 005 (the `plan_review_findings_absence_required_check` CHECK constraint) WAS applied to the production schema (doc-repo-33 verified it live while building G-108). Net effect: new-code-missing-but-new-constraint-present, so every `seedMatrix` insert for a freshly created engagement violated the constraint. Confirmed live by the planner: reproduced on the broken revision (`POST /api/plan-review/engagements` → `violates check constraint`), then diagnosed by deploying a fresh build from a clean `git checkout --detach origin/main` with temporary row-level logging (`console.error` before each `upsertFinding`, on a 0%-traffic canary tag `diag-g110`) — every row logged a correctly non-null `absence` for its `Unchecked` determination, proving the merged code was already correct and the live revision was simply stale. Diagnostic logging stripped (`git diff --stat` against `origin/main` confirmed byte-identical before redeploy), rebuilt clean, smoke-tested on a second 0%-traffic tag (`g110-fix`: real parcel 201, malformed parcel 201, DEPLOY-7 fabrication check still holds, G-108 editions route 200), then shifted to 100% (`plan-review-00019-puj`) and reconfirmed on the production URL. **A real, separate bug also found and left in place, not fixed here:** `mcpCall`'s `textPayload()` (`src/mcp.mjs`) only checks the JSON-RPC-level `rpc.error`, never the tool-level `result.isError` — an MCP tool call that fails at the tool layer (e.g., `get_property_atom_chain` given a malformed `parcel_node_id`) returns `{text: "MCP error ..."}` rather than throwing, so `matrixFromChain`'s `sourceUnreachable = Boolean(chain?.error)` never fires for this failure class. Currently harmless only because every downstream row-builder still produces a correctly-typed `Unchecked`+absence regardless of which branch it takes — but the SOURCE_UNAVAILABLE absence kind, which exists specifically to distinguish "corpus unreachable" from "corpus reached, nothing there," never fires when it should. Named as a residual, not dispatched. **Operational lesson for the standing decisions:** `gcloud run deploy --source .` builds from local working-directory contents, not the git tip at deploy time — a deploy run immediately after a merge, from a checkout that has not re-fetched a concurrently-landed second merge, silently ships stale code even though the operator/agent believes they just deployed "current main." Worth a `git fetch && git log -1 --format=%H HEAD..origin/main` (expect empty) check before every `--source .` deploy, not just before the first checkout of a session. | Operator: "fix" | nick; doc-repo-1d (diagnosis, fix, deploy, verify) |

| A-094 | 2026-09-02 | **G-111 DEPLOYED.** Per the coordination check with the property/Factory program (clean repo/store separation confirmed; two cautions given), used `hauska-mcp-server`'s documented pipeline (`gcloud builds submit --config=cloudbuild-mcp.yaml --substitutions=_TAG=40e48d4`), not a raw `--source` deploy — the known-risky path per the 2026-08-03 cortex-api key-rotation outage this exact caution named. Built from a clean `git checkout --detach origin/main` in the registered substrate worktree (`P:/seat-worktrees/substrate/hauska-mcp-icc-meter`; four CRLF-phantom-modified files present per `git status` but confirmed empty by `git diff --stat`, matching G-111's own prior finding — left untouched, not committed). **Real gotcha found:** this service's traffic is pinned to explicit revision names rather than "latest," so the plain `gcloud run deploy` inside the pipeline created a healthy new revision (`hauska-mcp-server-00055-8pz`) but did NOT route traffic to it — Cloud Run auto-retired it at 0% traffic within seconds. Caught by revision-list (not the `services describe` traffic block, which stayed on the old revision even after the build reported SUCCESS), tagged it back in at 0%, smoke-tested, then explicitly shifted. **Post-deploy verification, per the caution — an existing consumer path, not just the new tools:** secret bindings byte-identical between old and new revisions (same names, same `:latest` refs, diffed field by field); `get_property_atom_chain` (an existing, heavily-used tool) still serves real data; anonymous and wrong-header calls still correctly resolve to `product:"public"`, unchanged. Both G-111 features confirmed live: `x-hauska-request-id` header present on every tool-call response; the reader (`GET /obligations/source-ledger`, not an MCP tool — a plain HTTP route, absent from `tools/list`) correctly refuses with `platform_internal_required` to an unscoped/unkeyed caller. **Side effect, named not hidden:** `--to-revisions=X=100` replaces the ENTIRE traffic block, not just the percentage split — it silently dropped roughly eighteen historical 0%-traffic named tags (`fourgate`, `t1fix`, `a4b` through `g63`, `g11`, `p89-1ae9f28`, etc.) that existed before this deploy. The underlying revisions are untouched and still exist; only their tag aliases are gone. Likely harmless (these were one-off historical smoke-test URLs, not live dependencies) but not verified harmless — flagged as a residual. G-111's remaining leave_behind (plan-review needs a `platform_internal` key to call the reader; `seat_register.json`'s stale worktree-removal note) still stands. | Operator: "lets proceed" (after reviewing the coordination check) | nick; doc-repo-1d (deploy, diagnosis of the traffic-pinning gotcha, verification) |

| A-095 | 2026-09-02 | **G-108 re-graded CLOSED (from PARTIAL).** Live walk, not a re-read of old evidence: created a real production engagement on `48021:34737`, declared `IRC-2018`, submitted `proposedSetbackFrontFt=30` (above the 25ft Bastrop UDC minimum) — `14-02-003` returned `Pass` with real analysis text. A second engagement with `proposedSetbackFrontFt=15` returned `Fail`. Both citations carried non-empty `editionId` and a structured `bookId`/`sectionNumber` shape. Items 8, 9, 10 of the frozen WDLL are all genuinely met. **Minted a `platform_internal`/`codex`/`team` key** (`hauska-mcp-server` `POST /admin/keys`, key_id `19c13d1f-...`) for plan-review to call G-111's reader, stored as GCP secret `plan-review-platform-key` (project `plan-review-505715`, IAM-granted to the plan-review runtime service account, raw value never printed to chat or left on disk). **Not done, and named as real remaining work, not a quick follow-up:** nothing in plan-review actually CALLS the new reader yet. `src/ledger-reconciliation.mjs`'s `fetchLedgerRows()` still deliberately returns `null` (correctly — its own docstring names exactly this as the one seam to change once a read path exists, which now it does). Wiring it in touches `src/mcp.mjs`'s shared `mcpCall`/`textPayload` plumbing (used by every MCP-calling route in the service, not just this one) to capture the `x-hauska-request-id` response header, which `mcpCall` currently discards entirely along with all other response headers — a genuine multi-file change needing its own dispatch, not something to improvise inline. The secret is provisioned and waiting; nothing consumes it yet. | Operator: "how do we do that" | nick; doc-repo-1d (verification walk, key mint, secret provisioning) |

| A-096 | 2026-09-02 | **G-112 ADDED.** Wire plan-review's `fetchLedgerRows()` to G-111's live reader, closing the loop A-095 named as real remaining work. Not folded into the closed G-109 (govtech half) or G-111 (substrate half) — a third, distinct unit of work consuming both. Dispatch to follow. | Operator: "yes" (compile the dispatch) | nick |

| A-097 | 2026-09-02 | **A-093's root cause CORRECTED, not just restated.** doc-repo-25 (who ran the G-107 deploy A-093 described as "stale") pushed back with a specific, checkable claim: it fetched and checked out `origin/main` immediately before deploying, and per A-093's own commit order (G-107 merged before G-108), a correct fetch at that moment would have captured only G-107. Checked the actual evidence rather than picking a side: the broken revision's Cloud Run build-source upload timestamp is `2026-09-02T15:48:30.820Z`. G-108 (#10) merged `2026-09-02 10:48:28 -0500` = `15:48:28Z`. G-107 (#9) merged `15:47:02Z`. **The gap between G-108 merging and the source upload is 2.8 seconds.** `gcloud run deploy --source .` tars and uploads the working directory as a step, taking real wall-clock time after the checkout completes — so doc-repo-25's fetch+checkout almost certainly happened at or before the instant G-108 merged, not carelessly stale. **A-093's "didn't re-fetch" framing was wrong; the corrected lesson: a deploy pipeline has a non-zero window between reading source and the build completing, and a concurrent merge landing inside that window is missed regardless of fetch discipline immediately prior — this is a property of the pipeline, not a lapse by the operator running it.** The practical fix is verifying what's actually running post-deploy (a commit/build marker, or exactly the diagnostic-canary technique A-093 used), not trusting a pre-deploy fetch as sufficient when merges are landing within the same ~90 seconds. doc-repo-25's separate hypothesis — that G-108's schema constraint was applied to production out-of-band, ahead of G-108's own code merge — is plausible and would also explain the break, but is **not verified**: no direct timestamp evidence for when migration 005 actually landed on the schema was checked by either party. Left as an open, named hypothesis, not asserted as fact. The associated cross-session memory (`gcloud-source-deploy-stale-local-checkout.md`) is corrected to match. | doc-repo-25 (pushback, verification); nick (none — planner self-correction) | nick; doc-repo-1d (corrected timeline, re-verification) |

| A-098 | 2026-09-02 | **G-112 CLOSED PARTIAL.** `plan-review` PR **#11** merged (head `99516af` / merge `1f2d6ba`, CI `success` on both), deployed `plan-review-00021-huc` @100% with the `plan-review-platform-key` secret bound. 60/60 tests (5 new). Live-verified via direct SQL, not the diff: two real, distinct `request_id`s landed on production `plan_review_activity` rows post-deploy (one `/code` call, one `/matrix` call), where every pre-deploy row carried an empty string. `fetchLedgerRows()` now calls G-111's live reader. **Acceptance item 2 (a genuine matched/divergent row) could not be produced — a real, external reason, not a defect in this row:** the one ICC-tagged fixture atom built to prove this refuses an anonymous read (`"not readable under the caller's accessPolicy"`), the identical gap DEPLOY-75 already named in A-088. **The codex-tier-key gap is now a single blocker across two rows** (A-088's true-positive verification and G-112's genuine-match verification) rather than two separate ones — getting one key resolves both. A diagnostic activity row (`id 47fc24c3-...`, `bookId=G112-DIAGNOSTIC-PROBE`) was deliberately left in production, clearly labeled, as the record of the verification attempt rather than deleted. Caught and fixed its own tooling bug mid-session: a `Monitor` script that declared a CI check done before the check had started, caught by checking `gh api` directly rather than trusting the first report. Close `_inbox/2026-09-02_g112-reconciliation-wiring_close.json`. | doc-repo-1f (build, close) | nick; doc-repo-1d (recording) |

| A-099 | 2026-09-02 | **G-113 and G-114 ADDED** (WDLL items 13, 14 — the last two blockers before G-110's E2E proof is even attemptable). Both rescoped from the WDLL's literal 9-day-old text after checking source, not assumed: G-113's "vendor a citation validator into plan-review, smart-files, and smartcity-dashboards" narrows to plan-review only — neither other repo constructs a citation-shaped object today (grepped, zero hits), and `citation.mjs`'s `buildCitation()` (built inside G-108) already refuses construction without `editionId`/`bookId`/`sectionNumber`, so the real remaining gap is the spelling fix plus a durable CI gate, not new validator code. G-114 is grounded in a real, confirmed defect, not a hypothetical: `smartcity-dashboards/src/mounts.mjs`'s `planReviewEmbedUrl()`/`smartFilesEmbedUrl()` return a static env-configured origin with no `cityKey` threaded through, and `compose.mjs` calls them with no city argument — a genuine scope trap the moment Dashboards serves more than one city. Missions and dispatches to follow. | Operator: "yes please" (continuing phase 2/3, defer Bastrop/polish) | nick |

| A-100 | 2026-09-02 | **G-113 CLOSED.** `plan-review` PR **#12** merged (`3f426bb`), 64/64 tests (4 new, 2 rewritten). `ABSENCE_KINDS.VERIFIED_ABSENT` renamed `absent-verified` everywhere live (two remaining hits are historical prose in comments, explicitly named and excluded, not silently satisfied by a lenient grep). Re-confirmed single-repo scope with an independent fresh grep against both other repos' current `origin/main`, not trusted from the dispatch's own claim. **Real residual found and fixed, not assumed clean:** two hand-authored citation strings had gone dead-but-still-present in `CODE_BOOKS["BASTROP-UDC"].sections` since G-108 switched the route to `buildCitation()` — unread, so invisible to any live probe, but exactly the kind of defect that resurrects itself the next time someone reads that field back. Removed. The new CI gate is three layers, not one: a textual check (no `citation:` literal outside `citation.mjs`), a structural check (no `CODE_BOOKS` entry carries one), and a genuine meaning-shaped check — `matrixFromChain`'s shipped citation on a row must equal one rebuilt fresh from that row's own `editionId`/`bookId`/`sectionId`, two independently derived values required to agree. Violation-verified: planted the exact historical pattern in a file with no legitimate reason to touch citations, confirmed the gate caught it and named the file, restored, reran green. No separate CP1/CP2 files — close artifact carries everything, a deliberate choice over fabricating empty checkpoint files to satisfy a path check. Close `_inbox/2026-09-02_g113-seam-vocabulary_close.json`. | doc-repo-33 (build, close) | nick; doc-repo-1d (recording) |

| A-101 | 2026-09-02 | **G-114 CLOSED. WDLL items 1 through 14 now all met — G-110 is the only row left.** Three PRs merged and deployed: `smartcity-dashboards` #40 (`03250b3`, `smartcity-dashboards-00041-naf` @100%), `plan-review` #13 (`67b852c`, Vercel front only — Cloud Run backend correctly not redeployed, the change touched only `web/app.js`), `smart-files` #11 (`c38c20f`, Vercel front only, same reasoning). Live-verified past the point of "each half looks right": fetched the actual deployed `app.js`/`index.html` from both Vercel production URLs and ran them in `jsdom` with the exact URL the live compose response produces — `plan-review` resolves `localStorage pr_persona` to `template-city/staff`, `smart-files` resolves its persona select to `template-city/g71-calendar`, both against the real deployed bundle, not a local copy. This is the actual thing a presentation-convergence card has to prove: not that Dashboards' compose response looks right and not that the downstream apps' logic looks right in isolation, but that the two halves compose correctly in production. Cross-city differentiation proven at the unit level only (no second city pack exists in production to test against live — onboarding one was explicitly out of scope). All three SKUs (`SCOS-PLAN-DEP`, `SCOS-FILE-DEP`, `SCOS-DASH-DEP`) confirmed independent; the pre-existing G-13 dependency direction (Dashboards embeds the other two, never the reverse) is unchanged — this card fixed what goes in the iframe `src`, not whether the iframe relationship exists. **Named residual, correctly left alone:** `staff-review.mjs` carries dormant, never-called iframe-URL functions with the identical pre-fix static-URL shape — investigated, confirmed inert, deliberately not "fixed" for a defect they cannot currently cause. Flagged for whoever next touches that file, not fixed here (no scope creep for an unreachable code path). Close `_inbox/2026-09-02_g114-presentation-convergence_close.json`. | doc-repo-25 (build, close) | nick; doc-repo-1d (recording) |

| A-102 | 2026-09-02 | **DEPLOY-75's true-positive residual (A-088) CLOSED.** The `platform_internal`/`codex`/`team` key minted for G-112's reader turns out to also satisfy this — tested directly: `get_atom` on `did:hauska:jurisdiction-corpus:icc-model-code` through `hauska-mcp-server` succeeded (previously refused anonymous), and a live query of `source_obligation_ledger` immediately after shows the real row it produced: `source_actor_did=did:hauska:actor:org:icc`, `tool=get_atom`, `product=codex`, `tier=team`. Genuine accrual for genuine ICC content, not a fixture replay. **Named precisely, not overclaimed:** this proves the corpus-level accrual mechanism; it does not mean plan-review's own production code-lookup route can cite real ICC *sections* — tested that too, same key, `plan_review_get_code` against `IBC2018P6`/`R311.7` still correctly returns typed absence, because plan-review's local manifest has zero ingested sections (unchanged) and, per this same check, the substrate itself has no real per-section ICC content either (only the corpus-level atom exists). That's a data-acquisition gap, not an access or mechanism defect, and it directly bears on G-110 below. | Operator: "keep the pipeline going" | nick; doc-repo-1d (found, tested, closed) |

| A-103 | 2026-09-02 | **G-110 walked, CLOSED PARTIAL — the honest state of Wave 1's capstone row.** Full evidence above and in this row's own table entry. Four of five WDLL-item-15 observations are genuinely met on one coherent live session, not stitched from separate probes across the day. The fifth (a `source_obligation_ledger` row for THIS session) cannot be produced through plan-review's own production input surface today, for a reason established precisely, not assumed: no real per-section ICC content exists anywhere in the system — not in plan-review's local manifests (already known, G-108/G-113) and, newly confirmed this session, not in the substrate either (only the corpus-level `jurisdiction-corpus` atom is real; `plan_review_get_code` against `IBC2018P6`/`R311.7` through the same codex-tier key still correctly returns typed absence). This is a content-acquisition gap, not a build gap — Wave 1 built the mechanism; it was never scoped to also ingest real ICC section content. **Operator call owed:** does Wave 1 count as done with this residual named (the same pattern G-105/G-109/G-112 all used), or does closing G-110 fully require an ICC content-ingestion effort that was never part of this wave's scope? Not decided here. | Operator: "keep the pipeline going" | nick; doc-repo-1d (walk, evidence, this amendment) |

| A-104 | 2026-09-02 | **G-110 RE-GRADED CLOSED (from PARTIAL). Operator ruling: "residual named is fine." WDLL items 1 through 15 are now all met — Wave 1 is complete.** The ruling answers A-103's owed call directly: the missing session-scoped ledger row stands as a named content-acquisition gap, not a build defect, and does not block the row from closing. **Scope note, not a status change:** G-112's own open acceptance item (a genuine matched/divergent ledger row, A-098) shares this exact root cause — plan-review has no path to an ICC-citing call while no real per-section ICC content exists to cite. G-112's row is left at CLOSED PARTIAL here rather than silently upgraded under this ruling; it wasn't the row this ruling was asked about, and re-grading it deserves its own explicit ask rather than inheriting one by inference. **Real ICC section-content ingestion remains genuinely unscoped anywhere in this program** — named here so it isn't lost now that its two visible symptoms (G-110, G-112) are both resolved by residual-naming rather than by the content actually landing. | Operator: "residual named is fine" | nick |

## Assumptions carried (each reversible by one amendment)

1. **The rebuild named in `34_smartcity_smart_files_and_foundation.md` open-item 1 IS this program.** The operator was uncertain. If it names a different effort, amend the G-03 and G-14 scope.
2. ~~**Smart Files is a rendering pass, not a build.**~~ **FALSIFIED 2026-08-14 by G-03; see A-002.** Smart Files is a BUILD. Retained struck-through rather than deleted: the assumption was written to be tested, the test ran, and the mechanism working is the record worth keeping. Doc 34 asserted it, the schema refuted it.
3. **Sensors are Asset Management Tier 2, not a fifth lane.** Ruled from the doc 32 Tier 2 section plus the sensor overview; both describe telemetry identically. UNTESTED — no row currently falsifies or confirms it.

**On carrying assumptions.** A-002 is the argument for this section existing. The planner carried doc 34's self-assessment into a program assumption; a read-only audit falsified it in one day and the cost was one amendment instead of a lane built on sand. Every future assumption added here is written to be falsifiable and named in a row that tests it.

## GRADE LOG (one row per grading pass; statuses re-grade by instrument only)

| Date | Pass | Rows graded | Result | Instrument notes |
|------|------|-------------|--------|------------------|
| 2026-08-14 | baseline | G-01, G-24, G-30, G-41 | recorded as LIVE at source | G-01 by frontmatter sweep; G-24 from the verified doc 32 statement; G-30 by grep on engine main; G-41 from adapter code plus commit history |
| 2026-08-14 | G-06 close | G-06 | PASS | Five-case negative test on true exit codes (not pipe exits): valid G row 0; valid P row on default plan 0; wrong-prefix row 1; unknown row 1; unknown plan 1. The gate was proven able to fire before being trusted, per inherited constraint 9. |
| 2026-08-14 | G0 close | G-01..G-05 | G-02 PASS; G-03 PASS (verdict BUILD); G-01/G-04/G-05 findings filed, 6 rulings owed | Close `_inbox/2026-08-14_g0_close.json` plus four sub-agent artifacts. G-02 `scripts/doc-staleness.mjs` built by the lane planner directly (not delegated) and proven on five cases; repo-wide it found **366 violations, not the 16 in the brief** — the brief's figure was root-only — and the largest class is **129 docs with no `status` field at all**, an absence class nobody had named. G-03 verdict BUILD, planner-re-verified at the schema. G-05 found CTRL-1 and CTRL-2, both reproduced. |
| 2026-08-14 | planner verify of G0 | G-03 schema claim, CTRL-1, doc-34 register | all three CONFIRMED at source | Verification not taken on report, per the rule that verification never rises on trust alone: `brokerageWorkspaces.ts:54-76` read directly (9 columns, single notNull FK cascade, no updated_at/version/cid/access_policy); CTRL-1 reproduced in PowerShell (`G-9999` → no `P-\d+` match → `ok` = fail open; `P-9999` → matched → rejected = looked alive); doc 34 register rows read at source. |
| 2026-08-15 | A3 L1 close | G-10 (lane A half), G-14 | G-10 MET; G-14 CLOSED foundation | Identity `smartfile:<scopeType>:<scopeId>:<docSlug>` on main `015b15d6` (PR #432, 8/8 conclusion SUCCESS). Live apply 0078/0079/0080 on cortex-prod neondb `_schema_migrations` 2026-08-15T14:27Z (run 31889907009). information_schema confirms four tables + nullable jurisdiction_fips + NOT NULL scope columns. Not customer-done. Close `_inbox/2026-08-15_a3_close.json`. |
| 2026-08-17 | G-13 close | G-13 | PASS | Decision `_decisions/2026-08-17_g13_consumer_contract.md`. Caller-split contract; three-supplier table; G-61 WDLL cites the id. Close `_inbox/2026-08-17_g13_close.json`. Live city unchanged. G-11 remains OPEN. |
| 2026-08-17 | G-61 close | G-61 | PASS | Dashboards `00002-sxq` compose+packs live-probed. MCP `00076-veq` tools live-probed. City `00118-qox` unchanged. Close `_inbox/2026-08-17_g61_close.json`. |
| 2026-08-17 | G-62 close | G-62 | PASS | Dashboards `00003-jc2` unauth compose omits owner-fact. MCP `00078-xuv` tag g62 `dashboards_compose_city_manager` omits owner-fact. City `00118-qox` unchanged. Close `_inbox/2026-08-17_g62_close.json`. |
| 2026-08-17 | G-63 close | G-63 | PASS | Dashboards `00004-zsq` GET `/api/adapter-kinds` seven kinds, samsara writesTo files, template-city grantedAdapters []. MCP `00080-voc` tag g63 `dashboards_list_adapter_kinds` anon. City `00118-qox` unchanged. Close `_inbox/2026-08-17_g63_close.json`. |
| 2026-08-17 | G-11 close | G-11 | PASS (sequencing, not sprint-54 done) | Dashboards `00005-m5t` fixture-city tenant-private grants []. Unauth 401. Bearer 403. Identified pack 200. MCP `00082-mat` tag g11 whoami plus identified get_city_pack. City `00118-qox` unchanged. G-33 and G-42 stay OPEN. Close `_inbox/2026-08-17_g11_close.json`. |
| 2026-08-17 | G-45 close | G-45 | PASS (staff map on Dashboards; live Leaflet uncut) | Dashboards `00006-vfk` GET `/` auto-loads `48021:34137` iframe `https://smartsite.cloud/?parcelNodeId=48021%3A34137`. Browser 908 PINE / APN 34137. MCP `00082-mat` tag g11 `smartsite.url` match. City `00118-qox` CSP omits smartsite.cloud. Close `_inbox/2026-08-17_g45_close.json`. |
| 2026-08-17 | G-64 close | G-64 | PASS (staff reviewer on Dashboards; live PermitFlow uncut) | Dashboards `00007-8sc` GET `/?lens=development-services` iframe `https://plan-review-app-ten.vercel.app/`. GET `/` still 908 PINE. MCP `00082-mat` tag g11 `data.planReview.url` match. City `00118-qox` `/permitflow/review` 200. Close `_inbox/2026-08-17_g64_close.json`. |
| 2026-08-17 | G-65 close | G-65 | PASS (product kill on Dashboards; live island uncut) | Dashboards `00007-8sc` GET `/app.js` permitflow=0; GET `/permitflow` 404. City bundle permitflow=160; `/permitflow/review` 200. No product PR. Close `_inbox/2026-08-17_g65_close.json`. |
