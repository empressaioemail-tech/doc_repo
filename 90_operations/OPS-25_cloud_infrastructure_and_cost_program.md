---
id: OPS-25_cloud_infrastructure_and_cost_program
title: OPS-25 — Cloud infrastructure and cost program: plan of record
date: 2026-09-17
last_updated: 2026-09-18T12:40:00Z
status: active
owner: nick (amendments); doc_repo planner (grading)
purpose: The single benchmarkable plan for infrastructure cost, reliability, and cloud-provider work that is not itself a product feature — the phased GCP-to-DigitalOcean migration, extended 2026-09-18 (A-8) from the six always-on Cloud Run services to a full exit of every GCP project except atx-bulls, the reliability/noise findings surfaced 2026-09-16/17, and the SmartCity OS contract cost model. Work that cannot name a D-row here is not scoped.
related_canonical: [90_operations/OPS-16_texas_market_plan_of_record, 90_operations/OPS-17_govtech_stack_plan_of_record, _smartcity_masters/Pricing/00_pricing_basis, _state/shared/STANDING_DECISIONS]
---

# OPS-25 — Cloud infrastructure and cost program: plan of record

## Why this document exists

A 2026-09-16/17 session verified the live GCP bill (August: $1,955.47, Cloud Run trending 171% month-over-month, run rate heading toward $3,000-4,000/mo), found that six always-on Cloud Run services account for roughly 97% of Cloud Run spend regardless of the "the factory is heavy" assumption (all 39 factory/onboarding Cloud Run Jobs together are ~1.3% of it), confirmed the actual service call graph from live deployed env vars rather than names, and stood up DigitalOcean tooling (doctl, `do-apps`/`do-droplets` MCP servers) fleet-wide. None of this had a plan-row, so none of it was scoped. This document is that row.

**Definition of done, this program's first arc.** The six always-on services (hauska-retrieval-api, hauska-engine-api, smartcity-api, cortex-api, smartsite-mcp, hauska-mcp-server) running on DigitalOcean with the GCP originals decommissioned, each cutover verified by a bake period with matching error rate and latency, zero customer-facing downtime. A status claimed by narration instead of a live probe or a bill line fails.

**Definition of done, the second arc: the full exit (A-8, 2026-09-18).** Every GCP project except `atx-bulls` is shut down. The instruments grade it:
- every workload runs on DigitalOcean, each cutover baked and each GCP original at zero non-uptime requests on every hostname it answers (`scripts/infra/gcp-exit/callers.mjs`, rule 16);
- `code-refs.mjs` finds zero GCP references in code, config or deploy paths across every active repo, with a CI guard in each;
- the GCP billing export reads zero for the eight projects for a full calendar month;
- monitoring is at parity, each replacement fired once on purpose;
- no customer surface went dark.

The plan is `_inbox/2026-09-18_gcp_full_exit_PLAN.md`, the measured estate is `_inbox/2026-09-18_gcp_estate_EVIDENCE.md`, and the rulings are `_decisions/2026-09-18_gcp_full_exit_rulings.md`. The exit waits for a clean pause in the Texas scale-up, agreed with that program's planner first (D-15).

## Status as of 2026-09-17T21:00Z — read this before touching anything below

`smartcityos.io` and `www.smartcityos.io` are live on DigitalOcean App Platform, serving real customer traffic. `hauska-mcp-server`'s dashboards tools reach the DigitalOcean backend. The scraper's ten Cloud Scheduler jobs reach a DigitalOcean droplet with a shared secret instead of OIDC. This is real, verified, production state — not a plan.

**Nothing has been decommissioned.** All three D-5 droplets, all three GCP Cloud Run originals (`smartcity-api`, `smartcity-scraper`, `smartcity-dashboards`), and the GCP `hauska-mcp-server` revision that served dashboards before the flip are all still running, unmodified. They are the rollback path during the bake period governing rule 3 requires. Do not delete any of them on the assumption this is finished.

**Superseding note, 2026-09-17 (A-5, updated at D-12's close).** The dashboards half of this migration is owned by **D-12**, the CRITICAL PATH, because the operator ruled that all SmartCity surfaces move to DigitalOcean before any design build work begins. **D-12 closed CLOSED-PARTIAL the same day: steps 1 through 4 are done and verified, and the cutover itself is TWO OPERATOR ACTIONS that have not happened.** `dolphin-app` now builds from `main` (`source_commit_hash` read back as `3d3ec62a`), serves byte-identical assets to the GCP original, and is discriminable as DigitalOcean from outside this fleet host. **But `app.smartcityos.io` does not resolve (NXDOMAIN, planner-confirmed 2026-09-17T23:0xZ), so no customer traffic is on it and the staff dashboards surface is still entirely GCP.** The two acts, both the operator's because DNS is outside this program's token by design (rule 12): (1) add `app.smartcityos.io` to `dolphin-app`'s domains and read back the target names DigitalOcean gives; (2) create `CNAME app -> dolphin-app-y4ixf.ondigitalocean.app`, TTL Auto, **DNS ONLY (grey cloud)** until the certificate issues. Order matters and the lane deliberately did not do (1) first, because attaching a domain that does not resolve starts ACME retries and fires `DOMAIN_FAILED`. The bake follows, against a captured baseline of 286 GCP requests over 2026-09-16T10:32Z..2026-09-17T22:46Z, all on `00074-sil`. D-12 does NOT decommission anything.

**D-5, D-9, D-10 and D-11 evidence is now on `main` (2026-09-18, OPS-17 A-146).** Their closes had been only on `seat/dispatch-planner`, so a fresh clone of `main` could not see why those rows are CLOSED. Merged as `0b92f8d6`, keeping the cited commits reachable.

**Read 2026-09-18 (A-8).**
- **D-12's two DNS acts are done.** `app.smartcityos.io` resolves to `dolphin-app` (`dns.mjs`, 11:58Z), so the NXDOMAIN reading in the note above is stale.
- **Staff have not all moved.** The GCP dashboards original took 30 browser requests after 2026-09-17T21:00Z (`callers.mjs`).
- **The GCP `smartcity-api` domain mapping still takes requests** on `smartcityos.io` and `www` from clients with stale DNS.
- **Open items 1 to 4 below are now owned by D-20.**

**Six open items have no owner yet.** Do not assume someone else has them:

1. **Bake period, then decommission** — D-5/D-9/D-10's droplets and the GCP `smartcity-api`/`smartcity-scraper`/`smartcity-dashboards` originals, once the bake reads clean. No row currently owns this. **GCP `smartcity-api` is NOT decommissionable until D-13 lands (2026-09-18):** the dashboards read every Bastrop feed from it, so removing it cuts every live figure on the product.
2. **Scraper concurrency, unresolved.** D-10 found the droplet's browser automation serializes behind ONE GLOBAL lock (600s timeout) — Cloud Run absorbed concurrent jobs per-instance; one droplet does not. Two of the ten Scheduler jobs' natural fires 500'd during verification, colliding with a long-running job on that same lock. The schedules need re-spacing or the jobs need explicit serialization before this is production-stable, not just working.
3. **The scraper droplet serves plain HTTP, no TLS.** The shared secret that replaced OIDC travels in clear text over the public internet. The DO agent token has no networking scope, so a cloud firewall cannot be added by a lane — this needs either a broader token or another mechanism, and it is a real gap, not a formality.
4. **`smartcityos.io`'s apex certificate renewal path is not understood.** Both the apex and `www` certificates expire 2026-12-16. DigitalOcean's own API reports the apex's certificate issuance as failed (`certificate_expires_at = 1970-01-01`) while a valid, chain-verified certificate is actually served through a different path — do not trust that API field, and do not assume renewal happens automatically without checking the served chain well before December.
5. **`smartcity-os` `origin/main` has moved to `92c73e39` and is unvalidated against the live original** — and this now matters more than it did, because `walrus-app` (running `8bea7fa`) serves real customer traffic. A rebuild from HEAD without D-5/D-9's byte-level validation would go straight to production unverified. **Sized 2026-09-18 (OPS-17 A-146): the drift is ONE commit, `92c73e39`, and it touches only `Dockerfile.scraper` and `server/scraper-worker.ts`. The API image `walrus-app` runs is unaffected, so the v1 API can be built from `main`, which OPS-17 G-159 needs for its v1 platform routes. D-10's caution about rebuilding the SCRAPER from `main` still stands.**
6. **`P-154` (48021:33223), a real FAIL unrelated to this migration, surfaced incidentally during D-10's work and has no owner.** Route it to whoever owns that property surface — it is not a DO-migration item and should not be worked by a DO-migration lane.

None of these six block calling D-9, D-10, or D-11 closed on their own stated scope — each was deliberately withheld or explicitly out of scope, and each close says so. They block calling this *program* finished.

## Governing rules

1. **No dispatch without a row.** Every dispatch names `PLAN-ROW: D-xx`. Enforced by `scripts/dispatch.mjs` and the canon-gate hook through `_catalog/plan_registry.json`.
2. **Migration order is leaf-to-root by verified call graph, not by name or by cost rank alone.** hauska-retrieval-api and smartcity-api have no internal caller found in any of the six services' live env vars — they move first. hauska-engine-api is called by hauska-mcp-server and cortex-api — it moves once its callers' env-var cutover is proven. cortex-api additionally calls a private IP (`10.128.0.2`, the `overpass-tx-01` VM) — it does not move until that dependency is resolved (co-migrate the VM or give it a public IP with a source-restricted firewall rule). hauska-mcp-server is the aggregator with the most fan-out and is customer-facing (paying MCP clients) — it moves last, and only after `mcp.hauska.dev` is live in front of the GCP original so the eventual cutover is a DNS change, not a customer reconfiguration. **Amended 2026-09-18 (A-8): leaf-to-root cannot hold.** The verified call graph has loops: `hauska-engine-api` and `cortex-api` call each other, and so do `hauska-mcp-server` and `plan-review`. Every service also has callers that no env-var flip reaches: hardcoded code, Vercel functions, an installed extension, a Revit add-in, and Stripe (evidence F-5 to F-8). The order is now **hostname-first**. D-25 puts a stable hostname in front of each GCP original and repoints every caller to it. Each cutover and each rollback is then one DNS record, taken in any order, one service at a time.
3. **Every cutover keeps the GCP original running, unmodified, through a bake period before decommission.** Rollback is reverting the one env var (backend-to-backend) or DNS record (smartcity-api's `smartcityos.io`, hosted at GoDaddy) that was changed. No row closes until the bake period is observed clean.
4. **DNS-based cutovers lower TTL a day ahead of the actual switch**, execute the record change deliberately, and watch both origins' logs during propagation.
5. **Statuses re-grade only by instrument**: live request logs, live latency/error-rate metrics, or a bill line — never a report of intent.
6. **DO tooling is fleet-wide already** (`_state/shared/STANDING_DECISIONS.md`, 2026-09-17) — no row needs to re-provision doctl or the MCP servers.
7. **A new host's egress IP is part of the deployment and must be verified against a control host.** Found on D-5: `municodemeetings.com` (and by extension any third-party government or vendor site with WAF protection) blocks some DigitalOcean IPs and not others, per-IP rather than per-provider or per-region. Before trusting any new droplet or app's outbound calls to a third-party site, test from a second host in the same account and region as a control — that separates "the site blocks DigitalOcean" from "the site blocks this one address." A blocked host is rebuilt on a fresh address, not repaired in place.
8. **DigitalOcean-to-GitHub is connected, scoped to `smartcity-os` and `smartcity-dashboards` only** (operator, 2026-09-17). Widening it to the Hauska-substrate repos is a separate decision for when D-3 resumes, not a default. **Amended 2026-09-18 (A-8): the operator ruled the widening.** The connection covers every repo the exit deploys, and the operator makes the change (D-16). Deploy-on-push stays disabled everywhere; shipping stays a deliberate, dispatched act (rule 17).
9. **A config-only cutover is only durable if it survives the repo's own deploy path.** Found on D-11 (2026-09-17): `hauska-mcp-server`'s `cloudbuild-mcp.yaml` carries `_DASHBOARDS_BACKEND_URL` and sets `DASHBOARDS_BACKEND_URL` on every build via `--set-env-vars`, so an env-var-only flip on the service is silently reverted by the next Cloud Build deploy. Any row that flips an env var on a service with a committed deploy template must change the template in the same unit of work, and must then read the value back off the serving revision. A flip the next deploy undoes is not a cutover, it is a delayed outage. Operator ruling 2026-09-17 (A-3). **A second, related trap found on the same row:** the service's traffic can be pinned to a revision BY NAME rather than following `latestReady` — `gcloud`'s own success message ("revision X has been deployed and is serving 100 percent of traffic") is true of the OLD pinned revision and conceals that a new revision came up `Ready=True` but `Active=False`. Read the traffic spec by field before trusting a deploy message; a canary (`--no-traffic --tag`, verify, then shift) is the safe pattern on a traffic-pinned service.
10. **Verify a cutover from a vantage point that cannot lie to you, and never trust a status field over an observation.** Found on D-9 (2026-09-17): this fleet host has TLS-interception middleware that re-signs every certificate, so a local handshake check cannot distinguish DigitalOcean's certificate from Google's — it would have reported a confident wrong answer. Direct HTTP checks against `*.ondigitalocean.app` from this host also failed with connection resets that did not occur from GCP Cloud Build. Validate from a vantage point outside the local network (D-9 used Cloud Build) and distinguish origins by an unfakeable signal — an edge header (`server: cloudflare` vs `server: Google Frontend`), not a local resolver or a local TLS handshake. Separately: an API status field that has merely stopped changing is not evidence a process stopped — DigitalOcean's certificate API read as permanently failed while its ACME agent was silently retrying every 5-11 minutes the whole time. Count events in a log before declaring a retry loop dead.
11. **A byte-identical response can misread as diverged if framing is compared as payload.** Found on D-9: Cloud Run sets `Content-Length`, DigitalOcean App Platform streams chunked — the same bytes, different transport framing. Any endpoint-comparison instrument for this program must classify payload and framing separately, or a clean migration will read as a failed one.
12. **DNS and domain management are outside this program's DigitalOcean token by design, confirmed twice.** `doctl compute domain list` / `domain records list` and DO's Reserved IP and Container Registry APIs all refuse this token (403) — this is deliberate scope minimization, not a bug to route around. Every DNS change in this program is executed by the operator, by hand, at GoDaddy. Do not request a token widening for this reason; request the operator's hand instead.

13. **A DigitalOcean deployment can report success and run a stale commit, so read back the commit it is actually running.** Found on D-12 (2026-09-17): a deployment reported `build=SUCCESS deploy=SUCCESS`, went ACTIVE on branch `main`, and was serving `f776b4bf` when `main` was `3d3ec62a`. Nothing in the deployment record said so; only a byte comparison of served assets caught it. **Every repoint or rebuild of a DO app must read back `services[0].source_commit_hash` and compare it to the intended commit**, and the remedy when they disagree is `POST /v2/apps/{id}/deployments` with `force_build`. This is the DigitalOcean analogue of rule 9's Cloud Run traffic pin: in both cases the platform's own success message is true of something other than what you asked for.
14. **Do not read a DigitalOcean certificate or domain status to decide whether a cutover is working, and never attach a domain before its DNS record resolves.** Found on D-12, and it compounds rule 10's certificate-API warning. Attaching a hostname that has no record starts ACME retries against a name that cannot validate, which fires the app's own `DOMAIN_FAILED` alert at the operator for a hostname they have not created yet. Order is: create the record, let it resolve, then attach. Where a hostname is Cloudflare-proxied, edge headers stop discriminating origin at all, so a behavioural marker in a response body is the only honest origin test.

15. **No synchronous request longer than 90 seconds is served from App Platform.** DigitalOcean's edge reportedly cuts HTTP at 100 seconds. That limit is community-reported for all apps and documented only for PHP apps, and D-17 P1 measures it. On 2026-09-18 about 20 requests a week ran past 100 seconds on services bound for App Platform (evidence F-9). Long work becomes asynchronous (202, then poll) or is bounded with a reasoned refusal before the service's cutover (D-27). `long-requests.mjs OVER=90` is the gate, with a positive control drawn from before the change. (A-8)
16. **A GCP original is decommissionable only when the host census says so.** `callers.mjs` must show zero non-uptime requests on EVERY hostname the original answers (its `run.app` URL and every domain mapping), through a bake window in which each known caller class was exercised. Each class is proven present in the window by a positive control. D-13 is why: a caller that fires only when a person opens a lens is invisible in a quiet window. The census also caught stale-DNS clients still reaching the GCP `smartcity-api` domain mapping a day after DNS moved. (A-8)
17. **DigitalOcean configuration is code.** Every app's spec is committed in its repo and ships only through that repo's deploy workflow. The workflow fills secrets from GitHub environment secrets, deploys, and fails when `source_commit_hash` is not the intended commit (rule 13). A console edit is not a change; the next deploy overwrites it. This is rule 9 carried over to DigitalOcean. (A-8)
18. **Anything a row creates to test or roll back carries its own removal.** That covers a Cloud Run tag, a UAT app, a probe droplet, and an interim service-account key. The row that creates it removes it, or declares it as a leave-behind with an owner. About 96 percent of Cloud Run service spend on 2026-09-18 was 0%-traffic tagged revisions still holding warm instances (F-1). Two probe apps and two probe droplets from D-5, D-9 and D-12 were still running on DigitalOcean (F-16). (A-8)
19. **VPC access or a dedicated egress IP, chosen per app at design time.** DigitalOcean makes them mutually exclusive, and a `nyc` app attaches only to `nyc1` VPCs. Any app or job that calls third-party hosts that filter by IP is tested from its actual egress path before cutover. That covers county GIS, CAD sites, MyGov, Municode and broker APIs. This extends rule 7 to App Platform and DOKS. (A-8)
20. **Rows that change Texas-program repos or services run only inside a window agreed with that program's planner (D-15).** Each lane's CP1 reads `_catalog/lane_claims.json` and the live factory lease table, and refuses while either holds the files or stores it would touch. An open claim is not a finished lane (A-3). (A-8)

## Baseline

| Row | Date | Status | Title / Scope | Depends |
|---|---|---|---|---|
| D-1 | 2026-09-17 | ADDED | **THE `ENFORCEMENT.md` / `enforcement.mdc` MIRROR HAS DRIFTED, FAILING EVERY COMMIT'S CI.** All 30 of the last 30 `enforcement` GitHub Actions runs on doc_repo main have failed on `C-00 vehicle sync`: three sections added to `ENFORCEMENT.md` on 2026-09-01, 2026-08-30, and 2026-09-15 were never propagated to `.cursor/rules/enforcement.mdc`. This is the dominant source of the daily CI-failure email volume the operator flagged 2026-09-16. **Scope:** overwrite the `.mdc` body with the current `ENFORCEMENT.md` content, keep the `.mdc`'s own frontmatter, verify `C-00 vehicle sync` passes on the next commit. **Depends:** none. | N |
| D-2 | 2026-09-17 | ADDED | **TWO GOOGLE CLOUD ALERT POLICIES ON hauska-mcp-server ARE FIRING ON NORMAL TRAFFIC, NOT AN OUTAGE.** Verified 2026-09-16 against live Cloud Monitoring data: request volume is naturally sparse (several 15-minute windows at zero requests), and the p95-latency alert (26.6s, 7.1s spikes) fires on cold-start-skewed percentiles computed over 3-5 samples. Zero 4xx/5xx in the checked window. **Scope:** retune or replace the "request count drop to zero for 15m" and "p95 latency above 3000ms for 5m" policies with thresholds that account for this service's actual traffic volume (larger window, minimum sample count, or switch to an error-rate-based alert). **Depends:** none. | N |
| D-3 | 2026-09-17 | HELD (operator 2026-09-17: hold until `p275-live-currency-five-counties` frees fully; dispatch compiled and ready, not yet sent). **RECOMPILE BEFORE FIRING (A-8, 2026-09-18).** The compiled dispatch says `hauska-mcp-server` is the only caller. Read live, `hauska-retrieval-api` has at least seven caller paths: `hauska-engine-api` `RETRIEVAL_API_URL`, the dashboards on GCP and on DigitalOcean, `cortex-api`'s `BRIEF_RETRIEVAL_API_URL` secret, four hardcoded LDT api-server files, three hardcoded `hauska-map` files, and Vercel functions (evidence F-5). The row is now a DNS cutover behind D-25's hostname, in window W2, depending on D-21, D-22, D-25 and D-27 | **PHASE 1 PILOT: MIGRATE hauska-retrieval-api TO A DIGITALOCEAN DROPLET.** Repo `hauska-engine`. No internal caller of this service references anything it calls out to beyond its own databases (`DATABASE_URL`, `CORTEX_DATABASE_URL`, `FACTORY_DATABASE_URL_RO`) — it is a leaf in the verified call graph, and the lowest-blast-radius candidate to prove the whole migration pattern before touching a higher-cost or customer-facing service. **Scope:** stand up the service on a DO Droplet; replicate its secrets; smoke-test directly against the new address with real read queries; run in parallel with the GCP original for a bake period; flip `HAUSKA_BACKEND_URL` on hauska-mcp-server to the new address and redeploy just that caller; watch error rate and latency; keep the GCP original live and unmodified until the bake period reads clean. **Depends:** none. | Y |
| D-4 | 2026-09-17 | SCOPED (not started) | **PHASE 2: MIGRATE hauska-engine-api.** Called by hauska-mcp-server (`HAUSKA_ENGINE_API_URL`) and cortex-api (`ENGINE_API_URL`); currently the single largest Cloud Run cost line and observed running at or above its configured max-instance ceiling for large stretches of the month, a capacity signal worth checking independent of the migration. **Depends:** D-3 (pattern proven). **Amended 2026-09-18 (A-8).** Now a DNS cutover behind `engine.hauska.dev` (the name is proposed, D-16), in window W2. It also depends on D-27: the flood-drainage, feasibility-export and site-plan-export refresh routes ran for up to 191 seconds this week, past App Platform's edge. The measured instance counts were inflated by canary tags: its serving need is one or two dedicated 2 vCPU / 4 GiB instances (evidence §3). | N |
| D-5 | 2026-09-17 | CLOSED-PARTIAL (verified against committed artifact `14284eb7`, `seat/dispatch-planner`, hashes spot-checked and matched) | **PROMOTED, FRESH-BUILD MIGRATION OF THE WHOLE SMARTCITY PRODUCT LINE (smartcity-api, smartcity-scraper, smartcity-dashboards) TO DIGITALOCEAN.** Repos `smartcity-os` and `smartcity-dashboards`, confirmed idle since 2026-09-15 (`gh repo list`, last push both dates) while `hauska-engine`, `hauska-factory`, and `hauska-map` all show commits from today — zero repo overlap with the two currently active lanes (`p249-envelope-unlock`, `p275-live-currency-five-counties`), so this moves independently of the Hauska-substrate hold on D-3. Each of the three services already has its own dedicated Neon project, not the shared Factory/Cortex/atoms stores the busy lanes are working against — no database-level coupling either. `smartcity-dashboards` calls out to `hauska-retrieval-api` and `hauska-mcp-server` over plain HTTPS (`HAUSKA_RETRIEVAL_URL`, `HAUSKA_MCP_URL`); those stay pointed at their current GCP addresses, unchanged, for the duration of this row — this is a fresh build of SmartCity's own compute, not a Hauska-substrate cutover. `smartcity-api` serves `smartcityos.io` on a GoDaddy-hosted custom domain (confirmed via live request logs and DNS lookup) — the one piece of this row that is a DNS-based blue-green cutover rather than a green-field build; `smartcity-dashboards` and `smartcity-scraper` have no custom domain in front of them and can be stood up and cut over on their own `*.run.app`-equivalent DO addresses without a DNS step, other than repointing whatever calls them (verify against smartcity-dashboards' own callers before assuming none exist, per D-3's mission the same caution applies here). **Result:** all three services built and running on DO from their exact GCP-deployed commits (api `8bea7fa`, scraper `f21e49d`, dashboards `96efa35` — the latter two BEHIND `origin/main`, do not redeploy from HEAD without re-validating). 40 of 40 swept endpoints matched (23 api, 1 scraper, 16 dashboards — corrected from an internal 17 in the raw close), 3 of 3 health surfaces matched. No cutover performed; production remains entirely GCP. Three real findings carried forward: a third-party WAF (Municode) blocks some DO egress IPs and not others (governing rule 7); the dispatch's own "zero Cloud Scheduler jobs" and image-tag claims were wrong, corrected by the lane (ten enabled jobs drive the scraper via OIDC, real commits read from Secret-Manager-adjacent build metadata, not an env var); `Dockerfile.api`'s HEALTHCHECK calls `curl`, absent on `node:22-slim` — cosmetic on Cloud Run, will read as unhealthy on App Platform until fixed. Operator confirmed 2026-09-17 the Bastrop-facing password-reset side effect is a non-issue (customer already notified of cutover-related interruption). Continuation work carded as D-9, D-10, D-11. | Y |
| D-6 | 2026-09-17 | SCOPED (not started) | **PHASE 4: MIGRATE cortex-api, WITH `overpass-tx-01`.** Blocked until the private-IP dependency on the Overpass VM (`10.128.0.2`, `legacy-design-tools-prod`) is resolved — co-migrate the VM (the simplest lift-and-shift in the whole estate: a plain Compute Engine VM) rather than building a temporary cross-cloud allowlist. **Depends:** D-4 (calls hauska-engine-api). **Amended 2026-09-18 (A-8).** Overpass is REBUILT, not lifted, by D-24 from `legacy-design-tools/infra/overpass` on a VPC-only `nyc1` droplet; its CPU averaged 0.3 percent of 8 vCPU. This row then becomes a DNS cutover behind `api.smartsite.cloud` (proposed, D-16) in window W2. It also carries:<br>- the `records-request-worker`, in the shape D-17 decides;<br>- the `county-ledger-heartbeat` Scheduler job, which becomes a DOKS CronJob because it runs every 10 minutes, under App Platform's 15-minute floor;<br>- the Stripe webhook endpoint (D-26);<br>- the move of `cortex-api`'s uptime check out of `smartcity-os-prod`, where it lives today (evidence F-13).<br>It depends on D-4, D-23, D-24, D-26 and D-27. | N |
| D-7 | 2026-09-17 | SCOPED (not started) | **PHASE 5: MIGRATE hauska-mcp-server, LAST.** Customer-facing MCP surface; paying customers' MCP clients likely point at the raw `*.run.app` URL directly today. Put `mcp.hauska.dev` in front of the GCP original first (per HR-12/substrate-v1's original intended launch subdomain, `hauska.dev` now registered and resolving), confirm customers are hitting the friendly hostname, then execute the DO cutover as a DNS change behind it. **Depends:** D-4, D-5, D-6 (calls all three). **Amended 2026-09-18 (A-8).** `mcp.hauska.dev` is still NXDOMAIN (`dns.mjs`, 11:58Z). It is created in D-25, and paying clients are moved to it in D-26. The `hauska-brief-extension` 0.6.33 bundle hardcodes this service's `run.app` host. Window W2. It depends on D-3, D-4, D-6 and D-26. | N |
| D-9 | 2026-09-17 | CLOSED (verified against committed artifact `f3c3c4a1`, `seat/dispatch-planner`) | **CONFIGURE THE TWO DO APPS AND COMPLETE THE smartcity-api CUTOVER.** **Result:** `dolphin-app` = `smartcity-dashboards` (commit `178e968` = `96efa35` + a `.gitattributes` fix to preserve CRLF web assets across the build, no custom domain, no customer traffic, 17/17 paths byte-identical to GCP). `walrus-app` = `smartcity-api` explicitly (Dockerfile.api selected out of `smartcity-os`'s two components), commit `e783f351` = `8bea7fa` + the healthcheck fix, deploy-on-push disabled on both. `smartcityos.io` and `www` attached and LIVE: served by App Platform's Cloudflare-fronted edge over chain-verified certificates, 23/23 paths matching at apex and at `www`, 0 hard divergences. GoDaddy DNS changed by the operator, by hand, after the lane supplied the target records. GCP `smartcity-api` received zero real requests from 20:32:30Z on — only its uptime checker remains. **Self-corrected twice before closing:** an initial "no certificate exists" reading was wrong (DO's own issuance API tracked a failed attempt on a path the served certificate doesn't use); an initial "commit unrecoverable" state was resolved to none; and the close itself was first filed uncommitted, on a misreading of AGENT_CONTRACT.md §6, then corrected once precedent (D-10, D-11) was checked. Governing rules 10-12 come from this row. **Owed, explicitly out of this row's scope:** the bake period, then decommissioning the D-5 droplets and the GCP originals; understanding the apex certificate's real renewal path before 2026-12-16; a possible stale `DOMAIN_FAILED` alert (bookkeeping, not a fault); `smartcity-os` `main` has moved to `92c73e39`, unvalidated, and matters more now that `walrus-app` carries production traffic. **Depends:** D-5. | Y |
| D-10 | 2026-09-17 | CLOSED-PARTIAL (verified against committed artifact `f8a687cb`, `seat/dispatch-planner`) | **SWITCH THE SCRAPER'S TRIGGER FROM OIDC TO A SHARED SECRET.** **Result:** all ten Cloud Scheduler jobs repointed to `http://159.89.190.154:8080<path>` with a shared secret header (`X-Scraper-Trigger-Secret`), OIDC removed; the droplet enforces it fail-closed on every route including health; all ten jobs triggered for real and returned 200, corroborated by the app's own `[auth] authorized` log lines; seven negative probes with no/wrong header all refused with 401. GCP `smartcity-scraper` untouched, still costs money, still the rollback target. **Deliberately not closed, two real gaps named rather than hidden:** (1) the droplet's browser automation is serialized behind ONE GLOBAL Chromium lock (600s timeout) where Cloud Run absorbed concurrent jobs per-instance — two of the ten jobs' natural fires 500'd when this lane's own 750s verification run held the lock; the schedules need re-spacing or explicit serialization before this is production-stable. (2) The droplet serves plain HTTP with no TLS — the shared secret travels in clear text, and the DO token's lack of networking scope means a lane cannot add a cloud firewall. **Standing caution:** the deployed scraper image is `f21e49d` plus the auth patch, NOT `main` (`main` has since merged the auth middleware at `92c73e39` but the enrichment route files it dynamically imports differ from what's deployed) — do not rebuild from HEAD without re-validating. **Depends:** D-5 (droplet exists and is validated). | N |
| D-11 | 2026-09-17 | CLOSED (verified against committed artifact `40eaae82`, `seat/dispatch-planner`) | **FLIP hauska-mcp-server'S DASHBOARDS_BACKEND_URL.** First attempt stood down cleanly at 18:39Z when D-9 was still in flight (recorded as A-3) — re-fired once D-9's close landed. **Result:** `cloudbuild-mcp.yaml`'s `_DASHBOARDS_BACKEND_URL` substitution changed and pushed directly to `origin/main` (`10451ee`, 1 file, 1 line — `main` is unprotected on this repo) so the flip survives the next Cloud Build deploy, per governing rule 9. **Found the same row also needed a canary, not a plain redeploy:** the service's traffic was pinned to a revision BY NAME, so the first redeploy came up `Ready=True`/`Active=False` while `gcloud` reported the OLD revision as still serving 100 percent — a plain deploy would have been silently a no-op. Corrected with the repo's own canary path (`--no-traffic --tag`, verify on the tag URL, then `update-traffic`), now governing rule 9's second clause. Revision `hauska-mcp-server-00105-gex` serves 100% of traffic carrying the DigitalOcean address; 3/3 dashboards tools that traverse it return results semantically identical to pre-flip, 0 hard divergences; GCP `smartcity-dashboards` logged zero calls from the MCP server after the flip. **Depends:** D-9 (dashboards app must be live and validated first). | N |
| D-8 | 2026-09-17 | SCOPED (not started) | **SMARTCITY OS CONTRACT COST MODEL.** `_smartcity_masters/Pricing/00_pricing_basis.md`'s "fixed spine infrastructure runs $1,250-7,200/mo portfolio-wide" line is six weeks stale against verified live billing. Update it with real numbers: SmartCity-specific infra (smartcity-api, smartcity-dashboards, smartcity-scraper) currently runs ~$300-320/mo against Bastrop's $12,000/yr contract, meaning infra alone is ~30% of contract revenue at n=1 customer — true only until a second city amortizes the same fixed base. Recommend a cost-of-service escalator or a 12-18-month-forward cost basis for new multi-year contracts rather than pricing against today's snapshot. **Depends:** none. | N |
| D-12 | 2026-09-17 | **CLOSED-PARTIAL 2026-09-17 — STEPS 1-4 DONE AND VERIFIED; THE CUTOVER IS THE OPERATOR'S TWO ACTIONS.** Close `_inbox/2026-09-17_d12-dashboards-do-cutover_close.json`. **Done:** `main` is the build source with `services[0].source_commit_hash` read back as `3d3ec62a`; all 7 `web/` blobs on main carry the SAME blob sha as the pin branch with CRLF byte-shape intact; CI conclusion STRINGS read per job (a11y `success`, test `success`); dolphin-app discriminated as `server=cloudflare` against GCP `Google Frontend` from two probe hosts OUTSIDE this fleet host; the GCP original is unmodified as the rollback path. **NOT done, and it is the customer predicate:** traffic is not on the DO app because `app.smartcityos.io` does not resolve (NXDOMAIN, confirmed by the planner 2026-09-17T23:0xZ). DNS is outside this program's DO token BY DESIGN (rule 12), so the lane prepared the record and stopped, which is correct. **Three findings kept.** (1) **A DigitalOcean deployment reported `build=SUCCESS deploy=SUCCESS`, went ACTIVE on branch `main`, and was running a commit from BEFORE the change** (`f776b4bf` against a then-current main of `3d3ec62a`); only a byte comparison caught it. Every future repoint MUST read back `services[0].source_commit_hash`, and the remedy is `POST /v2/apps/{id}/deployments` with `force_build`. This is the DigitalOcean analogue of rule 9's Cloud Run traffic pin. (2) GATE B could not use a byte marker because `git show --name-only f776b4bf` returns ZERO `web/` files, so the lane used a behavioural marker (a 500 body on `/auth/sign-in`), which then doubled as an origin discriminator for a Cloudflare-proxied hostname where edge headers cannot discriminate. (3) The F1 falsifier fired early: an apparent "main does not build on DigitalOcean" was disproved by a paired control when the pin branch, the code serving healthy production, failed identically with the same `assertNoSupplierDsn` error — converting a false finding into "my secret-reading instrument was lossy". **Deliberately not done:** the domain was NOT attached to the app, because that starts ACME retries against a non-resolving name and fires `DOMAIN_FAILED` at the operator for a hostname they have not created. **Bake baseline captured for later comparison:** 286 GCP requests over 2026-09-16T10:32Z..2026-09-17T22:46Z, all on `00074-sil`. | **PUT CUSTOMER TRAFFIC ON THE DASHBOARDS DO APP, AND MAKE MAIN THE THING IT BUILDS.** Operator ruling 2026-09-17: all SmartCity surfaces move to DigitalOcean before any design build work begins. That promotes this from a D-9 leave-behind to the gate on the entire SmartCity design build program (`_decisions/2026-09-17_design_ratification_all_approved.md`, OPS-17 G-149). `dolphin-app` appears exactly once in this document before this row, inside D-9's close, which states it has no custom domain and no customer traffic. **Nothing owned this.** Measured 2026-09-17T21:2xZ, not remembered: `smartcityos.io` and `www` return `Server: cloudflare` + `CF-RAY` (DO edge), while `smartcity-dashboards-52ecsl5mvq-ue.a.run.app` returns `server: Google Frontend`, so the staff dashboards surface is still entirely GCP. **The backend consumer already cut over and the human-facing path did not:** D-11 flipped `hauska-mcp-server`'s `DASHBOARDS_BACKEND_URL` to the DO address, so `dolphin-app` already serves MCP tool traffic. **Scope, and the second item is the one that bites.** (1) The GCP original lives in its OWN project `smartcity-dashboards` (us-east1), NOT `smartcity-os-prod`, and its traffic is PINNED BY REVISION NAME to `smartcity-dashboards-00074-sil` at 100% with `latestRevision=false` and 29 accumulated tagged revisions, so governing rule 9's second clause applies to any rollback on it. (2) **Reconcile the pin-branch divergence, or every future build row silently misses its target.** `dolphin-app` builds from branch `d9-pin-96efa35` at `178e968b`, which against `main` is ahead 1 (the `.gitattributes` CRLF fix, absent from main) and behind 1 (`f776b4bf`, the G-134 WorkOS commit). Verified by `gh api compare`. A design build merged to main therefore does not reach the app even after a cutover. Land the CRLF fix on main, confirm the WorkOS commit builds clean on DO, and repoint the app at main rather than at a frozen side branch. (3) **The human-facing hostname is `app.smartcityos.io`, ruled by the operator 2026-09-17.** `smartcityos.io` and `www` stay on `walrus-app`; dashboards takes its own record under the domain Bastrop already knows. The reasoning is governing rule 2's own logic applied one level down: a real hostname in front of the app makes any future provider move a DNS change rather than a customer reconfiguration, and Bastrop staff re-bookmark once rather than twice. The record is created by the operator at GoDaddy per rule 12; the lane supplies the target values and never touches DNS. (4) Decide the deploy-on-push posture once main is the build source; leaving it disabled keeps merges inert and keeps shipping a deliberate act, which is the current and safer default. (5) Cut over, keeping the GCP original running unmodified through a bake per governing rule 3. **Instrument:** the staff surface resolves to the DO edge by `Server`/`CF-RAY` from a vantage point outside this fleet host per governing rule 10, payload and framing classified separately per rule 11, a commit merged to main demonstrably reaches the running app, and the GCP original logs zero real customer requests through a clean bake. DNS executed by the operator at GoDaddy per rule 12. **Depends:** D-9 (closed). | Y |
| D-13 | 2026-09-18 | **OPEN 2026-09-18 — THE CODE LANDED AND THE PRODUCTION STEP DID NOT (A-9).** `smartcity-dashboards` `origin/main` `7487d7c0` carries `src/platform-base.mjs` (one configured base, the per-feed defaults removed, an unset base REFUSING with a named basis), the GCP host is absent from `src/` and `web/`, and `d12-main-uat` sets the base to `walrus-app`, all read at source by the planner at 16:26Z. **Step 2 has not run:** `dolphin-app`, which serves `app.smartcityos.io`, carries no `SMARTCITY_V1_PLATFORM_BASE` and serves `3d3ec62`, where the feeds are the hardcoded GCP URLs this row exists to remove, so for customers this row's defect is still live. Steps 2 and 4 are the A-149 ship, which carries the variable in the same deploy; the feed-parity comparison (scope 4) and the zero-platform-request bake on GCP `smartcity-api` (scope 5) are ungraded, and no lane close, CP1 or CP2 is filed | **THE DASHBOARDS STILL READ EVERY BASTROP FEED FROM THE GCP v1. REPOINT THEM AT walrus-app, OR DECOMMISSIONING GCP CUTS EVERY LIVE FEED.** Read at `smartcity-dashboards` `origin/main` `fd8562c2`: `src/vendor-live.mjs` HARDCODES `https://smartcity-api-7dyaiy7wha-uc.a.run.app/api/platform/...` for Samsara, Spireon, FirstDue, Power BI and GoTo, with no override, and `src/mygov-live.mjs` defaults MyGov to the same host (overridable only by `MYGOV_PLATFORM_BASE`). That host is the GCP `smartcity-api` in `smartcity-os-prod`, which this program treats as the idle rollback copy. **It is not idle; it is the dashboards' data source.** Measured: on 2026-09-16 it served platform requests for every feed (Samsara, Spireon, GoTo, Power BI, all five MyGov routes, property-intel). It served none since 2026-09-17T12:00Z because nobody opened a lens in that window, not because the dependency ended. The query was checked against a positive control that returned 49 `/api/health` hits. So `dolphin-app` on DigitalOcean reaches across clouds to GCP for every live figure, and D-9's reading that GCP `smartcity-api` "received zero real requests" held for its window and does not describe the dependency. **Scope:** (1) replace the six hardcoded URLs with ONE configured base for the v1 platform routes, read from the environment with no silent default to a host that is being retired; (2) set it on `dolphin-app` to `walrus-app`, and confirm `walrus-app` carries the `PLATFORM_INTERNAL_API_KEY` the dashboards send; (3) per governing rule 9, the value lives where the next deploy cannot revert it; (4) read every feed back through `walrus-app` for `bastrop_tx`; (5) only then is GCP `smartcity-api` decommissionable, after its own bake. **Instrument:** each feed returns the same records through `walrus-app` as through GCP, compared as payload not framing (rule 11); `dolphin-app`'s `source_commit_hash` is read back (rule 13); GCP `smartcity-api` logs zero platform requests through a bake window in which a lens was actually opened. Repos `smartcity-dashboards`. **Depends:** D-9 (closed), D-12 (closed-partial). **OPS-17 G-159's finance bridge waits on this for its last step,** because its new routes deploy to `walrus-app` and the dashboards cannot reach them until this lands. **Scope widened 2026-09-18 (A-8, evidence F-8).** The GCP host `smartcity-api-7dyaiy7wha-uc.a.run.app` is hardcoded in FIVE `smartcity-dashboards` files at `fd8562c2`, not two:<br>- `src/vendor-live.mjs` and `src/mygov-live.mjs`, as named above;<br>- `src/mygov-permits.mjs` (`DEFAULT_MYGOV_PLATFORM_URL`);<br>- `src/property-map.mjs` (the property-intel summary and layers);<br>- `src/adapters.mjs`, where it is shown to users as each feed's `sourceUrl`, which is provenance.<br>All five read the one configured base. `code-refs.mjs` reads zero for that host in the repo. | Y |
| D-14 | 2026-09-18 | **LANDED 2026-09-18, read at source by the planner at 16:26Z (A-9) — two clauses ungraded, no lane close filed.** `walrus-app` deployment `e243db0a` is ACTIVE, deployed 15:41:52Z with `cause: manual`, and no longer builds from the side branch `d9-api-8bea7fa`. `services[0].source_commit_hash` reads `1f0262f15f8d1898a6c32826ec2e45407518e29e`, which IS `smartcity-os` `origin/main` HEAD read independently by `git rev-parse`, and the G-159 routes answer `401 platform_internal_required` on `smartcityos.io` while a fake path answers the 200 SPA shell. **Unverified, and it is why this row reads landed and not closed:** the payload-parity comparison of `smartcityos.io` and `www` against their state before the repoint, and the health probe. Counted landed, not done | **walrus-app BUILDS FROM A SIDE BRANCH, SO NOTHING MERGED TO v1 `main` CAN REACH `smartcityos.io`.** The same divergence D-12 found and fixed for `dolphin-app`, one repo over, and this one serves production. Read 2026-09-18: `walrus-app` builds from branch `d9-api-8bea7fa` at `e783f351`, which is `8bea7fa` plus the D-9 healthcheck fix (`Dockerfile.api` called `curl`, absent on `node:22-slim`, so every probe marked a healthy service unhealthy). That fix is NOT on `smartcity-os` `main`; `main` is one commit ahead with the D-10 scraper change, and the pin is one ahead with the healthcheck fix. So any v1 change merged to `main`, including OPS-17 G-159's finance platform routes, never reaches `walrus-app`. **Scope, D-12's shape:** (1) land the healthcheck fix on `main`; (2) prove `main` builds and runs on a NON-production DigitalOcean app first, since `main` also carries the scraper commit (which touches only scraper files, so the API image should be unaffected, and the proof is what establishes that); (3) repoint `walrus-app` at `main`; (4) keep deploy-on-push disabled, so shipping stays deliberate; (5) the GCP `smartcity-api` original stays untouched as rollback, per rule 3. **Instrument:** `services[0].source_commit_hash` on `walrus-app` read back and equal to `main` (rule 13, which exists because a DigitalOcean deploy once reported success while running the wrong commit); `smartcityos.io` and `www` serve payloads byte-identical to before the repoint, compared as payload not framing (rule 11), verified from a vantage point outside this fleet host (rule 10); the health probe passes. Repo `smartcity-os`. **Depends:** D-9 (closed). **OPS-17 G-159's v1 routes wait on this to reach production, and D-13 waits on it too if the dashboards are repointed at new routes.** | Y |
| D-15 | 2026-09-18 | ADDED (A-8) | **COORDINATE THE EXIT WITH THE TEXAS SCALE-UP PLANNER BEFORE ANYTHING TOUCHES ITS REPOS OR SERVICES.** The operator hand-carries `_inbox/2026-09-18_gcp_exit_TEXAS_COORDINATION.md`. **Output:** an agreement in `_inbox/` naming:<br>(1) windows W1 to W4 (plan §8) and their triggers, including the W3 factory-freeze slot, proposed at a Texas phase boundary;<br>(2) which of D-19, D-23, D-27 and D-31 the Texas program owns as OPS-16 rows;<br>(3) the timing of the canary-leak fix;<br>(4) the definition of "clean pause". **Gate:** the agreement exists and names all four; no Texas-repo row starts before it (rule 20). **Owner:** integration seat with the Texas planner and the operator. **Window:** none. **Depends:** none. | N |
| D-16 | 2026-09-18 | ADDED (A-8) | **OPERATOR PREREQUISITES FOR THE EXIT.**<br>(1) A migration DigitalOcean token scoped for apps, droplets, VPC, Kubernetes, registry, Spaces keys, firewalls, monitoring and uptime, databases, reserved IPs, load balancers and certificates. It carries NO domains scope (rule 12).<br>(2) The droplet limit read and raised (the fleet token cannot read the account).<br>(3) The DigitalOcean GitHub connection widened (ruled; rule 8).<br>(4) The GCP billing export to BigQuery enabled NOW. It records only going forward, and without it no bake can be graded on a bill line (rule 5).<br>(5) Alert destinations.<br>(6) The hostnames in plan §3 ratified or renamed.<br>(7) Who owns GCP project `1160337742`, which holds the SmartCity Google sign-in client.<br>(8) Who holds the `smartcity-agent@smartcity-os-prod` key.<br>(9) Access to the Stripe and Samsara webhook settings.<br>(10) Whether a broker allowlists the trading bot's IP. **Gate:** `scripts/infra/gcp-exit/do-read.mjs` shows the needed scopes as 200; the export dataset exists; items 6 to 10 are answered in writing. **Owner:** operator. **Depends:** none. | N |
| D-17 | 2026-09-18 | ADDED (A-8) | **PLATFORM PROBES, THEN A SHAPE DECISION RECORD.** Throwaway resources, all deleted at close (rule 18): one App Platform app, one droplet in `nyc1` and one in `ric1`, and a small DOKS cluster. Each probe states its falsifier:<br>P1: the 100-second edge, for plain HTTP and for streamable HTTP/SSE.<br>P2: whether component-to-component calls inside one app avoid the edge and its timeout.<br>P3: whether deploying one component restarts the others.<br>P4: whether a VPC-attached app is reachable privately from another app or from DOKS.<br>P5: the database round trip to Neon us-east-1 and us-east-2 from `nyc1` and from `ric1`.<br>P6: a DOKS Job launched through the API, a 10-minute CronJob, the pool scaling to zero, and image pulls from DOCR and from GHCR.<br>P7: whether a static egress path (NAT gateway) exists for DOKS.<br>P8: the GCS S3-interoperability API with an HMAC key, for our reads, writes, lists and presigned URLs.<br>P9: reachability from DigitalOcean to every third-party host the services and the factory call, with a control host (rule 7). **Output:** a decision record choosing: grouping, the records-worker shape, the region, the registry, the egress design, and D-34's storage path. **Owner:** a DigitalOcean lane. **Window:** none. **Depends:** D-16 (token). | N |
| D-18 | 2026-09-18 | ADDED (A-8) | **CAPTURE THE BAKE BASELINES.** For every service, commit dated output of `service-metrics.mjs`, `callers.mjs` (host census, with controls), `long-requests.mjs` and `cost-by-revision.mjs`, each with its snapshot line. From D-16's export on, add bill lines per project. Every Phase 4 bake compares against these. **Owner:** integration seat. **Window:** none. **Depends:** none. | N |
| D-19 | 2026-09-18 | ADDED (A-8) | **STOP THE CANARY-TAG LEAK: ABOUT $2,400 A MONTH, ABOUT 96 PERCENT OF CLOUD RUN SERVICE SPEND (evidence F-1).** Every tagged revision of a min-instances service keeps a warm instance. `smartsite-mcp` has 33 tags and averaged 30.5 warm instances while serving about 256 requests a day. P-251's removal of 37 engine tags on 2026-09-16 at about 16:53Z stopped their billing at that minute. **Scope:**<br>(1) remove the 0%-traffic tags that hold warm instances on the six min-instances services (`hauska-engine-api`, `hauska-mcp-server`, `hauska-retrieval-api`, `cortex-api`, `smartsite-mcp`, `smartcity-api`);<br>(2) each repo's canary procedure removes its tag after the traffic shift;<br>(3) extend P-279's post-deploy step to refuse a 0%-traffic tagged revision that holds a minimum instance. **Gate:** for 7 days, the serving revision carries at least 80 percent of billable time on each min-instances service (`cost-by-revision.mjs`); a stale tag left deliberately on staging is refused (proof by violation). **Owner:** property and substrate seats, coordinated through the Texas planner (P-279 is theirs). **Window:** W1. **Depends:** D-15. No serving revision changes. | N |
| D-20 | 2026-09-18 | ADDED (A-8) | **SMARTCITY DECOMMISSION BUNDLE; OWNS OPEN ITEMS 1 TO 4.** After D-12's bake (staff on `app.smartcityos.io`), D-13 and D-14:<br>(1) walrus-app reads the scraper URL from config, not the hardcoded GCP host in `server/routes/mygov.ts`; GCP hosts are removed from the CORS and CSP lists in `server/app.ts`; walrus-app is resized to shared 1 vCPU / 2 GiB with 2 instances (its p99 memory on GCP reached 0.85 of 1 GiB, F-4); health checks go on walrus-app and dolphin-app.<br>(2) The scraper droplet: the 10 Scheduler jobs become local cron with `flock` serialisation, trigger routes bind to localhost, and Caddy serves TLS for the one route walrus-app calls. This closes items 2 and 3 and keeps the WAF-accepted IP.<br>(3) DigitalOcean Uptime and SSL-expiry checks on `smartcityos.io` and `app.smartcityos.io`, closing item 4. `cortex-api`'s uptime check and alert move out of `smartcity-os-prod` first.<br>(4) The DigitalOcean leftovers `d12-main-uat`, `d5-capability-probe`, `d5-smartcity-dashboards` and `d5-smartcity-api` are deleted (rule 18).<br>(5) Once the host census reads zero on every host through a bake in which a lens was opened (rule 16): the GCP services, domain mappings, the 10 Scheduler jobs and the `smartcity-agent` key (once its holder is known) are deleted, and `smartcity-os-prod` and `smartcity-dashboards` are shut down. **Owner:** SmartCity lanes. **Window:** none. **Depends:** D-12, D-13, D-14. | N |
| D-21 | 2026-09-18 | ADDED (A-8) | **CONFIGURATION AND SECRETS AS CODE ON DIGITALOCEAN (rule 17).**<br>Every app's spec is committed in its repo. A hand-run (`workflow_dispatch`) GitHub Actions deploy fills secrets from GitHub environment secrets, deploys, and fails on a `source_commit_hash` mismatch.<br>A registry file lists which repos hold each shared key, and a fingerprint check fails when the holders disagree.<br>Piloted on walrus-app and dolphin-app, which are console-configured today.<br>It retires the Secret Manager to Vercel key-sync workflow in `hauska-map` and its `hauska-pe-key-sync` keys (F-12, F-14). **Gate:** a console edit is overwritten by the next deploy; a deploy of a stale commit fails; a planted key mismatch fails the drift check. All three are violation tests. **Owner:** each repo's seat. **Window:** none for the SmartCity repos, W1 for Texas repos. **Depends:** D-16. | N |
| D-22 | 2026-09-18 | ADDED (A-8) | **OBSERVABILITY PARITY BEFORE ANYTHING IS DELETED.**<br>Map each GCP uptime check (6), alert policy (9) and log-based metric (3) to its replacement: DigitalOcean Uptime on functional health endpoints that return non-2xx when functionally down; App Platform alerts; and one log-forwarding destination (decision here) for error rate and MCP latency, which App Platform cannot alert on (F-18).<br>Fix the `api.empressa.pro` uptime check now: it probes the literal path `/C:/Program Files/Git/health`, so it and its alert have measured nothing (F-13; markets seat). **Gate:** each replacement fired once on purpose. **Owner:** integration seat with each seat. **Window:** none. **Depends:** D-16. | N |
| D-23 | 2026-09-18 | ADDED (A-8) | **DOKS JOB PLANE, PROVEN ON STAGING.**<br>A cluster in the `nyc1` VPC, with a system pool and a jobs pool that scales 0 to N on 16 GiB nodes; `factory-control` runs as a Deployment.<br>A Kubernetes Jobs adapter sits behind `cloudrun-jobs.mjs`'s interface. `runs.mjs` and `heavy-lease.mjs` gain a host type whose identity is the Job uid, and a run record without its execution identity refuses to write.<br>CronJobs replace the two factory Scheduler jobs.<br>`secret-rotation.mjs` writes a Kubernetes Secret instead of Secret Manager.<br>Staging only; no production writes (F-10). **Gate:** the same jobs run on both planes against the same staging inputs produce row-identical outputs; every DOKS run record carries its Job identity. **Owner:** property seat, through the Texas planner (D-15). **Window:** none. **Depends:** D-16, D-17. | N |
| D-24 | 2026-09-18 | ADDED (A-8) | **OVERPASS REBUILT ON A VPC-ONLY DROPLET.** Built from `legacy-design-tools/infra/overpass` on the pinned image, in `nyc1`, bound to its VPC IP. A DigitalOcean cloud firewall allows VPC ingress only and SSH from the operator only; no RDP (the GCP VM allows SSH and RDP from `0.0.0.0/0` today, F-14). Sized from the import (CPU averages 0.3 percent today). Not used by `cortex-api` until D-6. **Gate:** `smoke.sh` passes; a fixed bbox set returns identical ways from GCP and DigitalOcean; the port is unreachable from outside the VPC (a violation probe). **Owner:** property seat. **Window:** none. **Depends:** D-16, D-17. | N |
| D-25 | 2026-09-18 | ADDED (A-8) | **STABLE HOSTNAMES IN FRONT OF GCP, THEN EVERY CALLER REPOINTED; ZERO BEHAVIOUR CHANGE.** Proposed names (D-16 ratifies): `engine.hauska.dev`, `retrieval.hauska.dev`, `mcp.hauska.dev`, `factory.hauska.dev`, `api.smartsite.cloud`, and for D-28 and D-29 their own names. The operator creates the records (rule 12), and a Cloud Run domain mapping serves each on GCP; confirm `us-east4` support for `factory-control`. Then one lane per owning repo repoints:<br>- the `cloudbuild-mcp.yaml` substitutions;<br>- the LDT workflow env;<br>- the engine env and the `cortex-api` `BRIEF_RETRIEVAL_API_URL` secret;<br>- the hardcoded files in evidence F-5 to F-8;<br>- every Vercel production environment (the evidence §8 blind spot);<br>- scripts and uptime checks. **Gate:** `callers.mjs` shows zero non-uptime requests on `*.run.app` for 7 days, with a positive control per known caller class on the new host; `code-refs.mjs` shows zero `run.app` hosts in code, config or deploy files. **Owner:** each repo's seat. **Window:** W1. **Depends:** D-15, D-16. | N |
| D-26 | 2026-09-18 | ADDED (A-8) | **EXTERNAL REGISTRATIONS AND INSTALLED CLIENTS.**<br>- The Stripe webhook endpoint (on `cortex-api` `/api/brokerage/v1/billing` today) moves to the hostname; the old endpoint stays until the census reads zero.<br>- The Samsara webhook is checked, and moved if registered.<br>- A `hauska-brief-extension` release carries the hostnames. 0.6.33 hardcodes the `cortex-api` and `hauska-mcp-server` `run.app` hosts; its `https://*/*` permission needs no manifest change.<br>- A `legacy-revit-sensor` release makes the base URL configurable and defaults it to the hostname.<br>- Paying MCP clients are moved to `mcp.hauska.dev`. **Gate:** `callers.mjs` shows each caller class on the hostname, and the old-version tail on `run.app` at zero, or at a residue the operator accepts in writing. **Owner:** the operator with the owning seats. **Window:** none. **Depends:** D-25. | N |
| D-27 | 2026-09-18 | ADDED (A-8) | **NO SYNCHRONOUS REQUEST OVER 90 SECONDS (rule 15).**<br>- The `hauska-engine-api` flood-drainage, feasibility-export, site-plan-export and dossier-export refreshes return 202 and are polled (up to 191 seconds this week).<br>- `cortex-api` street-search and gis-layer are bounded under 90 seconds with a reasoned refusal (504s and 502s today).<br>- `hauska-retrieval-api` `/stats` and `smartcity-api` `/api/ai/proactive-insight` are bounded.<br>- Callers (Property Explorer, `hauska-mcp-server`) poll. **Gate:** `long-requests.mjs OVER=90` shows zero on these services since deploy T, while the same query over the 7 days before T still shows the known routes (the positive control). **Owner:** property seat, and the SmartCity lane for `smartcity-api`; through the Texas planner (D-15). **Window:** W1. **Depends:** D-15. | N |
| D-28 | 2026-09-18 | ADDED (A-8) | **smart-files TO DIGITALOCEAN.** Hostname-first, then a DNS cutover.<br>Callers: the `hauska-mcp-server` template, the `plan-review` env, dolphin-app, the `smart-markets-api` secret, and the `smart-files-app` Vercel project.<br>Size: shared 1 vCPU / 1 GiB.<br>Phase 4 procedure (plan §7): build from the committed spec, payload parity (rule 11), TTL lowered a day ahead (rule 4), the operator flips the record, a bake against D-18, and the GCP original untouched until the census reads zero. **Owner:** govtech seat. **Window:** none. **Depends:** D-21, D-22, G-155 closed. | N |
| D-29 | 2026-09-18 | ADDED (A-8) | **plan-review TO DIGITALOCEAN.** Same procedure as D-28.<br>Callers: `cortex-api` (the LDT workflow env), `hauska-mcp-server`, and the `plan-review-app` and `icc-portal-app` Vercel projects.<br>Its own default `HAUSKA_MCP_URL` in `src/mcp.mjs` becomes config.<br>Traffic is pinned by revision name on GCP (rule 9). **Owner:** govtech seat. **Window:** none. **Depends:** D-28, and G-150's close-out. | N |
| D-30 | 2026-09-18 | ADDED (A-8) | **smartsite-mcp TO DIGITALOCEAN.** A DNS cutover at `mcp.smartsite.cloud`, which already exists and points at GCP's `ghs.googlehosted.com`. The WorkOS resource URL does not change.<br>Size: shared 1 vCPU / 1 GiB, 2 instances.<br>Deployed today by LDT's `cloud-run-deploy-smartsite-mcp.yml`, which is replaced by the D-21 path. **Gate:** the Phase 4 procedure, plus the `Claude-User` success rate no worse than the D-18 baseline (1,283 requests a week). **Owner:** property seat (the LDT MCP worktree). **Window:** W2. **Depends:** D-6. | N |
| D-31 | 2026-09-18 | ADDED (A-8) | **JOB PLANE CUTOVER TO DOKS, IN BATCHES.**<br>The 43 factory, engine and LDT jobs move in batches: idempotent and read-only jobs first, then writers, then the publish and gate jobs last.<br>Each batch switch runs in a W3 window: no factory run in flight, the gate trigger paused, no heavy-scan lease held.<br>A GCP job definition stays, disabled, until its batch bakes.<br>Acquisition jobs' third-party hosts are tested from the DOKS egress first (rule 19, D-17 P9).<br>The factory's 16 Cloud Build files are replaced by manifests and an image build to the chosen registry. **Gate:** each migrated job's first DOKS production run matches its last GCP run on a comparison sample, and its run record carries the Job identity; `jobs-cost.mjs` reads zero for migrated jobs. **Owner:** property seat, through the Texas planner. **Window:** W3. **Depends:** D-23, D-6. | N |
| D-32 | 2026-09-18 | ADDED (A-8) | **TRADING BOT AND cockpit-pg.**<br>- `empressa-bot` moves to a droplet with a Reserved IP, registered with the broker before cutover. It runs at 5 percent of 4 vCPU today.<br>- `cockpit-pg` (Postgres 16, 19 to 21 GB, about 30 connections) moves to DigitalOcean Managed Postgres in the bot's datacenter with point-in-time recovery, or to Neon; markets decides. `cloud-sql-proxy` is removed.<br>- `api.empressa.pro` is cut over by DNS.<br>- A cloud firewall allows SSH from the operator only, and no RDP.<br>- The cutover runs outside market hours. **Gate:** the cockpit health endpoint answers from DigitalOcean; database row counts match at cutover; the broker accepts the new IP. **Owner:** markets seat. **Window:** markets' own. **Depends:** D-16, D-22. | N |
| D-33 | 2026-09-18 | ADDED (A-8) | **smart-markets-api, RETIRE OR MOVE.** It had zero requests in the last 7 days and 48 in 30 (evidence §3). If markets names a live user, it moves to App Platform as shared 1 vCPU / 0.5 GiB behind a hostname, and the `smart-markets-app` `vercel.json` rewrite is repointed. If not, the retirement is recorded, the rewrite removed, and the project deleted. **Owner:** markets seat. **Window:** none. **Depends:** D-28 (it calls smart-files). | N |
| D-34 | 2026-09-18 | ADDED (A-8) | **GCS TO SPACES, THE FINAL CUT (ruling 2).**<br>- An S3-compatible storage adapter, with endpoint and credentials in config, in `hauska-engine`, `hauska-mcp-server`, `legacy-design-tools` (including `objectAcl.ts` semantics), `hauska-factory` and `empressa-trading` (F-11).<br>- About 13 GiB copied with per-object checksums.<br>- The `gs://` URIs stored in databases (the `permitRecord` path among them) rewritten as a recorded migration that refuses if the rewritten count differs from the copied count.<br>- `hauska-map-tiles` (public, 82,851 requests in 30 days) moved behind a tiles hostname. The operator picks the certificate approach: DigitalOcean DNS delegation of one subdomain, an uploaded certificate, or an App Platform proxy.<br>- A dual-read period with a logged GCS fallback that must reach zero.<br>- Then GCS read-only, then deleted. **Gate, per bucket:** checksums match; the fallback log reads zero; GCS `request_count` is zero for 14 days. **Owner:** each repo's seat. **Window:** W4. **Depends:** every Phase 4 row, D-31, D-32. | N |
| D-35 | 2026-09-18 | ADDED (A-8) | **RETIRE EVERY GCP PATH AND SHUT DOWN THE PROJECTS.** In order: `smart-files`, `plan-review`, `smart-markets`, `empressa-trading-prod`, `legacy-design-tools-prod`, then `hauska-prod-497015`. The SmartCity projects close in D-20.<br>- Remove every GCP deploy path: the Workload Identity pool `github-actions`, Cloud Build files, GitHub workflows, and service-account keys.<br>- Export from Cloud Logging whatever evidence is still needed, then delete each project. The 30-day recovery window is the final rollback.<br>- Add a `code-refs.mjs` CI guard in every repo.<br>- The billing account stays open for `atx-bulls`. **Gate:** the CI guard fails on a planted reference (violation); `code-refs.mjs` reads zero across repo mains; the billing export reads zero for the eight projects for a full calendar month. **Owner:** integration seat with each seat. **Window:** none. **Depends:** everything above. | N |

## Amendments

- **A-9 (2026-09-18).** **D-14 RE-GRADED LANDED AND D-13 RE-GRADED OPEN, FROM THE DEPLOYED RECORD RATHER THAN FROM PROSE.** Read by the planner at 2026-09-18T16:26Z; evidence `_inbox/2026-09-18_planner_four_row_regrade.md`. Both rows read `ADDED` here while the roadmap and OPS-17 A-150 described them as landed, which is this document's own failure mode: a status cell lagging its subject. **D-14 is LANDED, not closed.** The repoint is real and read back at source (`source_commit_hash` `1f0262f` = `smartcity-os` `origin/main` HEAD, deployment `e243db0a`, `cause: manual`), so the thing the row exists to fix is fixed in production. Two clauses of its own instrument are NOT graded: the payload parity of `smartcityos.io` and `www` before and after the repoint, and the health probe. No lane close, CP1 or CP2 is filed. **D-13 stays OPEN, and A-150's "D-13 LANDED" is corrected here.** What landed is the CODE: `7487d7c0` carries `src/platform-base.mjs` and drops the GCP host everywhere in `src/` and `web/`, proven on `d12-main-uat`. What did NOT run is scope step 2, setting that variable on `dolphin-app`, which carries none and serves `3d3ec62` where the feeds are still the hardcoded GCP URLs. The row's defect is therefore still live for customers, and code-done is not customer-done. Steps 2 and 4 are the A-149 ship, so the ship closes them; scope 4's feed parity and scope 5's zero-request bake on GCP `smartcity-api` stay ungraded either way. **G-161 keeps its LANDED wording and is not re-graded here** (OPS-17 A-150 already records it, with its close owed). **The instrument that surfaced all of this.** `scripts/govtech/smartcity-tracker.mjs` refused a verdict twice today for two separate defects, and both are fixed: it split table rows on EVERY pipe, so the G-161 row, whose status cell carries an escaped `\|`, parsed to nine cells and vanished from the run (a dropped row is now recorded and the run REFUSES rather than passing over it); and its status vocabulary could not express `LANDED`, so a landed row read UNREADABLE (now its own class, deliberately NOT counted as done, because landed means the change is present at its target and not that the row's instrument is graded). Both refusals were proven able to fire by violating them before this row was written.

- **A-8 (2026-09-18).** **OPERATOR RULING: FULL EXIT FROM GCP. D-15 TO D-35 CARDED.** The operator's five rulings are recorded in `_decisions/2026-09-18_gcp_full_exit_rulings.md`:
  1. A full exit of every GCP project except `atx-bulls`, waiting for a clean pause in the Texas scale-up and coordinated with that program's planner first.
  2. GCS stays in place through the compute moves, and everything is migrated off it by the final cut.
  3. Stable hostnames go in front of services before their cutover.
  4. `atx-bulls` is out of scope.
  5. The DigitalOcean GitHub connection is widened.

  Before carding, the integration seat measured the estate live, 2026-09-18 10:45Z to 11:59Z, and committed the instruments as `scripts/infra/gcp-exit/`. Every self-test in them was run against a known violation and failed as designed. The evidence is `_inbox/2026-09-18_gcp_estate_EVIDENCE.md`; the plan is `_inbox/2026-09-18_gcp_full_exit_PLAN.md`.

  **Six findings change this document:**
  - **(F-1) The largest cost is a defect, not the platform.** About 96 percent of derived Cloud Run service spend is 0%-traffic revisions that keep warm instances because they are tagged: about $2,490 of about $2,580 in 30 days. A cleaned-up GCP costs about the same as the DigitalOcean target. D-19 fixes the leak on GCP now, and the plan says so plainly rather than crediting the exit with that saving.
  - **(F-5, F-6) Rule 2's leaf-to-root order cannot hold.** The call graph has loops, and `hauska-retrieval-api` alone has at least seven caller paths against D-3's one. Rule 2 is amended to hostname-first; D-3 must be recompiled.
  - **(F-8) D-13 names two of the five `smartcity-dashboards` files** that hardcode the GCP host. Its scope is widened.
  - **(F-9) Six routes bound for App Platform ran past 100 seconds this week**, against a reported 100-second edge. That gives rule 15 and D-27.
  - **(F-10, F-18) App Platform cannot launch a job on demand**, and the factory is built on the Cloud Run Jobs API. The job plane goes to DOKS (D-23, D-31).
  - **(F-13) `cortex-api`'s monitoring lives in `smartcity-os-prod`**, and the trading uptime check probes a mangled path. That gives D-22 and D-20's ordering.

  **Rules 15 to 20 are added.** Rule 8 is widened by ruling. The operator's rulings supersede the seat's own recommendation to keep the factory on GCP this arc.
- **A-7 (2026-09-18).** **D-14 ADDED: `walrus-app` is pinned to a side branch, exactly as `dolphin-app` was before D-12.** Found while scoping OPS-17 G-159: its v1 finance routes would merge to `main` and never deploy, because `walrus-app` builds from `d9-api-8bea7fa` (`e783f351`), which diverges from `main` in both directions. Live finance data on the dashboards therefore needs D-14 (v1 builds from `main`) AND D-13 (the dashboards call `walrus-app`); G-159 is scoped to what it can finish without them. The pattern to watch for is now twice-proven: every DigitalOcean app stood up by D-5 and D-9 was pinned to a hand-made branch that `main` does not contain.

- **A-6 (2026-09-18).** **D-13 ADDED: the "idle" GCP `smartcity-api` is the dashboards' live data source.** Found while carding OPS-17 G-159, whose new finance routes deploy to `walrus-app`: the dashboards call GCP for every platform feed, so those routes would have been unreachable, and deploying them to GCP instead would modify the untouched rollback copy (rule 3). Measured by code and by logs, with a positive control on the log query. Open item 1 amended so decommissioning GCP `smartcity-api` waits on D-13. The general lesson is governing rule 2's own: migration order follows the VERIFIED call graph, and a server-to-server caller that only fires when a person opens a lens is invisible to a traffic window in which nobody did.

- **A-5 (2026-09-17).** **OPERATOR RULING: ALL SMARTCITY SURFACES MOVE TO DIGITALOCEAN BEFORE ANY DESIGN BUILD WORK. D-12 ADDED AND IS THE CRITICAL PATH.** Operator, in session: *"I want the dashboards to go to DO now before we do any work."* This reorders the program. The dashboards cutover was a named but unowned D-9 leave-behind and every OPS-17 design build row now waits behind it. Three facts were measured at the planner seat before carding rather than inherited from the two 2026-09-17 handoffs, and two of them are absent from both. **First**, the GCP dashboards original is not in `smartcity-os-prod`; it is in its own project `smartcity-dashboards`, us-east1, with traffic pinned by revision name to `00074-sil` and deployed by the operator's hand, so it carries the same traffic-pin trap D-11 found on `hauska-mcp-server`. `plan-review` was checked at the same time and has the identical shape (`00025-ley`, `latestRevision=false`, zero triggers), so the pin is the house pattern across this product family, not a one-off. **Second**, `dolphin-app` builds from `d9-pin-96efa35`, diverged from main in both directions, which means a cutover alone would leave every subsequent merge invisible to the running app. That is the failure mode where a merged PR, a green CI and a live app all coexist with the change absent, and it is why the divergence reconciliation is inside D-12's scope rather than carded behind it. **Third**, the "a merge ships nothing" claim both handoffs carry was verified rather than inherited, by two independent derivations: `smartcity-dashboards` has exactly one GitHub workflow (`ci.yml`, jobs `test` and `a11y`) with zero deploy references anywhere in the file, and `gcloud builds triggers list` returns zero triggers in `smartcity-os-prod`, `hauska-prod-497015` and `legacy-design-tools-prod`. The claim holds. Also corrected on this pass: OPS-25's open item 1 (bake then decommission) is unchanged and still unowned, and D-12 explicitly does NOT decommission anything, since the GCP original is the rollback path for its own bake.

- **A-1 (2026-09-17).** Operator held D-3 pending `p275-live-currency-five-counties` freeing fully, given active concern about cutting over `hauska-engine` while the Texas scale-up work is mid-flight there. Operator then asked whether SmartCity could start fresh on DO instead of waiting. Verified: `smartcity-os` and `smartcity-dashboards` repos idle since 2026-09-15, no overlap with either active lane claim, own dedicated Neon projects (not the shared Factory/Cortex/atoms stores). D-5 rewritten and promoted to front of queue as a fresh-build (not lift-and-shift) migration of the whole SmartCity product line, proceeding independently of D-3's hold. Governing rule 2's call-graph ordering still applies within the Hauska substrate once D-3 resumes; it does not gate D-5.
- **A-2 (2026-09-17).** D-5's close committed to `seat/dispatch-planner` (`14284eb7`), verified independently: three of seven artifact hashes spot-checked and matched the committed blob exactly, confirming the lane's own CRLF-vs-LF hash correction was real, not a cover story. `_catalog/lane_claims.json`'s removal of the stale `p249-envelope-unlock` entry confirmed benign (p249 has a landed close file, `_inbox/2026-09-16_p249-envelope-unlock_close.json`) and cleared to commit. One interpretive correction on the record: the close's claim that `scripts/surface-probe.mjs` is "non-deterministic across runs" (two artifacts 92 seconds apart, one differing verdict) is not itself verified — the more likely explanation is that the live `smartsite.cloud` surface it measures genuinely changed state in that window, given how much concurrent deploy activity the Texas sprint was running at the time. Does not affect D-5 either way, since that instrument doesn't cover D-5's rows regardless. Operator confirmed the boot-time password-reset side effect on production is a non-issue (Bastrop already notified of cutover-related interruptions). Governing rules 7 and 8 added from this close's findings. D-9, D-10, D-11 carded as the continuation.
- **A-4 (2026-09-17).** D-9's close initially misread AGENT_CONTRACT.md §6's "Doc_repo commits are planner-owned: leave repo edits uncommitted and list them in the close" as covering the lane's OWN close/CP1/CP2/probe artifacts, and left them uncommitted "as the contract requires" — the opposite of what every neighboring lane on this seat branch does (D-5, D-10, D-11 all commit their close artifacts) and the opposite of what the same section's own "checkpoint artifact paths" requirement presupposes (a path nobody can ever read is not a checkpoint). The practical cost was real: a completed production DNS cutover sat with zero durable record. Self-corrected by checking precedent rather than trusting its own reading; committed as `f3c3c4a1` and pushed. **Recommended, not yet made:** clarify AGENT_CONTRACT.md §6 to read "leave the repo's own tracked content — docs, scripts, catalogs — uncommitted; the lane's own close, CP1, CP2 and probe artifacts under `_inbox/` ARE committed by the lane, per the checkpoint-artifact-paths requirement above" or equivalent, so this ambiguity cannot recur. Operator's call whether to make that edit now. D-9 and D-11 (re-fired) both closed `status: closed` after this correction, verified against their committed blobs at `f3c3c4a1` and `40eaae82`. D-10 closed `closed-partial`, verified at `f8a687cb`, on its own stated (not omitted) remaining gaps. Full findings folded into the "Status" section above and governing rules 9-12.
- **A-3 (2026-09-17).** **D-11's scope was short of its own goal, and D-11 was fired concurrently with its own gate.** Both measured at the D-11 lane's start, not inferred. (1) *The scope defect.* The mission's "one environment variable, no code touched, a config-only redeploy" limit cannot produce a durable flip: `hauska-mcp-server`'s `cloudbuild-mcp.yaml` on `origin/main` (`f7a2f5e5`) carries `_DASHBOARDS_BACKEND_URL: "https://smartcity-dashboards-52ecsl5mvq-ue.a.run.app"` and sets `DASHBOARDS_BACKEND_URL` on every build via `--set-env-vars`, so a service-only flip is silently reverted by the next Cloud Build deploy. Operator ruling 2026-09-17: the one-line `_DASHBOARDS_BACKEND_URL` substitution edit is INSIDE D-11's scope, and nothing else in the repo is. Carded as governing rule 9, because it generalizes: D-3, D-4 and D-7 will each flip an env var on a service that has a committed deploy template. (2) *The gate collision.* D-11 was handed out while D-9 was still in flight: D-9's lane claim opened `2026-09-17T18:33:31Z` (seat `do-migration-lane`, which also held D-10 from `18:26:16Z`), and at `18:39:09Z` no `do-migration-app-platform-configure` CP1, CP2 or close existed in any checkout or seat worktree and the planner worktree stood at `14284eb7`, D-5's close. The D-11 lane stood down without claiming, without touching the service, and without executing; its gate (b) — `hauska-mcp-server` idle, no lane claim on it — passed clean at the same reading (origin/main `f7a2f5e5` @ 2026-09-16T06:57:29-05:00, serving revision `hauska-mcp-server-00102-naj` created 2026-09-16T16:54:31Z at 100% traffic). Re-fire D-11 only against a landed D-9 close. Standing caution for this program: an OPEN lane claim is not evidence that a dependency landed, and the dispatch's own "do not proceed on the assumption that it has" wording is the control that caught this.
