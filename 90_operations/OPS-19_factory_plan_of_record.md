---
id: OPS-19_factory_plan_of_record
title: OPS-19 — The Factory: plan of record
date: 2026-08-26
last_updated: 2026-08-30
status: active
owner: nick (amendments); doc_repo planner (grading)
purpose: The single benchmarkable plan for the Factory, the one machine that acquires, lands, canonicalises, publishes, verifies, and maintains smart sites per state under the instrument contract and the blueprint, hosted on Cloud Run with its own store and console. Baseline is frozen; changes are append-only amendment rows. Work that cannot name an F-row here is not scoped.
related_canonical: [_decisions/2026-08-26_factory_program_and_hold_lifts, _decisions/2026-08-26_factory_model_law_and_option_a, _inbox/2026-08-26_factory_program_design, 19_the_instrument_contract, _blueprint/10_model, _blueprint/20_pipeline, _blueprint/40_rule_register, 51_ingestion_pipeline_reference, 24_instrument_conformance_program, 90_operations/OPS-16_texas_market_plan_of_record]
---

# OPS-19 — The Factory: plan of record

## Why this document exists

Three factories ran as hand-carried lanes with a human as the only joint, evidence in `P:/tmp`, holds in a file, and no owner for publish. The blueprint's rule register lists twenty-two `BP-` rules and twenty of them have no executor. This plan is the structural answer: the Factory is the executor column of that register, one row per layer, stage, or capability, each naming the rules it arms, the instrument that grades it, and what blocks it, plus an append-only amendment log. Texas cleanup rows stay `P-` in OPS-16 and are referenced here; no row lives in two plans.

**Definition of done.** Texas complete on three finish lines graded separately by rule id: county manifest 254 x rails every node `populated` or `not-applicable` or `absent-verified` with scope; city manifest 1,223 x rails the same; a verify walk of known parcels on production Smart Site passes. Then one Utah county from discovery to visible on production through every layer and stage. A row graded by narration or by percentage fails.

## Governing rules

1. **The baseline table never gets edited.** Every scope change is a dated row in AMENDMENTS.
2. **No dispatch without a row.** Every dispatch names `PLAN-ROW: F-xx`. Enforced by `scripts/dispatch.mjs` and the canon-gate hook through `_catalog/plan_registry.json`.
3. **Statuses re-grade only by instrument** (Factory ledger, SQL, `gh`, live probe on the target site, `50_grading` rule-id output). The GRADE LOG records each pass.
4. **The model law wins.** `19`, `_blueprint/10_model`, `_blueprint/20_pipeline`, `_blueprint/40_rule_register`, `51`, `24`, the adopted layering target state, and the published package as tiebreaker. Where a row or a design disagrees with them, the row is the defect.
5. **One machine.** Nothing reaches a serving store except through L4 publish; only reconciled atoms enter the served graph, only through views. Every stage is a run with a record and a termination record; refusals are runs.
6. **Staging first, identical job second.** A production publish with no passed staging sibling refuses.
7. **Scoped leases.** One bulk writer per `(store, entity_type, county_fips)`; one heavy-scan scope per database. Custody is a table. This supersedes the OPS-16 write-slot law for Factory runs once F-02 lands, and AGENT_CONTRACT section 3 is amended at that time.
8. **Option A on the existing path.** P-82-lite lands as a bug fix with BP-WRITE-01; Bexar 48029 finishes on the current shape; no new county is written on the old shape. New fills wait for the conformant stage E writer.
9. **Agents propose, the operator approves, the machine records.** State recipes, adapter specs, setback tables, registry freezes, cert grades, metro re-acquires, and tier 3 adjudications are approvals in the console with evidence attached.
10. **Fewer agents, tighter contracts, harder gates.** Every lane cites the row and the stage; verification is never delegated below the lane planner; subagents do not commit.
11. **No regression of completed work.** Existing rows are adopted as level-one instruments; upgrades write provenance and verdicts; P-69 declares the unrecoverable. A row that would delete or overwrite adopted data without a verify walk is a defect.

## Lineage

Predecessors: OPS-14, OPS-16 (P-rows), `27a_jurisdiction_factory_engine_spec.md` (ledger is the product; G1 to G8), the three factory runbooks, `_inbox/2026-08-25_factory_operating_instructions.md`, the 2026-08-26 loader program (P-81 through P-84) with its adversarial review.

**Baseline v0 (2026-08-26 09:33, unratified, superseded the same day before any dispatch compiled against it).** Fourteen rows F-00 to F-14 named stations after the three factories (acquire, fabric, depth, publish, verify) and drained borrowed keys into per-parcel rows. The operator's question about the shape of nodes, atoms and edges surfaced that the design used the store's current shape rather than the model law. v1 below re-baselines on the four layers and five stages, adds the contract-type dependency (F-15), the shared resolution stage (F-16), reconcile and promote (F-17), intensional demotion (F-18), replay and idempotency (F-19), and records option A on the existing path. Row ids F-00 to F-14 keep their numbers where the subject survived; their scope is restated.

## PLAN OF RECORD v1 — baseline frozen 2026-08-26 (operator go, later session)

Status legend: **NOT STARTED**, **CARDED** (WDLL exists), **FLIGHT**, **LIVE** (graded by instrument), **HELD**.

| ID | Layer / stage | Work item | Arms | Pass/fail instrument | Blocked on | Status at baseline |
|----|---------------|-----------|------|---------------------|------------|--------------------|
| F-00 | control | Repository `hauska-factory`, seat registration, CI, image build from named engine, LDT and contract SHAs, Cloud Run service and job skeleton in `us-east4`, first execution records `engine_sha`, `image_digest`, `rtt_ms` | BP-FACTORY-01 (termination record on every job) | `gh repo view`; seat register row; run record read by field | operator go to create the repo | NOT STARTED |
| F-01 | store | Factory Neon project; schema for L2 landing, L3 candidates and resolution atoms, control, ledgers, quality, states; direct-host-only connection layer | BP-LAND-01 | pooler URL refused; migration table | F-00 | NOT STARTED |
| F-02 | L3 stage E, existing path | Option A: P-82-lite on `writePropertyAtomsBatch` (multi-row links, per-leg timing, lease v2 with the row locked in the chunk transaction, run ledger) **plus BP-WRITE-01** (refuse a non-canonical node binding and a sentinel in a key at the store boundary); Bexar 48029 cad resume as the proof; benchmark on the real table from `us-east4`. **No new county on this path after Bexar.** References OPS-16 P-81, P-82, P-83, P-84 | BP-WRITE-01, BP-KEY-SENTINEL-01, BP-EDGE-01 (links written), BP-DID-01 | drain card items; a bare key and a sentinel each refuse with zero INSERT | F-00, F-01, F-03 | CARDED (`_inbox/2026-08-26_cloud_loader_WDLL.md`, revised) |
| F-03 | control | Run ledger, termination records (max duration, success exit, failure exit, lease release), leases, holds (routing pin imported then retired by refuse), work list derivation, control API (start, stop, hold, approve, re-run, adjudicate, lane request) | BP-FACTORY-01 | a run cannot start without a row; a job without a termination record fails; hold refuse | F-01 | NOT STARTED |
| F-04 | console | Smart Site Factory skeleton in `hauska-map/apps/factory`, own Vercel project and base URL; screens States, County manifest, City manifest, Runs, Queues, Defects, Holds, Gates, Lanes, Walk, Cost; reads the control API only | none directly; makes BP-PROMOTE-01 and BP-LEDGER-01 visible | every number equals a `SELECT` at the same instant; Bexar reads partial without input | F-03 | NOT STARTED |
| F-05 | verify / ledgers | County manifest reading layer verdicts per node with a bounded per-cell materialise; city manifest from the roster with the same vocabulary; `quarantined` as a state; `hasWriter` and `atomFamilyState` derived from the store and varying; verified-absence pair required for `absent-verified`; one-way published copy to `neondb` | BP-LEDGER-01, BP-ABSENCE-01, BP-LANDUSE-01 | cell moves within 30 min of a verified run; indicators vary across cells; typed absence without the pair refuses | F-01 | NOT STARTED |
| F-06 | L4 serve | Publish: facet bakes tier 1 and 2, statewide PMTiles bake as the fabric join proof (tile id equals node id equals alias; unknown id fails the bake), `tiles.json` per environment, cortex-api canary discipline, PE per target, freshness stamp per county per target; access never defaulted; punctuation-only situs refused at serve | BP-SERVE-01, BP-SERVE-02, BP-ACCESS-01, BP-ADDRESS-01 | a county goes reconciled to visible with a recorded revision; a publish outside the job is detected as unrecorded; `", ,"` refused | F-02, F-05 | NOT STARTED |
| F-07 | L4 serve | Staging Smart Site under the Factory base URL: Neon branches reset from production per staging publish, canary-tagged cortex revision on staging secrets, staging PE at `/site`, staging tile path | rule 6 | identical walk results on both targets; production publish without a passed staging sibling refuses | F-06 | NOT STARTED |
| F-08 | verify | Verify walk of N known parcels per county on the target's real endpoints after every publish; freshness check; block promotion and quarantine on fail; every artifact graded by rule id per `50_grading` | BP-MEANING-01, BP-VERIFY-01 | walk fails on a deliberately broken parcel and passes on gold `48021:34137`; a presence-only grade reads UNMEASURED | F-06 | NOT STARTED |
| F-09 | L1 / L2 / stage A | Source manifests (origin, cadence, authority, license, retention class) required before any fetch; fetchers emit bytes plus checksum into immutable landing; `lib/cad-ingest` parsers become stage A adapters emitting six-field candidates with no binding; projection guard and FIPS filters as adapter parameters from the state recipe; vintage watchers raise drift events | BP-LAND-01, BP-MANIFEST-01, BP-ADAPT-01 | a fetch with no manifest refuses; a landed row cannot be mutated; an adapter that writes `atoms` directly fails CI; a non-Texas coordinate accepted under a non-Texas recipe and refused under Texas | F-01, F-03 | NOT STARTED |
| F-10 | texas | Texas cleanup on the new path: T1.1 to T1.7 from `24` (mint nodes and alias atoms, stop the leak, classify by type, demote enumerated families, verdicts before data, lineage for splits and merges, declare the unrecoverable) plus V1 to V15 from `_blueprint/40_rule_register.md` and the named defect list (Travis join P-80, Hays landuse hold, Ector keyKind, situs sentinels, tier2 flood, zoning ledger mis-score, Dallas and Tarrant vintages P-25, StratMap leftover fields, REST harvest P-79, footprint P-09, roads COVER) as defects with dispositions | closes V1 to V15 | defect register empty of open Texas classes; three finish lines graded by rule id | F-15, F-16, F-17, F-18, F-19, F-06, F-08 | NOT STARTED |
| F-11 | stage A / D | Factory 2 recipes as adapters (zoning stamp, setback tables, envelope) with stage D approvals (registry freeze, setback ratification, cert grade) carrying evidence; warm preflight reading minted parcel nodes | BP-PROMOTE-01 (human branch) | a city beyond Bastrop and Elgin warmed through the console with approvals recorded | F-16, F-04, F-05 | NOT STARTED |
| F-12 | verify | Defect register and lane requests: machine-found, QA-found, operator-filed; dispositions re-run, re-score, re-acquire, quarantine, or lane; lane request compiled by the planner through the doc_repo compiler with evidence attached; inspect versus manifest divergence check (2026-08-22 ruling 5) | BP-RECON-01 (surface), BP-LEDGER-01 | a defect cannot close until its cell moved and the walk passed on production; divergence beyond the stated window fails | F-03, F-04, F-08 | NOT STARTED |
| F-13 | discover | Discover flow: fixed probe set (federal reuse, state-published groupBy plus four corners, county-only enumeration, `uniformProduct` measured); findings schema; recipe and adapter spec records; approval; first as a compiled lane, then as a job | none; feeds F-09 | Utah findings produce adapter specs built without a second probe pass | F-04, F-09 | NOT STARTED |
| F-14 | all | Utah: one county from discovery to visible on production through every layer and stage, then the state | all | run ledger shows every stage for that county; walk passes on production; cost recorded | F-13, F-06, F-07, F-08, F-16, F-17 | NOT STARTED |
| F-15 | contract (substrate seat) | Track 2 types the stages need, each landing before the stage that needs it: 2.1 branded `NodeId`, 2.2 provenance-class union, 2.3 `derivesFrom` required on Derivation, 2.4 absence verdicts, 2.8 supersession as an edge, 2.10 alias as atom and lineage edges, 2.11 selector algebra, 2.12 two-field access; conformance fixtures and the behavioural suite published per stage with fixture-set hash and commit; ADR-030 rule 3 (a type publishes with a named producer, which is the Factory) | the "type" rows of `19`'s enforcement table | a bare string node id does not compile at any stage call site; a Derivation without `derivesFrom` does not construct; a published conformance run exists per stage | substrate seat capacity; operator ratification of aliases and lineage as atoms and edges (Track 4) | NOT STARTED |
| F-16 | L3 stage B | Shared resolution service: mint canonical node ids; alias atoms with validity eras; tier 1 exact, tier 2 scored, tier 3 provisional plus adjudication queue with owner, depth and throughput on the console; explicit node type from the adapter's declared set; one resolution atom per decision; `SPLIT_FROM` and `MERGED_INTO` edges; `canonical(id, knowledgeAt)` | BP-KEY-01, BP-PARCEL-KEY-01, BP-RESOLVE-01, BP-PROMOTE-01 | a defaulted type refuses; a resolution without its atom refuses; a provisional population that only grows reads as a failing gate on the console | F-15 (2.1, 2.10), F-01 | NOT STARTED |
| F-17 | L3 stages C and D | Reconciliation and promotion: same claim from higher authority keeps both; contradiction emits a CONFLICT edge; newer version emits SUPERSEDED_BY and closes the prior window; nothing overwrites; only reconciled atoms enter views | BP-RECON-01 | two authoritative stores disagreeing on one parcel fact produce a conflict edge, never a silent pick; an in-place update of a claim refuses | F-15 (2.8), F-16 | NOT STARTED |
| F-18 | L3 stage E, intensional | Demote flood, special district, rail corridor and pipeline: one selector atom per zone, district or corridor with an honest citation; materialised Derivation rows per node carrying `derivesFrom`, ids a pure function of (selector, store version, method); existing enumerated rows re-pointed, not re-fetched (T1.4 as rewritten by the Track 5 measurement) | BP-EDGE-01, BP-KEY-SENTINEL-01 (V13), the subject-expression row of `19` | region-scale family row count no longer exceeds the parcel count; a materialised row without `derivesFrom` refuses; rebuild reproduces the same ids | F-15 (2.3, 2.11), F-16 | NOT STARTED |
| F-19 | L2 | Replay from landing and idempotency proven: the same landed bytes plus the same logic version produce the same atoms; a replay overwrites rather than skips; proven on one source before any statewide backfill (51 §remediation steps 2 and 3) | BP-LAND-01 | replay of one county from landing produces identical atom ids and hashes; the idempotency probe is observed failing on a deliberately changed logic version | F-01, F-09 | NOT STARTED |

## Order of work (from 51 §remediation; resequencing is not a scope change)

F-00 and F-01 first, together. F-02 next (step 1, the leak). F-09 and F-19 (steps 2 and 3). F-15 in parallel from the start, each type before the stage that needs it. F-16 then F-18 (step 4). F-03, F-04, F-05 alongside. F-17 (step 7). F-06, F-07, F-08 (step 8). F-10 runs inside the backfill from landing (step 6) on the new path. F-11, F-12 as the console matures. F-13 and F-14 after one Texas source has gone through every stage.

## AMENDMENTS (append-only; never edit the baseline)

| # | Date | Row(s) | Change | Why | Operator-ruled |
|---|------|--------|--------|-----|----------------|
| A-000 | 2026-08-26 | all | Baseline v1 frozen from `_decisions/2026-08-26_factory_program_and_hold_lifts.md`, `_decisions/2026-08-26_factory_model_law_and_option_a.md`, and `_inbox/2026-08-26_factory_program_design.md`. Baseline v0 of the same morning superseded per Lineage. CTX / national hold and the Bastrop QA gate on the data path are lifted. Routing pin holds import as F-03 hold rows; which Texas rail holds lift for the conformant writer is the first amendment the operator rules after F-16 and F-18 land. | Program opened; model law adopted; option A ruled | Y |
| A-001 | 2026-08-26 | F-01, F-02, F-03, F-04, F-05 | **Phase A close leave-behinds recorded and the drain card bound.** (1) F-01: three per-table landing jobs running and two completed with ledger rows unread; graded at the drain lane's first handback, since the same seat runs both. (2) F-05: `published_at` reaches the served ledger only through an LDT change to `countyLedger.ts` `router.get("/")`; option A adds the field read from `county_ledger_snapshot` and names what it stamps (the snapshot, not the live grid); option B serves the snapshot and is the F-05 completion. Property seat, LDT worktree, its own PR; not folded into P-85. (3) F-04: `VITE_FACTORY_CONTROL_API_KEY` is compiled into the public console bundle (a 32-character literal bound to the `Bearer` header, verified by reading the served bundle; value withheld) at a URL that answers 200 unauthenticated, and `factory-control` exposes `POST` verbs on that key. A credential in a public bundle is a control whose scope is broader than its claim: item 11's 401 proves the header is checked, not that the holder is the operator. Rotate the key now; before any further mutation verb ships, move the call behind a server-side proxy with an operator login and re-grade item 11 by violation from an unauthenticated browser. First F-04 item of the next console card. (4) F-03: the `gcloud run jobs execute --args` PowerShell quoting that produced knl7d goes in the runbook. (5) F-02: the drain card `_inbox/2026-08-26_cloud_loader_WDLL.md` is bound to the `hauska-engine-drain` worktree on `seat/property-drain` and carries a sanctioned pre-step, apply engine migration 010 and run the ICC access-policy backfill on `hauska_mcp` as a recorded run (OPS-16 A-033), before any Bexar resume. | Lane close `_inbox/2026-08-26_f-phase-a_close.json`; planner verification at source 2026-08-26T22:50Z to 22:57Z. | Y |
| A-002 | 2026-08-27 | F-02, F-03 | **F-02 writer job carved out to its own lane (operator: "give me the writer job and I will give it to a new agent").** Gate 3 of the option A drain is the F-02 stage runner: the engine atoms-writer (`packages/engine-core/scripts/write-cad-parcel-roll-county.mjs` on engine main b402c8b, already refusing `--apply` without `--run-id` and any county but 48029) must run as a recorded Factory job in us-east4, and no engine writer image or job exists there. Card `_inbox/2026-08-27_f02_writer_job_WDLL.md`: engine image by digest (Dockerfile and cloudbuild only, no package changes), `factory-atoms-writer` job created by a script in the Factory repo with secrets resolving to the direct `hauska_mcp` host, run row written by the Factory before the writer starts and never by the engine image, counts read from the store, lease observed during and released after, three refusals recorded as executions, one dry run, exactly one 1,000-row canary apply on 48029 with a first atoms/s reading, console row, close artifact and handback. Worktrees registered ahead of creation: `hauska-engine-writer` and `hauska-factory-writer`, both on `seat/property-writer`. The drain lane keeps the full Bexar resume, item 7, and everything under `packages/`; it does not build the runner. Dispatch compiled `_dispatches/2026-08-27_f02-writer-job_dispatch.md`. | Operator go 2026-08-27; planner reads of engine main, the Factory repo, and `hauska_mcp`. | Y |
| A-003 | 2026-08-27 | F-02, F-03 | **Two writers in one checkout; ruling: one job, `factory-atoms-cad`, finished by the drain lane; the second agent stood down.** Cause, owned by the planner: at 01:30Z the drain lane was told to build the writer job; at 01:40Z the same work was carded for a new agent (A-002) without retracting the first instruction, so two agents built the F-02 runner on the same `seat/property-writer` worktrees. Verified at source: factory PR #6 `MERGED` 1f94f8d 01:31:54Z (`bexar-cad` runner: run row first, then `gcloud run jobs execute --update-env-vars=RUN_ID=… --wait`); `factory-atoms-cad` Ready in us-east4 since 01:35:48Z on digest `sha256:afdef0bb…`, secrets bound to the direct `hauska_mcp` and source hosts, `FACTORY_DATABASE_URL` bound, `APPLY=1` and `COUNTY=48029` baked into the job environment; engine PR #364 `BLOCKED` (vitest loads the node:test file `atoms-writer-job.test.mjs`; `Dockerfile.atoms-writer` copies the whole tree and installs with `--frozen-lockfile=false`), so the running image was built from unmerged engine code. The second agent's uncommitted tree (`cloudrun-jobs.mjs` Admin API execute, `atoms-writer-run.mjs`, `atoms-writer-counts.mjs`, `scripts/`, engine `services/atoms-writer/`) matches the card but staged deletions of the first agent's files. Ruling: keep the existing job; the drain lane continues alone and, before any Bexar apply, (1) makes `bexar-cad` read counts from the store (`atoms_built`, `updated_at >= run start`) instead of the gcloud exit code, salvaging `atoms-writer-counts.mjs`; (2) stops mutating the job per run: `RUN_ID`, `APPLY`, `COUNTY`, `LIMIT` travel as per-execution args or container overrides through the Cloud Run Admin API (salvage `cloudrun-jobs.mjs`), and `APPLY=1` and `COUNTY` come out of the job environment so the three refusal executions and the dry run are real; (3) gets engine #364 green (test under `node --test` outside vitest's glob or as a vitest test; `--frozen-lockfile`), merges it, rebuilds the image from engine main, re-pins the digest on the job. Then the 1,000-row canary, then the full resume and item 7. The second agent commits nothing and closes; its untracked files stay in the worktree for salvage and its staged deletions are restored. No atom row has been written on this path. | Drain lane handback 2026-08-27; planner reads of factory main, engine PR #364, the job by field, and both worktrees. | Y |
| A-004 | 2026-08-27 | F-02, F-03, F-18 | **F-02 stage runner DONE; Bexar complete without a resume; old-shape writes end; F-18 opens as design, not as a rewrite.** The runner is `factory-atoms-cad` generation 2 (grade log, same date). Bexar reads 703,257 = roll, so the drain card's Bexar resume item closes as complete by the prior writer, verified by the dry run and two canaries, and no further `--apply` runs on any county on the old shape, 48029 included. Item 7 result: 149.0 and 67.4 atoms/s on two 999-row rewrite chunks against the 300 prediction and 150 floor; both fail; both are chunk-scale cold readings and are recorded as such, not tuned. `factory-atoms-writer` (the stood-down path, digest `4fea88da`) is retired by decline: the Cloud Run job is deleted on the operator's word and an execute must 404; the image stays unreferenced; the stood-down agent's untracked salvage in the writer worktrees is the drain lane's to commit or delete. The writer worktrees stay registered to the drain lane. Planner error recorded in A-003 stands; the second job did produce the better of the two readings, and that is noted without changing the ruling, because its Dockerfile and creation script exist in no repository. | Drain lane close `_inbox/2026-08-27_f02-writer-job_close.json` (supersedes the stood-down close at the same path); planner reads by field. | Y |
| A-005 | 2026-08-27 | F-20 ADDED, F-15, F-16, F-17, F-18, F-19, F-02 | **F-20 ADDED: L3 stage E conformant graph write, stage-and-merge.** Candidates and edges COPYed to a stage table in the Factory store, one MERGE per `(entity_type, county_fips)` scope into `hauska_mcp` under lease v2, atoms and edges in one transaction, ids pure functions of their inputs, boundary on every row, counts read back from the store; pre-registered prediction at least 300 atoms/s sustained on 100,000 or more rows, floor 150. **Correction, planner error:** A-004, A-042 in OPS-16, the F-02 close grade row and the drain card's close ruling name "F-18 stage-and-merge"; F-18 is the intensional demotion row and always was. Stage-and-merge is this row, F-20. Those rows stand as written (append-only) and read with this correction. **Conformant writer card cut for a fresh lane:** `_inbox/2026-08-27_f16_f18_conformant_writer_WDLL.md` (F-16 resolution, F-17 reconcile and promote, F-20 write, F-19 replay proven inside it, F-18 flood demotion first; one Texas source, Bastrop 48021 CAD roll, end to end on the new shape beside the old rows; no publish, no repoint), dispatch `_dispatches/2026-08-27_f16-f18-conformant_dispatch.md`; F-15 requested from the substrate seat at `_inbox/2026-08-27_f15_contract_types_request.md` in stage order, shims allowed only with a self-defeating check. Worktrees registered ahead of creation: `hauska-factory-conform` and `hauska-engine-conform` on `seat/property-conform`. **Lanes retired by the operator:** the drain lane (P-81 to P-84, F-02; finish at `_inbox/2026-08-27_p81-drain_close.json`) and the stood-down writer lane; their worktrees hold uncommitted salvage listed in the new card's item 1 and are not opened. `factory-atoms-writer` was already absent from us-east4 at 10:35Z when the planner went to delete it on the operator's word; describe and execute both return NOT_FOUND, retirement proven by decline; the actor who deleted it is not recorded. Only `factory-atoms-cad` remains. | Operator 2026-08-27: "yes to F-15 to F-18", "delete it", "retiring the existing drain and the stand-down agent". | Y |
| A-006 | 2026-08-27 | F-05, F-06, F-07, F-08, F-10 | **Two parallel cards cut for two fresh lane planners (operator: "two cards in parallel, two lanes").** (A) `_inbox/2026-08-27_f10_wave1_cad_roll_WDLL.md`, dispatch `_dispatches/2026-08-27_f10-wave1_dispatch.md`: the CAD roll for all 254 counties through `factory-conformant` one recorded run per county from a work-list snapshot, named defect classes as register rows with dispositions before the loop starts, per-county V1 to V15 grades, the Factory county manifest reading verdicts per node with derived indicators (F-05), the one-way copy with `published_at` read by the served ledger (F-05 item 20). **Start gated** on the conformant card's close with items 6 to 8 graded, a Cloud Scheduler trigger on the reaper verified by violation, and the conformant PR merged on main; building before the gate is allowed, executing is not. (B) `_inbox/2026-08-27_f06_f08_bastrop_publish_WDLL.md`, dispatch `_dispatches/2026-08-27_f06-f08-bastrop-publish_dispatch.md`: staging Smart Site under the Factory base URL at `/site` on Neon branches reset from production, cortex-api canary by digest on staging secrets, Bastrop facet bakes tier 1 and 2 from the new shape, a Bastrop PMTiles bake as the fabric join proof with `tiles.json` per environment, freshness stamps, a verify walk on gold `48021:34137` plus an area-swept set and a deliberately broken parcel, production publish refusing without a passed staging sibling and executed only on the operator's word. (B) starts now; it needs Bastrop's new-shape rows, which exist, not the card's close. Both lanes run in lane-planner mode under `AGENT_CONTRACT` section 1 and rule 10: sub-agents hand back artifacts and never commit, deploy, or execute; verification stays with the lane planner. Worktrees registered ahead of creation: `hauska-factory-f10` and `hauska-engine-f10` on `seat/property-f10`; `hauska-factory-publish`, `legacy-design-tools-publish`, `hauska-map-publish` on `seat/property-publish`. F-05 item 20 is delivered once, by whichever lane lands it first, and the other lane cites it. | Operator 2026-08-27; planner cards. | Y |
| A-007 | 2026-08-27 | F-10, F-20, F-06 | **F-10 wave 1 start gate gains a fourth condition: edges.** The conformant writer's merge writes atoms and no edges (`applies-to`, `subject-to`, `derivesFrom` all 0 on 48021 after the close), so V11 (BP-EDGE-01, links starved) is FAIL on the one county the shape was proven on, and a job-asserted PASS was rejected. Before wave 1 executes any county: the F-20 merge writes `applies-to` edges parcel node to fact for every cad atom and `derivesFrom` edges for every Derivation row in the same transaction as the atoms (the card's item 5 as written), re-run on 48021 by replay (idempotent, supersession not update), and V11 graded from the store: `applies-to` count equals cad atom count, `derivesFrom` count equals Derivation count. The F-10 lane owns this because the conformant lane is retired and the writer code is on main (factory #9). The Bastrop publish lane (F-06) may bake from `parcelNodeId` in the body meanwhile, which is how the old serving path attaches facts today, but its rule grades will show V11 until the edges land, and it says so rather than passing it. Carried leave-behinds from the close: F-15 shims at 1.22.0 (substrate); two unzoned flood rows named; tier-2 precision UNMEASURED until a hand-graded adjudication sample at F-10 CP2. | Conformant close 2026-08-27T15:30Z; planner re-grade against `_blueprint/40_rule_register.md` V11. | Y |
| A-008 | 2026-08-27 | F-05, F-06, F-07, F-10 | **Rulings on the two parallel lanes' first handbacks.** (1) F-05 item 20 is the publish lane's: it lives on `legacy-design-tools-publish` and ships in that lane's LDT PR; the F-10 lane stops writing in any `-publish` worktree and cites the merged PR when its item 6 grades. (2) Pushes and PRs are self-service under rule 10 and the contract: both lanes push their branches now and open PRs on green; a PR with a red test does not merge, so the publish lane names and fixes its two failing tests first. (3) Deploys stay recorded: the Factory image is rebuilt and the jobs re-pinned by digest through the repo's own build and job scripts; Factory migrations 0002 and 0003 are applied through the Factory `migrate` job as recorded runs, never from a laptop; the planner verifies each by field. (4) Neon: creating the staging branches of `neondb` and `hauska_mcp` needs a Neon API key that the publish lane's environment does not have and the planner's shell does not have either; the operator places one in Secret Manager as `NEON_API_KEY` (placeholder convention, length echoed, never printed) bound to the `staging-reset` job, or authenticates `neonctl` in that lane's shell; nothing else on the publish card is blocked on it, so the PRs, the staging cortex revision, and the staging PE project proceed meanwhile. (5) F-10 execute stays refused until condition four (A-007) is built, 48021 is re-run by replay, and V11 reads from the store; then `--limit=10` for CP2, then the loop. (6) A close artifact is filed only at close; a partial handback is a checkpoint and is named as one. | Two handbacks 2026-08-27; planner reads of branches, artifacts, and the scheduler. | Y |
| A-009 | 2026-08-27 | F-03, F-10, F-20 | **Rulings after condition four closed.** (1) The F-10 loop orchestrator is a Cloud Run job (`factory-f10-cad-loop`, image by digest, `FACTORY_DATABASE_URL` from Secret Manager, run row for the loop itself, executes the county jobs through the Admin API from inside the job), not a laptop process; the `--limit=10` CP2 loop already running may complete because it is bounded, and the laptop credential is removed from the lane's machine after it; the 254-county loop does not start from a laptop. (2) Before CP2 closes, one small PR: the V11 grader scopes by the run's landing scope (county through the run row and aliases), never by an `entity_id` prefix, and proves itself non-vacuous with a fixture where the store has zero edges and the grade must be FAIL; `copyStage` bumps `staged_at` on conflict so replay counts read the store honestly; a rate-floor result terminates as `rate-floor` with the merge outcome recorded, never as `failed` when the merge succeeded, and the reaper reads that kind. (3) Console logs are not canon: `_inbox/2026-08-27_f10_cp2_loop.log` is not committed; the loop's record is its run row and `run_events`, and any human-readable log goes to the Factory store or `_scratch`. (4) `NEON_API_KEY` remains the operator's single action; everything else on the publish card proceeds without it. (5) Item 8 production publish stays not-started until the staging walk passes and the operator says go. | Two lane handbacks 2026-08-27; planner reads by field. | Y |
| A-010 | 2026-08-27 | F-03, F-10, F-20 | **Full-loop go criteria (the 254-county loop does not start on a 40 percent defect rate).** CP2 executed five counties clean and produced four defects out of ten, two of them large-county crashes and one a non-identical replay; a loop that would repeat that 25 times is not a drain, it is a defect generator. The full loop starts only when all of the following hold, each proven by an instrument and filed as CP3: (1) `planCadWorkList` has an idempotency guard (a county with a successful `f10_county_runs` row or an in-flight parent run is skipped, proven by re-running the CP2 list and watching it skip the five passes); (2) `exitKindFromExecution` maps a signal termination to `killed`, and the two CP2 signal rows are re-classified by the reaper, proven by cancelling one execution; (3) 48055's non-identical replay is explained from the two runs' diff and fixed or filed as a named defect class with a disposition; (4) the 48029 and 48085 large-county crashes are explained from the termination and `run_events` (memory, timeout, a row the boundary refused, an adapter fault) and fixed, and Bexar runs clean end to end, because Bexar is the largest county the loop will meet; (5) the CP2 list re-runs at 10 of 10 pass or named-class skip, with the rate distribution filed; (6) the work-list snapshot for all 254 reports the class distribution up front: how many counties execute, how many are skipped by which register class, how many have no landing, so the honest count is known before the first execution, not after. The planner gives the go on CP3, not on a message. Item 8 of the publish card is unaffected by this row and still waits for the staging walk and the operator's word. | CP2 graded 2026-08-27; planner ruling. | Y |
| A-011 | 2026-08-27 | F-04, F-06, F-07, F-08 | **F-04 card cut, and the publish lane's rulings.** (1) `_inbox/2026-08-27_f04_console_login_proxy_WDLL.md`, dispatch `_dispatches/2026-08-27_f04-console-proxy_dispatch.md`, a small fresh lane: Vercel serverless proxy holding `FACTORY_CONTROL_API_KEY` server-side, an operator sign-in (Smart Site OIDC with a server-side allow-list, or Vercel protection scoped to the project) in front of every screen and route, mutation verbs carrying the operator identity into the run row and refused by `factory-control` without it, Phase A item 11 re-graded from a browser that never signed in. Until it lands the console is honest-401 and the operator reads runs through the planner's field reads. (2) Publish lane: the 48021 `conformant-v1` rows exist (77,799 with 77,799 `applies-to` links, planner read 21:50Z); the lane files the exact query that returned zero and adopts the grader's predicate (`jurisdiction_tenant` plus `body->>'shape'`) for every count. (3) No bake or walk writes run from a laptop against any store, staging branches included; the Bastrop publish job is an image built from `legacy-design-tools` carrying the conformant tier 1 and tier 2 bake CLIs, county-scoped, run in us-east4 on the staging secrets as a recorded run; `factory-verify-walk` is rebuilt and re-pinned with the FK fix. (4) Item 6 grades only on a walk whose facets carry `conformant-v1` provenance per layer; the old-shape walk is recorded as the production baseline, not as item 6. (5) Item 8 stays NOT GO until items 3, 4, 6 are met on staging and the operator says go. | Operator screenshot and two publish handbacks 2026-08-27; planner reads by field and by query. | Y |
| A-012 | 2026-08-27 | F-15 | **F-15 cut as a card for a fresh SUBSTRATE lane (operator: can we send this one to a fresh agent).** Card `_inbox/2026-08-27_f15_contract_types_WDLL.md`, dispatch `_dispatches/2026-08-27_f15-contract-types_dispatch.md`, repo `hauska-atom-contract`, worktree `P:/seat-worktrees/substrate/hauska-atom-contract-f15` on `seat/substrate-f15` registered ahead of creation, because the primary substrate contract worktree carries PR #22 (Lane G NodeId and accessPolicy emitter, 1.21.0) with five dirty files and is read, not written, by this lane. Eight additive minor releases in stage order (2.1 NodeId, 2.10 alias and lineage, 2.2 provenance union, 2.3 derivesFrom, 2.4 absence verdicts, 2.8 supersession edge, 2.11 selector union, 2.12 two-field access), each a published npm version with a conformance fixture and a test that fails on the forbidden violation, a CHANGELOG line and an ADR-028 amendment; consumers on `^1.x` (engine, LDT, MCP server) proven green per release; the property F-10 lane deletes one shim per release. The seat is the point: a fresh agent is fine, a property agent is not, because the contract is Hauska substrate (ADR-018). | Operator 2026-08-27; contract repo read (main a3984e1, npm 1.22.0 of 2026-08-12, PR #22 open). | Y |
| A-013 | 2026-08-27 | F-04 | **Operator ruling: the console runs without sign-in.** After F-04 closed with a session door and an allow-list, the operator ruled the door off entirely (reads and the eight control verbs) and will deal with the public exposure later. What stays, because it is not auth: the control key never returns to the client; the proxy holds it server-side and `factory-control` keeps its Bearer check, so the public surface is the proxy, not the key. What changes: every `/api/proxy/*` route answers without a session; the sign-in page is gone; a mutation from the console records `operator = console:anonymous` on the run row, which is an honest record that nobody in particular pressed the button, never a fabricated identity; `factory-control` keeps refusing a mutation with no operator header at all. The auth code stays in the tree behind `FACTORY_CONSOLE_AUTH` with `on` as the code default and `off` set on the Vercel project, so a future deploy that forgets the variable fails closed and re-enabling is a variable flip and a redeploy, not a rebuild. Exposure named: anyone with the URL can read runs, manifests, defects, and costs, and can start, stop, hold, lift, approve, adjudicate, and re-run Factory work; the planner recommended reads open and writes gated and the operator chose fully open. Reversal: the operator says the word; the OIDC values and redirect URI from the F-04 close are the path back. | Operator 2026-08-27: remove auth completely, I will deal with the public thing later. | Y |
| A-014 | 2026-08-27 | F-15, F-10, F-06 | **After F-15: the shim deletion is the next F-10 PR, and LDT owes a contract rename.** (1) The F-10 lane pins `@empressaio/atom-contract` `^1.30.0`, deletes every shim under `src/contract-shim/` (their self-defeating checks are red on CI from this release onward), and adopts the three points where the contract is stricter than the shim (`responseRef` on `absent-verified`, `closedAt` on `SUPERSEDED_BY`, Observation and Synthesis as provenance classes) before its next PR; the note at `_inbox/2026-08-27_f15_f10_shim_delete.md` is the work list. This precedes CP3, not replaces it. (2) The property seat merges engine #365 (pin `^1.30.0`, CI green). (3) `legacy-design-tools` moves from the vendored `@hauska/atom-contract` tarball to `@empressaio/atom-contract` `^1.30.0` in all five packages as its own small property card (the 2026-07-06 rename never reached LDT); until it lands the Factory publish bakes in LDT cannot type against Track 2 and the walk rule `BP-CONFORMANT-01` grades on shape strings, which is recorded as UNMEASURED against the contract rather than as a pass. (4) Substrate leave-behinds stay theirs: `publish.yml` `workflow_dispatch` with `set_latest`, PR #22 disposition. | F-15 close 2026-08-27; planner reads of npm, GitHub, and LDT manifests. | Y |
| A-015 | 2026-08-27 | F-15, F-06, F-08 | **LDT contract rename cut for a fresh property lane (the A-014 finding, operator: give me the rename handoff).** Card `_inbox/2026-08-27_ldt_contract_rename_WDLL.md`, dispatch `_dispatches/2026-08-27_ldt-contract-rename_dispatch.md`, repo `legacy-design-tools`, worktree `P:/seat-worktrees/property/legacy-design-tools-rename` on `seat/property-rename` registered ahead of creation and taken from `origin/main` only after the publish lane LDT PR merges, so the two lanes never fork the bake CLIs. Sized at the snapshot: six manifests (four `file:` pins to `vendor/hauska-atom-contract-1.6.0.tgz`, two `^1.1.0`), four tarballs in `vendor/`, 73 import sites, zero imports of the current name. Method is the MCP server precedent: inventory first, dual pin with no behaviour change, then one PR per package with the serving process last, call sites adapting or refusing where 1.30.0 is stricter and never a shim or a cast, the bakes and `serveGuards` typed against Track 2 so `BP-CONFORMANT-01` grades on types, then the old name retired by a CI check proven by violation and the tarballs deleted, then a cortex-api canary on the operator go. Until it closes, the walk rule stays UNMEASURED against the contract as A-014 records. | Operator 2026-08-27; planner reads of LDT main b53a0571 and the MCP manifest. | Y |
| A-016 | 2026-08-28 | F-10, F-19, F-20 | **Two rulings from CP3: chunk the county, and explain the replay.** (1) The conformant pipeline processes a county in bounded chunks (an ordered slice of landing rows, on the order of 50,000, each with a `run_event`, the lease held across the whole county, stage B to E per chunk with idempotent stage and merge), so memory is bounded by the chunk and not by the county; 16Gi did not save Bexar and Harris is twice Bexar. Criterion 4 grades on Bexar clean end to end through chunks, and the chunk size and the peak memory per chunk are filed. (2) Criterion 3 grades only on an explanation: for 48055, the field-level diff of one atom that hashes differently between first run and replay, the cause (ordering, a timestamp in the body, tier 2 resolving against a warm store, a hash over an unordered set), and the fix or the named class with a disposition that is not re-run; F-19 says the same landed bytes and logic version produce the same atoms, and a class that admits divergence is a class the loop refuses. (3) The 254-county dry-run snapshot (4j6pr) is filed with its distribution before the next execute, as criterion 6 asked. (4) The loop stays NOT GO until CP3 is complete on all six; CP2 list re-run (criterion 5) comes after chunking lands, not before, so it is run once. | CP3 on Cloud Run 2026-08-28; planner reads by field. | Y |
| A-017 | 2026-08-28 | F-10, F-05, F-09 | **Wave 1 scope from the 4j6pr distribution.** Wave 1 is the **31 counties with landing** (25 execute + 6 idempotent). The **217** `TX-LANDING-ABSENT` counties are **not loop defects**; they are absent sources and read **lookup-failed with scope** on the manifest. The **F-09 acquisition card** is compiled by the planner when CP3 closes, not before. Full 254 loop stays NOT GO until CP3 completes on all six criteria. | Planner reply 2026-08-28 on 4j6pr distribution. | Y |
| A-018 | 2026-08-28 | F-03, F-06, F-08, F-10 | **Rulings after Bexar ran clean and the publish bake half-ran.** (1) Branches merge before grades: `seat/property-f10` (A-016) and `seat/property-publish` (A-017, rebased first, it is ten behind) open PRs and merge on the conclusion string; a job whose image was built from an unmerged branch is not a graded runner, it is a probe. (2) The reaper covers every Factory job, not only `factory-conformant`: the scheduled reap lists executions of all `factory-*` jobs and reconciles every `runs.status = started`, so a publish run that dies mid-bake terminates as `killed` or `crashed` without a hand fix; publish run 3eb8d70d is its first proof. (3) The tier 1 and tier 2 bakes chunk by `prop_id` pages the way A-016 chunks the writer, with a `run_event` per page, so a full-county bake neither holds the county in memory nor races the 3600 s timeout; 41,911 rows in five minutes says the whole county fits in about ten with chunking and does not without a termination. (4) The tier 2 diagnosis continues in the publish lane, no new dispatch: read 5r86n's log for the tier 2 failure, fix, re-execute `--skip-walk`, then `factory-verify-walk`, and grade item 6 only on rows carrying the publish run id. (5) The publish lane corrects its CP2: the four fixture rows were written by its own scripts from its own machine, a laptop write to the staging store; named as such, superseded by the succeeded run's rows before item 6 grades. (6) Criterion 3 is accepted on the record with the root cause and the deterministic mint; it grades MET when 2nd9z's replay fingerprints are filed identical across all chunks. Full loop NOT GO until CP3 closes on all six; then the 31-county wave 1. | Two handbacks 2026-08-28; planner reads by field. | Y |
| A-019 | 2026-08-28 | F-00, F-03, F-10 | **Job templates come from the build config, never from a hand `gcloud` in PowerShell; and the wave 1 go criterion is stated once.** (1) Every `factory-*` job's command, args, image digest, resources, secrets, and timeout are declared in the repository's Cloud Build config (`cloudbuild.conformant.yaml` gains the `factory-f10-cad-loop` deploy the lane left behind) and applied by the build, so a rebuild re-pins every job that tracks the image and no job template is ever typed into a shell; the loop job's collapsed `command` tonight, and Phase A's knl7d and the publish lane's `--prop-ids` before it, are the same defect three times and the runbook line did not stop it, so the control is the build, not the runbook. A hand `gcloud run jobs update` on a Factory job is a defect to report, not a shortcut. (2) Criterion 5 is `f10-cad-loop --apply --cp2` (the ten CP2 counties), never `--limit=10`; the planner's instruction was wrong and the lane's flag is the standard. (3) Wave 1 GO: when criterion 5 closes with the ten counties at pass or named-class skip and the rate distribution filed, the planner issues the go for the 31-county wave 1 (`f10-cad-loop --apply` over the work-list snapshot, which executes the 25 and skips the 6 idempotent and the 6 quarantined and the 217 without landing), as a Cloud Run job, one execution, watched by the lane and the reaper. The 217 stay `lookup-failed` on the manifest until the F-09 acquisition card. | CP3 handback 2026-08-28; planner reads by field and Cloud Logging; planner repair recorded. | Y |
| A-020 | 2026-08-28 | F-06, F-07, F-08, F-10, F-09, F-11, F-12, F-13, F-14 | **Course correction (operator): every Factory effort goes to Central Texas serving in production; everything else waits.** Central Texas is the P-85 six: Bastrop 48021, Travis 48453, Williamson 48491, Hays 48209, Caldwell 48055, McLennan 48309. The cutover mechanism already exists and is the publish job: the conformant tier 1 and tier 2 bakes write `place_layer_snapshots` keyed by the parcel node id the serving path already uses, cortex-api serves them, PE reads `tiles.json`; no new serving code is built for this. **Sequence, and nothing else runs:** (1) the publish lane finishes the Bastrop bake as a succeeded run on staging (tier 2 fixed, bakes chunked by `prop_id` pages), walks it on the rows that run wrote, files CP2; (2) item 8, the operator word, the identical job on production targets, walk on production; Bastrop is the first county a user sees on the new shape; (3) the F-10 lane, after criterion 5 closes, runs the loop for the six only (`--counties=48021,48453,48491,48209,48055,48309`), dispositions the two quarantined classes that sit on this slice (Travis join P-80, Hays landuse hold) in the defect register with a fix or an accepted degraded disposition that the manifest shows as such, and reports which of Williamson and McLennan have landing; a county without landing in this slice gets a scoped acquisition before anything else in F-09; (4) the publish job runs per county for the five, staging sibling first then production under rule 6, each on the operator word or a standing word for the slice if the operator gives one; (5) then everything deferred resumes. **Deferred by this row:** the 25-county remainder of wave 1, the F-09 card for the 217, the loop concurrency lever, F-11 to F-14, the LDT rename item 4 remainder, the console proxy question, the facets-without-bearer answer (the route serves the same way before and after and is asked, not blocking). **Not deferred:** the reaper, the build-config rule (A-019), any fix that keeps a job from lying in the ledger. Measure of done for this row: six counties on the new shape in production `place_layer_snapshots`, each with a passed production walk on gold plus an area-swept set and a freshness stamp, and the manifest cells for the six reading from verdicts. | Operator 2026-08-28: "all efforts focused on getting CTX serving in prod. everything else can wait." | Y |
| A-021 | 2026-08-28 | F-06, F-07, F-08 | **Standing production word for the Central Texas six (operator: they are all approved).** Bastrop 48021, Travis 48453, Williamson 48491, Hays 48209, Caldwell 48055, McLennan 48309 may each be published to production by the identical publish job the moment that county passes its staging sibling under rule 6: a succeeded staging publish run, a passed staging walk on the rows that run wrote (gold plus an area-swept set, provenance `conformant-v1` per layer), and the refusal fixtures green. No further ask per county. The planner runs or verifies each production publish, reads the production walk, and records the revision, the run id, and the freshness stamp per county in this log; a county whose staging walk fails does not publish and is named. The standing word covers these six counties and this publish job only; any other county, any other consumer repoint, and any retirement of old-shape rows still need a word. | Operator 2026-08-28. | Y |
| A-022 | 2026-08-28 | F-03, F-06, F-08, F-10, F-19 | **Rulings from the two CTX handbacks (planner, under A-020).** (1) Replay hydrates the alias state the run started from (`knownBefore = knowledgeAt`); a replay that reads pass one's own rows cannot fail for a non-deterministic mint, so the literal two-pass wording of card A item 1 is superseded by this. (2) Tier-1 binding takes the earliest-known node among several authoritative aliases; published node ids do not move (V9). A repoint of random-era nodes to deterministic ids is a separate card, not implied. (3) The emitted alias set is persisted per chunk with the conflict target as the only dedup; one era row per key per run is accepted for the slice and the growth is a backlog item (reuse the earliest era when the binding is unchanged). (4) `execute-degraded` is admitted per county on three named classes for the six only; the loop refuses a register that widens it. (5) A `started` run with no live execution is `orphaned` by the reaper on age or on parent termination; landing-import and any long job declare `scope.max_duration_s` or accept the 3600 default. (6) The production Smart Site base for the walk is `https://smartsite.cloud`; `OPERATOR_PUBLISH_GO` and `PRODUCTION_SITE_URL` are execution-time overrides, never template values. (7) The staging reset job carries no Neon project or parent-branch configuration in its template and the staging secrets are hand-set to the branch hosts; the reset is not run for this slice (the 08-27 20:00 branches stand) and the coupling is a leave-behind for the publish card. (8) The two stale staging branches in cortex-prod (br-rapid-surf, br-lingering-glade) are deleted after the six are served, not before. | Handbacks 2026-08-28; planner reads. | Y |
| A-023 | 2026-08-28 | F-06, F-08, F-15, F-16 | **The access pair is the contract's, at every hop.** (1) The canonical pair for a public record is `catalog-listed / anyone-free` (19_the_instrument_contract.md, contract 1.30.0); `public / anonymous` is a legacy writer encoding, never a value. (2) Serve translates exactly the declared legacy table to the canonical pair and marks the payload `accessNormalizedFrom`; a served payload without the marker carries a stored canonical pair; anything outside the table refuses `ACCESS_NOT_DEFAULTED`. This is a declared translation of a named legacy encoding, not a widened check, and it exists so the six can serve while the store is corrected. (3) The tier 1 bake writes the canonical pair and the marker; the conformant writer stamps the canonical pair on every new atom (hauska-factory `conformant.mjs` lines 153, 173, 509 and the flood path). (4) Every conformant atom already written with the legacy pair (48021, 48055, 48209, 48029, 48085, and whatever the running loop writes for 48309, 48453, 48491) is re-stamped in place by a Factory card with its own run row and a count read back from the store; when that card closes, the serve translation is retired by test (a legacy pair reaching serve fails) and the marker disappears from served payloads. (5) A staging sibling proves nothing about serving unless the staging and production cortex-api revisions run the same LDT commit; the publish job records both revision names and refuses production when they differ (F-07 backlog, publish card). | Bastrop production walk 2026-08-28 08:40Z; code read; planner ruling under A-020. | Y |
| A-024 | 2026-08-28 | F-06, F-07, F-08 | **Per-county publish mechanics for the five after Bastrop.** (1) Gold parcels are chosen from the landing by a stated rule, the median house on a long residential street with unique prop_ids (read-only query, rows and unique counts recorded): Caldwell 48055 gold `48055:20478` (PLUM ST, Lockhart 78644, 60 unique of 119 rows), Hays 48209 gold `48209:135570` (CIBOLO CREEK DR, Kyle 78640, 104 unique of 120), McLennan 48309 gold `48309:176914` (BEVERLY DR, Waco 76711, 118 unique of 118); Travis and Williamson were chosen the same way at 10:26Z from the landing (read-only, 90 s statement timeout): Travis 48453 gold `48453:493738` (SHOALWOOD AVE, Austin 78756, 138 unique of 138 rows), Williamson 48491 gold `48491:76149` (DAVIS ST, Taylor 76574, 140 unique of 140). The sweep for these counties is the gold's street area (no registered smoke parcels), floor unchanged. (2) The staging hauska_mcp branch (br-little-term, cut 08-27 20:00Z) predates every county written today, so a staging bake for Caldwell, Hays or McLennan would read zero conformant rows and refuse. The planner cut a fresh hauska_mcp branch from production through the Neon API and rotated `STAGING_HAUSKA_MCP_URL` to it; the staging neondb branch (br-super-cloud, the staging cortex-api's read target, which resolves the secret only at deploy) is kept, so the walk still reads the snapshots the staging bake writes. `staging_targets` still names the old hauska_mcp host; the reset job's template carries no Neon configuration and the secrets are hand-rotated; both are the F-07 backlog item named in A-022(7), not fixed here. (3) The publish image bundles LDT 2c592b08 from this point so new bakes write the canonical pair; the Bastrop production snapshots keep the legacy pair under the declared serve translation until the A-023 re-stamp. (4) Staging publishes for counties already written may run concurrently (one execution per county); production follows each passed staging sibling under A-021. | Planner 2026-08-28 09:20Z; landing read; Neon API. | Y |
| A-025 | 2026-08-28 | F-06, F-08 | **The served facet set is part of the publish contract, and the walk grades it.** (1) The conformant tier 1 bake projects everything the old tier 1 bake projected for the same parcel (the CAD claim fields, the zoning stamp join, the derived envelope, land use, acreage, structural where present) or writes an explicit absent-verified per facet, never an omitted key; the divergence between the old snapshot and the new for the same parcel is a test that fails on any missing facet. (2) The walk grades content: for every walked parcel, each facet key the old bake carried is present or explicitly absent-verified in the served body; a body that merely says `conformant-v1` does not pass. (3) Travis and Williamson production publishes are held until (1) and (2) land; Bastrop, Caldwell, McLennan and Hays are re-baked to production on the corrected bake under A-021 (their staging siblings re-baked first). (4) The old-shape snapshots stay in place until the re-bake has passed the content walk (V9). (5) The Property Explorer reader is changed only where the subagent proves it reads a key the corrected bake cannot supply; the UI is not assumed to be the gap. | Operator screenshot 2026-08-28 13:40Z; both routes probed; planner ruling under A-020. | Y |
| A-017 | 2026-08-28 | F-09, F-10, F-06, F-08 | **Wave 1 is 31 counties, not 254; 217 counties have no CAD landing; A-016 chunking is GO; three publish rulings.** (1) F-10 wave 1 executes the 31 counties with landing (25 execute plus 6 already idempotent) after CP3 completes; the 217 `TX-LANDING-ABSENT` counties are not defects of the loop, they are absent sources, and their manifest cells read `lookup-failed` with the scope named, never a fabricated absence. They become an F-09 card: source manifests per CAD or TxGIO product, fetchers into immutable landing with checksums, one source at a time, the honest count first; the planner cuts it when CP3 closes. The 8,021,862-row landing was always 37 counties and the card's honest-count item is what surfaced it before a single county executed, which is the item working. (2) A-016 chunking implementation is GO in the f10 worktree now: ordered landing slices of about 50,000 rows, a run event per chunk, the county lease held whole, stages B to E per chunk, idempotent stage and merge; Bexar clean through chunks with chunk size and peak memory filed grades criterion 4. Criterion 3 stays on the field-level diff of one 48055 atom; criterion 5 runs once after chunking. (3) Publish lane: the four conformant tier 1 rows on staging `neondb` written outside the publish job are a fixture, are named as such in CP2 with the process that wrote them, and are superseded by the first succeeded publish run before item 6 grades; a dry run never terminates `success` or writes a freshness stamp (fix with a test that fails on the false-success fixture 831fec74); the LDT bake CLIs are bundled at image build time (a built entry the job runs with `node`, no pnpm in the container; the `workspace` esbuild condition stays); fe8e286d and 9be5ec16 merge to LDT main before the image rebuilds; the walk job's exit 1 on a pass verdict is fixed so the ledger does not read a pass as `failed`. Item 8 still waits for the operator word. | CP2 and CP3 artifacts 2026-08-28; planner reads by field. | Y |
| A-026 | 2026-08-29 | F-05, F-06, F-08 | **Open the Hays and Williamson join as situs recovery on the conformant bake, not as a seed lift.** Operator: open the join; take care of the six in waves. The prop_id join on 48209 and 48491 fabricates (Williamson R-account vs six-digit CAD; Hays two bare-numeric systems). `LANDUSE_JOIN_DISABLED_FIPS_SEED` stays. The old bake already recovers those counties on situs plus `ownersAgree`. The conformant bake refused the offered row and never ran recovery. Card G (PE labels for `stamp-missing` / `unmeasured`) is independent and ships now. Card H wires recovery into the conformant write path. One publish wave of the six follows H under A-021. Decision `_decisions/2026-08-29_ctx_open_situs_join_not_prop_id.md`. WDLL `_inbox/2026-08-29_ctx_quality_WDLL.md`. | Operator 2026-08-29. | Y |
| A-027 | 2026-08-30 | F-05, F-06, F-08 | **One more pass, then the last production bake of the Central Texas six.** Operator: Central Texas complete; expand cards under that umbrella; one thorough review; one bake; do not rebake. Every defect that would force a second facet bake rides this pass: landUse projection (A-025), named tax year, situs recovery on leftover no-row after an owner-agree sample, upsert fail-closed so gate-blocked writes no usable point (~58,461 inherited centroids), walk grades that can fail (BP-CONTENT-01, S4, S5, S11), PE wiring as its own F-06 card (missing change, not deploy lag). Wave R is that bake, under A-021, after W0b and the W1 / walk pins. PE does not block the publish and does block parent close. Atom rails (footprint, wells, roads, easements, Rainmaker edges, F-11 stamps, F-18 flood on the five) and the 18,100 CAD rows with no conformant row stay out of this denominator so complete cannot be read as mold county-complete. P-80 stays parked. Seed stays. Decision `_decisions/2026-08-30_ctx_one_more_bake.md`. Parent `_inbox/2026-08-30_ctx_facts_complete_WDLL.md`. W0b `_inbox/2026-08-30_ctx_w0b_prebake_review_WDLL.md`. **Rails-out reversed the same day by A-028.** | Operator 2026-08-30. | Y |
| A-028 | 2026-08-30 | F-06, F-10, F-11, F-18, P-09, P-11, P-17, P-85 | **Complete is a finished dataset or a named honest absence. Wave R waits on rail prep.** Operator: those fields are in; RRC must surface this pass; work volume is not a reason to omit a rail. Bake means the production surface. Sequence is prep, verify, one publish. RRC: `tx_rrc_well` already exists and is county-joined; apply `well-fact` for the six; PE reads atoms only (P-50); a parcel with no well is absence. Build only three missing stores: F-11 registry-loaded setback landing (engine still hardcodes Bastrop layer 23), Factory landing for the four known public easement GIS layers, and a serve-visible rail-absence row (ADR-029). Do not build a PDD setback table. Inventory `_inbox/2026-08-30_ctx_w3_rail_inventory_WDLL.md`. Decision `_decisions/2026-08-30_ctx_complete_or_absent.md`. | Operator 2026-08-30. | Y |
| A-029 | 2026-08-30 | F-10, F-16, F-06 | **Persist successful CAD to TxGIO situs binds as aliases. Next bake reads the alias first.** Operator yes 2026-08-30. Not a seed lift. `identity.alias` per doc 19 plus landing `landing_cad_txgio_alias`. Method `cad-roll-address-join`. `ownersAgree` required. No alias on `gate-blocked` or cannot-bind. Backfill card H `joined-situs` (Hays 130,663 / Williamson 511,029 at 13:48:33Z) before Wave R. W1 emits new binds; Factory writes atoms. Two-count: landing open-era rows equal alias atoms per FIPS. Decision `_decisions/2026-08-30_ctx_cad_txgio_alias.md`. WDLL `_inbox/2026-08-30_ctx_w1_alias_WDLL.md`. | Operator 2026-08-30. | Y |
| A-030 | 2026-09-04 | F-01 | **CTX pipeline wrap-up sprint scoped and dispatched.** A 19-agent adversarial audit (`_inbox/2026-09-04_ctx_pipeline_wrapup_scope_audit.md`: 4 full-repo branch/PR sweeps, 7 thematic deep-dives, adversarial batch review) closed out ACQUIRE-GIS wave 1 / PARCEL wave 2 / serve-cutover into 19 findings, refined in conversation into a durable sprint roadmap: `90_operations/OPS-19b_ctx_pipeline_wrapup_sprint.md`. Three fix-shape rulings (exemptionCodes/landUseSource/acreageMethod: widen `stampText`, not a new companion-row shape; queue-claim: Postgres-native locking, not a new lock service; reporting-safe read path: Neon read replica, not a materialized view). `feat/permits-field` (hauska-engine, orphaned exploratory branch) resolved: stay with the live companion-shape rail, branch stays parked. New item added: landUse gets its own parcel_record cutover card (operator: "a lot of the function of the platform rides on this data field"), resolving the 2026-08-30 present-presented-as-absent defect as a consequence of migration rather than a patch to code slated for retirement. Atom-backfill (ADR-031) and a dedicated onboarding-runbook rewrite explicitly held out of this sprint — atomization is next after this ships; the runbook only gets written if it naturally falls out of the lanes re-proving the B-SLATE pattern. Wave R (item 19, the *old* conformant-bake pipeline) stays blocked, reinforced. Dispatched as four lane-planner assignments (legacy-design-tools, hauska-factory, hauska-engine, hauska-map), each spawning its own sub-agents and reporting back directly, rather than one dispatch per item. | Operator 2026-09-04, across the full scoping conversation: three fix-shape agreements, "give landUse a card so it's fixed properly," "hold on atoms... go on runbook... fix landUse right," "make this a durable artifact." | Y |

## GRADE LOG (one row per grading pass; statuses re-graded by instrument only)

| Date | Rows graded | Instrument | Result |
|------|-------------|------------|--------|
| 2026-08-30 (CTX card H production, filed late) | F-06, F-08, A-021, A-026 | planner: six production close lines from `_inbox/2026-08-29_ctx_h_land_process.md` and live recount `_inbox/2026-08-30_ctx_w0_residue_recount.json` (2026-08-30T13:48:33Z, store production neondb `place_layer_snapshots`); publish image sha256:7bef3ce7; LDT 889b1556 | **Card H published the six under A-021. GRADE LOG was empty until this row.** Bastrop `vzfnd` / `e2c5c6d7` written 61695 / 77799 gold 48021:34137 joined SF-1; Caldwell `jptqt` / `cd961998` 73159 / 48649 gold 48055:20478 joined; McLennan `kkdm4` / `70a92b2a` 113090 / 114255 gold 48309:176914 stamp-missing joined; Travis `hhxg2` / `bb77fa65` 873766 / 500307 gold 48453:493738 unmeasured no-row; Hays `x2rw7` / `003cdc7c` 304332 / 173050 gold 48209:135570 joined-situs Kyle; Williamson `8ghwj` / `4a4efa03` 602050 / 602050 gold 48491:76149 gate-blocked Taylor. Seed leak 0. Live remainder after H: unstamped_sentinel 232770; Travis no-row still 119389; Hays joined-situs 130663 / gate-blocked 41619; Williamson 511029 / 91021. A-025 landUse and the 58461 gate-blocked non-sentinel points are not claimed met; they are A-027. |
| 2026-08-26 | none | baseline | All NOT STARTED except F-02 CARDED |
| 2026-08-26 (CP2) | F-00, F-01, F-03, F-04, F-05 | planner: `gcloud run jobs describe/executions list` (us-east4), seat gate run by violation, CP2 JSON; lane: unit 26 pass, live landing 2 pass | F-00 **FLIGHT**: job `factory-snapshot` in `us-east4`, execution `factory-snapshot-nbv69` succeeded 2026-08-26T19:26:43Z at `@sha256:df0268b2...`, but the job is deployed by tag `phase-a-local` and the row recorded the tag (item 2 open); taskTimeout 120, maxRetries 0. F-01 **FLIGHT**: store `delicate-lake-78875790` in `aws-us-west-2` (wrong region; `rtt_ms` 1,077; cutover to east-1 ruled before any landing import); landing UPDATE and missing retention class refuse live. F-03 **FLIGHT**: 17 holds imported, routing-pin script exits 2, `plan --path=existing` returns Bexar only, `--path=conformant` refuses `STAGE_NOT_BUILT`; control API not yet deployed. F-04 NOT STARTED (no Vercel URL). F-05 **FLIGHT**: 1,223 city rows, 71 defects open, manifest diff filed; item 14 Bexar edge read and item 20 CC publish open. Item 19 corrected 777 to 735. |
| 2026-08-26 (CP3 + east-1 cutover) | F-00, F-01, F-03, F-05 | planner: `gh pr view/checks` (PR #1 `MERGEABLE`, `CLEAN`, `test` pass on `1284c8b`, merged by planner), `gcloud run jobs describe/executions list` (digest `ae25421a…`, execution `p8skf` succeeded 19:56:49Z), cutover JSON counts | F-00 **LIVE**: repo, seat, gate negative, CI, image pinned by digest, first executions recorded with termination records; PR #1 merged to main. F-01 **LIVE** on region and schema: `withered-surf-26870298` `aws-us-east-1`, pooler refused, landing immutability live; item 6 landing copies and item 7 manifest diff still open. F-03 **FLIGHT**: holds 17, plan path-aware, control API committed not deployed (item 11). F-05 **FLIGHT**: 1,223 cities, 8,561 rails, 735 unprobed, 71 defects open; items 14 and 20 open. Item 2 rtt prediction failed (144 ms measured as a TLS connect; instrument corrected to query RTT p50, re-test next execution). F-04 NOT STARTED; console worktree registered. |
| 2026-08-26 (CP4) | F-00, F-01, F-03, F-04, F-05 | planner: `gh pr view` and check-run API (factory PR #2 `test: success` twice, merged by planner to `1f05e17`; map PR #223 `No double-encoded source: success` but the required `test` check has not reported, `mergeStateStatus BLOCKED`), `gcloud run jobs executions list` (`factory-snapshot-lmpfx` succeeded 20:26:51Z; seven jobs exist; `factory-landing-import-5t46m` running since 20:38Z after three successes and one failure), `curl` (console `https://smart-site-factory.vercel.app` 200 on `/` and `/site`; live `GET /api/county-ledger` carries **no** `published_at` field at 20:5xZ); lane CP4 | F-00 **LIVE** (PR #2 merged). F-01 **FLIGHT**: item 6 partial, 3 of 9 tables copied and matching, FEMA running. F-03 **LIVE**: control API `factory-control` in `us-east4`, 401 unauthenticated, `MISSING_ENV` refuse; leases and holds live. F-04 **FLIGHT**: console live with zero audit differences; PR #223 blocked on the required `test` check. F-05 **FLIGHT**: manifest diff 667 of 3,556 on both dump and live with zero displayState differences (item 7 met); item 14 Bexar 660,000 links and 660,000 rewritten of 703,257 (met); item 19 met on the cutover counts; **item 20 lane-claimed met, planner could not reproduce**: the served ledger payload has no `published_at`; evidence required (which endpoint, which table). Item 2: `query_rtt_p50_ms` 5 (samples 5,5,6,4,5,5,4,5,13,5), `connect_ms` 773; the under-5 prediction failed by definition and every job inherits about 5 ms per query and about 0.8 s per connection. |
| 2026-08-26 (close) | F-00, F-01, F-03, F-04, F-05 | planner: `gh pr view`/check-run API (hauska-map PR #224 `test: SUCCESS` and `No double-encoded source: SUCCESS` at 22:49Z, `mergeStateStatus CLEAN`, merged `be8b7eb` 22:57:06Z by the planner, `seat/property-factory` preserved; factory PR #3 `MERGED` 567d38a 22:43:16Z); `gcloud run jobs executions list` us-east4 read by field (factory-landing-import 4 succeeded, knl7d `NonZeroExitCode`; per-table jobs pipeline-mrxbd and wells-9f846 `Completed True`, cad-dq7md, footprint-ppdgr, parcel-wqdzv running at 22:53Z); live `GET /api/county-ledger` (no `published_at`, three fetches); `curl` console URL 200 unauthenticated; bundle read (`Authorization: Bearer` bound to a 32-character build-time string literal, `factory-control` URL present, `POST` verbs live in `src/control/api.mjs:133`) | F-00 DONE; F-01 PARTIAL (4 of 9 landing tables matched, 2 more completed unread, 3 running); F-03 DONE (knl7d quoting noted); F-04 DONE for the read console, OPEN on the key exposure; F-05 PARTIAL (publish job writes, serve does not read; LDT route change owed). Phase A card finish card written; honest partial close accepted. Next card F-02 option A drain, dispatch recompiled `_dispatches/2026-08-26_p81-loader_dispatch.md`; the preamble extractor was found truncating at the first capital Z (JS has no `\Z`) and fixed in `scripts/lib/standing-decisions.mjs`, so the final compile carries preamble vd3c673f8 over all 22 standing bullets (contract v92aa194c, process vbb19bd34). |
| 2026-08-26 (A-001 handback, drain CP1) | F-01, F-02, F-04, F-05 | planner: `gcloud run services describe factory-control` by field (revision `factory-control-00002-gjr` 100 percent); `curl` unauthenticated `GET /health` 401, `GET /counts` 401, `POST /start` with a JSON body 401 and with a bogus bearer 401; live console bundle `index-Btnfczdu.js` read, zero `Bearer` occurrences and zero 32-character literals (the earlier bundle had one); `gcloud run jobs executions list` by field (cad-dq7md `Completed` 23:24:26Z, footprint-ppdgr `Completed` 23:47:01Z, parcel-wqdzv running); direct `hauska_mcp` reads at 00:01Z (`schema_migrations` row `010_drop_access_policy_defaults.sql` applied 2026-08-26T23:53:53.551Z; `atoms.access_policy` and `document_ingest_atoms.access_policy` `column_default` null, NOT NULL kept; `jurisdiction_tenant = 'icc-model-code'` 8,731 `platform-internal`, zero `public-free`); check-run API on the three open PR heads (hauska-map #226 `3c447de` test success, hauska-factory #4 `3af9ea5` test success, hauska-engine #362 `38fbffc` `typecheck + test` FAILURE at typecheck in `packages/atoms/src/__tests__/write-boundary.test.ts`, fixtures missing `atomDid` and typing `entityType` as `string` against the union; PR `BLOCKED`) | F-01: 8 of 9 landing tables matched on two-count ledger rows as reported by the lane (pipeline 491,178; wells 1,396,049; cad 8,021,862; footprint 10,674,975; planner verified the executions, not the ledger rows), `txgio_parcel` in flight. F-04: key rotated, exposure closed, console honest-401 until the proxy; source fix #226 green and open. F-05: item 20 still not-met. F-02 drain: pre-step DONE and planner-verified on `hauska_mcp` (E-1 database half closed; OPS-16 A-036); write boundary and batched links on #362, red on CI typecheck while the lane reports vitest 19/19 locally (CI runs `tsc --noEmit`; local did not). Bexar resume not started; `OLD_SHAPE_FILL_FROZEN` holds. |
| 2026-08-27 (drain merges) | F-01, F-02, F-04 | planner: `gh pr view` on the three PRs (hauska-factory #4 `MERGED` 9040087 00:06:53Z; hauska-map #226 `MERGED` 4217a57 00:09:26Z after the branch caught main; hauska-engine #362 `MERGED` 7012ac7 00:31:24Z on head 22cf19d with check-run `typecheck + test = success`, run 33026887880); `gh api branches` (all three seat branches kept); engine `origin/main` 7012ac7 read for `packages/atoms/src/write-boundary.ts` (162 lines) and its test (136 lines); `gcloud run jobs executions list` (parcel-wqdzv still running at 00:45Z) | F-02: BP-WRITE-01 write boundary and batched `atom_links` are on engine main. As merged the boundary refuses a bare key with no FIPS prefix and a decimal-padded parcel grammar as `NON_CANONICAL_BINDING`, sentinel tokens as `KEY_SENTINEL`, and a non-hauska `did:` method or a hauska DID whose type disagrees with the column as `DID_NAMESPACE` (body versus column, two derivations); the first cut's integer-only second token was too tight for the store's real grammar (feature ids, boundary suffixes, `fhfact_*`) and CI tests, not typecheck, caught it after the typecheck fixtures were fixed; the lane's local 19/19 was vitest without `tsc`, CI is the grade. Bexar resume UNBLOCKED; `OLD_SHAPE_FILL_FROZEN` holds every other county. F-01: 8 of 9 landing tables matched, `txgio_parcel` in flight. F-04: console source fix merged; proxy still owed. Substrate retirement note filed at `_inbox/2026-08-27_substrate_access_policy_retire_note.md`. |
| 2026-08-27 (lease v2 gates) | F-01, F-02, F-03 | planner: `gh pr view` (engine #363 `MERGED` b402c8b 01:05:41Z on head 51497d6, check-run `typecheck + test = success`, run 33028751986; factory #5 `OPEN`, `CLEAN`, tests green, head a1d9ea4); direct `hauska_mcp` read at 01:21Z (`schema_migrations` `011_atoms_writer_lease_v2.sql` applied 2026-08-27T01:05:58.913Z; `pg_class` `atoms_writer_lease_v2` relkind `r`; primary key `(scope_type, scope_id)`; zero lease rows); `gcloud run jobs executions list` by field (parcel-wqdzv `Completed True` 01:04:36Z); `gcloud run jobs list` us-east4 (twelve `factory-*` jobs, no engine writer job) | F-01 DONE: 9 of 9 landing tables matched on two-count ledger rows as reported by the lane (`txgio_parcel` 16,428,786 = 16,428,786, run 63436483; planner verified the execution, not the ledger row). F-02 gate 1 DONE (write boundary, batched links, lease v2 on engine main b402c8b); gate 2 DONE (011 applied through the Factory `lease-v2-migrate` run bcf3776f, planner-verified by catalog read; the apply ran from factory PR #5 head a1d9ea4 before #5 merged, which is recorded here and #5 merges next); gate 3 OPEN and correctly refused: there is no engine writer image or job in us-east4, so the Bexar resume cannot run as a recorded Factory job and the lane declined to fake the gate with a laptop `--apply` (item 25). Gate 3 is Factory build work, the F-02 stage runner, not data processing. Bexar rows written: zero. `OLD_SHAPE_FILL_FROZEN` holds. |
| 2026-08-27 (F-02 close) | F-01, F-02, F-03, F-04 | planner: `gcloud run jobs describe factory-atoms-cad` by field (generation 2, image `atoms-writer@sha256:5a3bf94d…`, `ENGINE_SHA=2c90b993`, env `CAD_PARCEL_ROLL_PATH` only, no `APPLY`, no `COUNTY`, no `FACTORY_DATABASE_URL`, secrets `DATABASE_URL` and `SUBSTRATE_DATABASE_URL` from `ATOMS_DATABASE_URL`, `CORTEX_DATABASE_URL` from `SOURCE_DATABASE_URL`, timeout 21600); `gcloud run jobs executions list` by field with per-execution args (5mxw8 no args exit 1; sptb8 `--county=48029 --apply` `LEASE_REQUIRED`; rxb6w `--county=48021 --apply --run-id` `OLD_SHAPE_FILL_FROZEN`; rq4t6 dry run `--limit=1000` success; zkwd6 `--apply --limit=1000 --run-id=7fd24437` success; 2zdgw on the generation-1 image with baked `APPLY` not graded); `gh pr view` (engine #364 `MERGED` 2c90b99 01:56:16Z; factory #8 `MERGED` 7d5f50d 02:19:23Z with `bexar-cad` reading counts from the store and passing args per execution, `--update-env-vars` banned in its own header); direct `hauska_mcp` read (lease rows 0 after; a planner re-count by `entity_id` prefix was refused after a four-minute scan on the 100M-row table, so the 703,257 and the 999 rewritten rows are the lane's `atoms_built` reads, not the planner's); the stood-down path's job `factory-atoms-writer` (digest `4fea88da`, five executions 01:49Z to 02:14Z incl. canary msmfx) read by field | F-02 items 1 to 8 MET on `factory-atoms-cad`; F-03 run rows exist before every execution; F-04 authenticated `/screens` shows the run, public console still 401 until the proxy. Two canaries on 48029, both 999-row rewrites of existing atoms: msmfx 149.0 atoms/s (6,706 ms writer wall, stood-down job) and zkwd6 67.4 atoms/s (14,821 ms writer wall, 76,837 ms Factory wall). Item 7 prediction 300 FAILED at chunk scale on both; floor 150 failed on both (149.0 is under 150); the chunks are too small and cold to be the benchmark and no larger leg runs on the old shape. Bexar 48029 reads 703,257 atoms against a 703,257-row roll on the dry run and both canaries: COMPLETE on the old shape already; the resume was moot. Old-shape writes END here. Next: F-15 to F-18, with F-18 stage-and-merge as the write design the conformant writer takes from these readings. |
| 2026-08-27 (conformant CP2) | F-16, F-17, F-18, F-20 | planner: `gcloud run jobs describe factory-conformant` by field (generation 5, image `hauska-factory@sha256:2af32780…`, env `CONTRACT_VERSION`, `IMAGE_DIGEST`, `CLOUD_RUN_REGION`, `FACTORY_CLOUD`, secrets `FACTORY_DATABASE_URL` and `ATOMS_DATABASE_URL`, timeout 3600); `gcloud run jobs executions list` by field (kjzhx `Completed` 11:17:39Z to 11:19:24Z on `09f7dfff` with args `conformant --apply --rate-probe --rate-rows=100000`; q67j4 and mnjld exit non-zero on the unquoted PowerShell `--args`; 57lxt, fpt7z, qp9j2 `Cancelled` after O(n squared) loops on 77,799 rows; dzhj9 non-zero; 5j4mc `conformant --apply --replay` running since 12:23:51Z on `2af32780`); CP2 `_inbox/2026-08-27_f16-f18-conformant_cp2.json`; `npm view @empressaio/atom-contract version` 1.22.0 | **Item 5 (F-20 stage-and-merge rate) MET as the lane read it from the store:** run `e8823e11`, 100,000 rows staged in the Factory store (`write_stage_atoms`) and 100,000 `conformant-v1` rows merged into `hauska_mcp` (`entity_type = 'cad-parcel-roll' AND body->>'shape' = 'conformant-v1'`), 1,165.19 atoms/s over 85,823 ms, per leg adapt 1,742 ms, copy 6,771 ms, merge 77,256 ms (90 percent), count 54 ms; lease released; replay sample identical. Against the pre-registered 300 prediction and 150 floor: met, and 8x to 17x the old path's two readings. The planner did not re-count the 100,000 (a `body->>` predicate over the cad-parcel-roll rows is a heavy scan on the serving store); the count is the lane's. **Open question for the lane before close:** the probe merged 100,000 rows while the Bastrop landing holds 77,799, so the probe rows span counties beyond 48021, which item 10 excludes; report their county composition from `write_stage_atoms` and, unless they are all 48021, remove them by a recorded cleanup run keyed on run `e8823e11` before close so F-10 never counts them. Items 1 to 4 unit-met (76 pass, 2 skip), not yet graded by violation on the console because `factory-control` was not redeployed (Queues and Gates screens are in the tree only). F-15 still open at contract 1.22.0; shims in place. Items 6 to 8 in flight: item 7 baselines before the landing apply (old `cad-parcel-roll` 48021 not conformant 77,073; `parcel-node` 48021 62,394; landing 48021 77,799; enumerated `flood-hazard-fact` 48021 62,256); three landing executes cancelled after tier 2, reconcile, and alias-match loops scanned every row (now indexed, plus `landing_cad_property_county_fips_idx` on the Factory store); `takeScopedLease` now steals an expired lease. The PowerShell `--args` quoting defect recurred from Phase A (knl7d); the F-03 leave-behind to record it in a runbook was never written and is now the card amendment. |
| 2026-08-27 (conformant CP2 amendments) | F-03, F-16, F-17, F-18, F-20 | planner: `gcloud run services describe factory-control` by field (`factory-control-00004-jin` 100 percent on `2af32780`); `curl` unauthenticated `/queues` 401, `/gates` 401; `gcloud run jobs executions list` by field (5j4mc `failed` at 13:24:12Z on the 3600 s timeout, `exit 0`; t85jf `--limit=2000` non-zero at 13:35:00Z on `b5fd26cd`; npg6c `conformant --apply --replay` running since 13:41:05Z on gen 7 `c9c80153`, 36 minutes at 14:17Z with no pipeline1 event per the lane); lane evidence `_inbox/2026-08-27_f16-f18-probe-county-composition.md`, `…-factory-control-serving.md`, `…-5j4mc-timeout.md`, `…-t85jf-per-leg.md`; runbook `90_runbooks/factory_cloud_job_execute.md` now exists; `npm view` 1.22.0 | (a) Probe rows: 100,000 `conformant-v1` rows all `county_fips` 48021, all `rateProbe = true`, none with a landing alias, against a 77,799-row landing: synthetic replicas in the serving store. The planner's "unless all are 48021" was the wrong test; real-versus-synthetic is the test, and a body flag is a sentinel every reader must remember. Cleanup keyed on run `e8823e11` is REQUIRED before close (card amendment f). (b) Runbook line landed. (c) `factory-control` redeployed by canary and shifted; Queues owner `property-seat-resolution` depth 0, Gates `bastrop-cad-promote` throughput 500 per hour; depth 0 is honest, no full persist yet. (d) F-15 still open at 1.22.0. (e) 5j4mc timed out persist-zero: the sit was in-memory `pipelineFromRows` before `persistAliases`; a 2,000-row leg measured pipeline1 2,619 ms, which scales quadratically to about 3,930 s on 77,799 rows; the remaining quadratic was `store.aliases.find` per `resolveCandidate`, fixed in gen 7 with `aliasesByKey`, plus a 42P07 TEMP-table collision fixed by dropping before each entity type. **F-03 defect found by the lane:** the timed-out execution left run `80d375af` at `started` with no termination row and an expired lease that had to be deleted by hand; a run that can end without a termination is silent degradation (card amendment g: termination reaper, verified by violation). **Rule for the next execute (card amendment h):** no further full-county execute until a 10,000-row leg with per-leg clocks shows pipeline1 at or under 5x the 2,000-row leg (linear); 25x means a quadratic remains. npg6c is 36 minutes in with no pipeline1 event, consistent with a remaining quadratic; the planner's recommendation to the lane is to cancel it rather than burn the hour. Items 6 to 8 ungraded. |
| 2026-08-27 (conformant f g h + full county) | F-03, F-16, F-17, F-19, F-20 | planner: `gcloud run jobs executions list` by field on job gen 8 `hauska-factory@sha256:ace17072` (rkk9n `reap` `Completed` 14:29:46Z; dhvb4 `--limit=10000` non-zero at 14:36:08Z, the rate-floor exit 3 the lane reported; n5xm6 `--cleanup-rate-probe --keyed…` `Completed` 14:40:46Z; **jmwdp `conformant --apply --replay` `Completed True` 14:43:01Z to 14:45:14Z**; npg6c `Cancelled` 14:20:42Z); read of the conform worktree (`src/control/reaper.mjs`, `src/jobs/reap.mjs`, `cli.mjs reap`; no scheduler, cron, or interval anywhere in `src`, `scripts`, or the README); `gcloud scheduler jobs list` empty; lane evidence `_inbox/2026-08-27_f16-f18-cleanup-e8823e11.md` | (f) DONE as the lane read it: 100,000 stage atoms and 100,000 live `cad-parcel-roll` rows with `rateProbe = true` deleted under run `c9a81467`; both stores read 0 probe rows after; old-shape 48021 still 77,073; `assertZeroRateProbe` (`RATE_PROBE_REMAINS`) is the F-10 detector and passed. (g) Reaper EXISTS and was verified by violation on the three dead executions (5j4mc timeout, t85jf crashed, npg6c killed) without a hand fix; SIGTERM on the writer writes `killed`. **It has no trigger:** it runs only when someone executes `--args=reap`, so by the doctrine it is dormant; card amendment (i) gives it a Cloud Scheduler trigger before any multi-county loop. (h) The remaining quadratic was named (tier 2 scanning every node because the 48021 landing has 58,716 usable situs strings and zero centroids; `nodesByAddress` indexed) and the 10,000-row leg clocked pipeline1 at 1,298 ms against a 13,095 ms gate, 0.50x of the 2,000-row leg; full execute cleared. Then the full Bastrop run jmwdp completed in 133 s wall: landing 1,106 ms over 77,799 rows, pipeline1 5,536 ms (tier 2 considered 6,607), replay identical at 10,219 ms; the persist counts (aliases, nodes, atoms, edges) and items 6 to 8 grade at the lane's close. Job of record gen 8 `ace17072`. F-15 still 1.22.0. |
| 2026-08-27 (conformant close) | F-03, F-16, F-17, F-18, F-19, F-20 | planner: close `_inbox/2026-08-27_f16-f18-conformant_close.json` (15:30Z); `gh pr view` (factory #9 `MERGED` 53c2342, both `test` SUCCESS); `gcloud scheduler jobs list --location us-east4` (`factory-conformant-reap` `*/10 * * * *` ENABLED Etc/UTC); `gcloud run jobs executions list` by field (nm9zb `conformant --apply --flood` `Completed` 15:16:08Z on gen 9 `552694bd`; scheduled reaps 4z9lb 15:20:03Z, szn54, r47k6, 6b8mz, gkqbx, hqslh, wjrlk every ten minutes, all `Completed`); `_blueprint/40_rule_register.md` V11 and `_blueprint/10_model.md` lines 49 and 85 | **Graded, with one re-grade.** Items 1, 2 (all shims at 1.22.0, F-15 open), 3 (serving control), 4 (unit), 6 (replay identical at full scale), 8 (flood: 4 selector atoms A, AE, AO, X live; 62,254 Derivation rows staged with `derivesFrom` in the row; 62,254 of 62,256 enumerated rows re-pointed, the two null-zone rows 48021:8720001 and 48021:8725799 named as `FLOOD_ZONE_MISSING` not re-pointed; 60,192 ms, 1,033.74 rows/s, the statewide-cost falsifier), (i) (scheduler verified by violation: 4z9lb wrote f2gln `crashed` and released the lease with no hand reap) MET as the lane read them. Item 5 rate MET (1,162.90 atoms/s on 135,598 rows, 1,165.19 on the 100,000 probe). **Item 5 edges and item 7 V11: re-graded.** The persist wrote 77,799 cad atoms, 77,799 alias atoms, 89,799 alias rows over 77,799 keys and nodes, and **zero edges** (`applies-to` 0, `subject-to` 0, `derivesFrom` 0); the lane reported "V11 PASS is job-asserted; the store has zero applies-to edges". V11 is BP-EDGE-01, the rule that the property `applies-to` link population must not be starved (`10_model` line 85: intended parcel to fact, 0 rows). A job assertion cannot pass a rule whose subject is a store count; V11 is FAIL (starved) on 48021, item 5 is PARTIAL (atoms and rate met, edges not written), and item 7 is MET on the store-graded rules (V1, V3, V7, V14 PASS; V2, V4, V5, V6, V8, V9, V10, V12, V13, V15 UNMEASURED) with V11 corrected. The conformant lane closed honestly and is retired; the edge write is now F-10 wave 1's fourth start-gate condition (A-007). Tier-2 precision on a hand-graded sample: UNMEASURED, carried to F-10 CP2. Job of record gen 9 `552694bd`. |
| 2026-08-27 (F-10 CP1 + publish partial) | F-05, F-06, F-07, F-08, F-10 | planner: `gh api branches` (none of `seat/property-f10` on hauska-factory, `seat/property-publish` on hauska-factory, legacy-design-tools, or hauska-map is pushed; no PRs); `_inbox/2026-08-27_f10-wave1_cp1.json` (start gate names three conditions, no mention of A-007, edges, or V11); `_inbox/2026-08-27_f06-f08-bastrop-publish_{cp1,cp2,close}.json`; `gcloud scheduler` last fire 16:10:04Z as the lane read it | **F-10 wave 1 CP1:** conditions one to three met with evidence; the lane built from the pre-A-007 dispatch, so **condition four (applies-to and derivesFrom edges written in the merge, 48021 re-run by replay, V11 from the store) is not built and not known to the lane**; wave execute stays refused until it is. Built on `seat/property-f10` @ dccafb4: `f10-cad-loop.mjs` (work list from landing, defect register seeded, Ector/Hays/Travis/Dallas-Tarrant/situs classes quarantined, run row first per county, `f10_county_runs`, second loop refused on lease `f10-cad-loop:48`), `manifest-read.mjs` (cad-rail cells from store verdicts, derived indicators, BP-LEDGER-01 self-test), `f10-defect-register.mjs`, migration 0003, 48021-only guard removed, 91 tests pass; nothing pushed, nothing deployed, migration 0003 not applied; no laptop dry-run because no store credential in the worktree, which is correct. **Cross-lane write:** the F-10 lane put the F-05 item 20 change (`GET /api/county-ledger` top-level `published_at`) on `legacy-design-tools-publish` @ 1fafca9b, the publish lane's worktree, which is the collision pattern of A-003 again; ruled to the publish lane (A-008). **Bastrop publish partial:** item 1 MET (serving-path map: production cortex `cortex-api-tds7av26va-uc.a.run.app`, PE PMTiles hash `b692c6534d26` pinned in config); items 2 to 7 built and not live (`staging-reset.mjs`, `bastrop-publish.mjs`, `verify-walk.mjs`, `publish-guards.mjs`, migration 0002 `staging_targets` and `publish_runs`; LDT `serveGuards.ts` with 422 on access and situs refusal and the tier 1 and tier 2 conformant bake CLIs; hauska-map `TILES_MANIFEST_URL`, `loadTilesManifest`, `resolveParcelTiles`, `tiles.json` placeholder); 91 tests with **89 passing and the two failures unnamed**; blocked on Neon authentication (no key in that shell) and on the staging cortex revision and staging PE project; item 8 not started by design. The lane filed a close artifact for a card that is not closed; it reads as a checkpoint. |
| 2026-08-27 (condition 4 + CP2 loop + publish PRs) | F-03, F-05, F-06, F-07, F-10, F-20 | planner: `gh pr view` (hauska-factory #10 `MERGED` 9d42fbb 16:30:47Z and #11 `MERGED` abbdc25 16:36:18Z; hauska-map #235 `MERGED` 91f1d43 16:34:08Z; legacy-design-tools #493 `MERGED` 983f56f 17:13:19Z); `gcloud run jobs executions list` by field (conformant image now `6738a2b7`; pj77s `--county=48021 --apply --flood --replay` 17:19:06Z to 17:27:58Z exit non-zero, the rate-floor exit 3 the lane reported with the merge succeeded; `factory-conformant-migrate` k5s2n `Completed` 16:37:17Z on `6738a2b7`; `factory-publish-migrate` b4wdp `Completed` 17:04:07Z on `0f4084ca` after p5tsh failed on the args form; scheduled reaps every ten minutes through 18:10Z; the first CP2 county execution 2xwsc `--county=48013 --apply --replay --run-id=…` running since 18:19:46Z); `gcloud secrets list` (no `NEON_API_KEY`); `gcloud run jobs list` (`factory-staging-reset`, `factory-publish-migrate`, `factory-conformant-migrate` now exist); `_inbox/2026-08-27_f10_cp2_loop.log` (a UTF-16 PowerShell console log written by a laptop process) | **F-10 condition four CLOSED on the store grade as the lane read it:** after the b5p2w CAD replay and the pj77s flood replay, `applies-to` 77,799 of 77,799 cad atoms and `derivesFrom` 62,254 of 62,254 Derivation rows on 48021; V11 PASS from the store. **Two defects the lane found on the way, both real:** the shipped in-job V11 grader filtered `entity_id LIKE '48021:%'` while conformant atoms carry hash entity ids and `nid_*` locators, so it reported a vacuous 0 of 0 PASS (the class the doctrine names first); and `copyStage ON CONFLICT` does not bump `staged_at`, so replay-overwritten rows are invisible to the `staged_at >= knowledgeAt` count and the job exits 3 on a false rate floor with the merge succeeded. Both fixes are owed before CP2 closes (A-009). **CP2 loop started as a laptop process:** `f10-cad-loop --apply --limit=10` runs on the lane's machine with a Factory store credential, writes the loop's run rows from there, and executes the county jobs in the cloud; the writers are in us-east4, the orchestrator is not, and the design retires laptop runners by refuse. Bounded to ten, it may finish; the 254-county loop does not run that way (A-009). **Publish lane:** the two red tests were named and fixed (factory: two landing refusals skipped without a database, CI now runs Postgres 16 and migrates; LDT: `serveGuards` assertions on message not `err.code`), all three PRs merged, migration 0002 applied through `factory-publish-migrate`, `cloudbuild.publish.yaml` failed at job-deploy because `NEON_API_KEY` is not in Secret Manager; blockers are the key (operator), the staging database secrets for the cortex staging tag, and the staging PE project at `/site`. Item 8 not started by instruction. |
| 2026-08-27 (F-10 CP2 partial) | F-03, F-10, F-20 | planner: `gh pr view` (hauska-factory #12 `OPEN` on `seat/property-a009` @ 70d37b4, `CLEAN`, both `test` SUCCESS; main abbdc25); `gcloud run jobs executions list` by field, county executions since 18:00Z: **18 executions for 9 counties**, every county launched twice (48013 2xwsc ok then f5ggq non-zero; 48019 whm68 ok then nnvdz non-zero; 48027 pp528 ok then t59nw non-zero; 48029 8njc9 failed at 87 s then vjgj2 non-zero, overlapping; 48031 94wqq ok then prpl6 non-zero; 48053 r5fgn ok then npzwl non-zero; 48055 hwd77 ok then jdllt non-zero; 48085 kzzhx failed then rjzq4 non-zero; 48091 pfsgz ok then bn28k non-zero, overlapping); `gcloud run jobs list` (no `factory-f10-cad-loop` yet) | The laptop CP2 loop was aborted at 8 of 10 as the lane reported, and the executions show two things the CP2 filing must explain from the run rows, not from memory: **(1) every county was executed twice, several second launches overlapping the first**; the fast non-zero second executions read as the scoped lease doing its job (a second writer on a held scope refused), which is the control working, but the loop launching a duplicate is a loop defect (retry on the abort, a restart, or two loop processes) and it is named here; **(2) 48029 (Bexar) and 48085 did not fail on their own: Cloud Run reports both first executions as `A signal terminated the container` with exit 0**, which is the abort killing running county jobs (8njc9 after 87 s of a 703,257-row county, kzzhx likewise); the second launches read as exit 3 on 48013 (the false rate floor on a replay of an already-written county, the `staged_at` defect) and exit 1 on the overlapping ones (the scoped lease refusing a second writer), so the pairs are the loop launching each county twice and the abort cutting the running ones; the termination rows must say so explicitly. Seven of nine first executions completed. A-009 PR #12 is open and green with the three fixes (V11 grader scoped by `jurisdiction_tenant` landing scope with vacuous and zero-edge fixtures that fail; `staged_at` bumped on conflict; rate-floor recorded as `mergeOutcome` in `run_events` with exit 0 when the merge succeeded and the reaper reading the kind) and the `factory-f10-cad-loop` job build config. Ruling for the CP2 re-run: after #12 merges and the image is re-pinned, the `--limit=10` re-run is the FIRST execution of `factory-f10-cad-loop` as a Cloud Run job, not the laptop, so it closes A-009 (1) and CP2 together; replay makes the seven completed counties idempotent. The log moved to `_scratch/2026-08-27_f10_cp2_loop.log` is acceptable there. |
| 2026-08-27 (F-10 CP2 on Cloud Run + staging live) | F-03, F-06, F-07, F-08, F-10, F-20 | planner: `gh pr view` (factory #12 `MERGED` 99b2cb3; hauska-map #246 `MERGED` a000532); `gcloud run jobs executions list` (`factory-f10-cad-loop` 57sm5 exit non-zero on the collapsed `--command`, then 9hqxp `Completed` 18:57:02Z to 19:34:45Z on `d0232ccc`, args `f10-cad-loop --apply --limit=10`); CP2 `_inbox/2026-08-27_f10-wave1_cp2.json` (loop run b4234513, per-county run ids); live probes (`staging---cortex-api` `/api/healthz` 200; `smart-site-factory.vercel.app/site` 200 with the Smart Site title; staging PE `/tiles.json` 200 with hash `b692c6534d26`); `gcloud secrets versions list` (`NEON_API_KEY` versions 2 and 1 both still enabled) | **F-10 CP2, first Cloud Run loop execution, graded from the run rows:** work list 10, executed 5, skipped 1, defects 4. PASS with replay identical and old shape unchanged: 48013 (34,649 atoms, 355.3 atoms/s), 48019 (32,755, 446.7), 48027 (165,576, 614.5), 48053 (49,243, 523.7), 48091 (103,207, 595.4). SKIPPED 48021 by the `TX-SITUS-SENTINELS` quarantine class (the county already written under the conformant card; the class skip is the register working, and it also means the class distribution across 254 must be reported before the full loop). DEFECTS: 48029 Bexar and 48085 `TX-CONFORMANT-FAILED` terminated `crashed` with `landing_only` (large-county crash, not a rate floor, not a signal this time); 48031 `TX-CONFORMANT-FAILED`; 48055 `TX-REPLAY-NOT-IDENTICAL` at 723.5 atoms/s. Rate distribution n=6: 355.3, 446.7, 523.7, 595.4, 614.5, 723.5 atoms/s on Cloud Run, below the single-county probe's 1,165 and above the old path by 4x to 10x; reported as a distribution as the card asked. A-009 closed with this CP2: grader scoped by `jurisdiction_tenant` with failing vacuous and zero-edge fixtures, `staged_at` on conflict, rate-floor as `mergeOutcome` with exit 0 on a successful merge, `countyConformantOk` held on the replay counties; the laptop credential cleared. The laptop abort forensics are authoritative from `runs` and `run_events`: `planCadWorkList` has no idempotency guard, a restart re-dispatched the full list while loop 8b27a68d was running, both loops succeeded, nine counties launched twice; second launches refused on the loop lease or exited 3 on the pre-A-009 false floor; 8njc9 and kzzhx were signal kills whose store terminations still read `crashed` because `exitKindFromExecution` does not map a signal to `killed`. **Full 254-county loop: NOT GO (A-010).** **Publish lane, live not built:** cortex-api revision `cortex-api-00603-biq` by digest `67b09026…` tagged `staging` at zero production traffic on `STAGING_DEPLOYMENT_DATABASE_URL` and `STAGING_ATOMS_DATABASE_URL` placeholder secrets with `CORTEX_STAGING=1` (as the lane reported; the tagged URL's healthz 200 planner-read); staging PE project `property-explorer-staging` with `CORTEX_API_URL` on the staging tag; `smart-site-factory.vercel.app/site` rewrites to it and serves Smart Site; `tiles.json` served from the staging PE; `ExplorerMap` loads tiles through `resolveParcelTiles` (PR #246, pinned by a source test); `factory-verify-walk` armed with the staging site URL. Waiting only on the Neon key binding, which the operator placed at 18:52Z (version 2; version 1 still enabled and to be disabled). Item 8 not started. |
| 2026-08-27 (publish CP2 graded) | F-04, F-06, F-07, F-08 | planner: direct `hauska_mcp` read at 21:50Z with a 90 s statement timeout, the V11 grader's own predicate (`entity_type = 'cad-parcel-roll' AND jurisdiction_tenant = '48021'` grouped by `body->>'shape'`): **`conformant-v1` 77,799 rows, 73.6 s**; `applies-to` links on those rows **77,799**, 17.3 s; indexes `atoms_jurisdiction_idx` present; CP2 `_inbox/2026-08-27_f06-f08-bastrop-publish_cp2.json`; Factory main 99b2cb3 read for the rate-probe guard's DELETE predicates (all carry `body->>'rateProbe' = 'true'`; nothing deletes conformant rows by county); the console's public URL rendering `{"error":"UNAUTHENTICATED"}` on every screen (operator screenshot; expected since the key left the bundle 2026-08-26) | **Item 2 (staging targets) MET live:** Neon branches `br-super-cloud-ap4ied3j` (neondb, host `ep-wispy-fire-apd2q819`) and `br-little-term-apz850jo` (hauska_mcp, host `ep-frosty-sound-apsdv5ev`) from `factory-staging-reset` run 5cfaf915 with `NEON_API_KEY` bound; staging secrets on direct hosts; cortex `cortex-api-00606-loh` tagged `staging` at zero production traffic; `/site` serves; the broken parcel refuses 400. **Items 3 and 4 NOT MET, item 6 PARTIAL, and the lane's blocker is a wrong instrument:** the lane reported zero `conformant-v1` rows for 48021 on production and staging; production holds 77,799 by the grader's predicate. The old-shape rows (77,073) do not carry `jurisdiction_tenant = '48021'`, so a query keyed the old way (an `entity_id` prefix or a body county field) sees only the old shape and a query keyed the grader's way sees only the new; the lane's query saw the old shape and called the new one absent. The staging `hauska_mcp` branch was created after 17:00Z from production and carries the same rows; the lane re-reads it with the same predicate. The walk (run 3c625651, five parcels plus the broken fixture) passed on old-shape facets (`source: baked-snapshot`, StratMap and cad-roll provenance), which is a walk of production's shape, not item 6; a bake that never ran cannot be walked. **Laptop writes against the staging branch:** the tier 1 bake attempts (run 1bd6316f, Neon 57P01 after a 327 s full-table scan; then a scoped four-parcel bake) ran from the lane's machine against the staging store; staging is a store; the do-not stands; and a bake that scans the atoms table statewide is the wrong query, the bake is county-scoped by `jurisdiction_tenant` and `shape`. The bakes are LDT code, so the publish job is an image built from `legacy-design-tools` (the F-02 engine-image pattern), run in us-east4 with the staging secrets, never a slim Factory container with an `LDT_ROOT`. The deployed `factory-verify-walk` failed on an FK insert order fixed locally and not redeployed. Item 8 NOT GO, correctly. **Console:** every public screen is honest-401 because the control key was rotated out of the public bundle on 2026-08-26 and no operator sign-in exists yet; the Factory store holds every run since Phase A; F-04 card cut (A-011). |
| 2026-08-27 (F-04 close) | F-04 | planner, from a shell with no session (the instrument the card names): console `GET /` 200 with no store content; `GET /api/proxy/counts` 401 `{"error":"UNAUTHENTICATED"}`; `GET /api/proxy/runs` 401; `POST /api/proxy/start` 401; served bundle `index-BEn__oZc.js` (147,180 bytes) with zero `Bearer`, zero `factory-control` hostnames, zero 32-character hex or base64url literals, one `/api/proxy` reference; `/site` still 200; `factory-control-00006-raz` at 100 percent on `sha256:9e5a3406`; `POST factory-control /start` with a forged `X-Factory-Operator` and no Bearer 401 `UNAUTHENTICATED`; `gh pr view` (factory #13 `MERGED` bc2166f; hauska-map #252 `MERGED` f1ac168); `/api/auth/status` reports `google: false, microsoft: false, anyProvider: false` | **F-04 items 1 to 6 MET, planner-verified by violation.** The key is out of the client, the proxy holds it server-side, the session door and the operator allow-list (`empressaioemail@gmail.com`, `dev@smartcityos.io`) are live, mutations carry the operator identity onto `runs.scope.operator` and `control_actions.actor` and are refused by `factory-control` without it (`OPERATOR_REQUIRED`, the old hardcoded api actor included), and Phase A item 11 is re-graded from the public internet. Two real `start` rows were recorded on the way (canary 362c94c2, signed-in 3241ba27). One leave-behind, the operator: the Google and Microsoft OIDC client secrets and `OIDC_STATE_SECRET` are not on the `smart-site-factory` Vercel project and the Google client lacks the redirect URI `https://smart-site-factory.vercel.app/api/auth/google/callback`, so the sign-in button returns an empty page until they are set and the console is redeployed. F-04 DONE at the console; the operator sees runs the moment the button works. |
| 2026-08-27 (A-013 live) | F-04 | planner, from a shell with no cookies at 22:50Z: console `GET /` 200 with no sign-in markup; `GET /api/proxy/counts` 200; `GET /api/proxy/runs` 200 with twenty rows, the lane probe run c536c8c6 present and the newest row a scheduled `f03-reap`; bundle `index-DmJSob9i.js` with zero `Bearer`, zero `factory-control` hostnames, zero 32-character literals; `POST factory-control /start` with no Bearer 401; `gh pr view` (hauska-map #253 `MERGED` 008f9d7, pathspec `apps/factory/**`); `factory-control-00006-raz` unchanged at 100 percent on `sha256:9e5a3406`; decision record `_decisions/2026-08-27_factory_console_auth_off.md` written by the lane in the decision-log format with owner operator | **A-013 is live as ruled and as bounded.** `FACTORY_CONSOLE_AUTH` defaults `on` in code and is `off` on the `smart-site-factory` Vercel project only; a deploy that forgets the variable re-locks; the OIDC and session code stay in the tree. Reads and the eight verbs answer with no session; a mutation stamps `console:anonymous` (the lane probe wrote run c536c8c6 that way); the key never returns to the client and `factory-control` keeps its Bearer check, so the public surface is the proxy. The exposure stands exactly as A-013 names it. The operator now sees every run since Phase A on the first screen. F-04 DONE; the door returns on the operator word by a variable flip and a redeploy. |
| 2026-08-27 (publish A-011 implemented) | F-06, F-07, F-08 | planner: `gcloud run jobs describe` by field (`factory-bastrop-publish` gen 2 on `sha256:64088330c` with `FACTORY_DATABASE_URL`, `STAGING_NEONDB_URL`, `STAGING_HAUSKA_MCP_URL`, timeout 3600; `factory-verify-walk` gen 3 on the same digest, timeout 900); executions (8fffp failed at 22:38:57Z after 28 s on the `.bin/tsx` ENOENT; 5jlgs starting at 22:55Z; the walk job has no execution since its three pre-fix failures at 20:19Z to 20:21Z); Cloud Build 890e89f1, 22dd2af7, 1ed42f0e SUCCESS; `gcloud secrets list` (`STAGING_NEONDB_URL`, `STAGING_HAUSKA_MCP_URL` present) | **A-011 implemented as ruled.** The wrong instrument is filed verbatim (`entity_id LIKE 48021:%` with the shape filter, which sees no conformant row) and a shared predicate module `src/lib/conformant-store-predicate.mjs` (`jurisdiction_tenant` plus `body->>shape`) now feeds the V11 grader, the LDT conformant tier 1 and tier 2 bake CLIs, and the walk, whose new rule `BP-CONFORMANT-01` rejects old-shape `node-facets-tier1-v1` and cad-roll baselines. No laptop store access. The bakes run as `factory-bastrop-publish` in us-east4 on an image that bundles LDT with the conformant CLIs (`Dockerfile.publish`, Node 22, `tsx` invoked by its `cli.mjs` after the first run died on a non-executable `.bin`). **Two cautions recorded:** the bundled LDT code is from the lane worktree and its conformant CLI fixes are not yet merged in LDT, so the image is built from unmerged code, acceptable for the staging proof and not for close (the LDT PR merges and the image rebuilds from main before item 3 grades); and a 77,799-row tier 1 bake against a 3600 s task timeout is close to the edge, so if 5jlgs nears it the bake is chunked per tier and batch with a run row each rather than the timeout raised blindly. Item 6 grades only on facets carrying `conformant-v1` provenance; item 8 waits for the operator word. |
| 2026-08-27 (F-15 close) | F-15 | planner: `npm view @empressaio/atom-contract versions` (1.23.0 through 1.30.0 present, latest 1.30.0 published 2026-08-27T22:53Z); `gh pr view` (contract #23 `MERGED` f8b7a6d; hauska-mcp-server #76 `MERGED` 9c0aedd; hauska-engine #365 `OPEN`, `CLEAN`, `typecheck + test` success); contract `src/conformance` on main lists `track2-node-id`, `track2-alias`, `track2-provenance`, `track2-derivation`, `track2-absence`, `track2-supersession`, `track2-selector`, `track2-access`; LDT `package.json` files read (api-server, engine-core, knowledge-atoms, portal-ui vendor `@hauska/atom-contract` from `vendor/` by `file:`, submission-classifier pins `^1.1.0`); close `_inbox/2026-08-27_f15-contract-types_close.json` CLOSED_PARTIAL | **F-15 CLOSED_PARTIAL, graded as filed with one finding added.** Eight additive minor releases in stage order, each with a fixture and a test that fails on the violation it forbids, each an ADR-028 amendment (dash-clean); PR #22 read not continued and the Lane G `NodeId` confirmed never shipped in 1.21.0 or 1.22.0. The contract is stricter than the Factory shim on three points (`absent-verified` requires `responseRef`; `SUPERSEDED_BY` requires `closedAt`; provenance adds Observation and Synthesis from `19`), which the shim deletion must adopt. Item 10 PARTIAL and accepted: one pin-bump CI per real consumer against 1.30.0 is the meaningful test, the intermediate versions only add unused exports; MCP pinned and merged, engine pinned with CI green and left for the property seat to merge. **Finding:** `legacy-design-tools` is not a consumer of `@empressaio/atom-contract` at all; it vendors the frozen `@hauska/atom-contract` tarball (the pre-2026-07-06 name, 1.6.x) by `file:` in four packages and `^1.1.0` in a fifth, so nothing in LDT, including the Factory publish bakes that are LDT code, can type against any Track 2 type until LDT moves to the current package (A-014). Leave-behinds carried: Factory shims until F-10 pins 1.30.0 and deletes them; engine #365 unmerged; `publish.yml` `workflow_dispatch` plus `set_latest` (substrate); PR #22 still open on `seat/substrate`. |
| 2026-08-27 (F-10 A-014 + CP3 partial) | F-10, F-20 | lane handback; `_inbox/2026-08-27_f10-wave1_cp3.json` read; nothing pushed yet, so nothing planner-verifiable beyond the artifact | **A-014 implemented on `seat/property-f10`, uncommitted:** `@empressaio/atom-contract` pinned `^1.30.0`, `CONTRACT_VERSION` 1.30.0 in the Dockerfile, the build config, and the job defaults; `src/contract-shim/` deleted and the shim check removed from CI and the CLI; three small Factory libs (`content-id`, `six-field`, `node-id-guard`) replace what the contract does not export; the three stricter rules adopted (`responseRef` on `absent-verified`, `closedAt` on `SUPERSEDED_BY`, Observation and Synthesis as classes); Record provenance on `fetchRef`; access on the contract vocabulary; 114 tests pass including `contract-track2.test.mjs`. **CP3 (A-010) PARTIAL:** criteria 1 (idempotency guard), 2 (signal to `killed`), 6 (class distribution up front) are unit-met on the branch and are not yet proven by violation on Cloud Run; criteria 3 (48055 replay diff), 4 (Bexar and 48085 crash cause, Bexar clean), 5 (CP2 list at ten of ten) are OPEN and need the deployed image and executions. Ruling: push and PR now on green, rebuild and re-pin, then prove 1 and 2 live (re-run the CP2 list and watch the five passes skip; cancel one execution and watch the scheduled reaper write `killed`), then 3, 4, 5. Untracked `scripts/` probe files are not committed; they are named in the CP3 close as scratch or deleted. Full loop still NOT GO. |
| 2026-08-27 (LDT rename PR) | F-15, F-06, F-08 | planner: `gh pr view` (legacy-design-tools #504 `OPEN`, `CLEAN`, head 3890902e on base main b53a0571 with 0 behind, +331/-231 over 105 files; check-runs Typecheck, Test, fixture drift, the L5 and SS greps all `success`); the PR file list (six manifests, `vendor/` tarballs removed, `scripts/check-no-hauska-atom-contract.mjs`, `serveGuards.ts` and its test, `nodeFacetBakeTier1ConformantCli.ts`); `gh api compare` (merge base b53a0571, so the branch was taken after the publish lane LDT PR #493 merged at 17:13Z); lane artifacts `_inbox/2026-08-27_ldt-contract-rename_{cp1,cp2,close}.json` | **Rename items 1, 2, 3, 5 MET on the PR; 4 PARTIAL; 6 OPEN on the operator go; 7 filed.** CP1 inventoried 88 import sites (the planner grep of 73 counted only `from` imports in ts, tsx, mjs; the difference is the instrument, not the tree; a background recount that walked `node_modules` and reported 1,618 was discarded correctly). Every `@hauska/atom-contract` reference moves to `@empressaio/atom-contract ^1.30.0`, the four tarballs are deleted, and a CI step in the typecheck job runs the retirement gate (its first push failed `GATE-DID-NOT-RUN` because the job had no ripgrep, fixed at 3890902e, which is the gate failing loud rather than passing silently). Fail-closed changes: `serveGuards` delegates to the contract `parseAccessPair` and refuses the legacy public and anonymous strings; the tier 1 conformant bake no longer defaults `body.access`; fixtures carry Track 2 values. Item 4 partial because the tier 1 bake SQL still filters `shape = conformant-v1` as a string. **Deviation accepted with the record:** one PR instead of the dual-pin plus six per-package PRs the card asked for; the per-package split was for breakage isolation and every check is green across the whole repository, so it is structurally compressed and functionally complete, as the lane wrote. **Coupling to act on:** the publish lane holds uncommitted conformant-CLI fixes in `legacy-design-tools-publish` on the same `nodeFacetBakeTier1ConformantCli.ts` and `serveGuards.ts`; after #504 merges it rebases onto main and reconciles, and the publish image rebuilds from main, before its bake result can grade. Item 6 is a cortex-api canary from the merged main, planner-owned on the operator word. |
| 2026-08-28 (CP3 on Cloud Run + rename closed) | F-06, F-10, F-15, F-19, F-20 | planner: `gh pr view` (factory #14 f119167, #15 7cd0202, #16 f62009e `MERGED`; LDT #504 532bddc3 and #505 b54b50ce `MERGED`, LDT main 7a36eb71); jobs by field (`factory-conformant` gen 14 on `c9ec2df9` at 4 CPU 16Gi, `factory-f10-cad-loop` gen 6 on `8612b8cc`, `factory-bastrop-publish` gen 3 and `factory-verify-walk` gen 4 on `3d6b1dae` from LDT b54b50ce at contract 1.30.0; publish build e053d769 SUCCESS 00:24:18Z after two failures); executions by field (bpzbb dry-run 23:44Z; px8xd `--apply --limit=10` non-zero 23:44Z; mnzqj cancelled by user 23:33Z; 4s977 48085 `A signal terminated the container` after pipeline1 on 8Gi; **wrn26 48029 on 16Gi `A signal terminated the container` at 00:18:09Z after 3.6 min**; 4j6pr 254-county dry-run `Completed` 00:18:43Z); CP3 `_inbox/2026-08-27_f10-wave1_cp3.json`; rename close `_inbox/2026-08-27_ldt-contract-rename_close.json` | **CP3 criteria on Cloud Run:** (1) MET, loop 5b2c4437 skipped the five CP2 passes as idempotent with the snapshot ids on record; (2) MET, cancel of mnzqj on 48029 produced run 04f1be29 `exit_kind = killed` at 23:40:23Z by the scheduled reaper, no hand reap; (6) evidence landed, the 254-county dry-run 4j6pr completed and the lane files its distribution; (3) NOT MET as filed: `TX-REPLAY-NOT-IDENTICAL` on 48055 carries two hash pairs and the disposition re-run, and A-010 asks for the explanation from the diff; a replay that hashes differently on the same landed bytes and logic version is an F-19 violation until the differing field and its cause are named (A-016); (4) PARTIAL with the cause now known: 48085 died by signal after pipeline1 on 8Gi (387,334 landing rows, 774,668 atoms in memory) and Bexar died by signal again on 16Gi at 3.6 minutes, so the county is held in memory whole and RAM is not the fix (A-016); (5) PARTIAL, ledger 6 pass, 1 skip, 3 defect. Full loop NOT GO. **Rename lane CLOSED:** items 1, 2, 3, 5, 7 met, 4 partial (tier 1 bake SQL still on the shape string, owed to the property seat), 6 open on the operator go (cortex-api canary, planner-run); the lane regenerated `_STATE.md` and the property state, which ride in this commit. **Publish lane:** rebased onto #504, reconciled the conformant CLIs (`conformantCadCountyWhere`, `assertAccessPair` with no default, content-hash snapshots), #505 merged, the publish image rebuilt from LDT b54b50ce and all four publish jobs re-pinned; the bake grade waits on that image. |
| 2026-08-28 (publish CP2 second + CP3 filed) | F-06, F-08, F-09, F-10 | planner: jobs by field (`factory-bastrop-publish` gen 7 on `afacb565` with args `--target=staging --county=48021 --skip-pmtiles`; `factory-verify-walk` gen 8 on `afacb565`); executions (publish: spqjv, 5mjks, klckt, jkjkj exit 1 across four images, and **vqvns on `2fca0bb0` `Completed successfully` in 74 s**, which the lane's own CP2 names a false-success run, 831fec74, that wrote a freshness row from a dry run; walk pwjkf exit 1 at 02:01:55Z); LDT commits 9be5ec16 and fe8e286d exist, the lane reports fe8e286d unmerged, LDT main b8983157; `_inbox/2026-08-28_f06-f08-bastrop-publish_cp2.json`; `_inbox/2026-08-28_f10-wave1_cp3.json` | **Publish CP2 (second):** three real causes traced and fixed in code (the `tsx` path under the pnpm layout; conformant node ids are `nid_*` with `sourceIdentifiers.prop_id`, not `48021:apn`; Cloud Run splits `--prop-ids` on commas, dot separator adopted), and the job still exits `BAKE_FAILED` on the pnpm subprocess inside the container. Items: 3 PARTIAL with no succeeded `publish_runs` row; 6 PARTIAL, the walk passes five parcels and gold carries `conformant-v1` provenance on a live curl, but the conformant rows it walked are **four tier 1 snapshots written to staging `neondb` outside the publish job** (the artifact does not say by what process); 8 not started. **Three planner rulings (A-017):** the four rows are a fixture, not a bake, and item 6 stays partial until the walk reads rows a succeeded publish run wrote; a dry run that terminates as a success and writes a freshness stamp is a ledger defect to fix with a failing test; the bake CLIs are bundled at image build time so the job runs `node` on a built entry with no package manager at runtime. **F-10 CP3 filed:** criteria 1, 2, 6 MET, 3 NOT MET, 4 OPEN pending A-016 chunking, 5 deferred until chunking. **The 254-county distribution is the finding of the day:** execute 25, idempotent 6, skip by class 6 (Travis join, Hays landuse, Ector keyKind, situs sentinels, Dallas and Tarrant vintages), **no landing 217** (`TX-LANDING-ABSENT`). The 8,021,862 landing rows cover 37 counties. Wave 1 as carded, the CAD roll for all 254, cannot run from the store as landed; the executable set is 31 counties, and 217 need L1 acquisition first (A-017). |
| 2026-08-28 (cortex-api canary shipped) | F-06, F-15, F-05 | planner-run deploy on the operator word: LDT `cloud-run-deploy.yml` `workflow_dispatch` `deploy-canary` with `image_tag` = full SHA b89831574c4133c0cf7c3296f90bd73f0bfa1702 (push build success 01:49Z), run 33137477526 `success` (revision tagged `canary` at zero traffic on `apps/cortex-api:b8983157…`); canary smoke by file instrument against the tag URL and against production before the shift (healthz 200, county-ledger 200 on both; payloads byte-equal in size, differing only in run-time fields); `shift-traffic` run 33137590542 `success` (`gcloud run services update-traffic cortex-api --to-tags=canary=100`); production after the shift healthz 200, county-ledger 200; `node scripts/product-surface-smoke.mjs` strict: every check PASS except the three pre-existing `envelope.sanity` fails (34073, 34785, 34017, `ringVerts=0`, the P-60b leave-behind) | **Rename card item 6 MET; the card is fully closed except item 4 remainder.** Production cortex-api now serves LDT main b8983157, which carries the contract rename (#504), the conformant bake reconciliation (#505), and the county-ledger `published_at` field (#493). Two observations for the record, neither a regression: (1) `published_at` is served as a top-level key with value `null` on both the old and the new revision, because the served grid is recomputed live (`computedAt` 02:50Z) and the Factory publish stamp of 2026-08-26 does not match it; the field exists, the stamp will populate when the Factory publishes the grid the GET serves, which is F-05 completion, not this card; the planner smoke printed `(absent)` for a null and is corrected here. (2) `GET /api/brokerage/v1/place/node/48021:34137/facets` answers 200 without a service bearer on both revisions; the PE spine proxy adds a bearer it does not need for this route, so either the facets read is public by design (Layer 1 free, plausible for public-record facets) or the route lacks the gate it was meant to have; the property seat answers which, with the route list, before the Bastrop production publish (item 8). |
| 2026-08-28 (A-016 landed, Bexar clean) | F-06, F-08, F-10, F-19, F-20 | planner: `gcloud run jobs executions list` by field (**factory-conformant-2nd9z `--county=48029 --apply --replay` on `8fd1cd3a` `Completed successfully in 57m2s`, 03:04:01Z to 04:01:03Z**; scheduled reaps every ten minutes through 05:10Z; `factory-bastrop-publish` 5r86n on `e75ab15c` exit 1 at 04:29:19Z after 5m34s; 9lpl5 exit 1 at 26 s on `7b3b5399`; no walk execution since 02:01Z); jobs by field (`factory-conformant` gen 15 on `8fd1cd3a` 4 CPU 16Gi; `factory-bastrop-publish` gen 9 and `factory-verify-walk` gen 10 on `e75ab15c`); `gh api` (factory commits 694c286, d99dda8, 8119196, 0804618 exist; factory main still f62009e; `seat/property-f10` 1 ahead, `seat/property-publish` 3 ahead 10 behind; LDT #513 27a7134a and #515 7986a919 `MERGED`); artifacts `_inbox/2026-08-28_f10_criterion3_replay_diff.json`, `_inbox/2026-08-28_f06-f08-bastrop-publish_cp2_a017.json` | **F-10: the largest county ran clean end to end through chunks.** A-016 as built: ordered landing pages by `prop_id`, 50,000 rows per slice, a `run_event` of kind `chunk` per slice with offset, rows, atoms, peak memory and fingerprint, the county lease held whole, a fresh reconcile store per chunk and a shared resolve store across chunks, persist, stage and merge per chunk. **Criterion 3 root cause found and fixed, and it was a real F-19 violation:** tier 3 nodes were minted with a random `mint()` and the replay ran against a warm reconcile store, so `nodeId`, `atomId` and `atomDid` diverged at scale; the fix is `mintDeterministicParcelNode(aliasKey)` (`nid_` plus the first 32 hex of a sha256 of the alias key), cold replay hydrated from the store after pass one, and a fingerprint roll per chunk comparing passes; four new tests, 121 passing. Criterion 4 grades when the lane files 2nd9z's chunk events (count, peak memory, replay identical) and its termination; criterion 5 then runs once. **The A-016 commit is on `seat/property-f10`, not on main, and the running image was built from the branch;** the PR merges before criterion 5. **Publish lane A-017 shipped on `seat/property-publish` (3 ahead, 10 behind main, unmerged):** dry-run and zero-write refuse with the 831fec74 fixture test, the walk exit fixed (`client.end()` out of `finally`), the bake CLIs bundled at build (`BAKE_CLI_ROOT=/app/bakes`, no runtime pnpm, an esbuild output-name defect fixed), LDT pinned. Run 5r86n wrote **41,911 conformant tier 1 snapshots to staging `neondb`** in about five minutes and then the job died before success or termination; Factory run 3eb8d70d is stuck `started` because no reaper covers publish runs. **The fixture attribution is wrong in the lane's artifact:** the four conformant rows were inserted by scripts the publish lane ran from its own machine (`insert_gold_conformant_tier1.mjs`, `insert_smoke_conformant_tier1.mjs`), not by the planner, and that was a laptop write to a store under A-011; the artifact is corrected by the lane (A-018). Items 3 and 6 stay partial; item 8 not started. |
| 2026-08-28 (CP3 five of six + loop repaired) | F-03, F-10, F-19, F-20 | planner: `gh pr view` (factory #17 51bdae9, #19 ce43267, #20 84f1d1c `MERGED`; main 84f1d1c); jobs by field (`factory-conformant` gen 18 and `factory-f10-cad-loop` gen 7 both on `3e1bbea0`; **the loop job template carried `command: ["node src/cli.mjs"]` and `args: ["f10-cad-loop --apply"]`, each collapsed into one token**); executions by field (25s5h and kpql2 exit 1 in about 15 s with correct `--cp2` args; jg9mh cancelled; dj6js and gbgt7 earlier `--counties=` runs completed); Cloud Logging for 25s5h: `Application failed to start: The container may have exited abnormally`, `Application exec likely failed`; planner repair from a bash shell: `gcloud run jobs update factory-f10-cad-loop --command=node,src/cli.mjs --args=f10-cad-loop` on the same digest (gen 8, command read back as two tokens), then `execute --args=f10-cad-loop,--apply,--cp2`, execution **factory-f10-cad-loop-scllr running since 05:58:01Z**; CP3 `_inbox/2026-08-28_f10-wave1_cp3.json` (5 of 6), Bexar evidence `_inbox/2026-08-28_f10_bexar_2nd9z_evidence.json` | **CP3: criteria 1, 2, 3, 4, 6 MET; 5 executing.** Criterion 4 as filed from run events: 2nd9z, 15 chunks over 703,258 rows, peak 2,109 MiB, 57 minutes, `exit_kind = success`; the same county that died on 8Gi and 16Gi whole fits in 2.1 GiB chunked. Criterion 3 MET with PR #17 merged (deterministic mint, cold replay, fingerprint per chunk). **F-03 fleet reap CLOSED:** PR #19 lists every `factory-*` job through the Cloud Run API (the container has no gcloud), a manual reap synced 2nd9z to `success` and reaped the stuck publish run 3eb8d70d as `crashed`, the scheduler still fires every ten minutes. **Criterion 5 was blocked by two things, one of them the planner's.** The planner wrote `--limit=10`, which slices the first ten FIPS codes and produced eight `TX-LANDING-ABSENT` plus two idempotent rows on run f1e57382, not the CP2 list; the lane added `--cp2` (PR #20) and that is the right invocation. Then every `--cp2` execute died at start because the loop job had been redeployed by hand from PowerShell, which collapsed `--command=node,src/cli.mjs` into one token; the lane's logs were empty because the container never ran. The planner read Cloud Logging, repaired the template from bash on the same digest, executed the CP2 loop, and handed the execution to the lane to watch; expected shape is seven skips (six idempotent plus the situs-sentinel class on 48021) and three executes (48029, 48055, 48085), one to two hours with Bexar. **Wave 1 GO is issued on criterion 5 closing**, not before (A-019). |
| 2026-08-28 (Central Texas program set up) | F-03, F-06, F-07, F-08, F-10, F-19 | planner: loop execution scllr summary line (`distribution` idempotent 7, skip 1, execute 2, defects 2); Factory store by field (`f10_county_runs` for 48055 run 1956886d and 48085 run 75ada535 `replay_identical=false`, both terminations `success`; `runs` rows at `started`: 309a07ab, 1bd6316f, c536c8c6; `publish_runs` e1b32af8 succeeded 06:17Z tier 1 61,695 of 77,799 over 10 pages, tier 2 61,695, `walk_id` null; `verify_walks` 6350cb8b pass 06:25Z, 5 parcels; `landing_cad_property` by `payload->>county_fips`: 48021 77,799, 48453 873,766, 48491 602,050, 48209 306,722, 48055 73,371, 48309 114,255); code read in the f10 and publish worktrees (`pipelineFromRows` stages `rs.aliases` cumulatively per chunk; `replayCountyCadChunks` rehydrates after pass one persisted; `requireStagingSibling` looks up the county from the unwritten production row and passes an unwalked sibling; `bakeEnv` falls back across targets); Neon API (staging branches br-super-cloud and br-little-term are children of cortex-prod `fancy-fire-06136146` branch br-crimson-feather, host ep-lucky-truth, both databases on one branch); `gh api compare` LDT b8983157...main ahead 6 | **Criterion 5 closed with a new defect, and it is by construction.** The CP2 loop ran ten counties as ruled: seven idempotent, Bastrop skipped under the situs-sentinel class, Bexar skipped as in-flight because a child run row 309a07ab from a 05:51Z loop sits at `started` with nothing running, and the two executed counties (Caldwell 48055 in 8m54s, Collin 48085 in 20m44s) both terminated success with replay non-identical. Cause read in code: every chunk stages the resolve store's whole cumulative alias list, and the cold replay rehydrates from a table that pass one just populated, so the two passes cannot match; the 48055 field diff (atomId only, same node and claim) is not yet explained and is assigned to the fix card as a failing test first. **Bastrop staging is one walk short of production**: the publish lane's run e1b32af8 succeeded on the new shape with the situs refusals honest (16,104 rows refused), the walk passed on gold and three smoke parcels, but with no area sweep, no link from the walk to the run, and a production guard that refuses always (county subquery on an unwritten row) and would pass an unwalked sibling once fixed. **Program under A-020/A-021, planner-run:** two registered subagent worktrees (`hauska-factory-ctx-replay` from main a70139a; `hauska-factory-ctx-publish` from the publish lane's pushed branch 9b27304) and two compiled dispatches (`_dispatches/2026-08-28_ctx-a_dispatch.md`, `_dispatches/2026-08-28_ctx-b_dispatch.md`; cards `_inbox/2026-08-28_ctx_a_replay_reaper_dispositions_WDLL.md`, `_inbox/2026-08-28_ctx_b_walk_sweep_production_publish_WDLL.md`), subagents produce diffs and tests only; the planner commits, merges, builds, executes. Production target secrets created in hauska-prod Secret Manager from the Neon API without printing: `PRODUCTION_NEONDB_URL` (host ep-lucky-truth, db neondb, 141 bytes, no trailing newline) and `PRODUCTION_HAUSKA_MCP_URL` (same host, db hauska_mcp, 145 bytes); access is project-level like the staging pair. All six Central Texas counties have CAD landing, so no acquisition is needed for the slice. Two stale staging branches (br-rapid-surf, br-lingering-glade) remain in cortex-prod from the first reset, leave-behind for the publish card. |
| 2026-08-28 (CTX cards A and B merged) | F-03, F-06, F-07, F-08, F-10, F-19 | planner: both diffs read in the worktrees; `npm test` run by the planner in each worktree (card B 159 tests, 157 pass, 2 live skips; card A 155, 153, 2; after merging main into A, 185, 183, 2); `gh pr view` checks conclusion `SUCCESS` twice each, base current (A rebased onto B's merge before its PR); `gh pr merge --merge` #22 (23c32d8) then #23 (f1da20a); run ledger `run_events` replay leg for 1956886d read in full; production facets route probed (`https://smartsite.cloud/api/spine/cortex/api/brokerage/v1/place/node/48021:34137/facets` HTTP 200, `source: baked-snapshot`, snapshotAt 2026-08-04, no `conformant-v1`; the staging site's same route carries `conformant-v1` and the publish run id); Secret Manager (two production secrets, parse-verified host and database); Cloud Build submitted from a clean `git archive` of main f1da20a (conformant build c4cb671e, publish build 139655d1) | **Card B (PR #22).** The walk now sweeps the gold parcel's street from the Factory landing (same situs street, city and zip; floor 10, default 12; ceiling declared as `truncated`), the publish run owns its walk (`walk_id` set inline, or by `verify-walk --publish-run`), the production guard reads the county from flags and refuses `STAGING_SIBLING_REQUIRED` / `UNWALKED` / `FAILED` / `NO_SWEEP` (the 6350cb8b shape is the fourth), and each target reads only its own store variables with `TARGET_ENV_MISSING` before any bake. One finding the planner would not have caught: the deployed publish job's flag reader matched only `--name value`, so the template's `--target=staging --county=48021` parsed as nothing and every run to date executed on defaults; the Bastrop runs were right by coincidence and a `--county=48453` execute on image 2c7c6479 would have published Bastrop. **Card A (PR #23).** Aliases are staged per chunk as the resolver's return, never the store's cumulative list (fixture (c): 240/270/300 staged before, 60/60/60 after); landing pages are a total order (`prop_id, id`); the cold replay hydrates only aliases known before the run's `knowledgeAt` so a non-deterministic mint is visible (fixture (b) could not fire under the card's literal wording, and the deviation is accepted); tier-1 binding takes the earliest-known node so published node ids do not move (V9); the reaper reconciles every `started` row by age or by parent, `orphaned` with a termination record; `execute-degraded` exists only per county on exactly three classes for the six and `assertRegisterScoped` refuses anything wider. **The 48055 divergence is settled and the card's statement of it was wrong:** the full ledger record shows prop_id 10092 with two landing rows (tax years 2025 and 2026, different owner and values), so the two passes compared different rows under a last-wins key; the planner's card had read a truncated log line as "claim equal". Card A's mechanism (ii) is the cause and the `ORDER BY prop_id, id` change is the fix. Migration 0004 (`f10_county_runs.degraded`) must run through `factory-conformant-migrate` before the first loop on the new image. Next: builds pin the jobs, migrate, the first scheduled reap reconciles the three orphans, staging publish 48021 with the inline sweep walk, production publish 48021 under A-021, then the loop for the six. |
| 2026-08-28 (new images executing: Caldwell proof, Bastrop bake, sweep refusal) | F-03, F-06, F-08, F-10, F-19 | planner: Cloud Build c4cb671e (conformant) and 139655d1 (publish) `SUCCESS`; jobs by field (conformant gen 19, loop gen 9 on `c08fc0b0`; publish gen 11, walk gen 12, reset gen 14 on `032c6b35`, the publish template carrying both store pairs); `factory-conformant-migrate-92qls` exit 0 and the `f10_county_runs.degraded` column present by catalog read; reap ticks 7qhkp (manual) and the scheduled zgqp5 at 07:40Z; Factory store by field: `runs` 309a07ab `killed`, 1bd6316f and c536c8c6 `orphaned` with termination records, zero rows at `started`; loop jb4l2 child run 8158c982 for 48055 (`replay_identical = true`, 146,742 atoms for 73,371 rows over 2 chunks, peak 638 MiB, exit `success`, no defect, F-10 row written); staging publish run f21b2e62 `succeeded` (tier 1 and tier 2 61,695 each) with its inline walk ac3b32da refused `SWEEP_TOO_SMALL` (`area PINE , BASTROP, TX 78602 yields 9 neighbours; floor is 12`); landing read under the corrected key (PINE 9, SPRING ST 25, JEFFERSON ST 67, CHESTNUT ST 120; union 221 with the anchors excluded); PRs #24 (timeout) and #25 (sweep follow-up) merged on `SUCCESS`, main 26e0c04; publish build 0cb443ce submitted; loop 65pvw running Hays then McLennan (Hays child ac68f4ec carrying `degraded: [TX-HAYS-LANDUSE, TX-SITUS-SENTINELS]`, 2 of 7 chunks at 08:11Z) | **The writer fix is proven on a real county.** Caldwell re-ran on the new image with the replay identical and exactly two atoms per landing row, where the same county staged 783,710 atoms and failed replay nine hours earlier. Hays is executing under the degraded disposition with both classes on its run scope, which is the manifest-visible state A-020 asked for. **Bastrop is baked on staging and waiting on its sweep walk.** The bake succeeded a second time on the new image (the flag fix confirmed by the run scope: county 48021, target staging, skipWalk false) and the walk refused honestly: the gold parcel's street holds nine neighbours and the street key still carried the `, BASTROP, TX 78602` suffix. The follow-up (PR #25) keys the bare street and sweeps the union of the gold and smoke parcels' streets (221 neighbours live), floor unchanged; the walk re-runs linked to run f21b2e62 once build 0cb443ce pins it, and production follows under A-021. **The county job timeout was wrong for the two big counties:** 3600 s on the template against a declared `max_duration_s` of 21600 and a measured 7 to 12 thousand rows a minute; Travis (873,766) and Williamson (602,050) would have been killed mid-write. PR #24 sets 21600 in the build config; the conformant rebuild waits for loop 65pvw to finish so the running loop's image pin is not disturbed, then Travis and Williamson run. |
| 2026-08-28 (Bastrop staging walk passed; Hays written; McLennan args split) | F-06, F-08, F-10 | planner: publish build 0cb443ce `SUCCESS`, walk job gen 13 on `f9577935`; walk 7182b3b4 (232 parcels, 230 pass, the broken fixture failing as designed, one `HTTP 504` on gold as the first request of a cold staging revision) then, after two 200 probes, walk 6d3d737d linked to publish run f21b2e62: verdict `pass`, 232 parcels, sweep 227 across PINE 9, JEFFERSON ST 67, CHESTNUT ST 126, SPRING ST 25, zero non-pass rows; production publish run db5b7abb opened 08:21:47Z with scope `target production, county 48021, skipWalk false` (guard passed, execution v7pmc running); loop 65pvw summary line (`workListSize 1`, executed 48209 only) and its execution spec (`args: ["f10-cad-loop","--apply","--counties=48209","48309"]`); `f10_county_runs` 48209 (306,722 atoms for 306,722 rows, replay identical, `degraded: [TX-HAYS-LANDUSE, TX-SITUS-SENTINELS]`, exit success) and `county_manifest` 48209 cell carrying both classes | **Bastrop's staging sibling is complete under A-021 and production is publishing.** The sweep walk passed on 232 parcels once the street key and the multi-anchor union landed; the only failure between the two walks was a gateway timeout on the very first request against a cold staging revision, so the walk needs a warm-up or a single retry on 5xx before it is trusted on a cold target (leave-behind, F-08). **Hays is written and honest**: every row, replay identical, the two accepted classes on the run scope, the F-10 row, and the manifest cell. **McLennan did not run, and the cause is the planner's argument form, the same class of defect card B found in the publish job the other way round:** `gcloud run jobs execute --args=` splits on commas, so `--counties=48209,48309` reached the loop as `--counties=48209` plus a stray token and the work list was one county. Cloud Run job args that carry a comma-joined list use the alternate delimiter form (`--args="^|^f10-cad-loop|--apply|--counties=48309,48453,48491"`). McLennan, Travis and Williamson run in one execution in that form after the conformant rebuild (21600 s) pins the county job. |
| 2026-08-28 (Bastrop production publish: bake succeeded, serve refused every parcel) | F-06, F-08, F-15, F-16 | planner: production publish run db5b7abb (`publish_runs` succeeded, tier 1 and tier 2 61,695 each, stamp `production:48021` with `stagingSiblingId f21b2e62`, walk 81ed4a91 linked); production walk 231 of 232 `HTTP 422`, the broken fixture failing as designed; live probes (`https://smartsite.cloud/.../48021:34137/facets` 422 `{error: ACCESS_NOT_DEFAULTED, errorClass: serve_refused, message: unknown discoverability}`, 48021:20500 422, a non-Bastrop id 404, the property-atoms route 200 on the atom chain; the staging site 200 on the same bake); LDT code read at b8983157, fe8e286d and main (`refusePayloadAtServe` calls `assertAccessPair` which calls the contract's `parseAccessPair`; `assertF06BakeAccessPair` from #515 is called only by the bake and admits the legacy pair); contract 1.30.0 `dist/access/access-pair.js` sets (`catalog-listed | unlisted | hidden`, `anyone-free | anyone-paid | named-parties | owner-only | platform-only`); 19_the_instrument_contract.md lines 253 and 254 name the same sets; Factory `conformant.mjs` lines 153, 173 and 509 stamp `{discoverability: public, entitlement: anonymous}`; PE `baked-facets.ts` calls the cortex proxy facets route from the browser; canary deploy (LDT workflow runs 33156428686 deploy-canary at main ab0cac20 and 33156552280 shift-traffic; the shift log shows 100 percent on cortex-api-00625-jub) | **Bastrop is baked on production and refused at serve, and the cause is at the source of the pipeline, not in the publish.** The conformant writer stamps every atom with a pre-contract access pair (`public / anonymous`), the bake copies it into the snapshot, and the serve guard correctly refuses it against contract 1.30.0 and the instrument contract, which name `catalog-listed / anyone-free` as the public pair. Staging served the identical bake because its cortex-api revision (00603-biq) is not on main; production (b8983157, and after the shift ab0cac20) applies the guard. #515 added a bake-side acceptance that admitted the bad value instead of translating it, and nothing at serve calls it: a merged, correct, dormant control. **State at 08:50Z:** every Bastrop facet on the production cortex proxy route returns 422 (the PE browser calls it); the atom-chain route still serves. The canary deploy and shift changed nothing on this axis (same guard on both revisions) and were still right: production now runs main. **Fix in flight, planner-authored in LDT (PR opened 08:58Z):** the serve guard translates the declared legacy pair to the canonical pair and marks the served payload `accessNormalizedFrom: public/anonymous`; the tier 1 bake writes the canonical pair with the same marker; a pair that is neither canonical nor in the legacy table still refuses. Then a canary, a smoke on the canary facets route (which needs no bearer, which answers the open question: the route is not gated), a shift, and the production walk re-linked to db5b7abb. The writer fix follows in hauska-factory after the running loop; the atoms already written for the six carry the legacy pair and are re-stamped by a later card (A-023). |
| 2026-08-28 (BASTROP LIVE on the new shape in production) | F-06, F-07, F-08, F-10 | planner: LDT PR #519 merged on `SUCCESS` (main 2c592b08); build-and-push run 33157808350; deploy-canary run 33158078856 (`cortex-api-00627-xay`); canary probes on the tag URL (gold `HTTP 200`, `access: {catalog-listed, anyone-free}`, `accessNormalizedFrom: public/anonymous`, `conformant-v1` twice, publish run id db5b7abb present; healthz and county-ledger 200); shift-traffic run 33158295231 (log: `100% cortex-api-00627-xay`); production probes after the shift (48021:34137, 48021:20500, 48021:34785 all 200 with the same fields); `factory-verify-walk-78qdc` linked to db5b7abb: `publish_runs.walk_id = de10d859`, verdict `pass`, 232 parcels, sweep 227, walk_results 231 pass on BP-MEANING-01 plus the broken fixture on BP-VERIFY-01, zero non-pass rows; loop gmfjd: 48309 written (114,255 atoms, replay identical, no defect), 48453 at chunk 5 of 18 at 08:59Z | **First county on the new shape in production, under A-021, at 09:12Z.** The production publish stands as run db5b7abb (staging sibling f21b2e62, stamp `production:48021` sourceVintage conformant-v1) and its production walk de10d859 passed on the full sweep after the serve fix landed; the 422 incident lasted from 08:40Z to 09:11Z on the cortex proxy route only. The served payload carries the declared translation marker until the store is re-stamped (A-023). Both cortex-api tags (staging on 00603-biq, production on 00627-xay) still run different LDT commits; the parity check is the F-07 backlog item. Next: the publish image pins LDT 2c592b08 so new bakes write the canonical pair; then Caldwell, Hays and McLennan publish staging-first with a gold chosen from the landing (a parcel on a long residential street) and the same sweep rule; Travis and Williamson after their writes. |
| 2026-08-28 (CTX card C handed back; three staging publishes running) | F-06, F-08, F-10, F-15, F-16 | planner: card C diff read in the worktree (boundary, staging INSERT, and merge projection all refuse a legacy pair with `ACCESS_NOT_CANONICAL`; one producer constant; `LEGACY_ACCESS_PAIRS` mirrored one for one from LDT #519; `factory-restamp-access` in `cloudbuild.conformant.yaml`, 7200 s, both secrets); `npm test` run by the planner: 215 tests, 213 pass, 2 live skips; the lane's violation: reverting the cad stamp fails two tests; publish image gen 13 on `80bf3507` with LDT 2c592b08 bundled (build 1dc0fefb); Neon API branch br-round-sea-api84wvk (host ep-bold-sky) cut from production for hauska_mcp staging and `STAGING_HAUSKA_MCP_URL` rotated (parse-verified); staging publish executions czlpc (48055, gold 48055:20478), csvv8 (48309, gold 48309:176914), wqnzb (48209, gold 48209:135570) running since 09:23Z with run rows e3901628, 9d52b392, 41bcc81a open and no refusal; loop gmfjd: Travis 48453 at chunk 10 of 18 at 09:22Z | **The source of the access defect is fixed and merged-pending, and the store correction is a job, not a script.** The writer can no longer produce the legacy pair (three refusal points, verified by reverting one and watching two tests fail), and `restamp-access` corrects stored rows per county under the county lease with a run row, batch events, a read-back-zero close, and a dry run that cannot succeed. The conformant image is rebuilt only after the running loop terminates so the pinned digest under the loop is not moved; then the re-stamp runs for the eight written counties (48021, 48055, 48209, 48029, 48085, 48309, 48453, 48491), and when every one reads back zero the LDT translation is retired by the test the lane handed back. Card C corrections to the record: `conformant.mjs` line 509 is the rate-probe synthetic atoms, not flood (flood already carried the canonical form through `mapAccessPolicy`); `publish-guards.assertAccessPair` still admits the legacy pair as an F-06 reader and flips when the re-stamp closes. **Staging publishes for three counties are baking concurrently** on the new publish image (canonical pair at bake) against the fresh hauska_mcp staging branch; each carries its gold and will walk its gold's street; production follows each passed walk under A-021. |
| 2026-08-28 (Caldwell staging passed; production publishing; bake writes both tax years) | F-06, F-08 | planner: execution czlpc exit 0 at 09:35:07Z; its log close line (`publishRunId e3901628`, `walkId d927376c`, `walkVerdict pass`, `tier1Summary {conformantCadRows 48649, pages 7, written 73159}`, stamp `staging:48055` sourceVintage conformant-v1); staging site probes at 09:41Z for the three golds (48055:20478, 48309:176914, 48209:135570 all `HTTP 200`, `conformant-v1`, `access {catalog-listed, anyone-free}`, `accessNormalizedFrom` present, each carrying its own publish run id); the Factory store answered none of the planner's reads for twelve minutes under the three bakes and the Travis writer (three timeouts), so the evidence above is from Cloud Logging and the live site; production publish for 48055 executed at 09:42Z under A-021 (gold 48055:20478, OPERATOR_PUBLISH_GO and PRODUCTION_SITE_URL as execution overrides) | **Caldwell's staging sibling is complete and its production publish is running.** The new publish image bakes the canonical pair and still declares the translation, which is right: the atoms it reads carry the legacy pair until the re-stamp. **A finding the counts expose:** tier 1 wrote 73,159 snapshots for 48,649 conformant parcels because the CAD roll carries two tax years per prop_id and the writer emits one atom per landing row, so the bake writes one snapshot per atom and the serve picks one by order rather than by a stated rule. Which tax year a parcel serves is therefore unmeasured. Not blocking the slice (both rows are the record, both carry provenance) and not silent any more: the bake must select one atom per parcel by the latest tax year and record the choice, and the writer or the bake must say which (F-06 backlog, next publish card). McLennan and Hays are still baking. |
| 2026-08-28 (McLennan staging passed; production publishing) | F-06, F-08 | planner: execution csvv8 exit 0 at 09:43:32Z; its log close line (`publishRunId 9d52b392`, stamp `staging:48309` sourceVintage conformant-v1, `tier1Summary {conformantCadRows 114255, pages 15, written 113090}`, walk id aca06366 with the verdict read from the same line); production publish for 48309 executed at 09:45Z under A-021 (gold 48309:176914, execution overrides as for Bastrop and Caldwell) | **McLennan's staging sibling is complete and its production publish is running**, the third county through the identical job today. Tier 1 wrote 113,090 of 114,255 conformant rows (1,165 refused at bake, the honest situs and access refusals); McLennan's roll carries one row per prop_id, so the two-tax-year finding from Caldwell does not apply here and the count is the parcel count. |
| 2026-08-28 (CALDWELL LIVE on the new shape in production) | F-06, F-07, F-08 | planner: execution kdg7f (`--target=production --county=48055 --gold=48055:20478`, OPERATOR_PUBLISH_GO and PRODUCTION_SITE_URL as execution overrides) completed 09:56:42Z with succeededCount 1, which the job reaches only after the guard found the staging sibling (e3901628, walked, pass, sweep at least ten) and its own inline production walk passed; production probes at 09:57Z: gold 48055:20478 and neighbour 48055:18354 both `HTTP 200`, `conformant-v1`, `access {catalog-listed, anyone-free}`, `publishRunId 5ede6f17` | **Second county live under A-021, 44 minutes after Bastrop.** The identical job ran unchanged for a county the card had never named, with a data-chosen gold and the same sweep rule; the production guard admitted it on the staging sibling's linked walk. The Factory store's own rows for run 5ede6f17 are read back later, when the writer load lifts (memory note). McLennan production and Hays staging are running; Travis is still writing. |
| 2026-08-28 (McLENNAN LIVE on the new shape in production) | F-06, F-07, F-08 | planner: execution 2kx94 (`--target=production --county=48309 --gold=48309:176914`, execution overrides as before) completed 10:04:58Z with succeededCount 1 (guard on staging sibling 9d52b392 plus its own inline production walk); production probes at 10:05Z: gold 48309:176914 and neighbour 48309:181856 both `HTTP 200`, `conformant-v1`, `access {catalog-listed, anyone-free}`, `publishRunId 82c838de` | **Third county live under A-021.** Bastrop 09:12Z, Caldwell 09:56Z, McLennan 10:05Z, each through the identical job with its own gold and sweep, each admitted by the guard on a linked staging walk. Remaining: Hays (staging bake running since 09:23Z), Travis (writer running since 08:43Z), Williamson (queued behind Travis in loop gmfjd). |
| 2026-08-28 (Hays staging passed; the reaper terminated it early by start-time match) | F-03, F-06, F-08 | planner: execution wqnzb exit 1 at 10:12:34Z with the only log line `23505`; Factory store by id (`runs` 41bcc81a `failed 23505`; `publish_runs` 41bcc81a `succeeded`, tier 1 22 pages, 304,332 written for 173,050 conformant rows, `walk_id ecd466a7`; `verify_walks` ecd466a7 verdict `pass`, 52 parcels, sweep 50, zero non-pass; `freshness` `staging:48209` stamped 10:12:08Z; `termination_records` 41bcc81a `crashed` recorded 09:50:25Z, e3901628 and 9d52b392 `success` at their own close times; no reap run names 41bcc81a in `counts`); `reaper.mjs` on main (90 s start-time fallback with job affinity when a run's scope carries no execution name); production publish for 48209 executed at 10:14Z under A-021 | **Hays's staging sibling is complete and valid, and the ledger about its run is wrong twice.** The three staging executions started within two seconds; the reaper's start-time fallback attached a finished sibling to the still-running Hays run and terminated it `crashed` at 09:50Z; when Hays's own job closed at 10:12Z the duplicate termination raised 23505 and the process exited 1 after a succeeded publish and a passed walk. The second mechanism, a freshness stamp conflict, is rejected: `staging:48209` stamped cleanly at 10:12:08Z. This is the over-broad control ENFORCEMENT names: a heuristic wide enough to reach a sibling. Card D (`_inbox/2026-08-28_ctx_d_reaper_execution_identity_WDLL.md`) replaces it with identity: every run names its own Cloud Run execution at start, the reaper matches by that name, a duplicate termination is a named refusal that cannot fail a succeeded publish, and a reconcile verb corrects 41bcc81a from its own close evidence. The run's `failed 23505` stays as written until that verb runs; the production guard reads `publish_runs` and the walk, so Hays production proceeds. Hays tier 1 wrote 304,332 snapshots for 173,050 parcels, the two-tax-year finding again (F-06 backlog). |
| 2026-08-28 (CTX card D handed back: execution identity) | F-03, F-06, F-10 | planner: card D diff read in the worktree (`matchExecution` by name only; `NEVER_AN_EXECUTION_HOSTS`; `EXECUTION_NAME_SINCE` set by the planner to 2026-08-28T11:00Z, the merge; `LEGACY_FALLBACK_REMOVE_BY` 2026-09-01 enforced by a test; `writeTermination` `ON CONFLICT (run_id) DO NOTHING RETURNING` with `ALREADY_TERMINATED`; every job starts through `startRun(..., {env})`; loop children named from the API response); `npm test` run by the planner twice (245 tests, 243 pass, 2 live skips, before and after setting SINCE); the lane's violation runs: both card fixtures fail against main's reaper, stripping the publish job's `{env}` fails three fixtures, moving the removal date fails the date test; Travis child 4b24cef2: 18 chunks, 873,766 rows, 1,747,532 atoms (two per row), pipeline 34 min, replay leg running at 10:37Z; Hays production run 11f711b4 at page 10 of 22 at 10:37Z | **Identity replaces the heuristic.** A run now names its own execution when it opens, the reaper reconciles only that execution, an unnamed row after the merge is never time-matched (it orphans by parent or age), and a duplicate termination is a named refusal that a succeeded publish records rather than dies on. The Hays row 41bcc81a is corrected by `reap reconcile-termination --run=<uuid> --apply` after the rebuild, dry run first, append-only with both records named; the `termination_records` row stays `crashed` as evidence. Nine older jobs still close through a direct `writeTermination` (now a refusal on duplicate, same class, backlog). The rebuild carries cards C and D together once loop gmfjd terminates. |
| 2026-08-28 (HAYS LIVE on the new shape in production) | F-06, F-07, F-08 | planner: execution x6b65 (`--target=production --county=48209 --gold=48209:135570`, execution overrides as before) completed 10:58:54Z with succeededCount 1 (guard on staging sibling 41bcc81a, whose `publish_runs` row succeeded and whose walk ecd466a7 passed, then its own inline production walk); Factory store by id: production run 11f711b4 `success`, 22 bake pages; production probes at 10:59Z: gold 48209:135570 and 48209:103403 (Indian Paintbrush Dr, a different street) both `HTTP 200`, `conformant-v1`, `access {catalog-listed, anyone-free}`, `publishRunId 11f711b4` | **Fourth county live under A-021: Bastrop 09:12Z, Caldwell 09:56Z, McLennan 10:05Z, Hays 10:59Z.** The Hays production run closed cleanly on its own (no sibling started near it, so the old reaper had nothing to mis-attach); its staging run 41bcc81a still reads `failed 23505` until the card D reconcile verb runs on the rebuilt image. Hays carries `degraded: [TX-HAYS-LANDUSE, TX-SITUS-SENTINELS]` on its manifest cell; what a user sees is the CAD roll on the new shape with those two classes declared. Remaining: Travis (replay pass running since about 10:30Z), Williamson (queued in loop gmfjd), then the fresh staging copy, their publishes, the rebuild with cards C and D, the eight re-stamps, the reconcile, and the LDT retirement. |
| 2026-08-28 (Travis written on the new shape; publish timeout raised) | F-06, F-10, F-19 | planner: execution k52fb log close (`runId 4b24cef2`, county 48453, `rate {rows 1747532, wallMs 8834566, atomsPerSec 197.8, floor 150 met, prediction 300 failed}`, `perLeg {landingMs 81531, pipeline1Ms 2066184, replayMs 2312561}`, replay leg `identical: true`, exit 0 at 11:10:40Z); pg_stat_activity on both stores idle during the replay pass (the pass is compute in the container); the three earlier staging publishes: Bastrop 10 pages in 17 min, Hays 22 pages in about 37 min at roughly 100 s per 8,000-row page | **Travis, the largest county in the slice, is written with the replay identical**: 18 chunks, 873,766 rows, two atoms per row, 147 minutes wall of which 38 in the cold replay, at 198 atoms a second against the 150 floor (A-009 rate-floor accepted; the 300 prediction failed again, a store-bound rate). Williamson executes next in loop gmfjd. **The publish job's timeout is the next wall**: at the measured per-page cost Travis bakes for about three hours per target and Williamson about two, against a 3600 s template; the planner raised it to 21600 s in `cloudbuild.publish.yaml` (build-config change, A-019) and is rebuilding the publish image; the per-page cost itself (a bake CLI process and its connections per page) is the F-06 backlog item that would make the six-county publish an hour, not a day. A fresh hauska_mcp staging branch is cut from production so the Travis staging bake can read its atoms; Williamson gets another cut when its write lands. |
| 2026-08-28 (ALL SIX Central Texas counties written on the new shape) | F-10, F-19 | planner: loop gmfjd summary line at 11:42:42Z (`distribution {execute 3, defect 0, idempotent 0, skip 0, degraded 1}`, executed 48309 114,255 atoms, 48453 873,766 atoms `degraded [TX-TRAVIS-JOIN]`, 48491 602,050 atoms; defects `[]`), execution completed 11:42:44Z succeededCount 1; Williamson child 46xnl 11:11:30Z to 11:40:48Z exit 0; `f10_county_runs` 48491 (602,050 atoms for 602,050 landing rows, replay identical, no defect, exit success) and 48453 (873,766, replay identical, degraded TX-TRAVIS-JOIN, exit success); `county_manifest` cells for both `satisfied-present`, Travis carrying its class | **The write half of A-020 is done: six of six counties on the new shape with the replay identical on every one.** Caldwell 07:54Z, Hays 08:18Z, McLennan 08:42Z, Travis 11:10Z, Williamson 11:40Z today, Bastrop yesterday; Travis and Hays carry their accepted degraded classes on the manifest; Williamson's roll has one row per prop_id, so its atom count is its parcel count. Williamson wrote at about 20,000 rows a minute with the store to itself, against Travis's 8,300 under three concurrent bakes, which measures the store contention rather than the writer. Serving: four of six live in production (Bastrop, Caldwell, McLennan, Hays); Travis is baking on staging (execution pg922, since 11:21Z); Williamson's staging publish starts on a fresh staging copy cut after its write. The conformant image is rebuilding with cards C and D now that no county writer is running; the eight-county re-stamp and the Hays row correction follow on it. |
| 2026-08-28 (conformant image rebuilt with cards C and D; Hays staging row corrected) | F-03, F-06, F-10, F-15 | planner: Cloud Build 48a023cf `SUCCESS` 11:47Z from main (cards C and D); jobs by field (`factory-conformant` gen 21, `factory-f10-cad-loop` gen 11, `factory-restamp-access` gen 1 with 7200 s, all on `614d141d`); `reap reconcile-termination --run=41bcc81a-d574-45e7-b695-68c8b18d3fd7` dry run (execution b6fqh, correction `apply: false` with the evidence: `publish_runs` succeeded 10:12:08Z, walk ecd466a7 pass, both after the reaper's 09:50:25Z termination) then `--apply` (execution wczkw); Factory store after: `runs` 41bcc81a `success`, refuse_code null; `run_events` `termination-corrected` `{from: {failed, 23505}, to: {success, null}, basis: card D item 4, evidence: publish run and walk}`; `termination_records` row left `crashed` as evidence | **The ledger about the Hays staging run now says what happened, with the correction on the record rather than over it.** The new image carries the writer that cannot stamp a legacy pair, the re-stamp job, and the reaper that matches by execution name; the first re-stamp (Bastrop, dry run then apply) is executing as the proof before the other seven. Travis (pg922) and Williamson (d5rgm) are baking on staging. |
| 2026-08-28 (re-stamp proof: Bastrop legacy pairs to zero) | F-06, F-15, F-16 | planner: `factory-restamp-access` dry run vbhck for 48021 (counting rule `atoms WHERE jurisdiction_tenant = county AND body.shape = conformant-v1, every entity type; classes measured by FILTER, never subtracted`; legacy `public/anonymous`: cad-parcel-roll 77,799, identity.alias 77,799, flood-hazard-fact 62,254, flood-hazard-selector 4; canonical 0; outOfTable 0; columnDisagrees 0; exit 1 `DRY_RUN_REFUSED` by design) then apply xkblf (11:51Z to 11:59Z, exit 0; close `before.legacyTotal 217856` to `after {legacyTotal 0, canonicalPublic 217856, columnDisagrees 0, outOfTable 0, total 217856}`) | **The store correction works and is counted from the store on both sides.** Bastrop's 217,856 conformant atoms carry the contract's pair with `accessRestampedFrom` recorded, the ADR-017 column agrees, and nothing outside the declared table was touched. Flood atoms were in the legacy set after all (62,254 facts and 4 selectors), contrary to card C's reading that `mapAccessPolicy` produced the canonical form; the job matched them by the pair, as designed, and the record stands corrected here. The seven remaining counties run dry first (counts), then apply in two waves so the atoms store, which the MCP server and the atom-chain route read, carries at most three update streams at a time. |
| 2026-08-28 (re-stamp dry runs for seven counties; apply wave 1) | F-06, F-15, F-16 | planner: seven `factory-restamp-access` dry-run executions (2892m, 45nm7, 7m52q, 6sj6t, hlllt, nvlp6, 9qxw4), each `DRY_RUN_REFUSED` by design with a `restamp-dry-run` event in the Factory store: 48055 146,742 (cad 73,371 plus alias 73,371), 48309 228,510, 48209 613,444, 48085 774,668, 48029 1,406,516, 48491 and 48453 the same two-type shape; every county `legacyTotal = total`, `outOfTable 0`, `columnDisagrees 0`, `identityHolds true`; apply executions launched 12:10Z for 48453, 48029 and 48085 | **Every conformant atom outside Bastrop carries exactly the one declared legacy pair and nothing else, so the re-stamp is a total, mechanical translation with the count as its own proof.** Flood atoms exist only for Bastrop (the F-18 demotion ran there alone), which is why the other counties show two entity types. Wave 1 (the three largest) runs three update streams against the atoms store, which the MCP server and the atom-chain route also read; wave 2 (Hays, Williamson, Caldwell, McLennan) follows. Cloud Logging lagged the job's own lines by more than ten minutes twice today; the Factory store's run events were the record read. |
| 2026-08-28 (re-stamp five of eight closed; retirement PR prepared; Bastrop re-bake) | F-06, F-15, F-16 | planner: `restamp-close` events in the Factory store (48021 217,856; 48085 774,668 in 14 min; 48453 1,747,532 in 20 min; 48029 1,406,516 in 22 min; 48209 613,444 in 8 min; each `after.legacyTotal 0`, `canonicalPublic = stamped = totalMatched = totalWritten`, `columnDisagrees 0`); applies 64m44 (48491), dgn24 (48055) and McLennan (12:34Z) running; Bastrop production re-publish executed 12:34Z on the canonical bake (staging sibling f21b2e62 stands, A-021); LDT PR #520 opened from `fix/access-pair-retire-legacy-table` at main 2c592b08 (`LEGACY_ACCESS_PAIRS` empty, the translation test replaced by the retirement test asserting the empty table and the refusal, 8 serve guard tests green) and held unmerged | **The store correction is five counties in with the count as its own proof each time, and the retirement is staged behind two gates**: the eighth close reading zero, and Bastrop's production snapshots re-baked with the canonical pair (the 08:21Z bake was the only one that still leaned on the serve translation; the later counties baked canonical with the marker). When both hold, #520 merges, the canary is smoked on gold expecting no `accessNormalizedFrom`, and the shift retires the translation with a test that fails if a legacy pair ever reaches serve again. Snapshots baked with the marker (Caldwell, McLennan, Hays, and the staging rows) keep it as provenance of the translation until their next bake; served payloads carry the canonical pair either way. |
| 2026-08-28 (re-stamp eight of eight closed: 6,339,368 atoms canonical, zero legacy) | F-06, F-15, F-16 | planner: `restamp-close` events for all eight counties in the Factory store (48021 217,856; 48029 1,406,516; 48055 146,742; 48085 774,668; 48209 613,444; 48309 228,510; 48453 1,747,532; 48491 1,204,100), each `after.legacyTotal 0`, `outOfTable 0`, `columnDisagrees 0`, `canonicalPublic = stamped = totalWritten`; `runs` with phase `restamp-access` and a status other than success or dry-run: none; the last three applies (64m44 Williamson 12:43Z, dgn24 Caldwell 12:35Z, jngd7 McLennan 12:36Z) exit 0 | **A-023 item 4 is done in the store: 6,339,368 conformant atoms across the eight written counties carry `catalog-listed / anyone-free` with `accessRestampedFrom` recorded, and no atom anywhere in them carries the legacy pair.** The writer cannot produce one (card C), the boundary and the merge refuse one, and the count on both sides of every county is the proof. One gate remains before the serve translation is retired: the Bastrop production re-bake (running since 12:34Z) must pass its walk, because Bastrop's 08:21Z production snapshots are the only served rows that still carry the legacy pair. Then LDT #520 merges, canary, smoke on gold expecting no `accessNormalizedFrom`, shift. |
| 2026-08-28 (Bastrop re-baked on the canonical pair; retirement merging) | F-06, F-08, F-15 | planner: execution 8xcx2 (`--target=production --county=48021`, staging sibling f21b2e62 standing, A-021) completed 12:47:20Z succeededCount 1 (guard, bakes, inline production walk); production probes at 12:47Z: 48021:34137, 48021:34785, 48021:20500 all `HTTP 200`, `conformant-v1`, `access {catalog-listed, anyone-free}`, `accessNormalizedFrom` absent, `publishRunId 8e7dc598`; LDT PR #520 CI green (typecheck, tests, the L5 and SS greps) | **Nothing served for Bastrop leans on the translation any more**: the canonical bake read the re-stamped atoms and wrote the contract's pair directly, and the production walk passed on it. With the eighth close at zero, the retirement PR merges: `LEGACY_ACCESS_PAIRS` empties and a legacy pair reaching serve refuses again, enforced by the test the card handed back. The canary is smoked on gold expecting the canonical pair and no marker before the shift. |
| 2026-08-28 (serve translation RETIRED in production; A-023 closed end to end) | F-06, F-08, F-15, F-16 | planner: LDT PR #520 merged on green (main after merge carries the empty `LEGACY_ACCESS_PAIRS` and the retirement test); build-and-push run 33172602443 `success` 12:52Z; deploy-canary run 33172884492 `success` 12:54Z (`cortex-api-00629-riz`); canary probes on the tag URL (48021:34137 200 canonical no marker; 48055:20478 and 48209:135570 200 canonical with the stored bake-time marker; healthz and county-ledger 200); shift-traffic run 33173100691 `success` 12:55Z (log `100% cortex-api-00629-riz`); production probes after the shift: 48021:34137 (no marker), 48055:20478, 48309:176914, 48209:135570 all `HTTP 200`, `conformant-v1`, `access {catalog-listed, anyone-free}` | **The declared translation lived four hours and forty-four minutes (09:11Z to 12:55Z) and is gone.** Production cortex-api refuses a legacy access pair again, enforced by a test that asserts the empty table and the refusal; the six counties serve the contract's pair from re-stamped atoms and canonical bakes. The bake-time marker remains inside the stored snapshots of Caldwell, McLennan and Hays (and the staging rows) as provenance of the translation that produced them; it is a stored field, not a serve-time act, and clears at their next bake (F-06 backlog, with the two-tax-year selection). Open on this thread: the LDT `assertAccessPair` reader in Factory `publish-guards.mjs` still admits the legacy pair as a reader (card C finding) and flips now that every county reads zero; the Factory and LDT legacy tables are both empty and their mirror test stands. |
| 2026-08-28 (Factory-side retirement handed back; a starved guard named) | F-06, F-08, F-15, F-16 | planner: card C follow-up diff (six files: `LEGACY_ACCESS_PAIRS` empty with the retirement comment; the re-stamp job takes an injected table for its tests and records `legacyTableInjected` on the run; `accessPolicyProjection` and `publish-guards.assertAccessPair` accept only the contract's pair); the lane's violations (table entry put back: 9 tests fail; guard reverted: 2 fail); `npm test` 250 tests, 248 pass, 2 live skips; source scans for `"public"`, `public/anonymous`, `mapAccessPolicy(` over `src` empty | **Both legacy tables are empty and the mirror test holds on each side.** The lane also named a starved guard: `publish-guards.assertAccessPair` has no caller, because the walk grades a served body by a string search for `conformant-v1` (`verify-walk.mjs` lines 86 to 90) and never reads `access`. That is exactly the class ENFORCEMENT warns about, an exported detector nothing calls in a gating position, and it is why the 08:40Z production 422 was found by the walk's HTTP status rather than by the guard. Routed as the next F-08 item: the walk grades every served body's `access` with the contract parser after the served shape is confirmed against a live payload. |
| 2026-08-28 (CUSTOMER-FACING REGRESSION: the conformant bake projects base facets only) | F-06, F-08 | operator screenshot 13:40Z (production Smart Site, 1005 PINE ST, 48021:34729: `This property has not resolved a fact sheet yet`, baked 12:38:50Z, source baked snapshot; zoning `carries no zoning stamp`, setbacks `no envelope was derived`, flood `not verified here`, land use `carries no land-use classification`); planner probes of both live routes for that parcel (the atom-chain route, old shape: `facets.zoning.district`, `facets.zoning.jurisdictionKey`, `facets.envelope.*` with setbacks, buildable area, height, `facets.baseFacts.*`, `facetCoverage.{baseFacts,landUse,acreage,zoning,envelope,structural}`; the cortex bake route, new shape: `facets.facets.base.{apn,parcelNodeId,situsAddress}` only, `facets.envelope = null`, `facetCoverage.{tier1,envelope,structural}`, `tier2.flood = null` with a `floodDisposition`; the fact atoms the route loads itself, `floodHazardFact`, `landUseFact`, `specialDistrictFact`, `pipelineFact`, present on both); Williamson staging execution d5rgm exit 0 at 13:36Z | **The cutover shipped a thinner brief, and the walk could not see it.** The conformant tier 1 bake projects the base facets and nothing else; the old bake joined the zoning stamp and derived the envelope, and the brief renders those from `facets.zoning` and `facets.envelope`, so every parcel published today shows `not verified here` where yesterday it showed a district and setbacks. The walk graded provenance and status, never content, so four counties passed to production on it. This is the defect class the walk card was written against and did not catch: CODE-DONE is not CUSTOMER-DONE. The UI is not the gap; it renders honestly. **Ruling A-025:** Travis and Williamson production publishes are HELD (their staging siblings stand) until the bake projects the full facet set and the walk grades content against the old bake for the same parcel; the four already published are corrected forward by re-bake, not rolled back. Card E carries it. |
| 2026-08-28 (Travis walk: the area key excluded three quarters of the county; fixed) | F-08 | planner: Travis staging run 8501ca22 (execution pg922) `publish_runs` succeeded (63 pages, tier 1 873,766 for 500,307 conformant parcels, tier 2 500,307) with its inline walk refused `SWEEP_TOO_SMALL` (`union of 1 anchor street areas yields 6 neighbours; floor is 12 (SHOALWOOD AVE 6)`); landing read through the walk's own `LANDING_SITUS_SQL`: 873,766 rows, `situs_city` null on 656,448, rows on the gold street in three forms (`4200 SHOALWOOD AVE` city null, `4200 SHOALWOOD AVE , TX 78756` city null, `4202 SHOALWOOD AVE A`), duplicate prop_ids across tax years; card B follow-up 2 handback (two files; the Travis-shaped fixture fails on main with the live `6` and yields 23 under the new key; 255 tests, 253 pass, 2 live skips; a NUL-byte incident caught by `git ls-files --eol` and fixed before handback) | **The walk's area key treated a null city as a different city, which on a roll that leaves city null for three quarters of its parcels excluded most of the county.** The key is now street plus zip, with city a tiebreaker only when both sides carry one; a bare trailing unit letter after a recognised suffix drops; duplicate prop_ids collapse; the zip inside the situs is the fallback and the body says which source it used. Bastrop's fixtures are unchanged and green. After the publish image rebuild, the Travis staging walk re-runs standalone linked to run 8501ca22 (the bakes stand); Travis production stays held under A-025 regardless, until the bake projection lands. |
| 2026-08-28 (Travis and Williamson staging siblings complete; production held under A-025) | F-06, F-08 | planner: factory PR (null-city key) merged on `SUCCESS`, main dc16b5f; publish build 93264dbd `SUCCESS` 13:59Z, walk job gen 16 on `b94b5b12`; `factory-verify-walk-vhm47` (`--target=staging --county=48453 --gold=48453:493738 --publish-run=8501ca22`) succeededCount 1; Factory store: `publish_runs` 8501ca22 succeeded with `walk_id f722e615`; walk verdict `pass`, 185 parcels, sweep 183 on SHOALWOOD AVE 78756 (`cityTiebreak applied`, `nullCityAdmitted 64`, `zipSource field`), zero non-pass; Williamson staging execution d5rgm exit 0 at 13:36Z (its inline walk passed on DAVIS ST, Taylor) | **All six Central Texas counties now have a passed staging sibling on the new shape**: Bastrop, Caldwell, McLennan, Hays (already in production), Travis (walk 183 swept after the key fix; the same street that read six under the old key), Williamson. Travis and Williamson production stay held under A-025 until the bake projects the full facet set, and the four in production are re-baked on that bake. The Travis street is a clean instance of the rule change: 183 neighbours of which 64 carry no city in the roll, all on one zip. |
| 2026-08-28 (CTX card E handed back: the fix is the bake; the reader is unchanged) | F-06, F-08 | planner: card E handback (LDT: two modified, five new files; the old CLI's derivation and txgio join extracted into `lib/nodeFacetTier1Assemble.ts` and `lib/nodeFacetTier1ParcelJoin.ts` and reused; `lib/nodeFacetBakeTier1Conformant.ts` builds the payload from the claim by name, never `ownerName`, with `REQUIRED_TIER1_FACET_PATHS` (27) and the divergence instrument; `facetSchemaVersion node-facets-tier1-conformant-v1`; 98 tests across three files, `tsc --build` and the api-server typecheck exit 0, the Factory bake bundle builds; `nodeFacetTier1ShapeDiffCli.ts --parcels=... --reference=48021:8704664` against production: every probed parcel `requiredPresent 0/27`, verdict fail; Property Explorer key table in CP2 with an empty diff); CP1 on the live rows: `place_layer_snapshots` has a unique index on `(adapter_key, place_key)` and the conformant CLI upserted in place, so the old-shape rows of every published parcel were overwritten (the old-shape reference had to come from un-rebaked Bastrop parcels via the coordinate index); Travis and Williamson golds have no production row; Hays 48209 has no txgio parcel row, so its zoning join is gate-blocked and stated as unmeasured | **The operator's question is answered with evidence: no UI change.** The Property Explorer reads the same keys it always read and rendered their absence honestly; the conformant bake had stopped writing them. The fix is the bake, now projecting the old structure with explicit absences and a test that fails on any missing leaf, plus a live instrument that reads the gap from the store. **A-025(4) is corrected by this measurement:** the old-shape snapshots were not left in place for the published parcels; the upsert replaced them, so the only way forward for the four live counties is the re-bake, which A-025(3) already orders. Sequence: LDT PR merges, `_LDT_SHA` pins to it, publish image rebuilds, the walk gains a content grade (card B follow-up 3), staging re-bakes for all six pass the shape diff, then production for all six under A-021. |
| 2026-08-28 (the walk grades facet content; LDT #522 and the walk PR merging) | F-06, F-08 | planner: card B follow-up 3 handback (two files; `REQUIRED_TIER1_FACET_PATHS` mirrored from LDT with a divergence test that reads the LDT source where the checkout exists and skips with a named reason where it does not; `gradeParcelResponse` grades BP-CONTENT-01 after BP-CONFORMANT-01: every required path present, an explicit absence counts, an omission does not, `facetSchemaVersion` exact; the 08-28 production shape fixture fails naming 25 of 27 missing; 259 tests, 257 pass, 2 live skips; NUL-byte guard 0); planner `npm test` in the worktree agrees; LDT PR #522 (card E) with eight of nine checks green and the Test check pending at 14:24Z, its chain set to merge on green; the Factory walk PR in its own chain | **The walk can now see what the operator saw.** A served body that carries only the base facets fails the walk with the missing paths named, and a passing row records 27 of 27; the provenance string search stays as the second check. The required list is a mirror across two repos with a divergence test on the machine that holds both checkouts and a named skip in CI. One publish image rebuild carries the corrected bake (`_LDT_SHA` to #522's merge), the content-grading walk, and the guard flip; the six staging re-bakes must pass the content walk and the shape diff before any production publish. |
| 2026-08-28 (LDT #522 merged; pin and rebuild in flight; staging atoms copy re-cut post re-stamp) | F-06, F-07, F-08 | planner: LDT PR #522 merged on green (main f4710c69); Factory walk PR merged (main 97e76f6); `_LDT_SHA` pin PR to f4710c69 opened and chained to merge and rebuild the publish image; Neon API: staging hauska_mcp branch br-billowing-queen-ap6npmua cut from production at 14:27Z (host ep-blue-unit) and `STAGING_HAUSKA_MCP_URL` rotated to it, parse-verified | **Two gates for the staging re-bakes are set before the image lands.** The previous staging atoms copy (br-late-star, 11:45Z) predates the 12:10Z to 12:44Z re-stamp, so its atoms still carry the retired pair, which the corrected bake now refuses at bake (the legacy table is empty on both sides); a re-bake against it would have written zero rows and refused under the 831fec74 rule. The fresh copy carries the canonical pair on every atom. Stale staging hauska_mcp branches now number six (br-little-term, br-round-sea, br-odd-dust, br-late-star, br-rapid-surf, br-lingering-glade) and are deleted after the six counties are served (A-022(8)). Next: the rebuilt image, then staging re-bakes in two waves with the content walk inline, the shape diff on staging, then six production publishes under A-021. |
| 2026-08-28 (first county through the corrected bake: Caldwell staging content walk passed; production re-publishing) | F-06, F-08 | planner: publish build a6ab3774 `SUCCESS` 14:33Z (publish job gen 16, walk gen 17, image `7bdecdb1`, LDT bundle f4710c69); six staging re-bakes executed 14:34Z and 14:47Z on the fresh atoms copy; Caldwell run 8bff7b18 (execution r6lzq) `publish_runs` succeeded, 7 pages, linked walk 55b6ed0e verdict `pass`, 61 parcels, sweep 59, zero non-pass under the content grade; the LDT shape-diff instrument against the staging database for 48055:20478, 48055:18354, 48055:19427: `requiredPresent 27/27`, `missing []`, `facetSchemaVersion node-facets-tier1-conformant-v1`, and a `referenceLeafDiff` naming `baseFacts.landUse.{code,description,source,vintage}` against the Bastrop reference parcel 48021:8704664 (its old row carries a populated land-use object; the Caldwell rows carry `baseFacts.landUse: null`, the explicit absence for a roll whose `propertyUseCode` is null); production re-publish for 48055 executed 15:02Z | **The corrected bake serves the full structure and the walk now proves it per parcel.** The instrument's per-parcel verdict is `fail` only on the reference comparison, which is value-shaped (a parcel with a code against parcels without one), not key-shaped; the contractual gate is the 27 required paths and the content walk, both passed. Bastrop's staging re-bake finished its pages and is in its walk; McLennan, Hays, Travis and Williamson are baking. Each passes staging then production under A-021; the four production counties stop serving the thin shape one by one as they re-publish. |
| 2026-08-28 (CALDWELL LIVE on the full facet shape in production) | F-06, F-08 | planner: execution qmn2n (`--target=production --county=48055 --gold=48055:20478`) completed 15:17:12Z succeededCount 1 (guard on the staging sibling 8bff7b18 with its content walk, then the inline production content walk); production probe at 15:17Z for 48055:20478: `facetSchemaVersion node-facets-tier1-conformant-v1`, `baseFacts {apn 20478, situsAddress 921 PLUM ST, situsState TX, acreage {0.2064 ac, shoelace-wgs84}, landUse null, situsCity null}`, `zoning {district RMD, jurisdictionKey lockhart_tx, provenance {Lockhart_City_Zoning_Online, stampedAt 15:10:05Z}}`, `envelope null` (explicit), `facetCoverage {tier1 populated, baseFacts true, zoning true, acreage true, structural true, landUse false, envelope false}`, `access {catalog-listed, anyone-free}`, `publishRunId 87c0cbc1`; Bastrop staging re-bake (run 38231af7, walk 1c8a2d1b pass, 232 parcels, 227 swept, zero non-pass) and McLennan (run ad45ef24, walk e8bee768 pass, 119 parcels, 117 swept) passed their content walks and their production re-publishes are running | **A brief on the new shape now says what the old one said, with the source named.** The zoning stamp came through the same join the old bake used, keyed by county and prop_id, and the absences are explicit fields rather than omitted keys. Six staging re-bakes ran concurrently on one publish job without conflict; the page cost rose with the concurrency (about 150 s a page against 100 s alone), which is the store contention again rather than the bake. Remaining: Bastrop and McLennan production walks; Hays, Travis and Williamson staging then production. |
| 2026-08-28 (BASTROP LIVE on the full facet shape; Hays staging passed; envelope value follow-up) | F-06, F-08 | planner: Bastrop production re-publish run a16ab8c2 succeeded with its production content walk 46089d32 (`pass`, 232 parcels, 227 swept, zero non-pass); production probes at 15:24Z for 48021:34729 (the operator's screenshot parcel) and gold 48021:34137: `facetSchemaVersion node-facets-tier1-conformant-v1`, situs, `acreage` by shoelace (0.263 and 0.383 ac), `zoning {district SF-1, provenance Zoned_Parcels, bastrop-city-tx}`, `landUse null`, `envelope null`, `facetCoverage {zoning true, acreage true, baseFacts true, structural true, landUse false, envelope false}`, `publishRunId a16ab8c2`; Hays staging re-bake run a84b216d succeeded with walk a19741a0 (`pass`, 54 parcels, 52 swept); Hays production re-publish executed 15:24Z; McLennan production at page 12 of 15; Travis staging 18 of 63, Williamson 29 of 76 | **The screenshot parcel serves its zoning district and its facts again**: the brief now reads SF-1 from the city's layer with the source named, the acreage, and the address, with explicit absences for land use. **One value is still short**: `envelope` is an explicit null on stamped SF-1 and RMD parcels where the old path derived setbacks and a buildable area; the content walk is key-shaped and passes it, which is exactly the caveat card B named. Routed to the card E subagent as a follow-up: establish whether the old tier 1 bake carried the envelope or the brief drew it from the live route, name the mechanism, and either carry the derivation through with a stated decline reason or leave the bake and describe the reader's two sources. Bastrop and Caldwell are live on the full shape; McLennan and Hays are publishing; Travis and Williamson follow. |
| 2026-08-28 (McLENNAN LIVE on the full facet shape) | F-06, F-08 | planner: execution mxbkl (`--target=production --county=48309 --gold=48309:176914`) completed 15:26:14Z succeededCount 1 (staging sibling ad45ef24 with its content walk, then the inline production content walk); production probe at 15:26Z for 48309:176914: `facetSchemaVersion node-facets-tier1-conformant-v1`, situs `4204 BEVERLY DR , WACO, TX 76711`, acreage 0.263 ac by shoelace, `zoning null`, `envelope null`, `facetCoverage {zoning false, acreage true, baseFacts true, structural true, landUse false, envelope false}`, `publishRunId 6ffc1e37` | **Third county on the full shape.** McLennan's zoning is an explicit null with `facetCoverage.zoning false`: Waco has no stamp row in the txgio join for this parcel (card E CP1 named 48309 as unstamped), so the brief says the district is not verified rather than showing a district it cannot cite. Bastrop, Caldwell and McLennan serve the full shape; Hays is publishing; Travis and Williamson are still baking on staging. |
| 2026-08-28 (envelope follow-up: pre-existing since 2026-07-23, not the cutover) | F-06, F-08 | card E subagent follow-up (no files touched; baseline 98 tests and typecheck green): stored rows read on production for 48021:34729 (run a16ab8c2) and the old-bake reference 48021:8704664 (2026-08-04, `node-facets-tier1-v1`): both store the byte-identical `envelope {status declined, declineReason atom_path_pending, jurisdictionKey, provisional, approximate, roadsPending, confidence null, disclosure}` with `facetCoverage.envelope false`; `computeTier1Envelope` in `lib/nodeFacetBakeTier1.ts` is decline-only since commit 75a7b6ba (2026-07-23, WDLL 3.7, #351) and the tier 1 path performs no setback lookup; `stripZombieEnvelopeFromFacets` in `routes/brokerageNodeFacets.ts` nulls `envelope` at serve; the BFF route `/api/spine/property-atoms/:id/facets` (`X-Pe-Read-Path atom-chain-warm`) derives `envelope {status ok, district SF-1, setbacks, buildableAreaSqFt, maxHeightFt, maxImperviousPct, minLotSize, secondSource}` in `atom-chain-to-facets.ts` lines 1267 to 1336; the inspect card and fact sheet read that route (`fact-sheet-resolver.ts` line 1543) while the R1 brief reads the bake only (`routes/propertyExplorer.ts` lines 560 to 587) and renders `extractEnvelopeBriefRefusal` as the operator's line; two mechanisms rejected with evidence (claim or geometry precondition: the rows carry a shoelace acreage and a joined parcel; district lookup key: no lookup exists on the tier 1 path) | **The setbacks line in the brief is not a cutover regression: it has read the same way for every baked parcel since 2026-07-23.** The corrected bake carries exactly what the old bake carried, the declined envelope, and the serve strip removes it as before; the setbacks a user sees on the inspect card come from the live atom-chain adapter, which the R1 brief never reads. The bake stays as it is. **Routing (operator decision, not in A-020 scope):** (a) the R1 brief route composes its setbacks section from the same atom-chain adapter the BFF uses, or (b) the brief panel composes it from the sheet it already holds, which carries the atom-chain envelope; the subagent recommends against (c) un-stripping at serve because it reverses WDLL 3.7. Recorded as an F-08 backlog card for the product seat. The empty branch `fix/f06-conformant-envelope` is deleted at the next cleanup. |
| 2026-08-28 (HAYS LIVE on the full facet shape; four of six) | F-06, F-08 | planner: execution rmhj4 (`--target=production --county=48209 --gold=48209:135570`) completed 16:24:27Z succeededCount 1 (staging sibling a84b216d with its content walk a19741a0, then the inline production content walk); production probe at 16:24Z for 48209:135570: `facetSchemaVersion node-facets-tier1-conformant-v1`, situs `275 CIBOLO CREEK DR, KYLE, TX 78640`, `acreage null`, `zoning {status absent, verdict not-applicable, authority none, scopeSearched municipal zoning authority for parcel shape=unincorporated}`, `facetCoverage {baseFacts true, structural true, zoning false, acreage false, landUse false, envelope false}`, `provenance.parcelJoin.basis` naming the gate block for 48209 (coverage ledger block verdict or LANDUSE_JOIN_DISABLED_FIPS_SEED), `publishRunId b6cda81e`; the Hays run took an hour under six concurrent bakes (22 pages) | **Four of six on the full shape: Bastrop, Caldwell, McLennan, Hays.** Hays shows what an honest absence looks like on the new shape: the zoning verdict names the scope it searched and the authority it found none of, and the acreage is null with the join gate named as the reason rather than a number derived from nothing. The ledger-versus-seed disagreement for 48209 and 48491 that card E named (the ledger scores them pass while the seed still blocks the join) is a ruling for the operator, recorded as an F-05 item. Travis (44 of 63 pages at 16:22Z) and Williamson (68 of 76) are finishing staging; their production publishes follow their content walks under A-021. |
| 2026-08-28 (WILLIAMSON staging re-bake passed the content walk; production publish launched) | F-06, F-08 | planner: staging execution rncdm (`--target=staging --county=48491 --gold=48491:76149`) completed 16:35:01Z succeededCount 1, close line `publishRunId c3eb5ef8`, `tier1Summary {conformantCadRows 602050, pages 76, written 602050}`, `walkId 9f22ab74`, `walkVerdict pass` (the content walk, BP-CONFORMANT-01 then BP-CONTENT-01); production execution vlh8k launched 16:38:57Z with the Hays production invocation form read back from execution rmhj4 and only the county and gold substituted (`bastrop-publish --target=production --county=48491 --gold=48491:76149 --skip-pmtiles`, `OPERATOR_PUBLISH_GO=1`, `PRODUCTION_SITE_URL=https://smartsite.cloud`) | Williamson goes to production under A-021 (standing word for the six) with the A-025(3) hold lifted: the bake projects the full facet set (LDT f4710c69 in image 7bdecdb1) and the walk graded content on the staging sibling. The invocation was derived from the Hays production execution template rather than retyped, and the derivation refuses if the template is not the expected one. Travis staging (jr264) is still baking. |
| 2026-08-28 (TRAVIS staging re-bake succeeded; its inline walk failed on one transient 504 on the gold; linked re-walk launched) | F-06, F-08 | planner: execution jr264 (`--target=staging --county=48453 --gold=48453:493738`) exited 1 at 17:10:24Z with the single log line `WALK_FAILED`; Factory store read-only read at 17:12Z (`read_travis_walk.mjs`, `default_transaction_read_only on`, 60 s statement timeout): `publish_runs eb442f7d` status `succeeded`, stamp 17:09:01Z, linked walk c0f17562 verdict `fail`, sweep 183, parcels 185, grades `{fail:BP-MEANING-01 1, pass:BP-MEANING-01 183, pass:BP-VERIFY-01 1}`, the one failure `48453:493738 {grade BP-MEANING-01, reason HTTP 504}`; the earlier staging run 8501ca22 (13:33Z, pre-projection) had passed the same sweep; direct probes of the exact URL the walk fetches (`https://smart-site-factory.vercel.app/site/api/spine/cortex/api/brokerage/v1/place/node/48453%3A493738/facets`) at 17:13Z: `200 0.857s`, `200 0.394s`; re-walk execution factory-verify-walk-jhx4s launched 17:14:22Z (`verify-walk --target=staging --county=48453 --gold=48453:493738 --publish-run=eb442f7d-6493-4b42-8b8e-e0ce51d5e5f3`) | The bake is sound: every one of the 183 swept parcels graded BP-MEANING-01 pass on the projected shape and the broken fixture failed as designed. The one failure is the gold itself timing out at the gateway once, on a run that shared the store with the Williamson production bake. The walk grades a 504 as a fail rather than retrying, which is correct: a walk that retries until it passes is a check that cannot fail. The re-walk is the standalone job linked to the same publish run, so a pass supersedes walk c0f17562 on the run row (`supersedesWalkId`) and that is what `requireStagingSibling` reads before production. Travis production launches on a pass under A-021 and A-025(3). Follow-up (F-07 backlog): the walk should record the fetch latency per parcel so a gateway timeout is distinguishable from a slow serve in the record, without retrying. |
| 2026-08-28 (TRAVIS linked re-walk passed; production publish launched) | F-06, F-08 | planner: execution factory-verify-walk-jhx4s completed 17:15:30Z succeededCount 1; store read-only read at 17:15Z: `publish_runs eb442f7d` now links walk 6753f6d2 `{verdict pass, sweepCount 183, supersedesWalkId c0f17562}`, walk body verdict `pass`, parcels 185, fails 0, grades `{pass:BP-MEANING-01 184, pass:BP-VERIFY-01 1}`; production execution 4l82p launched 17:16:41Z from the Hays production template with county and gold substituted (`bastrop-publish --target=production --county=48453 --gold=48453:493738 --skip-pmtiles`, `OPERATOR_PUBLISH_GO=1`, `PRODUCTION_SITE_URL=https://smartsite.cloud`); the derivation now also refuses if the template env differs from the expected pair | The re-walk is a fresh walk of the same 185 parcels against the rows run eb442f7d wrote, not a retry of the one parcel, so the pass stands on its own record and the run row names what it superseded. Travis goes to production under A-021 with the A-025(3) hold lifted (projection landed, content walked). Williamson production (vlh8k, since 16:39Z) and Travis production (4l82p) are the last two executions before the six are served. |
| 2026-08-28 (WILLIAMSON LIVE on the full facet shape; five of six) | F-06, F-08 | planner: execution vlh8k completed 18:26:10Z succeededCount 1 (1 h 47 min for 76 pages while the Travis production bake ran beside it); close line `publishRunId 38cf5b16`, `stagingSiblingId c3eb5ef8`, `tier1Summary {conformantCadRows 602050, pages 76, written 602050}`, stamp `production:48491` at 18:24:55Z `sourceVintage conformant-v1`, `walkId cceed65a`, `walkVerdict pass` (the inline production content walk); production probe at 18:26Z for 48491:76149: `facetSchemaVersion node-facets-tier1-conformant-v1`, situs `1804 DAVIS ST, TAYLOR, TX 76574`, `acreage null`, `zoning {status absent, verdict not-applicable, authority none, scopeSearched municipal zoning authority for parcel shape=unincorporated}`, `facetCoverage {baseFacts true, structural true, zoning false, acreage false, landUse false, envelope false}`, `publishRunId 38cf5b16`, `shapeSource conformant-v1` | **Five of six on the full shape: Bastrop, Caldwell, McLennan, Hays, Williamson.** One value-level observation for the walk backlog rather than a block: the gold carries a Taylor situs while the zoning verdict names `shape=unincorporated`; a situs city is a postal city and can sit in an ETJ, so the verdict may well be right, but the walk grades presence and provenance, not whether the incorporation claim agrees with the boundary tables, and that agreement is exactly a meaning-shaped check with two derivations (city limits polygon versus the parcel shape) the value-level walk item should carry. Travis production (4l82p, since 17:17Z) is the last execution. |
| 2026-08-28 (TRAVIS LIVE on the full facet shape; SIX OF SIX served; one served-value defect found and carded) | F-06, F-08 | planner: execution 4l82p completed 19:30:41Z succeededCount 1 (2 h 14 min); close line `publishRunId 1615b4ce`, `stagingSiblingId eb442f7d`, `tier1Summary {conformantCadRows 500307, pages 63, written 873766}`, stamp `production:48453` at 19:29:38Z `sourceVintage conformant-v1`, `walkId 3089903c`, `walkVerdict pass`; production probe at 19:31Z for 48453:493738: `facetSchemaVersion node-facets-tier1-conformant-v1`, situs `4707 SHOALWOOD AVE`, `publishRunId 1615b4ce`, `shapeSource conformant-v1`, `facetCoverage {baseFacts true, structural true, zoning false, acreage false, landUse false, envelope false}`; four-gold probe at 19:33Z (48453:493738, 48491:76149, 48209:135570, 48021:34137): `baseFacts.situsCity null` on all four, `baseFacts` keys `apn, acreage, landUse, situsCity, situsState, situsAddress`, zoning verdict `not-applicable` with basis `shape predicate: no zoning authority exists for unincorporated land` on the three without a stamp, Bastrop (stamped) unaffected; LDT code read at main f4710c69: `verdictLayerServe.ts` `parcelIsUnincorporatedShape` (no zoning stamp and no non-empty `baseFacts.situsCity`) feeding `not-applicable / authority none / scopeSearched municipal zoning authority for parcel shape=unincorporated`; `cityLimitsFactRead.ts` already implements the honest three-state read (unmeasured when `tx_city_boundary` is empty; unincorporated only when the index is populated and the point is outside every polygon) and the predicate does not call it; conformant CLI `written` increments once per parcel node written, pages are disjoint `prop_id` slices (`pagePropIds`) | **Six of six on the full shape in production: Bastrop, Caldwell, McLennan, Hays, Williamson, Travis.** The A-020 measure of done is met on its own terms (rows a succeeded run wrote, a passed production walk on gold plus an area sweep, a freshness stamp per county). Two findings ride along, both on served values the walk cannot grade. **(1) A wrong statement, not an absence.** Every conformant-baked parcel without a zoning stamp in a county carrying the unzoned-unincorporated doctrine is served as `not-applicable: unincorporated`, including 4707 Shoalwood Ave (inside Austin), 275 Cibolo Creek Dr (Kyle) and 1804 Davis St (Taylor). Mechanism: the conformant assemble never maps `situsCity` from the claim (the claim carries it; the served field is null on all four golds, including the two whose address string names the city), and the serve predicate treats that null as evidence of unincorporated land. A postal city would not be the right second derivation either; city-limits containment is, and the code for it exists and is uncalled. Second mechanism considered and rejected: the parcels really are unincorporated; rejected because the predicate consulted no boundary at all and three of the three addresses sit inside incorporated places. This is the ENFORCEMENT defect in its plainest form (an absent field collapsed into a claim) and it is carded now as CTX card F, fixed forward, production not pulled: the served shape is right, one verdict text on it is wrong. **(2)** `written 873766` against `conformantCadRows 500307` for Travis (Williamson wrote 602,050 against 602,050): mechanism, one snapshot per parcel node and a Travis `prop_id` joining more than one node (the P-80 divergent-join class); alternative, overlapping pages, rejected because pages are disjoint `prop_id` slices with no OFFSET. Upserts by `place_key`, so no user-facing effect; the count reconciliation goes to the backlog with the value-level walk item. Cleanup under A-022 (stale staging branches, the empty LDT branch) proceeds now. |
| 2026-08-28 (A-022 cleanup executed after the six: six stale staging Neon branches deleted; empty LDT branch deleted; card F dispatched) | F-06, F-07, F-08 | planner: Neon project fancy-fire-06136146 branch list read at 19:36Z (9 branches; the six candidates all leaf children of `br-crimson-feather-aphfmy91`, none protected, none with children); secret hosts read (host only): `STAGING_HAUSKA_MCP_URL` ep-blue-unit (`br-billowing-queen-ap6npmua`), `STAGING_NEONDB_URL` ep-wispy-fire (`br-super-cloud-ap4ied3j`), `PRODUCTION_NEONDB_URL` ep-lucky-truth; each candidate branch had its endpoint hosts fetched and compared against the three secret hosts before deletion (the deletion refuses on a hit; zero hits); deleted 19:37:41Z to 19:37:45Z: `br-little-term-apz850jo` (ep-frosty-sound), `br-round-sea-api84wvk` (ep-bold-sky), `br-odd-dust-ap5ezib6` (ep-square-darkness), `br-late-star-ap6f0ikt` (ep-bold-rain), `br-rapid-surf-ap15yw5n` (ep-winter-bread), `br-lingering-glade-apvevod1` (ep-winter-sound); remaining: production, `br-super-cloud` (staging neondb), `br-billowing-queen` (staging hauska_mcp); LDT `fix/f06-conformant-envelope` was never pushed (no remote ref) and had zero commits over its merge-base with origin/main, deleted locally; the LDT publish worktree now on `fix/f08-zoning-verdict-city-limits` at origin/main 5e5d1d95 (main moved past the f4710c69 pin by #523 to #528, P-85 and P-91 work by the other lane); the Factory ctx-publish worktree on `seat/property-ctx-walk-value` at 09e3cc3; seat register nested rows updated (a first pass wrote the seat-level branch by mistake and was reverted in the same session); card F written at `_inbox/2026-08-28_ctx_f_zoning_verdict_city_limits_WDLL.md`, dispatch compiled `_dispatches/2026-08-28_ctx-f_dispatch.md` | The A-022 cleanup ran as a state-changing operation should: the targets were enumerated from the authority (the branch list, not memory), each deletion was gated on a second derivation (the endpoint host against every store secret), and every deletion is recorded with its id, name, host, and timestamp. The publish image pin stays at f4710c69 until card F lands; the LDT main movement since then is the other lane and is not on the facet path. Card F is the next and only Factory execution until it closes; the six stay served on the current bake. |
| 2026-08-28 (card F handback: the reader, not the assemble, dropped the claim fields; verified; PRs open) | F-06, F-08 | subagent CP1 (`_inbox/2026-08-28_ctx-f_cp1.json`, production `place_layer_snapshots` 19:48Z, atoms `hauska_mcp` 19:54Z, read-only): `readConformantCadClaim` read `situsCity`, `situsZip`, `landAcres`, `propertyUseCode` from `body.claim` only, and the store holds no `body.claim` (120 of 120 sampled conformant bodies flat; the Factory stage E `stageRows` spreads the candidate, `{ ...c, atomId, ... }`, planner code read confirms); production consequence: `situsCity` null on 1,498,010 of 1,498,010 conformant rows, land use present on 0, acreage from the claim on 0 (Bastrop 46,153 and Caldwell 24,926 carry a shoelace acreage from the txgio join; Hays and Williamson 0, gate-blocked); 534,700 unstamped rows hold the 0,0 bake sentinel (no point), 667,497 carry a point; golds stored bodies carry AUSTIN 78756, TAYLOR 76574, KYLE 78640, BASTROP 78602; planner verification: `pnpm typecheck` exit 0, `verdictLayerServe.test.ts` 28 pass, `nodeFacetBakeTier1Conformant.test.ts` 22 pass, `cityLimitsFactRead` 8 and route contract 2 pass, the DB-backed `brokerageNodeFacets.test.ts` cannot run locally (CI authoritative), the three predicate fixtures reported failing on unchanged 5e5d1d95 (`3 failed, 10 passed`) then passing; hauska-factory `npm test` 268 tests 266 pass 0 fail 2 skipped (baseline 259/257); diffs read by the planner (reader resolves nested or flat; predicate derives from `loadCityLimitsFact` with `derivation` on the wire; walk BP-VALUE-01 fails closed on no point or empty table); LDT commit cb5ec9fa on `fix/f08-zoning-verdict-city-limits`, PR #532 updated onto main via update-branch; Factory commit adc1d2e on `seat/property-ctx-walk-value`, PR #34; both pushes used the logged `CLOSE_OVERRIDE=1` hatch because the doc_repo state docs sit in the pending commit block awaiting the operator word (target and cwd recorded by the hook) | **Card F premise corrected by its own measurement, which is the item working.** The card said the assemble never mapped `situsCity`; the assemble did, the reader looked in a place the Factory never wrote, and card E tested a nested fixture the store never had. That is one mechanism explaining three of the coverage-false flags recorded on the golds today (city, land use, claim acreage), and it means the honest-absence readings for land use across the six were reader defects, not absences; the Hays and Williamson acreage readings stay join-gate absences. Second mechanism considered and rejected: the claim itself null on the store (the first sample read `body->claim->>situsCity` and returned null for all 120, which looked like that until the body keys were listed). The three unstamped golds will serve `unmeasured` after the fix, not `stamp-missing for Austin`, because their bake point is the 0,0 sentinel; a sentinel standing for no point is the next defect class and is filed. Item 4 grade is BP-VALUE-01 with a declared shared input (the served query point) until the Factory holds an indexed parcel point. Next: merge on green, cortex-api canary and shift, redeploy the staging tag from the same image, re-pin `_LDT_SHA`, rebuild the publish image, re-bake the six staging sibling then production under A-021. |
| 2026-08-28 (card F merged; cortex-api canary smoke PASS on seven parcels; staging tag redeployed; publish image pinned) | F-06, F-08 | planner: LDT #532 merged on ten green checks (conclusion strings) at 20:43Z, main ee27845e; push build-and-push run 33209435602 success; `deploy-canary` run 33209719016 with `image_tag=ee27845e...` success 20:49Z, revision cortex-api-00643-rib at 0 percent tag canary; smoke instrument `canary_smoke_card_f.sh` (file-based; shown FAILING against production first at 20:37Z with three `shape predicate` hits) then PASS on the canary tag at 20:49Z: 48453:493738, 48491:76149, 48209:135570 serve `verdict unmeasured, authority unresolved, derivation {cityLimitsStatus unmeasured, queryPoint null}`; 48021:34137 serves `district SF-1` with `cityLimitsFact.queryPoint {-97.31654, 30.10981}`; 48453:231086 (6102 Laird Dr) serves `stamp-missing, authority Austin, derivation.place Austin, queryPoint {-97.73862, 30.33617}`; 48453:227161 and 48453:233153 serve `not-applicable` with `derivation.cityLimitsStatus unincorporated` and their points; zero `shape predicate` hits on all seven; before the fix the three pointed parcels served no zoning verdict at all (an omitted key); staging tag redeployed by `gcloud run deploy --tag staging --no-traffic` from the same image with `CORTEX_STAGING=1`, `DATABASE_URL<-STAGING_DEPLOYMENT_DATABASE_URL`, `ATOMS_DATABASE_URL<-STAGING_ATOMS_DATABASE_URL`, revision cortex-api-00644-soz; digests read by revision: 00643-rib and 00644-soz both `sha256:59a4696f...` equal to the `:ee27845e` tag digest, serving 00389-phv on `sha256:b29beb70...` until the shift; Factory #34 (walk BP-VALUE-01) merged main 10c544da, #35 (`_LDT_SHA` ee27845e) merged main 46b38977, publish image build 1c110248 submitted from the clean archive of that main; revision numbering on cortex-api restarted at 00387 today so revisions are compared by creation time and digest, never by number | The canary shows all three states the card demanded, on real parcels, with the derivation on the wire: unmeasured where there is no point, stamp-missing inside Austin with the place named, not-applicable outside every polygon with the point that proves it. The shift to 00643-rib is running (`shift-traffic` dispatch) and is verified by the traffic list by name and the same smoke against the production proxy. Next: the six re-bakes on the new publish image, staging sibling then production under A-021, Bastrop first. |
| 2026-08-28 (card F SERVING in production: shift to cortex-api-00643-rib; six staging re-bakes launched on the new publish image) | F-06, F-08 | planner: `shift-traffic` run 33209967076 success 20:51:36Z; traffic list by name: cortex-api-00643-rib 100 (tag canary), 00644-soz 0 (tag staging); production smoke via the smartsite.cloud proxy PASS on the seven parcels (the three sentinel golds `unmeasured`, 48453:231086 `stamp-missing`, the two outside `not-applicable` by containment, Bastrop SF-1), zero `shape predicate` hits; publish build 1c110248 SUCCESS 20:51Z, factory-bastrop-publish gen 17 and factory-verify-walk gen 18 on image 1b10d7e7 (LDT pin ee27845e); six staging re-bakes launched 20:54Z on that image: 48021 k224m, 48055 2kr9c, 48309 wkjgk, 48209 vj2sz, 48453 mnwhc, 48491 4j7r4 (golds 34137, 20478, 176914, 135570, 493738, 76149) | Card F serves in production for every parcel the route answers today: the wrong statement is gone from the served surface before a single row was re-baked, because the verdict is derived at serve from containment. The re-bakes now carry the corrected reader (situsCity, situsZip, land use and claim acreage populated where the claim carries them) and the walk on each staging sibling grades BP-VALUE-01 beside BP-CONFORMANT-01 and BP-CONTENT-01 (28 required paths, so every un-re-baked row fails BP-CONTENT-01 on `baseFacts.situsZip`, which is the gate working). Production publishes follow each passed staging walk under A-021. |
| 2026-08-28 (INCIDENT: staging serve 500 on every parcel after the A-022 cleanup deleted a branch the LDT project secrets still bound; fixed; linked re-walks) | F-06, F-07, F-08 | planner: staging re-bakes 2kr9c (48055) and k224m (48021) exited `WALK_FAILED` at 21:10Z with succeeded runs (7af35895 written 73,159; 8ccbfa65 written 61,695) and walks e252c580 and 8135cd50 failing BP-MEANING-01 `HTTP 500` on 60 of 60 and 231 of 231 parcels (the broken fixture still passing BP-VERIFY-01; BP-VALUE-01 ran with 1,222 polygons and nothing to grade); direct probe of the staging tag 500 on old and new rows alike, `/api/healthz` 200; revision 00644-soz app log: `password authentication failed for user neondb_owner` from `pg-pool` inside `loadBuildingFootprintFactAtom` and `loadPipelineFactAtom` (the atom-chain reads on `ATOMS_DATABASE_URL`); secret versions in legacy-design-tools-prod read by host only: `STAGING_ATOMS_DATABASE_URL` v3 and v4 (2026-08-27 20:01Z and 20:02Z) at ep-frosty-sound, which is `br-little-term-apz850jo`, deleted by the planner at 19:37:41Z under A-022; `STAGING_DEPLOYMENT_DATABASE_URL` v4 at ep-wispy-fire (`br-super-cloud`, live); fix: versions 5 of both secrets written from the Factory staging secrets (`STAGING_HAUSKA_MCP_URL` ep-blue-unit `br-billowing-queen`, `STAGING_NEONDB_URL` ep-wispy-fire), staging tag redeployed to revision cortex-api-00646-luj (same image digest 59a4696f) re-resolving `:latest`; direct probes 200: 48021:34137 `district SF-1, situsCity BASTROP, situsZip 78602, acreage 0.3827 shoelace, publishRunId 8ccbfa65`; 48055:20478 `district RMD, situsCity LOCKHART, situsZip 78644, acreage 0.2064`; 48453:493738 `unmeasured`; 48453:231086 `stamp-missing, authority Austin`; linked re-walks launched 21:22Z for the three succeeded runs whose inline walk hit the outage (48021 zf72q, 48055 76ght, 48309 6x8lv on run eb6405a0 written 113,090); Hays, Travis, Williamson staging bakes still running and watched | **Mechanism, and the error is the planner own.** The A-022 cleanup gated each deletion on a second derivation, the endpoint host against every store secret, and enumerated the secrets of one project (hauska-prod) while a second project (legacy-design-tools-prod) bound the same branch through its own staging secrets. The check was meaning-shaped and its scope was too narrow, and the narrow half was silent: the staging serve kept working on the resolved value until the next deploy re-resolved `:latest`, which is exactly the deploy-time secret trap already in memory. The Neon proxy answers a deleted endpoint with an authentication failure rather than a connection error, which pointed at a password before it pointed at a branch. Second mechanism considered and rejected: the new bake payload crashing the route, rejected because old-bake rows 500 identically and the stack is in the atom-chain reads. Two corrections follow: a branch deletion enumerates every project that binds the host (memory written), and the staging serve and the staging bake now read the same atoms branch, which they did not before today (the serve read br-little-term, the bake br-billowing-queen), so the staging walk was never a full second derivation over one store until now. The bakes themselves were sound and are not repeated; the re-walks supersede the failed walks on the run rows, and production follows each pass under A-021. |
| 2026-08-28 (card F re-bakes: Bastrop, Caldwell, McLennan re-walked PASS on the repaired staging serve; production publishes launched) | F-06, F-08 | planner: re-walk executions zf72q (48021, 21:21:26Z to 21:22:39Z), 76ght (48055, 33 s), 6x8lv (48309, 43 s) succeeded; store read-only read at 21:24Z: run 8ccbfa65 now links walk 85b0e635 `{pass, sweep 227, supersedesWalkId 8135cd50}` with 232 parcels `{pass:BP-MEANING-01 231, pass:BP-VERIFY-01 1}`; run 7af35895 links walk 2f90a92f `{pass, sweep 59, supersedes e252c580}` 61 parcels; run eb6405a0 (48309, written 113,090) links walk 1c1ed446 `{pass, sweep 117, supersedes 6c6d632b}` 119 parcels; each walk body carries `valueGrade {rule BP-VALUE-01, source landing_tx_city_boundary, polygons 1222, claimedUnincorporated 0, failed 0, passed 0}`; the staging tag now serves the re-baked rows with the claim fields (48021:34137 `situsCity BASTROP, situsZip 78602, acreage 0.3827`; 48055:20478 `situsCity LOCKHART, situsZip 78644, district RMD`); production publishes launched 21:23Z: 48021 58qh8, 48055 48gjr, 48309 sfk79 (`--target=production`, gold, `--skip-pmtiles`, `OPERATOR_PUBLISH_GO=1`, `PRODUCTION_SITE_URL=https://smartsite.cloud`) | The walk exit codes were not taken as the verdict: the three re-walks finished in under 75 seconds, which read as convenient, so the walk rows were read from the store and they are full walks of the same parcel sets that failed an hour earlier, superseding the failed walks on the run rows. BP-VALUE-01 had nothing to grade in these three sweeps (no served unincorporated claim on a same-street sweep around a stamped gold), which is recorded as zero graded rather than as a pass. Hays, Travis and Williamson staging bakes are still running on the repaired staging serve; their inline walks are the first that can exercise BP-VALUE-01 on pointed unstamped parcels. |
| 2026-08-28 (CALDWELL and BASTROP LIVE on the card F bake in production) | F-06, F-08 | planner: production execution 48gjr (48055) completed 21:37:09Z succeededCount 1, gold 48055:20478 on smartsite.cloud: `facetSchemaVersion node-facets-tier1-conformant-v1, district RMD, situsCity LOCKHART, situsZip 78644, acreage 0.2064, publishRunId f4eb42ac, bakedAt 21:30:47Z`; production execution 58qh8 (48021) completed 21:39:11Z succeededCount 1, gold 48021:34137: `district SF-1, situsCity BASTROP, situsZip 78602, acreage 0.3827, publishRunId 4fc360c7, bakedAt 21:29:25Z`; both inline production content walks passed (exit 0 on the publish job, which exits 1 on WALK_FAILED); the staging siblings 7af35895 and 8ccbfa65 carried their superseding pass walks | Two counties serve the card F bake in production: the claim fields the reader had been dropping (situs city and zip, claim acreage where the roll carries it) are on the wire for every re-baked parcel, and the zoning verdict on unstamped parcels is derived at serve from containment on every county already (cortex-api 00643-rib). McLennan production (sfk79) is running; Hays, Travis, Williamson are still in their staging bakes. |
| 2026-08-28 (the screenshot parcel: 1005 Pine St, Bastrop, serves its city, zip, district, and acreage on the card F bake) | F-06, F-08 | planner: production probe 21:41Z for 48021:34729 (the parcel in the operator screenshot at 13:40Z that read `not resolved a fact sheet yet`, `carries no zoning stamp`, `no envelope was derived`, `not verified here`): `facetSchemaVersion node-facets-tier1-conformant-v1`, situs `1005 PINE ST, BASTROP, TX 78602`, `situsCity BASTROP`, `situsZip 78602`, `zoning.district SF-1`, `acreage 0.263`, `facetCoverage {zoning true, acreage true, baseFacts true, structural true, landUse false, envelope false}`, `publishRunId 4fc360c7`, `bakedAt 21:29:25Z` | The operator question of the afternoon (does the UI need to change for the new shape) closes with evidence on the parcel that raised it: no UI change; the bake now projects the full facet set (card E), reads the claim where the Factory writes it (card F), and the zoning stamp joins. The two flags still false are honest: `landUse` because the CAD claim for this parcel carries no `propertyUseCode` (an absence in the source, not a dropped field), and `envelope` because the tier 1 bake is decline-only for envelopes since 2026-07-23 and the brief does not yet compose setbacks from the atom-chain adapter (the F-08 routing decision recorded earlier today). |
| 2026-08-28 (McLENNAN LIVE on the card F bake, its gold serving stamp-missing inside Waco; HAYS staging passed, production launched) | F-06, F-08 | planner: production execution sfk79 (48309) completed 21:48:53Z succeededCount 1; gold 48309:176914 on smartsite.cloud: `facetSchemaVersion node-facets-tier1-conformant-v1, zoning.verdict stamp-missing, situsCity WACO, situsZip 76711, acreage 0.2631, publishRunId 66bbd7bd, bakedAt 21:36:00Z`; Hays staging execution vj2sz completed with `walkVerdict pass` (inline content walk on the repaired staging serve, the first inline walk to run BP-VALUE-01 on pointed unstamped parcels), production execution r7qvx launched 21:41:46Z by the watcher under A-021 | Three counties on the card F bake in production (Caldwell, Bastrop, McLennan). The McLennan gold is the card F verdict doing its job on a real parcel: no zoning stamp, a point inside the Waco polygon, so the wire says the stamp is missing for Waco rather than that Waco has no zoning authority. Hays production (r7qvx), Travis (mnwhc) and Williamson (4j7r4) staging remain in flight. |
| 2026-08-28 (HAYS LIVE on the card F bake; four of six) | F-06, F-08 | planner: production execution r7qvx (48209) completed 22:21:33Z succeededCount 1 (inline production content walk pass); gold 48209:135570 on smartsite.cloud: `facetSchemaVersion node-facets-tier1-conformant-v1, situsCity KYLE, situsZip 78640, zoning.verdict unmeasured, authority unresolved, acreage null, publishRunId 564fa712, bakedAt 21:50:04Z` | Four of six on the card F bake (Caldwell, Bastrop, McLennan, Hays). The Hays gold shows the reader fix (city and zip now on the wire from the claim) beside the two honest absences that remain for the whole county: the prop_id join is gate-blocked for 48209 (no point, so containment and acreage are unmeasured, stated as such), which is the ledger-versus-seed ruling already routed to F-05. Travis (mnwhc) and Williamson (4j7r4) staging bakes are on their last stretch; production follows each passed walk. |
| 2026-08-28 (TRAVIS staging re-bake passed on the repaired serve; production publish launched) | F-06, F-08 | planner: staging execution mnwhc (48453) completed with `walkVerdict pass` on the inline content walk (the walk that runs BP-VALUE-01 over pointed unstamped parcels on the staging serve 00646-luj); production execution 28jmt launched 22:52:58Z by the watcher under A-021 (`--target=production --county=48453 --gold=48453:493738 --skip-pmtiles`, `OPERATOR_PUBLISH_GO=1`, `PRODUCTION_SITE_URL=https://smartsite.cloud`) | Travis is the county where the card F verdict has the most to say (Austin parcels without a stamp), and its staging walk passed with the value grade armed. Williamson (4j7r4) is the last staging bake in flight; its production follows its pass. |
| 2026-08-28 (WILLIAMSON staging re-bake passed; production publish launched; all six card F staging walks passed) | F-06, F-08 | planner: staging execution 4j7r4 (48491) completed with `walkVerdict pass`; production execution 6cpp2 launched 23:03:46Z by the watcher under A-021 (`--target=production --county=48491 --gold=48491:76149 --skip-pmtiles`, `OPERATOR_PUBLISH_GO=1`, `PRODUCTION_SITE_URL=https://smartsite.cloud`); card F re-bake ledger: staging walks passed for all six (Bastrop, Caldwell, McLennan by linked re-walk after the staging outage; Hays, Travis, Williamson inline); production complete for four (58qh8, 48gjr, sfk79, r7qvx) and running for Travis (28jmt, since 22:53Z) and Williamson (6cpp2) | Every county has a passed staging sibling on the card F bake with the value grade armed, and the last two production publishes are the only executions left in the program today. |
| 2026-08-29 (WILLIAMSON LIVE on the card F bake; five of six) | F-06, F-08 | planner: production execution 6cpp2 (48491) completed 2026-08-29 00:51:49Z succeededCount 1 (inline production content walk pass); gold 48491:76149 on smartsite.cloud: `facetSchemaVersion node-facets-tier1-conformant-v1, situsCity TAYLOR, situsZip 76574, zoning.verdict unmeasured, authority unresolved, acreage null, publishRunId c3c5a9a9, bakedAt 23:59:00Z` | Five of six on the card F bake (Caldwell, Bastrop, McLennan, Hays, Williamson). Williamson reads like Hays: city and zip now on the wire from the claim; containment and acreage unmeasured because the prop_id join is gate-blocked for the county (the F-05 ruling). Travis production (28jmt, since 22:52Z) is the last execution of the program. |
| 2026-08-29 (TRAVIS LIVE on the card F bake; SIX OF SIX on the card F bake in production; the day closes) | F-06, F-08 | planner: production execution 28jmt (48453) completed 01:38:29Z succeededCount 1 (2 h 46 min; run 5a985b47, 63 pages, tier 1 about 12,300 and tier 2 8,000 rows per page by the run events; inline production content walk pass); golds and probes on smartsite.cloud at 01:38Z: 48453:493738 `situsCity AUSTIN, situsZip 78756, zoning.verdict unmeasured, authority unresolved, acreage null, publishRunId 5a985b47`; 48453:231086 (6102 Laird Dr) `stamp-missing, authority Austin, situsCity AUSTIN, situsZip 78757, acreage 0.1598`; 48453:227161 `not-applicable, authority none, situsCity null, situsZip 78724, acreage 5.0177`; card F production ledger complete: Caldwell f4eb42ac, Bastrop 4fc360c7, McLennan 66bbd7bd, Hays 564fa712, Williamson c3c5a9a9, Travis 5a985b47, each from a passed staging sibling on the card F bake and a passed production walk | **Six of six counties on the card F bake in production.** Every conformant parcel now carries what its CAD claim carries (city, zip, land use where present, claim acreage), the zoning verdict on unstamped parcels is derived from city-limits containment with the derivation on the wire, and the walk grades a served unincorporated claim against a second copy of the polygons. The three honest absences that remain are named on the wire: no point where the join has no row or is gate-blocked (`unmeasured`), no land use where the roll carries none, and a null city on a claim that has none (48453:227161), which is an absence in the source rather than a dropped field. What the day did not close: the 534,700 sentinel rows need a point source; the gate-blocked join for Hays and Williamson needs the F-05 ruling; the R1 brief envelope routing needs the F-08 decision; the Property Explorer label set needs `stamp-missing` and `unmeasured`; the Factory needs an indexed parcel point so BP-VALUE-01 drops its shared-input clause. Nothing is executing in the program. |
