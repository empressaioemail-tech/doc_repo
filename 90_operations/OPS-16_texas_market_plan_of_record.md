---
id: OPS-16_texas_market_plan_of_record
title: OPS-16 — Texas complete and out the door: plan of record
date: 2026-08-12
last_updated: 2026-08-30
status: active
owner: nick (amendments); doc_repo planner (grading)
purpose: The single benchmarkable plan for closing the Texas flush launch gate and reaching market. Baseline is frozen; changes are append-only amendment rows. Work that cannot name a row ID here is not scoped.
related_canonical: [_decisions/2026-08-11_texas_flush_launch_gate_amendment, _decisions/2026-08-09_texas_flush_launch_gate, 90_operations/OPS-14_texas_flush_game_plan, _inbox/2026-08-12_PLANNER_HANDOFF_five_layer_reset, 76j_smartsite_launch_readiness_program]
---

# OPS-16 — Texas complete and out the door: plan of record

## Why this document exists

Every prior version of "the plan" lived in prose across five documents, and the program's measured base rate is that prose-shaped controls run 0-for-3 while structure-shaped controls run 1-for-1. This document is the structural answer: a frozen baseline table with stable row IDs, each row naming the gate item it serves and the pass/fail instrument that grades it, plus an append-only amendment log. Drift becomes a diff you can read instead of a feeling.

**Definition of done.** All 13 done-card items (DC-1 through DC-13) in `_decisions/2026-08-11_texas_flush_launch_gate_amendment.md` grade PASS via their named instruments, plus the Layer 5 market rows below. Measured-everywhere, not filled-everywhere. `texasCompletenessPct` is informational only (DC-12). A row graded by narration or doc assertion fails.

## Governing rules

1. **The baseline table never gets edited.** Every scope change (add, move, remove, re-層, re-scope) is a dated row in the AMENDMENTS table. What we believed on 2026-08-12 stays visible forever.
2. **No dispatch without a row.** Every dispatch names `PLAN-ROW: P-xx` in its preamble. Candidate for canon-gate hook enforcement (hook-shaped controls are the only class that has worked here).
3. **Statuses re-grade only by instrument** (ledger GET, SQL, gh, live probe) at session close. A status changed by narration is a defect. The GRADE LOG below records each grading pass.
4. **Lane-planner fan model (operator ruling 2026-08-12).** Hand-carried lane agents are planning agents that may spawn sub-agents, DEVIATING from the standing no-nesting default by explicit operator ruling for this program. Conditions that keep the old lesson alive: the lane planner supervises every sub-agent to completion (a coordinator that fans and returns abandons its workers), verification is never delegated below the lane planner, the lane planner adversarially reviews sub-agent output with at least two checkpoints, and the lane delivers a machine-checkable close artifact at a named `_inbox/` path.
5. **Write-slot law.** One atoms bulk-writer slot per database. Slot handoffs recorded in `_STATE.md`. Acquisition, staging, builds, and dry-runs are slot-free and parallel; only `--apply` against the atoms store queues.

## Lineage — baseline v0 and the A-000 correlation

The five-layer table of ~2026-08-09/10 is baseline v0 of this plan (operator artifact; states quoted verbatim). Its "F1/F2/F3" naming is the pre-relabel scheme; the 2026-08-12 operator ruling renamed the factories (statewide fabric = Factory 1, acquisition/staging = Factory 1.5, county depth = Factory 2). Correlate by function, not by name.

| Layer | v0 state (~08-09/10) | Verified state 2026-08-12 | Verdict |
|---|---|---|---|
| Foundation | "Built. 196/254 counties, flood loaded, contract proven in production." | `txgio_parcel` 253/254 (Wave 4 landed 57 reprojection counties; Donley the only gap); geometry rail 251/254 satisfied on the live ledger; NFHL loaded with geom+GiST; contract proven and the Factory 1.5 back half added (eng #316/#317) | Advanced. "Built" at 196/254 was premature then; nearly true now |
| Measurement | "Running now (Handoff D holds the slot chain: sweep, Bastrop anchors, scorer, three writers statewide)." | That chain completed: sweep 132/132 closed 08-11, scorers applied, writers grew three to twelve; 14/14 rails writer+atom; zero `no-writer`/`no-atom` live. Slot now held by A2 (SD, rail-corridor) | Chain done; a longer chain replaced it |
| Integrity | "Cert is honestly 6/7; the fix lane (G) and roads unblock (H) are in your hands to dispatch." | block13 reached 7/7 (T1 re-persist) but DC-10 as a whole is UNGRADED this arc. Roads: #293 salvaged, writer built and bound, `not-yet` x254; statewide ingest still awaits operator ruling, the one v0 item still literally open | Half closed; the roads ruling survived two baselines unchanged |
| Depth | "Started: Smithville corpus live, 35 CAD registry rows, Elgin correctly held. Scales after the slot frees." | The 35 registry rows became A1's 15-county apply (13.1M atoms); Smithville (91 rows) and Elgin (3,209 rows) staged in the Factory 1.5 seam; Elgin apply hold persists unresolved since v0. "Scales after the slot frees" proved false as written: scaling needs the Factory 1.5 FRONT half, which does not exist | Reframed. The bottleneck moved from the slot to acquisition machinery |
| Launch | "Parallel track; domain bought, limiter done, load test and MCP recon still owed." | Load test still owed; MCP recon became MCP1 (atom chain 15/16 live; five hardcoded lists remain). "Limiter done" is NO LONGER TRUE: the Upstash rate-limit DB died and MCP runs on the in-memory fallback; DC-11a would fail today. New since: Stripe "Hauska Pro" hard gate, pricing ladder lock | One row regressed silently. This is the whole argument for instrument-graded status |

## PLAN OF RECORD v1 — baseline frozen 2026-08-12

Status legend: **LIVE** = verified at source 2026-08-12 by the doc_repo planner. **DOC** = from a close artifact, not re-verified that day. **FLIGHT** = running at baseline time.

| ID | L | Work item | Serves | Pass/fail instrument | Blocked on | Status at baseline |
|----|---|-----------|--------|---------------------|------------|-------------------|
| P-01 | 1 | Geometry rail: last 3 counties to satisfied or ruled honest-absent (incl. Donley 48129) | DC-2 | Ledger geometry cells, zero pre-terminal | Donley source ruling | LIVE: 251/254 |
| P-02 | 1 | Ector 48135 re-key on geo_id (~71,673 atoms) | DC-2/DC-3 | Store query post-re-key | free lane | DOC: queued |
| P-03 | 2 | A2 special-district apply completes; slot released and recorded | DC-3 (mud) | Close artifact + `_STATE.md` handoff entry | none | FLIGHT: 3/253 landed, PID 43348 alive |
| P-04 | 2 | Rule mud vs special-district (same subject built twice) + declaration refresh + cortex redeploy | DC-1, DC-3 | `countyRailRefreshCli` + DC-1 coherence check | operator ruling | OPEN; blocks all P-03 output from lighting cells |
| P-05 | 2 | SD scorer run + rail attribution after P-04 | DC-3 | Ledger mud rail | P-03, P-04 | not started |
| P-06 | 2 | Rail-corridor apply (A2's second rail) + scorer | DC-3 | Ledger rail-corridor x254 | P-03 sequence | FLIGHT (queued in-lane) |
| P-07 | 2 | Flood metros apply (6 counties, PostGIS zone-major) | DC-3 | Ledger flood; Harris plan ~31 min proven | atoms slot | DOC: ready |
| P-08 | 2 | Flood remainder: 63 sub-threshold + 35 crash-cohort counties to satisfied x254 | DC-3 | Ledger flood, zero not-yet | P-07, slot | LIVE: 114 satisfied / 140 not-yet |
| P-09 | 2 | Footprint apply (10.67M staged rows, geometry-true join) + scorer | DC-3 | Ledger footprint x254 | slot | LIVE: x254 not-yet |
| P-10 | 2 | Well staged-writer swap PR (recover uncommitted `fetch-wells-staged.ts` + writer diffs; CP1-F2) | DC-3 | PR merged on CI string "success"; writer reads `tx_rrc_well` not Harris mirror | A2 release for the dirty files; recovery build may start read-only | OPEN; wells apply forbidden until landed |
| P-11 | 2 | rrc-wells apply statewide + scorer (W2 fail-closed merged, eng #321 @ 0dd28e6) | DC-3 | Ledger rrc-wells x254; `unknown` statuses present, zero silent defaults | P-10, slot | LIVE: x254 not-yet |
| P-12 | 2 | rrc-pipelines apply + scorer (writer built per G1 close) | DC-3 | Ledger rrc-pipelines x254 | slot | LIVE: x254 not-yet |
| P-13 | 2 | 18-cell delta attribution (405 vs 423, owed from capture) | DC-13 hygiene | Rail-by-rail diff of two ledger dumps | free lane | OPEN |
| P-14 | 2 | Ledger endpoint performance (10s baseline; 31-145s under load) | DC-13 | Timed GET under write load | free lane | LIVE: 31.5s on 08-12 |
| P-15 | 2 | SWEEP remediation: high-sev silent fallbacks + 14 tests-that-cannot-fail + 16 CI greppable fail-closed patterns as hooks | all DC | CI checks merged; each fix lands with failing-first test | free lane; must not touch A2-dirty files | DOC: 0 fixes applied |
| P-16 | 2 | SD midpoint-PIP verdict on the running apply; re-verify pass if confirmed | DC-3 integrity | A2 midrun report + boundary-parcel query | A2 answer | OPEN |
| P-17 | 3 | Roads statewide ingest GO/NO-GO ruling, then per-county apply + scorer | DC-3 | Ledger roads x254 | operator ruling | LIVE: x254 not-yet, writer bound |
| P-18 | 3 | Cert frame reconciled: block13 re-earn in true frame, W3 close artifact, OPS-11 cleared marker | DC-10 | The three DC-10 sub-checks | free lane | UNGRADED this arc |
| P-19 | 4 | Factory 1.5 front half build (handoff Q1 spec: versioned runner, catalogue queue, folder recursion, typed taxonomy, fail-closed, resume, close artifacts) | DC-6 zoning | Proven on 5-source-type verification set per spec | operator go | OPEN; the main gap |
| P-20 | 4 | Q7: four Z1 corrections + adopt seven-status absence taxonomy (decision record + roster schema). Houston layers planner-verified live 08-12: pubgis02 layer 13 = 722, layer 12 = 195 | DC-6 | Roster diff: Deer Park staged, Houston reclassified never-as-zoning, Webster HOST-BROKEN, 84 rows to NOT-FOUND-UNKNOWN-WHY | operator go | UNBLOCKED (re-probe done) |
| P-21 | 4 | Z1 re-probe of the 84+ unknowns with the real factory | DC-6 | Per-city typed verdicts artifact | P-19 | BLOCKED by design |
| P-22 | 4 | Zoning depth: stage, drain, stamp, score across the 28 footprint counties | DC-6 | Ledger zoning satisfied in footprint | P-19, P-20 | LIVE: 1 county real |
| P-23 | 4 | Envelope depth in footprint (needs zoning + geometry) | DC-6 | Ledger envelope in footprint | P-22 | LIVE: x254 not-yet |
| P-24 | 4 | Easement rail: satisfied or honest-absent path in footprint (writer merged per E1) | DC-6/DC-7 | Ledger easement | slot + scoping | LIVE: x254 not-yet |
| P-25 | 4 | CAD depth beyond the 15 counties: bulk_primary routing + CAMA parsers (Dallas/Tarrant 0.0% sqft) + footprint counties without CAD ruled honest-absent | DC-6 cad/owner/landuse | Ledger depth rails in footprint; store sqft % | free lane | DOC: registry knows, ingest ignores |
| P-26 | 4 | Utility lines probe (water/sewer/electric), probe-first, no rail assumed | feasibility scope | Probe artifact with per-source posture | operator go (Q2 spec) | SCOPED |
| P-27 | 4 | Address-to-parcel resolver (situs 99.3% populated; blocks API/paste-a-list/MCP lookup) | market surface | exact/ambiguous/not-found verdicts live | in/out of launch: needs ruling | SCOPED |
| P-28 | 5 | G1 hard gate: Stripe "Hauska Pro" to Smart Site naming (ruled exception, may run now) | DC-11e | Stripe product export, zero "Hauska" strings | free lane | OPEN; hard gate |
| P-29 | 5 | 76j capacity: rate-limit store (Upstash dead, replacement owed), load test, capacity doc | DC-11a-c | The three DC-11 sub-checks | Layers 1-2 close | OPEN; regressed from v0 "done" |
| P-30 | 5 | smartsite.cloud domain attach + canonical-URL sweep + Q11 copy/brand batch | DC-11d | `curl -sI` 200/301; QA register zeroed | Layers 1-2 close | OPEN |
| P-31 | 5 | MCP finish: five remaining hardcoded lists (per MCP1 close) + rate-limit dependency | market surface | MCP1 artifact list zeroed; live probe | Layers 1-2 close | DOC: chain 15/16 live |
| P-32 | 5 | Report engine + audience profiles + comparison mode | market surface | Per Q10 scope doc | Foundation+Measurement close | SCOPED; do not start |
| P-33 | 5 | Smart Files artifact store | market surface | Per Q11-handoff scope | P-32 | SCOPED; do not start |
| P-34 | 5 | Vercel to GCP migration | post-gate | Per Q12 scope | gate close | SCOPED; do not start |
| P-35 | 5 | Retrieval-key durability (hand-synced in two places; broke MCP once, PE once) | launch stability | Key sourced from workflow/shared secret ref | free lane | OPEN; cheap, hook-shaped |
| P-36 | 5 | Gate close grading run: DC-1..13 graded by instrument, DC-13 verbatim snapshot, launch sign-off | ALL | The done-card itself | everything above | THE FINISH LINE |
| P-37 | - | Hygiene: `_STATE.md` regenerated on the five-layer frame with ONE queue register; doc_repo session-close commit; engine uncommitted sweep post-A2 | plan integrity | git log + single register in `_STATE.md` | A2 release (engine part) | OPEN |
| P-38 | - | Old-register decisions ruled in or explicitly retired: E3 Elgin apply, OZ crossfilter branch, IDX index audit, harvest take-list | plan integrity | Each gets an amendment row here | operator rulings | OPEN; currently orphaned between registers |

## Write-path tee order (advisory; resequencing is not a scope change)

The atoms slot is the long pole. The goal is that slot occupancy is pure write time: every apply enters the slot with a merged writer on main, dry-run evidence, a ready runner, and a preflight gate.

Current chain: A2 SD (FLIGHT) -> A2 rail-corridor -> RELEASE -> flood metros (P-07) -> flood remainder (P-08) -> footprint (P-09) -> rrc-wells (P-11, gated on P-10 merge) -> rrc-pipelines (P-12) -> mud scorer (gated on P-04 ruling) -> easement/envelope applies as they become ready.

## AMENDMENTS (append-only; never edit the baseline)

| # | Date | Row(s) | Change | Why | Operator-ruled |
|---|------|--------|--------|-----|----------------|
| A-000 | 2026-08-12 | all | Baseline v0 (the 08-09/10 five-layer table) superseded by v1; deltas recorded in the Lineage section | Program reset on the five-layer frame after drift | Y |
| A-001 | 2026-08-12 | P-03, P-16 | A2 SD apply HALTED at operator ruling 21:16Z (verified dead 21:17:32Z; 29 counties / 1,245,279 atoms / 0 verifyFailures landed; 48061 mid-write re-enters). P-03 re-scoped: resume only after the SF-6 membership fix lands; the fix is prescribed as PostGIS zone-major true-geometry membership (the flood #315 pattern), which is also the speed fix | SF-6 confirmed in the running writer (A2 midrun report); correction after statewide completion would strand stale wrong-entityId absence atoms | Y |
| A-002 | 2026-08-12 | P-04, P-05 | RULED: mud is a special-district TYPE. The `mud` rail is served by `special-district-fact` atoms (district_type subcategorization in the body, per the R1 split rule); no separate mud build ever; declaration refresh + refreshCli + cortex redeploy, then the SD scorer may run | Same source and geometry; splitting would build the same subject twice | Y |
| A-003 | 2026-08-12 | P-17 | RULED: GO on roads statewide ingest. Queued in the write-path tee order after footprint | Writer bound, synthetic-id landmine closed, Bastrop held at 19,907 | Y |
| A-004 | 2026-08-12 | P-09, P-15 | NEW WORK from L4 tee: the merged footprint writer IGNORES the staged geometry-true join and would reintroduce the 59.5% bbox false-positive rate on apply. Footprint apply is BLOCKED until a staged-table consumer lands; the build is assigned to L5 (addendum) with file ownership over the building-footprint writer | L4 finding (close artifact 2026-08-12_L4_slot_chain_tee_close.json); the bbox join is the exact defect class the staging existed to remove | Y (operator put L4 in flight; blocking finding) |
| A-005 | 2026-08-12 | P-34 | P-34 decommission scope addition: revoke the hauska-map VERCEL_TOKEN and delete the PE Vercel key-sync workflow leg when PE moves to GCP. Token is bridge material with 60-90 day expiry; the SM-source-of-truth wiring from P-35 is the migration end-state and survives | Keeps the L6 bridge from outliving its purpose | Y |
| A-006 | 2026-08-12 | P-21 | P-21 scope note: Z1's Dallas metro rows (158 lowercase searched-and-absent, outside L3's owed set) re-enter through the factory re-probe alongside the NOT-FOUND-UNKNOWN-WHY queue | L3 close flagged them; same treatment, same machinery | Y |
| A-007 | 2026-08-12 | P-29 | RULED: the rate-limit store restore dispatches NOW as a regression restore (v0 recorded it done; the Upstash DB died after), not new Layer 5 work. The layer rule stands for everything else in Layer 5 | DC-11a fails today on the in-memory fallback; restoring a regression is not opening the layer | Y |
| A-008 | 2026-08-12 | P-38(E3) | RULED: E3 Elgin warm apply SUPERSEDED and retired. Elgin zoning depth flows through the Factory 1.5 staging seam (3,209 rows already staged) and the drain; the old warm-apply path with its unresolved 75/98-vs-102/72 parity anomaly is closed, not fixed. E3-ADV re-review no longer owed | Two paths to the same data is how parity anomalies breed; the staged path is the ruled architecture | Y |
| A-009 | 2026-08-12 | P-01, P-03 | NEW DRAIN LEG from L7: Harris 48201 has ZERO parcel-node atoms despite the full 1,523,641-feature txgio reload — reloaded after the sweep sizing snapshot, so it never entered the sweep queue and "132/132 complete" hid it. Harris parcel-node apply + geometry rescore added to the write-path tee order (before flood metros, same slot chain) | L7 close; the count-based-gate hole class, caught by the honest-absence instrumentation | Y |
| A-010 | 2026-08-13 | P-03, P-05, P-06 | ROGUE EVENT: the lost A2 agent resumed after the formal close and ran the SF-6 apply across 204 counties (~18,529,461 special-district-fact atoms, skipping 48439) plus a 5-county rail-corridor partial (~183,575 atoms) on the pre-#324 silent-default classifier, before its planner killed it (~00:50Z) and filed `_inbox/2026-08-12_A2_late_addendum.json` through the tombstone path. BOTH products ruled CONTAMINATED: SD atoms carry bbox-midpoint membership (L1 pilot measured 0.70% parcel flips incl. 340 false absences in one county); rail atoms predate the fail-closed classifier. Remediation = L1's proven delete-then-drain per county (pilot: zero post-drain flips, 1,397 atoms/s); rail partial scrubbed/re-applied under L1 custody with contract 1.22.0 semantics | The tombstone worked (evidence filed, no second restart, rogue runner killed) but not before the write; slot law needs a process-level guard, not only a file | Y (operator-ruled halt lineage; evidence-driven) |
| A-011 | 2026-08-13 | P-03, P-06 | ROGUE EVENT ESCALATION + NEUTRALIZATION: the rail runner resurrected TWICE more (rekill 08:06Z at 142 counties; third kill ~11:57Z by planner mid-48201 via a PowerShell watchdog). Final rogue rail inventory from its own progress file: **204 counties / 9,867,600 rail-corridor atoms**, present ratio 99.4% — itself anomalous (rail adjacency cannot be near-universal; suspect absence-emission or buffer-predicate defect in the pre-#324 run, on top of the classifier). Rogue SD inventory stands at 204 / 18.53M. Vector neutralized: all seven A2 runner scripts quarantined and replaced with exit-86 stubs. L1's remediation roster MUST derive from store truth (DISTINCT counties per family), never from progress artifacts | Third resurrection proved the file tombstone insufficient against a process that never re-reads it; stubs make relaunch structurally harmless | Y |
| A-012 | 2026-08-13 | P-10+, P-38(IDX), P-38(OZ), P-26, P-25, P-27, DC-11a | OPERATOR RULINGS BATCH: (0) slot-lease enforcement APPROVED — DB-enforced writer lease added to the drain machinery scope (folded into the drain-chain dispatch, not an interrupt to L1); (1) DC-11a instrument re-pointed to the MCP health endpoint (correction; as-written it could never pass — cortex has no limiter); (2) atoms_section_number_idx partialize-then-drop APPROVED, planner-executed, concurrent build, before the heavy drain legs; (3) L12 OZ rebase APPROVED to merge with the one-line test retarget; (4) utility who-serves territory set APPROVED as ACQUISITION ONLY (stage PUCT water CCN, PUCT sewer CCN zip, HIFLD electric territory, TWDB PWS, TCEQ districts with provenance; NO rail, NO product surface pre-gate; professional-tier product idea NOTED as a post-gate consideration — all post-gate product items get a dedicated consideration pass); (5) L9 vintage-spec PR APPROVED as a small lane; full loads announce-serialized after it merges; (6) P-27 ruled OUT of gate, FIRST post-gate build — planning captured in `_decisions/2026-08-13_p27_address_to_parcel_post_gate.md` so it is never rehashed | Operator walk-through 2026-08-13 | Y |
| A-016 | 2026-08-14 | P-36, DC-1 | INSTRUMENT CORRECTION (operator-approved): DC-1's SQL cross-check compared `COUNT(county_rail)` against `totalCells` (3,556) while the card's own architecture note says `county_rail` holds the 14-row RAIL dimension and cells materialize via CROSS JOIN — internally contradictory, could never pass. Corrected to `COUNT(county_rail) === totalRails` alongside the unchanged declaration check and arithmetic identity. Gate doc + harness both edited; nothing loosened — three independent sources still must agree | The harness's first fresh run surfaced it (evidence showed all real numbers agreeing under a FAIL); same class as the DC-11a re-point | Y |
| A-013 (renumbered; lane-authored, was duplicate A-007) | 2026-08-12 | P-18 | P-18 / DC-10 CLOSED_PASS by instrument (was UNGRADED). W3 close + named `block13-cert-grade` workflow + OPS-11 CLEARED + session naming block13 re-earn. Live 7/7 in raw txgio frame; eng #327 @ b2a8706 | L8 lane closed the evidence trail the gate amendment grades | Y (operator put L8 in flight) |
| A-014 (renumbered; lane-authored, was duplicate A-008) | 2026-08-12 | P-26 | P-26 CLOSED (probe-only). Artifact `_inbox/2026-08-12_L10_utility_probe_close.json`. Finding: v1 utility feasibility shape is territory who-serves (PUCT water CCN + HIFLD electric retail) with SERVICE-LETTER-REQUIRED residual; municipal mains/laterals are opportunistic city-scoped depth, not a uniform rail; electric distribution geometry is not a public statewide product. No cells / no writers proposed | Operator Q2 probe-first rule; L10 lane | Y (operator put L10 / P-26 in flight) |
| A-015 (renumbered; lane-authored, was duplicate A-010) | 2026-08-12 | P-29 | Store restore CLOSED. Serving MCP `00063-fic` `/health` `rate_limit_store.state=ok` (postgres, fail-degraded outage mode). DC-11a as written still points at cortex `/health` (SPA HTML, no field); the limiter instrument is MCP `/health`. DC-11b load test and DC-11c capacity-doc freshness remain. Close `_inbox/2026-08-12_L13_rate_limit_restore_close.json` | L13 live proofs (health + 429 + restart survival); inventory proved cortex/retrieval do not share the seam | Y (operator put L13 / A-007 in flight) |
| A-016 | 2026-08-14 | P-29 | DC-11b load test and DC-11c capacity-doc freshness CLOSED. Dated artifact `_inbox/2026-08-14_76j_C4_loadtest_results.json` on serving `00063-fic`; capacity doc `_inbox/2026-08-05_launch_capacity_audit.md` `last_updated: 2026-08-14`. DC-11d (P-30) probed same lane: HTTP 200 is GoDaddy parking lander, not Vercel attach; DNS fix out of scope. Close `_inbox/2026-08-14_l19_close.json` | L19 live proofs (1/5/20 rps + 429 band=rpm + 36/36 postgres primary); first_429 cumulative 72 is a wall-clock minute-bucket finding, not a store miss | Y (operator put L19 / P-29 residue in flight) |
| A-017 | 2026-08-17 | P-17, P-25, P-36 | RULED: QA and launch on the current map. Remaining statewide PBF roads and CAMA loads are post-launch backfill. Do not restart Harris statewide-PBF. Do not treat roads 254/254 or Dallas sqft/year as launch blockers. DC-3 uniform-rail 254/254 for roads is no longer the L26 close condition. Decision `_decisions/2026-08-17_qa_launch_current_map.md` | Operator: marketing and checkout first; redesign backfill later. Three Harris PBF extracts wrote 0 atoms | Y |
| A-018 | 2026-08-18 | P-39 ADDED, P-40 ADDED, P-41 ADDED, P-42 ADDED, P-43 ADDED, P-44 ADDED | **Smart Site QA remediation program opened at Layer 5, six rows.** The 2026-08-18 operator QA pass filed 21 numbered defects against smartsite.cloud. Planner traced every one to source in hauska-map and hauska-engine: they are six defects, not 21. P-39 fact-sheet unification (one subject store, one resolver, verdict consolidation, exports bound to factSheetId). P-40 consumer surface (internal engineering strings off the card, honest-absence redesign, provenance demoted to disclosure, source-encoding guard). P-41 map legibility (labels over satellite, zoning and flood palettes with legends, subject marker). P-42 instruments (measure and shape, panel manager, location fly-to-point, mobile sheet collapse, notes). P-43 statewide serving sweep, report first. P-44 County Manifest refreshed to current state plus the serving-sweep panel on its subtab. Contract frozen by the planner before dispatch at `_catalog/parcel_fact_sheet_contract/`; the six invariants are binding and a lane that disputes one stops and reports rather than changing it. Command Center is a CONSUMER of Smart Site per operator ruling, so P-39/P-41/P-42 land in shared substrate and CC inherits. | Operator ruling 2026-08-18: golden-parcel regression sets are REFUSED (sampling narrowed scope on Bastrop and certified a broken county once); the instrument is a statewide sweep of what the product SERVES, which is a different question from the County Manifest's did-a-writer-run. P-27 records situs as 99.3% populated while the operator has missing addresses on single-family parcels in at least Bastrop (48021:36521 = 1503 Farm St on the CAD roll) and Travis (17005 Simsbrook Dr, Pflugerville). Both cannot be true; P-43 settles it, and the follow-on is per-county source review and re-ingest | Y |

| A-019 | 2026-08-19 | P-45 ADDED, P-46 ADDED, P-43, P-44 | **Operator ruling: nothing is hand-rolled, ever. Every data fix is approached as macro coverage, statewide, from the start.** The Bastrop early-days lesson restated as standing law and applied to this programme: the planner's proposal to fix Bastrop's address adapter first and then sample three or four more counties is REJECTED. Distinction preserved so both lessons survive: one county end-to-end is for validating an INSTRUMENT (SS-W5 caught its own address predicate that way); it is never a pilot for a FIX. P-43 and P-44 are NOT closed by the 2026-08-18 lanes. P-43 delivered 2 counties of 253 (866,857 parcels of 13,071,975, 6.6%); statewide remains, as a scheduled background job. P-44 built the serving-sweep panel but nothing feeds it: `GET /api/serving-sweep` on cortex-api returns the SPA fallthrough, so the panel renders an honest not-served state. **Planner-owned seam error:** the record shape was frozen so P-43 and P-44 could run in parallel, and no lane was assigned the pipe between producer and consumer. P-45 ADDED: statewide data reconciliation programme for the DUPLICATE-SUBJECT class, opened by tier2 vs flood-hazard-fact disagreeing AO/AE, same shape as A-002's mud vs special-district ruling. Detector first (find every duplicate-subject divergence statewide), then reconcile, then RETIRE the loser rather than maintaining both. P-46 ADDED: map layer controls — road nodes toggle defaulted OFF, and RRC layer toggles across its several layers. | Operator, on reading the deployed console: the County Manifest must be a LIVE FEED, not a 4-day-old snapshot, and the console exists so defects of this kind are spotted rather than hand-found. Two stores of one subject is judged a by-product of an evolving product, so the answer is a reconciliation programme rather than a per-case adjudication | Y |

| A-020 | 2026-08-19 | P-47 ADDED | **P-47 ADDED: the SCORER CAPABILITY GAP, opened as its own row at operator ruling.** Measured by lane SS-W9 and verified independently by the planner against the live ledger. hauska-engine ships **twelve county writers**; legacy-design-tools ships **three scorer CLIs** (`countyCoverageScoreCli.ts` writes land-use, zoning, envelope only). Most existing ledger rows came from ONE-OFF LANE SCRIPTS (`score_cad_rails_fast.mjs`, `l16-score-mud.mjs`, `B2_cp2_geometry_scorer_apply.mjs`), not checked-in scorers — which is why no recompute route ever existed. Nobody removed it; scoring was never a standing capability. **Six of fourteen rails have ZERO rows in `county_facet_coverage`** (roads, footprint, easement, rrc-wells, rrc-pipelines, rail-corridor): 1,524 cells, **42.9% of the grid**, permanently not-yet regardless of what the store holds, with **35,159,990 atom rows** behind them that no recompute can reach. Planner-verified: those six rails have zero cells carrying a source AND zero carrying a coverage pct across all 254 counties. Two correctness defects ride the same row: the zoning scorer records 0.00% for Travis, Bexar, McLennan, Guadalupe and Bell while Smart Site SERVES zoning for those counties (planner live probe, `48453:149405` returns district SF-2, facetCoverage.zoning true); and `countyCoverageScoreCli.ts` writes facet `land-use` while the rail key is `landuse`, orphaning 19 live scorer rows. Across the grid, 1,170 of 3,556 cells hold atoms with no satisfied ledger cell — **59,744,770 atom rows invisible to the instrument**. | Operator ruling 2026-08-19 after the six-lane QA programme: the scorer gap is the largest single structural defect found and must not live inside the audit row. The portfolio's measurement problem is not staleness and not acquisition — the ledger cannot see data we already hold, because scoring was built as lane scripts rather than as a capability | Y |

| A-021 | 2026-08-21 | P-48 ADDED, P-49 ADDED, P-50 ADDED, P-51 ADDED, P-52 ADDED, P-53 ADDED, P-54 ADDED, P-55 ADDED, P-56 ADDED | **SmartSite remaining board opened as nine rows.** Operator 2026-08-21 evening: all remaining work is on the board (HOLD families, Wave A/B/C, OPS-18 OPEN). This is not a reopen of sellable. P-48 S1 special-district (`mud-pid`) serve. P-49 S2 `texas-rrc` / pipeline serve. P-50 S3 well-fact serve. P-51 S4 building-footprint serve. P-52 S5 rail-corridor serve. P-53 S6 property-boundary-edge serve. P-54 S7 owner-fact identified inspect only (never anonymous). P-55 Wave C identity writers (integer grammar, sentinels out of keys, `externalKeys`, `applies-to`, verified-absence pair unfed). P-56 geometry `48135` named denom excluding 3791 retired, then score and recompute (DC-2). Fan plan `90_operations/OPS-18c_parallel_execution.md`. WDLL `_inbox/2026-08-21_ops18_all_board_WDLL.md`. Decision `_decisions/2026-08-21_all_board_parallel_execution.md`. COVER holds the one `--apply` slot. SERVE does not `--apply`. A-017 Harris PBF stays NO. A2 honest-absent stays HELD until L7 vs pair. | No P-row existed for turning on HOLD families or Wave C writers. Geometry `48135` score after P-02 apply still lacked a named denom that excludes the 3791 retired rows. | Y |
| A-022 | 2026-08-22 | P-17, P-47, P-49 through P-55 | **RULED: COVER `--apply` PARKED.** Foreground is SERVE P-49 through P-54, IDENT P-55, DC-3 instrument P-47, then planner deploy for visual QA and GTM. P-17 remaining-roads halt after 48371; do not start 48373. Resume from `_inbox/2026-08-22_p17_roads_park_pickup.md`. Do not treat roads 254/254 as a QA or GTM gate. A-017 Harris PBF stays NO. Decision `_decisions/2026-08-22_serve_ident_then_background_cover.md`. | Operator: roads is least valuable for humans; map live and Manifest honest first; COVER fill is background. | Y |
| A-023 | 2026-08-22 | P-57 ADDED, P-58 ADDED, P-59 ADDED, P-60 ADDED, P-61 ADDED, P-62 ADDED | **Full atom surface program opened.** Operator 2026-08-22: all atoms wired and rendering on map and displaying in CC is the goal. P-57 live probe audit (gold + four counties). P-58 code-read matrix + gap backlog. P-59 A-020 scorer specs for six unspecified rails. P-60 SmartSite map layers for spatial atom families. P-61 inspect remainder (land-use atom, P-52 rail if scout GO). P-62 CC manifest parity after scorers + recompute. WDLL `_inbox/2026-08-22_atom_full_surface_WDLL.md`. Phase 1 audit only until close; COVER roads stays parked. SERVE near-term card remains closed slice. | Operator visual QA showed CC unchanged and random-parcel gaps; inspect-only SERVE is not full-surface done. | Y |
| A-024 | 2026-08-22 | P-63 ADDED | **Verdict layer serve opened.** Operator 2026-08-22 evening: replace HTTP 200 empty fact chains with typed layer absence verdicts per `19_the_instrument_contract.md` (`absent-verified`, `lookup-failed`, `not-applicable` + authority, scopeSearched, asOf, basis). Metro structural without CAMA load = `lookup-failed`. Unincorporated no zoning = `not-applicable`. Ships in cortex-api + PE inspect before CAMA ingestion. WDLL `_inbox/2026-08-22_verdict_layer_serve_WDLL.md`. Decision `_decisions/2026-08-22_verdict_serve_operator_go.md`. P-59 scorer plumbing may parallel; scorer boolean semantics blocked until verdict live. | Thesis planner + operator: empty-success is false statement about the world; only board item fixable without ingestion. | Y |
| A-026 | 2026-08-24 | P-73 ADDED, P-74 ADDED, P-75 ADDED, P-76 ADDED, P-77 ADDED, P-78 ADDED, P-79 ADDED, P-80 ADDED, P-25, P-09 | **Parcel public-facts write-path program opened.** Execution instrument `_inbox/2026-08-24_parcel_facts_write_path_game_plan.md`. WDLL `_inbox/2026-08-24_parcel_facts_write_path_WDLL.md`. Decision `_decisions/2026-08-24_write_path_data_capture_order.md`. **P-73** A3 ingest-bound field map (doc; gates new ingest). **P-74** situs sentinel bind (PE; isolated tree, not A2). **P-75** who-serves serve-time read over L22 staging. **P-76** city-limits ingest + incorporated/unincorporated PIP; ETJ stays unresolved. **P-77** Travis join measure + honest lookup-failed (not the 51% fix). **P-78** CAMA-vs-StratMap authority rule + `landuse.ts` YEAR_BUILT/GIS_AREA. **P-79** REST harvest writer (GEO_ID, plat, deed, school). **P-80** Travis join fix (crosswalk/TCAD + rebake). P-25 CAMA full loads stay on that row and start only after P-73+P-78; Travis CAMA after P-77 measure. P-09 footprint drain stays heavy (10.67M `tx_building_footprint`, not L20 zoning counts). Phase 2 and feasibility §6 no longer sequence ingest. A-017 and A-022 stand. | Operator: order by write-path dependency; quick bind first; heavy last. | Y (rows opened; Wave 1 go is A-027) |
| A-027 | 2026-08-24 | P-75, P-76, P-73, P-60 | **Operator go on Lane 3 Wave 1 bind cards and on 3b checkout.** P-76 is the ETJ adapter card honestly scoped: city-limits PIP, ETJ unresolved, no fabricated buffer. P-75 is the who-serves promotion build (serve-time PIP over L22 staging; decision `_decisions/2026-08-24_who_serves_promotion.md`). Dest names `_inbox/2026-08-24_p73_dest_bind_etj_who_serves.md`. WDLLs `_inbox/2026-08-24_lane3_p76_city_limits_WDLL.md` and `_inbox/2026-08-24_lane3_p75_who_serves_WDLL.md`. **P-25 CAMA and P-09 footprint stay not-parallel.** P-74 situs and P-77 Travis measure are named, not this go. **P-60 3b** checkout WDLL approved (`_inbox/2026-08-24_rebrand6_checkout_3b_WDLL.md`): Custom Checkout, isolated LDT then isolated hauska-map. | Operator: red card approved; go on 3b; put screenshot Lane 3 items in flight. | Y |
| A-025 | 2026-08-23 | P-63 CLOSED, P-64 ADDED, P-65 ADDED, P-66 ADDED, P-67 ADDED, P-68 ADDED, P-69 ADDED, P-70 ADDED, P-71 ADDED, P-72 ADDED | **Instrument conformance program rows opened (operator go 2026-08-23).** P-63 **CLOSED** — livePass true, close `_inbox/2026-08-23_p63-verdict-serve_close.json`; not-applicable ships behind declared interim applicability table `_inbox/2026-08-23_p63_interim_applicability_table.json`. **P-64** T5 four-properties score (filed `_inbox/2026-08-23_property_four_properties_score.json`). **P-65** T5 enumerated-family provenance sample flood CONFIRMED (`_inbox/2026-08-23_enumerated_family_provenance_sample.json`). **P-66** T1.3 classify 21 entity types (serve-layer, no ingest). **P-67** T1.4 demote enumerated families phase 1 flood+rail (selector atoms + derivesFrom; no delete). **P-68** T1.6 lineage edges after node minting. **P-69** T1.7 declare unrecoverable pre-contract rows. **P-70** T2.17–T2.18 Smart Files membership (`knowledgeAt`, no cascade). **P-71** T2.19 audience-blind content function. **P-72** T2.20 import accessPolicy from contract (substrate generates CHECK + CI drift test). Substrate requests filed `_inbox/2026-08-23_substrate_seat_requests_property.json`. P-59 scorer semantics **unblocked** post P-63 close. | Operator go with planner recommendation on instrument brief 2026-08-22; P-63 customer-done unlocks P-59. | Y |
| A-028 | 2026-08-26 | P-81 ADDED, P-82 ADDED, P-83 ADDED, P-84 ADDED, P-47, P-55, A-012 | **Ingest FROZEN on the operator PC; atoms county loader program opened.** Decision `_decisions/2026-08-26_ingest_freeze_and_cloud_loader.md`. Design `_inbox/2026-08-26_cloud_loader_design.md`. WDLL `_inbox/2026-08-26_cloud_loader_WDLL.md`. Measured 2026-08-26: live W5-A apply at 21 atoms/s because `writePropertyAtomsBatch` issues one `atom_links` INSERT per atom at 44 ms RTT (engine #356, never benchmarked); the county-partitioned lease handoff is rejected as the next move. **P-81** Cloud Run Job `atoms-county-loader` in `us-east4`, image pinned by digest, derived work list, holds as data, status CLI, scheduler. **P-82** set-based write path: COPY stage, chunked merge with server-side `applies-to` edges, set-based hash verify, bounded memory, per-leg timing, benchmark on the real path. **P-83** scoped lease v2 `(entity_type, county_fips)` with process-bound token and data-side scope check, heavy-scan scope, v1 retired by refuse, AGENT_CONTRACT section 3 amended when it lands (A-012 intent preserved and tightened). **P-84** `atoms_load_runs` run ledger (every run and refusal a row), `score_requests` drained by an LDT Cloud Run Job into `county_facet_coverage`, cost per run. Build order P-82, P-83, P-84a, P-81, first proof on two small counties, P-84b, Texas remainder unattended. W5-A finishes Bexar cad then releases; no further `--apply` from `P:/`. Out: `atoms` partitioning (own ADR), parcel-node rail on the loader, Neon read replica, acquisition automation, `countAtoms()` health scan (substrate seat), P-09, COVER, P-25, Factory 2, CTX / national. | Operator 2026-08-26: "stopping everything on the ingest ... reengineer this process and get it up to a cloud server off my PC ... fail proof ... turn it on and let it run." Two objectives, equal weight: finish Texas and build the machine that finishes any state. **Reviewed 2026-08-26** (`_inbox/2026-08-26_p81-review_close.json`): six fatal findings accepted; sequence is batched links first then measurement; no UNLOGGED stage; verify against writer counts; batch path not retired; holds are Factory store rows imported from the routing pin, and which rail holds lift for the drain is a pending operator amendment. These rows are the Texas drain under **OPS-19 F-02** (`90_operations/OPS-19_factory_plan_of_record.md`); W5-A stopped at Bexar cad 660,000 of 703,257, the remainder is the resume proof. | Y (direction); card text pending operator read |
| A-029 | 2026-08-26 | P-81, P-82, P-83, P-84, P-25, P-09, P-79, P-80 | **Option A ruled on the existing write path** (`_decisions/2026-08-26_factory_model_law_and_option_a.md`). P-82 is re-scoped to P-82-lite (multi-row links, per-leg timing, real-table benchmark) **plus BP-WRITE-01** (refuse a non-canonical node binding or a sentinel in a key at the store boundary; `_blueprint/40_rule_register.md`). Bexar 48029 cad finishes its resume on the current shape. **No new county is written on the old shape**: Harris 48201, Dallas 48113 and the Texas remainder are HELD to the conformant stage E writer (OPS-19 F-15 contract types, F-16 resolution, F-18 intensional demotion). The Texas cleanup classes on P-25, P-09, P-79, P-80 are worked under OPS-19 F-10 on the new path as T1.1 to T1.7 plus V1 to V15. The Factory is built to the model law (`19_the_instrument_contract.md`, `_blueprint/`, `51_ingestion_pipeline_reference.md`, `24_instrument_conformance_program.md`), not to the store's current shape. | Operator 2026-08-26: "Fix the path, freeze new fills on it ... every new fill, Texas remainder included, waits for the conformant writer. Texas takes longer; nothing new is written that P-69 will later have to declare unrecoverable." | Y |
| A-030 | 2026-08-26 | P-85 ADDED, P-79, P-28 | **P-85 ADDED: Central Texas courthouse-recorded easements on Smart Site.** Six counties: Bastrop 48021, Travis 48453, Williamson 48491, Hays 48209, Caldwell 48055, McLennan 48309. First-party acquisition by Public Information Act under LGC 118.011(e) (ADR-027; no TexasFile); six PIA letters drafted `_inbox/2026-08-26_p85_pia_letters.md`; landing and extraction start now on the Factory's L1/L2 and stage A/B (OPS-19 F-09, F-16); atomisation only through the conformant stage E (F-15, F-18) per option A; publish through F-06 to F-08 with staging first. Three-state promise per parcel: `populated`, `absent-verified` with scope, or `lookup-failed`; never a blank. **Easements are a Studio entitlement** (Free and Solo see withheld, no count). McLennan reconciliation is an item before any attachment there. WDLL `_inbox/2026-08-26_p85_central_texas_easements_WDLL.md`. Decision `_decisions/2026-08-26_p85_easements_studio_tier_and_six_counties.md`. Store fact at carding: zero courthouse instruments in `hauska_mcp`; three engagement uploads in `neondb`; MCP encumbrance tools read uploads only; PE has no easement facet; feasibility row 11 had no card. P-79 REST harvest (plat identity fields) is a resolution input, not absorbed. P-28 naming gate unchanged. | Operator 2026-08-26 after an Austin Smart Site event: prospects asked for easements; "go"; "5 + McLennan"; "easements as part of the Studio plan." | Y |
| A-031 | 2026-08-26 | P-85, P-60 | **P-85 re-shaped the same evening.** Online first: city and CAD easement GIS layers land and join now; clerk portal indexes are searched per parcel where each portal's terms, read and recorded per county, permit it. **Deep Easement Research** is an asynchronous Studio product: click on a parcel, background job (index search, instruments and plats bought at the clerk's price or queued to a human clerk run, vision read, clause extraction, corridor geometry as a Derivation with confidence), email on completion, results in the parcel's property records and the chat, drawn on the map; written through the existing tenant-scoped recorded-instrument path so it ships without the Factory's public writer. Images never fetched around a paywall; three verdicts, never an empty list. PIA letters drafted and **deferred** to a bulk step the operator times (Phase C); public-layer promotion is Phase D on OPS-19 F-15, F-16, F-18. Dependencies named: a transactional email provider (none wired) and Studio-level tier granularity from the P-60 Stripe live lane. Decision `_decisions/2026-08-26_p85_deep_easement_research_product_shape.md`. | Operator: "get whatever we can online today ... a deep easement research scraping tool ... premium product as part of Studio ... not an instant thing ... email ... property records ... our AI reasons over it and draws it on the map ... letters in bulk later." | Y |
| A-032 | 2026-08-26 | P-85 | **P-85 scope broadened to every recorded document; product is Records Request in the Reports area.** The run brings back every instrument the clerk's index ties to the parcel (deeds, deeds of trust, releases, liens, easements, rights of way, plats, restriction declarations, association notices, affidavits, lis pendens, trustee-sale notices, powers of attorney, lease memoranda, mineral instruments); extraction depth by type (clauses and geometry for the constraining types, header facts for the rest); purchase-threshold ask; unrecorded material named, never absent; **not a title plant, not a title opinion** (per-request only, TDI deferral in ADR-027 intact); ADR-020 `instrumentType` enum extension requested of the substrate seat. Instant sources queried live, no ingestion; browser agent with a recipe per portal vendor; first proof Tyler on Williamson and Hays. Decision `_decisions/2026-08-26_p85_records_request_scope.md`. | Operator: "instead of just easements we should be looking at all the recorded documents ... HOA docs and many other things ... grab all of it ... part of our reports dropdown." | Y |
| A-033 | 2026-08-26 | P-72, P-81, P-82, P-83, P-84 | **E-1 / E-2 accessPolicy fail-closed lane CLOSED on the writer half; the database half and the deploy are OPEN.** Engine PR #361 merged as `cfa18bc` 2026-08-24T22:47:11Z (`typecheck + test: success`), close `_inbox/2026-08-24_property_accesspolicy_close.json` (committed 2026-08-26; was cited and untracked). Merged: `resolveAccessPolicyOrRefuse`, producer stamping on every ingest path, read filters no longer widen absent to public-free, migration `010_drop_access_policy_defaults.sql`, resumable `backfill-icc-access-policy.mjs`; E-2 mock-LLM refusal (503, no fabricated blocker). **Planner-verified 2026-08-26T22:36Z: migration 010 is NOT applied on `hauska_mcp`** (`schema_migrations` lacks it; `atoms.access_policy` still `DEFAULT 'public-free'`), the ICC backfill has not run, and `hauska-engine-api` serves revision `00174-zus` built 2026-08-06, eighteen days before #361, so E-1's read-filter fix and E-2's refusal are merged, correct, and undeployed (ADR-030). BP-ACCESS-01 (V3) is therefore half closed. Routing: (1) apply 010 and run the ICC backfill as a **recorded Factory run** on `hauska_mcp` (F-03 run ledger, heavy-scan scope, snapshot stated), property seat, folded into the drain lane P-81..P-84 as a pre-step since it touches the same store and the same bypass table; (2) engine-api canary deploy from `cfa18bc` or later, planner-owned, on operator go; (3) substrate retires `hauska-mcp-server/src/access-policy.ts:87` only after (1) closes; (4) `chat.ts` `mode: "mock"` stays out of scope, named. Note: engine and MCP migrations share numbers in one filename-keyed `schema_migrations` (`010_rate_limit_counters.sql` is the server's); the Factory store keeps its own table. | Lane close relayed by the operator 2026-08-26; planner verification at source. | Y (recorded; deploy needs go) |
| A-034 | 2026-08-26 | P-85 | **Records Request path-to-live map filed** at `_inbox/2026-08-26_p85_records_request_path_to_live.md`: per-piece home, verified state, dependency, and the ten-step order to live. Lane is in flight on `legacy-design-tools` branch `feat/p85-records-request` with Phase A items 1, 2 and the item 4 schema written and untracked. Two binding branch facts: the lane branch was cut from `seat/property` (merge-base `1a55566b`, six ahead with already-merged P-63 commits, fifty behind main) and must be re-cut from `origin/main` before its first commit; the Smart Site half gets its own registered worktree `P:/seat-worktrees/property/hauska-map-records` on `seat/property-records` because the property hauska-map worktree carries the pricing lane's thirty-three dirty files. Critical-path operator inputs: portal ruling per county (Tyler pair first), transactional email provider, Stripe price ids for Studio and Team. PR #458 (`seat/property`, P-63) is stale and superseded by #459; property closes it. | Operator ask 2026-08-26: "whats left to get that live ... mapped out and stored durable". | Y |
| A-035 | 2026-08-26 | P-86 ADDED, P-87 ADDED, P-88 ADDED | **Smart Site agent distribution opened as three Layer 5 rows.** P-86 resolvable share links: mint a server-visible URL (hash tokens stay for humans); same instrument as HTML, markdown, or JSON; `smartsite.cloud/llms.txt`; share mint free on sign-in; locked full-fidelity rule is the done state (today's anonymous bake is a defect against it); share is a grant row (revoke one token without rotating the secret). P-87 Smart Site product MCP at `mcp.smartsite.cloud`, OAuth 2.1 + PKCE against the Smart Site account, Stripe tier as ceiling, exactly eight PE-proven tools, async report protocol, **Use in your AI** in the workbench; no key-paste fallback; Hauska MCP is not this connector (doc 51 listing/branding exception, named). P-88 vendor directory listings only after a live Claude Connect probe. WDLL `_inbox/2026-08-26_smartsite_ai_connector_WDLL.md` (draft; build waits on operator approval). Decision `_decisions/2026-08-26_smartsite_product_mcp_and_ai_connector.md`. Does not absorb P-31, P-32, or P-85. GPT-store, SEO parcel pages, and Zapier are not carded. | Operator 2026-08-26: explore AI distribution; Connect-my-AI button; "fresh MCP server, Smart Site functions only"; "yes lets get it planned out." | Y |
| A-036 | 2026-08-26 | P-72, P-81 | **E-1 database half CLOSED (A-033 leave-behind 1 done).** The drain lane applied engine migration `010_drop_access_policy_defaults.sql` on `hauska_mcp` (host `ep-lucky-truth-apodo8hr…us-east-1`, direct, not `neondb`) as recorded Factory runs (probe `6905003a`, apply `1186086b`) and ran `backfill-icc-access-policy.mjs`. Planner-verified by direct read at 2026-08-27T00:01Z: `schema_migrations` row applied 2026-08-26T23:53:53.551Z; `atoms.access_policy` and `document_ingest_atoms.access_policy` now have no default and stay NOT NULL; `jurisdiction_tenant = 'icc-model-code'` is 8,731 `platform-internal` and zero `public-free`. Consequences: BP-ACCESS-01 (V3) is closed at the store as well as the writer; the substrate seat's retirement of `hauska-mcp-server/src/access-policy.ts:87` is UNBLOCKED (their card; gated only on their own read of these numbers); A-033 leave-behind 2 (engine-api canary deploy carrying the E-1 read filters and the E-2 refusal) remains open on the operator's go, production still on `hauska-engine-api-00174-zus`. Memory `atoms-access-policy-default-still-live` rewritten to the closed state. | Drain lane CP1 `_inbox/2026-08-26_p81-loader_cp1.json`; planner reads at source. | Y |
| A-037 | 2026-08-26 | P-86, P-87 | **P-86 order bound and the P-87 authorization server named.** (1) P-86 item 1 runs grant row first (item 6), then a resolvable URL that carries the grant id; the HMAC stays on `/share#token` only; `request_records`, `check_request`, and item 8 wait on P-85 item 4. (2) P-87 housing amended to `legacy-design-tools` `artifacts/smartsite-mcp`; worktree `P:/seat-worktrees/property/legacy-design-tools-mcp` on `seat/property-mcp` registered ahead of creation. (3) The authorization server is **hosted, WorkOS AuthKit**: OAuth 2.1 with PKCE, dynamic client registration, refresh tokens, Google and Microsoft sign-in matching the existing OIDC BFF, and a documented MCP authorization-server role; Stytch Connected Apps is the named fallback if a live Claude custom-connector Connect cannot complete against AuthKit. Identity join rule: an AuthKit subject maps to a Smart Site user only through `peUserIdentities` by `(provider, subject)`, or by verified email on the same provider; the MCP server never creates a second account and never grants a tier the user's `peUserEntitlements` row does not carry. Pre-registered falsifier for the choice: item 10, one completed Claude custom-connector Connect resolving to the signed-in user id; if that fails twice, the fallback is used and this row is amended, not re-litigated. Vendor capability claims are as of the planner's reading and are verified at build time, not assumed. | P-86 lane request relayed by the operator 2026-08-26; planner ruling with the housing evidence from A-035's review. | Y |
| A-038 | 2026-08-27 | P-72, P-81 | **E-1 / E-2 DEPLOYED to hauska-engine-api (A-033 leave-behind 2 closed; E-1/E-2 fully closed).** Planner-owned canary on operator go. Source: engine `main` `cfa18bc`, exported clean by `git archive` (1,197 files, no `node_modules`) and built by Cloud Build `d91f175d` with `cloudbuild.engine-api.yaml` to `cloud-run-source-deploy/hauska-engine-api:cfa18bc`, digest `sha256:10ce385c6a5fc7ca79b3ba4e24b8f24549ecfcd954bedde578f326dcc8bc03b7`. Deployed by digest as revision `hauska-engine-api-00176-keb` at zero traffic, tag `e1e2`; revision read by field: same 22 environment entries as `00174-zus` (`AIR_FINDING_LLM_MODE` and `BRIEFING_LLM_MODE` `grok`, `DATABASE_URL` from secret), Ready. Canary smoke (file instrument, baseline run first against `00174-zus`): `/health` 200, `/ready` 200, `/v1/site-context/registry` 200 with `adapterCount` 30 on both; pre-registered falsifier `POST /v1/findings/generate` with `mode: "mock"`: old revision 200 with `producer: "mock"` (a fabricated empty result served from the public internet), new revision 400 `Invalid enum value. Expected 'grok' | 'anthropic'`. Traffic shifted 100 percent to `00176-keb` at 2026-08-27T00:22Z and re-read by field; production smoke repeats the 400. `scripts/product-surface-smoke.mjs` strict: 13 PASS, 3 FAIL on `envelope.sanity` (34073, 34785, 34017, `ringVerts=0`), which is the pre-existing P-60b leave-behind recorded 2026-08-24 (`_inbox/2026-08-24_p60b_inset_gate_fix_WDLL.md`; the envelope atom in the store carries no geometry and the facets path is atom-chain via retrieval, which this deploy did not touch). Rollback if ever needed: `gcloud run services update-traffic hauska-engine-api --to-revisions hauska-engine-api-00174-zus=100`. Note: the `envelope-canary` tag follows the latest revision and now points at `00176-keb`; it was not moved deliberately. | Operator go 2026-08-26; planner deploy and verification 2026-08-27T00:15Z to 00:23Z. | Y |
| A-039 | 2026-08-27 | P-72 | **FINDING: hauska-engine-api accepts self-declared gate-front headers from the public internet.** Found while preparing the A-038 smoke. `run.googleapis.com/ingress: all`, invoker `allUsers`, `ENGINE_API_GATE_TOKEN` unset in production, so the middleware's Bearer check never runs and the six `x-hauska-*` headers are validated by shape only. Verified by violation on `GET /v1/site-context/registry`: no headers 401; `x-hauska-product: cortex` with `x-hauska-access-tier: platform-internal` 200. Every `/v1/*` route, including findings and briefing generation on the project's LLM keys, is reachable at any self-declared tier. `hauska-retrieval-api` is not affected (401 both ways). Fix is configuration, not code: all four callers already send a Bearer when their environment carries it (`hauska-mcp-server` `HAUSKA_ENGINE_API_GATE_TOKEN`, substrate; `legacy-design-tools` `ENGINE_API_GATE_TOKEN`, property; `hauska-map` PE BFF `HAUSKA_ENGINE_API_KEY` or `ENGINE_API_GATE_TOKEN`, property; `smartcity-dashboards` Bearer, govtech). Order: mint one secret, set it on the four callers through their deploy configs and deploy them, then set and enforce on engine-api, then re-run the four probes expecting 401. Enforcing first is an outage. Not fixed in this session; the A-038 deploy neither widened nor narrowed the path. Full record `_inbox/2026-08-27_engine_api_gate_bypass_finding.md`; memory `engine-api-gate-headers-are-spoofable`. Routed: substrate seat owns the MCP caller and the engine-api enforcement step (engine-api is Hauska substrate); property and govtech set their callers' env on request. | Planner probes 2026-08-27T00:18Z. | Y (recorded; fix needs the three seats) |
| A-040 | 2026-08-27 | P-82, P-83 | **Drain lane lease v2 handback: Bexar write GO once three gates pass; 011 DDL on `hauska_mcp` sanctioned as a recorded run.** Planner-verified: engine main 7012ac7 serves `body.atomDid` as identity only with a `did:` prefix (`withGuaranteedAtomDid`), so BP-WRITE-01 admitting writer-local ids is correct and the question raised at the merge grade is closed. Landed on the drain worktree, not yet committed: migration `011` (`atoms_writer_lease_v2`), token-bound `HeldLease` with `GLOBAL` refused, `writePropertyAtomsBatch` requiring the lease with `FOR UPDATE` inside the chunk transaction and `SCOPE_MISMATCH` before any `INSERT`, `cad --apply` refusing without `--run-id` and refusing every county but 48029 as `OLD_SHAPE_FILL_FROZEN`, v1 take retired by `ATOMS_WRITER_LEASE_V1_RETIRED`, `AGENT_CONTRACT.md` section 3 amended (item 14; marker v92aa194c to v1890f0bb), Factory command `lease-v2-migrate`. Storage vitest 40 pass 1 skip, `tsc --noEmit` clean, factory control 11/11, as reported; CI is the grade. Gates before the first Bexar write, in order: (1) engine PR merged on the `typecheck + test` conclusion string `SUCCESS`; (2) `lease-v2-migrate` recorded on `hauska_mcp` with the `schema_migrations` row and table read back; (3) the resume job holds a Factory `runs` row. Item 7 benchmark stays a pre-registered prediction (300 atoms/s, floor 150; under the floor stop and open stage-and-merge); progress is `updated_at >= apply start` verified against `atoms_built`. `txgio_parcel` ungraded, `parcel-wqdzv` running since 22:40Z. | Drain handback relayed 2026-08-27; planner reads on engine main and the card. | Y |
| A-041 | 2026-08-27 | P-86 | **P-86 item 6 grant registry applied to production (LDT migration 0085).** LDT PR #481 (`feat(p86): share grant registry for resolvable share URLs`) merged `0dd3e159` 2026-08-27T01:06:27Z on `Test=success`, `Typecheck=success` after the schema-fixture drift fix. Planner-owned migration under the operator's standing go: `cloud-run-deploy.yml` `workflow_dispatch` `action=run-migrations`, run 33029068772 on head `0dd3e159`, `completed/success`; the job's own `_schema_migrations` listing from production reads `0084_p85_records_request.sql (2026-08-27T00:46:05.396Z)` and `0085_pe_share_grants.sql (2026-08-27T01:07:49.908Z)`, `migrate-prod: done`. Production head is 0085. The lane reads the row back itself and records it on the card (merged is not applied; applied is now recorded here). Next on P-86: item 1 (resolvable `/s/{grantId}` URL, hauska-map `seat/property-ai` pushed and PR opened) and item 2 (Accept and format). | Planner dispatch and log read 2026-08-27T01:07Z to 01:12Z. | Y |
| A-042 | 2026-08-27 | P-81, P-82, P-83, P-84 | **Option A drain card outcome: path fixed and proven; Bexar complete; no resume; old-shape fills end.** Landed on engine main (b402c8b, 2c90b99): BP-WRITE-01 write boundary, batched `atom_links`, lease v2 `(entity_type, county_fips)` token-bound with the v1 take retired by refuse, `--run-id` required for any apply, every county but 48029 refused as `OLD_SHAPE_FILL_FROZEN`; 010 and 011 applied on `hauska_mcp` as recorded runs; the F-02 runner `factory-atoms-cad` proven by recorded refusals, a dry run, and one canary. Bexar 48029 reads 703,257 atoms against a 703,257 roll, so the resume item (drain card item 26) closes as complete without a resume. Item 6/7 benchmark: 149.0 and 67.4 atoms/s on 999-row rewrite chunks, against 21 atoms/s before the fix and the 300 prediction; the prediction failed at chunk scale, the floor failed, and per the card the answer is stage-and-merge (OPS-19 F-18), designed for the conformant writer, not another old-shape rewrite. `OLD_SHAPE_FILL_FROZEN` becomes permanent: no `--apply` on the old shape for any county from here. Next card for the drain lane: OPS-19 F-15 to F-18. | Drain lane close 2026-08-27; OPS-19 A-004. | Y |
| A-043 | 2026-08-27 | P-07, P-08, P-09, P-11, P-17, P-24, P-25, P-79, P-80 | **Remaining Texas fill rows re-homed under OPS-19 F-10 (consequence of A-042).** With old-shape writes ended, every open Layer 1 and Layer 2 fill row that still depended on a laptop or old-writer apply is executed only through the conformant writer inside OPS-19 F-10 (Texas cleanup on the new path), in the order F-10 sets, and never by restarting a wave from the retired canvases: flood remainder and mud leftovers (P-07, P-08), footprint apply (P-09), rrc-wells statewide-absence honesty (P-11), roads COVER and the Harris PBF result of zero atoms (P-17), the easement writer (P-24), the Dallas and Tarrant P-25 KEEP with no DELETE, the REST harvest writer (P-79), the Travis join defect (P-80). Their OPS-16 statuses stop re-grading here; they grade as F-10 items by rule id. Nothing in this row starts any of them; F-10 starts after the conformant writer card closes. The Manifest GET is restamped only after F-05 item 20 lands or F-06 publishes, because new-shape rows are not served until then and a GET after a Factory write would not move. | Planner, aligning the operator's Factory and Texas board with the plan of record 2026-08-27. | Y |
| A-044 | 2026-08-27 | P-89 ADDED, P-90 ADDED, P-32 | **Report SKU ruling and X-ray generator honesty opened.** Operator 2026-08-27: Feasibility is a report; Comparison is a report and a tool (live Compare dock); Brief is a tool (inspect dock). Records stays a tool (P-85). X-ray and Flood remain reports. Site plan and terrain remain exports. Decision `_decisions/2026-08-27_report_sku_feasibility_comparison_brief.md`. Collateral 01/04/05 and the matching masters refresh. Feasibility and Comparison are not live generate paths; do not pitch them as live. **P-32 stays SCOPED, do not start** (assembler not opened). **P-89** Hauska MCP fail-closed: `refresh_parcel_dossier_export` refuses unresolved verdict or empty brief; GET download of a stored hollow refuses; `live_view_url` / `liveViewUrl` forwarded. WDLL `_inbox/2026-08-27_p89_xray_mcp_WDLL.md`. Substrate; isolated tree from origin/main; not the dirty seat/substrate checkout. **P-90** engine PDF honesty (`emitPdfDossier` verdict like flood, one site-plan sheet, no UNAVAILABLE chips, live-view printed on Flood/X-ray bytes, address title). Property seat. Do not start P-90 until P-89 refuse is live, or accept that PE-only is a costume. Do not start W8. | Operator: SKU ruling plus "give me the mcp dispatch". | Y |
| A-045 | 2026-08-28 | P-91 ADDED, P-92 ADDED, P-87, P-88 | **Smart Site MCP App opened as two Layer 5 rows.** Operator 2026-08-28: ship the screening board from `P:/tmp/files (12)`. **P-91** v1: five-state board plus parcel panel on the existing eight tools; batch `get_smart_site` (`stub`/`node`); `unread` as a fifth wire state; situs label fix; annotations; O1/O5 gates; item 21 copy then file. No ninth tool. **P-92** v2: `create_screen`, `save_property`, `set_property_status`, drainage overlay on `draw`, zoning `codeRefs`. Do not start P-92 until P-91 item 16 is customer-done and the connector WDLL is amended to name each new tool. WDLL `_inbox/2026-08-28_smartsite_mcp_app_WDLL.md`. Decision `_decisions/2026-08-28_smartsite_mcp_app_v1_v2.md`. Prototype `_inbox/2026-08-28_smartsite_mcp_app_prototype.html`. Does not absorb P-85 or Hauska catalog tools. **Amended same day by A-046.** | Operator: "this is what i want to ship" plus "add the v2 scope so we can roll right into it after v1." | Y |
| A-046 | 2026-08-28 | P-91, P-92, P-87 item 12 | **MCP App screen/save decouple.** Operator amendment: I5 (web search never enters the panel), I6 (render any node id without a save), screens hold references not saves, board reads a screen, `list_my_properties` is the saved list only. Tools named: `create_screen`, `add_to_screen`, `list_screens`, `save_property`, `set_property_status`. A11-A15 and O7 added. No listing feed. Find listing history is the only v1 ask-Claude button. In-flight P-91 items 1-5 stay live. Persistence tools start after connector item 12 names them; they do not wait for item 16. Iframe waits for O1 plus those tools. Do not file item 21 as an eight-tool listing we immediately grow. Decision `_decisions/2026-08-28_smartsite_mcp_app_screen_save_decouple.md`. WDLL amended. | Operator scope amendment 2026-08-28. | Y |
| A-047 | 2026-08-28 | P-93 ADDED | **P-93 ADDED: Smart Site kit write-path (QA W9).** Operator go 2026-08-28 after P-89 hand-carry: W0 is live, so W9 may start. Freeze `pe-tokens.css` plus `pe-chrome.ts` as the only kit. Six primitives (Button, Card, Dock, Input, StatusChip, Modal). ESLint or the existing `pe-chrome-kit-gate.mjs` fails on a known raw hex or raw button in chrome, except named islands (map overlay, print gold, Stripe). Convert touched surfaces only. Do not import the SmartCity kit. Do not start W8. Do not write the Stripe agent's tree. WDLL `_inbox/2026-08-28_p93_w9_kit_WDLL.md`. Isolated hauska-map worktree from `origin/main`. | Operator: compile W9, do not fan W8. | Y |
| A-048 | 2026-08-28 | P-94 ADDED | **P-94 ADDED: Team roster server half.** Isolated LDT tree `P:/tmp/legacy-design-tools-team-roster` on `feat/pe-team-roster`. GET `/api/property-explorer/v1/team/members` plus invite, cancel, remove, and patch. Migration `0089_pe_team_roster.sql` plus cortex-api canary, migrate, smoke, then shift. Customer-done is a signed-in Team tab that lights from GET with no PE client change. Invite stays blocked until Stripe writes `pe_user_entitlements.seats_purchased`. Accept-invite is out of this card. Does not absorb P-85, P-89, P-90, P-91, P-92, P-93, or the Stripe checkout tree. WDLL `_inbox/2026-08-28_p94_team_roster_WDLL.md`. Decision `_decisions/2026-08-28_p94_team_roster_server_half.md`. | Operator: add the row so the reviewed tree can PR, apply 0089, and deploy cortex. | Y |
| A-049 | 2026-08-28 | P-95 ADDED | **P-95 ADDED: Stone palette exact port.** Operator 2026-08-28 ruled three ways: open the row, do NOT keep the translucent chrome, and ship the exact look in one card rather than splitting colour from scale. All 63 `--ss-*` values taken verbatim from `P:/tmp/Smart Site Design System` plus the one addition `--ss-brand`. The six translucent chrome tokens go opaque; only `--ss-scrim` stays translucent. `PE` geometry and the `TYPE` ramp in `pe-chrome.ts` move with it, because all six `--ss-fs-*` tokens have zero consumers at `be60021` and a token-file edit alone is a no-op. Roughly 470 inline `fontSize` literals port with the ramp. `LEGAL_RADII` and the ramp set in `pe-chrome-kit-gate.mjs` move or CI fails, which is the gate working. Contrast failures (29 of 80 measured pairings) are priced in an annexe for individual ruling and never silently corrected; component-level failures such as `Marks` at 1.54:1 are fixed. **Starts only after P-93 closes**, same file `pe-chrome-kit-gate.mjs`. Does not absorb P-85, P-89, P-90, P-91, P-92, P-93 or P-94. WDLL `_inbox/2026-08-28_p95_stone_palette_WDLL.md`. Decision `_decisions/2026-08-28_stone_palette_exact_port.md`. Property seat, isolated `hauska-map` worktree from `origin/main`. | Operator: "1. yes 2. no 3. I want the exact look i gave you." | Y |
| A-050 | 2026-08-28 | P-96 ADDED | **P-96 ADDED: PE chrome defect pile (non-palette).** Found while scoping P-95 and deliberately kept out of it. The chrome gate sees **7.1%** of the colour literals it appears to police (25 seen, 327 blind): `chromeFiles()` walks `.tsx` only, and no rule matches `rgb()`/`rgba()`, bare decimal triples, or the 84 CSS named-colour sites. `pe-dock-in` is referenced at `Dock.tsx:54` and declared nowhere while `IMPLEMENTED.md:29` records it shipped. Five dialogs have zero focus-trap logic app-wide and the kit `Modal` has no Escape, so Settings, Pricing, Checkout and SignUp are keyboard-undismissable over a live map. `.ss-focusable` is in the focus-ring selector and on no element. 18 of 48 legacy aliases carry their own literal against their file's own header rule; the eight `--space-*` are also dead and deletable. `TransientChips.tsx` is a 113-line orphan. The ZONE slot prints `LANDUSE_JOIN_HOLD county 48491 — TxGIO prop_id does not join CAD property_use_code` to customers where the house pattern is a named gap in plain words. Geometry tethering waits for P-95's reference commit; same file. Every item graded by violation, never by a passing run. WDLL `_inbox/2026-08-28_p96_chrome_defect_pile_WDLL.md`. Property seat. | Operator: "yes to p96". | Y |
| A-051 | 2026-08-30 | P-91, P-92 | **P-91 v2 built and serving.** smartsite-mcp p558 (`smartsite-mcp-00065-siv`, URI `ui://smartsite/app-p558.html`) and cortex-api p543 (`cortex-api-00668-cos`), both 100 percent by field name 2026-08-30. LDT PRs #553 (`d8dfb319`) and #555 (`24553cfc`) merged. Card `_inbox/2026-08-30_smartsite_mcp_app_v2_WDLL.md`. Close `_inbox/2026-08-30_p91_v2_build_close.md`. Paired shift: p558 to 100 percent first, then p543, so no interval served a cortex `unread` through an MCP that rewrote it to `absent`. The p555 uuid-binding regression (unresolved row `id` painted as Open) repaired on p558. **P-92 re-scope:** N1 (neighbor seam), F9 (zoning codeRefs), R3 (`ask_the_map`, still `not_ready`). The persistence tools A-045 listed for P-92 (`create_screen`, `save_property`, `set_property_status`) shipped in v1 per A-046. Drainage overlay stays data lane (D8). F8 flood-study forms stay data lane. | operator 2026-08-30 "commit then spawn sub agents to execute and manage this build through deploy" | Y |
| A-052 | 2026-08-30 | P-92 | **Honesty contract scoped; X2 added.** Operator 2026-08-30: the 2026-08-28 QA-battery phrase is a design argument, not a verified property. Marketed claim is that the payload makes silence legible (five dispositions on sections; atom-miss is unknown; refusals carry `agentGuidance`; geometry is a server projection). Not "it will not degrade." Two live caveats: flood/land-use `present` with `citationsDegraded` and empty citations (F2, already on the card); edge metadata has no disposition and neighbor/adjacency serialize as flat fact (new). P-92 gains X2 (edge disposition on `draw.edges[]`). X1 remains the reciprocity instrument X2 consumes. Do not market the honesty differentiator until X2 is customer-done. Do not rewrite `_smartsite_masters/01` in this row. Decision `_decisions/2026-08-30_honesty_contract_is_silence_legible.md`. WDLL `_inbox/2026-08-30_smartsite_mcp_app_v2_WDLL.md` item X2. | operator 2026-08-30 restatement of the 2026-08-28 design argument plus the edge-metadata gap from this walk | Y |
| A-053 | 2026-08-31 | P-91 | **v3 rows OPENED; the VIEW is built and serving, the FRONT DOOR is not.** Freeze premise dissolved rather than lifted: the v3 WDLL conditioned these rows on OPS-16 carrying another seat's uncommitted edits, and OPS-16 is clean at `7dfc69e`. **Built and serving** on `smartsite-mcp-00076-fes` (tag `dbfix`, digest `sha256:674baef6`, which is the p562 image; the revision name moved for the Neon direct-endpoint secret and the code did not): M1 anchor, M2 batch anchors, M3 map ground, M4 multi-parcel canvas, M5 territory honesty, across cuts p560, p561 and p562. **M1 is proven UNNECESSARY, not deferred:** the anonymous cortex facets route already serves a per-parcel anchor, so the whole wave touched no api-server file. **Unbuilt v3:** Q1 selectors (`near`, `subdivision`, `street`), Q2 subdivision source instrument, Q3 `ask_the_map` retirement ruling, V1 to V8 vocabulary and report shape, P2 three-tier result discipline, and S1 to S3 which are cortex serialization and belong to the property seat. Function 7 reads as VIEW built and FRONT DOOR unbuilt: an area is visible only when its parcels are already named, and cannot yet be asked for. **Two open risks carried on this row.** The v3 panel is UNVERIFIED against `cortex-api-00672-ceq`, which is another seat's F-11 merge deployed after the wave closed on a new digest. And no write has been proven end to end through the product since the Neon pooler read-only incident, which is mitigated by unpooled direct-endpoint routing and not cured. **Scope law for these lanes:** MCP-only under `artifacts/smartsite-mcp`, no cortex and no api-server edit while another seat holds live worktrees there. Subagent lanes produce diffs; the planner commits, merges and deploys. Does not absorb P-85, P-92, P-93, P-94, P-95 or P-96. WDLL `_inbox/2026-08-30_smartsite_mcp_app_v3_WDLL.md`. Reconciliation `e4b312e`. | operator 2026-08-31 "dispatch agents and manage this through the deploy of v3" | Y |

| A-054 | 2026-08-31 | P-87 | **"Use in your AI" narrowed to CLAUDE SYNC, and the connected state made a fact rather than a flag.** Operator 2026-08-31 ruled three ways: cut every vendor but Claude on the rail card AND in Settings; two states, setup instructions when Claude is not connected and a single Sync button when it is; keep the share link on the card because handing a property to someone else and expanding your own toolset are different jobs. **The signal did not exist.** smartsite-mcp resolved a real Smart Site user on every call and discarded it, so "is Claude connected" was not knowable and the card had nothing honest to switch on. Three options were put up (verified signal / local self-declared flag / local-now-verified-later) and the operator chose verified. Built: migration `0090_pe_ai_connections.sql`, a recorder on the JSON-RPC `initialize` handshake (the only message carrying `clientInfo`, and the one a host sends the instant a connector is approved, so the card flips at setup rather than at first use), and `GET /api/property-explorer/v1/ai-connections`. An initialize that does not NAME its client writes nothing; there is no unknown-client row, because a row here paints a Sync button. Every failed read renders setup. **Sync cannot promise prefill:** Anthropic documents `claude://` for the desktop app only, the web `?q=` form is undocumented and was reported removed for web chat in Oct 2025, so Sync copies to the clipboard first and then opens a chat and says both. **Cutting Cursor removes no capability** (same OAuth server, existing connections keep working) but does remove the only place the product names the address for it; ruled on, not absorbed. **NOT YET VERIFIED, carried on this row:** the migration has never been applied and the upsert has never executed (no database was reachable this session); the CI schema fixture is hand-patched, not generated, and the drift check is what proves it; Claude's actual `clientInfo.name` string is unobserved, so the `claude`-prefix classifier is reasoned, not measured, which is why the raw `connections` array ships beside the classified answer. Decision `_decisions/2026-08-31_claude_sync_card_and_connected_signal.md`. Branches `seat/p87-claude-sync` (hauska-map) and `seat/p87-claude-sync-server` (LDT). Does not absorb P-91, P-92, P-93, P-94, P-95 or P-96. | operator 2026-08-31: "get rid of all the other connection options other than Claude", "called Claude Sync", "then it should just be a sync button", plus the three-question ruling. | Y |
| A-055 | 2026-08-31 | P-87 | **Claude Sync SHIPPED end to end, and all three of A-054's unverified claims are now closed by measurement.** Merges: LDT #563 (`f20b3e0`, 11 of 11 green) and hauska-map #317 (`d119e06`, 3 of 3 green), both CLEAN with unmoved bases. **Migration APPLIED:** `0090_pe_ai_connections.sql` at 2026-08-31T13:48:00.915Z via the sanctioned run-migrations dispatch, read out of the step log verbatim (`ok 0090_pe_ai_connections.sql applied`), against `neondb` on the `ep-lucky-truth-apodo8hr-pooler` endpoint. **Serving:** smartsite-mcp `00080-xum` and cortex-api `00676-hid`, both 100 percent read as JSON by field name, both canaried at 0 percent and smoked first. The mcp revision's running DIGEST `sha256:deefd7ab` was compared against the registry digest for the merge SHA and matches, so the tag is not the evidence. PE deployed by Vercel CLI (hauska-map does not auto-deploy) to `smartsite.cloud`, bundle moved `index-CgJc9x-_` to `index-DZW9ff_o`, stamped with `d119e06` and not UNSTAMPED, `--ss-claude: #D97757` live in the CSS asset, ChatGPT and Copilot at zero occurrences. **The three A-054 unknowns, each closed by a different instrument.** (1) The hand-patched CI schema fixture was verified by the drift check pg_dumping the drizzle-pushed schema and diffing it, which passed, so the hand edit matches what drizzle actually emits including the FK name. (2) The migration applied, above. (3) **Claude's clientInfo.name is now OBSERVED, not reasoned:** the production table holds `claude-code` version `2.1.247`, written 13:51:56 by the planner's own connector reconnecting after the traffic shift, and it satisfies the `claude` prefix predicate. **The upsert was proven separately** by replaying drizzle's exact ON CONFLICT statement against the real table inside a transaction that was ROLLED BACK: rows stayed at 1, the UPDATE branch fired, `last_seen_at` moved and `first_seen_at` was preserved. Route existence was proven at cortex-api directly by a four-way discrimination (bogus path 404, new route 401, same route 404 on the prior revision); the PE proxy probe was VACUOUS for that purpose and is recorded as such, because it 401s on everything unauthenticated. **Still unproven and owed to a browser session:** the card rendering its connected state for a signed-in user, and whether the undocumented web `?q=` prefill actually fills the composer. Rollback targets if needed: smartsite-mcp `00078-fat`, cortex-api `00674-rap`. | operator 2026-08-31 "can you deploy everything and commit and push". | Y |
| A-056 | 2026-08-31 | P-87 | **The card shipped with a STARVED read, found by the operator inside an hour, and the fix is the lesson A-055 should have drawn.** Operator connected Claude and the card still showed setup instructions. It was right to: the connection read was a mount-only `useEffect` with `[]` deps, so it rendered a decision computed BEFORE the connect and could never revisit it. Reopening did not help either, because the dock COLLAPSE keeps its content mounted (`maxHeight: isOpen ? 100000 : 0`) rather than unmounting; only a full page reload ever re-read. This is the enforcement doctrine's own category: the mechanism existed, was correct, and never fired again, and it passed every test in the suite while being incapable of running twice. **Fix (hauska-map #318, merged `7772706`, deployed, bundle `index-DZW9ff_o` to `index-BS3cC4j_`):** re-read on `focus` and on `visibilitychange`, since connecting happens in ANOTHER APPLICATION and the return to the tab is the signal; a HIDDEN document deliberately does not trigger a read, because a background tab waking is not a user returning. Plus a manual `Already connected? Check again`, because focus events do not fire in every window arrangement and the user should not depend on us guessing window semantics. The wiring was extracted to `subscribeConnectionRefresh(run, win, doc)` taking its event targets as arguments, because PE's suite has no jsdom and the alternative was shipping the fix with the same coverage the bug had; it now has four tests including the hidden-does-not-fire and unsubscribe cases. **Second defect, found by reading what a real connect actually wrote (LDT #565, merged `8416583`, cortex-api `00678-jup` serving):** one connect writes THREE rows, `claude-ai` v0.1.0, `Anthropic/ClaudeAI` v1.0.0 and `Anthropic/Toolbox` v1.0.0, and two of the three failed the `claude`-prefix predicate. Masked today because `claude-ai` is written alongside them, but a surface announcing only `Anthropic/*` would have read as not-connected while the connector worked. Predicate is now `claude` OR `anthropic/`, the slash making it a namespace rather than a word; both new tests were run against the old predicate and fail there. **The honest reading of A-055:** it closed three unknowns by measurement and was correct on all three, then listed the browser check as merely owed. The browser check was where the defect was. Measuring the server half thoroughly did not make the client half verified, and a card whose state depends on another application cannot be called done without exercising that round trip. | operator 2026-08-31: "i connected it and it still says this". | Y |
| A-057 | 2026-08-31 | P-87 | **The card was DEAD FOR EVERY ACCOUNT, not slow: its read was never on the deep-proxy allowlist.** Operator tested two separate Smart Site and Claude account pairs and reported "smartsite wont recognize that i am signed in"; both failed identically because `api/property-explorer/v1/ai-connections` was missing from `apps/property-explorer/api/_lib/deep-allowlist.ts`. **Why three rounds of checking missed it:** `spine-deep.ts` reads the session cookie FIRST and the allowlist SECOND, so signed out every path returns 401 and an unlisted path is indistinguishable from a listed one; only a signed-IN request to an unlisted path returns the 403 that reveals it. The planner probed the route through the proxy while signed out, got 401 for both it and a bogus control, RECORDED THE PROBE AS VACUOUS IN A-055, and then moved on instead of replacing the instrument. The answer was in the allowlist file the whole time. This is the doctrine's own rule failing in practice: code reading outranks output measuring, and the planner measured output and never read the write path. **Three fixes, because one leaves the failure mode intact** (hauska-map #319, merged `c0e5559`, deployed, bundle `index-BS3cC4j_` to `index-DpwI0elF`): the path is allowlisted; 401 and 403 are no longer collapsed into one outcome, since reading 403 as "sign in" is what turned OUR misconfiguration into a false claim about the user's account; and `unknown` now carries a reason the card PRINTS, because a failed read and an honest "you have not connected yet" painting the identical panel with no notice is the actual reason this survived a deploy cycle with the operator looking straight at it. **The control:** `proxy-allowlist.test.ts` asserts every deep GET a shipped client calls is allowlisted, with a not-vacuous case, verified by violation in both directions. A client-side fetch constant and a server-side allowlist entry are two independently authored halves and nothing checked they agreed. **Also fixed this round:** #318 the mount-only read that could never re-run (A-056), and LDT #565 the `Anthropic/*` classifier gap. **STILL UNVERIFIED and explicitly owed to the operator:** the planner cannot exercise a signed-in browser session, so the allowlist fix is proven by unit test and by the deployed SHA, NOT by a live authenticated round trip. The card showing CONNECTED for a real signed-in user has never once been observed. | operator 2026-08-31: "i tried this with two diffrent smartsite and claude accounts to verify". | Y |
| A-058 | 2026-08-31 | P-87 | **Claude Sync CONFIRMED WORKING by the operator, then trimmed to one job.** First observed success: "Connected as claude-ai, last seen 1 minute ago" with the Sync button, on a real signed-in browser session. That is exactly the round trip A-057 recorded as never once observed, and it is now observed. Three operator rulings on the working card (hauska-map #320, merged `d32386c`, deployed, bundle `index-DpwI0elF` to `index-BzFrvj6l`). **Share block REMOVED, reversing the earlier keep** — the operator's own reversal after using it. It was only ever a mirror of the Share bubble's stored state, never a second mint, so removal deletes no capability, and the Share rail bubble is verified still present in the deployed bundle. Asserted as ABSENCE in both states so it cannot drift back, and the dead plumbing (stored-state hook, both handlers, mint import, SharePhase type) went with it rather than being left orphaned. **Moved LAST in the rail**, below Compare: it is the hand-off out of the app, not another way to read the parcel. **Made EXPANDABLE**, dropping an `expandable: false` carried from when the card was a vendor list of one-line rows. **One quietly-wrong test found by the move:** `workbench.test.tsx` built its non-expandable fixture as `T("use-in-ai", false)` and asserted a column of only that tool cannot widen; because the fixture is local rather than the registry it KEPT PASSING once the real tool became expandable, while reading as coverage of a tool that now does the opposite. Renamed to a neutral id. A fixture named after a real tool and asserting the reverse of what that tool does is worse than no fixture, because it looks like the behaviour is pinned. **P-87 Claude Sync is customer-done:** two states driven by a measured fact, the connected state observed live, one job per card, rail position and width matching the rest of the workbench. | operator 2026-08-31: screenshot of the working connected card plus "remove that share link section", "move the claude logo to the bottom of the toolbar", "the contaier needs to be able to expand wider like the rest of them". | Y |
| A-059 | 2026-08-31 | P-87 | **"What you can do in Claude with Smart Site" added as a collapsed disclosure under Connect a different Claude** (hauska-map #321, merged `7d43146`, deployed, bundle `index-BzFrvj6l` to `index-DmuKytKi`). Collapsed by default so the card still leads with one action. **The list is TRANSCRIBED FROM `mcp.smartsite.cloud/llms.txt`, not written from memory**, which matters because that file publishes thirteen tools and marks THREE `not ready`: `request_records`, `check_request`, `ask_the_map`. All three are deliberately absent from the capability list. A card headed "what you can do" that names a tool answering `not_ready` is a promise the product cannot keep, and the user discovers it by asking Claude and getting a refusal, which is worse than never advertising it. The seven rows group the ten LIVE tools into jobs rather than function names. **What is not live is NAMED rather than omitted**, in one muted line, because silence reads as "not possible" instead of "not yet". **The guard is a test, not a convention:** it asserts no not-ready tool name and no not-ready phrasing reaches the list, and was verified by violation (injecting an "Ask the map" row fails it, removing it passes). When one of the three goes live it moves between `CLAUDE_CAN_DO` and `CLAUDE_NOT_YET` and the test moves with it. Deploy verified against the live bundle: the section and the not-yet line are present, `Ask the map` and `Check a request` are at zero occurrences, and the single `Request records` hit was traced to PE's own Records Request button label rather than the capability list. | operator 2026-08-31: "under connect a differnt claude we should have a collapsable area that is 'what you can do in claude with smart site'". | Y |
| A-060 | 2026-08-31 | P-85, P-91, P-50 | **MCP leftovers folded to plan rows, and the clerk acquisition fork is RETIRED as mis-scoped.** The smartsite-mcp planner handed back a measured block list with no plan-row homes; this row gives them homes and corrects one strategic read. **(1) The clerk index does NOT need bulk acquisition.** The handoff proposed a Local Government Code 191.008 access agreement or a PIA extract, per-county cost against the under-200-dollar-per-jurisdiction commitment, a counterparty, and an operator ruling. That analysis is sound for BULK and bulk is not what this product needs. P-85 Records Request is a PER-REQUEST path that is already built: worker live on Cloud Run since 08-27, six county portals registered, REC UI and live job status shipped, Bastrop portal search reaching a live Aumentum grid (dump 08-28), and document-surface purchase bind merged as LDT #531 on 08-28. Cost is per transaction and customer-initiated, so the cost-per-jurisdiction rule is not engaged at all. **The remaining blockers are engineering and small:** rebuild the worker from main now that #531 is in, read the live Aumentum grid rather than guessing selectors (job 08023fab / unresolved_result_row_header), and capture one document so the three-state classify probe becomes reachable, which nothing has ever run against. Note the honest limit that stays: `purchaseApproved` QUEUES A HUMAN, it does not buy. Do not describe the purchase path as automated. Williamson Tyler 403 is unresolved. **No 191.008 conversation is opened and no acquisition ruling is owed.** **(2) Homes for the rest.** P-91: `place/radius-search` hangs on valid params until Cloud Run kills at 300s while validation answers in 0.17s, so routing is fine and the query hangs inside `searchParcelsByRadius`; SHIPPED AND NON-FUNCTIONAL is worse than the refusal it replaced and a 300s timeout reads as transient when the truth is a broken query; isolated but NOT root-caused, and the two live mechanisms (unindexed candidate scan, lock or connection wait) cannot be separated from outside, confounded further because both services run UNPOOLED under the Neon mitigation so connection behaviour is not in its normal configuration; the instruments that settle it are an EXPLAIN of the candidate query and `pg_stat_activity` sampled while a request hangs, neither run. P-91: the stub wire contract carries zero geometry signal, so 48021:82112 (no ring, no edges, no yearBuilt) paints six rails identical to 48021:34137 which has full geometry and a 1910 structure; declared NOT FIXED rather than faked, needs an api-server change. P-91: street-search matches a token not a street, `q="Pine St, Bastrop"` returning PINEHILL DR / PINE TREE LOOP / PINECREST DR / ROYAL PINES DR with nothing declaring the match fuzzy; **RULED 2026-08-31: keep the broad match and DECLARE it**, because a partial-name searcher often wants neighbours and the defect is the silence, not the breadth. P-91 / F-06: v3 WDLL S1 (serialize bake-held fields the panel never serves), S2 (serve stored Tier-2 FEMA flood facet content reconciled against the live flood atom, and DECLARE a disagreement rather than picking silently), S3 (the measurement-5 defect list: draw landUse reads desc/taxYear where the bake writes description/vintage, yearBuiltFromBake reads keys no bake writes, absent reads for flood/well/specialDistrict drop sourceVintage so absent-verified is unreachable). P-85: the issued-job BLOCK audit is UNMEASURED because DATABASE_URL was unset; the `BLK(?:OCK)?` regex could never match "BLOCK" and was fixed in two files, but nobody has counted which already-issued requests were planned without a block term they should have carried; `recordsBlockJobAudit.ts` exists, a missing store is not a zero, and a wrong result there is indistinguishable from a right one from outside. **(3) Standing deploy rule, applies to every seat.** Every deploy of cortex-api or smartsite-mcp reads the new revision's `DATABASE_URL` secretKeyRef BY FIELD NAME and confirms `DEPLOYMENT_DATABASE_URL_DIRECT` before shifting traffic, however the deploy was triggered; `gcloud run deploy --image` inherits the service spec so a hand deploy propagates a bad binding forward silently, health checks cannot see it because the failure is write-only, and smartsite-mcp ran broken for ~2.5 hours on 08-31 with health answering ok throughout. **(4) The strategic read, recorded because it should change what gets funded.** Of thirteen ledger functions eight refuse; the CTX P0-P8 pipeline closes TWO of them (landUse at P7 via the merged LDT #566 re-bake, setbacks at P4) and DETECTS a third (flood Tier-2 via P5 S7) without fixing it. The other six are outside the pipeline entirely: owner is a by-design serve strip, liens and distress are P-85, permits and value and rent have no source or facet, drainage has NO PRODUCER ANYWHERE IN THE BAKE, subdivision is correctly ruled refuse. **Finishing the pipeline makes the product CORRECT, not CAPABLE, and nobody should expect the refusal count to fall much at Wave R.** The handoff's own recommendation (depth on what we hold, rule and bake Bastrop setbacks) needs no decision because it IS the CTX critical path and is already funded; breadth is the trap, because eight of the refusals are independent of jurisdiction coverage so more counties multiply refusals. | MCP planner handoff 2026-08-31; P-85 artifacts and LDT #531 read at source; operator rulings on street-search and the clerk scope. | Y |
| A-061 | 2026-08-31 | P-91 | **Q1 SHIPPED and serving; `near` is dead on arrival, declared.** `smartsite-mcp-00083-dun` (p564+p565, digest `sha256:c3260bb6`) and `cortex-api-00680-vog`. LDT #567, #568, #569, #570 merged; suite 464 to 506. `find_parcel` gains `near` and `street`, no new tool, catalog stays 13; a 422 `serve_refused` surfaces as a DECLARED REFUSAL and never an upstream error; five closed codes mapped; truncation declares itself. **`place/radius-search` HANGS** (504 at 300s; routing and auth both fine; not size-dependent), so `near` ships non-functional and is declared rather than discovered; RADIUS-HANG dispatch compiled by the concurrent planner with a candidate cause this isolation did not name. **Planner defect card WITHDRAWN:** the disposition-union finding had its mechanism inverted, caught by a build lane reading api-server before implementing the brief. **Second silent write outage:** `smartsite-mcp-00080-xum` served 2.5h with every MCP write failing after a pre-#564 workflow deploy wrote the pooler binding into the service spec; caught ONLY by the pre-shift binding gate; the count of failed writes in that window is UNMEASURED. **Neon control merged and RUN** (`7fbcf56d`): probes the POOLED endpoint every 10 min, checks `pg_settings.source` not only the value, and fails loud when it cannot measure. Posture: STAY UNPOOLED until 24-48h of clean runs. The Neon console is not an instrument here; its operations log was silent for production throughout the incident. Does not absorb P-85, P-92, P-93, P-94, P-95 or P-96. Session `_sessions/2026-08-31_p91_q1_shipped_and_the_neon_canary_claude_code.md`. | operator 2026-08-31 "open the cut and lets push through to completion", then "commit then build the control" | Y |
| A-060 | 2026-08-31 | P-97 ADDED | **P-97 ADDED: Stripe live-activation readiness.** Not the activation itself, which stays deferred per `_decisions/2026-08-24_stripe_annual_pricing_and_live_activation.md` and which the operator performs. This row is the CHECKLIST and its per-step verification, so activation day is executed rather than improvised. Opened because the planner verified the current state and found the deferral is sound but its own summary is incomplete: the 2026-08-24 decision says going live is "a key and price-ID swap only" and it is at least THREE things, since live-mode webhook endpoints issue their own signing secret distinct from test. Verified state 2026-08-31: `STRIPE_SECRET_KEY` is `sk_test_`, publishable is `pk_test_`, all eight price ids are test-mode, and all 13 Stripe secrets exist in `legacy-design-tools-prod` AND are bound to the serving revision `cortex-api-00678-jup`. So the wiring is real and the mode is the gap. Mitigating fact already established: the webhook route returns 400 on signature failure rather than 200, so a wrong secret produces visible failed deliveries in the Stripe dashboard and Stripe retries, rather than silently granting nothing. Lane is READ-ONLY audit producing an ordered checklist; it changes nothing and does not perform the switch. Does not absorb P-87 or the HighLevel CRM work, which is on hold pending an API key and has no row yet. | operator 2026-08-31 "go through all of these, spawn agents and you manage", against the planner's ranked list. | Y |
| A-061 | 2026-08-31 | P-97, P-87 | **Four-lane fan closed; the Stripe audit found a LIVE WRONG-PRICE PATH that no checklist step could have fixed.** Lanes: PE line endings, Claude Sync setup-state disclosure, Stripe live-activation checklist, and the billing downgrade now in flight. **The finding that matters:** `hauska-map apps/property-explorer/src/lib/billingClient.ts:103-106` falls back on a 403 or 404 to `startPeCheckoutInstallScoped`, which posts a hardcoded `tier: "pro"` with no interval and no seats, resolving to the RETIRED pre-ladder `STRIPE_PRO_PRICE_ID`. A Studio, Team or annual click silently opens a Pro checkout at the wrong amount. Verified at source by the planner, not taken from the lane. Three lines below, the 503 branch already refuses exactly this and states the rule ("NEVER retried as another tier, never routed to the legacy fallback"); the 403/404 branch predates that reasoning. **403 is not hypothetical:** the deep proxy returns exactly 403 for any path missing from `deep-allowlist.ts`, which happened to `ai-connections` the same day, so a one-line omission would have silently charged every customer the wrong price instead of failing visibly. No money is at risk today because Stripe is in test mode; it must land before live activation. Own lane `fix/pe-checkout-no-tier-downgrade`. **Checklist filed** at `_inbox/2026-08-31_p97_stripe_live_activation_checklist.md`: 20 ordered steps with a verification each, a silent-failure section, and a comments-disagree-with-code section. It CONFIRMS the webhook signing secret is a genuine third swap the 2026-08-24 decision omits, and adds several the planner had not considered: stale test-mode `cus_` ids are self-deadlocking, an EMPTY `STRIPE_PRO_PRICE_ID` returns a SIMULATED checkout under a live key (fail-open), there is no Stripe API version pin at either call site, and `terms.html:107` promises a cancellation flow with no code behind it. **PE line endings (#323, merged `6c7669c`):** one `.gitattributes`, zero existing blobs changed, full PE suite 2 failed to 158 of 158 files and 2087 tests. Root cause was two mechanisms: a CRLF byte comparison, and a vite 6.4.3 SSR-transform bug where `/^#!.*
/` never matches a CRLF shebang so the hoisted import block lands BEFORE the `#!`. Planner independently confirmed that regex behaviour. **A CI gap found by that PR failing to collect its own evidence:** no workflow listed `.gitattributes` in its path filters, so the file governing how every file is checked out was the one file that could change with no test running. Added to three workflows; PR 323 then reported five checks instead of one. **Setup-state disclosure (#324, merged `de0dca7`, deployed, bundle `index-ioetJIPy`):** capability list now in both states, still excluding all three `not ready` tools. Its lane also caught that a bare `indexOf` ordering assertion returns -1 on absence and would have gone green on a total miss. | operator 2026-08-31 "go through all of these, spawn agents and you manage" then "spawn an agent to do this". | Y |
| A-062 | 2026-08-31 | P-97 | **The published terms are AHEAD of the product, and that is a live-money blocker rather than a copy defect.** Operator ruling: `terms.html` states verbatim "You can cancel a paid plan through the Stripe billing flow in the product", and zero billing-portal references exist anywhere in `apps/property-explorer` (verified by grep, not inferred). Taking live money against a cancellation promise the product cannot honour is exposure. Elevated from checklist line to a BLOCKING Phase 0 item on P-97: it gates live activation alongside the tier-downgrade fix. **The sharp shape of it: the product is honest and the legal page is not.** `SettingsModal.tsx:21-22` says in its own header that "payment method, invoices and cancel need a billing portal that does not exist", and the Plan tab renders "Not built" to the user's face. The app declines to overclaim while the terms overclaim on its behalf, which is the inversion of the usual failure and is the half carrying legal weight, because the terms are the document a customer is held to and holds us to. **The fix is small, which is why amending the terms is the wrong trade.** The Stripe call already exists and is proven: `brokerageStripe.ts:245` posts `/billing_portal/sessions` and `POST /api/brokerage/v1/billing/portal` is live on the install-scoped seam. Missing is only the PE user-scoped equivalent; `propertyExplorer.ts` carries `billing/checkout` and `billing/property-unlock/checkout` and no portal route. Work is one route against the signed-in user's `stripe_customer_id`, its path added to `deep-allowlist.ts` (omitting it returns 403, the exact defect class that bit `ai-connections` the same day), and a control in Settings > Plan replacing "Not built". Removing a promise we can cheaply keep trades a customer-facing capability for nothing. **Verification:** a signed-in POST returns a portal URL on the customer's own id, Settings no longer renders "Not built" for cancel, and the terms sentence becomes true. If the ruling reverses to amending the terms instead, the sentence must be gone BEFORE any live charge, not after. Checklist `_inbox/2026-08-31_p97_stripe_live_activation_checklist.md` Phase 0 item 3. | operator 2026-08-31: "the published terms are ahead of the product... Taking live money against that is a real exposure, not a polish item." | Y |
| A-063 | 2026-08-31 | P-98 ADDED | **P-98 ADDED: the next-action rail, an activation engine that happens to mount in Settings first.** Operator ruled the Settings right rail should stop explaining the panel's own design and start moving accounts through the funnel. Four rulings: the rail is ACTION not information; **capability-first** (names what you get, never a price or a plan; where the action genuinely is a purchase the number lives in checkout, preserving "a plan is never priced in two places"); a **mountable component**, because Settings is low-traffic and a funnel engine that only fires there moves almost nothing (Settings is the prototype mount, not the destination); and **instrumented in this build**. **The design law:** one action per context, never a list, and THE RAIL MUST BE ABLE TO GO QUIET. A reachable empty state is what makes the non-empty state trustworthy; a next-action surface that always finds something to sell is an ad slot, and the panel already declares nothing in it is a control that does nothing. If the ladder cannot return zero it is wrong, and that is the acceptance test. **The trap in simply deleting the old copy:** the panel is full of "Not read" and "Not built" rows (Plan alone shows three of each) and the footer is what makes those read as honest rather than broken. Those rows are a bug list, not a design feature. **Ladder v1:** Account and Connections offer Connect Claude when `pe_ai_connections` shows none; Plan offers the unlock when free messages are nearly gone and **annual when paid monthly on Solo or Studio** (the strongest lever: expansion revenue with no capability to build, amounts already ruled, annual price ids already configured); Team offers invite only on Team tier with unused seats, quiet otherwise. **Verified readable 2026-08-31:** `accessTier`, `subscriptionTier`, `entitlementSource`, `freeMessagesUsed/Limit`, the connections read, saved properties, team roster. **NOT readable and therefore in scope:** an account-wide list of active unlocks with expiry (`peEntitlement.ts:167` filters by `parcelNodeId`, one parcel at a time). An expiring unlock is the highest-intent rung and a ladder without it is missing its best one. **Instrumentation is not deferrable:** the locked GTM plan names activation instrumentation as the blocker for affiliate optimization, and a ladder must compute activation state to decide what to show, so the event is nearly free once the ladder exists. Needs a PE-user-scoped event store; `gtm_events` is install-scoped for the extension and is the wrong spine. Decision `_decisions/2026-08-31_next_action_rail_activation_engine.md`. Sequenced AFTER the SettingsModal line-weight lane, which edited the same file and has merged. | operator 2026-08-31: "i think it should be used as next action or something that encourages movement in our funnel", then capability-first / mountable / instrument-now. | Y |


| A-064 | 2026-08-31 | P-99 ADDED, P-100 ADDED, P-87 | **Smart Site GTM opened as a working set, with two rows and three rulings.** New folder `_smartsite_gtm/` is the go-to-market working set; `_smartsite_masters/` still governs what may be SAID and wins any conflict. **P-99 ADDED:** GoHighLevel bizops buildout plus the product-to-CRM pipe. Credential provisioned and PROVEN LIVE 2026-08-31 (v2 Private Integration token, sub-account `KtUXFiFB5e22abpLp1MR`, company `vKCvP5UtZmDLMD4A6T4F`; locations/contacts/opportunities/users all 200 against a bogus-location control returning 403 — the first two probe runs were VACUOUS, real and bogus returning byte-identical scope errors, and only the third separated). A-060's "on hold pending an API key, no row yet" is therefore CLEARED. Pipe is unbuilt and needs the credential in Secret Manager bound to cortex-api; an operator disk copy cannot be read by a webhook handler. **P-100 ADDED:** share-loop and funnel instrumentation as the affiliate launch gate — `share_created`, `share_viewed`, recipient-to-signup attribution back to the sharer, and consent-flagged funnel events (locked handoff items 5 to 7, none wired). Does NOT absorb P-98, which instruments its own next-action rail; P-100 is the share-attribution and CRM-sync half P-98 does not cover. **Three operator rulings** (`_decisions/2026-08-31_ctx_gtm_rulings.md`): affiliate is OPT-IN with an application, not a universal link; McLennan 48309 is HELD for targeting (launch target is the Austin metro five; serving Waco is unchanged) as the first expansion proof; affiliate launch HOLDS on P-100, the only hold in the plan, and recruiting/kits/content proceed meanwhile. **Two decisions filed.** `_decisions/2026-08-31_gohighlevel_supersedes_pipedrive.md` supersedes the 2026-08-17 Pipedrive ruling; the seam rules transfer unchanged (Stripe webhook is the sole tier writer; no CRM person becomes a city feed) and two drift risks are named refusable: GoHighLevel Payments must never touch the Smart Site Stripe account (it would create subscriptions that grant no entitlement while PromoteKit credits an affiliate), and its human-sales machinery must never point at Smart Site subscribers per the humanless ruling. `_decisions/2026-08-31_smartsite_connector_is_a_door_not_a_tier.md` rules the Smart Site MCP connector stays available at every rung including Free and mirrors the workbench gates rather than carrying its own — the planner proposed moving `run_report` to Studio and WITHDREW it against the positioning master's "two doors, one truth" claim; where a capability needs gating it is gated at the tier and both surfaces inherit. **P-87 note:** the connector is acquisition and retention, not differentiation, and no campaign sells it as a reason to buy a rung. **Ladder re-cut PROPOSED, not ruled** (`_smartsite_gtm/03_ladder_recut_proposal.md`): a read of the live gates on LDT `394424f2` and hauska-map `fbda04aa` found Studio's whole differentiation is four items, three architect-only, so the largest segment has no reason to pass Solo; and screens/boards (`create_screen`, `add_to_screen`, `list_screens`) are UNGATED in both surfaces, giving away the set-level job the locked ladder reserves for Prospect. Proposal is "Solo answers one parcel, Studio works a list" with Prospect redefined as monitoring. Prices untouched. Hygiene found, not a gap: `requirePePaidDeep` is dead code with zero repo-wide mounts, superseded by `requirePePaidOrPropertyUnlocked` which carries four live mounts. **Numbering defect noted, not fixed** (baseline is append-only): A-016, A-060 and A-061 each appear twice in this table. | Operator 2026-08-31 across the GTM thread; planner code reads by field and paired-control live probes. | Y |

| A-065 | 2026-09-01 | P-98, P-95 | **P-98 SHIPPED both halves, plus a live billing-surface defect fixed on the way.** Server (LDT #573, `26068a1`): migration `0091_pe_activation_events` APPLIED 2026-09-01T00:08:18Z, read verbatim from the step log; `cortex-api-00684-foh` serving 100 percent; both new routes smoked with a four-way discrimination (401 on canary, 404 on a bogus path, 404 on the pre-shift production revision). Client (hauska-map #329, `8740558`, bundle `index-BQaj4JFu` to `index-kw3sOXl3`): all five action ids present in the shipped bundle. **THE DEFECT: Settings showed "Paid" to EVERY account including anonymous** (#328, `1a11a3e`). `SettingsModal:183` passes a null parcel on purpose since Settings is account-scoped; the entitlement route REQUIRES a parcel, so the hook returns its LOADING constant forever; that constant carries `locked:false` and `signedOut:false`; and the ternary's fallback branch was `"Paid"`. Two missed guards and a generous default. It survived because it reads correctly to anyone paid, which the operator is, so his own screenshot looked right. Now says "Not read", obeying the panel's own printed rule that a field with no traced source says Not read; the test pins the DIRECTION of the fallback rather than the string, because direction is the defect. Operator verified "not read" live before the rest deployed. **FOUR OF FIVE RUNGS ARE STARVED, on the record rather than discovered later:** only `connect_claude` can fire today; `billingInterval` is the single field unblocking `annual_upgrade`; two rungs are NOT RUNNABLE from this mount (no invite write path exists in the client, and routing an unlock extension through `onUpgrade` would open checkout for the MAP's active parcel rather than the lapsing one), so `SETTINGS_RUNNABLE` fails closed on both rather than drawing a control that opens the wrong checkout. **One account-level entitlement read that does not need a parcel fixes the Access field AND unblocks the highest-value rung.** CI proved the one item the lane could not: the hand-patched schema fixture passed the drift check against a real pg_dump. A `Test` failure on first run was an unrelated 10-second timeout in `lib/submission-classifier` under CI database contention; re-run green. The rail/Access merge conflict was resolved keeping BOTH, proven by their two pins passing in one tree (164 files, 2194 tests). **Probe honesty:** the production 401s on both new routes do NOT prove the allowlist, since the proxy checks the cookie before the allowlist and a signed-out caller gets 401 either way; the allowlist is proven by the parity test, which imports the path constants from the clients rather than retyping them. | operator 2026-08-31 "go on the fix", then "not read verified / get it deployed". | Y |

| A-066 | 2026-08-31 | P-101 ADDED, P-100, P-99 | **LADDER RE-CUT RULED, and P-100 assigned.** Operator ruled the 2026-08-10 LOCKED ladder AMENDED, not replaced: **Solo answers one parcel, Studio works a list of them.** Prices untouched (Free / $15-30day / Solo 49 / Studio 129 / Team 299-3seat, annual default). **Exactly one capability moves:** screens and boards (`create_screen`, `add_to_screen`, `list_screens`) go from UNGATED to studio-or-team. The records package is already Studio inside export kind `dossier` and merely becomes a visible row. Comparison surface regroups to Answer this parcel / Work a list of them / Hand it off / Work as a firm; Studio's badge stops being "The packet" (it told the largest segment the tier was not for them while holding the one thing they wanted). Studio goes 1 seat to 2 (P-94 roster server half already built). **Prospect REDEFINED** from "the set-level answer" to monitoring (alerts, saved searches), because Studio's screens eat its old job; the locked doc already names alerts as its natural hook. Decision `_decisions/2026-08-31_smartsite_ladder_recut_studio_works_a_list.md`; evidence and verified capability map `_smartsite_gtm/03_ladder_recut_proposal.md` (flipped from proposal to ruled, filename kept so references resolve). Commercial basis, locked doc's own method: $100K MRR is 2,041 seats at Solo alone, 776 at Studio. **P-101 ADDED:** implement the re-cut — the tier gate in BOTH surfaces (the connector inherits, it does NOT get its own gate, per the connector ruling), the `pricing.ts` regroup, the Prospect copy. **Staleness check run before ruling:** the four entitlement files behind the capability map are byte-unchanged between the read commit `394424f2` and current LDT `26068a1e`, so A-065's "Paid for everyone" defect (a client-side `SettingsModal` display bug) does not touch it. **P-100 ASSIGNED** to the property seat: card `_inbox/2026-08-31_p100_share_and_funnel_instrumentation_WDLL.md`, dispatch `_dispatches/2026-09-01_p100-instr_dispatch.md`. Card scope CORRECTED before compile: `gtmEvents`, `gtmConsent`, `recordGtmEvent` with many live call sites, a PE `gtmClient.ts`, and `share_created`/`share_viewed` at `brokerageWorkspace.ts:480`/`:129` ALL EXIST, so the 2026-08-24 "none are wired" claim is STALE and P-100 is measure-then-close-gaps, not build-from-zero. The one gap established repo-wide as genuinely absent is **sharer attribution** (zero hits for `sharerUserId`/`referredBy`/`attributedTo`/`referrerUserId`), so a recipient who signs up is credited to nobody. **P-99:** GHL pipeline creation PROVEN API-supported by a safe write probe (`POST /opportunities/pipelines` 422 validation; control `POST /pipelines` 404; nothing created). Browser-only work carded as an agent runbook `_smartsite_gtm/04_gohighlevel_agent_runbook.md`. Decision 4 (affiliate segment lines) re-pointed at the ruled ladder and still OPEN. | Operator 2026-08-31 "yes to the flip on the ladder", "assign p100", "do the api probe". | Y |

## GRADE LOG (one row per grading pass; statuses re-graded by instrument only)

| Date | Graded by | Ledger triple (satisfied / pct / cells) | Notes |
|------|-----------|------------------------------------------|-------|
| 2026-08-12 | doc_repo planner | 405 / 12.909486508370861 / 3,556 (read 20:34:35Z, 31.5s) | Baseline grading; zero no-writer/no-atom cells confirmed by full-cell parse |
| 2026-08-12 | L8 lane planner | (ledger not re-read; DC-10 is cert instrument) | P-18 / DC-10 CLOSED_PASS: W3 close `_inbox/2026-08-12_W3_cert_frame_close.json`; `gh … block13-cert-grade` → `[{"conclusion":"success"}]` @ `b2a8706`; OPS-11 CLEARED; session names block13 re-earn |
| 2026-08-12 | L13 lane planner | (ledger not re-read; DC-11a is health instrument) | P-29 store restore CLOSED: MCP #65 @ `57fa819` CI `success`; serving `00063-fic` `/health` `rate_limit_store.state=ok`; 429 + restart survival on tagged canaries. Literal DC-11a cortex URL still fails (wrong host). DC-11b/c residue. |
| 2026-08-14 | L19 lane planner | (ledger not re-read; DC-11b/c are load-test file + capacity-doc last_updated) | P-29 DC-11b/c CLOSED: artifact `_inbox/2026-08-14_76j_C4_loadtest_results.json`; capacity doc last_updated 2026-08-14. Serving `00063-fic` 36/36 health samples postgres/no-memory. 429 band=rpm; 20 rps after cap 301/301 429; 0 503. first_429 cumulative 72 (outside pre-registered 50-70; wall-clock minuteBucket). DC-11d HTTP/1.1 200 parking HTML to /lander, A 15.197.148.33 + 3.33.130.190, not Vercel. |
