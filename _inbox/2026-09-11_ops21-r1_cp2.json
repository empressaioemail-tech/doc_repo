{
  "lane": "OPS-21-R1",
  "checkpoint": "CP2 (pilot / mid-work)",
  "planRow": "P-146",
  "repo": "hauska-factory",
  "worktree": "P:/seat-worktrees/property/hauska-factory-ops21-r1",
  "branch": "fix/ops21-r1-not-specified-remediation",

  "whatWasBuilt": {
    "classificationTable": "src/lib/envelope-writer/not-specified-classification-table.json -- 329 rows, generated by executing doc_repo's own scripts/corpus_not_specified_audit.py classify_group function (via Python runpy, zero reimplementation) against @empressaio/setback-corpus at the exact commit/version (b8020c8b92df5e76c6bc22261ac5809e76ddf16c / 1.1.0) hauska-factory imports, independently verified byte-identical to the published npm tarball (dist.gitHead matches; every district JSON deep-equal).",
    "resolver": "src/lib/envelope-writer/envelope-corpus-lookup.mjs -- resolveField now classifies a flagged slot against the table (keyed on the RESOLVED corpus table's own jurisdictionKey, not the routing key -- they can differ, e.g. Bastrop). REAL_VALUE_FLAGGED overrides the flag and returns the raw value; SENTINEL_SUSPECT_IN_MIXED_GROUP returns a new ENVELOPE_ROUTER_FIELD_SUSPECT_SENTINEL reason; SENTINEL_UNIFORM (or, defensively, a table miss) keeps today's ENVELOPE_ROUTER_FIELD_NOT_SPECIFIED absent-verified shape unchanged. A version guard throws at import if the table's recorded corpusVersion diverges from CORPUS_VERSION.",
    "job": "src/jobs/parcel-envelope-cells.mjs -- same CLI/Cloud Run job as S2 (no new job, per the dispatch's literal instruction). Added: TARGET_RAIL_CELLS_SQL reads each of maxHeightFt/maxLotCoveragePct/maxFootprintSqFt's CURRENT state per page; gateFieldWrite/gateFootprintWrite decide, per field per parcel, whether a first-time write (currently unaccounted -- always writes, unchanged from S2) or a remediation re-visit (currently absent-verified under the specific prior finding -- writes ONLY when the classification actually changed the disposition; a SENTINEL_UNIFORM re-visit produces NO batch entry at all, not even a same-content rewrite). UPDATE_ENVELOPE_CELLS_SQL's WHERE clause widened to admit that specific revisit shape in addition to the original unaccounted-only gate. Applied uniformly across all three write branches (resolvedCells/noRuledTableCells/refusedFromUpstreamCells), not only the one that structurally needed it -- see the CP1 addendum for why the other two were already safe by construction and this is defense-in-depth.",
    "verificationInstrument": "scripts/verify-not-specified-classification.mjs -- new, permanent, read-only (SELECT only). Re-resolves every live flagged cell through the SAME corpus routing code the writer uses and classifies it against the checked-in table; reports per-class totals, per-(jurisdiction,field) breakdown, and a count+min/max(updated_at) fingerprint of the SENTINEL_UNIFORM population. This is the instrument the completion predicate's two-independently-measured-numbers check (CP1 addendum Finding 3) runs before and after apply.",
    "tests": "4 new unit tests in test/parcel-envelope-cells.test.mjs covering every classification branch (SENTINEL_UNIFORM revisit -> no batch entry; REAL_VALUE_FLAGGED revisit -> value, footprint follows in the same pass; SUSPECT revisit -> refused, sibling SENTINEL_UNIFORM field and blocked footprint stay untouched; an already-earned value/refused cell is never touched regardless of what the resolver would compute now). All 31 relevant tests pass; the 3 pre-existing failures (LDT-pin drift + 2 OPS-21-S3 findings, both explicitly marked out-of-scope in their own assertion text) are confirmed identical via git stash, ruling out this lane as their cause -- same method S2's own close used."
  },

  "liveVerification": {
    "note": "Every query below ran with SET default_transaction_read_only=on against a read-only Neon role (parcel_record_ro) or a read-only Neon compute endpoint, direct host (never -pooler). No write has occurred anywhere.",
    "dryRunAcrossSixRepresentativeCities": {
      "cities": ["Austin", "Round Rock", "Cedar Park", "Bastrop", "Taylor", "Lockhart"],
      "command": "factory parcel-envelope-cells --city=Austin --city=\"Round Rock\" --city=\"Cedar Park\" --city=Bastrop --city=Taylor --city=Lockhart (apply=false)",
      "result": "cellsMoved=0 everywhere (correct: dry run). Every non-degenerate outcome the audit predicts actually appears: Austin remediatedToValue=470370 = 156790 x 3 (maxHeightFt+maxLotCoveragePct+maxFootprintSqFt, exact -- Austin's entire flagged population is REAL_VALUE_FLAGGED, footprint unblocks in lockstep). Cedar Park remediatedToValue=153 / remediatedToRefused=948 (matches the audit's 3 REAL / 6 SUSPECT maxHeightFt district split exactly) and remediationLeftUntouched=40056 = 20028 x 2 (maxLotCoveragePct, 100% SENTINEL_UNIFORM there, plus its blocked footprint, both correctly left alone). Round Rock remediationLeftUntouched=27330, remediatedToValue=0, remediatedToRefused=0 (100% SENTINEL_UNIFORM on maxHeightFt, nothing else flagged there -- exact match to S2's own reported number). Bastrop remediationLeftUntouched=5462 = 2731 x 2 (maxLotCoveragePct + its blocked footprint). Taylor and Lockhart non-zero and non-degenerate on all three counters, consistent with their known mixed REAL/SUSPECT/UNIFORM composition."
    },
    "fullPopulationClassificationSnapshot": {
      "instrument": "scripts/verify-not-specified-classification.mjs",
      "artifact": "_inbox/2026-09-11_ops21-r1_pre_apply_classification_snapshot.json",
      "totalFlaggedCellsFetchedLive": 547657,
      "note": "547,657 fetched live independently matches the audit's and S2's own reported total exactly -- a genuine third independent measurement, not taken on either document's word.",
      "totals": { "SENTINEL_UNIFORM": 209483, "REAL_VALUE_FLAGGED": 331066, "SENTINEL_SUSPECT_IN_MIXED_GROUP": 7108, "UNCLASSIFIED_MISS": 0 },
      "sumCheck": "209483 + 331066 + 7108 = 547657 -- exact, zero remainder, zero unclassified misses (every live (jurisdictionKey, field, districtName) combination the corpus routing actually produces has a table entry -- no corpus/table drift detected).",
      "correctionToTheDispatchAndDecisionDoc": "Both the dispatch and the operator ruling state the SENTINEL_UNIFORM population as '~234,000 cells' (an approximation, explicitly marked as such in both). The exact, live, directly-measured count is 209,483 -- a real, material difference (about 24,500), not a rounding matter. Per DEV_PROCESS 4.5 ('report what IS, never tune to an expected number') and 1.4 ('two numbers that should agree and do not is a free finding'), this is reported as a correction rather than silently reconciled: the approximation undercounted how much of the flag population the audit's own worked per-city examples covered (its 'correctlyClassified_SENTINEL_UNIFORM_noRemediation' list in S2's close names only 9 illustrative cities/fields, not an exhaustive enumeration) and did not account for liberty-hill-tx maxLotCoveragePct (3,028, 100% SENTINEL_UNIFORM) or san-antonio-tx's now-confirmed-out-of-scope entries at all. This verification's total is exhaustive (every live flagged cell, not a sample) and is the number this lane's completion predicate uses -- not the dispatch's approximation.",
      "taylorAndLockhartExactLiveCounts": {
        "note": "STANDING FACTS asked these two jurisdictions be finished with exact live counts rather than inferred from the corpus district counts. Resolved:",
        "taylor-tx::max_height_ft": { "SENTINEL_UNIFORM": 8109, "REAL_VALUE_FLAGGED": 0, "SENTINEL_SUSPECT_IN_MIXED_GROUP": 0 },
        "taylor-tx::max_lot_coverage_pct": { "SENTINEL_UNIFORM": 0, "REAL_VALUE_FLAGGED": 7392, "SENTINEL_SUSPECT_IN_MIXED_GROUP": 717 },
        "lockhart-tx::max_height_ft": { "SENTINEL_UNIFORM": 0, "REAL_VALUE_FLAGGED": 465, "SENTINEL_SUSPECT_IN_MIXED_GROUP": 4961 },
        "lockhart-tx::max_lot_coverage_pct": { "SENTINEL_UNIFORM": 0, "REAL_VALUE_FLAGGED": 4961, "SENTINEL_SUSPECT_IN_MIXED_GROUP": 465 },
        "crossCheck": "Lockhart's two fields are an exact mirror of each other (465/4961 vs 4961/465) because the SAME district-level parcel populations swap classification between fields: the four commercial districts (CLB/CMB/CHB/IL, smaller population, 465 parcels) are REAL on height but SUSPECT on coverage; the three residential districts (RLD/RMD/RHD, larger population, 4,961 parcels) are SUSPECT on height but REAL on coverage. This internal symmetry is strong independent corroboration that the district-level classification and the live zoning-code join are both correct, not a coincidence to round off."
      },
      "sentinelUniformFingerprint": {
        "count": 209483,
        "minUpdatedAt": "2026-09-11T01:11:33.643Z",
        "maxUpdatedAt": "2026-09-11T01:27:13.362Z",
        "note": "This is the exact timestamp range of S2's own original --apply run. This is the BEFORE fingerprint the completion predicate re-measures after this lane's apply -- an unchanged count AND an unchanged max(updated_at) together prove the population was never touched, not just that its count happens to still add up."
      }
    }
  },

  "proposedPilot": {
    "correctionToCP1": "CP1 originally proposed san-antonio-tx as the SUSPECT-heavy pilot city; the adversarial review (Finding 1, cp1_addendum.md) found San Antonio is Bexar County, out of OPS-21's six-county scope entirely. Corrected pilot below.",
    "cities": ["cedar-park-tx", "taylor-tx"],
    "why": "cedar-park-tx exercises all three outcomes in one city (153 value, 948 refused, 40056 left-untouched) and is a real in-scope city (Williamson/Travis). taylor-tx additionally exercises the case where one field is 100% SENTINEL_UNIFORM (maxHeightFt, fully untouched) while its sibling field is a REAL/SUSPECT mix -- the per-(jurisdiction,FIELD)-not-per-jurisdiction scoping rule's own worked example.",
    "verificationAfterPilotApply": [
      "cellsMoved reconciles exactly to remediatedToValue + remediatedToRefused (from the dry run's own counts for these two cities) -- no more, no less.",
      "A second immediate re-run (apply=true, no code change) on the SAME two cities must show cellsMoved=0 -- the gate holding under a real re-apply, S2's own convention.",
      "Live spot-check: a handful of the 40,056 cedar-park-tx cells left untouched (maxLotCoveragePct SENTINEL_UNIFORM + its blocked footprint) show unchanged updated_at across the apply.",
      "Live spot-check: one corrected cedar-park-tx REAL cell (maxHeightFt=60, HC/LI/HI districts) and one corrected SUSPECT cell (maxHeightFt refused, GB/H/PS/OG/OR/MU districts) read back exactly as expected, with basis.classification recorded."
    ]
  },

  "outstandingBeforeFullApply": [
    "Commit these changes, open a PR, get CI green (gate8 + test conclusion strings, not 'gh pr checks' prose), merge (self-merge norm, per this program's own convention).",
    "Deploy: Cloud Build the new image, confirm the digest, run the pilot --apply on cedar-park-tx + taylor-tx via the Cloud Run job (parcel-envelope-cells --apply is Cloud Run us-east4 only, LAPTOP_WRITE_FROZEN blocks it anywhere else -- by design, not a workaround-able restriction).",
    "Verify the pilot per the plan above, then run the full --apply across all in-scope cities.",
    "Re-run scripts/verify-not-specified-classification.mjs post-apply; reconcile its SENTINEL_UNIFORM count and updated_at fingerprint against this checkpoint's BEFORE numbers -- two independently measured numbers, never subtraction.",
    "File the close artifact per AGENT_CONTRACT section 6 (missionPremise, completionPredicate, scopeBasis, PR number + merge SHA + CI conclusion strings, all three reconciled counts, taylor-tx/lockhart-tx counts, 3P-15 retirement note)."
  ],

  "status": "CP2 complete. Design validated against live production data at full scale, read-only, zero writes. Proceeding to commit/PR/CI/deploy/pilot apply."
}
