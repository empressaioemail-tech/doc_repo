{
  "artifact": "CP2",
  "lane": "p249-envelope-unlock",
  "planRows": ["P-249"],
  "seat": "p249-lane",
  "filedAt": "2026-09-16T18:26:00Z",
  "cp1": "_inbox/2026-09-16_p249-envelope-unlock_cp1.json",
  "built": {
    "legacy-design-tools": {
      "clone": "P:/tmp/legacy-design-tools-p249-envelope-unlock",
      "branch": "fix/p249-envelope-unlock",
      "startCommit": "ba6a5a69aebf8ac5e4900a8e9b50bfd04a04ec27",
      "headCommit": "52ac6c95d19c1fe39759a1c13338e181d7eab851",
      "diffstat": "7 files changed, 531 insertions(+), 17 deletions(-)",
      "files": [
        { "path": "artifacts/api-server/src/lib/buildableEnvelope/fetchPropertyAtomChain.ts", "what": "the wire type carries depthWarmPromotion and sourceCitation (the atom has no depthWarmPromoted field, A-184)" },
        { "path": "artifacts/api-server/src/lib/buildableEnvelope/reconcileAtomEnvelope.ts", "what": "isEnvelopeAtomVerified (exact marker, then the sourceCitation fallback only when the marker is absent); reconcileWithAtomEnvelope gated on it: an unverified no-buildable-area atom no longer empties a live envelope with a district and a table, an unverified positive area no longer replaces the live figure; a verified zero still wins; the diagnostic reason keeps its P-214 sanitized validation-failed decline" },
        { "path": "artifacts/api-server/src/lib/buildableEnvelope/composeBuildableEnvelopeDerivation.ts", "what": "the promotion fields travel with the outcome into reconciliation" },
        { "path": "artifacts/api-server/src/lib/buildableEnvelope/reconcileAtomEnvelope.test.ts", "what": "P-249 cases: verification predicate precedence, unverified zero kept out of a live envelope, verified zero still wins, validation-failed untouched" },
        { "path": "artifacts/api-server/src/lib/buildableEnvelope/composeBuildableEnvelopeDerivation.test.ts", "what": "the atom-reconciled test now declares a VERIFIED atom; a new test asserts an UNVERIFIED disagreeing atom changes nothing and is not marked reconciled" },
        { "path": "artifacts/api-server/src/lib/buildableEnvelope/__fixtures__/envelope-verification.json", "what": "the divergence fixture set (item 5): 13 cases, byte-identical to the hauska-map copy (sha256 98c32217ca57bc12db96086b9feb7f63e3ff3d0e64f3ee9c3e405c4577f30445)" },
        { "path": "artifacts/api-server/src/lib/buildableEnvelope/envelope-verification-divergence.test.ts", "what": "the divergence test (29 tests): each case's declared verified / machineVerifyDiagnostic answer must match this repo's predicates" }
      ]
    },
    "hauska-map": {
      "clone": "P:/tmp/hauska-map-p249-envelope-unlock",
      "branch": "fix/p249-envelope-unlock",
      "startCommit": "88ac6c517b12701502d88decfdd7cd938aaaa621",
      "headCommit": "ee7e3c760bbbe5d13b1610239236738e21640108",
      "diffstat": "9 files changed, 671 insertions(+), 13 deletions(-)",
      "files": [
        { "path": "apps/property-explorer/api/_lib/atom-chain-to-facets.ts", "what": "the unverified no-buildable-area branch serves the modelled envelope: status \"ok\" (which is what lets the live derive fire), real effectiveSetbacks + district, disclosure, declineReason stays \"envelope-unverified\", figureWithheld: true, envelopeCovered: true, and NO buildableAreaSqFt/Pct and no fabricated geojson" },
        { "path": "apps/property-explorer/src/lib/live-envelope-augment.ts", "what": "a withheld payload keeps the live derive's geometry and is never re-stamped with the augmentation's own recomputed area; the disclosure says the outline is live-derive, not a verified atom" },
        { "path": "apps/property-explorer/src/lib/fact-sheet-resolver.ts", "what": "figureWithheld + drawable rings renders the modelled envelope with disclosure and no area; withheld with no drawable ring is a named envelope-unverified state (atom-path-pending before the live pass, which is honest)" },
        { "path": "apps/property-explorer/src/lib/baked-facets.ts", "what": "figureWithheld?: boolean on the baked envelope payload type" },
        { "path": "apps/property-explorer/src/lib/atom-chain-to-facets.test.ts", "what": "the P-216 case updated: status ok, figureWithheld true, envelopeCovered true, geojson still undefined at the facet layer" },
        { "path": "apps/property-explorer/src/lib/live-envelope-augment.test.ts", "what": "figureWithheld: the live polygon is attached and no area figure is printed; un-withheld envelopes keep their live-derived figure" },
        { "path": "apps/property-explorer/src/lib/fact-sheet-resolver.test.ts", "what": "the withheld payload renders kind modelled with no area; no-geometry cases stay named" },
        { "path": "apps/property-explorer/src/lib/__fixtures__/envelope-verification.json", "what": "the same 13-case fixture set, byte-identical to the LDT copy" },
        { "path": "apps/property-explorer/src/lib/envelope-verification-divergence.test.ts", "what": "the divergence test plus the P-249 branch-shape test (the withheld payload draws through the card projection while printing no figure)" }
      ]
    }
  },
  "verification": {
    "legacy-design-tools": {
      "tests": "pnpm vitest run src/lib/buildableEnvelope -> 17 files, 184 tests passed (envelope-verification-divergence.test.ts 29, reconcileAtomEnvelope.test.ts 23)",
      "typecheck": "npx tsc --build (root, needed because the workspace libs were not built by a partial install) clean; tsc --noEmit in artifacts/api-server clean",
      "build": "node ./build.mjs in artifacts/api-server -> exit 0, dist/index.mjs 22.9mb",
      "knownUnrelated": "the repo's FULL suite has pre-existing failures unrelated to this lane (depthRailGateDivergence.test.ts, txgioSitusSearchBudget.test.ts, DATABASE_URL-dependent suites); reproduced with these changes stashed and restored after"
    },
    "hauska-map": {
      "tests": "pnpm vitest run in apps/property-explorer -> 215 files, 3147 tests passed, 1 todo",
      "typecheck": "tsc --noEmit clean",
      "build": "pnpm run build (workspace packages + vite) -> exit 0, built in 5.73s"
    },
    "falsifierFive": "OBSERVED FAILING, not asserted. Editing ONE predicate alone (isDepthWarmPromoted / isEnvelopeAtomVerified: trimming the marker, or relaxing the citation substring) makes each repo's divergence test FAIL on padded-marker-not-promoted / citation-case-variant-not-promoted / near-miss-* ; the edit was reverted and the suites re-run green. The first fixture did not catch a whitespace-tolerant edit, which is why the two -not-promoted cases were added."
  },
  "pullRequests": {
    "legacy-design-tools": { "number": 701, "url": "https://github.com/empressaioemail-tech/legacy-design-tools/pull/701" },
    "hauska-map": { "number": 409, "url": "https://github.com/empressaioemail-tech/hauska-map/pull/409" }
  },
  "divergenceFixture": {
    "path": "src/lib/__fixtures__/envelope-verification.json (hauska-map) == artifacts/api-server/src/lib/buildableEnvelope/__fixtures__/envelope-verification.json (LDT)",
    "sha256": "98c32217ca57bc12db96086b9feb7f63e3ff3d0e64f3ee9c3e405c4577f30445",
    "cases": 13,
    "declaredAnswers": "BOTH verified and machineVerifyDiagnostic on every case, so a loose reason predicate is caught as well as a loose promotion predicate",
    "predicates": [
      { "repo": "hauska-map", "name": "isDepthWarmPromoted", "form": "wire STRING: depthWarmPromotion === \"depth-warm-promoted-v1\" exactly, else sourceCitation.includes(\"depth-warm-verified\") when the marker is absent" },
      { "repo": "legacy-design-tools", "name": "isEnvelopeAtomVerified", "form": "the same form and precedence, so the two cannot disagree" },
      { "repo": "legacy-design-tools", "name": "isMachineVerifyDiagnostic", "form": "reason TEXT: \"; \" present, or /\\b(edge|road)\\s+\\S+:/i, or a 4+ decimal float" },
      { "repo": "doc_repo", "name": "scripts/envelope-draw-gap.mjs", "form": "BOOLEAN envelope.depthWarmPromoted === true, whose value its SQL derives as body->>'depthWarmPromotion' = 'depth-warm-promoted-v1' OR body->>'sourceCitation' like '%depth-warm-verified%' (lines 190-191, materialized at 226) -- the SAME string fact, one join earlier than the product predicates; its reason leg is classifyReason's \"machine-verify-diagnostic\" bucket" }
    ],
    "instrumentAlignmentNote": "The doc_repo instrument already reads the correct wire strings; its local BOOLEAN field is named depthWarmPromoted, which is the phantom field that does not exist on the atom (A-184) -- it is derived, not read from the atom. The integration seat should keep the SQL derivation and must NOT align the instrument by renaming a real atom field to depthWarmPromoted: align it the other way (treat the boolean as instrument-local, named so it cannot be mistaken for an atom field)."
  },
  "stagingProof": "not run by this lane (build lane; the integration seat deploys and runs it). The runnable checklist is in both PR bodies: serving build by asset existence before and after (never Age:), each measurement from a fresh browser context, the three branch cases per city (48209:97658 unverified zero -> draws with no figure; staging-minted verified zero -> stays empty with a named reason; a validation-failed ring -> declines and draws nothing), the XD-2 Waco fixture that must draw, Pflugerville 48453:427599 still drawing and printing no figure unless verified, and a rollback rehearsal showing the build identity move both ways.",
  "status": "build complete, both PRs open, CI captured in the close"
}
