{
  "lane": "p244a-siteplan-compose-timeout",
  "planRows": ["P-244"],
  "checkpoint": "CP1 (design/pre-build)",
  "timestamp": "2026-09-16",
  "type": "not applicable: solo investigation, no sub-agents spawned (AGENT_CONTRACT section 1 fan model not exercised, per dispatch's own \"you own your fan\" / no-nested-spawn instruction)",
  "plan": [
    "Claim lane p244a-siteplan-compose-timeout before any file edit (done: node scripts/lane-claim.mjs claim against P:/doc_repo's copy, exit 0, seat cente-p244a).",
    "Cut dedicated worktrees before touching anything: P:/seat-worktrees/p244a-siteplan-compose-timeout/doc_repo (branch lane/p244a-siteplan-compose-timeout, from origin/main) for claim+close, and P:/seat-worktrees/p244a-siteplan-compose-timeout/hauska-engine (branch fix/p244a-siteplan-compose-timeout, from origin/main) for the fix -- never P:/hauska-engine, which the dispatch itself flags as 181 commits behind and dirty.",
    "Read P-227's ruling and close (_inbox/2026-09-15_p227-standalone-vs-composed_ruling.md, _close.json) and P-231's OPS-16 row text before any new investigation -- P-227 already established composeSitePlanModelForParcel is symmetric between composed and standalone, and P-231 split out the descriptor gap as a live candidate for THIS row per the dispatch's own text.",
    "Read the actual failing code path: services/engine-api/src/routes/parcel-terrain.ts (runSitePlanExportJob, classifySitePlanExportJobError), packages/engine-core/src/site-plan/author.ts (composeSitePlanModelForParcel, authorParcelSitePlanExport, resolveSitusAddressForExport call), packages/engine-core/src/site-plan/emitters.ts and packages/engine-core/src/parcel-terrain/emitters.ts (emitDxfSitePlan/emitIfcSitePlan, runDxfWorker/runIfcWorker) before forming a hypothesis.",
    "Rule P-231's descriptor in or out FIRST, per the dispatch's explicit instruction, by reading resolveSitusAddressForExport and tracing what it feeds (situs-based edge-role labeling only) rather than assuming it explains the timeout.",
    "Read the engine's own Cloud Run request log for the exact failed job window (project hauska-prod-497015, hauska-engine-api, 2026-09-16T00:48-00:52Z) BEFORE hypothesizing further -- per dispatch known-trap 'read the job table, not the tool output' and the standing falsifier-before-scope practice.",
    "Query the engine's own DATABASE_URL (Neon, resolved live via gcloud secrets, not guessed) directly for the site_plan_export_jobs row and the parcel-terrain-model atom's artifact/coverage history for 48021:34049 -- to check whether this parcel's data is inherently large/slow (would support a legitimate-slowness verdict) or trivially small (would point elsewhere).",
    "Check the deployed Cloud Run service's own annotations for run.googleapis.com/cpu-throttling, since the dispatch's own known-traps section named and then dismissed CPU starvation -- re-examine only if new evidence (not re-litigate on the same grounds already refuted).",
    "Pre-registered falsifier checks to run literally: (1) name the throw site with file+function; (2) an explanation must not also predict the composed path failing -- if it would, it's wrong; (3) rule P-231 in or out explicitly; (4) do not fix by raising a ceiling -- if genuinely slow, say so with a measurement and propose the real change; (5) verify success on the job table + artifact download, not a 200/202; (6) if the cause is outside hauska-engine, say so."
  ],
  "falsifier": "Pre-registered per dispatch (five falsifiers listed above plus the known-traps checks); none were skipped.",
  "leaveOpen": "Whether the recommended Cloud Run --no-cpu-throttling change is applied is an operator/integration-seat decision, not this lane's to execute -- the dispatch's own 'do not deploy or merge' stop-point is narrower than and wins over the general canon-preamble's 'deploys are planner-owned.'"
}
