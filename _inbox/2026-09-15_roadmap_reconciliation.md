---
id: 2026-09-15_roadmap_reconciliation
title: SmartCity roadmap — the milestones, the order, what blocks each, and what was promised
date: 2026-09-15
last_updated: 2026-09-18
status: active roadmap (live row status is generated in _design/SMARTCITY_TRACKER.md)
kind: reconciliation
owner: nick
programs: [OPS-17, OPS-25]
related:
  - _inbox/2026-09-15_design_thread_HANDOFF.md
  - _design/INDEX.md
  - _decisions/2026-09-15_design_ratification_pass.md
  - _decisions/2026-09-15_plan_review_role_gate_deferred.md
  - _decisions/2026-09-14_staff_identity_and_department_rbac.md
sources:
  - smartcity-dashboards origin/main 423450c (PR #75, `g153`, merged 2026-09-18) and smartcity-os origin/main 85a332e (PR #60, `g162`, merged 2026-09-18), read by the planner via gh the same day
  - design-completion-gate.mjs re-run 2026-09-18T21:15:53Z, after A-159 made R3 RUN the instruments it counts: `exit 1`, verdict UNFINISHED, 4 R3 findings (it read `exit 0` and 0 findings before that change)
  - smartcity-tracker.mjs re-run 2026-09-18T21:15Z after regrading G-153, G-162 and G-160: PASS, M2 4/6, M3 5/9, M5 4/5
  - _inbox/2026-09-18_g153-fleet-police-lens_close.json, _inbox/2026-09-18_g162-v1-finance-honesty_close.json and _inbox/2026-09-18_g160-served-commit-parity_p2_close.json, filed from seat worktrees 2026-09-18 by `P:/tmp/file-closes.mjs`, each of the 15 artifacts plus one directory verified byte-identical by sha256 (OPS-17 A-161)
  - OPS-17 A-160 (the `smartcity-os` intent-row posture correction) and A-161 (the four-lane regrade)
  - smartcity-dashboards origin/main 7487d7c0, read 2026-09-18T20:19Z by design-completion-gate.mjs
  - _design/smartcity-flood-study, smartcity-map-dock, plan-review-departments and smartcity-overview-lens, read on disk 2026-09-18T20:19Z (the g148 lane's in-flight instruments, uncommitted)
  - node scripts/govtech/smartcity-tracker.mjs, node scripts/govtech/design-completion-gate.mjs and node scripts/govtech/design-instrument-exits.mjs, all re-run 2026-09-18 (the gate reads 18 of 20 folders instrumented, zero findings, exit 0; the exits instrument reads 18 run, 4 failing, and the gate's exit 0 alongside it)
  - smartcity-dashboards origin/main 96fdafb and smartcity-os origin/main 1f0262f1, read 2026-09-18T15:09Z
  - smartcity-dashboards origin/main 7267c3f and smartcity-os origin/main a400f7be, read 2026-09-18 earlier the same day
  - _inbox/2026-09-18_planner_three_lane_verify.json (the planner's twice-run probe of all three lanes and the four deployed apps, 14:42Z and 15:09Z)
  - _inbox/2026-09-18_planner_g135_credential_probe.json (the planner's own verification of the minted credential, 8 cells)
  - _inbox/2026-09-18_g135-mint_close.json and _inbox/2026-09-18_g154-dev-services-live_close.json (the two closes collected 2026-09-18)
  - Nick and Jaime call, 2026-09-15, transcript and summary supplied by the operator
  - smartcity-dashboards origin/main f776b4bf, web/index.html
  - smart-files origin/main 61c84f6
---

# SmartCity roadmap

**Live status is in `_design/SMARTCITY_TRACKER.md`, generated by `node scripts/govtech/smartcity-tracker.mjs`.**
This page carries the milestones, the order, what blocks each one, and what has been promised to
Bastrop. It does not copy row statuses, because copied status is how this program lost track of
itself: four OPS-17 rows read OPEN while their own closes said closed. The tracker reads the plan of
record and every lane close, and it refuses when they disagree.

## Where we are, 2026-09-18

Every SmartCity design is approved except People and access (G-143), which is DRAFT awaiting
ratification. The staff dashboards run on DigitalOcean behind `app.smartcityos.io`. The operator ruled
that **Bastrop is the proving pack**: designs are implemented against the live data Bastrop already has
wired, and a pass on `template-city` is not a pass.

Five lenses read live Bastrop data on `bastrop_tx`: Development services (MyGov), Fleet (Samsara),
Police (Spireon), Public works (Power BI, GoTo) and Fire and EMS (FirstDue). Finance does not yet, but its
path now exists. G-159 proved Bastrop's budget is live and readable, and built the v1 routes that serve
it. Two DigitalOcean rows, D-14 and D-13, make those routes reachable.

**Three rows moved today, and the production ship ran.** D-14 and D-13's code were read at source by the planner at 2026-09-18T16:26Z, and the ship was performed at 17:29Z (OPS-25 A-10). **D-14 LANDED:** `walrus-app` builds from `main` and serves `1f0262f`, so the three G-159 routes that used to fall through to the SPA shell now answer `401 platform_internal_required` while a fake path still answers the shell. **D-13 LANDED:** `dolphin-app` deployment `e98f127c` is ACTIVE serving `7487d7c0`, read back byte for byte against the dashboards' `origin/main`, with `SMARTCITY_V1_PLATFORM_BASE` set to `walrus-app` in the same deploy. The dashboards no longer read the GCP copy of v1. The ship is a two-part change because an unset base is now a refusal rather than a default, so the code and the variable had to move together in one deploy.

**Production moved at 17:29Z, and the surface is graded rather than assumed.** `app.smartcityos.io` served `3d3ec62` from before the Finance lens, the Development services corrections and the city-default fix until that deploy; the finance route answered `unknown lens` there. On the deployed surface now: the finance route serves, eight of eleven registered domains on `bastrop_tx` carry records with every record-less domain stating a basis, eight platform routes return payloads byte-identical to the host production read before the ship, and the work-order Subject column carries no resident free text. **Two of D-13's clauses are worth naming because they were the reason it existed:** the finance route's `unknown lens`, and the work-order Subject cell, which rendered the vendor's free-text title where residents write names and phone numbers. **D-13 is LANDED and not CLOSED:** scope 5, a bake window in which GCP `smartcity-api` logs zero platform requests, is ungraded, and nothing was decommissioned. A merge still deploys nothing.
**`dolphin-app`'s configured env carries no `SMARTCITY_V1_PLATFORM_BASE`, and at `7487d7c0` an unset base
is a refusal with a named basis rather than a silent default**, so shipping `main` without setting that
variable in the same deploy would take every v1 platform feed from serving to refusing.

**Production is also worse than `main` on one point that matters.** At `3d3ec62` the Development services
work-order table shows the vendor's free-text title, which is where residents write their names and
phone numbers. `main` no longer does (G-154). The operator RULED on 2026-09-18 that `main` ships to
`dolphin-app` (OPS-17 A-149); it is ONE deploy that must also set that variable, and it is the
`d14-d13-v1-reach` lane's last step.

**The credential that blocked every Bastrop proof is CLEARED, and it cost the program three days.** A tenant-private pack answers 401 to an anonymous read, so no lane could prove anything on `bastrop_tx` on a deployed app. G-135 asked the substrate seat for a verification-scoped key on 2026-09-15; it was never minted, and G-135's own artifacts sat untracked for three days, so nothing surfaced it. **The `g135-mint` lane minted it on 2026-09-18 at 14:44Z.** `key_id 96e40316`, tenant `bastrop_tx`, active, stored at Secret Manager `hauska-prod-497015/hauska-tenant-key-bastrop-tx-lane-verification` version 1 and recorded in `_catalog/credential_access_index.json` with a one-command `howToUse`. The planner verified it at source with its own instrument rather than on report (`_inbox/2026-09-18_planner_g135_credential_probe.mjs`, 8 of 8 declared cells: 200 on `bastrop_tx` with the key, 401 keyless, 401 on a garbage key, 403 on `fixture-city`, 200 keyless on `template-city`, 404 on an unknown city). Any lane can now obtain it from a documented location without asking a human, which is G-135's first parcel and the reason six rows were sitting blocked.

**Two things that key did not fix, and one it exposed.** G-135's second parcel, a full-shell authenticated probe on `bastrop_tx` that mounts the map iframe, has still been run by no lane, so G-135 stays CLOSED-PARTIAL and not closed. The lane's CP1 also corrected the census the dispatch was written on: THREE active `bastrop_tx` keys existed, not one, including a dormant G-134 lane key and a G-154 lane key minted minutes earlier. Deciding whether to revoke the two non-pilot keys is the planner's and the operator's call, and neither was touched; revoking the G-154 one would break a lane that is mid-flight.

**All four lanes that were in flight landed on 2026-09-18, and three of their closes were stranded.** `g148-design-instruments` had already closed and filed to `main`. `g153-fleet-police-lens`, `g162-v1-finance-honesty` and `g160-served-commit-parity` parcel 2 each filed a close inside their OWN seat worktree, where nothing in this repo reads it, so `smartcity-tracker.mjs` printed `PASS` while blind to all three (OPS-17 `A-161`). That is a starved control, not a wrong verdict: the mechanism was built, correct, and never fed. The three closes and their 15 referenced artifacts plus one directory are now filed to `main` `_inbox`, each verified byte-identical by sha256 and refusing if any cited file was absent from the worktree, and the tracker then fired as designed and named all three. Every close was read at source before its row was regraded, not accepted on report: the planner re-ran both `g153` instruments, confirmed all five `g160` files present on clean trees in both products with two unpushed commits each, and confirmed both PRs open and MERGEABLE.

**Both branch collisions are now cleared by merging, and a merge still deploys nothing.** PR #75 (`g153`, `smartcity-dashboards`) and PR #60 (`g162`, `smartcity-os`) were MERGEABLE and CLEAN and are now merged, at `423450c` and `85a332e`. That is what makes the next wave dispatchable: `g152` writes the dashboards and `g163` writes `smartcity-os`, and each would otherwise have branched onto a main that did not yet carry the lane before it. Merging is not deploying, because `deploy_on_push` is unset on all three apps.

**And the design gate went from green to RED while this page was being updated, because the operator fixed the rule that made it look green.** A-159 changed R3 from testing that a design's `check.mjs` EXISTS to testing that it PASSES. Re-run at 21:15:53Z the gate now reports `exit 1`, `verdict: UNFINISHED`, and four R3 findings, where an hour earlier it reported `exit 0`, `verdict: FINISHED` and zero findings on the same boards. **The gate did not change its mind; it had been answering a different question.** The 11 findings `g148` disclosed with no owner are now carded as **G-164**, which is the row that turns this gate green, and `G-146` still does not close on `G-148` alone (OPS-17 `A-152`, `A-159`).

## The milestones

The tracker groups every row under one of these five, from the same declared list, so the two cannot
disagree about which row belongs where.

### M1. Staff on v2: the soft launch

Real Bastrop staff sign in to v2 as themselves, and see what their department should see.

**The order.** Ship `main` to `dolphin-app` (RULED 2026-09-18, OPS-17 A-149; performed by the
`d14-d13-v1-reach` lane as its last step, with the configured platform base in the same deploy). Then the operator tells
staff to use `app.smartcityos.io`, which starts the D-12 bake. The WorkOS wiring (G-134) is built and
waits only on the operator's WorkOS credentials for its live test. The People and access design (G-143)
needs ratifying before department access (G-127) and the plan-review role gate (G-144) are built. The
access log (G-158) comes after G-134.

**What blocks it:** the ship decision, the operator's word to staff, the WorkOS credentials, G-143's
ratification, and an owner and turnaround for offboarding. A role limits nothing yet: every signed-in
account can open every lens.

### M2. The next deliverable: Finance, hotel occupancy tax, RBAC, city management board

Named by the operator on 2026-09-17 as one deliverable. RBAC is M1's work and is not repeated here.

**Finance, where it stands.** G-159 is closed-partial. OpenGov's Budgeting and Performance service
returns Bastrop's 12 budgets and reproduces the operator's capture to the digit. Four v1 platform routes
serve it, merged to `smartcity-os` `main` and not deployed. The dashboards' finance route now refuses a
request that names no city. OpenGov's older REST API returned 32 entities, none of them Bastrop's, so
nothing was built on it. Fund ledger and department spend are absent from every source G-159 read, and
the lens draws them as unaccounted, which is the honest state.

**The order for Finance.** The `d14-d13-v1-reach` lane puts v1 `main` on `walrus-app` (D-14), then points
the dashboards at `walrus-app` instead of the GCP copy of v1 (D-13). **Both are now done.** Then comes the
`bastrop_tx` finance grant, which is G-159's leave-behind. After it, the G-156 lens shows the budget as
measured with no lens change. G-162 fixes three v1 finance defects that G-159 found and left alone: a
budget filter that counts "do not use" budgets, a clamp that turns a negative difference into zero, and a
default tenant. It ran after D-14, so that D-14's repoint changed no behaviour, and **G-162 is now CLOSED-PARTIAL at 2026-09-18 and its PR #60 is MERGED to `smartcity-os` `main` at `85a332e`.** Two of its three defects are proven by live reads on the non-production app `g162-v1-uat`; the third is proven only at the ROUTE level, never on a deployed surface, because no deployed request can present a missing tenant binding, and that measured gap is why the row is partial rather than closed. Its close also names a finding the fleet needs: **the `staging` `smartcity-os` database IS the production one**, so any future lane told to test against staging is writing to production data.

**Permit revenue is wrong at the source, not just unreachable.** `mygov_fees` shows a 150 percent
collection rate, because a fee listed in several report windows is counted once per window. Which window
is authoritative is a question for Bastrop's finance owner, not a code question.

**Hotel occupancy tax (G-137)** waits on the Azavar reply being sent and Khalid's answers being filed
verbatim. The filings design's block (G-138) is closed. **The city management board (G-157)** waits on
a capture of the v1 combined dashboard, which no lane can take because v1 sits behind a Google sign-in.

### M3. Lens builds on Bastrop live data

Every lens design built into `smartcity-dashboards` and proven on `bastrop_tx`.

**The order.** The credential is no longer the gate: `g135-mint` minted it 2026-09-18 and any lane reads
it from Secret Manager in one command. **G-154 (Development services) is CLOSED-PARTIAL**, proven on the
live `bastrop_tx` surface on `d12-main-uat` at 14:49Z: three of its four acceptance items PASS and the
fourth is named unmeasured because the live surface draws no workload ranking to measure it on. **G-161
HAS LANDED** (planner read at source, 2026-09-18T16:26Z): its six `|| "template-city"` fallbacks are
replaced in `src/server.mjs` at `7487d7c0`, and live on `d12-main-uat` at that commit `/api/city-domains`,
`/api/city-identity`, `/api/shell` and `/api/lenses/finance/sources` all answer `400 city_key_required`
keyless, where the 15:09Z read answered 200 with the demo city; naming `template-city` explicitly still
answers 200. **No close is filed for that lane**, so the row moves on the deployed record and the lane's
own acceptance items and leave-behind are still owed.

**G-153 (Fleet and Police) LANDED AND CLOSED-PARTIAL on 2026-09-18, and it is MERGED.** PR #75 merged to `main` at `423450c`, so the next dashboards lane branches onto it instead of colliding with it. **The planner graded it CLOSED-PARTIAL where the lane graded itself `closed`, for one measured reason:** the row's own instrument ends "both surfaces are reached on the DO app per D-12" and the DELIVERED build is not on the DO app. Nothing was deployed, which is correct because main is not live, but it is exactly the shape of gap for which `g160`'s lane graded itself partial, so the two rows are now graded consistently. Both instruments exit 0 with non-zero matched inputs (fleet pairs 3, operatorRefs 22, verbatim 11 of 11; police pvIds 10, plateScanTokens 11, verbatim 16 of 16) and both are proven by violation, re-run by the planner rather than read from the close. **It also carried G-135's parcel 2 and DID run it:** the full-shell authenticated probe mounts the map iframe at `app.smartcityos.io` with 6 of 6 tiles and 12 of 12 layer responses arriving from the frame, so the only thing G-135 had left unmeasured is measured.

**LEAVE_BEHIND WITH NO OWNER, and it is the thing that decides whether the lens is useful: the vendor mapping.** All 102 live fleet and patrol records fail their declared shape, so once this is deployed Bastrop's Fleet and Police regions read `refused` with the faults named. That is a declared absence and not a blank, which is correct fail-closed behaviour, but until the mapping lands the two lenses show nothing useful. Also left: the two design dumps lag the code and need a recapture, and Fleet ships no durable `violate.mjs`, which is a planner call.

**The queue, re-ordered on a measured dependency rather than on the row order (OPS-17 `A-154`, updated).** **G-152 (Public works and Fire and EMS) HAS LANDED AND MERGED** (PR #76 to `ea27024`), and it released the dashboards slot to **`g153-vendor-mapping`**, which is the dashboards lane now. **G-151 (Parks) is NEXT AFTER THAT**, then **G-149**. G-149 cannot move up for the reason A-154 measured and A-159 confirmed: it INHERITS `_design/smartcity-flood-study/check.mjs`, which is now DELIVERED rather than being built, and that design carries one of G-164's findings, the stale rainfall-atlas claim, which is entangled with G-125's broken `parsePfdsDepthTable` **and is now G-165's to fix at the parser**. G-155 (Smart Files) and G-150 (plan review) build in their own repos and are closed. G-155 still owes a check on Bastrop's own files.

**What blocks it:** one dashboards lane at a time, and that is the real bottleneck, because the queue (G-152, G-151, G-149) is longer than the number of lanes that may run. The credential is not a blocker for any row here, and neither G-161 nor D-13 is any longer.

### M4. The DigitalOcean migration complete

Every SmartCity service on DigitalOcean, and the GCP originals decommissioned after a clean bake.

**The order.** D-5, D-9, D-10 and D-11 are closed. D-12 is closed-partial: `app.smartcityos.io` now
resolves to `dolphin-app`, and the bake starts when staff move. **D-14 and D-13 both LANDED on 2026-09-18** (OPS-25 A-9 and A-10): `walrus-app` `e243db0a` builds from `main` and serves `1f0262f`, with the three G-159 routes
answering `401 platform_internal_required` where a fake path answers the SPA shell; and the ship ran at 17:29Z, so `dolphin-app` deployment `e98f127c` is ACTIVE serving `7487d7c0` with `SMARTCITY_V1_PLATFORM_BASE` set to `walrus-app`. After them comes the SmartCity decommission bundle
(D-20), then the bakes, then decommissioning. A full GCP exit is being planned by another seat, and this
milestone is its first phase.

**What blocks it now:** the bake, which needs staff on `app.smartcityos.io`. **The ship HAS happened, and it was a two-part change for the reason it was predicted to be.** Production served `3d3ec62` from a deployment ACTIVE since 2026-09-18T00:27Z, where the finance route answered `404 unknown lens`; it now serves `7487d7c0` from deployment `e98f127c` ACTIVE at 17:29Z, where the finance route serves. The two parts moved together because an unset base is a refusal rather than a default, so shipping `main` without the variable would have taken every v1 platform feed from serving to refusing. **D-13 is LANDED and not CLOSED**, because its scope 5 bake is ungraded: a window in which a lens is actually opened and GCP `smartcity-api` logs zero platform requests. The GCP
`smartcity-api` is no longer what the dashboards read, so it is D-20's bundle rather than this row's
blocker; what still reads GCP is whichever surface has not been repointed, and that is what D-20
enumerates. **One new row came out of the ship's acceptance, G-163:** `firstdue/apparatus` and `goto/call-summary` answer an opaque Cloudflare `504` on `walrus-app` where GCP answered a structured `503` naming the vendor permission to request. Both feeds were already `unavailable`, so nothing broke; the stated reason is what degraded.

### M5. Design complete, and the controls that keep it honest

The design completion gate exits 1, and it is honest about why: every shipped nav surface is designed or excluded by a dated ruling, but four designs past DRAFT now carry an adversarial read that does not pass.

**The gate read `exit 0` and `verdict: FINISHED` an hour earlier, and that is a lesson about the instrument rather than about the designs.** At 2026-09-18T20:32:13Z it read `20 design folders, with an instrument: 18`, zero findings under every rule, and `exit 0`; at 21:15:53Z, on the same boards, it reads `exit 1`, `verdict: UNFINISHED`, four R3 findings. Nothing in the designs changed in between. **What changed is R3: it used to test that a design's `check.mjs` EXISTS (`design-completion-gate.mjs:157-163`, reading no exit code), and the operator's A-159 ruling made it test that the instrument PASSES.** So the earlier green was a true answer to the wrong question, and the honest reading of a green R3 before that change was "instrumented", never "clean". **The Citizen lens (G-142) and Records search (G-147) are DRAWN AND CLOSED as of 2026-09-18**, so both of the gate's R4 findings are gone, and R4 is not what is failing.

**All four instruments `g148` delivered FAIL, and the gate now says so itself.** Measured at the same disk state at which the gate was still reporting `exit 0` (the table is below, and `A-156` carries the full reading). `smartcity-flood-study` is the one worth using as the example: its `check.mjs` exits **1** on the
shipped boards, because the design states that naming a depth by return period is unavailable since it "would
need a local rainfall atlas nobody has cited yet", while the engine already carries `rainfallSource:
'noaa-atlas14'`, cites NOAA Atlas 14 seven times, and renders the default through
`returnPeriodYearsForDepthInches` as "100-yr (NOAA Atlas 14)". Its `violate.mjs` catches 30 of 30 planted
violations.

**The gate's R3 rule tests that an instrument EXISTS, never that it passes.** R3 asks whether a folder past DRAFT has a `check.mjs` and answers by testing `hasCheck` (`design-completion-gate.mjs:157-163`), reading no exit code. Its own wording is honest about this, printing "carries an instrument", but `exit 0` from an artifact named `design-completion-gate` reads as "the designs are clean" and four of them are not. **This is the concrete instance of why `A-152` ruled that G-146 does not close when G-148's instruments land. Both halves of the repair are now DONE, ruled by the operator under `A-159`:** the four design repairs are carded as ONE row, **G-164**, and the gate RUNS the instruments it counts, so it can no longer report green over a failing design. **G-164 is now the row that turns this gate green**, and until it lands the gate exits 1 by design.

**Why the flood-study finding is not a one-line copy fix.** G-149's own row records the contradicting
fact: G-125's unowned leave-behind is that NOAA's Atlas 14 HDSC endpoint changed payload shape,
`parsePfdsDepthTable` never matches, and every no-parameter study in every county silently uses Bastrop's
9.5in regional default regardless of location. So the engine cites NOAA Atlas 14 as though it were a live
value while that citation is produced by a broken parser. The design's stated reason is wrong and its
conclusion may still be right, and the two facts have to be reconciled together: a design-only correction
would entrench the parser defect by making the page agree with a value the parser invents. G-149's mission
carries both, which is a second reason it is not in the current wave.

**One detail of that reading stays true and is worth keeping.** The two folders without an instrument are `plan-review` and `smartcity-place-tab`, and both are SUPERSEDED by dated rulings, so neither requires one (R3's instrument-required set is exactly `RATIFIED`, `APPROVED` and `IN REVIEW`). That exemption is correct, and it is why 18 of 20 is the right denominator rather than a gap.

**All four instruments the gate counted FAIL, and they are the four `g148` delivered** (OPS-17 `A-156`). Run at the same disk state at which the gate was still reporting green:

| Instrument | Exit | Its finding |
|---|---|---|
| `smartcity-overview-lens` | **1** | `Sparse.dc.html` is granted 1 of 10 sources and still promotes Connections above the decision |
| `smartcity-flood-study` | **1** | the rainfall-atlas claim (see above) |
| `smartcity-map-dock` | **1** | `Expand.dc.html`: the tab nav drops "Licenses" |
| `plan-review-departments` | **1** | the design states the product has "no department model at all", where the README's source-state section says otherwise |
| `smartcity-fleet-lens` | 0 | 2 artboards pass (`g153`'s, in flight) |
| `smartcity-police-lens` | 0 | 4 artboards pass (`g153`'s, in flight) |

**These are design defects and not instrument defects, proven rather than assumed:** each failing folder's
`violate.mjs` exits 0, catching its planted violations on a real artboard (`plan-review-departments` 17 of 17)
and confirming that a repaired copy passes with a matched-input count. **And the whole reading is now one
instrument rather than a hand-run loop:** `node scripts/govtech/design-instrument-exits.mjs` prints every
instrument's exit code beside the gate's verdict and derives that ownership itself, with a 6 of 6 self-test
(OPS-17 `A-157`).

**In fairness to the gate, its own wording does not overclaim.** It prints "every design past DRAFT **carries
an instrument**", which is exactly what it tested and exactly what is now true. The defect is that `exit 0`
from an artifact named `design-completion-gate` will be read by every downstream consumer as "the designs
are clean", and four of them are not. **So four designs carry an open, reproducible finding, and since A-159 they have ONE owner rather than none: G-164.** The `g148` lane closed at 20:43:47Z and **disclosed exactly this**: 11 findings across the
four designs, listed by name as `leave_behind` and deliberately not fixed, because its dispatch was to
instrument what exists and not to redraw it. Its close also records that it expected these four RATIFIED
designs to be clean and they are not (OPS-17 `A-158`). So the count is no longer a planner's reading against a
lane's claim: it is one number both report independently, and **the only open question is who fixes them.** The repairs are design work in `doc_repo` (planner-owned), and each is an adjudication rather
than a typo: whether the flood study may name a return period at all depends on G-125's broken parser;
whether the map dock's tab nav should carry Licenses is a navigation question; and whether the product has a
department model is a factual question about `smartcity-os` that A-145 already answered in the affirmative.

## In flight

**`g152-public-works-fire-ems` LANDED AND MERGED 2026-09-18** (PR #76 to `smartcity-dashboards` `main`
`ea27024`, `test` pass, `a11y` pass). **It released the dashboards slot to `g153-vendor-mapping`**,
which was queued behind it.

**Six dispatches are compiled, gated (`allow`) and handed over: `g163-opaque-platform-routes`,
`g164-design-repairs`, `g146-design-declarations`, `g165-noaa-parser`, `g153-vendor-mapping`, and
`g152` before it.** Five of those are the wave that is live now.

**The 22:12Z wave has resolved, and each resolution was read at source rather than taken on report.** `g163-opaque-platform-routes` **CLOSED** (PR #61 merged at `03cebec5`, shipped to `walrus-app` as deployment `2657e628`, all three clauses verified live on production by the planner, OPS-17 `A-170`) and `g164-design-repairs` **CLOSED-PARTIAL** (the design gate moved from four R3 findings to one, OPS-17 `A-167`). `g146-design-declarations` and `g165-noaa-parser` hold worktrees and no claims; `g153-vendor-mapping` holds the one live claim and, with it, the one dashboards slot. **The earlier reading of this page was wrong twice, and the corrections matter more than the fact.** An audit at about 21:50Z found no claim and no worktree for any of the five and recorded the absence; the lanes started at 21:58Z and 22:02Z, minutes after the operator fired them, so that absence was a property of the minute rather than of the lanes, and OPS-17 `A-166` carries the correction. **A measurement of an absence is a claim about an instant, and the instant has to travel with it.** The second correction is `A-168`: re-measured with the wave provably started, three of the four active lanes still held no claim, which is a real miss rather than a snapshot error. **The claim guard fires for some lanes and not others, and the ones it misses are exactly the ones where a second session would duplicate the work instead of standing down.**

**`g164-design-repairs` is writing into the planner's own checkout, `P:/doc_repo`, rather than a worktree.**
Three of the four R3 designs (`_design/plan-review-departments/`, `_design/smartcity-map-dock/`,
`_design/smartcity-overview-lens/`) were dirty in the main tree at 22:07Z, while `g146` and `g165`, whose
deliverables are the same kind of thing, each took a worktree. **Anyone committing in `P:/doc_repo` while
this lane runs must add by explicit pathspec and never `-A`**, because `-A` from this seat would sweep a
lane's half-finished design work into a planner commit.

**The throughput limit is unchanged and it is the program's real constraint.** Every lens renders into
`web/app.js` and `web/index.html`, so one dashboards lane at a time (A-144). `g153-vendor-mapping` holds
that slot now; **G-151 (Parks) and G-149 (flood study) queue behind it, in that order**, which is why
G-151 is not the next thing to fire even though `g152` has landed.

## The wave that landed, and the wave that is next

**All four lanes from the 2026-09-18 wave have landed, and every one of their rows is regraded** (OPS-17 `A-161`). The operator hand-carried `g135-mint`, `g154-dev-services-live`, `d14-d13-v1-reach`, `g161-never-default-a-city`, `g142-citizen-lens`, `g147-record-search`, then `g160-served-commit-parity`, `g148-design-instruments`, `g153-fleet-police-lens` and `g162-v1-finance-honesty`. **Three of the last four filed their closes inside their own seat worktrees, where this repo could not read them**, so the tracker reported `PASS` while blind to all three; they are now filed to `main` `_inbox`, byte-verified, and the tracker named all three as soon as it could see them. **`g152-public-works-fire-ems` and `g163-opaque-platform-routes` were compiled in that same session and are now the next wave**, and what held them was a real collision rather than a queue: both would have branched onto a main that did not yet carry the lane before them. **PR #75 (`g153`) and PR #60 (`g162`) are merged**, which is the condition that releases them, and a merge deploys nothing because `deploy_on_push` is unset on all three apps. **All were verified to carry the current canon markers (v49001500 preamble) with zero mismatches**, so a hand-carry will not trip the canon gate. Every status below was read at source by the planner, not taken from a lane's report or from the fact that a dispatch was sent.

| Lane | Rows | Status, verified at source | The evidence |
|---|---|---|---|
| `g135-mint` | G-135 | **LANDED 2026-09-18T14:44Z.** Its close is collected to `main` and its artifacts are no longer untracked. The row stays CLOSED-PARTIAL because parcel 2 is unmeasured | Secret Manager `hauska-tenant-key-bastrop-tx-lane-verification` version 1 ENABLED; the planner's own probe returns 8 of 8 declared cells on `d12-main-uat` at `53ade8a9`; the index reads `ACTIVE` with a `key_id` |
| `g154-dev-services-live` | G-154 | **LANDED.** Its branch is merged to `main` and the row is regraded CLOSED-PARTIAL: three of four acceptance items PASS, item 2 named unmeasured | live export at 14:49Z, 250 rendered rows across five queues on `d12-main-uat`, `manifest.auth` recording that a key was used and never its value; close, CP1, CP2 and probe on `main` |
| `d14-d13-v1-reach` | D-14, D-13 | **D-14 LANDED AND D-13 LANDED; D-13's production step was run by the planner at 17:29Z (OPS-25 A-10) because that lane holds no claim and filed no close.** D-13 stays LANDED and not CLOSED: scope 5's bake is ungraded | D-14: `walrus-app` `e243db0a` ACTIVE, building from `main`, `source_commit_hash` `1f0262f` = `smartcity-os` `origin/main` HEAD; the three G-159 routes answer `401 platform_internal_required` while `xyz-nonexistent` answers the 200 SPA shell. D-13: `dolphin-app` `e98f127c` ACTIVE at `7487d7c0` read back byte for byte, `SMARTCITY_V1_PLATFORM_BASE` set to `walrus-app`; the planner's acceptance instrument passes every clause on the deployed surface |
| `g161-never-default-a-city` | G-161 | **LANDED, read at source at 2026-09-18T16:26Z.** CP1 was filed; no close is in this worktree, and the row may own cells beyond the dashboards | `src/server.mjs` at `7487d7c0` carries six `G-161. Was \|\| "template-city"` sites plus the module-level and searchParam defaults; live on `d12-main-uat` at `7487d7c0`, `/api/city-domains`, `/api/city-identity`, `/api/shell` and `/api/lenses/finance/sources` all answer `400 city_key_required` keyless while `template-city` named explicitly still answers 200 |
| `g142-citizen-lens` | G-142 | **CLOSED 2026-09-18.** Claim released. The planner re-ran all three instruments rather than taking the close's word: `check.mjs` exits 0 with every matched-input count non-zero, `violate.mjs` catches 16 of 16, and the gate's `lens:citizen` R4 line is gone | `_design/smartcity-citizen-lens/` drawn against `7487d7c0`; premise read from the product's own source (0 of 11 domains, 0 of 3 route literals); its sharpest finding came off the SHIPPED page (`None on file` is outside the declared badge vocabulary); falsifier filed and unresolved |
| `g147-record-search` | G-147 | **CLOSED-PARTIAL 2026-09-18.** Claim released. The planner re-ran its instruments: `check.mjs` exits 0 with all 18 predicate counts non-zero, `violate.mjs` catches 24 of 24 with boards restored, and the `records` block in `surface_coverage.json` flipped. Records search PASSES; Compass is DEFERRED, not dropped | `_design/smartcity-records-search/` drawn against `7487d7c0`; the applicant view adjudicated COVERED by the RATIFIED `smartcity-applicant-precheck` and not redrawn; the close records that no seat worktree was created, against the dispatch's preference |
| `g160-served-commit-parity` | G-160 | **LANDED 2026-09-18, CLOSED-PARTIAL.** Parcel 2 armed the check: `SERVED_COMMIT` is set TWICE on the Cloud Run revision (env var and label) from the same resolved value, and the console reports the commit it serves without holding a platform credential, refusing rather than defaulting. **The planner verified it at source rather than on report:** all five files present on CLEAN trees in BOTH `plan-review-worktrees/g160-served-commit-parity` (`cdd4cb3`) and `smart-files-worktrees/g160-served-commit-parity` (`2c5d4e1`), each two commits ahead of its `origin/main` and **UNPUSHED**. **What holds it at partial: the row's instrument ends "its first scheduled run records whether Smart Files is split today", and no scheduled run can have happened on an unpushed branch.** A close claiming `closed` would assert a green live run this lane cannot produce | OPS-17 `A-153`, `A-161`; **left to the integration seat, named by the lane:** push both branches, open the PRs, and deploy both products THROUGH `scripts/deploy.mjs` rather than by hand, which is this check's own named bypass. **Record defect the lane disclosed rather than implied:** parcel 1's CP1 was overwritten by parcel 2's CP1 at the same untracked path and is UNRECOVERABLE; the lane searched the worktree, the inbox, the Cursor caches and all git refs and says so |
| `g148-design-instruments` | G-148 | **CLOSED 2026-09-18 (OPS-17 `A-158`). Claim released.** All four instruments delivered, the gate reached `exit 0`, and **all four instruments exit 1 on real findings: 11 across the four designs, 4 + 1 + 4 + 2, re-derived by the planner from each instrument's own total line rather than read from the close.** The lanes's close is honest about it: it records `contradicted` including that it expected the four RATIFIED designs to be clean and they are not, and it names the 11 as `leave_behind` rather than dropping them. **The 11 are real board defects, mostly stale negatives, and they now have ONE owner: G-164 (A-159)** | verified at source by the planner: the gate read zero R3 findings and `exit 0` when R3 only tested for existence, and reads `exit 1` with four R3 findings now that A-159 made R3 run the instruments; four `check.mjs` self-test 48/48, 45/45, 47/47, 57/57; violate proofs 17/17, 30/30, 21/21, 41/41; the tracker refused `DISAGREE G-148 row=open close=closed` until the row was regraded, and passes after. The surface-probe redirect it left is UTF-16, a PowerShell artifact, and is excluded from the commit |
| `g153-fleet-police-lens` | G-153 | **LANDED 2026-09-18, CLOSED-PARTIAL, MERGED as PR #75 to `main` `423450c`.** The first lens build, and it also **carried G-135's parcel 2 and RAN it**, so G-135's last unmeasured cell is measured. **The planner graded it CLOSED-PARTIAL where the lane graded itself `closed`**: the row's instrument ends "both surfaces are reached on the DO app per D-12" and the DELIVERED build is not on the DO app, which is the same gap shape for which `g160`'s lane graded itself partial. **LEAVE_BEHIND WITH NO OWNER: the vendor mapping.** All 102 live fleet and patrol records fail their declared shape, so Bastrop's Fleet and Police regions will read `refused` with faults named until it lands | its blocker D-12 had landed and `app.smartcityos.io` serves `7487d7c0`; both instruments exit 0 with non-zero matched inputs (fleet pairs 3, operatorRefs 22, verbatim 11 of 11; police pvIds 10, plateScanTokens 11, verbatim 16 of 16), both proven by violation (13 of 17 and 17 of 17), the three live-mapper defects shown by a violation that refuses 102 of 102 LIVE records with the faults named, and the map iframe mounted at `app.smartcityos.io` with 6 of 6 tiles and 12 of 12 layer responses from the frame. Close `_inbox/2026-09-18_g153-fleet-police-lens_close.json` |
| `g162-v1-finance-honesty` | G-162 | **LANDED 2026-09-18, CLOSED-PARTIAL, MERGED as PR #60 to `smartcity-os` `main` `85a332e`.** The lane's own grade stands. Two of the three defects are proven by LIVE READS on the non-production app `g162-v1-uat` (the two DNU budgets leave the production count, 6 of 12 to 4 of 12; the clamp is gone and the signed difference renders as a named conflict); **the third is proven only at the ROUTE level, never on a deployed surface**, because no deployed request can present a missing tenant binding and the lane did not stage a violation it could not produce. **Its close names a finding the whole fleet needs: the `staging` `smartcity-os` database IS the production one** | its blocker D-14 had LANDED, which was the exact condition its row set; each change was a bridge proven on a non-production app under A-146 rule 4. Close `_inbox/2026-09-18_g162-v1-finance-honesty_close.json`. Still owed by the operator: which MyGov fee report window is authoritative |
| `g152-public-works-fire-ems` | G-152 | **LANDED 2026-09-18, MERGED as PR #76 to `smartcity-dashboards` `main` at `ea27024`. THE LANE FILED NO CLOSE, NO CP1, NO CP2, AND HOLDS NO CLAIM IN THE REGISTRY AT ANY READING**, so this row is graded on the planner's own source measurement rather than on the lane's, and its `leave_behind`, falsifier and contradicted list do not exist (OPS-17 `A-166`). The planner re-ran both `_design/` instruments: public works exits 0 with 35/35 self-tests both directions, 4 artboards, non-zero matched inputs (pairs 14, badges 77, cells 69, numbers 188, cipIds 10, queueRefs 11, verbatim 8 of 8); fire and EMS exits 0 with 34/34, 2 artboards, non-zero matched inputs (pairs 4, badges 37, chips 12, cells 72, numbers 116, faIds 12, stnRefs 15, verbatim 5 of 5). The four matrix states have a real fixture | `test` pass 23s and `a11y` pass 8m28s on PR #76; `6aa3c80` is the lane's single clean commit; the four-state proof is `src/public-works-matrix.test.mjs`, exercising `mtx-zero-cell`, `mtx-unread-cell`, `mtx-hatch-cell` and a no-source zero. **The acceptance clause ending "both are reached on the DO app per D-12" is UNGRADED**, because a merge deploys nothing and nothing was deployed |
| `g163-opaque-platform-routes` | G-163 | **CLOSED 2026-09-18. MERGED as PR #61 to `smartcity-os` `main` `03cebec5`, SHIPPED to `walrus-app` as deployment `2657e628`, and all three clauses verified LIVE ON PRODUCTION by the planner** (OPS-17 `A-170`). The lane graded itself `closed-partial` honestly: its own proof app `g163-v1-uat` cannot satisfy the clause that names `walrus-app`, so this seat took the ship the lane was never assigned. **The row's own diagnosis was wrong and the row has been corrected:** it is not a Cloudflare `504` and not a handler defect, it is **DigitalOcean App Platform's edge discarding an application-generated 502, 503 or 504** and substituting its own 1263-byte HTML page. The fix is therefore a boundary rule, `server/edge-status.ts` installed once in `server/app.ts`, re-issuing those three codes as `424` with `x-orig-status` preserving the handler's code and the body untouched | the planner's three legs against `https://smartcityos.io` under the canonical `PLATFORM_INTERNAL_API_KEY`: pre-ship `--expect pre-fix` **7/7 PASS, exit 0** (both routes `504 text/html` 1263 bytes, vendor body NOT readable); post-ship `--expect post-fix` **7/7 PASS, exit 0** (both routes `424 application/json`, `x-orig-status 503`, `x-do-orig-status 424` meaning the edge forwarded the app's own status, handler bodies intact); and post-ship `--expect pre-fix` **5 PASS / 2 FAIL, exit 1**, which is what makes the pass non-vacuous. Every leg carried the paired controls (mutated bearer `401` both routes, third route `200` with 294 records, keyless `401`). `source_commit_hash` read back `03cebec5` byte for byte. **Left open by the lane and still open: `g163-v1-uat` is ACTIVE and shares production's Neon store; it is this seat's to tear down or re-point.** |
| `g164-design-repairs` | G-164 | **CARDED 2026-09-18 (A-159), COMPILED AND DISPATCHED 2026-09-18 (A-163).** The 11 findings `g148` disclosed, now ONE row because they are one class of defect (a claim checked against source that went stale, mostly a stale negative) and because repairing a design and proving it are one unit of work. **This row is what turns the design gate green:** since A-159, R3 runs each design's instrument, so the gate exits 1 until these land. Ten are unblocked; the flood-study rainfall-atlas finding is entangled with G-125's broken `parsePfdsDepthTable` and carries that half blocked | carded from A-158's disclosure; the four failing instruments are `plan-review-departments`, `smartcity-flood-study`, `smartcity-map-dock` and `smartcity-overview-lens`, all exiting 1 on the shipped boards as re-read at 21:15:53Z. **Dispatch `_dispatches/2026-09-18_g164-design-repairs_dispatch.md`**, compiled with `--mission-file` and passed through the real canon gate (`allow`). **The mission forbids repairing the flood-study rainfall-atlas finding in the imperative**, because a design-only correction there would make the page agree with a depth the broken `parsePfdsDepthTable` invents, so that one folder is deliberately left exiting 1 and the gate reports one finding rather than none until the parser is fixed. **It also tells the lane it must not loosen a `check.mjs` to make a finding go away, and that a disputed instrument goes in the close for the planner rather than being deleted** |

**A control that fired on two of the four lanes, and the two that skipped it were the two that never appeared.** `g148-design-instruments` claimed, ran, and released on its close at 20:43:47Z. `g153-fleet-police-lens` claimed and released. **`g162-v1-finance-honesty` ran unclaimed, and `g160` stopped without one.** The earlier wave was worse: **neither `d14-d13-v1-reach` nor `g161-never-default-a-city` ever appeared in the registry**, and both were compiled with the claim instruction in them. The claim is what tells a second session to stand down (exit 3) instead of starting the same lane, so on the lanes the operator believed were running, the guard that exists to prevent the 2026-09-14 duplicate-dispatch failure reported nothing. This is the shape `ENFORCEMENT.md` names directly: a control that fails to fire produces no complaint, and nobody finds the miss from the outside. **All four who claimed also released**, confirmed by the absence of every lane id from the claim status after their closes, so the practice works when it is run.
**Seven claims are now stale, and one of them is this program's.** `d12-dashboards-do-cutover` holds a claim on D-12 started 2026-09-17T22:46Z, now **22.4 hours old**, against a row that is already closed-partial; the other six are OPS-16 rows (`p249-envelope-unlock`, `p262-edge-labeller`, `p285-county-runner`, `p260-p263-setbacks-and-envelopes`, `p279-tagged-revision-security`, `engine-deploy-p260-p263`) and belong to that program's planner. The D-12 claim is left in place rather than released at this seat, because `lane-claim.mjs release` takes `--seat` and releasing it from here would record a release by a seat that did not make it. **It does not block the next wave:** claims are keyed by lane id, not by repo, and neither `g152-public-works-fire-ems` nor `g163-opaque-platform-routes` collides with it.

Nothing is in flight as of this write. Both lanes that were live proved on `d12-main-uat` and re-read its `source_commit_hash` before each probe. Worth knowing for the next read: the raw `*.ondigitalocean.app` hostnames for `walrus-app` and `dolphin-app`
reset the connection from this network (ECONNRESET, three of three retries, identical at both reads),
while `app.smartcityos.io` and `d12-main-uat` answer normally. That is why D-14 is read through the apex
rather than through `walrus-app` directly.

## Critical paths, stated as dependencies

| To reach | Needs, in order |
|---|---|
| Staff on v2 | ship `main` to `dolphin-app` → operator tells staff the URL → D-12 bake → WorkOS credentials → G-134 live test → G-143 ratified → G-127 |
| Finance live on Bastrop | D-14 → D-13 → G-159 grant, proven with the G-135 key → G-156 shows measured |
| Any Bastrop proof on a deployed app | ~~the G-135 verification key minted~~ **CLEARED 2026-09-18T14:44Z.** One `gcloud secrets versions access` command against `hauska-prod-497015/hauska-tenant-key-bastrop-tx-lane-verification`, recorded in `_catalog/credential_access_index.json` |
| The next lens build | ~~G-161 and D-13's dashboards change merged~~ **G-161 AND D-13'S CODE LANDED 2026-09-18.** **G-153 then G-152 have both closed and merged** (PR #75, `423450c`; PR #76, `ea27024`), so Public works, Fire and EMS, Fleet and Police now exist on `main`. **The dashboards slot is held by `g153-vendor-mapping`, which is what makes the two finished lenses show records instead of `refused`; G-151 (Parks) and G-149 queue behind it, in that order.** The only constraint remains one dashboards lane at a time |
| The flood study on the page | G-152 and G-151 (dashboards serialization) AND G-164 (the flood-study design carries one of its 11 findings, whose instrument is now `_design/smartcity-flood-study/check.mjs`, DELIVERED by `g148` and not to be rebuilt) → G-149, whose lane must ALSO fix G-125's NOAA parser, because the parser silently defaults every county to Bastrop's 9.5in while the engine cites it as a live value |
| Hotel occupancy tax | Azavar reply sent and answers filed → G-137 |
| City management board | v1 combined-dashboard capture → G-157 |
| GCP decommissioned | D-13 → a bake in which a lens was actually opened → D-20 |

## Owed by the operator

| Item | Blocks |
|---|---|
| ~~**Ship `main` to `dolphin-app`?**~~ **RULED YES 2026-09-18 (OPS-17 A-149).** **RULED YES 2026-09-18 (OPS-17 A-149) AND SHIPPED 2026-09-18T17:29Z (OPS-25 A-10).** The `d14-d13-v1-reach` lane holds no claim and filed no close, so the planner performed the ship as A-149's own named fallback: `dolphin-app` deployment `e98f127c` ACTIVE serving `7487d7c0` read back byte for byte, with the configured platform base in the same deploy. **Governed by nothing further.** D-13 stays LANDED and not CLOSED on its scope 5 bake | staff moving to v2 on the better surface: the work-order Subject cell no longer renders resident free text, and the finance route serves |
| ~~Hand-carry the three dispatches~~ **DONE 2026-09-18.** Operator: *"i have already sent all three"* | nothing; M2 and M3 now wait on the lanes |
| ~~**Hand-carry four compiled dispatches** (`g160-served-commit-parity`, `g148-design-instruments`, `g153-fleet-police-lens`, `g162-v1-finance-honesty`)~~ **DONE 2026-09-18.** `g148` and `g153` claimed their lanes, `g162` is running unclaimed, and `g160` stopped on a verdict its own artifacts refute | M5's gate (G-148), M3's next lens and G-135's last unmeasured parcel (G-153), M2's finance honesty (G-162), and the console-vs-API control (G-160) |
| ~~**Hand-carry `g160-served-commit-parity` PARCEL 2**~~ **DONE 2026-09-18.** Landed, verified at source by the planner, row regraded CLOSED-PARTIAL. **Its last two steps are the integration seat's, named by the lane:** push the two unpushed branches, open the PRs, and deploy both products through `scripts/deploy.mjs` rather than by hand, which is this check's own named bypass | the console-vs-API control. G-160 stays partial until a first scheduled run exists on a PUSHED branch, which no lane can produce from an unpushed one (OPS-17 `A-153`, `A-161`) |
| ~~**Rule whether the design gate should RUN the instruments it counts, and name an owner for the 11 design findings**~~ **RULED AND DONE 2026-09-18 (A-159).** The gate now RUNS the instruments it counts, so `exit 0` can no longer be read as "clean"; and the 11 findings are carded as ONE row, **G-164** | G-146's meaning is now honest, and **G-164 is the row that turns the gate green**. The gate reads `exit 1`, `verdict: UNFINISHED`, 4 R3 findings as of 21:15:53Z (OPS-17 `A-155`, `A-156`, `A-157`, `A-158`, `A-159`) |
| ~~**Compile `g164` as a lane, or work it at the planner seat?**~~ **DONE 2026-09-18 (A-163): compiled as a lane**, because it is 11 adjudications across four designs and each needs its own before-and-after instrument run. Its dispatch is `_dispatches/2026-09-18_g164-design-repairs_dispatch.md` | nothing now; G-164 is the row that turns the design gate green |
| ~~**CARD THE NOAA ATLAS 14 PARSER AS ITS OWN ROW**~~ **CARDED AND DISPATCHED 2026-09-18 (A-165) AS `G-165`.** The operator instructed the carding. It is `hauska-engine` `packages/adapters/src/hydrology/noaaAtlas14.ts`, the engine repo is free, and this is the one row in the program that could fire immediately rather than queue behind the dashboards serialization. Dispatch `_dispatches/2026-09-18_g165-noaa-parser_dispatch.md`, compiled with `--mission-file` and passed through the real canon gate (`allow`) | nothing now; G-165 unblocks G-164's one blocked half and makes the flood-study design repairable, which is why G-164's mission forbids repairing it in the meantime |
| ~~**Track the 12 PNGs in `_design/exports/send/`, or ignore them?**~~ **RULED 2026-09-18: IGNORE THEM**, by operator instruction, and done. They are now in `.gitignore` with the reason recorded beside them, and the `git check-ignore` result was NOT trusted as the evidence: `git ls-files --others --exclude-standard` and `git add -n` both showed them untracked and NOT ignored before the change, and both show zero after, with `git status --short _design/` clean. **That is what unblocks the `--violate` sweep**, and with it DESIGN-vs-UNPROVEN ownership across the 18 design instruments | the sweep, and therefore the answer to whether each design's failure is a design defect or an instrument defect (OPS-17 `A-164`) |
| ~~**DESIGN-vs-UNPROVEN ownership across the 18 instruments**~~ **MEASURED 2026-09-18T21:47Z, and the answer is DESIGN for all four failures.** Run as `node scripts/govtech/design-instrument-exits.mjs --violate`: 18 instruments run, 4 failing, and **all four fail with `violate exit 0`**, which means the violation IS caught, so the instrument is sound and the DESIGN is wrong. That settles G-164's 11 findings as real board defects rather than instrument artifacts, which was the thing the planner could not establish by reading. Fourteen instruments exit 0. Eight `_design` subdirs carry no `check.mjs` (two of them, `plan-review` and `smartcity-place-tab`, exempt as SUPERSEDED by the gate), and seven passing folders carry no `violate.mjs` because their second direction is embedded as self-tests inside `check.mjs`. **The script's own exit 0 means the MEASUREMENT ran, not that the designs are clean**; the gate is the control and it reads `exit 1`, UNFINISHED, 4 findings | nothing; the finding is filed, and the four designs are G-164's to repair |
| **Name an owner for the vendor mapping that leaves the Fleet and Police lenses empty** | whether G-153's build shows anything. All 102 live fleet and patrol records fail their declared shape, so after deploy those two regions read `refused` with faults named. That is correct fail-closed behaviour, but no row owns the mapping and the lenses are the deliverable (OPS-17 `A-161`) |
| **FIRE THE COMPILED DISPATCHES** (`g152-public-works-fire-ems` **LANDED + MERGED**; `g163-opaque-platform-routes`; `g164-design-repairs`; `g146-design-declarations`; `g165-noaa-parser`; `g153-vendor-mapping`) | M3's lens queue, the two opaque platform routes, the design gate, the declarations the review canvas omits, **the fabricated NOAA depth served to every county**, and whether the Fleet and Police lenses show anything at all. **`g163`, `g164`, `g146` and `g165` may all run in parallel:** four different repos, and `g164` and `g146` both write `doc_repo` `_design/` with DISJOINT file sets, which the `g146` mission states explicitly. `g146`'s gate reading will be noisy while `g164` edits the boards it measures, so run those two serially if you want a clean number. **`g153-vendor-mapping` holds the one dashboards slot now that `g152` has merged**, and G-151 then G-149 queue behind it. **The wave is LIVE as of 22:12Z**: live claims from `g164-design-repairs` and `g163-opaque-platform-routes`, worktrees for `g146-design-declarations` and `g165-noaa-parser`. An earlier reading of this page recorded no trace at all for these five; that was a measurement taken minutes before the lanes started and OPS-17 `A-166` carries the correction. **`g164` is writing `doc_repo` `_design/` in the MAIN checkout, not a worktree, so commit by explicit pathspec in `P:/doc_repo` while it runs, never `git add -A`** |
| ~~G-154's live proof: wait for the G-135 key rather than placing your own pilot key in `P:\tmp\g154-hauska-key.txt`~~ **DONE 2026-09-18T14:49Z, your key never read.** The G-154 lane minted its OWN credential (`key_id 2510a3ff`) under A-118's precedent rather than using yours, so the pilot key stayed untouched | nothing; G-154 is regraded |
| ~~**Housekeeping the G-154 proof leaves:** delete `P:\tmp\g154-hauska-key.txt` and revoke `key_id 2510a3ff`~~ **BOTH DONE 2026-09-18, verified at source.** `P:\tmp\g154-hauska-key.txt` is ABSENT and no other `g154` key material remains in `P:\tmp`. `2510a3ff` already read `revoked` when the planner read the census, so that lane filed its own key. | nothing; the plaintext credential is gone |
| ~~**Decide the fate of two non-pilot `bastrop_tx` keys the mint's census found:** `acf2cf9f` and `2510a3ff`~~ **DECIDED AND EXECUTED 2026-09-18.** Operator: *"if we dont need these delete them dormant G-134 lane key"*. The planner revoked `acf2cf9f` with its own instrument (`_inbox/2026-09-18_planner_revoke_dormant_g134_key.mjs`, artifact `_inbox/2026-09-18_planner_revoke_dormant_g134_key.json`), which reads the admin key at point of use and never prints it, and which refuses unless the target is the exact expected `key_id`, is absent from a must-not-touch list (the pilot, the minted key, and the G-154 key), and is genuinely dormant. `acf2cf9f` last ran 2026-09-15T00:49:49Z, three days idle, so the dormancy condition held. `2510a3ff` was already revoked. **The `bastrop_tx` census is now the minted verification key `96e40316` plus the operator's own pilot, and nothing else.** Both keys were safe to remove because the G-135 verification key supersedes them for any future G-134 work, which is why nothing in flight breaks | nothing; the credential census is true rather than approximately true |
| **Two secrets named `*_MCP_URL` in `hauska-prod-497015` hold Postgres DSNs with passwords, and a lane printed them to a terminal** while looking for an HTTP URL by name. The values reached no artifact, commit or message. **OPERATOR RULING 2026-09-18: do NOT rotate the secrets right now.** The second half stays open and is the cheaper fix: rename or split the secrets so the next lane following a `*_MCP_URL` name does not print a production database credential. No lane should rotate a production store credential on its own initiative | the DSN shape stays as it is by ruling; the naming trap is still armed for the next lane |
| Tell Bastrop staff to use `app.smartcityos.io` | the D-12 bake, and everything after it in M1 and M4 |
| WorkOS org, client id, API key, MFA | G-134's live test, then all of RBAC |
| Ratify G-143, People and access | G-127, G-144, G-158 |
| Amend the 2026-09-14 ruling, which says both "read-only for the city manager" and "admin only" | nothing now; the code follows the first |
| Name an owner and a turnaround for offboarding | the first staff departure after go-live |
| Send the Azavar reply, and file Khalid's answers verbatim | G-137, and lifting the scope card's Q1 and Q2 freezes |
| Ask Bastrop's finance owner which MyGov fee report window is authoritative | permit revenue reading true; G-162 leaves it out on purpose |
| A capture of the v1 combined dashboard | G-157 |
| Whether to republish the stale design links (filings, reasoner) | the gallery Bastrop reviews |
| The blending ruling | the applicant precheck |

**Operator instruction, 2026-09-18.** *"go on Ship, staff word, WorkOS creds, ratify G-143 whenever it
makes sense to do so"* and *"M2 - hold on this - I have the answers out to Khalid"*. Read together with
the ship ruling: the ship is not a separate operator step, since A-149 already made it the
`d14-d13-v1-reach` lane's last action; the staff word and the WorkOS credentials are the operator's own
and move at the operator's timing; and **G-143 is the highest-leverage item on this table**, because it
is the single ratification that unblocks three rows (G-127, G-144, G-158) and it is a design that is
already DRAFT with its instrument built. The M2 items are HELD by the operator until Khalid replies, so
G-137 stays blocked on the operator and nothing was dispatched against it.

Two further operator rulings the same day, both closing items that were waiting on a triage call rather
than on new information: **G-148's hold is released** and its lane is compiled
(`_decisions/2026-09-18_g148_hold_released.md`), and **G-135's parcel 2 is folded into the next lens
build lane** rather than given a credential lane of its own.

## Promised to Bastrop, against what exists

The reconciliation below was written to keep three columns apart: what is designed, what is built, and
what has been described to the customer. As of 2026-09-18:

| Described to the customer | What exists |
|---|---|
| Role-based access and MFA; Finance restricted to Finance | Staff sign-in is built and waits on WorkOS credentials. **Department access is not built and not enforced**, and MFA is switched on by hand in WorkOS, with nothing checking that it was (G-143 findings) |
| "Drop any link, Google Drive or OneDrive, into one database" | **No server path exists** (G-141). Smart Files takes bytes in hand only |
| Hotel occupancy tax integration, priced separately | The design is buildable. The integration waits on Azavar's credentials |
| Departmental parallel plan review | The design is approved. The build is blocked by the deferred role gate (G-144) |
| v2 replaces the v1 dashboard | Five lenses read live data. Finance has its v1 routes built and waits on D-14 and D-13. The combined city management board is not built |
| Four-inch flood question | Built into the flood engine (G-125), but the depth basis is silently wrong: the NOAA Atlas 14 parser never matches and every county falls back to Bastrop's 9.5in. The v2 flood lens (G-149) is queued behind G-152 and G-151 in the dashboards serialization, its design carries one of G-164's 11 findings, and its lane must fix the parser (`A-155`, `A-159`) |

---

# History: the 2026-09-15 reconciliation

*Kept as written on 2026-09-15. Its design counts, gaps and sequencing are superseded by the roadmap
above; its record of what the Jaime call changed still holds.*

## The reconciliation

Three columns that are usually conflated: what is **designed**, what is **built**, and what has
been **described to the customer**. They do not match, and the places they do not match are the
whole point of this document.

## 1. Design state, verified against the shipped nav

| Group | Designed | Not designed |
|---|---|---|
| Lenses | 3 of 9: Overview, Development services, Finance | Citizen, Public works, Parks, Police, Fire and EMS, Fleet |
| Development services tabs | 7 of 7 | none |
| Work | 2 of 3: Plan review, Files | Records search |
| City | 0 of 3 | Assets, Connections, People and access |
| Cross-cutting | Map dock | Compass, shell chrome, applicant-facing plan review |

48 screens exist as images in `_design/exports/`, in both themes. 37 are cleared to show a city.

## 2. What the Jaime call changed

**v1 is being retired, not run alongside.** Stated to the customer: *"the version two dashboard.
I'm retiring the one you're working off of."* That makes every v1 surface a migration obligation
rather than a nice-to-have, and it raises the bar on the six undesigned lenses: today they exist
in v1 and they would disappear.

**Role-based access and MFA are now a customer commitment**, not an internal plan. Jaime asked
directly whether Finance can be restricted to Finance, and the answer given was yes. That is
G-132 then G-127, and neither is built. **This is the largest gap between what has been said and
what exists.**

**Seats are a commercial line, not a technical one.** Base contract was roughly one seat (city
manager) plus five integrations. Actual deployment is eleven to thirteen integrations. Finance
likely needs a seat; directors possibly more. This belongs in the 70-band, not here, but it is
recorded because the access design and the seat model are the same conversation.

**Hotel occupancy tax is separately priced**, outside the current subscription. Everything else
discussed sits inside v2 and inside the subscription.

**The flood study scope is confirmed as designed.** Scenario comparison at different rainfall
depths, multi-property screening, and living inside the Development services tab. The design
matches what was described, including Sylvia's four-inch question.

**Plan review as a companion is confirmed**, and the departmental parallel review was described
to the customer: *"fire review will be able to use theirs and zoning and structural."*

## 3. The gaps, in the order they will bite

### a. Smart Files "bring any link" was described as a capability and is chrome

Described to the customer: *"drop any link, if it's a Google Drive, if it's a Microsoft one,
whatever it is, drop it in and make actually one database for everything that AI can reason
over."*

What exists, from `smart-files` `origin/main`: the Bring files page is **fixture data against a
pasted link**, and the page says so on itself — *"This page is chrome against fixture data. It is
not a live Drive sync."*

Nothing improper was said; this is a roadmap description. But it is the item most likely to be
clicked in a demo, and the distance between the description and the build is larger than for
anything else on this list. **Close it or qualify it before it is shown.**

### b. Departmental plan review cannot be built as currently ruled

`_decisions/2026-09-14_staff_identity_and_department_rbac.md` gates plan review to Development
services and city-manager. Parallel review needs Fire and EMS, Public works and Parks to reach
that surface. The amendment was DEFERRED 2026-09-15 with a trigger.

**The call moved that trigger closer.** Departmental review was described to the customer as a
feature of v2. The deferral still has no mechanism — its only trigger is that somebody remembers
before launch, which by the three-question gate is not a control.

### c. Six lenses are undesigned and v1 is being retired

Citizen, Public works, Parks, Police, Fire and EMS, Fleet. Jaime raised engineering and fire as
likely flood-study users, and Sylvia may add directors. If v1 goes away and those lenses are not
designed, the cutover removes capability.

### d. Two live defects surfaced on the call, neither carded

- **The camera / API connection is broken.** Owner: us. Not in any plan row read here.
- **GIS zoning gaps.** A property's zoning district was changed from commercial and the map does
  not reflect it, plus other gaps. Needs a check with the city's GIS, then a data fix.

### e. Finance lens is designed; its filings tab is not cleared

The Finance lens is drafted and exportable. The hotel occupancy tax tab
(`smartcity-finance-filings`) is under an **AMEND** ruling: it prints an ordinance rate attributed
to Bastrop's own code that traces to no source, and scores three of their filings against it.
**Since HOT is the prioritised, separately-priced integration, this is now on the critical path
rather than parked.**

Its integration is also still blocked on four credentials and four questions to Azavar.

## 4. What this means for sequencing

**Superseded 2026-09-15 by operator ruling, recorded as OPS-17 `A-137`.** Finishing the design
work is the first card, `G-146`, ahead of everything below. Bastrop approves the design before we
build, so every build row here sits behind it. The list below stands as the order *within* the
build work once design lands, and item 4 and item 5 are themselves design items that `G-146`
now sequences.

The reason the ruling changed the order is a number nobody had: counted against the shipped nav,
**5 of 15 surfaces are designed** and 2 more are excluded by ruling, leaving 8 uncovered. Eleven
folders and 48 boards read as much more than that.

**REGRADED 2026-09-18 (`A-152`).** Those counts were the 2026-09-15 snapshot and this file was still
quoting them three paragraphs below its own line reading `uncovered: 0`, so it contradicted itself.
Re-measured against nav `7487d7c0`: **13 of 15 surfaces designed**, 2 excluded by ruling, **0
uncovered**, across **20 design folders and 72 boards** (26 subdirs less 6 non-design dirs). What
remains at the front of the queue is not design coverage but the four instruments `G-148` writes. See
OPS-17 `A-152` for the measurement, the instrument and the ruling that `G-146` does not close on
`G-148` alone.

The customer conversation has reordered things. The honest order now:

1. **Hotel occupancy tax.** Prioritised by the customer and separately billable. Blocked on the
   Azavar credentials and on the AMEND fix to the filings design.
2. **Role-based access and MFA.** Committed verbally, gates the seat conversation, and everything
   departmental depends on it. G-132 then G-127.
3. **The two live defects.** Cameras and GIS zoning. Small, visible, and they are the customer's
   current experience of the product.
4. **People and access design.** Same surface as the WorkOS work, so design and build land
   together.
5. **Citizen lens**, then the remaining five lenses, because v1 is being retired.
6. **Smart Files bring-files**, closed or qualified.

Plan review and flood study are designed and ahead of their dependencies. Neither is the
bottleneck.

## 5. Owed by the operator

| Item | Status |
|---|---|
| Email to Sylvia: v2, go-live date, RBAC changes, HOT integration, pricing, seat costs, copy Jaime | committed on the call, owed same day |
| HOT integration proposal with pricing, then a follow-up meeting with Sylvia | owed after the email |
| Confirm with Sylvia which departments and directors need access, and whether Finance needs a seat | owed |
| WorkOS org, client id, API key, MFA, `bastrop_tx` key | outstanding, blocks item 2 above |
| Azavar reply: four questions, four credentials | outstanding, blocks item 1 above |
| Ratify, amend or kill: Finance lens, Smart Files, Development services lens | outstanding |
| Which of the three plan review designs a city is shown | outstanding |

## 6. What this document does not do

It does not assign plan rows. Nothing here is carded to OPS-17, and the two live defects and the
Smart Files gap in particular have no row. That is the next planner's first job, not a
reconciliation's.

**Done 2026-09-15, same day.** Carded as OPS-17 `G-137` through `G-145` per amendment `A-136`.
Every row was written against a source read rather than transcribed from this document, and two
came back different from how they are described above. The camera connection is not a defect to
debug: the code is built and fails closed, and no Verkada credential exists in any of the three
GCP projects, so it is vendor onboarding (`G-139`). And the GIS zoning gaps are neither a data gap
nor a cache: the product reads a city layer last edited 2023-04-28 while Bastrop publishes three
newer ones, evidence in `_inbox/2026-09-15_bastrop_zoning_layer_findings.md` (`G-140`).
