---
id: 2026-09-11_ops23_wave1_verify_p167
date: 2026-09-11
row: P-167
kind: read-only fact verification (precedes the lane mission)
snapshot:
  hauska-atom-contract: origin/main 8dde29c
  hauska-map: origin/main 6ab6914
  hauska-engine: origin/main 79fa573
  legacy-design-tools: origin/main 3950ce9b
  hauska-mcp-server: origin/main 3b5626b (bonus grep only)
  npm: "@empressaio/atom-contract@1.31.0 (npm view, 2026-09-11)"
method: git fetch, then git show / git grep against origin/main only; no working tree read, nothing built or installed
---

# 1. hauska-atom-contract

`package.json`: name `@empressaio/atom-contract`, version `1.31.0`, equal to npm live `1.31.0`. `exports` carries 18 subpaths plus `./package.json`: `.`, `./testing`, `./encumbrances`, `./workspace`, `./read-contract`, `./conformance`, `./export`, `./temporal`, `./og`, `./reasoning`, `./property`, `./identity`, `./provenance`, `./derivation`, `./absence`, `./lineage`, `./selector`, `./access`. Each maps `types` to `./dist/<name>/index.d.ts` and `import` to `./dist/<name>/index.js`.

Publish: `.github/workflows/publish.yml`, name "Publish to npm", triggers on `push.tags: "v*.*.*"` and `workflow_dispatch` (inputs `ref`, `set_latest`). It does NOT publish on push to main. Lines 41-46 skip when `name@version` is already on the registry, else `npm publish --access public`. `ci.yml` runs lint, typecheck, test on push and PR to main across Node 18/20/22.

Display/vocab/label subpath: NOT FOUND. Searched `src/` for `display`, `vocab`, `label`, `render` at file and line level. `src/render.ts` is render MODE resolution (`FALLBACK_ORDER`, `resolveMode` over `AtomMode`), not display strings. Remaining hits are field names (`displayName`; `displayLabel` at registration.ts:77, inline-reference.ts, export/downloadable-atom.ts:77) and "vocabulary" in comments (og/common.ts:8, temporal/common.ts:4, registry.ts:67). `src/property/buildable-envelope.ts` holds schema and derivation only. None of the item 5 strings appear here.

Tests: vitest (`"test": "vitest run"`), `vitest.config.ts` includes `src/**/*.test.ts`; tests are colocated (`src/render.test.ts`) and under per-subpath `__tests__` dirs (`src/property/__tests__/property.test.ts`). `prepublishOnly` runs clean, lint, typecheck, test, build.

# 2. hauska-map

`apps/property-explorer/src/lib/buildable-display-vocab.ts`, 303 lines, sha256 `45c277d7...29cac`. Exports: types `BuildableDisplayKind` (17), `EnvelopeStatusInput` (28), `WarmEnvelopeKind` (30), `BuildableDisplayInput` (38), `BuildableDisplayVocab` (61); functions `resolveBuildableAreaSqFt` (85), `mapBuildableDisplay` (125), `violatesHistoricalDisagreementGuard` (291).

```
export type BuildableDisplayKind =
  | "absent" | "loading" | "pending" | "provisional"
  | "buildable-with-area" | "declined-consume" | "not_specified"
  | "not-applicable";
```

Branch strings (cardLabel / pdfLabel / agreementToken), by line:
- 135 loading: "Loading buildable area…" / "pending — atom path loading" / `loading`
- 146 declined: null / `unavailable — ${decline}` / `absent:${decline}`
- 160 not-applicable: "Not zoned — no setback requirements" / "not applicable — unincorporated, no zoning ordinance" / `not-applicable`
- 171 not_specified: "build-to-line · buildable % pending" / "pending — build-to-line governs (scalar setback silent)" / `not_specified`
- 182 area or geometry: `N%`, `~N sq ft`, or "buildable envelope on file[ (build-to-line)]", plus " (provisional)" / " (PROVISIONAL)" when provisional; `buildable:<n>` / `provisional:<n>`
- 227, 242 declined-consume: "0% — setbacks consume lot" / `unavailable — ${reason}` / `declined-consume`
- 262: "provisional · buildable % pending" / `provisional:pending`
- 271 pending: "setbacks present · buildable % pending" / "pending — setbacks on file; buildable area not yet derived" / `pending`
- 281 absent: null / "unavailable — no buildable envelope on file" / `absent`

Peer fixture: `apps/property-explorer/src/lib/__fixtures__/buildable-display-vocab.peer.fixture`, 303 lines, byte-identical to the engine source (same sha). Beside it `__fixtures__/buildable-display-vocab.parity.lock.json`:

```
  "peerRepo": "hauska-engine",
  "peerPath": "packages/engine-core/src/site-plan/buildable-display-vocab.ts",
  "localPath": "apps/property-explorer/src/lib/buildable-display-vocab.ts",
  "sha256": "45C277D7A10EF9CAF1AD355644840F524FA2D131A038873F074C90E5E1D29CAC",
```

Importers: one runtime importer, `src/lib/baked-facets.ts:29` (`import { mapBuildableDisplay } from "./buildable-display-vocab";`), plus `buildable-display-vocab.test.ts:11` and the parity test (lines 38-54: LF-normalised byte equality with the fixture, sha256 equal to `lock.sha256`).

Dependency: `@empressaio/atom-contract` NOT FOUND anywhere in hauska-map (all seven `package.json` files, `pnpm-lock.yaml`, content grep). `apps/property-explorer/package.json` has no atom-contract dependency of any name. Root `package.json:15` and `packages/map-renderer/package.json:25` carry the legacy `"@hauska/atom-contract": "^1.5.0"`. For the mission, importing the package is a new dependency, not a bump.

# 3. hauska-engine

Lock path confirmed: `packages/engine-core/src/site-plan/__fixtures__/buildable-display-vocab.parity.lock.json`. Same four keys as the map lock with `"peerRepo": "hauska-map"`, `"peerPath": "apps/property-explorer/src/lib/buildable-display-vocab.ts"`, the same sha256, note "M0 dual-copy handshake. sha256 must match hauska-map lock." Test: `packages/engine-core/src/site-plan/__tests__/buildable-display-vocab.parity.test.ts`, lines 43-59:

```
    expect(local, "engine buildable-display-vocab.ts drifted from peer fixture — sync both repos").toBe(peer);
    ...
    expect(digest).toBe(lock.sha256);
    expect(lock.peerRepo).toBe("hauska-map");
```

Engine copy: `packages/engine-core/src/site-plan/buildable-display-vocab.ts`, the same eight exports at the same lines, re-exported from `site-plan/index.ts:83-90`, consumed by `site-plan/site-model.ts:24`. A THIRD copy: `packages/retrieval/src/serving-sweep/vendor/buildable-display-vocab.ts`, imported by `vendor/baked-facets.ts:28`, guarded by `serving-sweep/__tests__/vendor-drift.test.ts`, which compares against a local `P:/hauska-map` checkout and SKIPS in CI (lines 10-14, 92). All five copies hash to `45c277d7...29cac` at these SHAs.

Adjacent finding, outside P-167: `vendor/atom-chain-to-facets.ts` is 847 lines against map's 2038 and `vendor/baked-facets.ts` 524 against 1155; the drift control skips without a map checkout, so it never fired.

Dependency: `packages/engine-core` (line 136), `packages/atoms`, `packages/atom-contract-pin` at `^1.22.0`; `og-sources`, `og-title`, `retrieval`, `storage`, `workspace`, `services/retrieval-api` at `^1.20.0`; root NOT FOUND.

# 4. legacy-design-tools

`artifacts/smartsite-mcp/src/vocabulary.ts` (450 lines). Header lines 1-2: "P-91 v3 V1/V2. The single vocabulary table for the machine tokens this server puts on the wire". Imports nine display constants from `./mcp-app.js`, one-way by design (lines 18-21). Exports: `VocabularyEntry` `{token, displayText, meaning}` (35); `WIRE_DISPOSITION_DISPLAY_TEXT` (60), six keys, `present: "Present"`, `refused: "Refused"`, `absent: "Reported absent"`, `unread: "Not read"`, `unknown` and `absent-verified` read from `STATE_WORDS`; `DERIVED_FIGURES_POLICY` (78) denying `area, coverage_ratio, lot_coverage_pct, setback_distance, buildable_area`; `VOCABULARY` (119), 34 entries by grep count while the line-109 comment still says "19 entries" (stale after the Q1, P-106, A-072 additions). Tokens: present, absent, absent-verified, unknown, refused, unread, citationsDegraded, gis-approximate, seed, side_corner, atom_path_pending, upgrade_required, parcel_not_found, baked_snapshot_not_found, parcel_batch_cap, open_did_not_reach_me, depth_not_implemented, declined-in-bake, not-in-bake, radius_invalid, radius_exceeds_max, radius_unbounded, bare_street_unbounded, bare_street_not_a_street, constraint_bound_missing, constraint_county_out_of_scope, constraint_single_address, constraint_filters_missing, constraint_rail_unknown, constraint_op_unsupported, constraint_cap_invalid, constraint_rail_unmeasured, constraint_projection_missing, out_of_coverage. Also `VOCABULARY_RESOURCE_URI = "docs://smartsite/vocabulary-p91v3.json"`, `buildVocabularyResourceText`, `registerVocabularyResource`, `STANDING_VOCAB_BLOCK_TEXT`, `STANDING_VOCAB_CONTENT_PART`.

"p563 table": REFUTED as a code label, CONFIRMED as a doc_repo deploy label. No `p563` string exists in LDT origin/main. LDT PR #563 is P-87 (AI client connection records) and touched none of these files. `git log --oneline -3 origin/main -- vocabulary.ts`: `9b8c0b83` (#616, A-072), `89445899` (#580, P-106), `be98c30c` (P-91 v3 Q1). In doc_repo, `p563` is the Cloud Run image tag of the P-91 v3 deploy (`_inbox/2026-08-31_p91_p563_deploy.md`: tag p563, revision `smartsite-mcp-00078-fat`, LDT PR #562). The mission should name it "the P-91 v3 `VOCABULARY` table (deployed as p563)".

`tool-honesty.ts` (1046 lines) imports `DERIVED_FIGURES_POLICY, VOCABULARY, WIRE_DISPOSITION_DISPLAY_TEXT, VocabularyEntry` (3-8) and `envelopeHuman` (2). It enforces: `facetGuidance` 404-405 reads the disposition word from the table; `honestOverlay` 442 attaches `reasonDisplayText: envelopeHuman(reason) ?? reason` and nulls `finding` on unknown; 512 attaches `derivedFigures: DERIVED_FIGURES_POLICY`; 542 and 562 attach `dispositionDisplayText`; `placeSearchRefusalDisplayText` 929-940 throws when a code has no row (same shape at 1004-1010). `envelopeHuman` (mcp-app.ts:448-451) maps exactly one token; every other overlay `reason` ships raw as `reasonDisplayText` (not verified whether those are already human-readable).

Dependencies: `artifacts/smartsite-mcp/package.json` NOT FOUND (no atom-contract of any name; deps are `@workspace/db`, MCP SDK, express, jose, pg, zod); `artifacts/api-server` and `lib/{engine-core,knowledge-atoms,portal-ui,submission-classifier}` `^1.30.0`; root NOT FOUND. The MCP surface owning the table does not consume the package.

# 5. Other copies of the display strings

"Withheld, setbacks unruled": LDT only. `artifacts/smartsite-mcp/src/mcp-app.ts:449` (TS `envelopeHuman`) and a second copy at `mcp-app.ts:3881` inside the served panel script: `if(reason===["atom","path","pending"].join("_")) return "Withheld, setbacks unruled";`. Tests: `mcp-app-served.test.ts:820`, `mcp-app.test.ts:479,491,1160`, `tool-honesty.test.ts:862`, `tools.test.ts:878,885`, `vocabulary.test.ts:141`.

"buildable % pending": the five vocab copies (lines 14, 175, 266, 274, 301 in each), `buildable-display-vocab.test.ts:124,162` in map and engine, and map `api/_lib/setback-not-specified.ts:209` ("Buildable % pending honest geometry.", capitalised, in `buildToLineDisclosure`).

"Not stamped here": map `src/browse/InspectCard.tsx:495` (`reason: label ?? "Not stamped here"`), `InspectCard.consumer-voice.test.tsx:383`, comment at `src/lib/baked-facets.ts:814`.

"setbacks present": map `api/_lib/atom-chain-to-facets.ts:1958` and engine `vendor/atom-chain-to-facets.ts:762` ("Atom-chain envelope (setbacks present; geometry absent on proof atom — not fabricated)."), map `baked-facets.test.ts:225,268`, `live-envelope-augment.test.ts:20`, plus the vocab copies.

Adjacent setback-string sources, no parity lock between them: map `api/_lib/setback-not-specified.ts` (`formatSetbackDisplay`, `buildToLineDisclosure`) and engine `site-plan/setback-display.ts` (`formatSetbackSummaryLine`, `formatSetbackEdgeLabel`), both carrying "build-to-line governs" strings.

hauska-atom-contract: none of the four strings. hauska-mcp-server (bonus): none, no vocab module, depends on `@empressaio/atom-contract ^1.30.0`.

# Fleet memory

- GROUND-TRUTH 2026-09-11: hauska-map and smartsite-mcp have no `@empressaio/atom-contract` dependency; map carries legacy `@hauska/atom-contract ^1.5.0` only.
- GROUND-TRUTH 2026-09-11: five byte-identical `buildable-display-vocab.ts` copies (sha `45c277d7...`), two parity locks, one vendor-drift test that skips in CI.
- LESSON: "p563" is a Cloud Run image tag, not a code label; the table is `VOCABULARY`, 34 entries, comment says 19.
- OPEN: engine `serving-sweep/vendor/{atom-chain-to-facets,baked-facets}.ts` drifted under a control that skips in CI.
- DEAD-END: `git show origin/main:.github/...` under MSYS mangles the colon path; set `MSYS_NO_PATHCONV=1`.

leave_behind: none
