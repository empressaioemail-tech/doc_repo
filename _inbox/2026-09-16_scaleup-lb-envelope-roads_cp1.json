{
  "lane": "scaleup-lb-envelope-roads",
  "checkpoint": "CP1-design",
  "filedAt": "2026-09-16T13:40:00Z",
  "laneClaim": {"status": "claimed", "seat": "dispatch-planner-lb", "planRow": "P-249", "exitCode": 0},
  "reposLocated": {
    "note": "P: and C:\\Users\\cente carry duplicate-named clones at different sync states (dual-clone topology, confirmed generically, not just for doc_repo). The p216-envelope-served seat worktree at /p/seat-worktrees/p216-envelope-served/{legacy-design-tools,hauska-map,hauska-engine} was the freshest lane-scoped checkout found (branch heads 2026-09-14/15), and origin/main was fetched fresh inside each to read the true merged-trunk state rather than a stale lane branch.",
    "legacy-design-tools": {"path": "/p/seat-worktrees/p216-envelope-served/legacy-design-tools", "mainHead": "b5dfb508 2026-09-15 08:09:53 P-214"},
    "hauska-map": {"path": "/p/seat-worktrees/p216-envelope-served/hauska-map", "mainHead": "fabf9b7b 2026-09-15 08:09:57 P-216 #404"},
    "hauska-engine": {"path": "/p/seat-worktrees/p216-envelope-served/hauska-engine", "note": "not yet fetched at CP1 time; site-plan trace delegated to sub-agent"}
  },
  "coreMechanismReadAlready": {
    "LDT reconcileAtomEnvelope.ts": "read in full from origin/main. isMachineVerifyDiagnostic(reason) gate (added by P-214, merged 2026-09-15) ALREADY exempts diagnostic-shaped reasons from clobbering a good live envelope (line ~158: `if (isDiagnostic && !derived.empty) return derived;`), but the else branch (non-diagnostic reason: unzoned/not-onboarded/reason-less/copied-snapshot) has NO such guard and unconditionally sets emptyKind='consumed', clobbering any live-derived polygon. Mission premise CONFIRMED on current main, narrower than the dispatch's flat description because P-214 already carved out the machine-diagnostic slice.",
    "hauska-map atom-chain-to-facets.ts": "read relevant branches (~line 2007-2100). This is a SEPARATE, independently-written reconciliation for the facets/get_smart_site path, gated on isDepthWarmPromoted(chain) (env.depthWarmPromotion === 'depth-warm-promoted-v1' or sourceCitation includes 'depth-warm-verified') rather than string-sniffing the reason. When not depth-warm-promoted, it declines outright (status:'declined', declineReason:'envelope-unverified') WITHOUT ever consulting a live LDT derive. A code comment at this exact branch (2026-09-15, P-216) explicitly names 48209:97658 as the confirmed live repro: a nine-edge ring with five side-labelled edges collapsing to a false zero -- this is an independently-documented second mechanism for the San Marcos symptom that appears to survive as real (not yet independently re-derived by me at CP1 time).",
    "implication": "TWO separate implementations of 'do not let an unverified atom override a good local read', in two repos, keyed on two DIFFERENT signals (string-shape of `reason` vs a structural depthWarmPromotion flag). hauska-map's signal is the principled one and is already computed by the SAME retrieval-api endpoint LDT calls (`GET /property-nodes/:id/atom-chain`) -- confirmed by reading hauska-map's local `PropertyAtomChain` type (declares `buildableEnvelope.depthWarmPromotion`) against LDT's `PropertyAtomChainWire` type (`fetchPropertyAtomChain.ts`), which narrows the SAME wire response down to `{kind, areaSqFt, reason}` and simply never reads/types depthWarmPromotion. The minimal fix is a wire-type + call-site extension in LDT plus a predicate change, not a redesign -- but it must land in BOTH reconcileAtomEnvelope.ts (live-derive path) AND atom-chain-to-facets.ts's declined/envelope-unverified branch (facets path) to actually unlock San Marcos on both surfaces, since they are two independent gates."
  },
  "preRegisteredFalsifierAnswers": {
    "F1_classTotalsReproduce": "EXPECT partial reproduction from factory store alone -- the scope's 6-class breakdown (unzoned/not-onboarded/zero-no-reason/no-layer-23/road-edge-failures/inset-failures/superseded) is an ATOMS-STORE reason classification and cannot be reproduced from the factory store's parcel_record_cell alone. Will attempt full reproduction only after the L-A atoms-store gate clears; if it does not clear in time, this falsifier will read UNMEASURED for the atoms-side classes with the factory-store side (setbacks-on-record counts) reported as the measured half.",
    "F2_notVacuous_sanMarcos_pflugerville": "PRE-REGISTERED PASS EXPECTED: 48209:97658 (San Marcos) is expected to land in 'setbacks on record, no envelope drawn' (setbackFrontFt rail = value; live derive/facets = declined or empty) and 48453:427599 (Pflugerville) is expected to land outside that bucket (buildable atom, envelope draws). Will verify both through the live MCP surface and, once available, the facets/derive endpoints directly, not by assertion.",
    "F3_notVacuous_forUnlock": "PRE-REGISTERED: the P-249 count (parcels that would draw after generalizing the depth-warm-promoted gate) MUST include 48209:97658 and MUST exclude a genuinely depth-warm-verified no-buildable-area lot. Candidate negative control not yet identified -- will search the atoms store (once gated open) for a `no-buildable-area` atom with depthWarmPromotion='depth-warm-promoted-v1' AND a small/narrow ring, or fall back to a hand-reasoned tiny lot if none is found, and will say explicitly which."
  },
  "planForRemainingWork": [
    "Two background sub-agents (fan depth 1, each prompt carries the do-not-spawn-further clause as its first line): (A) labeller/registry import graph (Q4) + re-derive requirements (Q5); (B) footprint rendering trace in hauska-engine site-plan/* (Q2 footprint) + pull exact ruling text from _decisions/ for citation accuracy.",
    "Main thread: factory-store per-city setback-on-record query (announcing heavy scan per AGENT_CONTRACT section 4 -- parcel_record_cell is 76,055,442 live rows, query is index-bounded on rail_key, expected duration 1-3 min), live Smart Site MCP calls for San Marcos/Pflugerville wording (Q6), bounded poll for the L-A atoms-reads-done marker (max 45 min per operational notes), then atoms-store draw-gap join + envelope-family atom counts + road-node counts if/when cleared.",
    "Adversarial review of both sub-agents' deliverables against source before inclusion in the report, per contract section 1."
  ],
  "heavyScanAnnouncement": {
    "database": "factory store (FACTORY_DATABASE_URL_RO, neondb)",
    "target": "parcel_record_cell (76,055,442 live rows), filtered scans on rail_key in ('setbackFrontFt','situsCity') joined by place_key, index-bounded via parcel_record_cell_rail_idx",
    "expectedDuration": "1-3 minutes per query, run sequentially not concurrently"
  }
}
