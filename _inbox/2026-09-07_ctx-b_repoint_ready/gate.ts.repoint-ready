/**
 * Setback extraction acceptance gate.
 *
 * 2026-09-07 (CTX-B dispatch, item 5): retired as a vendored copy. The
 * logic now lives in `@empressaio/setback-corpus`'s own `./gate` export
 * (ported there from this exact file, plus a new G7 rule for the
 * max_height_ft sentinel discipline this dispatch also introduced) — this
 * file is now a thin re-export so the package's own `./setbacks/gate`
 * export path, and every existing consumer/test importing from it, keeps
 * working unchanged. Do not add new logic here; add it to the corpus
 * package so hauska-engine and legacy-design-tools share one gate again.
 */
export * from "@empressaio/setback-corpus/gate";
