{
  "lane": "p244a-siteplan-compose-timeout",
  "planRows": ["P-244"],
  "status": "closed-partial",
  "repo": "hauska-engine",
  "snapshot": {
    "docRepo": { "worktree": "P:/seat-worktrees/p244a-siteplan-compose-timeout/doc_repo", "branch": "lane/p244a-siteplan-compose-timeout", "baseCommit": "03fbc52d" },
    "hauskaEngine": { "worktree": "P:/seat-worktrees/p244a-siteplan-compose-timeout/hauska-engine", "branch": "fix/p244a-siteplan-compose-timeout", "baseCommit": "2d85fee", "commit": "0bf58e5", "pr": "https://github.com/empressaioemail-tech/hauska-engine/pull/455", "merged": false, "deployed": false }
  },
  "probe": {
    "artifact": "_inbox/2026-09-16_111638_surface_probe.json",
    "note": "This artifact is a general environmental probe (no --rows filter) run from this lane's doc_repo worktree; it carries no site-plan-export leg (P-244 is in OPS-24's PREDICATE_DEBT range 200-250 per scripts/enforcement/probe-close-gate.mjs -- no scripts/surface-probe.mjs row exists for this predicate yet, same acknowledged gap P-227 already flagged). P-244a's row is NOT a read-only review (a real code fix was made and a PR opened), so probe.notApplicable was judged the wrong declaration even though the mechanical gate would accept it for a debt-range row; this artifact is cited instead to satisfy 'a close must cite an artifact that measured its rows' honestly. The actual authoritative-record verification for THIS row's predicate (site_plan_export_jobs reaching ready) was done directly against the engine's own DATABASE_URL and Cloud Run request log, per the dispatch's own explicit method -- see ruling falsifiers 1 and 5."
  },
  "falsifier": "All six falsifiers pre-registered in the dispatch were run for real and answered in the ruling doc: (1) throw site named with file+function; (2) composed-vs-standalone explained structurally, not by timing; (3) P-231 ruled out explicitly; (4) not fixed by raising the timeout -- root cause and real fix named instead; (5) success on the job row NOT achieved, said so plainly with the reason; (6) throw site is inside hauska-engine, root cause is Cloud Run config outside the application code.",
  "contradicted": "This dispatch's own known-traps section ('Do not assume Cloud Run CPU starvation... it was hypothesised by the planner and REFUTED'). That refutation's logic (elapsed wall-clock time implies CPU was allocated throughout) does not hold, and new evidence not available to it -- this exact parcel's prior successful synchronous run on identical tiny data, and the ~11-minute gap between the P-240 async-port deploy and the first async-path failure -- reopens it. Also contradicted: the dispatch's own leading hypothesis that P-231's descriptor defect was 'a live candidate cause for this timeout' -- ruled out explicitly, with mechanism.",
  "leave_behind": [
    "Named, not applied (crosses this dispatch's own deploy stop-point): set run.googleapis.com/cpu-throttling: \"false\" on hauska-engine-api (gcloud run services update hauska-engine-api --no-cpu-throttling --region=us-central1 --project=hauska-prod-497015), so detached background jobs (this route's P-240 pattern, and P-155's feasibility-export pattern it was ported from) get real CPU. Unverified whether this fully resolves the failure or only reduces its frequency -- a follow-up run of the standalone export on 48021:34049 after the change lands, verified against the job table per falsifier 5, is the actual completion predicate for this row.",
    "Named, not built: no scripts/surface-probe.mjs leg exists for the standalone site-plan-export / export_instrument kind=siteplan customer surface. P-227 already flagged this same gap; still open.",
    "P-231 (the standalone caller's missing address/countyName descriptor) remains a separate, real, unfixed defect -- ruled out as this timeout's cause, not resolved. Still needs its own lane in whichever repo builds the /site-plan-export/refresh request body, per P-231's own row text.",
    "hauska-engine's classifySitePlanExportJobError now distinguishes dxf_emission_failed from ifc_emission_failed and generic compose_timeout (this lane's PR #455) -- any future dashboard or alert keyed on the old 'compose_timeout' string for this route should be checked against the new class name."
  ],
  "missionPremise": "P-244 leg A: the standalone site-plan export fails on its own at ~116s with error_class compose_timeout, and P-231's descriptor gap was named as a live candidate cause needing to be ruled in or out first.",
  "completionPredicate": "A standalone site-plan export reaches ready on 48021:34049 and the artifact downloads through the connector. NOT MET -- root cause identified and evidenced, but its fix (Cloud Run CPU-throttling config) crosses this dispatch's own 'do not deploy' stop-point, so the customer-facing failure is unchanged. What WAS delivered: an accurate, evidenced diagnosis (file+function, live logs, live DB reads, live service config) replacing a misleading error classification, plus the classification fix itself, in a green, unmerged PR.",
  "scopeBasis": "Scoped to hauska-engine per the dispatch's repo assignment. The actual fix (Cloud Run service annotation) is infrastructure, not hauska-engine application code, and there is no IaC file for this service checked into the repo (confirmed by search) -- it is a live gcloud operation, which is exactly the class of action this dispatch's own stop-point reserves for the operator/integration seat. Did not chase P-231 into whichever repo builds the standalone request body (out of this lane's repo scope, consistent with 'you own your fan' not extending across repos without a new dispatch)."
}
