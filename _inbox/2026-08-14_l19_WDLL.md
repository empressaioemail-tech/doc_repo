---
id: 2026-08-14_l19_WDLL
title: WDLL — L19 DC-11b/c/d closeout (load test, capacity doc, domain)
status: approved
date: 2026-08-14
operator_approval: 2026-08-14 (L19 dispatch PLAN-ROW P-29)
related: [76j_smartsite_launch_readiness_program, 2026-08-05_76j_workstream_C_rate_limit_and_pooling_WDLL, 2026-08-05_launch_capacity_audit]
owner: nick
---

# WDLL: L19 DC-11b/c/d closeout

Date: 2026-08-14
Status: approved
Operator approval: 2026-08-14 (compiled dispatch `_dispatches/2026-08-14_l19_dispatch.md`, PLAN-ROW P-29)

Parent WDLL: `_inbox/2026-08-05_76j_workstream_C_rate_limit_and_pooling_WDLL.md` (C1/C2 store+pooling). This card is the C4 residue named in the L13 close (`DC-11b` load test, `DC-11c` capacity-doc freshness) plus the P-30-adjacent domain probe `DC-11d` that the dispatch folded in.

## Done looks like

A dated load-test artifact exists against the serving MCP surface (revision family 00063+). Bounded RPS ramps (1/5/20) report p50/p95/p99 and status histograms. Postgres rate limiter engagement is proved under concurrency: HTTP 429 at the anonymous free-IP rpm threshold, health samples throughout show `rate_limit_store.primary=postgres` and `memory_fallback=false` (no silent memory fallback). Cortex-api and retrieval-api are not load-fanned except incidental MCP health dependency probes. The 76j Workstream C capacity doc is refreshed with those measurements and a `last_updated` bump. `curl -sI https://smartsite.cloud` is recorded verbatim (fixing DNS is out of scope).

## Acceptance items

1. **Live MCP pin** | Serving revision family 00063+ confirmed at source (`gcloud` traffic + `GET /health`). `dependencies.rate_limit_store.state=ok`, `primary=postgres`, `memory_fallback=false`. | check: verbatim health JSON + revision name | grade: [ ]
2. **Bounded load test artifact** | Dated JSON at `_inbox/2026-08-14_76j_C4_loadtest_results.json` (76j C4 path family; pointer from the capacity doc). Tiers 1/5/20 rps, fixed duration, p50/p95/p99, status counts. Target is MCP only (`/health` latency + `POST /mcp` initialize limiter). No PE facets, no cortex parcel routes, no MCP tool calls that fan to retrieval. | check: artifact file exists; scenarios named; no pe_/cortex_ load scenarios | grade: [ ]
3. **Limiter engages at threshold** | Anonymous free-IP budget (`HAUSKA_FREE_IP_RPM` default 60, 60-second fixed window) produces HTTP 429 with `data.band=rpm` once the window fills. First 429 in a fresh window lands near request 61 (pre-registered band 50-70). Zero HTTP 503. | check: status histogram + first 429 body verbatim | grade: [ ]
4. **No silent memory fallback** | Every `/health` sample during the run (pre, per-tier, post) has `primary=postgres` and `memory_fallback=false`. | check: health_samples[] in the artifact | grade: [ ]
5. **Capacity doc refresh** | `_inbox/2026-08-05_launch_capacity_audit.md` (the 76j Workstream C capacity doc) carries the 2026-08-14 measurements and `last_updated: 2026-08-14`. Measured-facts sibling updated with a pointer. Doc_repo edits left uncommitted. | check: frontmatter last_updated + new measurements section | grade: [ ]
6. **DC-11d domain probe** | `curl -sI https://smartsite.cloud` recorded verbatim. Pass = HTTP 200 or 301-to-Vercel. Otherwise report what IS. No DNS fix in this lane. | check: verbatim headers in close | grade: [ ]

## Dependencies

- Item 2 depends on 1 (wrong host/revision is a failed test, not a measurement).
- Item 3 depends on 2 (limiter proof is a named scenario inside the same harness).
- Item 4 is sampled throughout 2+3.
- Item 5 depends on 2+3+4.
- Item 6 is independent.

## Constraints

- L16 holds the atoms bulk-writer slot. This lane does not take the slot, does not `--apply`, does not heavy-scan.
- Do not fan load into cortex-api or retrieval-api beyond incidental MCP `/health` dependency probes (15s dep cache).
- Exit-bounded: fixed durations, no watch/tail.
- Isolated worktree: doc_repo planner tree only. No hauska-mcp-server code edit this lane unless a live defect forces a pin (none expected).

## Amendments

(none at start)

## Finish card (graded at close)

1. **met:** Serving `hauska-mcp-server-00063-fic` @100% tag `l13-57fa819`. `/health` `rate_limit_store.state=ok primary=postgres memory_fallback=false`. gcloud + live GET 2026-08-14T13:54Z.
2. **met:** `_inbox/2026-08-14_76j_C4_loadtest_results.json` ts `2026-08-14T13:57:53.397Z`. Tiers 1/5/20 rps on `/health` and `POST /mcp` initialize. No pe_/cortex_ load scenarios.
3. **partial:** 429 `data.band=rpm` `tier=free_anonymous` verbatim; 20 rps after cap 301/301 429; zero 503. first_429 cumulative **72** outside pre-registered [50,70] because `minuteBucket=floor(now/60000)` is a wall-clock UTC minute (CP2 F1). Limiter engaged; threshold alignment missed.
4. **met:** 36/36 `/health` samples `primary=postgres` `memory_fallback=false` `state=ok`. Offenders 0.
5. **met:** `_inbox/2026-08-05_launch_capacity_audit.md` `last_updated: 2026-08-14` plus L19 measurements. Measured-facts sibling pointed at the dated artifact. Uncommitted.
6. **partial:** `curl -sI https://smartsite.cloud` → `HTTP/1.1 200 OK` Content-Length 114. Body is a JS lander to `/lander`. A records 15.197.148.33 and 3.33.130.190. No Vercel headers. DNS fix out of scope (P-30).
