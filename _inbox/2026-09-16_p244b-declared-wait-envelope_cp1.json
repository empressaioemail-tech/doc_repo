{
  "lane": "p244b-declared-wait-envelope",
  "planRows": ["P-244"],
  "checkpoint": "CP1",
  "filedAt": "2026-09-16T11:00:00.000Z",
  "seat": "cente-vsc",
  "designSummary": "Traced the live connector call (mcp__claude_ai_Smart_Site__export_instrument kind=siteplan, same parcel 48021:34049 named in the dispatch) before editing anything, per the dispatch's own instruction. The dispatch's own quoted example -- {\"raw\": \"<declared-wait prose>\", \"status\": \"error\", \"reason\": \"upstream_error\", \"upstreamStatus\": \"unmeasured\"} -- has the fingerprint of a downstream wrapper that JSON.parse()s an MCP tool's error content and, on failure to parse, falls back to a generic {raw, status:error, reason:upstream_error, upstreamStatus:unmeasured} shape. hauska-mcp-server's own errorContent() helper (src/tools.ts:209) returns isError:true with PLAIN PROSE text, never structured JSON -- so any isError:true response from this repo, healthy or not, collapses to that same generic bucket downstream.",
  "smartSiteConnectorFinding": "Searched for legacy-design-tools/artifacts/smartsite-mcp (the path the dispatch asked to check) -- it does not exist. `find artifacts -maxdepth 3 -iname *smart*` in P:/legacy-design-tools returns only artifacts/api-server/README-brokerage-mcp-service.md; grepped that whole repo for the literal tool name 'export_instrument' (the actual Smart Site connector tool I have live access to) and got zero matches anywhere in legacy-design-tools, and zero matches in hauska-mcp-server itself. The consolidated 'export_instrument kind=siteplan|terrain|dossier|feasibility' tool surface is implemented in NEITHER repo I have access to -- confirmed live by calling mcp__claude_ai_Smart_Site__export_instrument(kind=dossier) successfully (real ready-state PDF returned) and observing the envelope is `{\"data\": {...}}`, matching hauska-mcp-server's own ToolEnvelope shape verbatim on the SUCCESS path. This means on success the Smart Site layer is a thin passthrough of hauska-mcp-server's own JSON -- consistent with the scope doc's Invariant I1 (`P:/2026-08-28_smartsite_mcp_app_v1_scope.md`: 'the app may never call anything the conversation could not... auth flows through the host's existing OAuth session'). I could not locate the source that performs the ERROR-side wrapping (the {raw,status,reason,upstreamStatus} shape) in any repo on this machine. Per falsifier 5 and the Do-not clause: I am NOT claiming it lives in legacy-design-tools (it demonstrably does not), and I have nothing to edit there. This is reported as an open item, not silently dropped.",
  "rootCauseInThisRepo": "Independent of the unlocated downstream wrapper, hauska-mcp-server's OWN contract already has the fix precedent: check_parcel_site_plan_export_status (the P-240 poll tool) treats state:queued/running/failed all as ORDINARY STRUCTURED DATA (envelopeContent, isError never set) -- verified by its own existing tests (tests/site-plan-export-catalog.test.ts:444-490). But download_parcel_site_plan_export's 404 export_in_progress catch branch (src/tools.ts, was ~line 1365) and download_parcel_flood_drainage_export's 404 flood_drainage_in_progress catch branch (was ~line 2705) both call errorContent() -- isError:true -- for the IDENTICAL queued/running state the poll tool already treats as healthy data. That inconsistency inside this repo's own contract is the defect P-244 leg B is scoped to, and it exists whether or not any downstream wrapper is later fixed.",
  "planRows_tools_enumerated": [
    "refresh_parcel_site_plan_export -- already correct (envelopeContent, buildEnvelope; queued/running never isError)",
    "check_parcel_site_plan_export_status (P-240 poll tool) -- already correct, confirmed by existing passing tests",
    "download_parcel_site_plan_export -- BUG: 404 export_in_progress -> errorContent (isError:true). 422 site_plan_export_failed already names its real errorClass in prose -- leaving as isError:true, that is a genuine failure.",
    "refresh_parcel_flood_drainage_export -- already correct (still_processing branch uses envelopeContent)",
    "download_parcel_flood_drainage_export -- BUG: 404 flood_drainage_in_progress -> errorContent (isError:true), same shape as the site-plan bug. 422 flood_drainage_refresh_failed already names errorClass in prose, leaving as isError:true.",
    "dossier, terrain, feasibility exports -- grepped tools.ts for _in_progress/still_processing/JobFailedError: no matches outside the two flagged tools + their already-correct siblings. Confirmed these three kinds do not have this async-job pattern (dossier verified live as synchronous; feasibility calls engine-api directly per its own tool description)."
  ],
  "plannedFix": "Change both buggy 404 branches from errorContent(prose) to the same buildEnvelope/finalizeReadEnvelope/envelopeContent pattern already used by the sibling refresh tools for the identical state, carrying {parcelNodeId, state, jobRef, pollAfterMs, message}. Leave the 422 (genuine failure) branches as isError:true -- the dispatch's own three-state framing calls a genuinely failed job 'an error that names the real mechanism', and both branches already interpolate the real errorClass from the engine body rather than a hardcoded default.",
  "openRisks": [
    "My own Claude.ai session's Smart Site subscription tier is 'solo' (confirmed live: export_instrument kind=siteplan on 48021:34049 returned upgrade_required, subscriptionTier:solo) -- site-plan and flood-drainage export require Studio/Team. I cannot reproduce the queued/running/failed states through my own live connector call; falsifier 1 will be satisfied via the existing/updated test suite (which replays the exact body shapes quoted in the dispatch and in this repo's own pre-existing tests, e.g. compose_timeout) plus a live 'ready' state I already reproduced (kind=dossier, same parcel) proving the connector passthrough behavior, not via a live queued/running/failed call on my own account.",
    "Do not touch hauska-engine (leg A) or legacy-design-tools (P-243's repo this session) -- confirmed no edits needed in either."
  ]
}
