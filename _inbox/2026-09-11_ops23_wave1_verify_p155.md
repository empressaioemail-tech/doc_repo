---
id: 2026-09-11_ops23_wave1_verify_p155
date: 2026-09-11
row: P-155
type: read-only verification
snapshot:
  hauska-map: origin/main 6ab6914
  legacy-design-tools: origin/main 3950ce9b
  hauska-engine: origin/main 79fa573
method: git fetch + git show/grep origin/main only; no working tree read, no build, no install
---

# P-155 fact check: feasibility refresh timeouts and the absence of an async primitive

## 1. hauska-map BFF handler (6ab6914)

`apps/property-explorer/api/_lib/pe-feasibility-export-handler.ts`

Timeout constant, line 61: `const FEASIBILITY_ENGINE_TIMEOUT_MS = 55_000`. Comment at 56-59: "The Vercel function cap is 60 s; budget just under it so an overrun classifies as the honest transient engine_timeout".

Engine calls, both direct to engine-api (no MCP hop, per header comment lines 17-21):

- Line 150, POST `${engineApiBaseUrl()}/v1/property-nodes/${parcelNodeId}/feasibility-export/refresh`, `signal: AbortSignal.timeout(FEASIBILITY_ENGINE_TIMEOUT_MS)` (line 160).
- Line 213, GET `.../feasibility-export/download`, `signal: AbortSignal.timeout(30_000)` (line 220).

Timeout return path: catch at 189-192 calls `engineFailure` (105-125), which calls `classifyEngineFailure` (`_lib/pe-site-plan-export-core.ts:286`; line 301 regex `/timed out|timeout\b|aborted|aborterror/` returns `'engine_timeout'`), then `retryableFeasibilityEngineFailureResponse` (`_lib/pe-feasibility-export-core.ts:356-375`):

```
  if (kind === 'engine_timeout') {
    return {
      status: 503,
      body: {
        error: 'engine_timeout',
        message: FEASIBILITY_ENGINE_TIMEOUT_RETRY_MESSAGE,
        retryable: true,
        detail,
```

Cold-start strings, `pe-feasibility-export-core.ts:342-346`:

```
export const FEASIBILITY_ENGINE_TIMEOUT_RETRY_MESSAGE =
  'Feasibility Study engine timed out — this usually means a cold start. Try the report again in a moment.'

export const FEASIBILITY_ENGINE_UNREACHABLE_RETRY_MESSAGE =
  'Feasibility Study engine did not respond — it may be restarting. Try the report again in a moment.'
```

`maxDuration`: `apps/property-explorer/vercel.json:8`: `"api/pe-site-plan-export.ts": { "maxDuration": 60 }`. The feasibility handler is folded into that function (`api/pe-site-plan-export.ts:685-686`: `if (kind === 'feasibility') { await handleFeasibilityExportRequest(req, res)`). No exported `config` in the handler; grep `maxDuration` across `apps/property-explorer/**` hits only vercel.json, one test fixture, and two comments.

Browser caller: `apps/property-explorer/src/workbench/tools/feasibility-export.ts:65-105`, `requestFeasibilityExport`, single call at line 70: `await fetchImpl("/api/pe-site-plan-export?kind=feasibility", { method: "POST", ...})`. No loop, no interval, no retry. Consumer is `src/workbench/tools/ReportsTool.tsx` (import line 71; call line 1367 `const result = await requestFeasibilityExport(body);` inside `run`, followed by `setBusy(false)`). Awaits once. On the 503 the notice falls through to `feasibility-export.ts:137`: `return result.message ?? "Feasibility Study export failed. Try again."`, so the customer sees the BFF cold-start sentence.

## 2. legacy-design-tools smartsite-mcp (3950ce9b)

`artifacts/smartsite-mcp/src/feasibility-export.ts`

Line 16: `const REFRESH_TIMEOUT_MS = 55_000;` Line 17: `const DOWNLOAD_TIMEOUT_MS = 30_000;`

Engine calls: line 92 `parcelPath = /v1/property-nodes/${parcelNodeId}/feasibility-export`; lines 102-108 POST `${config.baseUrl}${parcelPath}/refresh` with `body: "{}"`; lines 164-168 GET `${parcelPath}/download`. Timeout via AbortController + setTimeout (`timeoutSignal`, lines 76-80).

Cold-start string: NOT FOUND. Searched this file for `cold|warm|try again|timed out|timeout`; only hits are the `timeoutSignal` doc comment (lines 70-75) and the constants.

Timeout report shape: abort throws in the fetch, catch at line 108 returns `engineThrewResult(err)` (lines 61-67):

```
function engineThrewResult(err: unknown): ToolResult {
  const message = err instanceof Error ? err.message : String(err);
  return {
    content: [{ type: "text", text: message }],
    isError: true,
  };
}
```

So `export_instrument` returns the raw abort error message as bare text with `isError: true`, with no `status`/`tool`/`kind`/`reason` envelope, unlike the 422 (lines 115-137) and not-configured (lines 35-52) paths, which carry `tool: "export_instrument", kind: "feasibility"`. Dispatch: `src/tools.ts:1044-1052` `case "export_instrument"` then `if (kind === "feasibility")` calls `executeFeasibilityExport` (import line 37); tool registered at `src/constants.ts:69`.

## 3. hauska-engine routes (79fa573)

Route file: `services/engine-api/src/routes/parcel-terrain.ts`, inside `buildParcelTerrainRoutes` (line 313). Mounted at `services/engine-api/src/server.ts:183` `v1.route("/property-nodes", buildParcelTerrainRoutes());` and `server.ts:186` `app.route("/v1", v1);`.

- Refresh: line 601 `app.post("/:parcelNodeId/feasibility-export/refresh", ...)`; success `c.json({...}, 201)` at line 696; any throw returns 422 `feasibility_export_failed` (lines 697-701).
- Record read: line 704 `app.get("/:parcelNodeId/feasibility-export", ...)` returns the atom and `artifacts["pdf-feasibility"]`.
- Download: line 710 `app.get("/:parcelNodeId/feasibility-export/download", ...)`; 404 `artifact_unavailable` when `!artifact || artifact.deferred` (714-719); 410 `artifact_evicted` when `artifactStore.get(ref)` is empty (721-726).

PDF generation is synchronous inside the request handler. Chain:

1. `parcel-terrain.ts:611` `await authorParcelFeasibilityExport({...})`
2. `packages/engine-core/src/site-plan/feasibility-author.ts:139` `await composeParcelReport(...)` (drainage study, fact resolvers, narrative at 171/202)
3. `feasibility-author.ts:222` `await emitPdfFeasibility(model, ...)` (from `./pdf/feasibility.js`, import line 12)
4. `feasibility-author.ts:234` `await options.artifactStore.put({ format: "pdf-feasibility", bytes: pdf.bytes, ...})`
5. back in the route, 201 with the atom and artifact record.

Nothing is detached; the 201 is emitted only after bytes are stored.

Async job primitive: NOT FOUND. Searched `services/**/*.ts`, `packages/**/*.ts`, `*.mjs`, all `package.json` (tests excluded) for: `status(202)` / `, 202)` / `202 Accepted` (0 hits); `Cloud Tasks|tasks.googleapis|cloudtasks` (0); `pubsub|@google-cloud/pubsub` (0); `report_runs` (0). `jobId` hits are the Municode TOC client params (`packages/corpus/src/adapters/municode/json-client.ts:14-16`) and the `P85StoreRef` pointer type naming `records_request_jobs` (`packages/engine-core/src/parcel-record/companion-shapes.ts:41-46`); no SQL in this repo defines that table (`records_request_jobs` in `**/*.sql`: 0). `queue` hits are the corpus HTTP throttle promise chain (`packages/corpus/src/adapters/http.ts:26-92`) and ingest-script backpressure, not a job queue.

Cloud Run service: image `us-central1-docker.pkg.dev/hauska-prod-497015/cloud-run-source-deploy/hauska-engine-api:latest` built from `services/engine-api/Dockerfile` (`cloudbuild.engine-api.yaml:3,11`). Request timeout: NOT FOUND anywhere in the repo. `cloudbuild.engine-api.yaml:15` `timeout: 3600s` is the build timeout. `services/engine-api/Dockerfile:82-85` sets `ENV HYDROLOGY_WORKER_TIMEOUT_MS=120000` with the comment "120s stays inside the ldt spine call timeout (180s) and Cloud Run request timeout", naming the Cloud Run timeout without a value. No `gcloud run deploy` for engine-api exists in `*.md|*.sh|*.mjs|*.yml|*.yaml` (only `hauska-retrieval-api` in `services/retrieval-api/DEPLOY.md:83,114,135`); `.github/workflows/ci.yml` has no engine-api deploy; `services/engine-api/docs/` holds only `gate-front-seam.md` and `map-layers-contract.md`. The live request timeout must be read from the Cloud Run service JSON, not from the repo.

Artifact store note: `parcel-terrain.ts:246-255` `artifactStoreFromEnv` picks GCS when `TERRAIN_ARTIFACT_BUCKET` is set, else local disk, else `MemoryArtifactStore` (line 213). The 410 message "no longer on this instance" (line 725) is only true for the memory store.

## 4. Report-run persistence

`reportRun|report_run|instrument_run|instrumentRun`: NOT FOUND in hauska-engine `services/**`, `packages/**`, `migrations/**`, `*.sql` (tests excluded). Migration set enumerated: `packages/storage/migrations/005-012`, `packages/engine-core/scripts/migrations/0072-0076`, `packages/document-ingest/migrations/004_document_ingest.sql`, `packages/engine-core/src/parcel-record/schema.sql`; `CREATE TABLE` matching `run|job|artifact|export|feasib`: 0 hits.

What does persist: the `parcel-terrain-model` atom via `storage` (StoragePort), whose `artifacts["pdf-feasibility"]` record carries `ref`, `deferred`, `deferredReason` (route lines 714-717), readable at GET `/:parcelNodeId/feasibility-export` (line 704). That is a presence-or-deferred read written after `artifactStore.put` (feasibility-author.ts:231-234). It has no in-progress state, so a poller cannot distinguish "running" from "never requested"; it is not a run-status row.

## Fleet memory

GROUND-TRUTH (2026-09-11, SHAs above): all three clients budget 55 s on refresh and 30 s on download; the engine emits 201 only after synchronous compose + PDF + store; no 202/job/queue/Cloud Tasks/PubSub primitive exists in hauska-engine; the Cloud Run request timeout for engine-api is not declared in the repo.

LESSON: the LDT MCP timeout surfaces as bare abort text without the `export_instrument` envelope, so an agent caller cannot classify it as retryable the way PE's BFF (`retryable: true`) can.

OPEN: the live engine-api Cloud Run request timeout (read from service JSON by field) is the one number this fact check could not source from any repo.
