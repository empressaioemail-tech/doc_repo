---
id: 2026-08-24_p75_CP1
title: P-75 CP1 — who-serves 0076 pin and existing reader
status: pass-to-build
date: 2026-08-24
plan_row: P-75
wdll: _inbox/2026-08-24_lane3_p75_who_serves_WDLL.md
decision: _decisions/2026-08-24_who_serves_promotion.md
tree: P:/tmp/ldt-lane3-wave1
branch: fix/lane3-wave1-p75-p76
commit: 244567a50ae62334984b3f990d776872e1c206ea
---

# P-75 CP1

Snapshot: isolated tree `P:\tmp\ldt-lane3-wave1`, branch `fix/lane3-wave1-p75-p76`, HEAD `244567a50ae62334984b3f990d776872e1c206ea` (origin/main @ 244567a5). Plan row P-75. Schema 0076 is present on this tree. Build proceeds. Live serving-table describe is UNMEASURED.

## 1. 0076 on this tree

Present. File: `lib/db/drizzle/0076_tx_utility_territory_staging.sql`.

Creates `"tx_utility_territory_staging"` with PK `staging_row_id`, `service_kind` CHECK (`water` | `sewer` | `electric` | `water-district`), `geometry_crs` CHECK `EPSG:4326`, Texas bbox CHECK, and source / county / bbox indexes.

L22 close listed the uncommitted acquisition file as `0075_tx_utility_territory_staging.sql`. On this tree `0075` is `cad_property_vintage_crosswalk`. The territory table is `0076`. The acquisition script still names the 0075 path. That is a leftover, not a miss of 0076.

Live / serving table describe: UNMEASURED. This seat has no live Neon describe from this isolated tree. L22 close counts (including 3925) are not re-measured here and are not restated as current.

## 2. Drizzle schema module

NONE. `lib/db/src/schema/` has `txCityBoundary`, `txCountyBoundary`, `txFemaNfhlFloodZone`. No `txUtilityTerritoryStaging`. `schema/index.ts` does not export the table. Build will add the typed module matching 0076. No production migrate on this card.

## 3. Existing reader / PIP against this table

NONE. No `whoServes` reader, no inspect attach, no serve-time PIP over `tx_utility_territory_staging`.

## 4. How special-district containment is done (reuse, do not copy TCEQ into who-serves water)

Serve path for special districts is an atom read: `artifacts/api-server/src/lib/specialDistrictFactRead.ts` over `special-district-fact`. It does not live-PIP `tx_special_district`.

Geometry math already in-tree: `lib/cad-ingest/src/txgio/geo.ts` (bbox prefilter + even-odd `pointInGeometry`). City / county containment (`lib/cad-ingest/src/boundary/containment.ts`) and NFHL evaluation reuse those helpers. Who-serves reuses the same geo helpers plus the 0076 bbox columns. It does not edit `containment.ts` (P-76).

TCEQ additive staging rows (`source_key=tceq-water-districts`, staged `service_kind=water-district`) stay complementary who-governs. They are not remapped to water CCN. Who-governs water districts remain the special-district atom family.

## 5. Live row counts

UNMEASURED.
