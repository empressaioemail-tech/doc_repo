---
date: 2026-08-20
seat: property substrate
artifact: T-25 enumeration continuation, rows 31 to 46, plus W-9 status change
continues: 30 rows verified by the prior property reader (handover NOT FILED, see F-0)
status: verified partial
---

# T-25 property rows 31 to 46

## Snapshots

Every row below was read at these commits via `git show <sha>:<path>`, never from a working
tree. The working trees do NOT match the handoff and were not used.

| repo | read at | working tree actually at | matches handoff |
| --- | --- | --- | --- |
| doc_repo | cc96276 | cc96276, main | yes |
| hauska-engine | d3f3794 | 8d8e880, detached HEAD | NO, read from object db |
| legacy-design-tools | 1113c649 (origin/main) | 10069854, feat/s1-instrument-hardening | NO, read from object db |
| hauska-map | 204789f | d3510a6, fix/p35-vercel-token-preflight | NO, not read this pass |

`origin/main` in all three product repos equals the handoff SHA, verified by
`git log --oneline -1 origin/main`.

Live store snapshot for W-9, rows 45 and 46: cortex-prod, database `hauska_mcp`, user
`neondb_owner`, `2026-08-20T12:37:26Z`. Supplied by the operator through the existing Neon
channel. No new credential was created. Recording explicitly that `atoms` lives on
`hauska_mcp` and not on `neondb`, because a reader assuming the cortex-api database would
query the wrong database and find nothing, which would read as a stronger absence than the
real one.

## Pre-registration, filed before reading, and how each resolved

1. **"No shared module" is an ownership claim and I expected to confirm it by name matching.**
   RESOLVED, claim HOLDS. Import blocks of all six `geo.ts` read at source: four import
   nothing at all, `utility-easement` imports `polygon-clipping` plus a contract type,
   `building-footprint` imports a local type. No barrel, no re-export, no shared helper.
   Extended: the divergence is worse than the sibling table records, because the CALL SITES
   diverge too. See row 32.

2. **I expected to file a confidence default as a row when it is an absence.**
   FIRED, and it caught a fabricated row before it was written. The dispatch predicted
   confidence defaults in the writer family. **They do not exist.** `building-footprint` is
   the only one of the six carrying a confidence field, and it OMITS rather than defaults:
   `...(observation.confidence !== undefined ? { confidence: observation.confidence } : {})`
   at `building-footprint-writer.ts:135-137`. Had the prediction been filed without reading,
   this pass would have entered a defect that is not there. Recorded as a negative result,
   not a row.

3. **I expected to answer S-21 from a description of the bundle rather than the bundle.**
   AVOIDED. The bundle itself was fetched into a scratch bare repo and walked as git objects,
   with a positive control. See the S-21 section.

## F-0. Outranks the enumeration: the handover document does not exist

The dispatch names a handover document as the source of truth and instructs that it be read
in full including a section headed "what this test cannot see". **No such document is
reachable.** Searched: all of doc_repo by content, and hauska-map, hauska-engine and
legacy-design-tools by content. The only filed T-25 property rows in the estate are rows 1
and 2, inside `_inbox/2026-08-19_ss-w16_close.json` under key `t25Rows`. Rows 3 through 30
exist only as the condensed summary carried in the dispatch text itself.

The shape is the same as S-21. A producer's output survived only as a summary inside a
downstream artifact and was never committed. The enumeration's own handover has now
reproduced the defect the enumeration was written to catch. Stated, not acted on.

Consequence for this pass, stated rather than worked around: rows 31 onward continue the
numbering on the dispatch's assertion that the prior reader stopped at 30. That assertion is
unverifiable here. If the lost rows are recovered and overlap, renumbering is a merge
problem, not a correctness one.

## F-1. Outranks the enumeration: a carried gate is already satisfied

The handover and durable memory both carry: "special-district writer's verify is VACUOUS,
verifies the in-memory atom, never a store readback, verifyFailures can never be non-zero."
The paused order's Step 3 gate depends on it.

**Superseded at hauska-engine `9fb41c7`, PR #306, 2026-08-11**, subject
`fix(special-district): read stored bytes back by atom_did PK after write`. The diff is
literally `- verifyStoredSpecialDistrictFactAtom(atom, {` to
`+ verifyStoredSpecialDistrictFactAtom(back, {`.

Second mechanism considered and rejected: that the memory was simply wrong. Rejected by
reading the history of the file rather than only its current state. The measurement was true
of the source it measured and has since been fixed. This is the third instance of that
pattern in this operation's memory.

**All six writers do a real store readback at d3f3794.** Each issues
`SELECT body FROM atoms WHERE atom_did IN (...)`, builds `storedByDid`, and passes the DB
body as `back`. The vacuity claim is retired across the whole family, not just
special-district.

## W-9 STATUS CHANGE: negative-by-text-search becomes confirmed-by-query

W-9 was filed as the weakest evidence in the enumeration: a negative claim about database
constraints reached by text search over schema files. It has now been run against the live
store and **the text search answer was correct**. Both constraint queries returned no rows.

The outcome that most justifies the refusal to let it stand: the answer would have been
right either way and the reader would not have known which. The confirmation does not change
the finding, it changes its evidence class.

Two things exist that do not shrink the finding, and both belong in the row.

**`atoms.entity_id` is `text NOT NULL`.** That refuses nulls and nothing else. Every non null
string satisfies it, including a raw source key, a concatenated separator, a placeholder, or
a caught exception's default.

**A unique index `atoms_entity_composite_unique` exists on the pair (entity type, entity id).**
It is a `CREATE UNIQUE INDEX` rather than a table constraint, which is why it correctly
returned nothing from both constraint queries. Recording it deliberately: had the absence
been reported without it, the answer would have been right and incomplete, and a later reader
finding the index could reasonably conclude the finding was overstated.

**Why neither constitutes binding validation.** `NOT NULL` asks whether a value is present.
The unique index asks whether a pair is unused. Neither asks whether the value resolves to a
node. They are presence-shaped and uniqueness-shaped predicates over a property that is
referential. A fabricated identifier that has never been used before satisfies both.

**The full size finding, both layers, now evidenced rather than inferred.**

| layer | mechanism | status |
| --- | --- | --- |
| application | a presence test that RETURNS NULL rather than raising, so malformed candidates are dropped with no error and no record | confirmed, read from source (W-6 / W-7) |
| application | an insert with no predicate on the binding | confirmed, read from source |
| application | a derived identifier that inherits whatever the binding was | confirmed, read from source |
| database | no primary key, no unique constraint, no foreign key and no check on `entity_id`, in either table | confirmed, queried against the live store 2026-08-20T12:37:26Z |

The store takes any non null string the writer sends, provided the pair has not been used,
and nothing at any layer asks whether the binding resolves to a node.

## The rows

Column meanings unchanged. Derivation states: 1 available now (two independent sources), 2
available once a named dependency lands, 3 internal consistency only, 4 none exists,
subdivided foreclosed / absent / self authored.

Rows 31 to 44 are READ from source, not executed. Rows 45 and 46 are READ from source for the
write path and VERIFIED by live query for the database layer. No writer was run.

### Row 31
**location** `packages/engine-core/src/flood-hazard-fact/flood-hazard-fact-atoms.ts:103-136`, `verifyStoredFloodHazardFactAtom`
**predicate** schema parse, then `parcelNodeId` equality, then `outcome` equality. Nothing else.
**cheapest satisfier** any atom that parses `FLOOD_HAZARD_FACT_SCHEMA`, carries the expected `parcelNodeId`, and lacks all three absence markers. `floodZone`, `inSpecialFloodHazardArea`, `zoneSubtype`, `baseFloodElevation` and `accessPolicy` are never compared to anything.
**semantically valid** NO. This is the write-then-verify for the rail that carries the hazard determination and it cannot see the hazard. It is the weakest of the six: `well-fact` and `rail-corridor-fact` both check `accessPolicy`, `building-footprint` refuses a present atom with no geometry, and flood checks neither a policy nor a value.
**derivation state** 3, internal consistency only. Both sides originate in the same writer run.
**note** W-5 manufactures a wrong `inSpecialFloodHazardArea` and this verify increments `verified` on it. The two compose: one produces the wrong value, the other certifies the write.

### Row 32
**location** call sites `packages/engine-core/scripts/write-flood-hazard-fact-county.mjs:184-187` and `:754-757`, against the module derivation at `flood-hazard-fact-atoms.ts:127-130`
**predicate** the call site computes `expected.outcome` as `atom.absence || atom.sourceTier === "absent"`. The module computes `storedOutcome` as `atom.absence || atom.sourceTier === "absent" || atom.verifiedAbsence`. **The two derivations of the same property differ.**
**cheapest satisfier** an atom with `verifiedAbsence` truthy, no `absence`, and `sourceTier !== "absent"` makes the two disagree and the verify fails. Any atom without that combination satisfies both.
**semantically valid** NO as a design. This is a divergence, not a check. It fails closed when it fires, which is the right direction, but it fires on a state the writer is not known to produce, so it is untested load-bearing logic.
**derivation state** 3, internal consistency only.
**the non-uniformity, per module, which is what the reconciliation scopes against**

| module | call site `expected.outcome` | module `storedOutcome` | agree |
| --- | --- | --- | --- |
| flood-hazard-fact | `absence \|\| sourceTier==="absent"` | `absence \|\| sourceTier==="absent" \|\| verifiedAbsence` | NO |
| special-district-fact | `absence` only | `absence \|\| sourceTier==="absent" \|\| verifiedAbsence` | NO, widest gap |
| well-fact | `absence` only | `absence` only | yes |
| rail-corridor-fact | `absence \|\| sourceTier==="absent"` | `absence \|\| sourceTier==="absent" \|\| verifiedAbsence` | NO |
| utility-easement | three valued, incl. `county-coverage` | three valued, same shape | yes |
| building-footprint | `absence \|\| sourceTier==="absent" \|\| verifiedAbsence` | same | yes |

Three of six diverge between the call site and the module it calls. `well-fact` agrees only
because both halves are equally narrow. `building-footprint` is the only module where a
complete derivation appears on both sides.

### Row 33
**location** `special-district-fact-atoms.ts:140-148`
**predicate** `expected.outcome === "present" && expected.districtId && atom.districtId !== expected.districtId`
**cheapest satisfier** OMISSION. If the caller passes no `districtId`, or an empty string, the check is skipped in silence. The call site at `write-special-district-fact-county.mjs:178` passes `atom.districtId`, so an atom built without one disables the check that would have caught it.
**semantically valid** NO. The guard is satisfied by the same absence it exists to detect.
**derivation state** 3, internal consistency only.

### Row 34
**location** `utility-easement-atoms.ts:158-163`
**predicate** `expected.easementId && atom.easementId !== expected.easementId && expected.outcome !== "county-coverage"`
**cheapest satisfier** OMISSION, as row 33, plus a second exemption: `outcome === "county-coverage"` disables the identity check outright.
**semantically valid** NO, and worse than row 33. `utility-easement-atoms.ts:40` writes the literal sentinel `easementId: "county-coverage"` for exactly that outcome. The sentinel is written, and the check that would see it is switched off for precisely the case that carries it.
**derivation state** 4, none exists, self authored. The writer authors both the sentinel and the exemption.

### Row 35
**location** `packages/atoms/src/building-footprint-writer.ts:155-159` and `:143`
**predicate** NONE. `const footprintId = observation.footprintId ?? "primary";`
**cheapest satisfier** OMISSION. An unresolved `footprintId` becomes the string `"primary"`.
**semantically valid** NO, and this is the most serious row of the set. The defaulted value is passed into `buildingFootprintAtomDid({parcelNodeId, footprintId})` at `:156-159` and into `entityIdOf(parcelNodeId, footprintId)` at `:143`. **The sentinel becomes the atom identity.** Two unresolved footprints on one parcel produce the same DID and collide on write. This is the doctrine's named prohibition on writing an identifier that was not explicitly resolved.
**derivation state** 4, none exists, self authored. `verifyStoredBuildingFootprintAtom:144` compares `atom.footprintId` to `expected.footprintId`, and the call site supplies `expected` from `atom.footprintId`. Both sides carry `"primary"` and agree.
**relationship to W-9** this is the application-layer half of W-9 made concrete in one module: a derived identifier inheriting an unresolved binding, with no database constraint underneath it to catch the collision as anything other than a silent `ON CONFLICT` no-op.

### Row 36
**location** `packages/atoms/src/building-footprint-writer.ts:160`
**predicate** NONE. `const sourceTier = observation.sourceTier ?? "ml-derived";`
**cheapest satisfier** OMISSION.
**semantically valid** NO. An unknown provenance tier becomes an asserted provenance tier, and `sourceTier` is an input to the content hash and to the verify's outcome derivation in four of six modules.
**derivation state** 4, none exists, self authored.

### Row 37
**location** all six `packages/atoms/src/*-writer.ts`, three sites each, eighteen sites total. `flood-hazard-fact-writer.ts:89,133,171` and the same shape in the other five.
**predicate** NONE. `const observedAt = provenance.observedAt ?? new Date().toISOString();`
**cheapest satisfier** OMISSION. Absent observation time becomes write time.
**semantically valid** NO. `observedAt` means when the source was observed. Defaulting it to now silently converts an absence into a fabricated present, and it is the field every staleness and vintage computation downstream reads. It is also written to `extractedAt` and `evaluatedAt` (`building-footprint-writer.ts:124,133`), so one fabricated value populates three provenance fields.
**derivation state** 4, none exists, absent. There is no second party who could attest when we observed a source.
**call site enumeration before scoring this remediable** The engine-core layer types its run provenance as `Omit<PropertyFactWriteProvenance, "contentHash" | "observedAt"> & { observedAt: string }`, identically in all six `*-fact-atoms.ts`. That makes `observedAt` REQUIRED for every caller routed through engine-core, so the default cannot fire on the county writer path. It remains open at the `@hauska-engine/atoms` package boundary, which is a published contract with consumers outside this repo. The default is defended at one layer and open at the other, and removing it would change nothing on the path that is currently exercised. Both halves are stated because reporting either alone is misleading.

### Row 38
**location** all six `packages/atoms/src/*-writer.ts`, three sites each. `flood-hazard-fact-writer.ts:117,155,192` and siblings.
**predicate** NONE. `verificationStatus: provenance.verificationStatus ?? "machine"`
**cheapest satisfier** OMISSION.
**semantically valid** NO. "Nobody stated a verification status" and "a machine verified this" become the same stored row. This is the absent, zero, unmeasured collapse applied to verification state.
**derivation state** 4, none exists, self authored.
**divergence, and the one place the family gets it more right** `building-footprint-writer.ts:129-131` alone is conditional: `observation.verificationStatus ?? (observation.sourceTier === "ml-derived" ? "unsurveyed" : "machine")`. It derives a weaker and more honest claim for ML data instead of asserting `machine`. Five of six do not.

### Row 39
**location** `packages/atoms/src/rail-corridor-fact-writer.ts:106` and `:157`
**predicate** NONE. `observation.bufferMeters ?? RAIL_CORRIDOR_DEFAULT_BUFFER_METERS`
**cheapest satisfier** OMISSION.
**semantically valid** NO. `bufferMeters` is the distance that decides whether a parcel is near a rail corridor. It is the measurement's unit of meaning, and it defaults silently. `rail-corridor-fact-atoms.ts:90` separately passes `bufferMeters: 0` into the county coverage content hash, so two different unstated buffers exist in one module.
**derivation state** 4, none exists, self authored.

### Row 40
**location** `flood-hazard-fact-atoms.ts:44-49` and `:62-67`, `floodHazardFactClaimContentHash`
**predicate** NONE. The content hash is computed over `parcelNodeId`, `sourceTier`, `inSpecialFloodHazardArea`, `floodZone` only.
**cheapest satisfier** any two atoms differing only in `zoneSubtype` or `baseFloodElevation` produce an identical content hash.
**semantically valid** NO where the hash is used for change detection or dedup. A corrected base flood elevation is invisible to any consumer keyed on the hash.
**derivation state** 4, none exists, self authored. `sourceTier: "fema-nfhl"` is a hardcoded literal at both sites regardless of the provenance actually attached, so the hash asserts its own source rather than deriving it.

### Row 41
**location** `packages/atoms/src/*-writer.ts` content hash part lists, six modules. `flood-hazard-fact-writer.ts:77-80`, `well-fact-writer.ts:93-98`, `rail-corridor-fact-writer.ts:89-95`, and siblings.
**predicate** NONE. Every claim field is coalesced as `parts.X ?? null` before hashing.
**cheapest satisfier** `undefined`. Absent and explicitly null hash identically.
**semantically valid** NO. The three states absent, null and unmeasured collapse to one hash input, so the hash cannot distinguish a field nobody looked up from a field looked up and found empty.
**derivation state** 4, none exists, absent.

### Row 42
**location** `flood-hazard-fact/geo.ts:198-200`, inside `findZoneAtPoint`
**predicate** `const sfha = candidates.find((c) => isSfhaFlag(c.sfhaTf)); return sfha ?? candidates[0]!;`
**cheapest satisfier** any candidate array. When `isSfhaFlag` returns false for every candidate, `find` yields undefined and the function returns `candidates[0]`, the first zone in array order that contains the point.
**semantically valid** NO, and this is a consequence of W-5 the handover did not name. W-5 is filed as a boolean that fails open. It is also a **zone selection** that fails open. The doc comment at `:171-174` claims "Prefers SFHA zones when multiple intersect (stricter finding wins)". Under a county encoding change that preference is silently lost and an arbitrary overlapping zone wins by iteration order, so the reported `floodZone` string is wrong too, not only the boolean.
**derivation state** 1, available now. The zone polygons carry their own zone codes; a zone code in the SFHA set with `isSfhaFlag` false is a detectable contradiction between two fields the source did not coordinate.
**filed as its own row** because it is a distinct predicate at a distinct line with a distinct failure, and W-5 as written scopes only the boolean.

### Row 43
**location** `flood-hazard-fact-atoms.ts:43`, `special-district-fact-atoms.ts:40`
**predicate** truthiness spread. `...(entry.sourceVintage ? { sourceVintage: entry.sourceVintage } : {})` and `...(entry.taxRate ? { taxRate: entry.taxRate } : {})`
**cheapest satisfier** for dropping the field: empty string, or the number `0`.
**semantically valid** NO for `taxRate`. A genuine special district tax rate of `0` is falsy and is silently omitted, so a district that levies nothing is stored identically to a district whose rate was never looked up. Absent and zero collapse, and the collapse is in the direction that makes a real zero unrepresentable.
**derivation state** 4, none exists, absent.

### Row 44
**location** all six writers, `write-*-county.mjs`, the `if (!back)` branch. `write-flood-hazard-fact-county.mjs:179-185` and siblings.
**predicate** `const back = storedByDid.get(atom.atomDid); if (!back) { push failure }`
**cheapest satisfier** any stored row whose `body.atomDid` matches. The map is keyed on `s.body?.atomDid`, a value inside the JSONB body, while the SQL selects on the `atom_did` column.
**semantically valid** PARTIALLY, and it is the strongest check in the family. It is a genuine round trip and it catches truncation, JSONB coercion and wrong-column writes. It cannot catch a wrong value, because the writer authored both the row and the expectation.
**derivation state** 3, internal consistency only.
**why it is still worth having** it is the one place in the six where a real second read of durable state occurs. Every other check in rows 31 to 43 compares the writer to itself without touching the store.

### Row 45, NEW TABLE
**location** `packages/document-ingest/migrations/004_document_ingest.sql:32-46`, table `document_ingest_atoms`
**predicate** `entity_id text NOT NULL`. That is the whole of it.
**cheapest satisfier** any non null string. The empty string satisfies it.
**semantically valid** NO, and **this table is strictly weaker than `atoms`**. `atom_did` is the primary key at `:33`. The only index is `idx_document_ingest_atoms_source_document_cid` at `:46`, on the source document cid. There is **no unique index on (entity type, entity id)**, so this table does not even carry the pair-uniqueness that `atoms_entity_composite_unique` gives the primary path. No foreign key, no check, no constraint of any kind on `entity_id`.
**derivation state** 4, none exists, absent.
**snapshot** DDL read at hauska-engine d3f3794; database layer confirmed by live query at 2026-08-20T12:37:26Z on `hauska_mcp`.

### Row 46, NEW TABLE, write path
**location** `packages/document-ingest/src/pg-atom-store.ts:73-105`, `PgDocumentAtomStore.writeDocumentAtom`
**predicate** NONE on the binding. `instance.entityId` flows unchecked into `buildAtomDid(instance.entityType, instance.entityId).raw` at `:76`, and that derived value becomes `atom_did`, the primary key, at `:105` under `ON CONFLICT (atom_did) DO NOTHING`.
**cheapest satisfier** any non null string reaching `instance.entityId`. Nothing between the caller and the insert asks whether it resolves to a node.
**semantically valid** NO. This is the same shape as the primary path's `pg-storage.ts:112` finding, reproduced independently in a second writer: **a derived identifier that inherits whatever the binding was.** The primary key is computed from the unvalidated binding, so a malformed binding does not collide with anything and inserts cleanly as a new row.
**derivation state** 4, none exists, self authored. The writer authors the binding, derives the identifier from it, and the identifier's uniqueness is the only thing checked.
**the separate read-path observation, which is NOT this row** `parseStoredAtom:44-59` returns null on unknown entityType or parse failure, and its own comment at `:38-43` states the intent: "a corrupt / schema-drifted row degrades to 'not found' rather than throwing, the read path stays honest". On a read path that is a defensible choice and it is declared, which is the doctrine's standard. It is recorded here so a later reader does not mistake it for the write-path defect. **The write path has no equivalent guard at all, declared or otherwise.**
**consequence for the enumeration** `document_ingest_atoms` was not in the prior enumeration. Its writer's binding handling is unenumerated and unguarded at both layers, the same as the primary path, and its database layer is one constraint weaker.

## W-12 resolution

The handover flagged `well-fact/geo.ts:153` as having a `geometryCentroid` whose ring path is
not the same text as flood's, and instructed that it not be called correct or defective from
a name match. Both were read.

**The divergence is real, and it inverts. Neither copy is uniformly correct.**

| input | `flood-hazard-fact/geo.ts:145` | `well-fact/geo.ts:153` | which is right |
| --- | --- | --- | --- |
| Point | returns the point | returns the point | same |
| Polygon | `ringCentroid(outer)`, which **de-duplicates the closing vertex** via `n = ring.length - 1` when first equals last (`geo.ts:132-136`) | inline vertex mean `sx / outer.length` over the whole ring, **no de-duplication** | **flood** |
| MultiPolygon | `ringCentroid` of the **first part only**, silently, with no declaration that N-1 parts were discarded | **returns null** | **well-fact** |

On Polygon, RFC 7946 requires the ring be closed, so `well-fact` double counts the closing
vertex on every well formed input and biases the centroid toward it by 1/n. On MultiPolygon,
`well-fact` refuses and `flood` answers for part one of N without declaring the degradation.
Refusal is the doctrine's shape; silent part-one selection is the defect.

A reconciliation onto either copy alone imports the other's defect. This bears directly on
the paused order's Step 3, which asks for the multi-part parcel population to be sized: flood's
centroid path answers for multi-part parcels today and does not say so.

**Reachability, resolved by reading and not by counting grep hits.** Three definitions of
`geometryCentroid` exist. Only one is reached.

- `flood-hazard-fact/geo.ts:145` is LIVE, called at `write-flood-hazard-fact-county.mjs:504` and in three `f1_pip_*` probe scripts.
- `well-fact/geo.ts:153` is DORMANT. `write-well-fact-county.mjs:31` imports it by exact name, unaliased, and the only occurrence of the string "centroid" in that entire file is the import line itself. No internal caller exists inside `well-fact` either.
- `special-district-fact/geo.ts:162` is DORMANT. Its own writer does not import it. Only the two test files reference the module's barrel.

Second mechanism considered and rejected: that `geometryCentroid` is reached indirectly through
`planCountyWellFacts`, imported from the same barrel, which would make the import redundant but
the function live. Rejected by enumerating every textual occurrence of the identifier across
`packages/` and `services/` at d3f3794 and finding none inside `well-fact/` other than the
definition and the barrel re-export.

**Correction to the sibling table.** Its first column reads as capability. Two of its three
`geometryCentroid: yes` entries are unreached, so they admit nothing today. The `well-fact`
closing-vertex defect is not currently firing. It is a trap rather than a live defect: the next
caller gets a biased centroid from a function whose name matches the correct one two
directories away.

## S-21 bundle answer: NOT FOUND

**Artifact searched.** `_catalog/branch_bundles/ldt-bp01-152-2026-08-19.bundle`, the bundle
itself, not a description of it. Fetched into a fresh bare repo with legacy-design-tools
`origin/main` supplied first as the thin bundle's prerequisite.

**Method.** 86 refs fetched from the bundle, 89 commits unique against ldt main. Every tree of
every bundle ref enumerated with `git ls-tree -r`, yielding 3,262 distinct paths. Searched for
`B2_cp2`, `geometry_scorer`, `geometryScorer`, `geometry-scorer`, then widened to any path
matching `scorer`, `B2` or `cp2`. Separately ran `git log --diff-filter=A --name-only` across
all 263 refs in the probe repo, which covers added-then-deleted and covers ldt's own full
history as well as the bundle.

**Positive control.** The traversal returns 45 `package.json` paths, so it is not a search that
cannot find anything.

**Result.** Zero matches. One near miss, `artifacts/api-server/src/_P1-2_cp2_verify.mjs`, which
confirms the lane-prefixed one-off `.mjs` naming family is real and did sometimes get committed.
It is not the producer.

**Coverage, stated precisely.** The bundle is thin. 86 of the 152 refs are packed, being the 82
squash-or-cherry refs plus the 4 empty-diff refs. The other 66 are ancestry-merged and their tips
are already on ldt `origin/main`, which the handover's prior sweep covered and which the
`--diff-filter=A` run above also covered. The README records 99 unique commits against
`5688aa31`; this probe measured 89 against current `origin/main`, consistent with ldt main having
advanced and absorbed some of them since the bundle was cut.

**Verdict.** Preserving the required wording: `B2_cp2_geometry_scorer_apply.mjs` is **NOT
RECOVERABLE FROM ANYTHING REACHABLE HERE**, not "gone". The systems seat's bundle was the last
named place and it does not hold it. Disposition, recover or re-derive or retire, is the
operator's and is not decided here.

Marked VERIFIED, by execution against the bundle, with a positive control. With W-9 rows 45 and
46, these are the only executed results in the pass.

## Counts

| bucket | carried | this pass | total |
| --- | --- | --- | --- |
| two-independent-sources | 13 | 1 | 14 |
| source-vs-our-derivation | 4 | 0 | 4 |
| dependency-pending | 0 | 0 | 0 |
| internal-consistency-only | 9 | 5 | 14 |
| none-exists | 5 | 10 | 15 |
| of which foreclosed | n/a | 0 | 0 |
| of which absent | n/a | 5 | 5 |
| of which self authored | n/a | 5 | 5 |
| **rows verified** | **30** | **16** | **46** |
| flagged not filed | 2 | 0 | 0 remaining |
| CHECKS THAT HOLD | 2 | 1 | 3 |

W-9 moves from UNMEASURED to CONFIRMED. It is a status change on an existing row, not a new row,
so it does not increment the count.

The third check that holds is `building-footprint-atoms.ts:160-161`, `if (storedOutcome ===
"present" && !atom.footprintGeometry) return fail(...)`. It refuses a present atom that carries
no geometry. Like S-15 and S-18 it REFUSES rather than defaulting, and it is the only one of the
six verifies that requires the atom's actual subject matter to be present before it will pass.
It remains internal consistency only, so it is a check that holds in shape rather than a check
with a second source.

Still zero dependency-pending rows, looked for explicitly again in this pass. The remediable set
remains buildable now.

## Paths completed against paths remaining

**COMPLETED**
- Path 2, the writer bodies. All six `*-fact-atoms.ts` verify and construction predicates read in full. All six `write-*-county.mjs` verify call sites read. All six `packages/atoms/src/*-writer.ts` swept for defaults and coalescing. All six `geo.ts` import blocks resolved for the shared-module question.
- W-12, resolved with a result that inverts the expected direction.
- S-21, answered by executing against the bundle.
- W-9, status changed to confirmed, both layers, with the two non shrinking caveats recorded.
- `document_ingest_atoms`, added to the enumeration, DDL and write path traced, rows 45 and 46.

**REMAINING**
- **Path 1, `packages/retrieval` beyond `three-layer-audit.mjs`. NOT STARTED. Not opened, not skimmed, zero rows.** The dispatch's prediction for it stands untested: sentinel defaults on serving sweep field tallies, and the `?? 0` collapse of absent, zero and unmeasured. This is the whole of the remaining enumeration and it is handed over intact.
- Within Path 2, three narrower items were not read and are named rather than implied: the six `plan-county-*.ts` planners, which is where `absenceKind` and `reason` are chosen before the writers receive them; the six zod schemas themselves, which set the floor for every `safeParse` in rows 31 to 44 and would sharpen every cheapest-satisfier column; and `hauska-map` at `204789f`, which was not read at all this pass.
- `document_ingest_atoms` callers were not enumerated. Rows 45 and 46 establish that the writer validates nothing; who calls `writeDocumentAtom` and what bindings they supply is unenumerated.

**Stopping basis.** Stopped on the instruction, not on exhaustion. Path 2 is complete and verified,
and beginning Path 1 with the remaining budget would have produced a skim of the retrieval package
rather than a read of it. A named untouched path is worth more than a thin one.

## Flagged, outranks the enumeration, not acted on

- **F-0**, the handover document does not exist anywhere reachable. Rows 3 to 30 survive only as dispatch prose. Same failure shape as S-21.
- **F-1**, the special-district vacuous-verify gate on Step 3 is already satisfied as of PR #306, 2026-08-11. Durable memory carries the stale form. Step 3's stated preconditions should be re-read against source before it runs.
- **F-2**, W-5's blast radius is larger than filed. It is a zone selection failure as well as a boolean failure. Row 42.
- **F-3**, the flood write-then-verify cannot see the flood determination. Row 31. W-5 and row 31 compose into a wrong hazard value that is written, certified and counted as verified.
- **F-4**, `footprintId ?? "primary"` reaches the atom DID and the entity ID. Row 35. This is an identity defect, not a field defect, and it is the row most likely to be mis-scored as cosmetic.
- **F-5**, `document_ingest_atoms` is a second atom store with one constraint fewer than the primary one and it was not in the enumeration. Whatever is concluded about binding validation on the `atoms` path is at least as true here, and the pair-uniqueness backstop does not exist.

## leave_behind

    leave_behind:
      - item: scratch bare repo at scratchpad/s21probe.git, 263 refs, used for the S-21 bundle walk
        owner: property seat
        plan_row: none, disposable, delete on read
