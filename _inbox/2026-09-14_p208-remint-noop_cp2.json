{
  "lane": "p208-remint-noop",
  "checkpoint": "CP2 (pilot/mid-work — evidence gathered, tentative resolution before the decisive live read)",
  "planRows": ["P-208"],
  "at": "2026-09-14",

  "evidenceGathered": [
    "doc_repo/_decisions/2026-09-11_setback_source_most_current_wins.md, 2026-09-11_ruling_b_reversed_polygon_only.md, 2026-09-12_footprint_merge_421_count_before_acquiring.md, 2026-09-13_engine_gate_token_minted_and_mounted.md and 2026-09-13_share_the_most_current_setback_resolver.md all cite 48021:34049 as an actively-used, live reference parcel through four different production callers (cortex-api, smartsite-mcp, hauska-mcp-server, Property Explorer BFF) and the engine's own setback resolver, as recently as 2026-09-13 -- one day before this dispatch's decline. All of these reads go through the serve path or engine compute, so by themselves they are consistent with EITHER hypothesis (the account could be genuinely live, or the serve path could simply be blind to a retirement that already happened by then) -- not yet decisive alone.",
    "doc_repo/_inbox/2026-09-03_parcel-txgio-reacq_close.json: the most recent Bastrop (48021) TxGIO re-acquisition, 2026-09-03. Full DELETE-then-INSERT reload of all 74,729 Bastrop rows; vintage unchanged (202503); row count byte-identical before/after. The SAME close independently discovered and had to fix a previously-unknown, silent per-column data-integrity defect in this exact reload path: upsertTxgioParcels never wrote the geom column, wiping geometry to NULL on every one of the six reloaded counties, caught only because Caldwell's expected-nonzero geom count came back zero. Verification in that close was coarse (row-count equality only, '74729=74729, not a loss'); no per-row field-fidelity check (e.g., that every feature's prop_id token round-tripped unchanged) was performed or claimed.",
    "doc_repo/_inbox/2026-09-13_prebake-eng_close.json (P-181, a dedicated read-only audit of this exact hauska-engine write-path family, completed the day before this dispatch): finding ENG-03, verified at hauska-engine origin/main 112bccb -- plan-county-parcel-nodes.ts's own assertion that the planner's key normalization matches a SQL-side resolver's is FALSE; parcel-geometry-resolver.ts:176 has no all-digit guard and no one-digit floor, so a token that the JS planner and the SQL resolver should treat identically can resolve to a different parcel. This is independent, dated, verified evidence that this exact identity-continuity machinery has a live, unfixed divergence-bug class as of one day before the retirement was observed.",
    "Code read (packages/engine-core/scripts/depth-warm-city-batch.mjs, p186-remint-wrapper worktree): the SAME script that runs the parcel-node preflight gate ALSO bulk-loads a live BCAD (Bastrop County Appraisal/GIS) currency map (bcadByPropId, via bastrop-batch-bulk-prefetch.mjs's bulkLoadBcadRingsByPropId -> fetchBcadParcelRings) and has a dedicated function, parcelCurrencyFromBcadMap, whose whole purpose is exactly this question: is this prop_id still in the county's live cadastral roll? The parcel-node gate runs FIRST in the per-parcel loop (line ~740) and `continue`s immediately on any decline, including parcel-node-retired -- so the BCAD currency check at line 782 is NEVER REACHED for 48021:34049 in this run. The script already contains the exact cross-check that would settle this contradiction; control-flow order simply never lets it run for a parcel the earlier gate has already declined."
  ],

  "tentativeConclusion": "The weight of evidence (parcel live through four production consumers as of 2026-09-13; a documented, verified per-row data-integrity defect in the exact reload that most plausibly produced this retirement; an independently-audited, currently-unfixed normalization-divergence bug in the same identity-continuity code, found one day prior) points to MECHANISM 1 (dispatch framing): the decline is spurious, the node is fine, and the fix belongs in the re-mint/reconcile's identity-continuity determination -- NOT mechanism 2 (genuine retirement, serve-path defect, escalate). This is not yet certain: the strongest possible confirmation is a live, out-of-band read of the true CAD source of record (the county's own public cadastral service), independent of every code path already examined. That read is the next and final step before closing.",

  "nextStep": "packages/engine-core/src/boundary-primitive/lot-line-scrub.ts exports BASTROP_BCAD_PARCELS_URL, a public, unauthenticated Bastrop County ArcGIS FeatureServer -- the same public-record endpoint fetchBcadParcelRings queries and the same one canon rule 3/4 requires ('NO PRIVILEGED DATA... uniform public-record'). A direct, read-only query against it for prop_id=34049 is the single decisive, at-source read: if the county's own live cadastral roll still carries this account, the retirement is proven spurious independent of any code-reading inference; if it does not, mechanism 2 is confirmed and this lane escalates instead of proposing a fix."
}
