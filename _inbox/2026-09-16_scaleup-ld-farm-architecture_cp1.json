{
  "checkpoint": "CP1",
  "kind": "design / pre-work adversarial review, filed before the bulk of source-reading was interpreted",
  "lane": "scaleup-ld-farm-architecture",
  "planRows": ["P-187", "P-198"],
  "seat": "dispatch-planner-ld, claimed via scripts/lane-claim.mjs (exit 0) in P:/seat-worktrees/dispatch-planner/doc_repo",
  "constraintsAcknowledged": [
    "read-only: no cloud resource created, modified or deleted; no product code written; no git commit/push (planner-owned)",
    "no sub-agents spawned (this dispatch's own line 1: 'You spawn nothing')",
    "gcloud/Secret Manager/Neon API reads only; Neon API used only through NEON_API_KEY already present in Secret Manager, never printed",
    "any DB read against FACTORY_DATABASE_URL / ATOMS_DATABASE_URL is a SELECT-only metadata/run-record read, never a write; no DSN printed to stdout or a committed file"
  ],
  "falsifiersPreRegistered": [
    "1. The corrected diagram must differ from the draft's diagram somewhere, with each differing element verified at source (gcloud describe, Neon API response, or a run-record query) -- not just re-asserted.",
    "2. The recommended option must be walked step by step against the actual publish-job code for 'two counties publish in the same hour', not asserted from the draft's prose.",
    "3. Every capacity number in the final report names its source (an API response, a run record, or a catalog query); none may be carried over from the draft's memory-sourced figures without independent re-sourcing, and any figure that cannot be re-sourced in this session is marked UNMEASURED with the reason."
  ],
  "sourceSnapshotAtStart": {
    "docRepo": "P:/seat-worktrees/dispatch-planner/doc_repo, branch seat/dispatch-planner, ahead 171 of origin",
    "hauska-engine": "root checkout /p/hauska-engine was on a detached HEAD (8d8e880) at session start; origin/main re-fetched to eb42ff2 (newer than the draft's cited 2d85fee) -- all code reads in this lane use `git show origin/main:<path>` / `git grep <pattern> origin/main`, never the stale working tree",
    "hauska-factory": "root checkout on local main, 270 commits behind; origin/main confirmed bfb7303, unchanged from the draft's snapshot",
    "hauska-map": "root checkout on fix/p35-vercel-token-preflight with local edits; origin/main confirmed f7fbcbf, unchanged from the draft's snapshot",
    "legacy-design-tools": "root checkout on feat/s1-instrument-hardening; origin/main re-fetched to 70113a14 (newer than the draft's cited ba39b4f5)",
    "note": "because two of the four repos moved past the draft's snapshot, this lane treats the draft's specific line citations as hypotheses to re-verify at current origin/main, not as ground truth to quote forward"
  },
  "credentialsConfirmedAvailable": {
    "gcloud": "authenticated as empressaioemail@gmail.com, project smartcity-os-prod default; hauska-prod-497015 and legacy-design-tools-prod reachable for run jobs/services/secrets list+describe",
    "NEON_API_KEY": "present in Secret Manager (hauska-prod-497015, created 2026-08-27); read-only GET calls against console.neon.tech/api/v2 confirmed working (projects, branches, endpoints, operations, organizations)",
    "psql": "PostgreSQL 18 client available locally; used against FACTORY_DATABASE_URL and ATOMS_DATABASE_URL for schema introspection and bounded SELECTs only, DSNs never echoed"
  },
  "planForThisPass": [
    "1. Enumerate every Cloud Run job/service across hauska-prod-497015 and legacy-design-tools-prod (gcloud run jobs/services list+describe) to build the writer/reader inventory the draft's diagram only partially sourced.",
    "2. Enumerate every Secret Manager DSN name (list only) to map which secret backs which store, cross-referenced against job env (names only, never values).",
    "3. Query the Neon API (projects/branches/endpoints/operations) for the factory and cortex-prod projects to get real compute sizes, autoscaling caps, branch counts and branch ages -- the draft flagged these as UNMEASURED.",
    "4. Run bounded, indexed or pg_stats-based read queries against the factory store (runs/run_events/termination_records/publish_runs/parcel_record_cell/leases) and the atoms store (atoms/atom_links/atoms_writer_lease_v2/lease_history) for schema facts and throughput samples, avoiding any unindexed full scan (heavy-scan rule).",
    "5. git grep origin/main across all four repos for the specific claims the dispatch names: SIX_COUNTIES, CONSTRAINT_SEARCH_COUNTIES, the Bastrop publish job's genericity, retract-by-run-id, atom_did shape (P-193), row versions on parcel_record_cell, atomBacked in the map/PE adapter.",
    "6. Write the report against the six report sections and the close JSON against the exact skeleton in the dispatch."
  ],
  "earlyFindingWorthFlaggingNow": "gcloud secrets list already shows FACTORY_DATABASE_URL_REPLICA (created 2026-09-04) and FACTORY_DATABASE_URL_RO (created 2026-09-02) alongside FACTORY_DATABASE_URL -- both post-date the draft-cited 2026-08-28 factory-store read-timeout incident by less than a week, suggesting a read-replica remediation already happened that the draft's diagram does not show. This will be checked against the Neon endpoints list for withered-surf before being reported as fact.",
  "constraintsHonored": [
    "no git operation beyond read-only fetch/show/grep/log",
    "no sub-agents",
    "writes confined to _inbox/ in this doc_repo worktree",
    "product repos read only via origin/main refs, working trees never touched",
    "every gcloud/psql/curl call wrapped in timeout"
  ]
}
