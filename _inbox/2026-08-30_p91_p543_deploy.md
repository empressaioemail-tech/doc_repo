---
id: 2026-08-30_p91_p543_deploy
title: p543 cortex canary on cortex-api-00668-cos, shift paired with p558
date: 2026-08-30
status: canary-0pct (shift after smartsite-mcp p558 is at 100%)
plan_row: P-91 (v2 card B2 F7 D2 F5 F2)
service: cortex-api
revision: cortex-api-00668-cos
tag: p543
digest: sha256:679683fd69626beec8a575758626c6b250e65da961a241f998d5f7bc0ec05332
tree: P:/tmp/legacy-design-tools-p91-cortex-leftover feat/p91-v2-cortex 0bcced84 (S9 cut 37cf5286 plus the build config)
build: Cloud Build 1e96eee0-2441-40d9-b12f-53393dd520ff SUCCESS (config cloudbuild.p543.yaml, root Dockerfile)
pr: empressaioemail-tech/legacy-design-tools #553 (Test SUCCESS, Typecheck SUCCESS on 37cf5286 and again on 0bcced84; squash-merged d8dfb319)
checkpoints: _inbox/2026-08-30_p91_v2_build_cp.md (S9 section)
---

# Why the shift waits

This image emits brief sections with an explicit `disposition`, including `unread` for drainage, and overlays whose `absent-verified` is downgraded to `unknown` with a reason when the vintage is not known. The MCP serving today (p557) re-derives a section's disposition from `data` and `refusal`, which would turn an `unread` drainage section into `absent` on the wire, a stronger claim than the record supports. The MCP cut that keeps an explicit disposition (S10, on `feat/p91-v2-panel`, commit `4cd2b57b`) ships as p558. Order: p558 to 100% first, then this tag, so no interval serves a claim the record does not make.

# Canary

Digest from Artifact Registry by field name. `gcloud run deploy cortex-api --image ...@sha256:679683fd... --no-traffic --tag p543`; service config carried; no `--set-env-vars`; no migration.

Tag `https://p543---cortex-api-tds7av26va-uc.a.run.app/api/healthz` answered `{"status":"ok"}`. Traffic by field name: `revisionName=cortex-api-00666-cuf percent=100 tag=p542`; `revisionName=cortex-api-00668-cos percent=undefined tag=p543`.

# What is on this image

B2: two queries resolving to one parcel write one row; later spellings are declared in `degraded.duplicates` with the kept query (no whole-screen refuse). F7: node depth carries a `drainage` section, `unread` with a reason, until the facet exists; the stub rail agrees. D2: every brief section carries an explicit disposition from one predicate per field; the stub is a projection of node. F5: `absent-verified` is earned by a known vintage; otherwise `unknown` with `provenance degraded; vintage unknown` or `provenance unknown; vintage unknown`; the pipeline radius prints as `500 ft`. F2: `zoneExposureSummary` withheld while `citationsDegraded`; em dashes removed. Consequence named on the card: every live typed absence paints `unknown` until typed-absence atoms carry a vintage (data lane).

# Shift

2026-08-30, after `smartsite-mcp-00065-siv` (tag p558) read 100% by field name and `https://mcp.smartsite.cloud/health` answered ok on that revision. `gcloud run services update-traffic cortex-api --to-tags=p543=100` exit 0. Prod `/api/healthz` `{"status":"ok"}`. Traffic by field name after the shift: `revisionName=cortex-api-00666-cuf percent=undefined tag=p542`; `revisionName=cortex-api-00668-cos percent=100 tag=p543`; `latestReady=cortex-api-00668-cos`; revision `imageDigest` equals the Artifact Registry digest above.

Live wire read through the planner's connector after both shifts: `create_screen` with five queries wrote four rows and declared `{"query":"111 Rainmaker Cove, Bastrop TX 78602","parcelNodeId":"48021:8720522","keptQuery":"111 Rainmaker Cv, Bastrop TX"}` under `degraded.duplicates` (B2); every resolved row's stub carries `drainage: "unread"` (F7, stub agrees with node); `get_smart_site` node on `48021:49295` carries `{"id":"drainage","disposition":"unread","reason":"drainage facet not produced for this parcel"}`, the pipeline overlay `state: unknown, reason: "provenance degraded; vintage unknown", provenance: degraded, vintage: UNKNOWN` labelled `No pipeline within 500 ft`, special district `unknown` with `provenance unknown; vintage unknown`, and no `zoneExposureSummary` while `citationsDegraded: true` (F5, F2). Status of this record: serving.
