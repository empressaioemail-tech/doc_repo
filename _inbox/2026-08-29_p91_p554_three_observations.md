---
id: 2026-08-29_p91_p554_three_observations
title: Three zero-deploy observations on p554
date: 2026-08-29
status: ready
plan_row: P-91
serving: smartsite-mcp-00057-xuk
uri: ui://smartsite/app-p554.html
source: _inbox/2026-08-29_p91_mcp_app_deep_dive.md
---

# How to run

Stay on `ui://smartsite/app-p554.html`. Do not disconnect for a new URI. Catalog stays 13. Use only Smart Site tools. Do not search the web. Do not call `ask_the_map`. Do not call `save_property`.

Look at two panels after every Send: the original board, and any new panel under the `get_smart_site` row. Prior grades scored "the panel" as whichever one showed a picture.

Paste everything below the line.

---

You are recording three observations on serving `p554`. Do not ship anything. Do not Open until the boot strip is copied.

## O1. Boot strip

On the live board, copy the boot strip verbatim. It starts `script-ran` and includes `caps=`.

Pass for the record: the full `caps=` list is in the reply. If it contains `serverTools`, say so. If it does not, say so. Do not paraphrase.

## O2. Gold Open

On a board that already has Pine resolved (`48021:34137`), click Open on that row. Send.

Predict: at about 12s the original board reads `Open did not reach me`, and a new panel under `get_smart_site` shows the parcel drawing.

Kill: the original board itself becomes the parcel. Then the deep dive is withdrawn.

Write both panel sentences. Do not score the new parcel as the board succeeding.

## O3. Miss Open

On screen `4316b571-c7d2-4b9f-9e50-4f7a16dbfa94` (A13), click Open on `48021:900099` (middle or lower row, not the top teal `34169`). Send. Do not use leftover-add-exist; that board has no Open on `900099`.

Predict: a new panel under the tool row reads `No screen yet. Paste addresses in the chat.` The original board goes dead-Open at 12s.

If no new panel mounts, say so. That is also a result.

Then stop.
