---
id: 2026-08-29_p91_build_cp2
title: P-91 build CP2. Adversarial review of sub-agent deliverables before commit
date: 2026-08-29
status: in-flight
plan_row: P-91
lane_planner: integration seat, P:/doc_repo main
build_plan: _inbox/2026-08-29_p91_build_plan_p555_p542.md
cp1: _inbox/2026-08-29_p91_build_plan_p555_p542.md section 6
---

Each section is written when that sub-agent returns and before its diff is committed. The planner reads the diff, reruns the suite, and names what would have made the deliverable wrong. Nothing here is accepted on the sub-agent's report alone.

# S2 server (stone clone), returned first

Diff read by the planner: `tools.ts` (strict override, enum schemas, `get_smart_site` non-OK branch, `ask_the_map` case deleted as dead after the readiness flip), `tool-honesty.ts` (`mapGetSmartSiteNonOk`, `buildRunReportErrorBody`, three old exports removed), `health.ts`, workflow smoke step. Suite rerun by the planner in the stone package: `Tests 3 failed | 123 passed (126)`; the three failures are `tests/pe-screen-query-resolve.test.ts`, a cross-package test committed with the p554 record that asserts the leftover clone's api-server behaviour, so it turns green only after the cortex branch is on main. That fixes the merge order: cortex PR first, then merge main into the stone branch.

Attack 1: the `Object.create` schema override could publish one schema and enforce another. Checked: the published JSON schema is the strict object (test asserts `additionalProperties: false` and exactly two required keys); the override only replaces the `unrecognized_keys` issue text. What would have made it wrong: the SDK calling `safeParse` or `parseAsync` instead of `safeParseAsync`; S2 traced the SDK's `validateToolInput` and the leak test drives that path with the five internal field names and asserts none echo. Accepted.

Attack 2: a 402 becoming `isError: false` could let a refusal be read as success. Checked: the body is `{ parcels: [], notFound: [], refused: [...] }` with `reason` on every row; the shape check throws when `reason` is stripped (falsifier test); `run_report` keeps `isError: true` on 402 with `status: "error"`, so the two tools disagree only where the contract says they must (the panel needs a mounted result; the report has no panel). Accepted.

Attack 3: the 404 rewrite could fabricate per-id absences on an array. Checked: the mapper returns null unless exactly one id was requested, and a test asserts a 404 on an array passes through as an error. Accepted.

Attack 4: health and the gate could still diverge through a different config object. Found: `app.ts` called `buildHealthReport()` with no argument, so an injected `authConfig` reached the gate but not health. Fixed by the planner (`app.ts`, one line) since S2's file list excluded it. Re-verified in the next suite run.

Attack 5: the runtime batch-cap refuse is now shadowed by the schema `.max(50)` and can never fire from MCP. Kept as defence in depth; recorded here so nobody reports it as a working gate.

Leave-behinds accepted from S2: `askTheMapArgsLeakInternalFields` and `sanitizeAskTheMapErrorBody` stay exported and tested with no `src/` caller until the `ask_the_map` wiring item re-arms the handler; `readOnlyHint` on `export_instrument` and `sanitizeExternalDraw`'s substring float checks were not in scope and remain deep-dive rows 8 and 6.

# S1 iframe (stone clone), returned third

Diff read by the planner: the listener's first line (`ev.source !== window.parent` refuse, counted on the boot strip), the reply branch (a `{}` reply on a pending Open clears the timer and paints the sent line; an error reply paints dead; `isError` and `content` replies `accept`), `accept` (clears timer, `openWait`, `openFail`, `openSent`, then parses; nothing to decide once the result carries its own sentence), the `INLINE_SHARED` embed (twenty exported helpers serialised by `Function.prototype.toString` into the served script, parity-tested against `parseToolResult` on thirty fixtures), county map and the six sentences. Suite rerun by the planner: 141/144 with the same three cross-package failures; `mcp-app` 20/20 and `mcp-app-served` 15/15.

Attack 1: the embed is proven under vitest's esbuild transform and a standalone `tsc` emit, but the Dockerfile builds production with `esbuild --bundle --format=esm` over `src/index.ts`, and a bundle renames a helper when two modules define the same top-level name; the template calls `parseToolContent(result)` by literal name, so a rename would throw `ReferenceError` in the iframe while every test stays green. Planner check, run: none of the twenty names is defined in any other `src/*.ts` file; a local bundle with the Dockerfile's exact flags (`esbuild --bundle --platform=node --target=node22 --format=esm`, same externals, esbuild 0.27.3) emits every helper under its own name (`function asRecord(`, `function parseToolContent(`, and so on, one each), and `_inbox/2026-08-29_p91_iframe_instrument_p555.mjs` run against that bundle passes 12/12, including the fresh-instance miss, the ack line, and the foreign-source refuse. Closed.

Attack 1b, found by CI on #550 rather than by review: the cross-package test `artifacts/smartsite-mcp/tests/pe-screen-query-resolve.test.ts` exists in two versions (main's calls the resolve core without `lookup` and asserts parse-equals-found; the stone p554 record's passes `lookup` but keeps a "lookup missing" case S3 removed). Either version fails on one branch. Fix: one version mirroring S3's lib test, committed with identical content on both branches, red on stone until cortex merges (documented in both PR bodies).

Attack 2: `p554`'s legacy miss shape (no `reason`) now paints as a board of unresolved rows, not a county claim. Answer: the server and iframe ship in one revision, so the legacy shape never reaches p555 from p555; from an older server it paints an honest "node unresolved" row rather than inventing a county. Accepted.

Attack 3: the two doc_repo instruments recorded p554 and deliver events with no `source`; under p555 they refuse every message and would report the old mechanism as broken. Planner re-pointed the instrument at the 4.1 contract with the source set (`_inbox/2026-08-29_p91_iframe_instrument_p555.mjs`); the p554 files stay as the record of what they measured.

Attack 4: `status: "degraded"` and `status: "refused"` bodies (batch cap, cortex unconfigured) still paint the empty copy because the wire carries no field the panel can name. Declared by S1, carried as a follow-up (declare-degradation on those bodies); not a regression.

Verification by mutation, run by S1 and read by the planner: guard removed fails 6, county map wrong fails 2, quote escaping removed fails 2, sent state disabled fails 1, embed helper dropped fails 3; restored copy byte-identical and 35/35.

# S3 cortex fail-closed (leftover clone), returned second

Diff read by the planner: `addToScreen` lookup catch (refuse before `maxOrdinal`, nothing written), `resolveQueryRow` classifying a node-id throw as `ScreenLookupUnavailableError` and `createScreen` refusing the whole create before the transaction, the resolve core taking `lookup` as a required argument, `sendBriefMiss` (probe null -> `parcel_not_found`, hit -> `baked_snapshot_not_found`, throw -> 503), the `ltrim` predicate with the post-filter kept, and the `/api` boundary on Express 5.2.1. Suites rerun by the planner in the leftover package: 11 files, 99/99 (lib, txgio, the four new tests, situsCompose).

Attack 1: a refuse of the whole create on one node-id throw could look like a regression against A5 forty (one bad id kills forty rows). Answer: a partial screen that marks a real parcel absent is the worse outcome (durable, pinned by the idempotent path, no API to correct); the 503 is retryable and names the query. Situs-search throws still propagate and now land as JSON 500 through the boundary rather than HTML. Accepted.

Attack 2: the `ltrim` arm turns a point lookup into a county-index scan on a miss. Answer: only on a miss, single-id path only, and S3 refused to add per-id probes on the array path for exactly this reason; the expression index is named as leave_behind. Accepted with the cost stated.

Attack 3: the entitlement fixtures were changed but could not run in the isolate (no `DATABASE_URL`). One fixture seeds a raw `txgio_parcel` row with a leading zero so CI exercises the `ltrim` arm against real Postgres. That is the one place CI can go red from this cut, and a red there is a loud failure, not a false pass. Accepted; CI conclusion string is the gate before merge.

Attack 4: the boundary's `expose: true` 4xx pass-through could rewrite a real server error as a client error. Checked: it fires only when the framework set `expose: true` and a 4xx status (body-parser), everything else is 500 `internal_error`. Accepted.

Not done and carried: total create budget still not bounded to the 25s contract; 8s timer still counts pool-queue wait; `reason` column and expression index are migrations (leave_behind).

# S1b batch flat rails (stone clone), after the p555 shift

Found by the planner's live probe through its own connector, not by review: the live batch stub carries rails flat on each parcel; the plan's 4.2 named a nested `stub` (planner error) and S1 built to it, so the p555 batch board painted six `unread` per row. S1b reads `stub` when present, else the flat record, through the same whitelist; the verbatim live JSON is the fixture in both suites, observed failing first; the mutation run fails exactly those two. Planner rerun: live JSON through `parseToolResult` now yields the six real states; iframe suites 37/37. Ships as p556. Lesson recorded: a wire contract written from the plan, not from a live call, is a claim; the probe is the check.

# S4 cortex rails at first paint (leftover clone), returned fourth

Diff read by the planner: `attachScreenStubs` (pure; resolved rows only; `mapPool` 8; a row not started inside 6000 ms is `skipped`; assembler null is a measured miss, every rail `unknown`, `stubRead: "ok"`; a throw or an out-of-vocabulary rail is every rail `unread` with `stubRead: "error"`; a resolved row with a null node is declared, not left bare) and `attachStubsForResponse` (route wrapper over `assembleStubBody` with a warn log per read error; applied on POST /screens and GET /screens/:id only; bare list untouched). Suites rerun by the planner: 11 files, 100/100, S3's 88 intact.

Attack 1: the paid gate. `assembleStubBody` is a bare function; `requirePePaidOrPropertyUnlocked` is middleware on the brief and research routes only, and `GET /saved-properties` already serves the same rails to the same user under `requirePeAuthenticated`. The route test's entitlement mock answers 402 if the paid gate is ever reached on a screens route. Accepted: a Free user sees rail states on their own screen, which is the intake surface, and nothing the stub path did not already serve.

Attack 2: `unread` standing in for a miss (the saved list's `?? "unread"`). Verified by violation: flipping the null branch to `unread` fails three tests (lib falsifier, POST, GET); restored byte-identical. Accepted.

Attack 3: the budget could be presence-shaped (a test that only passes by waiting). S4's first falsifier failed only by a 20 s hang and was rejected as weak; the test now asserts the started count (16 of 20) before the await, and disabling the budget fails that assertion. Accepted.

Cost stated: two sequential store reads per resolved row with a bake (snapshot, then the flood atom), one on a miss, zero on unresolved; cap 50 all-resolved is up to 100 reads, 8 wide, cut at 6 s. Carried: a `SERVE_REFUSED` snapshot read lands as `error`/`unread`, not `refused`; a started row has no per-row cap past the budget (same shape as S3's carried 8 s timer).
