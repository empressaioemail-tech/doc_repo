{
  "lane": "p167-strings",
  "checkpoint": "CP2 (mid-work: all three PRs open, package published, deploys pending)",
  "planRows": ["P-167"],
  "sinceCp1": [
    "atom-contract: PR #31 (feat/p167-no-zoning-stamp-token) merged as 7450249b97403c4e80a7f3c094f70a903a994ac2, all 3 CI jobs (18.x/20.x/22.x) SUCCESS. Tagged v1.34.0 and pushed; publish.yml (run 34782658177) completed success; npm view confirms @empressaio/atom-contract dist-tags latest=1.34.0 live on the real registry (verified after a short registry-propagation delay -- the first check right after the workflow completed still showed 1.33.1 and 404'd on 1.34.0; a second check ~15s later showed 1.34.0). Reported to the planner immediately per the briefing so other lanes are not blocked.",
    "hauska-engine: PR #440 (fix/p167-pdf-labels-from-vocab) opened, CI (typecheck + test) SUCCESS. site-model.ts:658-660's hardcoded buildable-refusal literal now reads envelopeHuman('atom_path_pending') instead of a literal substitution of buildableVocab.pdfLabel (which would have leaked the raw figure -- see CP1's pre-registered falsifier, confirmed true). author.ts's resolveZoningSummary now prefers zoningFact.absence?.reason over .kind, with envelopeHuman(kind) as a second fallback. Bumped @empressaio/atom-contract to ^1.34.0 across all 9 importers, refreshed pnpm-lock.yaml. 427/427 engine-core/src/site-plan tests pass; added 4 new regression tests (2 in feasibility-author.test.ts against the REAL pdf-feasibility surface wave 4 probed, 2 documenting the render.ts site-plan-sheet's own unrelated 12-word house-style gate). Updated 4 pre-existing tests that asserted the old hardcoded literal. tsc --noEmit clean. 5 pre-existing unrelated failures noted (4 vitest .mjs script-loading SyntaxErrors, 1 timeout flake under full-suite parallel load) -- confirmed by grep that none reference @empressaio/atom-contract; not caused by this diff.",
    "hauska-map: PR #399 (feat/p167-zoning-provenance-wire) opened, CI in flight. Bumped @empressaio/atom-contract to ^1.34.0. Fix 1 (shared with the above): the panel's envelope.disclosure FALLBACK (used only when an atom carries no absence.reason) now reads envelopeHuman('no-zoning-stamp') instead of a fourth hand-typed sentence; the common case (atom WITH absence.reason) already converged correctly before this lane touched anything. Fix 3: closed the OPS-23 P-152 lane 4 documented gap -- zoningProvenance rail's value now flows override.provenance (pe-record-to-facets.ts) -> facets.zoning.provenance (pe-property-atoms.ts's applyZoningOverride, widened its early-return guard so provenance-only overrides are not dropped) -> BakedFacetPayload.zoning's new optional field (src/lib/baked-facets.ts) -> zoningDistrictFromPayload no longer drops it (layer-absence.ts) -> the panel's zoning row prints it via provenance({sourceUrl}) (fact-sheet-resolver.ts). 2990/2990 tests pass across 204 files including all 4 test-time guard scripts; tsc --noEmit clean.",
    "A significant CP1 finding played out exactly as pre-registered: naively swapping site-model.ts's hardcoded string for buildableVocab.pdfLabel would have reintroduced an R-2 figure leak for the buildable-with-area/provisional dispositions. Routed through envelopeHuman('atom_path_pending') instead -- genuinely vocabulary-sourced, no leak, and happens to make the PDF byte-identical to the MCP's own existing wording for this disposition (confirmed live in the actual PDF text dump during testing: 'Buildable area is refused: Withheld, setbacks unruled').",
    "A second finding surfaced only while writing tests: render.ts's SEPARATE 3-sheet 'pdf-site-plan' export has its own pre-existing sheetReason/isCleanReasonSentence house-style gate (section 11: sentence case, <=12 words, ends in a period, no colon) that ALREADY rejected the raw 13-char 'no-zoning-stamp' code (no trailing period) before this lane's work and fell back to a fixed 'No zoning record on file for this parcel.' sentence -- so that sheet never actually leaked the raw code, and the 13-word humanized vocabulary sentence ALSO fails that same word-count gate (would also fall to the fixed fallback, unchanged). This sheet is UNTOUCHED by this lane (not this lane's to widen a pre-existing quality gate); the actual customer PDF surface OPS-23 wave 4 probed and named ('WHAT BINDS IT') is a DIFFERENT artifact (pdf-feasibility, via report-model.ts -> feasibility.ts's factOrChip, no such gate) and is where this lane's fix and regression tests actually apply."
  ],
  "outstandingBeforeClose": [
    "Merge hauska-engine PR #440 (CI already green) and hauska-map PR #399 (CI in flight) once green.",
    "Deploy hauska-engine-api under a lease (check _catalog/leases/hauska-engine-api.json first -- none held as of CP1), verify serving revision by field name.",
    "Deploy hauska-map via Vercel CLI under a lease (hauska-map is shared with p152-siblings this wave -- check _catalog/leases/hauska-map.json first).",
    "Regenerate the five-parcel side-by-side observation table via live probes (panel facets GET, MCP get_smart_site / export_instrument+pdftotext) post-deploy and write the observations file.",
    "Note for the planner / next lane: legacy-design-tools (the MCP surface, artifacts/smartsite-mcp) is NOT a registered worktree for this wave. It already imports envelopeHuman generically from the package (tool-honesty.ts), and wave-4's PR #672 already pinned it to ^1.33.1 (caret) -- so the MCP's no-zoning-stamp fix requires only a pnpm-lock.yaml refresh + redeploy to actually pick up 1.34.0's new mapping, no code change. This lane cannot do that (no registered worktree there) -- flagging for the close's leave_behind / next lane."
  ]
}
