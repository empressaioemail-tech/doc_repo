# WDLL: W5 depth-factory program (divide and conquer; the engine that outlives Texas)

Date: 2026-08-09  Status: approved
Operator approval: 2026-08-09 (card frozen; changes are amendments from here). Amendment 2026-08-09: item 1's "commit and push the scraper branch" precondition was resolved differently by live truth: origin/main already carries the full ecode360-scraper adapter and the local branch is 185 commits behind with no unique delta; branch recorded superseded, work proceeds from main. E-lane close grades: E1 partial (corpus PASS, stamp honest-absent), E2 pass (35 registry rows tranche 1), E3 correctly blocked (zero 48021 parcel-node anchors; runner behaved correctly).
Governs: OPS-14 workstream W5. Executing brief: Handoff E (dispatched 2026-08-09); E-lane numbers map to acceptance items below. Operator intent on record: not a slow crawl but multiple factory programs running in parallel on the frozen wave machinery, attention-free, because Texas is one of fifty and depth per parcel is what makes the product real.

## Done looks like

Three depth factories (F1 CAD rolls, F2 code/zoning corpus, F3 depth warm) run as standing parallel programs on the frozen wave shape, each on its own database write slot, each metered per jurisdiction, each producing machine-checkable close artifacts the Command Center can show. The launch-footprint counties reach satisfied on every depth rail. A state template exists that makes standing up state two a registry-plus-adapters exercise, sanity-checked against the Utah, New Mexico, Colorado, and Arizona candidate set. The operator can redirect attention entirely and read progress from the ledger.

## Acceptance items

1. F2: scraper branch committed and pushed; Smithville corpus live with eval scores; independent fidelity regrade by a non-builder sub (baseline method: 836 sections, 12,793 blocks, zero missing spans) | check: eval artifact plus regrade artifact | grade: [ ]
2. F2: per-city runbook delta exists and city N+1 runs from it mechanically (next CAPCOG city or the Pflugerville PDF path) | check: second city corpus live citing the runbook, no new machinery | grade: [ ]
3. F1: CAD source-registry tranches for the launch-footprint COGs first, then statewide; every tranche passes a 10 percent independent re-probe; districts with no public path get honest registry rows | check: registry JSON tranches plus re-probe artifacts | grade: [ ]
4. F1: CAD ingest waves land launch-footprint counties (`cad_property` grows beyond its current 15-county set) under the neondb write slot and the wave failure contract | check: per-county SQL counts; slot handoffs visible in `_STATE.md` | grade: [ ]
5. F3: Elgin unified dry-run at 0-mismatch against legacy counters, adversarially reviewed BEFORE apply; apply on planner go under the atoms write slot; Elgin serves | check: counter artifact, reviewer verdict, serve probe | grade: [ ]
6. F3: the city-batch runner proves registry-driven generality: a second city warms from its registry `warmRunner` row alone, no per-city script authored | check: run invocation cites only `--row-id`; no new script in the diff | grade: [ ]
7. Launch footprint DEPTH-SATISFIED: every county in `_decisions/2026-08-09_launch_footprint_counties.md` shows satisfied on CAD, owner, zoning, envelope, land use, and easements rails | check: per-county ledger displayState pasted | grade: [ ]
8. Cost metering live: ldt #393 tables record per-jurisdiction actuals for every factory wave; each wave brief carries a commitment-3 checkpoint | check: metering rows queried; a wave without cost rows fails review | grade: [ ]
9. State template worksheet exists: registry row schema, adapter contract list, and a "what changes for state two" section, sanity-checked by read-only recon against the UT, NM, CO, AZ GIS postures | check: worksheet doc plus four state recon notes | grade: [ ]
10. No state constant in shared machinery: every factory PR carries a reviewer check for hardcoded Texas facts outside registry rows and adapter modules | check: reviewer artifact per PR names the check | grade: [ ]
11. Wave failure contract observed everywhere: append-only run logs, status-artifact integrity, fresh idempotency apply2 after any resume | check: reviewer artifact per wave | grade: [ ]
12. Attention-free proof: one full factory wave runs end to end with zero operator involvement, progress read from the Command Center and ledger only | check: operator confirms from CC without reading any agent report | grade: [ ]

## Amendments

(none yet)

## Finish card (graded at close)

(pending)
