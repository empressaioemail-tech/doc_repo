# G-67 design-system kit copy

WDLL `_inbox/2026-08-17_g67_kit_copy_WDLL.md` (approved 2026-08-17). Sequence `_decisions/2026-08-17_ux_implementation_sequence.md`. Cite WDLL items 2-5 on every PR.

## Mission premise

Operator approved G-66, G-67, G-68, and G-69 on 2026-08-17. This card is first. Copy `_inbox/2026-08-17_sc_kit.css` byte-identical into three product repos as `web/sc-kit.css`. Link it from each `index.html` if a one-line link is cheap. Do not rewrite UI. Do not edit a token value. G-66, G-68, and G-69 do not start until this card is graded.

## Completion predicate

Three files exist and diff empty against `_inbox/2026-08-17_sc_kit.css` token block: `P:\smartcity-dashboards\web\sc-kit.css` (from origin/main `fdd615c`, not stale local main), `P:\smart-files\web\sc-kit.css`, `P:\plan-review\web\sc-kit.css`. Light `--sc-atom` is `#177F78`. Dark `--sc-atom` is `#4CC9C0`. Serving pins may stay. Live `smartcityos.io` unchanged. Planner owns merge.

## Scope basis

G-67 only. Kit file drop. Not Dashboards four-lens UI. Not Files browser. Not Plan Review console. Not Compass. Not AM repo. Not atoms `--apply`. Not L26. Not a second MCP. Not `P:\smartcity-os`.

## Workers (no deploy required, no merge)

Do not spawn further agents. Isolated worktrees from origin/main.

1. Dashboards (WDLL 3, 4): worktree from `origin/main` at `fdd615c`. Add `web/sc-kit.css`. Optional `<link>` in `web/index.html` and static route in `src/server.mjs`. No lens rewrite. PR.
2. Smart Files (WDLL 3, 4): worktree from origin/main. Add `web/sc-kit.css`. Optional `<link>` in `web/index.html`. Do not touch `src/*` or `web/api/files.js`. PR.
3. Plan Review (WDLL 3, 4): worktree from origin/main. Add `web/sc-kit.css`. Optional `<link>` in `web/index.html`. Do not touch `src/*` or `renderTab` logic. PR.

## Invariants

P:\smartcity-os no-touch. L26 holds atoms writer (idle; do not take the slot). One Hauska MCP. Deploys planner-owned. Verification stays with the lane planner. Token bytes must match. A local override is a defect.

CHECKPOINTS AND CLOSE:
  CP1: _inbox/2026-08-17_g67_cp1.json
  CP2: _inbox/2026-08-17_g67_cp2.json
  CLOSE: _inbox/2026-08-17_g67_close.json
