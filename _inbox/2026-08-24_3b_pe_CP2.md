---
id: 2026-08-24_3b_pe_CP2
title: Smart Site 3b checkout — PE CP2 close (scaffold)
status: checkpoint
date: 2026-08-24
plan_row: P-60
wdll: _inbox/2026-08-24_rebrand6_checkout_3b_WDLL.md items 4-9
tree: P:\tmp\hauska-map-checkout-3b
branch: fix/pe-checkout-3b
base_commit: ec36da5
---

# CP2: PE 3b checkout scaffold (items 4-9)

Implementer close. No commit. No push. No writes to `P:/seat-worktrees/property/hauska-map`. Product edits are only in `P:\tmp\hauska-map-checkout-3b`.

## Snapshot

| Field | Value |
|-------|-------|
| Tree | `P:\tmp\hauska-map-checkout-3b` |
| Branch | `fix/pe-checkout-3b` |
| Commit | `ec36da5` (uncommitted scaffold on top) |
| WDLL | Items 4-9, scaffold only |
| Frame | 3b — full-page subscription + unlock modal + return card |

## Files changed

### New

| File | WDLL |
|------|------|
| `apps/property-explorer/src/checkout/CheckoutPage.tsx` | 4 |
| `apps/property-explorer/src/checkout/checkoutLanding.ts` | 4 |
| `apps/property-explorer/src/checkout/checkoutCopy.ts` | 4 |
| `apps/property-explorer/src/checkout/UnlockCheckoutModal.tsx` | 5 |
| `apps/property-explorer/src/checkout/CheckoutSuccessCard.tsx` | 8 |
| `apps/property-explorer/src/lib/stripeAppearance.ts` | 6 |
| `apps/property-explorer/src/lib/stripeCheckoutMount.ts` | 4,5,7 (wrapper only; refuses missing secret; not called from CheckoutPage) |
| `apps/property-explorer/src/lib/checkoutOrigin.ts` | 9 |
| `apps/property-explorer/src/checkout/checkout-page.test.tsx` | 4 |
| `apps/property-explorer/src/checkout/unlock-checkout-modal.test.tsx` | 5,8 |
| `apps/property-explorer/src/lib/stripe-appearance.test.ts` | 6 |
| `apps/property-explorer/src/lib/billing-client-custom.test.ts` | 7 |
| `apps/property-explorer/src/lib/checkout-origin.test.ts` | 9 |
| `apps/property-explorer/src/lib/stripe-checkout-mount.test.ts` | 7 |
| `apps/property-explorer/src/browse/use-checkout-actions.test.ts` | 4,5,7 |

### Modified

| File | Change |
|------|--------|
| `apps/property-explorer/src/App.tsx` | `/checkout` pathname branch (mirror `/share`); success card on confirmed/timeout |
| `apps/property-explorer/src/lib/billingClient.ts` | `uiMode: "custom"` + `returnUrl`; result carries `clientSecret` + `publishableKey`; custom success = secret present; 200 without secret and without hosted Stripe URL is `ok:false` / `kind: "error"`; hosted `checkoutUrl` kept as fallback |
| `apps/property-explorer/src/browse/useCheckoutActions.ts` | If `clientSecret`, open `/checkout` or unlock modal; else if Stripe `checkoutUrl`, `location.assign`; never assign a non-Stripe URL |
| `apps/property-explorer/src/browse/PricingModal.tsx` | Renders `UnlockCheckoutModal` when custom unlock session is present |
| `apps/property-explorer/src/browse/ExplorerMap.tsx` | Passes inspected situs into PricingModal |
| `apps/property-explorer/src/lib/usePostCheckoutRefresh.ts` | On confirmed, `kickQueuedJobIfOrigin` (no-origin does not kick) |
| `apps/property-explorer/src/workbench/tools/ReportsTool.tsx` | Persists checkout origin when a locked report/export opens the paywall |
| `apps/property-explorer/src/lib/pe-checkout-tier.test.ts` | Asserts `uiMode: "custom"` on the wire |
| `apps/property-explorer/src/lib/property-unlock.test.ts` | Empty-secret 200 is error; body carries `uiMode` |
| `apps/property-explorer/package.json` | `@stripe/stripe-js` ^7.9.0 |
| `apps/property-explorer/vercel.json` | CSP allowlist for `js.stripe.com` + Stripe frame/connect hosts |
| `pnpm-lock.yaml` | Lockfile for the Stripe dep |

## Tests

Command (from `P:\tmp\hauska-map-checkout-3b`):

```
pnpm --filter property-explorer test -- src/lib/stripe-appearance.test.ts src/lib/billing-client-custom.test.ts src/lib/checkout-origin.test.ts src/lib/stripe-checkout-mount.test.ts src/lib/pe-checkout-tier.test.ts src/lib/property-unlock.test.ts src/lib/post-checkout-refresh.test.ts src/checkout/checkout-page.test.tsx src/checkout/unlock-checkout-modal.test.tsx src/browse/use-checkout-actions.test.ts src/browse/pricing-modal.test.tsx
```

Result: **11 files, 83 passed, 0 failed** (2026-08-24, vitest 3.2.7, tree `ec36da5` + uncommitted scaffold).

Violations that were shown to fail:

- Empty-secret 200 (subscription and unlock) → `ok:false` / `kind: "error"`
- Missing `clientSecret` on `mountStripeCheckout` → throw
- No checkout origin → `kickQueuedJobIfOrigin` does not call kick
- Non-Stripe hosted URL → navigation error, never `location.assign`

## WDLL items 4-9 — scaffold vs waiting on cortex

| Item | Scaffold-met | Still waiting |
|------|--------------|---------------|
| **4** Subscription page | `/checkout` route, left column from `PE_PRICING` + frame 3b, empty `data-testid=stripe-payment-element` | Live Payment Element mount; submit/confirm; live hard-refresh after cortex secret |
| **5** Unlock modal | Situs + `$15.00` + 30 days + `Pay $15.00`; same empty mount slot; no full-page nav on custom path | Live Element mount in the modal |
| **6** Appearance | Exact WDLL values unit-tested | Live wallets / promo on a sandbox card (needs mount) |
| **7** Client refuses missing secret | Custom success = `clientSecret`; empty 200 is error; hosted Stripe URL still accepted as fallback; mount wrapper throws without secret; CSP header added | Live cortex custom session; in-repo CSP does not prove smartsite.cloud headers |
| **8** Return card | `CheckoutSuccessCard` replaces the thin banner for confirmed + timeout; timeout title is `Confirming failed` and does not say paid | Live post-pay entitlement flip + receipt line from a real session |
| **9** Queued report | Origin persist; kick only if origin exists; violate no-origin still kicked | Real report/export engine start after a live confirm (kick callback is injectable; default is a no-op record) |

## Left unmounted

`CheckoutPage` and `UnlockCheckoutModal` do **not** call `loadStripe`, `initCheckout`, or `mountStripeCheckout`. The right-hand / modal slot is an empty named mount (`data-testid=stripe-payment-element`). `mountStripeCheckout` exists, refuses a missing secret, and is not wired to the UI.

Hosted `checkout.stripe.com` assign remains as WDLL item 3 fallback until cortex custom sessions return a secret and PE mounts.

Not done (as ordered): remove hosted fallback; invent card/email/ZIP fields; flip live Stripe keys; write LDT/cortex.

## leave_behind

- item: live Stripe Payment Element mount after cortex items 1-2 return `clientSecret` + `publishableKey`
  owner: PE lane on this isolated tree
  plan_row: P-60
- item: hosted `location.assign` kill (WDLL item 3) after mount is live on smartsite.cloud
  owner: PE + cortex
  plan_row: P-60
- item: PE-user Stripe Customer Portal for the success-card Billing button
  owner: property / LDT
  plan_row: P-60
