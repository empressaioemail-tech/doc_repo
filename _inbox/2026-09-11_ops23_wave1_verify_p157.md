---
id: 2026-09-11_ops23_wave1_verify_p157
date: 2026-09-11
row: P-157
kind: read-only fact verification
snapshot:
  hauska-factory: origin/main 217b7dd
  legacy-design-tools: origin/main 3950ce9b
  hauska-engine: origin/main 79fa573
  doc_repo: working tree (registry and inbox reads only)
---

# P-157 fact check: TCAD improvement detail for Travis (48453)

All reads via `git show origin/main:<path>` and `git grep origin/main`. No working tree was touched.

## 1. Rail registry (hauska-factory)

Path confirmed: `src/lib/parcel-record-engine/rail-keys.js` (197 lines). Structure or improvement rails, all in `group: "cad"`:

```
25:    { key: "yearBuilt", grain: "scalar", group: "cad", access: PUBLIC_RAIL_ACCESS },
29:    { key: "improvementValue", grain: "scalar", group: "cad", access: PUBLIC_RAIL_ACCESS },
30:    { key: "livingAreaSqft", grain: "scalar", group: "cad", access: PUBLIC_RAIL_ACCESS },
```

Other `sqft` hits are land or envelope, not structure: `acreageSqft` (23), `parcelAreaSqFt` (47), `buildableAreaSqFt` (48), `maxFootprintSqFt` (52). `stories`, `structur`: NOT FOUND. No rail exists for stories, building class, or improvement count.

## 2. How Travis appraisal data is ingested

hauska-factory does not ingest TCAD appraisal data. Searches in `src/` and `scripts/` for `IMP_DET`, `IMP_INFO`, `PROP.TXT`, `appraisal export`, `roll export`: NOT FOUND. The factory reads a pre-existing `cad_property` table on the cortex Neon store:

- `src/jobs/parcel-record-fill.mjs:85` `export const CAD_TABLE = "cad_property";` and `CAD_ROWS_SQL` at 277-285 selects `year_built, living_area_sqft, improvement_value` among others `FROM cad_property WHERE county_fips = $1`.
- `src/jobs/landing-import.mjs:12` copies `cad_property` to `landing_cad_property`; `src/jobs/f10-cad-loop.mjs:71` plans the statewide loop from `landing_cad_property`.
- `src/stages/adapt/cad.mjs:48-49` passes `yearBuilt`/`livingAreaSqft` through to the six-field candidate.
- `src/config/cad-declared-vintages.mjs:34` `"48453": { taxYear: 2026, tier: "cad-export" }`, a copy of LDT `lib/cad-ingest/src/vintage.ts` (header lines 1-17).
- The only TCAD-specific factory job is `src/jobs/acquire-ag-valuation.mjs`, which queries ArcGIS `TCAD/MapServer/0` (142-143) with `TCAD_OUT_FIELDS = ["OBJECTID","PROP_ID","geo_id","land_state_cd","land_type_desc","tcad_acres","market_value","appraised_val"]` (146). No improvement fields.

The actual loader is legacy-design-tools `lib/cad-ingest`:

- `lib/cad-ingest/src/counties.ts:32-38`: `"48453": { fips, name: "Travis", cad: "TCAD", format: "pacs", bulkPage: "https://traviscad.org/publicinformation/" }`.
- `lib/cad-ingest/src/sources.ts:120` `resolveCadBulkSource` has no 48453 entry (test `sources.test.ts:55` asserts undefined), so Travis is a manual `--file` load (`cli.ts:13-15`).
- `lib/cad-ingest/src/pacs/parser.ts:5-7`: "Input is the fixed-width `*_APPRAISAL_INFO.TXT` file, optionally enriched from `*_APPRAISAL_IMPROVEMENT_DETAIL.TXT` (year built + living area of "MAIN AREA" segments)." Lines 223-225: `const rollups = opts.improvementDetailFile ? await readImprovementRollups(...) : undefined;` and 191-192: `yearBuilt: rollup?.yearBuilt ?? null, livingAreaSqft: rollup?.livingAreaSqft ?? null`.
- `lib/cad-ingest/src/cli.ts:123-135` `discoverFiles` for pacs: `APPRAISAL_INFO.TXT` is required (fails without it); `APPRAISAL_IMPROVEMENT_DETAIL.TXT` is optional (`files.find`, undefined if absent, no warning). `--improvement-file` override at 21, 346-350.
- `lib/cad-ingest/src/pacs/layout.ts:107-117` `IMPROVEMENT_DETAIL` spans: `propId 1-12, propValYr 13-16, typeCd 41-50, typeDesc 51-75, yrBuilt 86-89, area 94-108`.

So year built and living area for any PACS county come ONLY from the improvement-detail file, and its absence from the drop is silent. Whether the Travis load included that file: NOT FOUND in either repo (no runbook, no `*.md` mention of `IMPROVEMENT_DETAIL` in LDT). doc_repo `_inbox/2026-09-02_parcel-scout-ossf_close.json:37` records `travis_livingAreaSqft.symptom: "0 of 500,307 at TCAD source"` with the mechanism "the ingest pulled the certified export only, which never contained the file the field lives in", confidence "asserted with provenance, not proven". `_catalog/tx_cad_source_registry.json` rows[9] (48453): `bulk_primary: false, current_tier: "cad-export", live_feature_count: 386682`.

## 3. Writer for the structural rails

Job: `src/jobs/parcel-record-fill.mjs` (`JOB_ID` line 47; Cloud Run job `factory-parcel-record-fill`, header 16). `FILL_OWNED_RAIL_KEYS` includes `improvementValue` (125), `yearBuilt` (128), `livingAreaSqft` (129). Engine module (pinned copy of hauska-engine): `src/lib/parcel-record-engine/ingest-existing.js`, `applyCadScalar` 91-162:

```
138:    stampNumber("improvementValue", cad.improvement_value);
141:    stampNumber("yearBuilt", cad.year_built);
142:    if (livingAreaIsPresentPositive(cad.living_area_sqft)) {
143:        stampValue("livingAreaSqft", cad.living_area_sqft);
145:    else if (livingAreaIsZero(cad.living_area_sqft)) {
146:        // existing >0 rule: 0 is not a CAD null and is not a value
148:    else if (isCadNullNumber(cad.living_area_sqft)) {
149:        stampAbsent("livingAreaSqft");
```

Decline on null: `stampAbsent` calls `cadNullVerified` (77-90), which emits `{ kind: "absent-verified", basis: { source: "cad_property", countyFips, propId, taxYear, vintage } }`; if `cad.tax_year` is null or blank it returns null and the cell stays `unaccounted` (78-81). Living area 0 stays `unaccounted` (header 13-14). Join miss: `applyCadJoinMiss` 184-207 stamps `absent-verified` with a basis carrying no taxYear (169-177). Sampling targets Travis nulls: `isTargetedShape` line 268 `if (county === "48453") return cad.living_area_sqft == null;`.

Flag for the lane: a null `living_area_sqft` on a matched Travis row becomes `absent-verified` with basis `cad_property`, regardless of whether the detail file was ever in the file set. Given item 2, that is the ENFORCEMENT case "never convert unaccounted to absent-verified"; the writer cannot distinguish "looked and found none" from "never loaded the file".

## 4. Per-rail null-rate or present/absent instrument

`nullRate`, `null rate`: NOT FOUND. Three instruments exist:

- `src/jobs/county-rail-coverage-report.mjs` (131 lines, read-only): `CELL_STATE_COUNTS_SQL` 66-73 counts `cell_state->>'kind'` per county per rail from `parcel_record_cell`; denominator is `count(DISTINCT prop_id) FROM cad_property` (52-55); summarised by `src/lib/county-rail-coverage.mjs:64` (`pct` null when unmeasured, 60-63). Flags: `--county=<fips>` repeatable, `--rail` optional (defaults to all rails), `--now-iso`. Trigger: NOT FOUND. `src/cli.mjs` usage (355) does not list it; only caller in the tree is `test/county-rail-coverage-report.test.mjs`. Dormant per ENFORCEMENT.
- `scripts/ctx-b3/store-census.mjs`: censuses baked leaves in `place_layer_snapshots`, including `baseFacts.cadRoll.improvementValue` (95); livingAreaSqft noted as "the fifth" (84). Invocation (40-41): `node scripts/ctx-b3/store-census.mjs --target=staging [--county=48021 ...] [--batch=2000]`.
- `scripts/gate8/field-state.mjs`: per-parcel field states on the served wire, `yearBuilt` 248-252, `livingAreaSqft`/`improvementValue` in `CAD_VALUE_FIELDS` 160-166. Invocation `scripts/gate8/run.mjs:5-7`: `node scripts/gate8/run.mjs --arm production|fixture|selftest|both --surface pe --county 48021`.

## 5. legacy-design-tools structuralFact

Producers: `artifacts/api-server/src/lib/structuralFactResolve.ts` (187 lines, pure) and `structuralFactRead.ts` (88 lines, async). Source is the `cad_property` roll table, not an atom: `structuralFactRead.ts:68-69` `makeCadPropertyLookup(db)` then `cadLookup(countyFips, propId)`; `cadPropertyLookup.ts:73-83` selects `FROM cadProperty WHERE countyFips, propId, taxYear = declared.taxYear`. A parcel_record overlay is merged on top in `routes/propertyExplorer.ts:298-301` via `structuralFactWithParcelRecordOverlay`, fed by `cadRollFactFromParcelRecord.ts:144-153` (`loadParcelRecordCell(countyFips, propId, "livingAreaSqft")`, Factory store via `FACTORY_DATABASE_URL_RO`, `parcelRecordCellRead.ts:160`).

Shape, `structuralFactResolve.ts:18-35`:

```
18: export type StructuralFactPresent = {
19:   state: "present";
20:   source: typeof STRUCTURAL_FACT_SOURCE;     // "structural-fact"
21:   entityType: "cad_property";
22:   countyFips: string;  23: propId: string;  24: taxYear: number;  25: tier: string;
26:   livingAreaSqft: number | null;
27:   yearBuilt: number | null;
28:   sourceVintage: string | null;
31: export type StructuralFactAbsent = LayerAbsenceWire & { source: ... };
```

Two discriminators: present uses `state: "present"`; absent uses `status: "absent"` plus `verdict` (`verdictLayerServe.ts:65-79`), values from `LAYER_ABSENCE_VERDICTS` including `absent-verified`, `lookup-failed`, `stamp-missing`, `not-applicable`, `refused`, `unmeasured`. Decision at 152-171: both fields null and county not in `BULK_PRIMARY_COUNTY_FIPS` (`{48113, 48439}`, line 82, so Travis is not) returns `structuralFactAbsentVerified(... "CAD row present but structural fields (living_area_sqft, year_built) are null")`. Same flag as item 3: for Travis this is `absent-verified` on a field the loader may never have been given.

## 6. hauska-engine entity types

`packages/atoms/src/property-instances.ts:198-216` `PROPERTY_ENTITY_TYPES` (17): parcel-node, zoning-fact, setback-rule, buildable-envelope, parcel-terrain-model, building-footprint, utility-easement, flood-hazard-fact, cad-parcel-roll, land-use-fact, owner-fact, rail-corridor-fact, well-fact, special-district-fact, road-node, rrc-pipeline-fact, property-boundary-edge. Structural or improvement entity type: NOT FOUND (searched `structural`, `improvement`, `living-area`, `year-built` across `packages/`). The structural fields ride on `cad-parcel-roll` as optional observation fields: `packages/atoms/src/cad-parcel-roll-writer.ts:57-58` `yearBuilt?: number; livingAreaSqft?: number;`. Engine source of the factory's pinned module: `packages/engine-core/src/parcel-record/ingest-existing.ts:187,190` and `rail-keys.ts:31,35,36`.

## Searched and not found (summary)

hauska-factory: `IMP_DET`, `IMP_INFO`, `PROP.TXT`, `appraisal export`, `roll export`, `nullRate`, `null rate`, `stories`, `structur` (in rail-keys), any CLI or cloudbuild trigger for `county-rail-coverage-report`. legacy-design-tools: `IMP_DET`, `IMP_INFO`, `PROP.TXT` anywhere; any `.md` record of a Travis load with `APPRAISAL_IMPROVEMENT_DETAIL.TXT`; `48453` in `sources.ts`. hauska-engine: any structural/improvement entity type.
