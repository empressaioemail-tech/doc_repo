---
id: 2026-08-30_p91_p558_deploy
title: p558 Smart Site MCP App v2 panel, serving on smartsite-mcp-00065-siv
date: 2026-08-30
status: serving
plan_row: P-91 (v2 card D1 D2 D3 D4 D7 F1 F2 F5 F6 P1 C1 C2 R1 B1 B2 B3 B4 B5 F7 H1 H2)
service: smartsite-mcp
revision: smartsite-mcp-00065-siv
tag: p558
uri: ui://smartsite/app-p558.html
digest: sha256:1d11950148f8bd5942152a8a00e117f1493816218fd5703490a1296462409ed0
tree: P:/tmp/legacy-design-tools-p91-stone feat/p91-v2-panel ff36c8f0 (S10 4cd2b57b, S6 525f3bca, S7 29086224, S8 8a34f2c6, then origin/main merged with zero conflicts)
build: Cloud Build a9ba50e4-aabb-4912-b3de-cf0f0a6d705b SUCCESS (config artifacts/smartsite-mcp/cloudbuild.p558.yaml, project legacy-design-tools-prod)
pr: empressaioemail-tech/legacy-design-tools #555 (opened from ff36c8f0; Test SUCCESS, Typecheck SUCCESS, CLEAN; squash-merged 24553cfc)
paired_with: cortex-api tag p543 on cortex-api-00668-cos, record _inbox/2026-08-30_p91_p543_deploy.md
checkpoints: _inbox/2026-08-30_p91_v2_build_cp.md
---

# Canary and shift

Digest read from Artifact Registry by field name. `gcloud run deploy smartsite-mcp --image ...@sha256:1d119501... --no-traffic --tag p558`, exit 0; service config carried, no env or secret change. Tag `https://p558---smartsite-mcp-tds7av26va-uc.a.run.app/health` answered `{"status":"ok","service":"smartsite-mcp","name":"Smart Site","version":"0.0.1","authConfigured":true,"cortexConfigured":true,"revision":"smartsite-mcp-00065-siv","failureDomain":"smartsite-mcp"}`. The shift was gated on `status: ok` and `authConfigured: true` in that body; `update-traffic --to-tags=p558=100` exit 0; `https://mcp.smartsite.cloud/health` then answered the same body on `00065-siv`. Traffic by field name after the shift: `00033-hin` tag p542 no percent, `00035-tov` tag p543 no percent, `00063-rej` tag p557 no percent, `00065-siv` percent 100 tag p558; `latestReady` is `00065-siv`; the revision's `imageDigest` equals the digest above. Only after that did cortex p543 shift, so no interval served a cortex that emits `unread` through an MCP that rewrote it.

# Wire read after both shifts

Through the planner's own connector, four calls against the serving pair.

`create_screen` "Higgins v2 wire probe" with `102 Higgins St`, `108 Higgins St`, `zzzz-not-a-situs-99999`, `111 Rainmaker Cv`, `111 Rainmaker Cove ... 78602`: four rows; `degraded.duplicates` is `[{"query":"111 Rainmaker Cove, Bastrop TX 78602","parcelNodeId":"48021:8720522","keptQuery":"111 Rainmaker Cv, Bastrop TX"}]`; resolved rows carry `stub` with `situs present, zoning present, landUse unknown, flood present, drainage unread, envelope refused` and `stubRead: ok`; the zzzz row is `parcelNodeId: null` with a uuid `id` and `resolution: unresolved`, the shape S8's regression fixture binds to no node.

`get_smart_site` node on `48021:49295`: sections zoning `present` with the ArcGIS citation, setbacks-envelope `refused` (`atom_path_pending`), flood `present` with `citationsDegraded: true` and no `zoneExposureSummary`, land-use `absent`, drainage `unread` with reason `drainage facet not produced for this parcel`; overlays flood `present` (Zone AE FLOODWAY, tint-ring), footprint `unknown`, envelope `refused`, pipeline `unknown` with `provenance degraded; vintage unknown` and the label `No pipeline within 500 ft`, specialDistrict `unknown` with `provenance unknown; vintage unknown`, well `unknown`; eleven edges each with `ft`, bearing, adjacency and, where mapped, a neighbor id or a road node.

`list_screens` with no id: `{"screens":[{id, name, rowCount, createdAt, updatedAt}, ...]}`, 27 screens, the picker's shape.

`get_smart_site` node with 26 ids: refused as an error body `{"status":"refused","reason":"parcel_batch_cap","cap":25,"received":26,"depth":"node"}`; the host mounts no widget on an error result, which is the H2 design (the description names the cap and why).

# What this image serves

The iframe on `ui://smartsite/app-p558.html`: hover a property line and read its length, bearing, adjacency and neighbor from the edge object (D1); a shared line to a named neighbor is a door that drafts Open or Add to screen (D2, C2), never across a right of way (O3); the zoning district and jurisdiction on the drawing with the citation on the text (D3, F1); flood tint by SFHA (D4); north arrow and a fixed-list scale bar labelled unit reference (D7); flood facts as a row, no prose (F2); `absent-verified` painted only when earned (F5); as-of on every row (F6); the why-turn drafting a question from the record's own fields (P1); Save with a status chooser (C1); a local Report view (R1); board candidates and Look this up (B1), declared duplicates and reads (B2), reopen picker (B3), county groups (B4), completeness sort (B5); explicit `unread` kept through normalization (F7); every non-OK body declared with `status` and `reason` (H1); node batch cap 25 (H2). Repaired on this image: the p555 uuid binding on unresolved rows.

# Not on this image, by name

Footprint geometry, envelope and setbacks, land use, drainage facets, flood citation URLs, a vintage on typed-absence atoms (data lane; everything re-warms when it lands). `countyFips` on candidates and the flood `asOf` fallback (cortex, property seat). Exports (substrate seat endpoint). N1, F9, R3 (P-92). Free tier unchanged.
