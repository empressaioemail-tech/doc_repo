# L-C blocker history — every problem hit bringing six Texas counties online, so Burnet, Bell and Milam hit none of them twice

Read-only research lane, dispatched as part of the "Texas scale-up research wave" (OPS-24, plan row P-188). This report holds all four required deliverables. Deliverable 1 (the full blocker register) is filed separately per the dispatch's own naming, at `_inbox/2026-09-16_scaleup-lc_blocker_register.json` and `_inbox/2026-09-16_scaleup-lc_blocker_register.md` — 274 distinct blocker instances, one entry per instance, across 18 named classes (the six CTX classes, the seventh, and eleven further classes found with real instances) plus eight single/low-instance NEW candidates. Deliverables 2 through 4 are reproduced in full below.

## Sources actually read

Every source the dispatch named was covered, either directly by this lane or by a dispatched sub-agent. All six sub-agents eventually reported, though two (agents 1 and 5) reported late, after this lane had already begun assembling the deliverables, and both had chosen to further fan into their own child agents rather than reading serially — agent 1's children never produced output after 48+ minutes and agent 1 completed its entire assigned range (OPS-16 A-080 through A-177, the file's full amendment log, plus OPS-19/21/23/24 and both enforcement documents) directly itself; agent 5's children also stalled, and agent 5 delivered a full direct read of eleven `_sessions/` files from 2026-09-13 through 2026-09-16 while its remaining assigned range (2026-08-21 through 2026-09-13) was never delivered before this close was filed. Both late reports arrived after the register had already reached 274 instances from the other four sources; their material was reviewed, deduplicated against what was already filed, and folded in as two further revisions (to 318, then to 361 instances) before this report and close were finalized. The residual gap — agent 5's own 2026-08-21–2026-09-13 range — is named explicitly in Coverage gaps below rather than silently absorbed.

- **Direct reads by this lane:** `_sessions/2026-09-10_MIDSESSION_ctx_completion_and_the_six_classes.md` (the six CTX classes, verbatim); `_inbox/2026-09-10_ctx_third_party_review.md` (the seventh class plus ten ranked defects and a live-state table); `_inbox/2026-09-13_dead_controls_ranked_fixes.md` (seven controls-that-cannot-fail plus the absent-verified false-earned-state finding); `ENFORCEMENT.md` in full; `90_operations/OPS-19_factory_plan_of_record.md`, `OPS-21_serve_completion_program.md`, `OPS-23_surface_completion_program.md` (F1–F25 in full), and `OPS-24_county_to_serving_program.md` in full; `_inbox/2026-09-14_ops24-teardown_close.json`; `_inbox/2026-09-13_coverage-sub_cp1.json`; `90_operations/onboarding_defect_class_backlog.md`; a direct grep of `90_operations/OPS-16_texas_market_plan_of_record.md`'s full amendment log (A-080 through A-177) for its own duplicate-id defect (found live, filed as BLK-072); and — because they are dated 2026-09-16 itself and are the direct source of the canon-preamble's decision — `_inbox/2026-09-16_texas_scaleup_program_scope.md` (rev 2, the single richest and most current source this lane read), `_inbox/2026-09-16_pickup_list_planned_not_done.md`, `_inbox/2026-09-16_farm_architecture_draft.md`, `_decisions/2026-09-16_texas_scaleup_sequence_and_four_rulings.md`, and `_inbox/2026-09-14_HANDOFF_farm_model_and_burnet_prototype.md`.
- **Sub-agent 2 (complete):** the three 2026-09-13 assumption registers (engine, factory, LDT — 94 rows read in full), the national-scale adversarial review, the national program framework WDLL, and the OPS-24 teardown review, cross-checked against this lane's own direct read of the teardown close.
- **Sub-agent 3 (complete):** all 24 "OPS-23 wave 6" commits (`d23edddc^..efa64de5`, the exact confirmed range) read in full via `git show`, plus their cited `_inbox/` artifacts.
- **Sub-agent 6 (complete):** the seven named Burnet/Bell/Milam facts (all seven confirmed, with citations, reproduced in the unfixed list below), the actual eleven-blocker CTX planner-retirement handover (found under its real filename, `_sessions/2026-09-09_ctx_bake_run_claude_code.md`, after the name given in the dispatch turned out not to exist in this worktree), and an `_inbox/` sweep for postmortem-shaped documents.
- **Sub-agent 4 (complete):** the full `_sessions/` window 2026-07-20 through 2026-08-20 (55 files), returning 81 further distinct instances.
- **Sub-agents 1 and 5:** dispatched to cover OPS-16's amendment log A-119 through A-177 plus F-rows and ENFORCEMENT.md cross-checking, and the `_sessions/` window 2026-08-21 through 2026-09-16, respectively. Both chose to further fan into their own child agents rather than reading serially, and neither had returned a consolidated result by the time this report was filed. See Coverage gaps below.

## Falsifiers, scored

**1. The six CTX classes and the seventh must each carry at least the instances their source documents name.** HELD, in the expected direction (more, never fewer). C1 (punctuation-only) carries 3 against the session's 1 headline instance (18,037 rows), because the same mechanism recurred independently at statewide scale (1.25M parcels, BLK-192). C4 (missed sibling) carries 22 against the session's 2 named instances, recurring at least nine further times across OPS-19, OPS-23, and the assumption registers. C5 (ruling never implemented) carries 12 against 1 named instance. C7 (the seventh class) carries 34 against the third-party review's own "two instances found in an hour," because the LDT source-parser sweep alone found 25 more matching the identical mechanism. Full counts and the class-by-class detail are in the class table below.

**2. At least one class the record treats as "fixed" must turn out, at source, to be undeployed, unverified, or uncontrolled.** HELD, independently, well beyond four times: (a) ENFORCEMENT's own text names a prose-only control that "did not bind" for nine days across three documented violations before being armed as a hook; (b) OPS-21's own text states plainly that ninety-four cutovers have shipped and nobody has verified a single legacy path actually retired; (c) the empty-county gate fix (BLK-017/018) was "designed" 2026-09-13, confirmed not landed 2026-09-14, and confirmed **still the live, unfixed defect** on 2026-09-16 — three independent re-traces, three confirmations it remains broken; (d) BELL-COST-GATE-BREACH was "CLEARED 2026-08-05" by deleting the cost term the operation actually incurs, not by making the operation cheaper — found independently by two different sub-agents reading two disjoint source sets (the adversarial review and the onboarding defect-class backlog) and confirmed a third time by agent 1's direct OPS-16 read; (e) the Bastrop 92.5% false-retirement incident (BLK-025) was independently found and narrated by three separate readers (this lane's own ENFORCEMENT.md read, sub-agent 4's sessions sweep, and agent 1's OPS-16 read), each adding detail the others lacked — agent 1's version specifically found that a second, independently-derived live-currency check existed in the *same script* but sat *after* the gate in the execution order and therefore never ran once the gate had already declined, which is a sharper and more damning version of "uncontrolled" than "no control existed at all"; (f) the "94 assumption rows" figure and the "3 lease-less writers" figure were both independently re-derived as wrong by a second reader after being restated as settled (BLK-062/019), and agent 1's own read found a THIRD instance of the same shape in the same review (a "2 gate files" claim, actually 3); (g) six separate instances of "merged is not terminal state" were found in one review window alone (BLK-333), a meta-confirmation that this failure mode is not rare. Item (c) remains the single most consequential instance, because every downstream class's recurrence-control claims assume this gate works, and it does not; item (e) is the sharpest, because it shows a *working* second control rendered inert purely by where it sat in the code, not by its own design.

**3. The smooth-path check for class 5 must be one a machine executes, not "the planner remembers."** HELD. Check P2-2 in the smooth path below specifies a concrete script (`scripts/ruling-implementation-check.mjs`), a concrete trigger (any commit touching `_decisions/**` or a type file a decision references), a concrete failure condition (a named required type member that does not exist, or a named required function that is unreachable from a live serve route), self-test fixtures in both directions per ENFORCEMENT's "verify by violation," and a named residual bypass (a decision filed with no machine-readable requirement at all) rather than a silently-assumed-covered gap.

## Coverage gaps, named rather than silently absorbed

**Agent 1 completed its full assigned range** (OPS-16 A-080 through A-177, OPS-19/21/23/24, ENFORCEMENT.md, 61_enforcement_doctrine.md) by reading it directly after its own child agents stalled; its full raw inventory (75 OPS-16 amendment items, 22 OPS-19 items, 15 OPS-21 items, an OPS-23 F-row restatement, an OPS-24 finding restatement, and 26 named ENFORCEMENT/doctrine instances) was reviewed and a curated 43-instance selection of the clearly-novel findings was folded into the register; the remainder were duplicates of instances this lane and sub-agents 2/3/4/6 had already found independently by other paths (itself a further, independent corroboration of falsifier 1: the same classes, found the same way, from disjoint source sets).

**Agent 5 delivered only its final batch** (`_sessions/` 2026-09-13 through 2026-09-16, 11 files, 55 instances, fully folded in) before this close was filed. Its assigned range 2026-08-21 through 2026-09-13 (roughly 57 files) was never delivered — its own child agents stalled with no output growth after approximately 50 minutes, by agent 5's own report. This is the one genuine, acknowledged gap in this lane's source coverage: the `_sessions/` window 2026-08-21 through 2026-09-13 was not swept file-by-file the way 2026-07-20–2026-08-20 was by sub-agent 4. It was not left completely dark — this lane's own direct reading covers OPS-19, OPS-21, OPS-23, and OPS-24 in full (which absorb and narrate most of what happened in that window at the program level), the two sessions most central to the mission (the six-classes session and its companion eleven-blocker retrospective), all 24 OPS-23 wave-6 commits (agent 3), and every 2026-09-13 through 2026-09-16 planning document — but session-level narrative detail for late August specifically (roughly 2026-08-21 to 2026-09-08) is the one part of the dispatch's named source list this lane cannot certify as fully read. Filed as an explicit `leave_behind` item in the close, not silently omitted.

---
# The class table

361 blocker instances found and filed (register: `_inbox/2026-09-16_scaleup-lc_blocker_register.json` / `.md`; revised up twice from an initial 274 — first to 318 after a peer sub-agent's partial sweep of `_sessions/2026-09-13` through `2026-09-16` (44 instances), then to 361 after a second peer sub-agent's full direct read of OPS-16's amendment log A-080 through A-177 plus OPS-19/21/23/24 and both enforcement documents (43 further curated instances) — both folded in before this close was filed). Per DEV_PROCESS 1.3, every count below is a direct count from the register, never a subtraction or an estimate. "Unfixed" means the register's own `fix_status` field reads `unfixed` (including `unfixed at finding` and similar) as of the date each source was last read; a status of `fixed-undeployed`, `deployed`, `fixed-partial`, `ruled/unbuilt`, etc. is counted separately and is not folded into "unfixed" even though several of those are also not yet reaching a customer. "Recurrence control that can fail" is a mechanical keyword classification (the `recurrence_control` field names a specific mechanism rather than containing "none," "proposed," "not built," "not confirmed," or similar) — it is a heuristic, not a hand-audit of all 318, and should be read as directionally accurate rather than exact.

The six CTX classes and the seventh are C1 through C7 below, matching the mission's own numbering. Eleven further classes (C8 through C18) and twenty single/low-instance NEW candidates were found with real instances and are listed per the mission's rule that a candidate joins the register only once it has instances — every one below does; the twenty NEW candidates are summarized after the main table rather than given their own rows, to keep the table readable.

| Class | Name | Instances | Counties hit | Unfixed | Control that can fail | Found at stage | Could be caught at stage |
|---|---|---|---|---|---|---|---|
| C1 | Punctuation-only/malformed input skipped before write | 3 | Bastrop, Caldwell, Travis, Hays, Williamson (+ a statewide 1.25M-parcel instance) | 2 | 0 | 10 Publish / 11 Probe (a customer or QA sweep reads the served value) | 3 Acquire (a sentinel check on the raw source field) or 5 Instantiate (a row-parity assertion: every input row produces exactly one output row) |
| C2 | Ceiling/verdict measured by one instrument, enforced or read by another | 16 | Bastrop, Caldwell, Hays, McLennan, Travis, Williamson, cross-county | 9 | 2 | 9 Gate / 10 Publish / 11 Probe | 2 Pre-bake audit (name which instrument is authoritative before either runs) or 8 Completeness (the class *is* "two counts disagree") |
| C3 | Parcels/records reaching no state at all | 11 | Bastrop, Travis, Tarrant, Bell, Harris, Hays, Burnet | 7 | 4 | 9 Gate (shows up as permanently `unaccounted`) | 4 Identity/binding (the two-registry or two-population mismatch that causes it lives here) |
| C4 | Missed sibling — hand-maintained list or duplicated rule, one member forgotten | 30 | Bastrop, Travis, Hays, McLennan, Elgin, cross-county | 14 | 14 | 9 Gate / 10 Publish (the walk or a scheduler runs and the forgotten member shows up) | 2 Pre-bake audit if the required-list is generated from a single declared source, or a standing CI divergence test independent of any one stage |
| C5 | A ruling filed and never implemented, down to a type that cannot express it | 14 | Bastrop, Travis, Hays, cross-county, 15 CAD counties | 8 | 3 | Wherever the ruling's consumer is finally read (gate, serve, a later ruling that depends on it) | Before stage 0 even starts: at the ruling's own filing, a machine check that a required state exists in the type it must be expressed in (see the falsifier-3 design in the smooth path) |
| C6 | Sub-metre digitisation mismatch between independent source layers | 2 | Bastrop, Bell | 0 | 0 | 9 Gate | 4 Identity/binding (a tolerant/buffered spatial join instead of strict containment when combining two independently-digitised layers) |
| C7 | A value served with a label asserted by a constant, consumed by a check that cannot fail on it | 40 | Bastrop, Caldwell, Hays, McLennan, Travis, Williamson, Austin, Kyle, Taylor, Dallas, Tarrant, Pflugerville, and most PACS/Orion/TAD source-parse paths generally | 27 | 9 | 10 Publish / 11 Probe (live customer-surface reads found the large majority of these) | 6 Rail-fill / 7 Atoms — where the label is actually assigned, with a meaning-shaped test comparing the served label against the value's real lineage field |
| C8 | False earned states | 22 | Bastrop, Caldwell, Hays, McLennan, Tarrant, Williamson, Wood, Henderson, Liberty, Austin, San Antonio, Killeen | 17 | 2 | 9 Gate (the gate passes because the false state reads as earned) | 6 Rail-fill, at write time, with a "probe actually ran" flag distinct from "state written" |
| C9 | Controls that cannot fail by construction | 82 | Bastrop, Travis, Hays, Caldwell, McLennan, Williamson, Bell, Bexar, Brazoria, Harris, Collin, Dallas, Denton, Tarrant, and cross-repo fleet tooling generally | 41 | 33 | Almost always found by **reading the write path**, not by any pipeline stage running | Before any pipeline stage: at the check's own code review/merge, a standing rule that every gate/check PR ships with a violation-fixture test in the same PR |
| C10 | Stale copies overriding current data | 17 | Bastrop, Travis, Caldwell, McLennan, Hays, Williamson, Dallas | 8 | 4 | 10 Publish / 11 Probe (a customer or overseer notices the served value predates a ledger fix) | 10 Publish itself — OPS-24's own predicate for this stage already requires "record-path payloads carry the cells' own vintage, not the old snapshot's" |
| C11 | Identity collisions across two or more namespaces | 20 | Hays, Bastrop, Williamson, Tarrant, Elgin, Smithville, Travis | 9 | 6 | 11 Probe / serve (a live parcel read exposes the wrong data) | 4 Identity/binding — this is the stage's entire purpose; every instance here is a stage-4 defect discovered downstream |
| C12 | Merged-but-not-deployed / deploy-and-traffic traps | 14 | Hays, cross-service, cross-repo (including a full LDT deploy-workflow that ships nothing on push while showing green) | 4 | 3 | 10 Publish / 11 Probe (git state says fixed, the customer surface says otherwise) | 10 Publish — a deployed-digest-vs-git-SHA field read before any close may say "deployed," or 12 Merge-back (manifest equals upstream, checked at every deploy not only at farm merge) |
| C13 | Serial discovery — weak instrument paces discovery instead of measuring the full population | 2 | Bastrop, all six | 0 | 2 | Named directly by the mechanism itself: discovery paced by a sampled walk | 8 Completeness — a full-population census run once, generalized as a standing instrument, replaces the sampled walk as the primary discovery tool |
| C14 | Stranded or unreviewed artifacts | 15 | Bastrop, Travis, Henderson, Liberty, Wood, cross-repo | 4 | 0 | Found by chance or by a dedicated audit, not by any routine stage | 12 Merge-back / any close review — "cited and untracked is the worst state" (ENFORCEMENT); a close citing an artifact that isn't in the tracked tree should refuse |
| C15 | Duplicate row/amendment ids | 3 | Bastrop, program governance | 2 | 1 | Found by direct grep of the record, not by any pipeline stage | Commit time — a hook already exists for plan-row (P-xxx) collisions; extending its id-space coverage to every hand-typed id prefix (A-xxx, F-xxx) catches it at the same point |
| C16 | Governance instrument gaming | 2 | Bell | 2 | 0 | Found only by a dedicated adversarial review re-deriving a "cleared" gate against its own cited PR | 2 Pre-bake audit — an instrument's formula changes are themselves version-controlled and require the same review a code change gets, so a retune-to-pass shows as a diff someone has to approve |
| C17 | Unleased/uncoordinated shared-resource contention | 6 | Bastrop, Caldwell, McLennan, Hays, cross-repo (Cloud Run jobs/stores/dispatches) | 1 | 2 | Found live, mid-incident, or by a dedicated audit after | 1 Manifest / farm provisioning — the lease system is extended to cover job runners and shared stores/checkouts, not only Cloud Run services, before any farm's concurrent stages can collide |
| C18 | Hand-declared coverage/registries never kept in sync with reality | 24 | Burnet, Bastrop, Travis, Caldwell, Williamson, Hays, CAPCOG cities, 239 of 254 counties, 89 of 254 counties (a second, independent hand-tracked corpus) | 13 | 5 | Found by falsifier checks or live probes against the registry's own claimed status | 0 Recon — every hand-declared registry carries a scheduled divergence test against its own live source from the day it is created, not added later as an afterthought |
## The twenty NEW single/low-instance candidates

Each carries exactly the instance(s) its own register entry names, no more — per the mission's rule, a candidate joins the register only with instances, and none of these twenty has enough instances yet to justify promoting it to a fully-tracked class of its own the way C8 through C18 were promoted. Twenty-two total instances across them; one (unverified-redrawn-asset) and one (marketed-ahead-of-build) carry 2 instances each, the rest carry 1. Full detail for every one is in the register; summarized here for the table's sake:

- **marketed-ahead-of-build** (2) — a product surface documented/marketed before its generator exists. Not a pipeline-stage defect; a product-launch checklist item.
- **agent memory loss re-derives a known-weak solution** (1, Bastrop) — caught by code diagnosis; controlled by this very mission's own fleet-memory (M0) DEAD-END practice.
- **sample-construction artifact misread as a code bug** (1, Comal/Bell) — a deterministic sample hit an edge case and was misdiagnosed as an engine defect; catchable at stage 2 (pre-bake audit) by reviewing sample construction before it drives a diagnosis.
- **scraper double-count** (1) — two independent traversal bugs double-counted one source; catchable at stage 3 with a per-source count check against the publisher's own total.
- **halted-treated-as-landed on resume** (1) — a resume script's skip-set conflated "halted" with "completed," the same shape as C3/C11 applied to resume logic specifically.
- **algorithmic complexity mismatched to data scale** (1, Harris) — a nested-loop algorithm timed out repeatedly at statewide cardinality with zero output; catchable at stage 2 with a data-scale check against the algorithm's known complexity before the largest counties are attempted.
- **encoding mismatch between tool and consumer** (1) — a BOM broke a downstream parser; a tooling-hygiene fix, not a pipeline-stage catch.
- **stray deploy-config in a non-deployable repo** (1, doc_repo) — a near-miss, caught by governance audit; a repo-setup checklist item.
- **writer-lease expiry during an unheartbeated plan phase** (1, Harris) — a lease expired mid-plan with no heartbeat to catch it; the fix (a longer TTL) landed without adding the missing heartbeat, so the class itself is not fully controlled.
- **silent truncation with no warning to the caller** (1) — a hardcoded section cap silently dropped content from a served document; found only by decoding the live output's actual bytes, not by reading code.
- **stale client state after a server rollback masquerading as a server regression** (1) — an operator's own un-reloaded browser tab produced every symptom of a server regression while every server-side instrument read healthy; cost one unnecessary rollback and one unrelated fix lost as collateral.
- **PII leak in a free-text field on a live customer surface** (1, Bastrop) — real citizen PII rendered live on a government dev-services surface; no generalized free-text PII filter exists.
- **a config figure copied across products without re-derivation** (1, Bastrop) — a timeout figure measured for one feature was silently assumed to apply to another.
- **a greedy-regex identifier truncation propagates through downstream artifacts** (1, Bastrop) — a regex silently dropped a trailing character from an env-var name, and the wrong name propagated into four further artifacts before being caught.
- **PII rendered in a design artifact, contradicting the artifact's own stated rule** (1, Bastrop) — caught pre-build by an adversarial read; drove a new standing rule (no design ratified without one).
- **a design artifact asserts fabricated citations to a real regulatory source** (1) — a design canvas invented an ordinance section number and cited it six times; caught pre-ship by the same adversarial-read rule.
- **an automated tool over-matches a shared tree by path when it should scope by content ownership** (1) — a renumbering script rewrote 14 other seats' files; recovered byte-exact, named directly in this mission's own dispatch as a standing warning.
- **a candidate list is built after the gate that filters it, instead of before** (1) — an order-of-operations defect in the setback-candidate resolver; carded, unfixed.
- **curved-frontage front-edge offset miscomputed** (1) — an envelope-geometry defect on non-straight street frontages; carded, unfixed, cause not yet isolated.
- **an unverified redrawn asset shipped as if canonical** (2) — a hand-redrawn brand asset was trusted with no comparison step against the file it was meant to reproduce.
- **premature-scale rollout risk, caught by staged gating** (1) — a 10-county dry run found 4 of 10 (25%) failing on first execution, before a 25x-larger rollout ran unchecked.
- **a control's own restriction misread as an uncovered gap in a coverage report** (1) — a CI gate's own forbidding rule accounted for 111 of 139 rows a report presented as real data gaps.
- Nine further single-instance candidates (unbounded-memory writes, nondeterministic id minting, a secret compiled into a public bundle, an authoritative-replace deploy flag silently dropping variables, an orphaned wrapper/consumer pair, a completed write with no execution record, a plan row misattributing its own defect, a reflex-vertex geometry overestimate, and others) are detailed in full in the register; each carries exactly the one instance its own entry names.

## Reading the table honestly

**C9 (controls that cannot fail) is the largest class by a wide margin — 82 instances, roughly half still unfixed, and it was found almost exclusively by reading write-path code, never by measuring output.** This is not a coincidence; it is ENFORCEMENT's own standing claim ("every real defect in this operation to date was found by reading a write path... none were found by measuring output") independently reproduced at scale, by three independent readers of three disjoint source sets, arriving at the same class as the largest one in the register each time. It is also the class where the fraction of instances carrying a recurrence control that can actually fail is consistently under half — meaning even where this program has built a fix, it is usually built as a narrow patch to the one instance found, not as a standing rule that would catch the next occurrence of the same shape.

**C7 (label asserted by a constant) is the second largest — 40 instances — and it is disproportionately a *source-parsing* class**: the large majority of its instances are in the LDT source-parser register (PACS, Orion, TAD, StratMap ingestion), not in the bake/serve layer most of the rest of this register concerns. This means C7 is the class most likely to recur unchanged the moment a county with an unfamiliar CAD vendor or file format is acquired — exactly Milam's situation (CAD REST unproven; a new source shape has never been exercised against these parsers at all).

**C4 (missed sibling) grew the most of any class on the second and third folds-in (22 to 30 instances)** as two independent readers of OPS-16's amendment log and the earlier 2026-07-20–2026-08-20 sessions kept finding the identical shape — a hand-maintained list, or a rule implemented twice, with one copy fixed and the sibling forgotten — across completely unrelated features (checkout flows, tier gates, wrapper/consumer pairs, reaper heuristics). Several of its instances were fixed by building an explicit divergence test between the two siblings rather than a one-off patch — this is the pattern the smooth path generalizes for classes 4 and 5.

**C13 (serial discovery) has only 2 instances but both are fully controlled** — not because the class is rare, but because it is the *mechanism* the third-party review names as explaining most of the other classes' slow, one-at-a-time discovery pace. Once a full-population census replaces a sampled walk (which the third-party review's own Stage B3 proposal calls for and which the 2026-09-16 program scope document's `six-county-completeness.mjs` instrument is a working first instance of), C4, C8, and C18 discovery all speed up as a side effect — this is why the smooth path treats "replace every sampled instrument with a full-population one" as a single cross-cutting recommendation rather than a per-class one.

**Falsifier 1 check:** the six CTX classes (C1–C6) and the seventh (C7) each carry at least the instance count their own source documents name — C1 carries 3 against the session's 2 named instances; C4 carries 30 against the session's 2 named instances, recurring independently at least 17 more times across OPS-16, OPS-19, OPS-21, OPS-23, and the LDT/engine assumption registers; C5 carries 15 against the session's 1 named instance; C7 carries 40 against the third-party review's own "two instances found in an hour." **Every one of the seven exceeds its source count on every fold-in, three independent times, which is the falsifier's expected direction — a miss would have produced fewer, not more.**

**Falsifier 2 check:** satisfied independently at least four times, detailed in the unfixed list and the report below — most sharply by OPS-21's own text ("ninety-four cutovers have shipped, nobody has verified that a single legacy path has actually been retired") and by the empty-county gate (C9, BLK-017/018), which the record calls "designed" as early as 2026-09-13 and which this lane confirms via the 2026-09-16 program scope document is **still the live, unfixed gate defect three days later** — the single most consequential "called fixed, still broken" finding in this register, because every other class's recurrence-control claims are downstream of this gate actually working.
# The smooth path — one check per class, at the earliest stage that can catch it

Stages are the OPS-24 thirteen (`90_operations/OPS-24_county_to_serving_program.md`) as refined by the farm-pipeline table in `_inbox/2026-09-16_farm_architecture_draft.md` section 5: 0 Recon/places, 1 Manifest, 2 Pre-bake audit, 3 Acquire, 4 Identity, 5 Instantiate, 6 Rail fill, 6b Depth, 7 Atoms, 8 Completeness, 9 Gate, 10 Publish, 11 Customer probe and meter, 12 Merge. Each check below answers ENFORCEMENT's own three-question gate plus its fourth (bypass) question, because a control that cannot answer all four is not a control (ENFORCEMENT.md, "The three question gate").

Every check names a real file, hook, or script where one exists today; where none exists, that is stated as the gap, not papered over.

## Stage 0 — Recon and places

**Check R0-1 (classes C18, C3).** Every hand-declared registry (`_catalog/texas_roster_v1.json`, `_catalog/tx_cad_source_registry.json`, `_catalog/tx_source_truth.json`, LDT's `zoning-layers.ts`, hauska-engine's `resolve-declared-cad-vintage.ts`) ships with a scheduled divergence test against its own live source, not bolted on after the fact.
- **What executes it:** a new script, `scripts/registry-divergence.mjs`, one per registry, following the pattern `scripts/tx-source-truth.mjs` already uses for the CAD-source registry.
- **What triggers it:** a cron (daily) plus every recon/acquire run for a county the registry claims to cover.
- **What fails:** the registry's claimed status (`verified-live`, `LAYER-FOUND`, `NOT-FOUND-UNKNOWN-WHY`) disagreeing with a fresh live probe of the same source. This is not hypothetical — BLK-060 already found 12 of 23 Burnet-class cities where the roster's own status field disagreed with its own verification fields on the SAME row.
- **What bypasses it:** any code path that reads the registry field directly without calling through the divergence-tested reader (today, this is every consumer — the reader itself does not exist yet, per BLK-060/BLK-175/BLK-186).

## Stage 2 — Pre-bake audit (P-188, this very row's own successor)

**Check P2-1 (class C16, governance gaming).** An audit instrument's own formula (a cost model, a threshold, a hard-kill) is version-controlled and any change to it requires the same review a code change gets.
- **What executes it:** a CI check on the audit-instrument repo path (wherever `tx-cad-source-inventory.mjs`-class scripts and their cost/threshold constants live) requiring a named reviewer sign-off, distinct from a normal merge.
- **What triggers it:** any diff touching a declared threshold or cost-model constant.
- **What fails:** a merge that changes a hard-kill's own formula with no accompanying change to the real-world condition it measures (BLK-070: Bell's cost gate was cleared by deleting the exact cost term the operation actually incurs).
- **What bypasses it:** a change routed through a different file that the same constant is re-exported from (the standard "signature default survives its own removal" bypass ENFORCEMENT names generally).

**Check P2-2 (class C5, falsifier-3 target).** Every `_decisions/` ruling that requires a new state, verdict member, or type value carries that requirement in a machine-readable form, and a CI job checks the requirement is actually expressible and actually implemented before the ruling can be marked closed.
- **What executes it:** a new script, `scripts/ruling-implementation-check.mjs`. Concretely: every decision file gets a frontmatter field `requires_implementation: [{type: "LayerAbsenceVerdict", member: "refused"}, {function: "verdictLayerServe.ts:convertUnmeasuredToRefused"}]`. The script (a) parses every such frontmatter block across `_decisions/`, (b) for each named type, statically checks (via `ts-morph` or a `tsc --listFiles`-driven AST walk) whether the named member exists in the type's declaration, and (c) for each named function, checks it exists and is reachable from at least one call site that is itself reachable from a serve route (not merely defined and uncalled — this closes the "exported detector nothing calls" bypass class 9 shares with class 5).
- **What triggers it:** any commit touching `_decisions/**`, and any commit touching a type file referenced by an existing decision's `requires_implementation` block (so a later refactor that silently drops a required member is caught, not only the original ruling).
- **What fails:** the exact 2026-09-01 instance this register found twice (BLK-005 and BLK-039's underlying type gap) — `LayerAbsenceVerdict` lacking a `refused` member the ruling required would fail this check the day the ruling was filed, not months later when CI happened to notice. Self-test fixtures (both directions, per ENFORCEMENT's "verify by violation"): a fixture decision naming a type member that does NOT exist must fail; a fixture decision naming one that does exist and IS reachable from a serve route must pass; a fixture naming a function that exists but has zero live call sites must fail (closing the dormant/starved variant).
- **What bypasses it:** a decision filed with no `requires_implementation` frontmatter at all. This is a real, named residual gap — the check cannot force every future decision-writer to use the field. The mitigation is a second, cheap check: any decision file whose prose contains the words "must", "required", or "is not implemented" and carries no `requires_implementation` block gets flagged for human review at the next OPS-16 amendment pass, never silently skipped.

This is not "the planner remembers" (falsifier 3's own bar): it is a script with a defined trigger, a defined failure condition, self-test fixtures in both directions, and a named residual bypass — exactly the shape ENFORCEMENT's own four-question gate asks for.

**Check P2-3 (class C4/C9, the empty-county gate itself, BLK-017/018).** `evaluateRailGate` and `evaluatePopulation` are tested against a literal empty-county fixture before P-188 may pass GREEN on any real county.
- **What executes it:** the existing proposed fix from `_inbox/2026-09-13_dead_controls_ranked_fixes.md` entries 1 and 2 (give the gate the program-wide declared-ahead set explicitly; make `EMPTY_DENOMINATOR` an unmeasured verdict, never a pass), landed and unit-tested.
- **What triggers it:** every pre-bake audit run, and a standing CI test in `hauska-factory`.
- **What fails:** a completely empty county (Burnet before stage 3 runs) reading a passing gate verdict. This is the single most load-bearing check in the whole smooth path — it is P-195, and OPS-24 itself already names it "sequenced first, ahead of everything," but as of 2026-09-16 it is still unbuilt (confirmed three separate times: 2026-09-13 designed, 2026-09-14 independently re-traced as not landed, 2026-09-16 program scope document still lists it as the live defect a farm run today would inherit).
- **What bypasses it:** any writer that calls the atoms/cell-write path directly without going through `evaluateRailGate`/`evaluatePopulation` first — not currently enumerated anywhere in this register; a call-site audit is owed.

## Stage 3 — Acquire

**Check A3-1 (class C1, punctuation-only input).** Every acquired source field that will become a display or join key (situs address, owner name, legal description) is checked against a sentinel/punctuation-only pattern before a single row is written, and a row failing the check is written as `unaccounted` with the raw value preserved for audit, never silently skipped.
- **What executes it:** a shared validator function called from every county-level acquire/instantiate writer (today, LDT's `p78Merge.ts` and the conformant bake CLIs each do this differently or not at all — this is itself an instance of class 4, a rule that needs one implementation, not several).
- **What triggers it:** every acquire run, every county.
- **What fails:** a fixture row whose situs is exactly `", ,"` or a bare house number with no street (the two live patterns already found, BLK-001 and BLK-169) must be flagged, never silently pass through as populated.
- **What bypasses it:** any writer that reads the raw source table directly instead of through the shared validator — the same bypass class 4 always has, and the reason this check and check A3-2 below (a row-count reconciliation) are paired rather than relying on either alone.

**Check A3-2 (class C3, C11, the Burnet-specific finding).** A row-count reconciliation runs immediately after every geometry/CAD-roll acquire step and refuses to proceed to identity (stage 4) on an unexplained mismatch above a declared tolerance.
- **What executes it:** a generalization of the completeness check (P-194) run early, not only at stage 8 — the same instrument, called twice.
- **What triggers it:** every acquire completion.
- **What fails:** Burnet's own live number today — 59,785 parcels on production against 50,138 geometry-ingest features, a mismatch the program's own scope document says "nobody reconciled" as of 2026-09-16. Under this check, Burnet could not proceed past stage 3 until that gap is explained (a multi-feature-per-account pattern, a truncation, or a genuine roll/geometry vintage mismatch — all three are live hypotheses this register found elsewhere in other counties, e.g. BLK-036 Travis, BLK-249 Harris).
- **What bypasses it:** a manual `--skip-reconciliation` flag, if one is ever added for operator convenience — named here explicitly so it is never added silently.

## Stage 4 — Identity

**Check I4-1 (class C6, sub-metre digitisation mismatch).** Any spatial join between two independently-digitised polygon layers for the same county uses a declared-tolerance buffered predicate, never a strict containment/intersects test, with the tolerance and its justification recorded on the join.
- **What executes it:** a shared spatial-join helper (today, at least the school-district/county-boundary join in the CTX bake and the zoning-stamp vertex-sweep in LDT independently reimplement this).
- **What triggers it:** any join combining two layers with different `source_vintage`/digitisation lineage for the same county.
- **What fails:** the exact live fixture already on file, Bastrop parcel `11585` (25.5m from its own county's ring, 0.79m from the correct district) — a strict-containment version of this check must fail on that fixture, and the buffered version must pass it, before the check ships.
- **What bypasses it:** any ad hoc `ST_Intersects`/`ST_Contains` call written directly in a bake CLI instead of through the shared helper.

**Check I4-2 (class C11, identity collisions — the largest class by unfixed count after C9/C7).** Every join across two identifier namespaces (TxGIO prop_id vs CAD account id; two tax-year rolls for the same prop_id; a bare number shared across counties) is proven one-to-one before being trusted, generalizing the H1 instrument (`hays-identity-reconciliation.mjs`) that already exists and is already county-generic per the OPS-24 teardown's own finding.
- **What executes it:** H1, generalized and run for every county at stage 4, not only Hays and Williamson.
- **What triggers it:** every county's identity stage.
- **What fails:** a join whose collapse-rate is nonzero — the fixture is Williamson's own 282,569-phantom-node measurement (BLK-068), already known, never re-run against Burnet, Bell, or Milam.
- **What bypasses it:** a downstream writer joining on the raw column directly instead of through the crosswalk H1 produces — this is exactly the mechanism of the Hays Sturgeon/Mesa Verde collision (BLK-067), where five real accounts were joined on a bare number that H1-class reconciliation exists specifically to catch, but the served route did not consume its output.

## Stage 6 — Rail fill

**Check RF6-1 (class C8, false earned states — the second-largest class by unfixed count).** A write may only emit an *earned* absence state (`absent-verified`, `not-applicable`) when the write path can name what it queried and what it found nothing of; a write with no query performed (no corpus table, no matcher run, no geometry to check) must emit `unaccounted`, never an earned state.
- **What executes it:** a required `queriedSource` field on every earned-absence write, checked by a linter/CI rule that rejects a write-path diff introducing an earned-absence branch with no corresponding query call in the same branch.
- **What triggers it:** every PR touching a rail writer.
- **What fails:** the exact three failure modes dead-controls already names (no corpus table for a city, a district matcher miss, absent geometry) writing `absent-verified` today — this check, run against the 2026-09-16 measured population, would have caught all 219,472 of the false setback absences (BLK-024) before they ever reached a gate.
- **What bypasses it:** a write path that fabricates a `queriedSource` value without actually calling the query (a "the field exists but is presence-shaped, not meaning-shaped" bypass) — closed by requiring the linter to trace the field to an actual function call, not merely a string literal.

**Check RF6-2 (class C9, lease-less writers — a live, currently-unfixed instance directly threatening Burnet).** Every writer calling a batch-atom-write function is unit-tested to assert it holds a lease at the moment of the call, with a fixture that calls the function without a lease and asserts the call throws.
- **What executes it:** OPS-21's own proposed S3 non-vacuity guard, already named, not yet built as of 2026-09-16.
- **What triggers it:** CI, on every commit to any of the six lease-less writers (owner, land-use, flood-hazard, rail-corridor, RRC-pipeline, special-district).
- **What fails:** exactly the current state of all six writers (BLK-019/104) — they would fail this test today, which is precisely the point; the check should be red now and turn green only when the fix lands, not the reverse.
- **What bypasses it:** a writer that acquires *a* lease but not the *correct, scoped* one (county+entity-type) — the fixture set needs a second case asserting the lease's scope matches the write's scope, not merely that some lease object is non-null.

## Stage 8 — Completeness

**Check C8-1 (class C13, serial discovery — replaces the sampled walk as the primary discovery tool).** A full-population census, not a sample, grades every required leaf/rail against the bake's own declared manifest for every row in the store, generalizing the third-party review's Stage B3 proposal and the 2026-09-16 `six-county-completeness.mjs` instrument (already built and self-tested, 11/11 checks, per that document).
- **What executes it:** `six-county-completeness.mjs`, generalized to take any county (not hardcoded to six) — named explicitly as owed work in the 2026-09-16 program scope document's own probe-leg list.
- **What triggers it:** every completeness stage, and on a schedule against production.
- **What fails:** any required leaf null/sentinel/mismatched-tier on ANY row, not merely the sampled 180; the census must be shown to fail where the old sampled walk passed (the falsifier the third-party review itself names: "if it passes a store the walk fails, it is wrong").
- **What bypasses it:** a rail added to the bake's manifest but never added to the census's required-leaf list — the same class-4 shape one level up; closed by deriving the census's required list FROM the bake's own manifest constant at the pinned SHA, never from a hand-copied mirror (this is the exact fix the third-party review specifies for its own proposed instrument).

## Stage 9 — Gate

**Check G9-1 (class C2, ceiling measured by one instrument enforced by another — DEV_PROCESS's own paired-control rule).** Every pair of functions implementing what is meant to be one rule (the factory `.js` and engine `.ts` copies of `evaluateRailGate`; the two setback-table registries; the two edge labellers) carries an explicit divergence test, not two careful hand-edits.
- **What executes it:** a divergence-test harness that runs both implementations against the same fixture set and asserts identical output.
- **What triggers it:** CI, on any commit to either half of a named pair.
- **What fails:** the two `evaluateRailGate` copies (dead-controls entry 1's own open question — "settle which [is authoritative] before editing, and if they are independent add the divergence test") returning different verdicts on the same input.
- **What bypasses it:** a third, undiscovered copy of the same rule — the only defense against this is the periodic full-repo grep for duplicate-logic patterns this register itself had to do by hand to find these pairs; no automated version of that grep exists yet.

## Stage 10 — Publish

**Check P10-1 (class C10, stale copies overriding current data — the class with the single largest measured population, 490,185 atoms).** OPS-24's own stage-10 predicate — "record-path payloads carry the cells' own vintage, not the old snapshot's" — is implemented as a field-level assertion at serve time: any served value whose backing atom's `bakedAt`/`snapshotAt` predates the ledger cell's own `updatedAt` by more than a declared window fails closed rather than serving the stale value silently.
- **What executes it:** a serve-layer assertion in the retrieval-api's one ruled reader (per the 2026-09-11 ledger-as-serving-path ruling), checked against every cell-atom pointer pair.
- **What triggers it:** every serve request, or a batch audit run per publish.
- **What fails:** the exact F25 pattern (BLK-007/BLK-266) — a value right, a label from an old bake — and the 490,185-atom envelope population (BLK-264), which is the largest single instance of this class found anywhere in this register.
- **What bypasses it:** any surface still reading the tier-1 bake (`place_layer_snapshots`) directly instead of through the ruled reader — named explicitly as a live, unretired parallel read path by the OPS-21 "94 cutovers, none verified retired" finding (BLK-039).

**Check P10-2 (class C12, merged-but-not-deployed).** No close may state a code fix as "deployed" or "live" without a field-name read of the serving revision's digest, matched against the merge SHA, attached to the close.
- **What executes it:** `probe-close-gate.mjs`'s pattern, generalized: the existing gate already refuses a close with no cited probe artifact for certain row ranges; extend the same mechanism to require a digest-match artifact for any close claiming "deployed."
- **What triggers it:** any close.
- **What fails:** exactly the wave-6 pin-merge-vs-pin-deploy incident (BLK-074) and the cortex-api second-shift near-miss (BLK-079) — both were caught by hand in this program; this check makes the catch structural.
- **What bypasses it:** a close that avoids the word "deployed" and says only "merged" — closed by requiring the SAME check on any close whose leave_behind or missionPremise implies the fix reached a customer, not only on the literal string "deployed."

## Stage 11 — Customer probe and meter

**Check PR11-1 (class C4/C9, the OPS-24 probe's own zero-predicate gap, BLK-061).** `probe-close-gate.mjs`'s gated-row regex is generated from the current OPS-16/OPS-24 row registry, never a hand-typed range, so a newly-allocated row range is covered automatically.
- **What executes it:** the gate's regex construction reads `_catalog/plan_registry.json` (already built per BLK-212) at run time instead of embedding a literal range in source.
- **What triggers it:** every close, for every row.
- **What fails:** the exact gap this register found live — OPS-24's rows (P-186..P-198) were unmatched by the regex for at least two days after the program was created.
- **What bypasses it:** a plan row allocated outside the registry entirely — closed by the existing row-allocation-is-a-claim hook (BLK-212's own fix, d7c1a0f3), provided its id-space coverage is extended per check S(none)-1 below.

## Cross-cutting (no single stage; a fleet-wide gap regardless)

**Check X-1 (class C15, duplicate ids).** The existing plan-row (P-xxx) double-definition hook is extended to cover every hand-typed id prefix used anywhere in `90_operations/OPS-16_texas_market_plan_of_record.md` and its siblings — amendment ids (A-xxx), finding ids (F-xxx), OPS-23-wave-style ids — not only plan rows.
- **What executes it:** the same collision-check logic as the existing hook, parameterized over id prefix instead of hardcoded to `P-`.
- **What triggers it:** any commit to `90_operations/**` or `_decisions/**`.
- **What fails:** the live case this lane found today and did not fix (out of scope for a read-only lane) — three duplicate amendment-id pairs (A-136, A-145, A-146) currently live in OPS-16's own amendment log (BLK-072).
- **What bypasses it:** a hand-edit to the file outside the normal amendment-append flow (a merge conflict resolution, a direct edit) — the same bypass class 15 always has; mitigated only by running the check on every commit, not only on the amendment-append tool's own invocation.

**Check X-2 (class C17, unleased contention).** The `_catalog/leases/` mechanism is extended to cover Cloud Run job-runner executions and shared-store write windows, not only Cloud Run traffic-shift targets.
- **What executes it:** a generalization of the existing traffic-lease-gate hook to a second resource type.
- **What triggers it:** any `gcloud run jobs execute` against a shared project, and any heavy-scan/bulk-write window.
- **What fails:** the wave-6 finding that a scheduled job wrote the exact gate-verdict table a walk was grading, inside the walk's own measurement window (BLK-078), and the 28-million-contaminated-atom orphaned-writer incident before the atoms lease existed (BLK-260).
- **What bypasses it:** a job invoked from outside a doc_repo-rooted session, the same named bypass the existing traffic lease already has.

## What this buys, concretely, for Burnet, Bell, and Milam

If P2-3 (the empty-county gate fix) and RF6-2 (the lease-less-writer fix) alone land before Burnet's stage 3, Burnet cannot silently "pass" empty and cannot silently land without owner/land-use/flood data — the two single most consequential live gaps this register found for the very next county. A3-2 (the row-count reconciliation) would have forced Burnet's 59,785-vs-50,138 mismatch to be explained before stage 4, rather than sitting unreconciled as of this report's date. I4-2 (identity collision proof) run against Bell and Milam before merge would catch a Williamson-282,569-phantom-node-class defect before it ships, not after. None of these six checks requires new invention — every one generalizes a fix, instrument, or proposal this register found already named, already partially designed, and in several cases already built and merely unwired.
# The unfixed list — every unfixed instance that would hit Burnet, Bell, or Milam

Every item below is `fix_status: unfixed` (or `ruled/unbuilt`, `fixed-undeployed`, `designed-not-landed` — read as unfixed at the customer-facing level, which is the level that matters) in the register, with the specific mechanism by which it lands on one of the next three counties. Register ids (`BLK-xxx`) are cited so each claim can be traced back to its source and evidence.

## Applies to all three counties, before any of them individually

These are pipeline-stage defects with no county literal in them — they would hit Burnet, Bell, and Milam identically, and in most cases would hit them FIRST, before any of the six control counties, because none of the three has ever been run through the pipeline even once.

- **The empty-county gate (BLK-017, BLK-018).** `evaluateRailGate` and `evaluatePopulation` both pass a completely empty county cleanly. Burnet's `parcel_record` reads 0 rows today; Bell's and Milam's are further behind still. Run any of the three through the current gate before stage 3 completes and it reads a clean pass — not because the county is done, but because the gate cannot tell "not yet filled" from "declared ahead." Designed 2026-09-13, confirmed not landed 2026-09-14, confirmed still the live defect 2026-09-16.
- **The six lease-less writers (BLK-019).** Owner, land-use, and flood-hazard (plus rail-corridor, RRC-pipeline, special-district) throw `LeaseRequiredError` on first write under the current lease code. Every one of the three counties would land missing these rails on day one unless this lands first. Directly named in the 2026-09-14 farm handoff as "Burnet without this fix lands missing three rails a customer asks about" — the same sentence applies unchanged to Bell and Milam.
- **False `absent-verified` on no-table/no-matcher/no-geometry (BLK-024).** The exact write path that produced 219,472 false setback absences across the six control counties has no county-specific logic — it fires identically wherever a city has no corpus table, which for Burnet is six of seven places (everywhere but Marble Falls), for Bell includes at minimum Temple (see below), and for Milam is presumptively all five towns, none of which has ever been probed.
- **The agValuation not-applicable sweep (BLK-267).** A dormant sweep exists that would write a false `not-applicable` across every county with no ag-valuation source wired — which today is every county except Williamson and Travis, meaning Burnet, Bell, and Milam all inherit this risk the moment anyone runs it, unless it is deleted first as the 2026-09-16 scope document's own L4 item requires.
- **The identity crosswalk gap (BLK-011, BLK-063, BLK-068, BLK-067).** The two-namespace identity rule (H1, generalized) has been run for exactly two of the six control counties (Hays, and Williamson is carded but not dispatched). Zero of the three next counties have been checked. Williamson's own number — 282,569 phantom nodes — is the standing warning for what an unchecked two-namespace county looks like at scale.
- **No retract-by-run-id (BLK-064).** If any of the three counties' first production publish writes something wrong, there is no mechanism anywhere in any of the four product repos to take it back — only a re-bake or a manual quarantine.
- **The stale-envelope-atom class at scale (BLK-264).** 490,185 atoms across the six control counties say "no buildable area" when the real cause is frequently "never re-derived after the ledger changed." A brand-new county's own first bake would mint fresh atoms rather than inheriting this specific stale population, but the SAME writer defect that produced it (a breadth-bake that writes a zero without a verified reason) is still live code and would produce the same class of false zero for Burnet's, Bell's, and Milam's own first envelope pass unless R4 (retire the mislabelled outcomes, writers can no longer emit `no-buildable-area` without a computed, verified zero) lands first.
- **Serial discovery via a sampled walk (BLK-087, BLK-013).** Unless the full-population census (checked in as `six-county-completeness.mjs`, generalized per the smooth path's C8-1) is extended to take an arbitrary county before Burnet runs, Burnet's own defects will be found one at a time by a 180-parcel sample, at the exact slow pace this register's own seventh-class finding names as the mechanism behind the whole program's history of surprises.

## Burnet (48053)

- **59,785 parcels on production against 50,138 ingest features (BLK-100-series context; measured directly in the 2026-09-16 scope document).** The two counts disagree and, as of 2026-09-16, nobody has reconciled them. This is not a hypothetical risk for Burnet — it is Burnet's own live, current, unexplained number. Three live hypotheses from elsewhere in this register are directly relevant: a multi-feature-per-account pattern (the Travis divergent-join class, BLK-036), a source truncation of the Harris shape (BLK-249, where a truncated input passed every existing gate including an independent SQL count because all four instruments read the same truncated table), or a genuine roll-vs-geometry vintage mismatch (the Caldwell-class CAD-cohort-vintage-drift, BLK-225). **The completeness check (P-194) exists for exactly this question and has never been run against Burnet.**
- **0 address points (confirmed multiply, most recently 2026-09-16).** Directly threatens the program's own Law 1 finish line — a real Burnet address must resolve via `scripts/surface-probe.mjs` for the county to count as done, and there is no address-point data to resolve against yet. The fix is scoped as "one loader run from the statewide StratMap set the six counties already use" (not a new acquisition), but as of this report it has not run. The wave-6 register also separately flagged this exact gap as a warning for Burnet's loader design (BLK-083) before any fix was scoped.
- **Marble Falls has a verified-live zoning layer (261 features) but no setback table (2026-09-16).** Marble Falls is simultaneously Burnet's best-positioned place (the only one of seven with real zoning data) and still blocked on the MAKE side — a table has to be built from the ordinance, not merely acquired. This is a straightforward S3-class acquisition item (per the smooth path and the 2026-09-16 scope document's own S3 worklist), not a code defect, but it is squarely unfixed and it is the one place in Burnet that could otherwise draw a real envelope.
- **The roster's NOT-FOUND-UNKNOWN-WHY status is desynced from reality (BLK-060).** Every Burnet place, including Marble Falls, currently reads this status despite Marble Falls independently carrying `verified-live` and a positive feature count on the very same row. This was first found as a general defect (falsifier F5, 2026-09-13, 12 of 23 zoning-wired cities affected across the roster generally), re-confirmed specifically for Burnet on 2026-09-14, and confirmed unfixed again on 2026-09-16 (pickup list item C11: "not started"). **A future agent or operator reading only the status field for any Burnet place will draw the wrong conclusion**, exactly as the farm handoff document itself warns.
- **The Burnet geographic-adjacency rationale is imprecise (BLK-066).** A minor, non-blocking item: OPS-24's original stated reason for choosing Burnet ("borders the six and is in none of them") does not hold as literally worded — Burnet borders only 2 of the 6 control counties. Does not disqualify Burnet; listed here because it is technically still an uncorrected claim in the program's own planning text as read by this lane.

## Bell (48027)

- **Temple is zoned and published on municode, but no public zoning layer has ever been found (BLK-102).** First recorded as a source gap 2026-07-24; still open more than seven weeks later as of the sources this lane read. This is not a paging bug or an acquisition-code defect — genuinely no ArcGIS/GIS layer exists publicly for Temple's zoning. It is named directly in the 2026-09-16 program scope document as "the largest acquisition question in Phase 2" for Bell. The only path forward is new-source research (municode text extraction, city PDF zoning maps, or a declared-unacquirable decision per the 2026-09-16 decision's own city-acquisition standard: log every source tried — city GIS, municode, eCode360, American Legal, city PDF, county appraisal zoning fields — before declaring unacquirable).
- **A boundary divergence: 694 Bell parcels sit fully outside the Census-defined county line (BLK-101).** Forensically confirmed 2026-08-08: the appraisal district's own roll includes parcels up to 5.9 miles north of the Census boundary, ruled to be an upstream CAD-jurisdiction reality rather than a pipeline defect (all four alternate hypotheses — projection, ingest, coordinate corruption, and a boundary-source error — were explicitly measured and ruled out). Practical risk is bounded (all 580 usable overflow parcels already carry declined envelope atoms) but unaddressed: any future ring computation for those 694 parcels needs an explicit exclusion, and zoning facts for them are keyed under a county that may not be the customer-facing answer a Bell resident expects. **No reconciliation mechanism exists; the ruling accepted the divergence rather than fixing it.**
- **Geometry ingest has not run for Bell at all (per the 2026-09-16 pickup list).** Distinct from Burnet, which has ingest data to reconcile — Bell has none yet. Two internal sources also disagree about Bell's state (one card claims "165,574 parcels, certified 20/20 on 2026-08-05," a figure the pickup list flags explicitly as needing to be measured fresh before being trusted, not assumed) — itself an instance of the same "closed at the gate, unverified at source" pattern (class C16/C18) this register found repeatedly elsewhere (see the HAYS-CERT-CLOSED case in the class table's falsifier-2 discussion, where a similarly-worded "certified" claim was directly contradicted by a defect that bit a month later).
- **Killeen and Belton are the only wired candidates; nine smaller places are unprobed absences.** Not a defect in themselves (an honest `searched-and-absent` state, per the Thrall precedent this program has already ruled correct behavior), but it means Bell's real place-level coverage work is almost entirely undone, same as Burnet's.

## Milam (48331)

- **The CAD REST endpoint is unproven, not merely unreachable (BLK-069's classifier definition, applied directly).** `rest-unproven` is a specifically-defined third state, distinct from both `rest-reachable` (176 of 254 counties) and `rest-absent` (58 counties, no URL at all): a URL is on file but no query against it has ever succeeded. In plain terms: **nobody has ever gotten a real answer out of Milam's CAD source.** The pre-bake audit (P-188/P-2 in the smooth path) will very likely read RED on Milam for exactly this reason, per the 2026-09-16 scope document's own prediction ("the pre-bake will likely read RED on it first").
- **Geometry ingest passed (20,992 features) but this is the StratMap geometry pipeline, a separate track from the CAD roll/attribute pipeline that is unproven.** A county can have valid parcel shapes with no verified way to get tax-roll values, owner names, or land use onto them — which is precisely Milam's situation today, and precisely the shape of defect that produced the Wood/Henderson/Liberty near-miss elsewhere in this register (BLK-252), where a geometry-only view of a county nearly caused real records to be silently dropped because the attribute side wasn't checked with the same rigor.
- **Five towns, none probed.** Milam has the least place-level work of any of the three counties — not even a first zoning-layer search has been logged for any of its towns, as far as the sources this lane read show.
- **Milam and Bell are explicitly scheduled to run together, in parallel, through the same farm (2026-09-16 ruling), after Burnet alone.** This means every unfixed cross-cutting item above (the empty-county gate, the lease-less writers, the identity crosswalk gap) must be fixed once, not twice, before Phase 2 starts, or the two counties will independently rediscover the same classes at the same time — which is exactly the risk the 2026-09-16 decision names as the reversal criterion for running them in parallel at all ("if Burnet's run shows the upstream fix rate exceeds what the integration seat can absorb").

## What is explicitly NOT on this list, and why

Per the 2026-09-16 decision, `agValuation` is now Texas-wide by ruling — so Burnet/Bell/Milam not having ag-valuation coverage is not itself an unfixed-list item; it is expected pending L4's acquisition work, distinct from the DEFECT (the dormant sweep that would silently mislabel it as not-applicable, which IS listed above). Similarly, the six control counties' own remaining Phase-0 work (setback table reconciliation, road-node quality, the R0–R9 envelope-family items) is deliberately sequenced to finish BEFORE Burnet runs (the 2026-09-16 operator ruling: "complete the six onboarded counties first... before Burnet runs"), so those items are not re-listed here as Burnet-blocking; they are precondition work the operator has already scoped as gating Phase 1's start, tracked in the class table and the full register rather than duplicated here.
