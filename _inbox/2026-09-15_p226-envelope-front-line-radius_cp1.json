{
  "lane": "p226-envelope-front-line-radius",
  "planRows": ["P-226"],
  "checkpoint": "CP1 (design/pre-build)",
  "timestamp": "2026-09-15",
  "seat": "cente-p226",
  "type": "not applicable: solo lane, no sub-agents spawned (single-threaded diagnose-and-hand-back; AGENT_CONTRACT section 1 fan model not exercised, matching p221/p227 precedent)",
  "plan": [
    "Claim lane p226-envelope-front-line-radius before any file read/edit (done: node scripts/lane-claim.mjs claim against P:/doc_repo/scripts/lane-claim.mjs by absolute path, exit 0, seat cente-p226).",
    "Cut dedicated worktrees before touching anything: P:/seat-worktrees/p226-envelope-front-line-radius/doc_repo (branch lane/p226-envelope-front-line-radius, from origin/main 76345a4c) for CP/close artifacts, and P:/seat-worktrees/p226-envelope-front-line-radius/hauska-map (branch lane/p226-envelope-front-line-radius, from origin/main b360138) for the code being investigated. No hauska-engine or legacy-design-tools worktree created — this lane's mandate is read-only on any repo other than hauska-map, and git show/git --no-pager grep against origin/<repo>/main reads those repos without a worktree or any write.",
    "FIRST TASK per dispatch: establish draw-time vs write-time BEFORE editing envelope-overlay.ts. Two evidence tracks planned: (1) the dispatch's own discriminator — export the PDF site plan for 48453:289990 via mcp__claude_ai_Smart_Site__export_instrument(kind:siteplan) and inspect its front edge (PDF consumes verbatim server geometry, no stripRingSpikes); (2) full static trace of hauska-map's own code from the live-derive fetch call down to the paint call, to establish independently whether hauska-map's OWN code is even capable of relocating the front line.",
    "Pre-registered falsifier answers (dispatch's own four, answered BEFORE running anything beyond reading code / attempting the live export):",
    "  F1 (PDF shows same defect as map -> not the overlay, any hauska-map fix would mask): if the live PDF export is unreachable (timeout/outage), fall back to the static trace as the deciding evidence, and say so explicitly rather than asserting a live PDF result I did not obtain.",
    "  F2 (fix correct on named parcel but not a second curved-frontage parcel -> fitted to one ring): not applicable if the row closes as write-time/hand-back rather than a hauska-map fix -- no hauska-map fixture is being built this pass.",
    "  F3 (drawn envelope AREA changes as a side effect -> check against the facets route for the same parcel): not applicable for the same reason -- no geometry-affecting edit is being made in hauska-map.",
    "  F4 (front edge becomes correct only by moving the whole envelope inward uniformly -> uniform-inset approximation, not a real fix): directly relevant as a DO-NOT: insetParcelBySetbacks (the dead, uniform-inset fallback) will not be wired up regardless of what the investigation finds, per the dispatch's explicit instruction.",
    "Known trap to check before concluding anything: the envelope draw is entitlement-gated (ExplorerMap.tsx handleEnvelope: mayDraw = snap != null && isEntitled(snap); setEnvelopeOverlays([]) when false) -- confirm entitlement before treating 'nothing drew' as a geometry finding, if that comes up.",
    "Known trap: hauska-map does not auto-deploy and the local P:/hauska-map primary checkout is hundreds of commits behind on a feature branch -- this lane's own hauska-map worktree was cut fresh from origin/main b360138 specifically to avoid that trap; verification claims about 'the deployed map' still need the live smartsite.cloud surface or the MCP tools, never a local dev server against stale code."
  ],
  "falsifier": "Dispatch's four falsifiers pre-registered above, answered before running the investigation, not fitted after seeing results.",
  "leaveOpen": "Whether the live PDF export will succeed at all today -- not yet attempted at CP1 time; hauska-engine-api has a same-day known-outage precedent named in the dispatch's own 'Known traps' section (API healthy while map was blank), so an engine-side failure on the export call would not by itself be surprising or diagnostic of P-226."
}
