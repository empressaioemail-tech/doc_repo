{
  "lane": "p226-envelope-front-line-radius",
  "planRows": ["P-226"],
  "checkpoint": "CP2 (pilot/mid-work)",
  "timestamp": "2026-09-15",
  "seat": "cente-p226",
  "liveExportAttempt": "mcp__claude_ai_Smart_Site__export_instrument(parcelNodeId:'48453:289990', kind:'siteplan') called 4 times: every call timed out identically at exactly 50000ms hitting https://hauska-engine-api-h7gvu7rgcq-uc.a.run.app/v1/property-nodes/48453%3A289990/site-plan-export/refresh ('the engine may be cold-starting; retry in a moment'). A same-parcel export_instrument(kind:'dossier') call in between returned FAST with a real structured refusal (pipeline_output_absent, 422) rather than a timeout -- proving the MCP bridge and entitlement gate are both up and this account can reach the engine; the site-plan-export/refresh route specifically is not responding today. The dispatch's own PDF discriminator could not be run live. This is named as a leave_behind (a live infra issue independent of P-226), not treated as evidence either way about the front-edge defect.",
  "staticTraceInHauskaMap": "Read apps/property-explorer/src/browse/envelope-overlay.ts in full (375 lines) and apps/property-explorer/src/browse/ExplorerMap.tsx's handleEnvelope (~line 1640). Confirmed: normalizeEnvelope() reads result.geometry/result.geojson VERBATIM off the server response and does not compute an inset. envelopeInsetOverlay() passes that geometry through stripEnvelopeSpikes()/stripRingSpikes() before painting -- read stripRingSpikes' full implementation: it only ever SPLICES OUT a vertex, either an exact near-duplicate (<0.25m) or a >160deg reversal whose out/back mouth is <1.5m; it never moves, adds, or displaces a vertex, and a spike by construction encloses zero area. This is a set of deletions from an already-computed ring, not a geometry computation -- it is architecturally incapable of pulling a front edge out to (or past) the frontage. handleEnvelope's own comment at the empty-branch confirms the same design intent: 'No client uniform inset -- geometry must come from live derive (WDLL)'. insetParcelBySetbacks (the uniform-inset dead code the dispatch flags) is called from nowhere in this trace, confirmed by its own module comment and by grep (only its own test file references it) -- not touched, not wired up.",
  "traceOfTheLiveDeriveCall": "fetchBuildableEnvelope (src/lib/buildable-envelope.js) POSTs {cortexBase}/brokerage/v1/place/buildable-envelope -- resolved via api/spine.ts's proxy table to process.env.CORTEX_API_URL, default https://cortex-api-tds7av26va-uc.a.run.app. This is a DIFFERENT deployed service than hauska-engine-api (the one the site-plan PDF export calls) and a DIFFERENT repo than hauska-engine: grep across hauska-map's own tree plus a read-only git grep of legacy-design-tools' origin/main (git show/git grep only, no worktree, no write, per the dispatch's 'do not write to hauska-engine' and this lane's own read-only-elsewhere discipline) located the actual route and its implementation entirely inside legacy-design-tools: artifacts/api-server/src/routes/brokeragePlaceBuildableEnvelope.ts (route registration, POST /buildable-envelope) calling artifacts/api-server/src/lib/buildableEnvelope/composeBuildableEnvelopeDerivation.ts, which calls deriveBuildableEnvelope() (derive.ts) -> insetPerEdge()/insetProjected() (geometry.ts), with per-edge front/side/rear roles assigned by labelEdges()/insetFeetForLabeling() (edgeLabeling.ts). hauska-engine's own labelEdgesFromRoads (packages/engine-core/src/depth-warm/edgeLabeling.ts) is a SEPARATE, differently-named function used by hauska-engine's depth-warm/promote pipeline (the atom-chain ground-truth verification path P-216 investigated), not by this live-derive call. The dispatch's own premise ('the bad front edge is plausibly computed server-side, in hauska-engine') names the wrong repo for THIS call path -- same class of correction P-216's close made to its own dispatch's repo assignment.",
  "mapExportParityFinding": "composeBuildableEnvelopeDerivation.ts's own module comment states it was extracted specifically so a SECOND caller, parcelDrawEnvelopeModel.ts (the get_smart_site draw-block orchestration), 'can run the EXACT same composition ... that this route's map/export path runs, rather than a re-implementation' -- 'map/export parity' is named twice in that file as the deliberate design goal. Independently, geometry.ts already carries its OWN write-time reversal-spike stripper (stripReversalSpikes, lines 254-313) with the same 160deg/1.5m-mouth shape as hauska-map's client-side stripRingSpikes, called INSIDE insetProjected() before the ring reaches ANY caller -- meaning the wire geometry, the PDF export, and the map draw all already receive an identical, pre-spike-stripped ring; hauska-map's own client-side stripping is redundant defensive duplication of a control the server already applies, not the source of any residual defect.",
  "candidateRootCauseNamedNotFixed": "geometry.ts's insetPerEdge()/insetProjected(): buildForbiddenStrips() unions one independent axis-aligned rectangle per ring edge (setbackStrip: {a, b, offset(b), offset(a)}, offset along that edge's own inward normal only), then polygonClipping.difference()s the union from the parcel polygon. This has no mitred/rounded join at ring vertices. On a straight run of collinear edges this is exact; on a CONVEX vertex (every joint along a curving frontage digitized as several short chords is one, per edgeLabeling.ts's own module comment: '48453:280239: 7 segments with ~0.6deg joints') each pair of adjacent offset rectangles meets the shared vertex at two DIFFERENT offset points (bOff from edge i's own normal, bOff from edge i+1's own normal), not a single mitred point -- the plain rectangle union can leave a small uncovered wedge of buildable area right at that vertex, closer to the true property line than the front setback allows. edgeLabeling.ts's groupCollinearEdges/expandGroupLabelsToEdges (lines ~315-444) was written specifically (WDLL P-60b item 3, its own comment cites 48453:280239) to make ALL chords along one curved frontage share ONE 'front' label so the per-edge distance is uniform along the curve -- meaning the label itself is very likely correct (front_ft applied to every chord), and the mechanism above (rectangle-union under-coverage at each of several closely-spaced convex joints along the SAME curve) is the more likely source of a front line that reads as running along/into the frontage rather than cleanly offset: not one visible spike (stripReversalSpikes already removes those), but a shallow, repeated under-cut along the whole curve. This is READ, NOT RUN: no fixture, no live repro, no edit -- offered as the named file/function/mechanism the dispatch's own 'done looks like (b)' asks for, with the actual verification (a real ring for 48453:289990 run through insetProjected, or the blocked PDF check) left to whoever picks this up in legacy-design-tools.",
  "entitlementCheck": "Not reached / not applicable this pass: no live map click-through was performed (no browser session; verification was via MCP + static code read, per this session's known Smart Site MCP access and its own documented limits -- a browser-authenticated smartsite.cloud session is outside this session's available tools). Noting handleEnvelope's mayDraw = snap != null && isEntitled(snap) gate exists and was read, not exercised."
}
