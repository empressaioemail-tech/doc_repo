# W5 review: R-09 launch gate instrument repair

Snapshot of this review: `P:/doc_repo` on `main` at `d254467788c795c6f8fa5a9256ad6a074859b615`. Seat integration (planner). Property seat owns LDT. This worker did not enter `P:/legacy-design-tools` or `P:/seat-worktrees/property/legacy-design-tools`. No merge, no deploy, no store write.

Sources: `_scratch/r09_gate_repair.md`, `_inbox/2026-08-21_r09-gate-repair_{cp1,cp2,close}.json`, `gh pr view 447` / `gh pr checks 447` / `gh pr view 447 --json state,mergeable,statusCheckRollup,commits,title,body` against `empressaioemail-tech/legacy-design-tools`. OPS-18 R-09 fence and `_decisions/2026-08-09_texas_flush_launch_gate.md` read for criterion shape only.

**Recommendation to planner: LEAVE ALONE.**

---

## 1. Is R-09 still running in the property LDT worktree?

R-09 has not closed. Treat it as live. Occupancy of the property LDT worktree is **UNCERTAIN**. This worker did not `git status` that tree.

Close artifact `_inbox/2026-08-21_r09-gate-repair_close.json` status is `PR_OPEN_AWAITING_CI`, not merged. It names commit `6fea02c5c80c7928892b3d9bde7da8299bc4d870`, PR https://github.com/empressaioemail-tech/legacy-design-tools/pull/447, branch `seat/property`.

GitHub at review time: `state` `OPEN`, `mergeable` `MERGEABLE`. PR HEAD is not the close-cited commit. After `6fea02c5` the PR carries two further commits:

- `0ae7de53b40f44f07a2225462df1cf543f0e02ab` (2026-08-21T01:26:45Z) `fix(manifest): strict merge store vs derived; update ledger tests for…`
- `164378da2b09c68e8c1797e7b796538aae39dd4f` (2026-08-21T01:29:59Z) `fix(manifest): import mergeHasWriter in manifestGridRead`

Those pushes after the close filing are evidence the property seat kept writing past `PR_OPEN_AWAITING_CI`. Last observed PR commit is `164378da` at 01:29:59Z. Test check completed 2026-08-21T01:39:58Z. This review is hours later with no further PR commits, which looks idle, not proven empty. A second occupant of that worktree is a collision SEAT-01 will not catch. Leave the tree alone.

OPS-18 still lists R-09 as OPEN. The row is not closed until a live firing grades, which this PR has not produced.

## 2. Against the mission: FIRING with live payload and cell id, or only a local Cloud Run probe simulation?

Only a local Cloud Run probe simulation against deployment Postgres. Not a live probe on the deployed surface. Mission not met. Code-done is not customer-done.

Scratch GROUND-TRUTH 2026-08-21T01:12:50Z: live materialized GET `county-ledger` still all 3556 cells `hasWriter=true`, `atomFamilyState=present`, `isPartial=false`, `computedAt=2026-08-14T17:41:22.500Z`.

Scratch GROUND-TRUTH 2026-08-21T01:15:22Z names the proof instrument as "proof script, Cloud Run probe", not HTTP GET:

- `hasWriter` false: 508 cells, sample `48001:easement`
- `atomFamilyState` not present: 3048 cells partial, sample `48001:geometry` displayState `no-atom`
- `isPartial` true: 18 cells, sample `48027:zoning` (coverage 0 vs threshold 95); legacy path had 0

Close and CP1 quote the same cells and counts, and they label the injection:

- hasWriter proof: `"injected": "cloudRunManifestReadProbeOptions on deployment DB"` / `"probe": "cloudRunManifestReadProbeOptions (requireEngineRoot true, absent engine tree)"`, cell `48001:easement`, value false, count 508
- atomFamilyState: cell `48001:geometry`, value `partial`, displayState `no-atom`, count 3048
- isPartial: `"injected": "none — store already held 18 partial rows"`, cell `48027:zoning`, value true, legacyCount 0, repairedCount 18

Close `launchCriteriaCapableOfFailing` says DC4 and DC5 are `"partly after deploy+recompute"` and `"Live GET still 2026-08-14 snapshot until POST recompute (out of R-09 fence)"`. CP2 verdict on hasWriter is `"accepted with deploy-surface probe"`. That probe has not been run on the deployed revision. The default GET still serves the 2026-08-14 snapshot. A grade on this row would be a live GET (or `?compute=live` after deploy) returning a named cell that reads negative, not the proof script.

## 3. What remains for the planner: merge, canary `--no-traffic`, POST recompute?

In-fence for R-09 was instrument repair and a firing proof. No store writes. No traffic shift. No launch-criteria change. The property close left PR #447 as `leave_behind`.

CI conclusion strings from `statusCheckRollup` (GitHub GraphQL, uppercase): every completed check is `"SUCCESS"`. Nine of nine: Typecheck, Test, L5 SF-30 Cotality warm grep, L5 SF-24/SF-25 score greps, L5 silent-fallback greps, L5 SF-26 hand hasWriter grep, SS-W16 tier2 flood not served, SS-W18 api-server boots, L17 ci-vintage-predicate. `gh pr checks` displays these as `pass`. There is no single combined conclusion field in that JSON payload; the per-check string is `"SUCCESS"` on all of them. This worker does not merge.

Remainder, none of it in-fence for a still-live R-09 worker, and none of it this W5 pass:

| Step | Owner | In R-09 fence? | Notes |
| --- | --- | --- | --- |
| Merge PR 447 | planner, after occupancy confirmed idle | merge was the close's next gate, not this worker | Eligible on CI `"SUCCESS"`. Close SHA `6fea02c5` is not HEAD `164378da`. Re-read the two post-close commits before merging. |
| Canary `--no-traffic` | planner (deploys planner-owned) | no | Smoke the canary revision. Do not shift production traffic. |
| Live GET / `?compute=live` on that revision | planner grade | this is the mission firing | Named cell id plus payload. Until this returns a negative, R-09 is not done. |
| POST `/api/county-ledger/recompute` | planner / operator after a serving revision | explicitly out of R-09; no store write from that lane | Default GET stays 2026-08-14 snapshot until then. |

Operator does not own the deploy. Operator would only be in the loop if POST recompute is treated as a production store mutation requiring a go beyond planner deploy authority. R-09 already ruled that recompute out of its own fence.

This pass's call stays LEAVE ALONE because the property worktree occupancy is UNCERTAIN and the row is still `PR_OPEN_AWAITING_CI` / PR OPEN. After the property seat is confirmed not occupying `seat/property`, the planner's next call is MERGE AFTER CI, then CANARY NO-TRAFFIC, then a live GET. Do not collapse those into a close.

## 4. Second mechanism for each indicator "firing"

Could the injected probe options have produced the negatives without the production read path actually being repaired? Yes for two of three. The third is a local function comparison, not a live GET.

**hasWriter (508, `48001:easement` = false).** Claimed mechanism: overlay `effectiveRailFieldsByKey` at read/materialize, with `requireEngineRoot: true`, so Cloud Run (no sibling engine) derives false where the store column is true. Second mechanism: `cloudRunManifestReadProbeOptions` injects an absent-engine world the production GET path does not use, while the served grid still materializes from the 2026-08-14 snapshot without overlay. The same observation (508 falses in the script, 0 falses on live GET) is exactly what that second mechanism predicts. CP2 already named the sibling-engine trap: a colocated hauska-engine worktree still derives all true without the Cloud Run probe. Rejected as "production is already repaired" because the live GET at 01:12:50Z is still all `hasWriter=true`. The injected probe is sufficient to produce the negative without the deployed handler having changed.

**atomFamilyState (3048 partial, `48001:geometry` → display `no-atom`).** Claimed mechanism: overlay derived state; partial maps to `no-atom` for DC-4. Second mechanism: the proof script runs repaired compute with Cloud Run probe options against store columns that are all `present`, so the overlay manufactures `partial` in-process while the HTTP snapshot path still serves `present`. Live GET all-present is the same second-mechanism signature. Additionally STARVED: `rrc-wells` still derives `present` from the checked-in engine snapshot while OPS-15 says the property spine holds zero O&G. That rail cannot fire from live store input. Do not treat 3048 overlay partials as proof that every family, including RRC, is gradeable.

**isPartial (18, `48027:zoning` = true).** Claimed mechanism: stop `applyDepthRailDisplayGate` from clearing `isPartial` on depth downgrade; SQL already held 18 partial zoning rows; legacy served 0. Second mechanism: the proof compared repaired compute to legacy compute inside `lib/db/scripts/r09-indicator-fire-proof.mjs`, not live GET to live GET. The store already containing 18 partials means a local call to the new function can return true without any deployed read path serving it. Scratch says the legacy read path erases all to `isPartial=false`. Live GET still all `isPartial=false`. This one did not inject Cloud Run options (`injected: none`), so the probe-options objection is weaker here. The deployed-surface objection is not: preservation in a script is not preservation on GET.

Rejected alternative for all three: "the 2026-08-14 snapshot is the only defect and the read path was already honest." Scratch DEAD-END already killed the fake `fileExists` true-for-all `/hauska-engine/` paths as the deploy-shaped constant. The remaining constant on live GET is the un-recomputed snapshot plus an unrepaired-or-undeployed read path. Until a deployed revision is probed, those two are not distinguishable from outside.

---

R-09 repairs an instrument. It has not yet shown that instrument firing on the customer surface. Leave the property LDT worktree alone. Do not merge from this worker. Do not canary from this worker.
