---
id: 2026-08-29_p91_wave_h_p547_deploy
title: Wave H Stone paint live on p547
date: 2026-08-29
status: serving
plan_row: P-91
item: 12
serving: smartsite-mcp-00043-mex
tag: p547
digest: sha256:3f1718cb5edfbdc1476216cecc89cd7d067c94e697143e10193d5602ee1f8f31
uri: ui://smartsite/app-p547.html
---

# Deploy

Nick said deploy. Isolated tree `P:/tmp/legacy-design-tools-p91-stone` on `feat/p91-wave-h-stone` (dirty paint on base `006fd009`). Cloud Build `7f2774b8-2a72-4d28-b7f2-9baddc5e642a` SUCCESS. Image digest read from Artifact Registry `image_summary.digest`, then pinned on the revision.

Pre-shift: `smartsite-mcp-00041-caj` @100% tag `p546`. Production `/health` `revision` was `smartsite-mcp-00041-caj`.

Canary: `smartsite-mcp-00043-mex` @0% tag `p547`. Tag URL `/health` `revision` was `smartsite-mcp-00043-mex`. Revision image is the digest above. `autoscaling.knative.dev/minScale` is `1`. Then `--to-tags=p547=100`.

Post-shift `/health` on `https://mcp.smartsite.cloud/health`:

`{"status":"ok","service":"smartsite-mcp","name":"Smart Site","version":"0.0.1","authConfigured":true,"cortexConfigured":true,"revision":"smartsite-mcp-00043-mex","failureDomain":"smartsite-mcp"}`

Traffic 100% row: `revisionName=smartsite-mcp-00043-mex`, `percent=100`, `tag=p547`.

Cortex untouched: `cortex-api-00656-vek` @100% tag `p539`. `cortex-api-00646-luj` still tag `staging` at 0%.

`GET /llms.txt` still `Tools (13)`.

No `--set-env-vars`. Cortex not deployed.

Reconnect required. Old iframe is p546. This is serving, not a customer-done paint grade. Open is still typed. 3.8 copy still owed.
