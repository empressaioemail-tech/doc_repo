---
id: 2026-08-24_3b_cortex_CP2
title: CP2 — Cortex Custom Checkout (3b items 1-2, hosted-keep)
date: 2026-08-24
plan_row: P-60
wdll: _inbox/2026-08-24_rebrand6_checkout_3b_WDLL.md
items: 1-2 and hosted-keep half of 3
snapshot: isolated tree P:/tmp/ldt-checkout-3b branch fix/pe-checkout-3b @ 244567a5 (uncommitted)
leave_behind: none
---

# CP2: Cortex Custom Checkout

Tests passed. No commit. Isolated tree only.

## Files changed

- `artifacts/api-server/src/lib/brokerageStripe.ts` — `StripeCheckoutResult.checkoutUrl` optional; add `clientSecret?`
- `artifacts/api-server/src/lib/pePaywallStripe.ts` — `uiMode` / `returnUrl` on both create functions; custom/embedded post `ui_mode` + `return_url`; live custom fail-closed on missing `client_secret`; simulated custom returns `cs_test_…_secret_sim` and omits `checkoutUrl`
- `artifacts/api-server/src/routes/propertyExplorer.ts` — both checkout schemas accept `uiMode` + `returnUrl`; handlers pass them through
- `artifacts/api-server/src/__tests__/pe-pricing-ladder.test.ts` — custom/hosted both-directions + custom-without-secret violation
- `artifacts/api-server/src/__tests__/pe-paywall-stripe.test.ts` — unlock custom contract + same violation

Doc_repo (planner inbox, not the product tree):

- `_inbox/2026-08-24_3b_cortex_CP1.md`
- `_inbox/2026-08-24_3b_cortex_CP2.md`

## Command

From `P:\tmp\ldt-checkout-3b\artifacts\api-server`:

```
pnpm exec vitest run src/__tests__/pe-pricing-ladder.test.ts src/__tests__/pe-paywall-stripe.test.ts
```

`TEST_DATABASE_URL` was an unpooled throwaway Neon (schema fixture). Not written into the tree.

## Raw result

```
 Test Files  2 passed (2)
      Tests  60 passed (60)
   Start at  20:50:43
   Duration  47.90s
```

Both files green. Existing tier / interval / seats / webhook cases stayed in the 60.

## How the custom-without-secret case was violated

`fetch` was mocked to return a Checkout Session with `id` and a hosted `url` and no `client_secret`. `createPeSubscriptionCheckoutSession({ uiMode: "custom", … })` (and the unlock twin) threw `Stripe checkout session missing id or client_secret`. That is the live custom fail-closed path. A custom 200 with only a hosted URL cannot pass.

## Item grades

1. Cortex subscription session is custom. **met.** `uiMode: custom|embedded` returns `clientSecret` + `publishableKey` + `sessionId`, no `checkoutUrl`. Form sends `return_url` (with `{CHECKOUT_SESSION_ID}`) and does not send `success_url` / `cancel_url`. Missing price ID still 503. Tier / interval / seats unchanged.
2. Cortex unlock session is custom. **met.** Same `uiMode` contract on `/entitlement/checkout` and the property-unlock alias. Body still `{ parcelNodeId, returnUrl? }` plus optional hosted URL fields and `uiMode`. Live custom form still posts `metadata[checkout_kind]=property_unlock` and `metadata[parcel_node_id]`.
3. Hosted-keep half. **met.** `uiMode` absent or `"hosted"` still returns `checkoutUrl`. Hosted-kill after PE mounts is not this agent.

## Not finished

- No commit / push (by instruction).
- PE items 4-9 (mount, appearance, return card, queued report) are the other tree.
- Hosted-kill half of item 3 waits for the PE alias.
- Did not write `P:/seat-worktrees/property/*` or `P:/legacy-design-tools`.
