---
id: 2026-08-29_p91_wave_i_p548_deploy
title: Wave I Look up serving on p548
date: 2026-08-29
status: serving-not-graded
plan_row: P-91
items: [1, 2, 3, 4]
serving: smartsite-mcp-00045-kes
tag: p548
digest: sha256:f19261461bedb02599f0ad1680723ac8f72df4b15969bc9b7ff487bcb467021d
uri: ui://smartsite/app-p548.html
---

# Deploy

Nick said go on Wave I. Isolated tree `P:/tmp/legacy-design-tools-p91-stone` on `feat/p91-wave-h-stone` (dirty Wave I on base `006fd009`). Cloud Build `8b8b0aa8-d438-4401-b2d6-7d7516432f64` SUCCESS. Image digest read from Artifact Registry `image_summary.digest`, then pinned on the revision.

Pre-shift: `smartsite-mcp-00043-mex` @100% tag `p547`. Production `/health` `revision` was `smartsite-mcp-00043-mex`.

Canary: `smartsite-mcp-00045-kes` @0% tag `p548`. Tag URL `/health` `revision` was `smartsite-mcp-00045-kes`. Revision image is the digest above. `autoscaling.knative.dev/minScale` is `1`. Then `--to-tags=p548=100`.

Post-shift `/health` on `https://mcp.smartsite.cloud/health`:

`{"status":"ok","service":"smartsite-mcp","name":"Smart Site","version":"0.0.1","authConfigured":true,"cortexConfigured":true,"revision":"smartsite-mcp-00045-kes","failureDomain":"smartsite-mcp"}`

Traffic 100% row: `revisionName=smartsite-mcp-00045-kes`, `percent=100`, `tag=p548`.

Cortex untouched: `cortex-api-00656-vek` @100% tag `p539`. Staging stays 0%.

No `--set-env-vars`. Cortex not deployed. Tools stay 13.

Tests in that tree: 89 passed (12 mcp-app, including three Wave I cases).

# What shipped

Open is a unique turn (`Open this parcel` + `get_smart_site` depth node, do not save). Parcel panel strokes `draw.ring` and lists `draw.edges` only when those fields are on the tool result. Envelope reason `atom_path_pending` maps to `Withheld, setbacks unruled` at render. The iframe HTML source does not contain `atom_path_pending`. Flood overlay is quieter. Node id stays `--ss-atom`. No invented road nodes `4429` / `4430` / `4431`. A ring-only draw does not invent alley / 34169 / PINE / ROW.

# Not a customer-done grade

Reconnect required. An open iframe is still p547. Wave I items 1-4 are serving, not walked. Live `draw.edges` is unmeasured until Open on gold: the tools fixture `GOLD_DRAW` has a ring and no edges; the 2026-08-28 p87 probe had four dump edges. I1 forbids inventing the dump labels if the live brief omits `edges`.

Wave J (legend, empty, fail pair, listing on the drawn panel) has not started.
