# WDLL: L18 ledger materialize-on-write (P-14)

Date: 2026-08-14  Status: closed (all 6 items met)
Operator approval: dispatch `_dispatches/2026-08-14_l18_dispatch.md`; addendum 2026-08-14T12:22Z operator-carried
PLAN-ROW: P-14
Repos: legacy-design-tools (cortex-api), hauska-map (command-center)

## Done looks like

GET `/api/county-ledger` serves a precomputed snapshot in constant time. Freshness is part of the response (`computedAt` + `servedAt`). The Command Center County Manifest page at cmdcenter-blush.vercel.app loads fully during an active atoms drain. A stale snapshot is visible on that page (computedAt stamp + STALE banner), never a silent old grid. A passing curl with a timing-out console is a fail.

## Acceptance items

1. Materialized ledger summary + grid that GET serves in constant time (sub-second, never heavy aggregation on the request path). | check: timed GET during a drain window <2s | grade: met | evidence: prod GET http=200 time=0.773s during L16 pipelines current=48009 landed=4
2. Writer-owned refresh: `countyLedgerMaterializeCli --apply` (same discipline as countyRailRefreshCli). Recompute cost lives at write time. No DB trigger at CP1. | check: CLI --apply upserts snapshot; scorers can invoke it | grade: met | evidence: --apply wrote id=current computedAt=2026-08-14T13:10:24.947Z duration=1072.2s
3. Freshness is part of the response: `summary.computedAt` + `summary.servedAt`. A stale view is visibly stale. | check: deliberately-stale snapshot still 200 with old computedAt; CC shows the stamp | grade: met | evidence: backdate 20min still 200; CC banner "STALE - materialized at 2026-08-14T12:50:24.947Z"
4. Contract preserved: manifestCells shape, displayState semantics, applyDepthRailDisplayGate, DC-1..DC-9/DC-13 instrument reads identical. Legacy compute behind `?compute=live` only, never default. | check: parity test materialized vs live compute on same DB state | grade: met | evidence: LDT #425 CI Test run 31800703606 conclusion success; live default is snapshot
5. Deploy lane-owned (cortex-api canary 0% -> smoke -> shift; full 40-char SHA). | check: live timed GET + computedAt fresh after materialize | grade: met | evidence: canary 00503-yiq SHA 19da3b1b828e5751b419f171cd4a01d0f3c87c58 then shift 31802014518
6. (ADDENDUM) After canary shift, CC County Manifest (`#panel=county-manifest` on cmdcenter-blush.vercel.app) loads fully during an active drain window. If the served materialization is stale, the page shows the computedAt stamp via the existing STALE-banner pattern. Curl-pass + console-timeout = FAIL. | check: browser/page probe, not curl alone | grade: met | evidence: page probe in-page fetch 1082ms; computedAt visible; stale reload showed banner. Not curl-only.

## Amendments

- 2026-08-14: item 6 added because the operator named the CC county manifest page as the customer-done acceptance probe (a passing curl with a timing-out console is a FAIL). Original dispatch constrained LDT only; this amendment adds hauska-map command-center.

## Finish card (graded at close)

All six items met. Close `_inbox/2026-08-14_l18_close.json`. Start-vs-Finish: addendum item 6 was the only scope add (hauska-map command-center); no silent absorption. Scorer auto-invoke of the CLI remains open (honesty: CC will show STALE after 15 min until the next --apply).
