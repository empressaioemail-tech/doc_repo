{
  "lane": "p167-pdf",
  "checkpoint": "CP2 (mid-work: PDF/panel/MCP legs collected, PR open, deploy pending)",
  "planRows": ["P-167"],
  "sinceCp1": [
    "LDT pin PR #672 opened (chore/p167-pin-and-strings), all six stale package.json files bumped to ^1.33.1, pnpm-lock.yaml re-resolved to a single @empressaio/atom-contract@1.33.1 entry, pnpm -r ls confirms 1.33.1 for all 7 importers, root pnpm run typecheck passes.",
    "hauska-engine-api-00211-vam confirmed by CONTENT (not timestamp) to contain PR #429 and #430: downloaded the exact Cloud Build source tarball (build 4e134a3d..., image digest sha256:894c951e3... matches the serving revision's spec.containers[0].image exactly) and verified packages/engine-core/src/site-plan/buildable-display-vocab.ts is ABSENT (deleted by #429), buildable-display-vocab-retired.test.ts is PRESENT (added by #430), and atom-contract-pin/package.json + engine-core/package.json both pin ^1.33.1.",
    "Generated feasibility PDFs via export_instrument for 4 of 5 parcels (48021:34049, 48021:33223, 48453:474034, 48453:367134). 48453:113408 returned {status:'error',reason:'engine_unreachable'} on two consecutive attempts; stopped after the second per the two-strikes rule and F14's precedent that this class of hang is intermittent, not a state -- recorded as an UNMEASURED leg, not a failed one.",
    "Pulled panel facets (GET /api/spine/property-atoms/<id>/facets) and MCP get_smart_site depth=node for all 5 parcels.",
    "Built the full side-by-side comparison and found the row FAILS on all 5 parcels for concrete, cited reasons -- summarized in the observations file and the close's table. Headline: (a) a raw internal code 'no-zoning-stamp' leaks unhumanized on both MCP and PDF for the two no-zoning parcels, while the panel shows a full hand-written sentence for the same disposition -- 'no-zoning-stamp' is not in the P-91 v3 VOCABULARY table or the atom-contract display catalog at all; (b) hauska-engine's site-model.ts:658-660 deliberately substitutes a hardcoded literal ('pending — buildable-envelope atom not yet on file') in place of the shared vocabulary's own pdfLabel for two of its five buildableDisplayKind branches, by design (code comment cites Ruling B: the vocabulary's own pdfLabel for those two kinds would otherwise leak a raw figure) -- so PDF and MCP wording differ even on the one parcel (48453:367134) where the underlying zoning/setback DATA agrees byte-for-byte across all three surfaces; (c) for 34049 and 33223 the PDF's own per-parcel adapter still computes and prints a concrete buildable figure and its own setback numbers independent of the record/MCP path, which is the long-lived F3/F4 divergence (P-152/P-154/P-159 territory), still live 2026-09-13.",
    "Wrote observations file _inbox/2026-09-13_p167_observations.json in the probe's exact shape."
  ],
  "outstandingBeforeClose": [
    "PR #672 CI (Test job specifically, ~13 min) not yet concluded as of this checkpoint -- polling in background.",
    "Deploy: api-server (cortex-api service) and smartsite-mcp workflows not yet triggered. Lease check: _catalog/leases/ holds no active lease file for either service at CP2 time. Will write leases, deploy, verify serving revision by field name, then delete the lease, per AGENT_CONTRACT section 3 and the operational addendum.",
    "cortex-api is flagged in the wave-4 planner checkpoint as concurrently held by P-177 'by lease' even though no lease FILE exists; a live signal check (gh pr list for P-177, recent gcloud builds list) shows P-177's most recent LDT PR (#671) already merged and built, no in-flight workflow_dispatch action visible in the last 10 cortex-api builds. Proceeding cautiously with a self-written lease, one service at a time, verifying before deleting."
  ]
}
