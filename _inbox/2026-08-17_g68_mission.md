# G-68 Smart Files product UI

WDLL `_inbox/2026-08-17_g68_smart_files_ui_WDLL.md` approved. Layouts `30c_smartcity_platform_ia.md` sections 15-17. Kit on main. Cite WDLL items 3-7.

## Mission premise

Replace the QA two-column rooms page with the file browser: Places rail, list default, access rail (tenant-private stated), share dialog, People page, Bring files chrome with fixture states. Write API still works. No OAuth. Cortex stays 404.

## Completion predicate

PR on smart-files web/: browser chrome live in the worktree; create/upload/share still call the existing BFF; persona select is a fixture panel. Planner owns merge and Vercel deploy.

## Scope basis

G-68 only. Do not touch `src/*` except if a read is required. Do not add Google OAuth. Not G-53. Not Dashboards Work compose unless cheap.

## Workers

Do not spawn further agents. Isolated worktree from origin/main.

1. Browser + access + share + Bring files fixtures (WDLL 3-6). Preserve `api()` helper and hash share loader. PR.
