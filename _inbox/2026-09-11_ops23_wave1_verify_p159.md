---
id: 2026-09-11_ops23_wave1_verify_p159
date: 2026-09-11
row: P-159
snapshot: hauska-engine origin/main 79fa5737096cba09fda328a6b79ea598ba5e9b2b (79fa573)
kind: read-only fact verification
---

# P-159 fact check: feasibility PDF buildable figures and narrative

Snapshot: `hauska-engine` origin/main `79fa573`, fetched 2026-09-11. All reads via `git show origin/main:<path>`; no working tree touched.

## 1. Buildable figures the site model carries

`packages/engine-core/src/site-plan/site-model.ts` lines 530 to 610 contain no percent computation. The percent lives in the renderers (item 2). The model carries two numeric sources and one string:

**(a) `buildableAreaSqFt`** (line 547): local offset-ring area, null on zero-inset or degenerate ring.

```
546  const lotAreaSqFt = polygonAreaSqMeters(ringLocal) / SQMETERS_PER_SQFOOT;
547  const buildableAreaSqFt =
548    offset.offsetRing && !isZeroInsetEnvelope(ringLocal, offset.offsetRing)
549      ? polygonAreaSqMeters(offset.offsetRing) / SQMETERS_PER_SQFOOT
550      : null;
```

**(b) `warmAreaSqFt`** (line 564): the buildable-envelope atom's own area, from `inputs.envelopeOutcome` (type at lines 108 to 112).

```
564  const warmAreaSqFt =
565    warmKind === "buildable" && typeof inputs.envelopeOutcome?.areaSqFt === "number"
566      ? inputs.envelopeOutcome.areaSqFt
567      : null;
```

**(c) `buildablePdfLabel`** (lines 588 to 597): a string that prefers WARM over LOCAL on both branches.

```
588  const buildablePdfLabel =
589    buildableVocab.kind === "provisional" && honestNote
590      ? `${
591          warmAreaSqFt != null
592            ? `${Math.round(warmAreaSqFt).toLocaleString("en-US")} sq ft`
593            : buildableAreaSqFt != null
594              ? `${Math.round(buildableAreaSqFt).toLocaleString("en-US")} sq ft`
595              : "buildable area"
596        } (PROVISIONAL — ${honestNote})`
597      : buildableVocab.pdfLabel;
```

The non-provisional branch goes through `buildable-display-vocab.ts` `resolveBuildableAreaSqFt` (lines 85 to 93), which also returns the warm figure first. `buildableAreaPct` is a vocab input that site-model.ts never passes (NOT FOUND in site-model.ts; only supplier is `packages/retrieval/src/serving-sweep/project-sheet.ts:227`, off the PDF path).

**Percent** is computed twice, always from the LOCAL figure: `pdf/render.ts` 1371 to 1374 (sheet-2 summary row, `s.buildableAreaSqFt / s.lotAreaSqFt`) and `pdf/feasibility.ts` 699 (cover qualifier, `sqFt / sp.lotAreaSqFt` where `sqFt = sp.buildableAreaSqFt`).

Count on one document: cover headline = `buildablePdfLabel` (warm preferred, feasibility.ts:706); cover qualifier percent = local (699); copied site-plan header stat = local (`buildableHeaderStat`, render.ts:558 to 563); copied sheet-2 row = local plus local percent (render.ts:1375 to 1386). Two numeric sources rendered through four surfaces. When warm differs from local, the cover headline and its own qualifier disagree within two adjacent lines. The mission's "four figures" is right on surfaces, wrong on sources: there are exactly two.

## 2. PDF composition, sheet order, narrative generator

Assembler: `packages/engine-core/src/site-plan/pdf/feasibility.ts` (doc id `FS-<parcelNodeId>`, line 1373). Order from `sheetPlan` (1401 to 1412) and numbering (1327 to 1350): cover; NARRATIVE (`notesPlanned`); aerial; copied site-plan sheets (drawing + summary, emitted by `pdf/render.ts` via `emitPdfSitePlan`, 1343); flood-drainage sheets; brief (facts) pages; web-findings; how-to-read. Comment at 1393 to 1400: narrative moved to sheet 2 on operator direction 2026-09-09.

Sheet 2 narrative text (1248): `options.narrativeOverride?.text ?? model.package.narrativeSkeleton`. The override is LLM output: `feasibility-author.ts` 169 to 173 calls `generateFeasibilityNarrative` (in-process, `narrative-generator.ts`, grok-4.3 at line 124), ON BY DEFAULT since 2026-09-09 (author 164 to 169).

**No sentence template describes the lot's structures.** NOT FOUND for `vacant`, `empty lot`, `no structure`, `no building` in `narrative-generator.ts`, `feasibility-author.ts`, `narrative-section-client.ts`, `report-model.ts` except one prompt rule, `narrative-generator.ts:140`:

```
4. An absent fact is not a negative finding. "No record was found" never becomes "there is none". Never infer a vacant site from a missing footprint.
```

The deterministic skeleton fallback (`report-model.ts` 266 to 297) writes county, zoning, lot area, flood, open items; no structure sentence.

The prompt claims "A sentence with no marker will be discarded" (137). Code does not do that: the only refusal is `cited.length === 0` (generator 286 to 292). Nothing compares narrative content to any report figure. The in-process payload (`buildFullNarrativeFacts`, client 165 to 236) sends BOTH `geometry.buildableAreaSqFt` (local, line 186) and `verdict` (line 230), and the verdict opens with `buildablePdfLabel` (`report-model.ts:263`), so the model can see two different numbers for one parcel.

## 3. Data-quality note

One note type exists. `report-model.ts` 329 to 336:

```
329  const dataQuality: DataQualityNote = {
330    supersededNotes:
331      model.facts.flood.status === "present" && model.drainage.status === "present"
332        ? [
333            "Flood determination: the parcel-scoped drainage study supersedes the statewide screening fact; the screening value is not shown as a second, independent finding.",
334          ]
335        : [],
```

Rendered as section "Data quality" at `feasibility.ts` 496 to 501, and passed to the narrative as `dataQualityNotes` (client 235). Nothing buildable-related.

## 4. Footprint fact: state or value

The narrative reads the STATE. `narrative-section-client.ts:146` sends `footprint: model.facts.footprint` whole, a `FeasibilityFactState<FootprintFacts>` (`feasibility-model.ts` 24 to 26: `present` or `absent` with `kind: AbsenceKind`, `reason`, `consequence`). Population, `report-model.ts` 469 to 488: `failed-this-run` on fetch failure; `clear` when an absence atom exists, with consequence "The site reads as unimproved, so redevelopment is unlikely to require demolition." (478); `blocked-at-source` when never checked, "Existing structures are unknown, not absent." (483).

The PDF facts page has a second-derivation guard: `footprintContradictsAppraisal` (`feasibility.ts` 927 to 929) fires when footprint is not present AND the appraisal roll carries year-built or living area, and 565 to 576 replaces the "unimproved" consequence with "Sources disagree ... Do not treat this parcel as vacant". That guard does NOT reach the narrative: NOT FOUND for `improvement`, `appraisal`, `contradict`, `yearBuilt` in `narrative-section-client.ts`. The LLM receives the "clear" consequence sentence and `parcelOwnership` separately, with no flag, and rule 4 is the only thing stopping a vacancy sentence.

## 5. Tests

Site-model: `__tests__/site-model.test.ts` asserts `buildableAreaSqFt` null/not-null and less-than-lot (120, 121, 180, 215, 256, 257, 288, 314, 329, 357). `__tests__/export-consumes-boundary-primitive.test.ts` 141 to 143 asserts warm vs local within 0.2 percent, boundary-primitive basis only. `__tests__/buildable-display-vocab.test.ts` asserts `pdfLabel` strings (55, 86, 150). `pdf/__tests__/render.test.ts:188 to 194` asserts the sheet-2 "% of lot" qualifier exists.

Narrative: `pdf/__tests__/feasibility.test.ts` 121 to 123 asserts the SKELETON contains county and zoning. `__tests__/manifest-is-load-bearing.test.ts` 322 to 338 tests `footprintContradictsAppraisal` and that the facts-page caption drops "unimproved". `__tests__/narrative-generator.test.ts` and `__tests__/feasibility-narrative-wiring.test.ts` assert only fallback reasons, citations, and web-block splitting.

NOT FOUND: any test asserting `buildablePdfLabel` equals the sheet-2 row or the cover qualifier's figure; any test asserting generated narrative text against a report figure or the vacancy rule (searched those files for headline, buildable, vacant, contradict, of buildable area).

## 6. Hosting service and deploy

Routes in `services/engine-api/src/routes/parcel-terrain.ts`: POST `/:parcelNodeId/feasibility-export/refresh` (601), GET `/:parcelNodeId/feasibility-export` (704), GET `.../download` (710). Mounted at `server.ts:183` `v1.route("/property-nodes", buildParcelTerrainRoutes())` under `/v1` (186), so the full path is `/v1/property-nodes/:parcelNodeId/feasibility-export`.

Deploy: root `cloudbuild.engine-api.yaml` builds `services/engine-api/Dockerfile` into `us-central1-docker.pkg.dev/hauska-prod-497015/cloud-run-source-deploy/hauska-engine-api:latest` (build and push only; the Cloud Run deploy step is not in the file). NOT FOUND: `services/engine-api/DEPLOY.md` (docs dir holds only `gate-front-seam.md`, `map-layers-contract.md`); any `.github/workflows` file mentioning engine-api.

## Fleet memory

LESSON: `buildablePdfLabel` prefers the warm atom figure; every percent and every copied site-plan stat uses the local offset figure. One-figure-per-document means picking a source, not a surface.

LESSON: the narrative prompt's "discarded" claim is unenforced; the only code gate is zero citations.

GROUND-TRUTH (2026-09-11, 79fa573): narrative is in-process grok-4.3, on by default; LDT `/research/narrative-section` path is an explicit override only.

OPEN: `footprintContradictsAppraisal` guards the facts page but not the narrative payload; the "unimproved" consequence string is sent to the model verbatim.

leave_behind: none
