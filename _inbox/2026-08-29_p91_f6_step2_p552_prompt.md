---
id: 2026-08-29_p91_f6_step2_p552_prompt
title: F6 step 2 new-chat Connect paste after p552
date: 2026-08-29
status: ready
plan_row: P-91
serving: smartsite-mcp p552
uri: ui://smartsite/app-p552.html
screen_id: 4316b571-c7d2-4b9f-9e50-4f7a16dbfa94
plant: 48021:900099
---

# How to run

Disconnect Smart Site MCP. Reconnect. This must be a new chat. URI must be `ui://smartsite/app-p552.html`. If the first board already says `Open did not reach me`, stop. That is the old iframe.

Do not search the web. Do not call `ask_the_map`. Do not call `save_property`. Do not `create_screen`.

Paste everything below the line.

---

You are grading Smart Site MCP F6 step 2 on a fresh `app-p552.html` iframe. Use only Smart Site tools.

First confirm the resource URI is `ui://smartsite/app-p552.html`. If it is not, stop and say so.

Call `list_screens` with screenId `4316b571-c7d2-4b9f-9e50-4f7a16dbfa94`. The board must have no fail sentence. If it already reads `Open did not reach me` or `Not on file in Bastrop`, this is the old iframe. Stop.

The plant is the row whose NODE is `48021:900099`. Not `48021:34169` (query-alpha puts it above). Not `48021:900001` (different screen, no Open).

Ask the operator to click Open on `48021:900099` and Send. Do not call `get_smart_site` yourself. If the composer names any other id, refuse.

Pass: `get_smart_site` runs once for `48021:900099`. Panel reads `Not on file in Bastrop`. Fail: `Open did not reach me`, or any ring, edges, or label for 900099.

Do not score MET from the tool JSON. `baked_snapshot_not_found` is unchanged from p551. The grade is the panel sentence.

Then stop.
