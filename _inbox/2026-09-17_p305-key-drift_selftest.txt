2026-09-17T10:20:40.7895395-05:00  check-preview-key-drift --selftest
  pass  RECORDED 401 (503 wrapper): the incident's own body must FAIL
  pass  RECORDED 200: the fixed preview's own body must PASS
  pass  RECORDED 401 under HTTP 401 (unwrapped)
  pass  the recorded 401 body with its marker stripped is not auth drift
  pass  a bare 401 with an empty body fails by status
  pass  a bare 403 is a REFUSAL, not the predicate (bypass 1)
  pass  a 500 with a non-auth body is a REFUSAL, never drift (bypass 2)
  pass  a 200 that is not JSON is a REFUSAL, never ok (bypass 5)
  pass  a 200 carrying an auth marker still FAILS (marker beats status)
  pass  fixture integrity: 401 fixture carries the marker, 200 fixture does not

all 10 self-test cases pass (2 recorded bodies, both directions, 401-by-status, 403/500/HTML refusals, marker-beats-status, fixture integrity)
selftestExit=0

2026-09-17T10:20:40.9284883-05:00  inventory-key-consumers --selftest
  pass  Vercel: a predicate key in preview+production is found with BOTH environments
  pass  Vercel: an alias is NOT a predicate hit (bypass 1 depends on this being invisible here)
  pass  Cloud Run: a service env literal under a predicate name is found
  pass  Cloud Run: a job's secretKeyRef is found one level deeper than a service's
  pass  Cloud Run: a ref that follows a secret version reads as secretKeyRef, not as a literal
  pass  Cloud Run: a JOB is invisible to the service path (no false hit, bypass of the wrong reader)
  pass  Cloud Run: an env var outside the predicate is ignored

all 7 selftest cases pass (both matchers, both directions)
inventorySelftestExit=0
