{
  "lane": "scaleup-lb-envelope-roads",
  "checkpoint": "CP2-pilot-mid-work",
  "filedAt": "2026-09-16T13:43:00Z",
  "subAgentsSupervised": [
    {
      "id": "labeller-registry-rederive",
      "status": "completed",
      "durationMs": 461969,
      "toolUses": 57,
      "adversarialSpotCheck": "Verified independently: grepped hauska-engine origin/main packages/engine-core/src/registry/jurisdiction-registry.ts myself for pflugerville/san-marcos/georgetown/waco/kyle (zero hits, confirmed) and for '48453' (three hits, all comments explicitly EXCLUDING Travis County from this pass, e.g. line 483 'EXCLUDED: Travis County (48453) is deliberately NOT added in this pass.'). Sub-agent's headline claim -- no jurisdiction-registry row exists for any of the five named wired cities and none at all for Travis County -- CONFIRMED at source, not taken on the sub-agent's word alone.",
      "keyFindingsAccepted": [
        "LDT reconcileAtomEnvelope.ts has ZERO depth-warm-verification awareness (grep for depthWarmPromotion/depth-warm-verified returns nothing) -- confirms and sharpens my own CP1 finding that the wire type simply never carries the field.",
        "hauska-map atom-chain-to-facets.ts DOES carry isDepthWarmPromoted() gating -- matches my own CP1 reading exactly (same DEPTH_WARM_PROMOTION_MARKER constant, same line range).",
        "The 490,185 no-buildable-area atoms are from cortex-tier1-snapshot-breadth-bake specifically (not depth-warm) -- breadth-bake writer path never calls labelEdgesFromRoads or gateWarmCohort at all; it composes from an already-baked coarse snapshot.",
        "depth-warm (not breadth-bake, not a new writer) is the correct re-derive-at-scale writer -- it already runs real edge-labelling and gateWarmCohort.",
        "P-208's re-mint read as a no-op because of a FALSE parcel-node retirement (warm-preflight-gate.ts declines on stored status='retired'), not because there was nothing to re-mint -- a live BCAD check during that lane confirmed the parcel is still current at the county source.",
        "Write-lease scope is (entity_type, county_fips) via atoms-writer-lease.ts v2 -- NOT (store, entity_type, county_fips) as the dispatch's own prose assumed; sub-agent explicitly flagged this discrepancy rather than silently conforming to the prompt's wording (good -- DEV_PROCESS 3.2 behavior).",
        "Staging is a genuinely separate Neon DB branch + separate hauska_mcp DB (not a flag on the same store); --target defaults to staging in every job checked, production requires an explicit opt-in."
      ]
    },
    {
      "id": "footprint-trace-decisions-text",
      "status": "still running at CP2 time"
    }
  ],
  "liveSurfaceVerification": {
    "method": "mcp__claude_ai_Smart_Site__get_smart_site (depth=node) and run_report against the live, authenticated Solo-tier Smart Site connector -- NOT a code read, a genuine live probe on the deployed customer surface (DEV_PROCESS 4.4).",
    "parcelsProbed": ["48209:97658 (San Marcos, Hays)", "48453:427599 (Pflugerville, Travis)"],
    "falsifierF2Result": "PASS, confirmed live (not just inferred from code): San Marcos 48209:97658's draw.overlays envelope = {state:'refused', reason:'atom_path_pending', label:'Buildable envelope not computed'} -- no polygon. Its brief.setbacks-envelope section is simultaneously PRESENT with real numbers (front 25/side 5/rear 20/corner 15 ft). Pflugerville 48453:427599's draw.overlays envelope = {state:'present', geom:<real polygon>, basis:'modelled-figure-withheld'} -- polygon draws, area withheld, matching Ruling B reversed (R-2) exactly.",
    "threeLiveWordingDefectsFound": [
      "San Marcos envelope overlay: reasonDisplayText 'Withheld, setbacks unruled' / token atom_path_pending, while the SAME response's setbacks-envelope brief section shows real, present setback distances two sections earlier. The wording claims setbacks are unruled when they are on record.",
      "San Marcos boundaryEdgeFact: every one of 9 edges carries setback.basis = 'No setback table configured for jurisdiction descriptor.' -- the exact string the dispatch named -- while the top-level setbacks-envelope brief section shows a real table (SF-6, san-marcos-tx). Traced to source: hauska-engine packages/engine-core/src/property-reasoning/emit-setback-rule.ts:90 emits this literal string. Two different code paths resolve 'does a setback table exist for this jurisdiction' and disagree for the same parcel.",
      "Pflugerville buildingFootprintFact is PRESENT with real ml-derived footprint polygon geometry (footprintGeometry, ml-global-building-footprints-v1), but the SAME response's draw.overlays footprint entry says geom none, state unknown, label 'Structure of record (1998), footprint unmeasured' -- the map-card draw block ignores footprint data that demonstrably exists elsewhere in the identical payload. This is the live, first-party confirmation of QA-08/P-248."
    ],
    "pdfLeg": "export_instrument(kind='siteplan') on 48209:97658 returned status upgrade_required, reason studio_report, subscriptionTier 'solo' -- CONFIRMED genuinely not entitled on this account (Studio/Team or a 30-day unlock required); PDF wording marked UNMEASURED-live per dispatch instruction, sourced instead from Agent B's code trace of hauska-engine site-plan/pdf/*."
  },
  "coreMechanismConfirmedAtCP1": "unchanged -- see CP1. Agent A's independent grep of reconcileAtomEnvelope.ts (zero hits for depth-warm anything) corroborates rather than contradicts.",
  "instrumentBuilt": {
    "path": "scripts/envelope-draw-gap.mjs",
    "selfTest": "17/17 PASS (node scripts/envelope-draw-gap.mjs --self-test)",
    "livePhase1Run": "Executed against FACTORY_DATABASE_URL_RO live, 18.8s wall time, 981,405 parcels, atomsJoined=false (atoms store not yet queried -- gate pending). Per-city setback-on-record counts produced for all six counties; San Marcos (Hays) setbackOnRecord=14,262 matches independent hand-aggregation from the raw q1_setback_per_city.sql query (13,861 'SAN MARCOS' + 401 'SAN MARCOS TX') exactly -- two independent code paths over the same data agree (DEV_PROCESS 1.4 reconciliation, positive case)."
  },
  "atomsStoreGate": {
    "markerPath": "_inbox/2026-09-16_scaleup-la-atoms-reads-done.json",
    "statusAtCP2": "not present. Polled at 13:38, 13:40, 13:41, 13:42Z (lane claimed ~13:24Z per factory-store now() reading). No L-A report/close/artifact of any kind found in _inbox/ yet either, so no direct evidence L-A is mid-query vs. not yet started vs. running slowly -- continuing to poll within the 45-minute bound per operational notes, doing atoms-independent work in the meantime.",
    "heavyScanDiscipline": "No atoms-store connection attempted yet, including for schema introspection -- operational notes read as an unconditional gate ('Before running ANY query'), so schema-only reads are being held to the same bar as data scans rather than carving out an exception on my own authority."
  }
}
