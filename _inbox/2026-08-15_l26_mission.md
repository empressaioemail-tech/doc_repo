# L26 — flood from-plan drain + Harris plan + pipeline SQL port

WDLL: `_inbox/2026-08-15_texas_flush_server_plan_WDLL.md` (approved 2026-08-15)
Items this lane owns: 1, 2, 3, 4, 5, 6, 7, 8, 10, 11
PLAN-ROW: P-07, P-08, P-12

Do not seat L25. Do not restart per-county JS pipeline runners. Do not `--apply` pipelines until WDLL item 5 (landed-county parity) passes.

## Parallel streams (safe)

1. Harris 48201 `--plan-only` on neondb (off-slot, one heavy scan). Worktree `P:/hauska-engine-worktrees/l24-flood-plan-emit`.
2. Flood `--from-plan --apply --batch=5000` on hauska_mcp under lease holder L26. Does not read parcels. Smallest-first. Harris joins the queue only after item 1 NDJSON exists.
3. Pipeline PostGIS port in an isolated worktree off origin/main. No `--apply`. Template is special-district TEMP+GiST, `geography` ST_DWithin(152.4), no GeoJSON on the wire.

## Hard rules

- One atoms custodian (L26). Parallel connections allowed; no second lease holder.
- At most one heavy neondb parcel/PostGIS scan (Harris). Pipeline port may EXPLAIN.
- Verify-by-PK. Throw-and-rerun is the resume path (upsert).
- Watches registered before start. Progress is work (atomsWritten / plan.log bytes), never a touch-loop.
- Doc_repo commits stay planner-owned.
