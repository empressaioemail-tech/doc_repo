{
  "id": "2026-08-30_ctx_alias_persist_cp2",
  "date": "2026-08-30",
  "seat": "integration",
  "snapshot": {
    "factory_worktree": "P:/seat-worktrees/property/hauska-factory-ctx-publish",
    "factory_branch": "seat/property-ctx-walk-alias-schema",
    "factory_commit": "866c38b",
    "tests": "14 pass alias-landing-schema + alias-persist; alias-two-count --self-test ok"
  },
  "card": "_inbox/2026-08-30_ctx_w1_alias_WDLL.md",
  "method": "code-reading of the write path, then tests. Not the handback.",
  "found_and_fixed": [
    {
      "claim": "CLI --apply writes landing and atoms",
      "mechanism": "runAliasPersist only called writePersistedAlias when deps injected clients; the real CLI never injected them",
      "rejectedAlt": "dry-run default is enough — rejected; a later planner --apply would report wroteLanding without INSERT",
      "fix": "openAliasPersistStores uses resolveTargetStores(--target) + refusePoolerHost; --apply without --target refuses ALIAS_TARGET_REQUIRED; missing target URLs refuse TARGET_ENV_MISSING"
    }
  ],
  "accepted": [
    { "item": 4, "grade": "met as code", "why": "classify then persist; refuse never reaches INSERT; A-022 reuse; GO + run row + target stores required" },
    { "item": 6, "grade": "met", "why": "gate-blocked / owner-disagree / punctuation / cannot-bind write zero; Taylor fixture" },
    { "item": 7, "grade": "met as instrument", "why": "disagree fails; 0=0 vacuous; seed leak is state===joined on 48209/48491 only" },
    { "item": 8, "grade": "met", "why": "ALIAS_WRITER_HELD when another identity.alias holder is on that FIPS" },
    { "item": 3, "grade": "shape only", "why": "parser fail-closes without parcelJoin.txgio_id; live backfill not run" }
  ],
  "leave_behind": [
    "apply migration 0005 on the neondb the bake reads (not only the Factory control DB)",
    "item 3 live card H backfill; extract must carry txgio_id or use a W1 bind dump",
    "live two-count per FIPS",
    "Wave R pin after items 3 and 7 pass on live counts"
  ]
}
