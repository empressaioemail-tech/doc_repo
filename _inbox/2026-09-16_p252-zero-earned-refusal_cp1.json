{
  "checkpoint": "CP1",
  "kind": "orientation + pre-registered falsifiers, filed BEFORE any code was written and before any test was run",
  "lane": "p252-zero-earned-refusal",
  "planRows": ["P-252"],
  "seat": "p252-lane",
  "seatNote": "claimed via `node scripts/lane-claim.mjs claim --lane p252-zero-earned-refusal --seat p252-lane --plan-row P-252` in P:/seat-worktrees/dispatch-planner/doc_repo -- exit 0, 2026-09-16T17:39:28.696Z. The seat that compiled this dispatch (dispatch-planner) holds p241-etj-acquisition and is live; this lane did not touch that lane's files.",
  "docRepoWorktree": "P:/seat-worktrees/dispatch-planner/doc_repo (the dispatch planner's own seat worktree -- where the session running this lane is rooted per the operator; artifacts written here, never into P:/doc_repo, the integration seat's checkout)",
  "productRepo": {
    "repo": "hauska-factory",
    "clone": "P:/tmp/p252-zero-earned-refusal (fresh clone of https://github.com/empressaioemail-tech/hauska-factory.git)",
    "branch": "fix/p252-zero-earned-refusal",
    "startCommit": "91712795ea2a1eb7c27f7328cf7f52fcb7810b59",
    "startCommitSubject": "fix(P-201): excluded is three states wearing one word (#156)",
    "declaredBeforeWritingAnything": true
  },

  "sequenceActuallyFollowed": [
    "1. Claimed the lane first (exit 0), before any read, per the dispatch's own ordering.",
    "2. Found the dispatch: it exists ONLY in P:/doc_repo (commit 1a8c8f6f, 'compile the four lead Phase 0 lanes'); the planner seat worktree had not yet been fast-forwarded to it. Workspace resolution was ambiguous (the session's workspace path does not exist on disk), so the operator was asked and named P:/seat-worktrees/dispatch-planner/doc_repo as this session's rooted doc_repo worktree and p252-lane as the seat id.",
    "3. Cloned hauska-factory fresh from origin/main into P:/tmp/p252-zero-earned-refusal; cut fix/p252-zero-earned-refusal from origin/main. HEAD = 9171279, exactly the commit the dispatch names (.6359 sha = 9171279). Nothing written before this was recorded.",
    "4. Read the four named gate files plus the county-scoped-rail test BEFORE editing: src/lib/parcel-record-engine/publish-gate.js, src/lib/gate-exclusion-classifier.mjs, src/lib/publish-readiness-gate.mjs, src/jobs/publish-gate-sched.mjs, test/gate-county-scoped-rail.test.mjs.",
    "5. Confirmed WHICH evaluateRailGate copy runs (dispatch step 1's own conditional): src/jobs/publish-gate-sched.mjs runGateCli() execFile('node', [<this repo>/src/lib/parcel-record-engine/gate-rail-cli.mjs, ...]) -- the FACTORY's vendored copy, not hauska-engine's. gate-rail-cli.mjs imports ./index.js -> ./publish-gate.js. The engine's own original is at P:/seat-worktrees/property/hauska-engine/packages/engine-core/src/parcel-record/publish-gate.ts (read-only, different repo, different lane -- not edited).",
    "6. Read the store contract: migrations/0009 (pass|refuse|excluded CHECK) and 0011a (widened to the three excluded-* strings, and its own header records that 0011a was NOT applied to production by P-201's lane).",
    "7. Read the serve-side rule at source before stating it: `git show origin/main:artifacts/api-server/src/lib/parcelRecordAllowlist.ts` + `parcelGateVerdictRead.ts` in legacy-design-tools."
  ],

  "instrumentsReconnaissance": {
    "npm test": "node --test test/*.test.mjs (package.json), 100+ files, no store needed for the gate tests",
    "gate-rail-cli.mjs": "sets default_transaction_read_only=on and statement_timeout 20s itself; writes nothing. Would be the dry-run instrument if a read-only DSN were mounted.",
    "FACTORY_DATABASE_URL_RO": "NOT SET in this session's environment. Checked: process env (absent), any .env under P:/tmp (none exist), _catalog/credential_access_index.json (no entry -- it is a Secret Manager secret per _inbox/2026-09-16_scaleup-ld_farm_architecture_report.md). The teardown lane in this same session family hit the identical wall and marked its store-dependent numbers UNMEASURED. This lane does the same rather than substituting FACTORY_DATABASE_URL or any other credential, and rather than mounting a new secret (an operator stop point)."
  },

  "contradictionFoundInTheDispatchAndHowItIsBeingHandled": {
    "what": "Mission step 1 requires the rail gate to refuse a zero-earned rail that is live program-wide and whose county cells are unaccounted. Mission step 1 ALSO requires test/gate-county-scoped-rail.test.mjs to stay green, calling it the empirical proof of the county-scoped property. Those two requirements are mutually exclusive: that file's FIRST test asserts exactly the shape now required to refuse -- `evaluateRailGate(cells(['unaccounted','unaccounted','unaccounted']), 'maxImperviousCoverPct', {programWideDeclaredAheadRailKeys: []})` must read excludedDeclaredAhead + ok:true + 'excluded-not-applicable' (lines 31-37), and its LAST test re-states the same for Bastrop (lines 166-176).",
    "rootCause": "The dispatch's own parenthetical in step 1 ('the way maxImperviousCoverPct's out-of-scope sweep writes them outside Travis') is contradicted by gate-exclusion-classifier.mjs's live-verified 2026-09-16 note: that writer never writes ANY cell outside Travis, not even not-applicable -- Hays reads 100% 'unaccounted'. So the writer whose precedent the county-scoped test protects does NOT produce the all-not-applicable shape the dispatch says survives; it produces precisely the shape P-252 says must refuse.",
    "resolution": "The mission's own falsifier 2 is consistent with the fix and is the property actually being preserved: 'The all-not-applicable maxImperviousCoverPct fixture stays an exclusion.' The all-UNACCOUNTED assertions in the county-scoped test encode the defect P-252 closes, so those two tests are amended (with the reason written into the test), and the surviving county-scoped property is carried by a new all-not-applicable fixture. Reported prominently in CP2 and the close rather than silently rewritten.",
    "consequenceNamedNotHidden": "maxImperviousCoverPct is in PUBLISH_FLOOR_RAIL_KEYS (SLATE_1B). Under the fix it must refuse its floor rail in every county where its cells are all unaccounted -- i.e. all five non-Travis counties. The publish consequence is stated in bold in the close, as the mission's dry-run section requires."
  },

  "falsifiersPreRegistered": [
    {
      "id": 1,
      "text": "The all-unaccounted fixture refuses at both layers (rail gate AND pre-bake check). If it clears either, the row is not done.",
      "predicted": "PASS. Rail gate: new branch returns ok:false with unaccountedCount = the unaccounted cells. Pre-bake: the row then reads 'refuse' -> RAIL_REFUSED. Both are asserted in the checked-in fixture test, and the pre-bake half is asserted through evaluatePreBakeReadiness with the verdict the rail gate actually produced (end-to-end through toGateVerdictKind), not a hand-written string."
    },
    {
      "id": 2,
      "text": "The all-not-applicable maxImperviousCoverPct fixture stays an exclusion. If it refuses, the county-scoped property is broken.",
      "predicted": "PASS. Cell kinds are not-applicable only, so no unaccounted cell exists and the new branch is not taken; classifyExclusion's live branch sees notApplicable === cellCount and returns excluded-not-applicable with a basis naming the county's own cells. If it refuses, the fix is too wide and I am to say so."
    },
    {
      "id": 3,
      "text": "A rail with zero cells in a county still returns RAIL_NEVER_FILLED (P-195 unchanged).",
      "predicted": "PASS by construction -- the cells.length === 0 branch is checked before the new one and its return shape is untouched. Asserted explicitly, including the existing county-scoped test that covers it."
    },
    {
      "id": 4,
      "text": "The dry run's verdict changes are exactly the rails that are live elsewhere and unaccounted in that county. A change outside that set means the logic is wider than the claim.",
      "predicted": "UNMEASURED. FACTORY_DATABASE_URL_RO is not in this environment and I will not substitute another credential, so no (county, rail) verdict can be measured. The claim the dry run would have tested is instead argued from the code's own branch structure (the new branch is reachable only when !isEarnedCell for every cell AND the rail is NOT in the program-wide declared-ahead set AND at least one cell is unaccounted), and the exact reproduction command is named in the close for the integration seat."
    },
    {
      "id": 5,
      "text": "The divergence test between the two evaluateRailGate copies fails when you edit one.",
      "predicted": "PASS. The factory copy is the one that runs, so it is the one edited; the pinned-engine semantics are checked in as an executable fixture (the pre-fix copy at 9171279, which ENGINE_PIN.json declares byte-identical to hauska-engine 2cc2057's compiled emit). The test asserts the disagreement set between the two copies is EXACTLY one fixture -- so editing either copy (including regenerating the pin after an engine-side fix) changes that set and fails the test. I will prove it by editing a copy in a scratch run and recording the failure output."
    }
  ],

  "scopeDiscipline": [
    "FAN-DEPTH 0 -- this lane launches NO sub-agents; spawned 0, maxDepth 0.",
    "No deploy; no job run against a writing store; no store write of any kind.",
    "No rail's DATA fixed (agValuation, derived rails are P-266/P-267).",
    "requireCountyPopulation is not touched.",
    "Will not touch P-284's files (stage/cost telemetry, migrations + job-runner record writing)."
  ],

  "durableFindingsThisLaneAlreadyCarries": [
    "GROUND-TRUTH 2026-09-16T17:39Z: the executed evaluateRailGate is the factory's vendored src/lib/parcel-record-engine/publish-gate.js, reached via gate-rail-cli.mjs invoked as a node subprocess by publish-gate-sched.mjs runGateCli. Evidence: publish-gate-sched.mjs GATE_CLI_PATH = <repo>/src/lib/parcel-record-engine/gate-rail-cli.mjs, and that CLI imports evaluateRailGate from ./index.js.",
    "GROUND-TRUTH: production parcel_gate_verdict CHECK constraint admits ('pass','refuse','excluded') per migrations/0009, widened to the three excluded-* strings by migrations/0011a whose own header records it was NOT applied to production by P-201's lane. 'refuse' -- the only new verdict this lane's rail gate writes -- is inside the ORIGINAL constraint, so P-252 needs no migration of its own.",
    "GROUND-TRUTH (code read, not measured): legacy-design-tools origin/main resolveAllowlist() returns 'legacy' for any (county,rail) not in the code-owned PARCEL_RECORD_SLATE regardless of verdict, and 'refused' (which serves the same legacy value, visibly) for a slated pair whose verdict is not 'pass'. So a flip to 'refuse' changes the SERVING PATH only for slated pairs that were previously 'pass' -- and this lane's change cannot move a 'pass' pair, because a pass requires at least one earned cell and zero unaccounted cells, while the new branch fires only when there is no earned cell at all."
  ]
}
