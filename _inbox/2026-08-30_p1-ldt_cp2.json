{
  "checkpoint": "CP2",
  "lane": "P1-LDT",
  "card": "P1 edge honesty on the wire — DrawEdge.state union, retired-edge filter, sourceAdapter passthrough",
  "planRows": ["P-92"],
  "writtenAt": "2026-08-30T22:42:00Z",
  "seat": "P1-LDT executor in P:/seat-worktrees/property/legacy-design-tools-p1-edges on seat/property-ctx-p1-ldt; no sub-agents; no commit, push, PR, deploy, mint, or store write; p2b-serve tree not opened",
  "snapshot": {
    "legacy-design-tools": "P:/seat-worktrees/property/legacy-design-tools-p1-edges, branch seat/property-ctx-p1-ldt, base 28969a36cc55b32247aeb3d362ae4f2d3054bf49, uncommitted card edits",
    "cp1": "_inbox/2026-08-30_p1-ldt_cp1.json"
  },
  "tscFailEvidence": {
    "when": "after DrawEdge.state was widened, before the fail-probe was removed",
    "file": "artifacts/api-server/src/lib/__tests__/drawEdgeState.singleton-probe.ts",
    "assertion": "type _OnlyPresent = DrawEdge['state'] extends 'present' ? true : never; const _singletonStillPresent: _OnlyPresent = true",
    "error": "src/lib/__tests__/drawEdgeState.singleton-probe.ts(9,14): error TS2322: Type 'true' is not assignable to type 'never'.",
    "whatThatProves": "DrawEdge.state is no longer a one-inhabitant literal. The type that used to make that assignment compile now reduces to never.",
    "probeDisposition": "deleted after the failure was captured so it is not left as a permanent red file"
  },
  "cardFileTsc": {
    "instrument": "tsc -p tsconfig.json --noEmit, then Select-String parcelDraw|boundaryEdgeFact|drawEdge",
    "cardFileHitsAfterWriterComplete": 0,
    "fullProjectExit": 2,
    "fullProjectReason": "worktree has no own node_modules; junctions to P:/legacy-design-tools node_modules (feature branch ~200 commits behind origin/main 28969a36). Unrelated files fail on missing workspace types. Not a card-file diagnostic.",
    "falsifier": "a hit on parcelDrawStub.ts, parcelDrawFromReads.ts, boundaryEdgeFactRead.ts, or their tests after the probe was deleted"
  },
  "tests": {
    "command": "vitest run src/lib/__tests__/parcelDrawStub.test.ts src/lib/boundaryEdgeFactRead.test.ts src/lib/__tests__/parcelDrawFromReads.test.ts",
    "result": "55 passed / 55, 3 files, duration 657ms",
    "DATABASE_URL": "postgres://unused:unused@localhost:5432/unused (dummy so @workspace/cad-ingest can import; not a live connection)"
  },
  "bothArms": {
    "refutedCannotEmitPresent": "assemble of GOLD edge 1 with neighborClaim.state=refused emits state=refused, neighbor kept, agentGuidance set; expect(state).not.toBe('present')",
    "goldReciprocalMayStayPresent": "gold edge 1 with neighborClaim.state=present emits state=present neighbor=48021:34169 sourceAdapter=descriptor-fixture",
    "retiredAbsentActivePresent": "assemble mixed list omits entityId boundary:99; interpret mixed list keeps only GOLD_FRONT; all-retired interpret is malformed-atom empty-after-filtering; retired-only assemble refuses the ring",
    "unverifiedNotRefused": "gold edge 2 (ROW + 34121, no claim) is unknown, neighbor kept, not refused — ROW invariant not added",
    "sourceAdapterOnWire": "every served DrawEdge has sourceAdapter; fromReads drops retired and keeps depth-warm-v1 on the active edge"
  },
  "designHeld": [
    "no adjacencyKind ROW/alley => neighbour NULL",
    "no writer / mint",
    "no p2b-serve tree",
    "live fromReads still invents no neighborClaim, so live ids serialize unknown until X1 feeds a claim"
  ]
}
