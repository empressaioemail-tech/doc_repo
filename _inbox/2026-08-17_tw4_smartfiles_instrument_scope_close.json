{
  "row": "TW-4",
  "plan": "_rd_disclosure_twin/08_build_scope.md",
  "planRow": null,
  "planRowBasis": "CANON_OVERRIDE: Smart Markets is unregistered R&D with bounded PRs, so no PLAN-ROW exists by design. Operator-ruled 2026-08-17.",
  "repo": "empressaioemail-tech/smart-files",
  "date": "2026-08-17",

  "branch": "tw4/instrument-scope",
  "headSha": "b306cd5f235f027aa6805ae3af523e37681b3384",
  "baseSha": "abd77f3",
  "prUrl": "https://github.com/empressaioemail-tech/smart-files/pull/5",
  "prState": "OPEN, mergeable, NOT MERGED. Owning lane (OPS-17 Smart Files / Plan Review seat) reviews before merge and before any Neon apply.",

  "ciConclusion": "success",
  "ciDetail": {
    "runId": 32087147131,
    "workflow": "Test",
    "jobs": [
      { "name": "Unit", "conclusion": "success" },
      { "name": "HTTP round trip", "conclusion": "success" }
    ],
    "note": "This repo had NO CI before this PR. `gh api repos/empressaioemail-tech/smart-files/actions/workflows` returned {\"total_count\":0,\"workflows\":[]} and main is not branch-protected. `.github/workflows/test.yml` is added by this PR and is flagged in the PR body as additive scope the owning lane may strip.",
    "roundTripInCi": "39/39 checks passed against a postgres:16 service with sql/001..004 applied",
    "absenceGuardInCi": "PASS: not-applicable is still refused by the absence CHECK"
  },

  "writePathChanges": [
    "src/store.mjs createFolder: INSERT hardcoded VALUES ($1, 'tenant', $2, $3, $4) -> parameterised on a resolved scope, VALUES ($1, $2, $3, $4, $5).",
    "src/store.mjs uploadFileToFolder: guard `folder.scopeType !== 'tenant' || folder.scopeId !== orgId` -> assertFolderInScope() pair comparison; the tenant path keeps the exact error string 'folder is not in this org'.",
    "src/store.mjs uploadFileToFolder: document INSERT hardcoded VALUES ($1, 'tenant', $2, ...) -> parameterised; both buildSmartFileEntityId call sites (initial and the dedupe loop) take the resolved scope instead of the literal 'tenant'.",
    "src/store.mjs createShare: same tenant-hardcoded folder guard -> assertFolderInScope().",
    "src/store.mjs: new exported resolveWriteScope({scopeType, scopeId, orgId}); defaults to 'tenant', validates via identity.assertScopeId, throws status 400 on a bad scope.",
    "src/store.mjs: version provenance sourceLabel is 'instrument-write' on instrument writes and stays 'qa-upload' on tenant writes.",
    "src/server.mjs listFolders: allowlist literal ['jurisdiction','tenant','site'] -> SMART_FILE_SCOPE_TYPES, plus scopeIdIsValid() on the scopeId.",
    "src/server.mjs: new requireWriteScope(body,res). Absent or 'tenant' scopeType keeps requirePersona unchanged; 'instrument' takes scopeId from the body, validates it, and writes as the INSTRUMENT_SERVICE_ACTOR ('instrument/service'). Wired into POST /folders and POST /folders/:folderId/files.",
    "src/cid.mjs buildFolderId(orgId, slug) -> buildFolderId(scopeId, slug, scopeType = 'tenant'). Default keeps every existing folder id byte-identical; instrument rooms are folder:instrument:<node_id>:<slug>.",
    "src/actors.mjs: new INSTRUMENT_SERVICE_ACTOR = actorKey('instrument','service')."
  ],

  "validatorDesign": {
    "shape": "sec_ or iss_ followed by a 26-character canonical Crockford base32 ULID",
    "regex": "/^(?:sec|iss)_[0-9ABCDEFGHJKMNPQRSTVWXYZ]{26}$/",
    "crockford": "Alphabet omits I, L, O and U by design; each exclusion is covered by its own rejection case in identity.test.mjs.",
    "colons": "Excluded by the character class, so an instrument scopeId can never swallow a segment under the last-segment-is-slug parser. parseSmartFileEntityId('smartfile:instrument:<node>:extra:doc') returns null.",
    "extensionPoint": "New SCOPE_ID_VALIDATORS table in src/identity.mjs maps scopeType -> rule. jurisdiction keeps the pre-existing FIPS regex; tenant and site are `() => true` beyond non-empty; instrument gets its own named function isInstrumentNodeId. The table, not SMART_FILE_SCOPE_TYPES, is the extension point, per the generalization question in 08_build_scope.",
    "writableScopes": "New WRITABLE_SCOPE_TYPES = ['tenant','instrument']. Names what was already true: jurisdiction and site documents are produced elsewhere and are read-only through this service.",
    "notReused": "Does NOT reuse JURISDICTION_FIPS_RE and does NOT reuse the tenant free-string rule. isInstrumentNodeId('48021') === false and isInstrumentNodeId('acme') === false, both asserted."
  },

  "tenantUntouchedProof": {
    "rule": "tenant scopeId remains ANY non-empty string. No tenant regex added.",
    "slugsExercised": ["icc-demo", "acme", "empressa", "template-city", "g58-probe"],
    "method": "All five driven through the real HTTP routes on identical baselines, before (origin/main abd77f3) and after (branch b306cd5). icc-demo, acme, empressa and template-city ran the full create-folder / upload / read / list-folder-files / list-folders path via their QA personas. g58-probe has no QA persona, so it ran the read path: GET /folders?scopeType=tenant&scopeId=g58-probe and GET /files/smartfile:tenant:g58-probe:isolation-note.",
    "rowsIdentical": "scope_type|scope_id|doc_slug|access_policy|jurisdiction_fips|created_by identical before and after, run tag normalised: acme|tw4-acme-RUNTAG|tenant-private||acme/joe ; empressa|...|empressa/nick ; g58-probe|isolation-note|tenant-private|| ; icc-demo|...|icc-demo/reviewer ; template-city|...|template-city/g71-calendar",
    "beforeRun": "31/33 checks, the 2 failures both being the instrument scope that does not yet exist",
    "afterRun": "39/39 checks",
    "pinnedByTest": "identity.test.mjs 'every live tenant slug still builds and parses' and 'tenant accepts shapes no other scope would' fail if anyone adds a tenant regex.",
    "jurisdictionFipsOnInstrument": "NULL. Verified by SQL on the instrument row (jurisdiction_fips empty) and asserted by a DO block in sql/004 that raises if any non-jurisdiction row ever carries a FIPS."
  },

  "httpRoundTrip": {
    "verdict": "PASS 39/39",
    "environment": "Local throwaway PostgreSQL 18 cluster on 127.0.0.1:55432, database built clean from sql/001..004 plus scripts/ci_probe_seed.sql. Reproduced in CI on postgres:16.",
    "prover": "scripts/scope_roundtrip_probe.mjs (committed). Exercises real routes only; never starts a server itself; exits 0/1.",
    "instrumentPath": [
      "POST /api/smart-files/folders {scopeType:instrument, scopeId:sec_01JCZK8QW9V4T6XH2NBGRPY5MD} -> 201 folder:instrument:sec_01JCZK8QW9V4T6XH2NBGRPY5MD:sec-filings-clean",
      "POST /api/smart-files/folders/:folderId/files -> 201 smartfile:instrument:sec_01JCZK8QW9V4T6XH2NBGRPY5MD:tw4-10k-clean",
      "GET /api/smart-files/files/:entityId -> 200 status=held scopeType=instrument",
      "GET /api/smart-files/folders/:folderId/files -> 200 files=1",
      "GET /api/smart-files/folders?scopeType=instrument&scopeId=sec_... -> 200 folders=1 (listFolders sees it)",
      "GET /api/smart-files/files/:entityId/placements -> 200 placements=1"
    ],
    "negativeChecks": "8 malformed instrument scopeIds rejected 400 at POST /folders (lowercase ULID, wrong prefix, no prefix, 25 chars, Crockford-excluded I, extra colon, FIPS shape, tenant slug); listFolders rejects a tenant-shaped instrument scopeId 400; a tenant persona uploading into an instrument room 403; an instrument scope uploading into a tenant room 403.",
    "serverLifecycle": "Every run started the server in the background, curled with --retry-connrefused, ran the probe, killed the PID and waited on it, then confirmed the port was dead. No foreground server, no deploy, every command exited."
  },

  "migrationSql": {
    "file": "sql/004_instrument_scope.sql",
    "widens": [
      "smart_file_documents_scope_type_check -> ('jurisdiction','tenant','site','instrument')",
      "smart_file_folders_scope_type_check -> ('jurisdiction','tenant','site','instrument')",
      "smart_file_placements_target_type_check -> ('folder','parcel','project','asset','permit','meeting','instrument')"
    ],
    "alsoUpdatedInPlace": ["sql/001_foundation.sql", "sql/002_rooms.sql"],
    "notWidened": "smart_file_absence_determinations. Verified after apply: CHECK (verdict = ANY (ARRAY['absent-verified','lookup-failed'])). INSERT of 'not-applicable' still raises, locally and in CI. not-applicable stays a twin-layer concept; if Plan Review meets the same wall the store changes once, deliberately.",
    "assertion": "DO block raises if jurisdiction_fips is non-null on any row whose scope_type is not 'jurisdiction'.",
    "rerunnable": "Every constraint is DROP CONSTRAINT IF EXISTS before ADD. Applied twice locally, second run exit 0.",
    "targetIfApproved": "files Neon branch snowy-bread-83475727 (project smart-files) ONLY."
  },

  "neonApplied": false,
  "neonNote": "Not authorized and not attempted. No Neon credential was read or used. cortex-prod untouched; migrations 0078-0081 untouched; no DROP. All proof ran against a throwaway local PostgreSQL cluster and a CI postgres:16 service container.",

  "testsProvenByRemoval": [
    {
      "guard": "SCOPE_ID_VALIDATORS.instrument (the validator itself)",
      "removal": "instrument: isInstrumentNodeId -> instrument: () => true",
      "failure": "npm test 15/18, 3 tests fail. Over HTTP, all 8 malformed scopeIds return 201 instead of 400 and listFolders accepts 'acme' as an instrument id (200). Probe 30/39.",
      "restored": "18/18, probe 39/39"
    },
    {
      "guard": "the tenant free-string rule",
      "removal": "tenant: () => true -> tenant: (id) => /^[a-z]+$/.test(id)",
      "failure": "npm test 15/18. Over HTTP icc-demo and template-city folder create return 400, g58-probe listFolders returns 400, and the seeded g58-probe read degrades to verdict=not-sought, a SILENT honest-absence rather than a loud error. That is what a tenant tightening would look like in production.",
      "restored": "18/18"
    },
    {
      "guard": "migration 004 (the CHECK widening)",
      "removal": "Full branch code run against a database built from origin/main sql/001..003 only",
      "failure": "npm test 18/18 GREEN; POST /folders scopeType=instrument returns 500 'violates check constraint smart_file_folders_scope_type_check'.",
      "restored": "004 applied, probe 39/39"
    },
    {
      "guard": "the write path (THE FINDING the owning lane named)",
      "removal": "git checkout abd77f3 -- src/store.mjs src/server.mjs, keeping the widened enum, the applied migration and the new identity.mjs",
      "failure": "npm test 18/18 GREEN and the migration already applied, yet POST /folders scopeType=instrument returns 400 unknown_persona. The enum-plus-validator change passes its own tests and fails the HTTP round trip, exactly as called.",
      "restored": "probe 39/39"
    },
    {
      "guard": "the listFolders allowlist",
      "removal": "SMART_FILE_SCOPE_TYPES.includes(scopeType) -> ['jurisdiction','tenant','site'].includes(scopeType)",
      "failure": "npm test 18/18 GREEN; instrument create, upload and read all PASS, but GET /folders?scopeType=instrument returns 400. The room exists and is invisible. Probe 38/39.",
      "restored": "39/39"
    }
  ],

  "removalProofConclusion": "Proofs 3 and 4 are the symmetric halves: neither the migration nor the code is sufficient alone. Proof 3 also establishes the deploy order, since this branch is safe against the un-migrated Neon (tenant untouched, instrument writes 500 until 004 lands).",

  "reviewerDecisions": [
    "listFolders read-path tightening. scopeIdIsValid is applied uniformly, so GET /folders?scopeType=jurisdiction&scopeId=bastrop now returns 400 where it previously returned 200 {folders:[]}. tenant and site are provably unaffected because their rule is non-empty, which the route already required. No live caller uses jurisdiction scope here (the MCP blueprint tool and the QA BFF both pass tenant). Narrowable to instrument-only on request.",
    ".github/workflows/test.yml is additive scope. The repo had no CI and no branch protection; the program merge rule gates on a conclusion string, so a gate was built. Strip it if the owning lane would rather add CI on its own terms."
  ],

  "remaining": [
    "Owning lane review of PR #5, then merge, then apply sql/004_instrument_scope.sql to Neon snowy-bread-83475727. Both gates are the owning lane's, not this seat's.",
    "Paired control named in the A-029 reply, to run after the Neon apply: existing tenant:icc-demo rooms still read, and the leftover folder:tenant:template-city:public-meetings stays unread rather than deleted.",
    "Version provenance on instrument writes is a fixed sourceLabel 'instrument-write'. TW-6 needs real provenance (accession number, source URI) and will widen the write path once, deliberately. Left out here rather than opening an unbounded input surface on this row.",
    "Instrument rooms cannot be shared. createShare generalised to a scope-pair comparison so an instrument folder correctly 403s a tenant persona, but the POST /folders/:id/share route stays persona-only. Correct-by-absence rather than half-built; a later row can open it.",
    "Instrument documents default to access_policy tenant-private via the column default. TW-6 wants public-free on compiled filings, which is a write-path parameter this row did not add.",
    "web/ QA UI is tenant-only and untouched. It does not render instrument rooms.",
    "TW-6 remains blocked on this merge plus the TW-7 distribution run."
  ],

  "blocked": [],

  "workingEnvironment": {
    "clone": "Fresh clone of origin/main at P:/tmp/tw4-smartfiles. P:\\smart-files was NOT used, NOT reset, NOT cleaned, NOT checked out over.",
    "database": "Throwaway PostgreSQL 18 cluster initdb'd into the session scratchpad on port 55432. Four scratch databases: _before (origin/main baseline), main (after), _no004 (pre-migration proof), _clean (final clean-room). No Neon DSN read, no ~/.empressa credential touched."
  }
}
