# Site-plan export samples — `48029:105129`

Generated by `packages/engine-core/scripts/generate-site-plan-sample.mjs` in `hauska-engine`
(Wave 1 on branch `feat/site-plan-export`; Wave 2 PDF added on branch `feat/site-plan-pdf`).
Regenerate with:

```
cd packages/engine-core
npx tsx scripts/generate-site-plan-sample.mjs
```

## Fixture disclosure (read before using these files for anything)

**Synthetic fixture, not a live TxGIO/USGS export.** This sandbox has no outbound network
egress (documented in `_inbox/2026-07-25_site_plan_export_STATUS.md` Wave 0 evidence: DNS
resolves but TLS to Cloud Run fails, no `TXGIO_DATABASE_URL`/`DATABASE_URL`/`GATE_SERVICE_TOKEN`,
both hauska MCP servers report `serverStatus: error`). Concretely:

- **Parcel ring** is an approximate ~62 ft x 125 ft rectangle (~7750 sqft, matching the ~7756
  sqft lot size on record) centered near the parcel's public street address (1127 N Pine St,
  San Antonio TX 78202, Dignowity Hill). It is NOT a surveyed or TxGIO-sourced boundary.
- **DEM** is a synthetic 6x6 gentle-slope grid in the plausible San Antonio elevation band
  (~193-198 m), NOT a live USGS 3DEP clip.
- **Setback values ARE real and atom-chain verified**: front=10 ft, side=5 ft, rear=20 ft,
  cited `san_antonio_tx/udc/35-310.01`. These match both the 2026-07-24 live StoragePort write
  (`_inbox/2026-07-24_post_breadth_three_gaps_MILESTONE.md`) and an independent re-derivation
  from `packages/adapters/src/local/setbacks/san-antonio-tx.json` this session — see STATUS
  Wave 0 evidence for the verbatim chain.
- **STREET layer is honest-absence**: no road-anchor atom exists in hauska-engine for this
  parcel, so the layer is declared (present in the DXF LAYER table / as an IFC annotation
  category) but carries zero drawn geometry, not a fabricated street.
- **Flood zone is honest-unavailable (Wave 2)**: the FEMA NFHL adapter needs live network
  egress this sandbox does not have (same TLS/schannel hazard as the DEM/TxGIO fetches above —
  verified again this session), so the PDF's summary block and provenance panel both say
  "unavailable" rather than asserting a zone. **Address, county name, and zoning district
  (R-6) ARE the real, dispatch-verified values** for this parcel (same status as the setback
  values above), passed into the composer as caller-supplied descriptor/zoning inputs.
- **Buildable area is labeled PROVISIONAL (planner HOLD-1 fix, 2026-07-25)**: this parcel's
  4-edge ring has no caller-supplied front-edge hint, so the composer's own front-edge basis
  falls to the disclosed geometric heuristic (shortest roughly-parallel edge pair, south-most
  side) rather than a resolved road-anchor. The 4,940 sq ft buildable-area figure in the
  summary block is printed alongside an explicit provisional note rather than as a bare
  number, and the same note fires for a uniform-minimum fallback basis or an independently
  provisional buildable-envelope atom — only a caller-resolved `frontEdgeIndex` with no
  contradicting envelope outcome omits it.

## Files

- `48029_105129_site_plan.dxf` — AC1015 (R2000) layered DXF: `PROPERTY_LINE`, `DIMENSION`,
  `SETBACK`, `CONTOUR`, `ELEVATION_LABEL`, `STREET` (empty, honest-absence), `NORTH`. Entities
  carry `HAUSKA` XDATA citing the source atom (setback rule atom DID, geometry source ref, or
  the DEM citation) per entity.
- `48029_105129_site_plan.ifc` — IFC4, `IfcProject -> IfcSite -> {closed-solid terrain mass,
  annotation layers}`. Terrain is a single closed `IfcTriangulatedFaceSet` (`Closed=True`):
  top = shared triangulation, vertical skirts from the boundary loop down to a flat bottom
  ~0.46 m (1.5 ft) below min-Z, bottom cap — one watertight solid, not a thin surface. Property
  line, setback offset, and contour annotations are `IfcAnnotation` entities grouped by
  `IfcPresentationLayerAssignment` (`PROPERTY_LINE`, `SETBACK`, `CONTOUR`), each carrying its
  source citation in `Description`. Vertical datum: NAVD88 (declared on `IfcProjectedCRS`).
- `48029_105129_site_plan.pdf` — **Wave 2.** Two-page sheet rendered from the SAME
  `SitePlanModel` the DXF/IFC emitters above read (`composeSitePlanModel` — one composer, three
  renderers; CAD and PDF cannot diverge). Page 1 is the drawing (property boundary + corners,
  per-segment dimensions, setback offset + F/S/R labels, contours + elevation labels, the
  honest-absence STREET note, north arrow, scale bar). Page 2 is the summary block (parcel ID,
  address, county, zoning district, lot area, setbacks, buildable area, flood zone, elevation
  range with NAVD88 stated), the provenance/citation panel (every layer's source + as-of +
  confidence, including the `san_antonio_tx/udc/35-310.01` setback code cite), and the exact
  honesty line: "Derived from public GIS records. Not a boundary survey. Not for legal record."
- `48029_105129_manifest.json` — machine-readable fixture disclosure, setback values, bbox,
  entity/vertex/page counts, the IFC spatial-validation report (`ok: true`), and the full
  `summary` block (lot/buildable area, elevation range, zoning, flood-zone honesty) from this
  run.

## What live proof still needs

A genuine live sample (from a real TxGIO parcel-ring resolve + real USGS 3DEP DEM fetch, plus a
real FEMA NFHL flood-zone read for the PDF) needs either: (a) network egress to
`hauska-retrieval-api` / TxGIO / USGS / `hazards.fema.gov` from wherever this is run (re-checked
this session — same `schannel: CRYPT_E_NO_REVOCATION_CHECK` TLS failure as Wave 0/1), or
(b) `TXGIO_DATABASE_URL`/`DATABASE_URL` + `GATE_SERVICE_TOKEN` env vars wired for a direct
StoragePort + adapter run outside this sandbox. Planner has DB/MCP paths this sandbox does not
per the Wave 0 evidence note; that is the place to re-run `authorParcelSitePlanExport` for
`48029:105129` against live TxGIO ring geometry, a real DEM clip, and a real FEMA NFHL read
before Wave 4 finish check-in, and to diff the resulting DXF/IFC/PDF against these fixtures for
spot-checking layer/annotation/summary structure only (not for geometry or flood-zone accuracy,
since the ring, terrain, and flood-zone-unavailable verdict here are synthetic/sandbox-limited).
