---
id: 2026-08-29_p91_claude_design_mockup_grade
title: Grade of Claude Design companion mockup
date: 2026-08-29
status: punched
plan_row: P-91
source: P:/tmp/Smart Site MCP Companion (standalone).html
unpacked: P:/tmp/ss-mcp-companion-unpacked.html
brief: _inbox/2026-08-29_p91_claude_design_brief.md
---

# Grade

Verdict: accept as the product picture. Punch applied 2026-08-29: same mockup, four bindings only. Open `P:/tmp/Smart Site MCP Companion (punched).html`. This is a mockup grade, not customer-done. Do not cut a build card until Nick says build.

The standalone file is a Claude Design bundle. The inner document is the thing to grade.

## Frames

| Frame | Grade | Note |
| --- | --- | --- |
| F1 Paste | met | Three queries, two node ids, zzzz kept verbatim, guest well `#1c1c1c` named. |
| F2 Board | partial | Five glyphs present. Legend words swap unknown and unread. |
| F3 Parcel | partial | Ring from gold points. Three sides labeled PINE. Header node id is gold. |
| F4 Listing | met as story, not as identity | Split is right. Panel is a collapsed stand-in, not byte-identical to F3. |
| F5 Empty | met | Title plus one sentence. No skeleton. No search box. |
| F6 Failures | met | Dead Open retries with warn. Honest miss has no control and the empty glyph. |
| Mobile F2/F3 | met | Narrow crops exist. |
| Inventory + tokens + four copies | met | Asked artifacts are in the file. |

Hard nos held: no map, no 42%, no cream, no lime, no Hauska, no sales column in the panel.

## Punch list (rule before build)

1. **Legend swap.** Hatch is labeled "Not read yet." Gold dot is labeled "New since you last looked." Bind is hatch = unknown, gold dot = unread (fetch not attempted). Do not ship the swap. Drainage in F3 repeats the same collapse.
2. **Gold leak on the node id.** F3 header paints `48021:34137` as `--ss-gold`. Gold is the mark and the unread dot. Openable ids stay `--ss-atom`.
3. **Three Pines.** Gold edges are rear alley, neighbor `34169`, front PINE (`48021:road:15113284`), side_corner ROW. The brief said "three of four front a road (Pine)" and the mockup followed that. The dump wins. Label front as PINE, side_corner as ROW, rear as alley, west as neighbor 34169. Invented road nodes `4429` / `4430` / `4431` are out.
4. **Save to screen.** That label is the opposite of A12. The button is Save property. Saving does not write the screen.
5. **F4 identity.** Keep the transcript split. Do not treat the collapsed F4 chrome as the parcel layout. F3 is the parcel layout.
6. **Oxygen files.** Helmet ships bundled Oxygen woff2. Frames use the system stack. Do not carry the faces into the iframe.

## Copies (keep, then tighten)

- Envelope: `Withheld — setbacks unruled` plus the partial-set sentence. Human. Not `atom_path_pending`. Keep the meaning. Drop the em dash if we file it.
- Unresolved: `Situs unresolved` plus stay-as-typed. Action slot: `Nothing to open until this resolves`. Keep.
- Dead Open: `Open did not reach me`. Retry. Keep.
- Honest miss: `Not on file in Bastrop`. No retry. Keep.

## Leftovers the mockup already named

Guest well `#1c1c1c` has no `--ss-*` name. Hatch is a derived stripe, not a kit primitive. Both stay leftovers.

Helmet also dumps a warm Stone `:root` (`#2A2A28` / `#323230`) then a second `:root` with live PE hexes. Frames read the second. Do not copy the first.

## What this unblocks

One card after punch: board + parcel drawing + listing split, as one surface, against F1 F2 F3 F5 F6 and the F4 split only. Open is still the live click leftover. Do not start that card from this grade alone.
