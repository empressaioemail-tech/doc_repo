{
  "lane": "p308-join-miss-companions",
  "planRows": ["P-308"],
  "status": "closed-partial",
  "at": "2026-09-17T18:20:00Z",
  "seat": "cente-vsc-p308",
  "repo": "hauska-engine",
  "dispatch": "_dispatches/2026-09-17_p308-join-miss-companions_dispatch.md",
  "subAgents": { "spawned": 0, "maxDepth": 0, "fanDepth": 0, "note": "FAN-DEPTH 0; this lane launched no sub-agents and no deep agent chain. Both numbers are literal counts, not estimates." },

  "probe": {
    "notApplicable": "engine build lane; graded by the factory re-vendor's dry run",
    "whyThisIsTheDispatchsOwnWord": "the dispatch's Close section prescribes exactly this object for this lane: `probe`: `{\"notApplicable\": \"engine build lane; graded by the factory re-vendor's dry run\"}`. OPS-24 Law 1's parenthetical says notApplicable is for read-only reviews, which this is not, so the two instructions disagree; the dispatch is the lane-specific instruction and is followed, and the disagreement is named here rather than hidden. scripts/enforcement/probe-close-gate.mjs resolves it mechanically: P-308 falls in its OPS-24 range 252-320 and therefore in PREDICATE_DEBT, and a close whose gated rows are ALL in PREDICATE_DEBT is allowed on probe.notApplicable with the debt printed (read at origin/main, 2026-09-17). No surface predicate exists for P-308 because the row's surface is the factory's fill job, not a customer-facing parcel card.",
    "noSurfaceProbeRun": "scripts/surface-probe.mjs was NOT run by this lane and no surface-probe artifact is cited, because this lane changes no serving surface: it changes an engine module the factory vendors, and the fix is invisible to a customer until the factory re-vendors. Claiming a probe artifact here would be a false citation."
  },

  "missionPremise": "The finding this lane rests on is the P-266/P-268 close, `_inbox/2026-09-17_p266-p268-factory-open-rails_close.json` (status closed-partial), whose leave_behind names this exact gap as OPEN: 'the engine-side half of the companion gap -- src/lib/parcel-record-engine/ingest-existing.js, applyCadJoinMiss stamps the 13 CAD scalars and neither companion (acreageMethod / landUseSource). The job-level completion fixes these six counties only; the next consumer of the engine gets the same gap.' The matched path is not new behavior: engine dfdf6fd established the join-miss basis and the matched branch stamps a companion absence beside a null/blank primary; the ASYMMETRY is the join-miss branch, which the factory worked around job-side (factory 1fa850e7, completeJoinMissCompanionsOntoRecords, P-268). PLAN-ROW P-308 (OPS-16) owns the engine half, repo hauska-engine per the dispatch header. Verified at source before building, not taken from the close's prose: the 13-stamp form with no companion stamp, in both this repo at 7b3dda0b and the factory's vendored copy at the pin it runs (2cc2057).",

  "completionPredicate": "Done means all six: (1) `applyCadJoinMiss` stamps `acreageMethod` and `landUseSource` absent-verified with the join-miss basis beside their primaries, never a value, never a refusal, never rewriting an earned companion; (2) a test exists that FAILS on the pre-fix shape, proven by reverting the source and reading the failure; (3) a parity test runs one fixture set through the engine's join-miss path and through the factory's job-level step (its logic copied into the test as the expected-output oracle, factory commit named) and fails on any cell disagreement; (4) the matched-row path is unchanged and still carries a taxYear-bearing basis on both companions; (5) the engine typechecks and its suite is green on the pushed commit; (6) the factory follow-on - re-vendor at the new engine sha, then retire completeJoinMissCompanionsOntoRecords with its call sites enumerated - is written into this close as commands for the integration seat. A stranger can evaluate (1)-(4) from PR 468's diff and its tests, and (5) from the CI conclusion strings below. (6) is written, not performed, which is why the status is closed-partial and not closed.",

  "scopeBasis": "hauska-engine only, and inside it only `packages/engine-core/src/parcel-record/ingest-existing.ts` plus its test file. Deliberate exclusions a successor should read as decisions: (a) no edit to `packages/adapters/src/local/setbacks/` or `services/retrieval-api/` - the dispatch names those as the concurrent P-299/P-282 lane's paths; (b) no edit to hauska-factory at all, including no scratch file - the follow-on is written as commands, and this lane's emit experiment read that repo through `git show` only; (c) no store read or write, no lease, no migration, no deploy, no merge; (d) no change to the matched-row branch and no change to any other rail's emission; (e) no new export - the guard stays module-internal, so the existing structural-scoping test (which asserts applyCadScalar/cadNullVerified/applyCadJoinMiss/cadJoinMissVerified stay unexported) is untouched and index.ts does not change; (f) no change to instantiate.ts, whose optional nowIso parameter already existed and which the parity test only passes through.",

  "pr": {
    "number": 468,
    "url": "https://github.com/empressaioemail-tech/hauska-engine/pull/468",
    "head": "fix/p308-join-miss-companions",
    "headCommit": "532e39f6bff5257e37a2b5bc5aa0286c927ba6f5",
    "mergeSha": null,
    "merged": false,
    "whyNotMerged": "the dispatch puts the merge behind a maintainer decision ('wait for a maintainer decision before merging', CP2). The PR is parked with the evidence posted as https://github.com/empressaioemail-tech/hauska-engine/pull/468#issuecomment-5718950911 and is an OPEN thread for the next lane."
  },

  "ciConclusionStrings": {
    "_rule": "AGENT-CONTRACT section 6: conclusion strings, literally. Both commands with their raw output; merge SHA is null because nothing merged.",
    "checkCount": 1,
    "gh pr checks 468 --json name,state,bucket,link": "[{\"bucket\":\"pass\",\"link\":\"https://github.com/empressaioemail-tech/hauska-engine/actions/runs/35255390349/job/105317639860\",\"name\":\"typecheck + test\",\"state\":\"SUCCESS\"}]",
    "gh run view 35255390349 --json conclusion,status,headSha,event,workflowName": "{\"conclusion\":\"success\",\"event\":\"pull_request\",\"headSha\":\"532e39f6bff5257e37a2b5bc5aa0286c927ba6f5\",\"status\":\"completed\",\"workflowName\":\"ci\"}",
    "whatTheOneCheckCovers": "ci.yml runs `pnpm typecheck` then `pnpm test` (pnpm -r run test) over the monorepo, so the single green check is the typecheck AND the whole suite - not the parcel-record file alone.",
    "mainIsAlsoGreen": "ground-truth 2026-09-17T17:59Z: gh run list --workflow=ci.yml --branch main --limit 5 -> runs 35243439195, 35229576204, 35219904238, 35163185082, 35162864486, all conclusion success."
  },

  "countsWithTheirRules": {
    "diffFiles": { "value": 2, "rule": "git diff --stat 7b3dda0b4212861f62c2dcbd6ca502ef3de1f189 HEAD" },
    "diffInsertions": { "value": 410, "rule": "same command; 65 in ingest-existing.ts, 345 in the test file, 0 deletions anywhere" },
    "testsAdded": { "value": 7, "rule": "it() blocks named '(P-308 ...)' in packages/engine-core/src/parcel-record/__tests__/parcel-record.test.ts, counted by grepping the committed file at HEAD: falsifiers 1, 2, 3a, 3b, 6, 7 and the parity test" },
    "testsInTheFileAfter": { "value": 39, "rule": "pnpm exec vitest run src/parcel-record/__tests__/parcel-record.test.ts -> 'Tests  39 passed (39)'; 32 existed before" },
    "testsFailingOnRevert": { "value": 5, "rule": "_inbox/2026-09-17_p308-join-miss-companions_falsifier4-revert.log: only ingest-existing.ts reverted to 7b3dda0b with the final test file in place -> 'Tests  5 failed | 34 passed (39)': falsifier 1, 3a, 7, 6, parity" },
    "cellsMovedOnAJoinMiss": { "value": 15, "rule": "the falsifier-7 test's assertion on ingestCadOntoRecords(...).cellsMoved: 13 value-scalars + 2 provenance fields; 13 before this change" },
    "cellsMovedByTheFactoryStepAfterTheFix": { "value": 0, "rule": "the parity test's own assertion on the frozen factory oracle's return value when it is fed the engine's post-fix output" },
    "vendoredFilesReproducedByTheDocumentedEmit": { "value": "11 of 13", "rule": "_inbox/2026-09-17_p308-join-miss-companions_revendor-emit-check.log: emit at the pinned sha 2cc2057 vs the factory's vendored copy, CRLF-normalised byte comparison; the 2 that differ are exactly ENGINE_PIN.json.factoryDivergence.files (index.js, publish-gate.js)" },
    "vendoredFilesTheP308RevertChanges": { "value": 1, "rule": "same log, emit at this lane's HEAD vs the vendored copy: ingest-existing.js is the only additional difference" }
  },

  "checkpointArtifacts": {
    "CP1": "_inbox/2026-09-17_p308-join-miss-companions_cp1.json (design / pre-build; falsifiers pre-registered, rule chosen, finding verified at source, the unread cohort figure declared as inherited)",
    "CP2": "_inbox/2026-09-17_p308-join-miss-companions_cp2.json (pilot / mid-work; PR 468 evidence, CI strings, per-falsifier verdicts, the rule-choice adversarial recheck, the local-vs-CI reconciliation)",
    "evidenceLogs": [
      "_inbox/2026-09-17_p308-join-miss-companions_falsifier4-revert.log",
      "_inbox/2026-09-17_p308-join-miss-companions_guard-fires.log",
      "_inbox/2026-09-17_p308-join-miss-companions_preexisting-failures.log",
      "_inbox/2026-09-17_p308-join-miss-companions_revendor-emit-check.log"
    ]
  },

  "falsifier": "The pre-registered falsifiers and their measured verdicts (full detail in CP2): F1 both provenance fields absent-verified with a taxYear-less cad_property basis -> PASS; F2 matched-row companions unchanged, with taxYear -> PASS; F3a an earned companion is never rewritten -> PASS and the guard proven able to fire by removing it alone (guard-fires.log VIOLATION A: the test fails at parcel-record.test.ts:926, source restored from a byte copy and the restore verified by git diff); F3b a primary absent-verified without the join-miss badge never earns a join-miss provenance -> PASS and the guard proven able to fire (VIOLATION B: fails at test line 961); F4 reverting the fix fails the old-shape test -> MEASURED 5 of 39 fail; F5 parity, one fixture set through both paths agreeing cell for cell with the factory step moving 0 cells -> PASS, and it FAILS pre-fix; F6 idempotent replay moves nothing -> PASS; F7 exactly 15 cells move and no others -> PASS. The single most load-bearing falsifier is F4: the whole claim that these tests test anything rests on the pre-fix failure being observed rather than asserted, and it was observed against the FINAL test file (the first run of that experiment was against an earlier test revision and was re-run rather than left stale).",

  "contradicted": "Nothing in the mission premise was contradicted: the dispatch's four verifiable claims (two write paths, the 13-scalar count, the companions present in the matched branch and absent in the join-miss branch, the factory workaround's predicate) all held at source. Three things were corrected rather than confirmed, and they are named so they do not propagate: (1) the pre-registered falsifier 3 was ONE claim in CP1 and had to be SPLIT into 3a and 3b, because as written it could be satisfied by two different mechanisms and neither half was independently provable - a single test asserting both halves passes pre-fix on one of them and hides which half is being measured; (2) a cross-session summary of this lane claimed the lane modified instantiate.ts to add a nowIso parameter - the committed diff (2 files) and `git grep nowIso 7b3dda0b -- .../instantiate.ts` both show the parameter already existed at the start commit; the close records the measured fact; (3) the first falsifier-4 log measured an earlier test revision and its figure was stale - re-running against the final file changed the reading from a filtered subset to 5-of-39. Also declared, not contradicted: the dispatch's Hays cohort figures (acreageMethod 433, landUseSource 636) were NOT re-measured by this lane - they are inherited from the P-266/P-268 lane's lease-held store read and would have needed a factory-store read this lane is not assigned. The residual assumption behind the chosen rule is named in CP2: no producer in this repo can emit a taxYear-less cad_property basis other than the join-miss path, because cadNullVerified refuses to emit without a taxYear.",

  "constraintsHonored": [
    "FAN-DEPTH 0, no sub-agents launched and no deep agent chain: subAgents {spawned: 0, maxDepth: 0}.",
    "Built in a FRESH CLONE at P:/tmp/p308-engine (not a worktree of P:/hauska-engine), branch fix/p308-join-miss-companions cut before any file edit, as the dispatch requires. Scratch git worktrees P:/tmp/p308-emit-src and P:/tmp/p308-emit-head plus their emit directories were created for the re-vendor emit check and removed before the close.",
    "Start commit declared and matched: 7b3dda0b4212861f62c2dcbd6ca502ef3de1f189 is byte-for-byte the 'engine main 7b3dda0b at compile' the dispatch names.",
    "Stayed out of the concurrent lane's paths: no change to packages/adapters/src/local/setbacks/ or services/retrieval-api/.",
    "No merge, no deploy, no re-vendor, no store read or write, no lease, no migration. GPU/DB/net: none.",
    "hauska-factory not edited in any way, including no scratch file; it was read with `git show`/`git grep` against origin/main only, so its local checkout at cb8b5d2 remains untouched.",
    "The clone is registered in _catalog/seat_register.json under the property seat as name hauska-engine-p308 and that entry is REMOVED as part of this close (see docRepoEdits).",
    "doc_repo commits are planner-owned: every doc_repo edit this lane made is left UNCOMMITTED and listed below.",
    "No notification-based waiting: the CI check was polled with a bounded loop and a timeout (max 14 minutes, 25-second interval), never by ending the turn."
  ],

  "docRepoEdits": {
    "_rule": "AGENT-CONTRACT section 6: leave repo edits uncommitted and list them.",
    "uncommittedFiles": [
      { "path": "_inbox/2026-09-17_p308-join-miss-companions_cp1.json", "change": "added (CP1)" },
      { "path": "_inbox/2026-09-17_p308-join-miss-companions_cp2.json", "change": "added (CP2)" },
      { "path": "_inbox/2026-09-17_p308-join-miss-companions_close.json", "change": "added (this file)" },
      { "path": "_inbox/2026-09-17_p308-join-miss-companions_falsifier4-revert.log", "change": "added, then rewritten in place when re-run against the final test file" },
      { "path": "_inbox/2026-09-17_p308-join-miss-companions_guard-fires.log", "change": "added" },
      { "path": "_inbox/2026-09-17_p308-join-miss-companions_preexisting-failures.log", "change": "added" },
      { "path": "_inbox/2026-09-17_p308-join-miss-companions_revendor-emit-check.log", "change": "added" },
      { "path": "_catalog/seat_register.json", "change": "MODIFIED: the hauska-engine-p308 object (name/path/worktree/branch/note) removed as the entry's own note requires; the neighbouring hauska-engine-p299 entry is deliberately left alone (a different, still-open lane)" }
    ],
    "filesOutsideDocRepo": ["C:/Users/cente/_tmp_p308_pr_body.md", "C:/Users/cente/_tmp_p308_cp2_comment.md", "C:/Users/cente/_tmp_p308_emit_diff.mjs", "C:/Users/cente/_tmp_p308_violate.mjs (the guard-removal harness used for the guard-fires log)"],
    "note": "the doc_repo worktree at C:/Users/cente/doc_repo is behind origin/main (origin/main carried the dispatch this lane executed); artifacts were written into the worktree as the dispatch directs, without committing."
  },

  "factoryFollowOn": {
    "_rule": "The dispatch requires the follow-on written as commands. Every command below was exercised in its read-only parts by this lane; the parts that write to hauska-factory were NOT run (this lane must not edit that repo) and are marked as instructions, not as verified runs.",
    "who": "the integration seat (this lane does not re-vendor, merge, deploy or write a store)",
    "precondition": "PR 468 merged to hauska-engine main. Let MERGE_SHA be the squash-merge sha; read it, do not guess it.",
    "steps": [
      { "n": 0, "what": "read the merge sha and confirm the fix is in it", "cmd": "git -C P:/tmp/p308-engine fetch origin; git -C P:/tmp/p308-engine log --oneline -1 origin/main; git -C P:/tmp/p308-engine grep -n isCadJoinMissBasis origin/main -- packages/engine-core/src/parcel-record/ingest-existing.ts" },
      { "n": 1, "what": "factory branch", "cmd": "cd P:/hauska-factory; git fetch origin; git checkout -b chore/p308-revendor origin/main" },
      { "n": 2, "what": "emit the parcel-record module at MERGE_SHA with the pin's documented invocation (the engine's own build cannot do it: tsconfig.base.json sets noEmit:true)", "cmd": "git -C P:/tmp/p308-engine worktree add --detach P:/tmp/p308-revendor $MERGE_SHA; cd P:/tmp/p308-engine; pnpm exec tsc P:/tmp/p308-revendor/packages/engine-core/src/parcel-record/index.ts --module nodenext --moduleResolution nodenext --target ES2022 --lib \"ES2023,DOM\" --types node --outDir P:/tmp/p308-emit-new --skipLibCheck", "caveatMeasured": "the quotes around the --lib value are mandatory on Windows PowerShell: unquoted, `--lib ES2023,DOM` is split on the comma and tsc fails with TS6046. Measured 2026-09-17 and recorded in _inbox/2026-09-17_p308-join-miss-companions_revendor-emit-check.log" },
      { "n": 3, "what": "paste ONE file and prove the rest is a no-op", "cmd": "for f in access-pair cell-state companion-shapes config ingest-existing instantiate liveness load not-applicable-audit rail-keys record-shape; do diff --strip-trailing-cr P:/tmp/p308-emit-new/$f.js P:/hauska-factory/src/lib/parcel-record-engine/$f.js; done; cp P:/tmp/p308-emit-new/ingest-existing.js P:/hauska-factory/src/lib/parcel-record-engine/ingest-existing.js", "expectedMeasured": "measured at this lane's HEAD: of the 13 vendored files, exactly ONE - ingest-existing.js - differs from the emit; index.js and publish-gate.js also differ and MUST NOT be copied because ENGINE_PIN.json's factoryDivergence block declares them divergent by factory design. STOP if any other file differs: that would mean the new sha carries more than P-308 and the pin note's only-the-changed-files convention needs a decision, not a blind copy" },
      { "n": 4, "what": "update the pin, preserving the divergence", "cmd": "edit src/lib/parcel-record-engine/ENGINE_PIN.json: engineSha <- $MERGE_SHA, compiledAt <- now, and rewrite the `note` (the pin's own changelog: say what changed relative to 2cc2057, which is ingest-existing.js alone). LEAVE factoryDivergence IN PLACE - this sha does NOT carry P-252's zero-earned refusal, which is a different lane's work - and do NOT regenerate test/fixtures/engine-pin-2cc2057/. Then move test/engine-pin-divergence.test.mjs:38 ENGINE_PIN_SHA to $MERGE_SHA (its source leg reads git show <sha>:<path> and asserts the engine does NOT carry the refusal, which stays true).", "hazardQuoted": "ENGINE_PIN.json factoryDivergence.howToRetire: 'Do NOT regenerate this directory from 2cc2057 while this block is present: it would silently revert the fix, and both the divergence test and the fixture test would then fail by design.'" },
      { "n": 5, "what": "retire the job-level step IN THE SAME CHANGE (a copied default outlives its own removal), enumerating every call site", "callSitesEnumeratedAtFactoryOriginMain": { "definition": "src/jobs/parcel-record-fill.mjs:902 (export function completeJoinMissCompanionsOntoRecords)", "passCall": "src/jobs/parcel-record-fill.mjs:1156", "passResultField": "src/jobs/parcel-record-fill.mjs:1173", "countyWideAccumulatorInit": "src/jobs/parcel-record-fill.mjs:1426", "countyWideAccumulation": "src/jobs/parcel-record-fill.mjs:1456-1457", "countyWideResultField": "src/jobs/parcel-record-fill.mjs:1514", "logSegment": "src/jobs/parcel-record-fill.mjs:1595 (joinMissCompanionCells=)", "countySummary": "src/jobs/parcel-record-fill.mjs:1715", "proseReferencesThatWouldGoStale": ["src/jobs/parcel-record-fill.mjs:203 (the landUseSource rail note names the function as the thing completing this job-side)", "src/jobs/parcel-record-fill.mjs:221 (the acreageMethod rail note, same)"], "tests": ["test/parcel-record-fill.test.mjs:57 (import)", "test/parcel-record-fill.test.mjs:1251 (the P-268 describe)", "test/parcel-record-fill.test.mjs:1259, 1271, 1283, 1291, 1302 (five direct calls)", "test/parcel-record-fill.test.mjs:1310 (asserts pass.joinMissCompanions.cellsMoved === 2)", "test/parcel-record-fill.test.mjs:1481-1482 (asserts filled.joinMissCompanions)"], "nothingElseReadsIt": "git grep -n joinMissCompanion origin/main (whole repo, no pathspec) returns only the lines above, so no other file in hauska-factory reads the function or its result fields" } },
      { "n": 6, "what": "prove the retirement is a no-op instead of assuming it", "cmd": "on a join-miss cohort, run the fill job and read joinMissCompanionCells from the county-wide log line (parcel-record-fill.mjs:1595). It must read 0 on records written by the new engine. If it is not 0, the engine did not absorb the pass and the deletion is premature.", "and": "the parity test in PR 468 is the unit-level proof that the two implementations agree cell for cell; it stays in the engine so the equality is re-checked on every future change to either side" },
      { "n": 7, "what": "ship", "cmd": "cd P:/hauska-factory; pnpm test   # node --test test/*.test.mjs, which includes test/engine-pin-divergence.test.mjs and test/parcel-record-fill.test.mjs" }
    ],
    "whyTheStatusStaysClosedPartialUntilThisHappens": "hauska-factory vendors this engine at ENGINE_PIN.json engineSha 2cc2057, and its vendored src/lib/parcel-record-engine/ingest-existing.js still carries the 13-stamp form with no companion stamp (read at origin/main 2026-09-17). Until steps 1-7 run, the running factory does not have this fix and its job-level step is still doing the work."
  },

  "whatRemainsOpen": [
    "PR 468 is open and unmerged pending a maintainer decision (the dispatch's CP2 gate). Nothing merges it by itself. If a later lane finds it still open, that lane must decide - merge, or close with a written reason - rather than leave an ambiguous PR (AGENT-CONTRACT section 2).",
    "The factory follow-on (steps 0-7 above) is unwritten work in hauska-factory: re-vendor, then retire completeJoinMissCompanionsOntoRecords, then prove the retirement is a no-op on a join-miss cohort.",
    "The store census for the two companion rails was NOT re-measured by this lane (inherited from P-266/P-268 under a lease this lane does not hold). If a later lane needs the live cohort, it needs its own factory-store read under a heavy-scan lease.",
    "No surface probe exists for P-308 and none was run: the row's surface is the factory's fill job. OPS-24 Law 1's predicate debt for P-308 remains, and probe-close-gate.mjs prints it on this close rather than hiding it."
  ],

  "leave_behind": [
    { "kind": "OPEN", "text": "hauska-engine PR 468 (fix/p308-join-miss-companions @ 532e39f6bff5257e37a2b5bc5aa0286c927ba6f5) is parked at CP2 awaiting a maintainer merge decision, with the evidence in issue comment 5718950911. CI is green on that sha (ci run 35255390349, conclusion success)." },
    { "kind": "OPEN", "text": "the factory follow-on is unwritten: re-vendor at the merge sha, then retire completeJoinMissCompanionsOntoRecords in the same change. Call sites and the two prose notes are enumerated in factoryFollowOn above. The retirement must be proven a no-op (joinMissCompanionCells == 0 on a join-miss cohort) before the deletion is trusted." },
    { "kind": "GROUND-TRUTH", "text": "2026-09-17T17:59Z: origin/main of hauska-engine is GREEN (runs 35243439195, 35229576204, 35219904238, 35163185082, 35162864486 all success), and this machine's local engine-core suite fails 5 files / 2 tests on the SAME commit with and without the P-308 change. The local failures are therefore this machine's environment, not the repo's state - CI green on the identical sha 532e39f proves it. Do not read the local baseline log as 'main is red'." },
    { "kind": "GROUND-TRUTH", "text": "2026-09-17T18:00Z: factory origin/main ENGINE_PIN.json engineSha = 2cc20578262bca1881ad81c84231f9c6288fe94a, compiledAt 2026-09-14T16:37:00Z, module packages/engine-core/src/parcel-record, and factoryDivergence names exactly publish-gate.js and index.js as divergent (P-252's factory-side zero-earned refusal, whose engine-side half is still unfixed at 2cc2057)." },
    { "kind": "GROUND-TRUTH", "text": "2026-09-17T18:10Z, measured: the pin's documented emit command reproduces the factory's vendored copy for 11 of 13 files; the 2 exceptions are exactly the factoryDivergence files. Against this lane's HEAD, one additional file differs: ingest-existing.js. So re-vendoring P-308 is a one-file paste and the two divergent files must NOT be copied." },
    { "kind": "LESSON", "text": "the engine repo cannot reproduce its own vendored emit: tsconfig.base.json sets noEmit: true, so `pnpm -r run build` (tsc -b over include:['src'] exclude:['src/**/__tests__/**']) emits nothing. The pin's tsc invocation is the only route, and on Windows PowerShell its --lib value MUST be quoted (`--lib \"ES2023,DOM\"`) or PowerShell splits it on the comma and tsc dies with TS6046." },
    { "kind": "LESSON", "text": "a parity test comparing two instantiations of the same fixture is a time bomb if any cell's vintage comes from new Date().toISOString(): pin the instant. The engine already had the optional nowIso parameter and the flake lived in the test (it failed whenever a millisecond crossed between the two instantiations)." },
    { "kind": "LESSON", "text": "a revert-proof log goes stale the moment the test file is edited. The first falsifier-4 log measured an earlier revision with a combined falsifier 3; re-running it against the final file took seconds and changed the reading to 5-of-39. Re-run the experiment at the end, not in the middle." },
    { "kind": "LESSON", "text": "a falsifier that asserts two mechanisms in one test can be satisfied by either, so it cannot say which one it measured. Falsifier 3 had to be split into 3a (an earned companion is never rewritten) and 3b (a matched-basis primary never earns a join-miss provenance) before each half could be independently proven able to fire - and 3b turned out to be the ONLY new test that passes pre-fix, which is exactly the information the combined test hid." },
    { "kind": "DEAD-END", "text": "stamping the companions unconditionally inside applyCadJoinMiss (i.e. gating on the record's join-miss state rather than the primary's own badge) was designed, rejected and is guarded against: it writes a join-miss provenance beside a primary that a matched-row pass already stamped with a taxYear, breaking the pair invariant and the parity equality. Falsifier 3b is the standing test against it, proven able to fire." },
    { "kind": "DEAD-END", "text": "do not regenerate test/fixtures/engine-pin-2cc2057/ or delete ENGINE_PIN.json's factoryDivergence block while re-vendoring for P-308: this merge sha does not carry P-252's zero-earned refusal, and doing so would silently revert the factory's publish-gate fix (ENGINE_PIN.json's own howToRetire says so, and the divergence test would fail by design)." }
  ]
}
