---
id: 2026-09-12_ops23_wave3_verify_p154
date: 2026-09-12
row: P-154
kind: read-only-verification
snapshot:
  legacy-design-tools: origin/main 78ad8a51
  hauska-engine: origin/main 99f9146
  hauska-factory: origin/main 7a94ae5
  hauska-map: origin/main 4350ab9
  hauska-setback-corpus: main b8020c8 (gh api; no P:/ checkout)
  npm @empressaio/setback-corpus: 1.1.0 (published 2026-09-07T21:52:27Z)
  arcgis_read_at: 2026-09-12
---

# P-154 fact check: five setback producers for 48021:34049

Read-only. No working tree touched; every file read via `git show origin/main:<path>`; corpus via `gh api`; ArcGIS via public GET.

## 1. legacy-design-tools

`artifacts/api-server/src/lib/buildableEnvelope/authoritativeSetbackSource.ts`

Ranking rule, lines 57-61 and 148-156: tier first, date only as tiebreaker.

```
57 const TIER_RANK: Record<SetbackSourceKind, number> = {
58   "codified-ordinance": 3,
59   "gis-per-parcel": 2,
60   "atom-chain": 1,
...
151 const tierDiff = TIER_RANK[cur.sourceKind] - TIER_RANK[best.sourceKind];
152 if (tierDiff > 0) return cur;
153 if (tierDiff < 0) return best;
154 return cur.effectiveDate >= best.effectiveDate ? cur : best;
```

Sources it ranks: exactly two candidates (lines 182-219): the codified table row from `getSetbackTableForZoning` (`@workspace/adapters`) and the atom-chain `setbackRule` from `fetchPropertyAtomChain.ts:56` (`GET <retrieval>/property-nodes/:id/atom-chain`). Layer 23 is not fetched here; it is recognised only by adapter string (line 131 `adapter.includes("layer-23")` -> `gis-per-parcel`). Layer 83: NOT FOUND anywhere in LDT api-server or adapters src (searched `layer[- _]*83`, `/83`, `Revisions`); only `lib/cad-ingest/src/txgio/zoning-layers.ts:286-300` references `Zoned_Parcels/FeatureServer/83` as the zoning stamp source, and its comment says "setback NUMBERS come from ordinance text ... never GIS card". Corpus package: NOT imported by LDT (no `setback-corpus` in `lib/` or `artifacts/`); LDT vendors its own JSON at `lib/adapters/src/local/setbacks/`.

Dates read: table date via `effectiveDateForTable` (lines 84-93): explicit `effectiveDate`, else `accessed YYYY-MM-DD` in `note`, else `"1970-01-01"`. Atom date via `atomEffectiveDate` (lines 140-146): `extractedAt ?? sourceVintage ?? "1970-01-01"`. `extractedAt` is the emit time (hauska-engine `emit-setback-rule.ts:141` `const extractedAt = new Date().toISOString()`), not a source effective date. No source's ArcGIS `lastEditDate` is read anywhere.

Table selection for Bastrop: `lib/adapters/src/local/setbacks/index.ts:263-277` routes the three Bastrop keys with an SF code to `bastrop-development-code.json`, lines 5 and 10-13: `effectiveDate: "2026-04-14"`, SF-1 front 30 / rear 30 / side 10 / corner 20. Tier 3 beats tier 2 regardless of date, so this row wins the envelope endpoint (`routes/brokeragePlaceBuildableEnvelope.ts:1171-1175`) and the draw model (`parcelDrawEnvelopeModel.ts:141-145`). Fixture `scripts/fixtures/surface-probe/envelope-address_48021-34049.json` confirms `setbackSource: codified-ordinance`, 30/10/30/20.

`setbacksFactServeCutover.ts:47-59` and `setbacksFactFromParcelRecord.ts:141-193`: record-served setbacks read the four `setback*Ft` cells from `parcel_record_cell`; `sourceVintage`/`evaluatedAt` are the cell `vintage` (line 191-192). `nodeFacetTier1Assemble.ts` does NOT call this loader (it only computes `computeTier1Envelope`, lines 402-420, and never counts envelope as coverage). The loader's consumers are `routes/propertyExplorer.ts:284,456` and `routes/brokerageNodeFacets.ts:836`. So for 48021 the MCP/brief 30/10/30/20 comes from the factory-written cells (section 5), not from the LDT resolver.

## 2. hauska-map

`apps/property-explorer/api/_lib/atom-chain-to-facets.ts`

```
92  export const BASTROP_LIVE_SETBACK_ADAPTER = "bastrop-per-parcel-record-layer-23";
120   return (rule.sourceAdapter ?? "").trim() === BASTROP_LIVE_SETBACK_ADAPTER;
141   if (adapter === "bastrop-per-parcel-record-layer-23") return false;
155   return adapter !== "bastrop-per-parcel-record-layer-23";
1838  const setbacks = mapSetbacks(rule, district);
1839  const tableSetbacks = setbacks ?? (hasDistrict && jurisdictionKey
1842      ? resolveCodifiedSetbacksForStamp(jurisdictionKey, district, opts?.perParcelSetback)
1848  const effectiveSetbacks = setbacks ?? tableSetbacks ?? undefined;
```

For a 48021 city parcel, any setback rule whose adapter is not the layer-23 string is treated as stale (lines 129-156); the atom-chain rule wins when present (line 1838), else the codified path. `codified-setback-from-zoning.ts:34-40` puts all four Bastrop keys in `PER_PARCEL_RECORD_ONLY_SETBACK_KEYS`, and `setback-tables/` holds only `austin-tx`, `elgin-development-code`, `pflugerville-tx`, `san-antonio-tx` (no Bastrop table). `pe-property-atoms.ts:602-621` fetches layer 23 live (`pe-bastrop-per-parcel-setback.ts:23-24`, `Parcels_One_Click/FeatureServer/23`) when the chain lacks a live rule. No date is read on either path. Panel 25/5/25/15 is therefore layer 23, either baked in the chain (fixture `facets_48021-34049.json`: `snapshotAt 2026-08-05`, `secondSource` note citing layer 83) or fetched live.

## 3. hauska-engine

`packages/adapters/package.json:130` `"@empressaio/setback-corpus": "^1.1.0"`; `pnpm-lock.yaml:571` resolves `1.1.0`. `packages/adapters/src/local/setbacks/index.ts:26,61-71` builds `SETBACK_TABLES` from the corpus for 15 vendored keys; `corpus-divergence.test.ts` compares the on-disk JSON to the package. Per-jurisdiction dates: only the table's own `effectiveDate`. Bastrop precedence, `index.ts:175-199`: a supplied `bastropPerParcelRecord` wins; without one, city BDC codes return `null` ("R13 (AMENDMENT 8): city BDC districts require layer-23 per-parcel record"). `bastrop-setback-currency.ts:7-9,68-69`: any non-layer-23 Bastrop rule is stale, fail-closed. `bastrop-per-parcel-record.ts:22-23` reads layer 23; lines 459-465 hard-code the layer-83 conflict text ("front 30 / interior side 10 / corner side 20 / rear 30"); line 456-457 cites `Parcels_One_Click/FeatureServer/83`, which returns `"The requested layer (layerId: 83) was not found."` (GET 2026-09-12). Line 606 requests outField `Ordinance_Link`; the live layer has no such field (it has `Ordinance_`), so the ordinance citation the engine attaches to layer-23 rows reads empty. No date fields are read from layer 23 or 83. Engine's Bastrop values: 25/5/25/15 (layer 23), with 30/10/30/20 only as a disclosure string.

## 4. hauska-setback-corpus

`src/setbacks/bastrop-development-code.json` (main b8020c8) lines 4-13: display name "City of Bastrop, TX (Bastrop Development Code / Ord. 2026-06)", `effectiveDate: "2026-04-14"`, SF-1 front 30 / rear 30 / side 10 / corner 20, `citation_url` = the Ordinance 2026-06 PDF, per-field `verification_state: "human-verified"`, `atom_did: bastrop_tx/bdc-2026-adopted/14.02.003`. `verificationTier`: NOT FOUND; the schema field is `verification_state` (`table-types.ts:20`). `bastrop-city-tx.json` (repealed B3) has no `effectiveDate`, `note` says "accessed 2026-07-23", P-2/P-3 front 25 with side/rear 0. `bastrop-tx.json` (county legacy) has no date. `package.json` version 1.1.0; npm live 1.1.0 (versions 1.0.0, 1.0.1, 1.1.0, all 2026-09-07). Byte-identical to LDT's and engine's vendored BDC file on the fields read.

## 5. hauska-factory

`src/jobs/parcel-setback-cells.mjs` (OPS-21 S1, writer id `f11-setback`): lines 11-19 state it ports LDT's routing and sources data from `@empressaio/setback-corpus` (`package-lock.json:44` resolved 1.1.0; router `CORPUS_VERSION = "1.1.0"`, `setback-table-router.mjs:40`). Line 130 maps `Bastrop -> "bastrop-development-code"`; router lines 76-83 route SF codes to that table. Cells (lines 282-299) carry `value`, `jurisdictionKey`, `resolvedTableKey`, `source`, `vintage`; companion row (lines 305-314) carries `effectiveDate: rule.effectiveDate` = `table.effectiveDate` (router line 200). `vintage` is run time (`nowIso || new Date().toISOString()`, line 414), not a source date. The factory never reads layer 23 or LDT's table. So the ledger's Bastrop SF-1 cells are 30/10/30/20 with `effectiveDate 2026-04-14` and a run-time vintage.

## 6. Source dates, read at source

| Source | URL | Date at source | Values for 34049 (SF-1) |
|---|---|---|---|
| Layer 23 `Parcel_OneClick_Join` | `services7.arcgis.com/qOeXJdBtGknaCJC4/arcgis/rest/services/Parcels_One_Click/FeatureServer/23` | `editingInfo.lastEditDate 1789071883755` = 2026-09-10T20:24:43Z; `dataLastEditDate 1787597890284` = 2026-08-24T18:58:10Z; row `LASTUPDATE null` | `FrontSetback_ 25, SideSetback_ 5, RearSetback_ 25`, `SideSetback "5 ft (Corner Side Street Setback: 15 ft)"`, `Ordinance_ "2019-51"` |
| Layer 83 `Zoned_Parcels_Revisions_Clip` | `.../Zoned_Parcels/FeatureServer/83` (LDT's URL; engine's `Parcels_One_Click/83` does not exist) | `editingInfo.lastEditDate 1784844239427` = 2026-07-23T22:03:59Z | `FrontSetback "30 feet", SideSetback "10 feet", RearSetback "30 feet", CornerSideSetbacks "20 feet"`, `ZoneTypeClass 3` |
| Ordinance 2026-06 PDF | corpus `citation_url` (HTTP 200, 7.85 MB, 236 pp) | "Section 7. Effective Date. This Ordinance shall take effect immediately after its final passage"; "PASSED & APPROVED on Second Reading ... the 14th day of April 2026" (first reading 24 March 2026) | p.12 Sec. 14.02.003 SF-1: "Front Setback* 30 feet Side Setback 10 feet Corner Side Street Setback 20 feet Rear Setback 30 feet" |

Layer 23's row cites Ordinance 2019-51, a pre-BDC ordinance, while carrying an SF-1 `ZoneTypeClass`; its `lastEditDate` is the newest of the three but its data edit (2026-08-24) postdates the ordinance and still prints the older numbers. Whether 2026-08-24 touched this row is unreadable (`LASTUPDATE null`, no per-feature edit tracking exposed).

## 7. Which values, where

| Producer | Values | Source and date it carries |
|---|---|---|
| PE panel (hauska-map) | 25/5/25/15 | layer 23 via atom chain (`snapshotAt 2026-08-05`) or live fetch; no source date read |
| Feasibility PDF (engine) | 25/5/25 (+15 corner) | engine per-parcel adapter, layer 23; `extractedAt` = emit time |
| Envelope endpoint + draw (LDT) | 30/10/30/20 | `codified-ordinance`, `effectiveDate 2026-04-14`, wins by tier not date |
| Ledger cells + MCP `get_smart_site` (factory -> LDT parcel_record) | 30/10/30/20 | corpus 1.1.0 BDC table, `effectiveDate 2026-04-14`, `vintage` = run time |
| Ordinance text (authority) | 30/10/30/20 | effective 2026-04-14 |
| Layer 83 (city Revisions) | 30/10/30/20 | last edit 2026-07-23 |
| Layer 23 (city OnClick) | 25/5/25/15 | last data edit 2026-08-24, cites Ord. 2019-51 |

For the mission: under "most-current source wins, date read at source", the ordinance (2026-04-14) and layer 83 (2026-07-23) agree on 30/10/30/20; layer 23 carries a later edit stamp (2026-08-24) but an older cited ordinance (2019-51). A resolver that ranks by ArcGIS edit date alone would pick 25/5/25/15; one that reads the ordinance citation on the row would pick 30/10/30/20. That ambiguity is exactly the conflict-row case the ruling names, and no producer reads either date today.

## Fleet memory

- GROUND-TRUTH (2026-09-12): layer 23 row 34049 = 25/5/25/15, `Ordinance_ 2019-51`; layer 83 row 34049 = 30/10/30/20.
- LESSON: engine's `BASTROP_LAYER_83_REVISIONS_URL` is a dead layer; live is `Zoned_Parcels/83`. Engine requests `Ordinance_Link`, which layer 23 lacks.
- LESSON: no producer reads `editingInfo.lastEditDate`; LDT's atom "effective date" is emit time.
- DEAD-END: corpus field is `verification_state`, not `verificationTier`.
- OPEN: whether layer 23's 2026-08-24 edit touched 34049 is unreadable from the service.

leave_behind: none
