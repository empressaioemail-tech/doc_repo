---
id: 2026-08-29_p91_f6_step2_grade
title: F6 step 2 FAIL, wrong sentence
date: 2026-08-29
status: fail
plan_row: P-91
wdll: 6
serving: smartsite-mcp p551
screen_id: 4316b571-c7d2-4b9f-9e50-4f7a16dbfa94
plant: 48021:900099
---

# Score

Fail. `get_smart_site` ran once on `48021:900099` and returned `baked_snapshot_not_found`. No draw. The board then painted `Open did not reach me`. That sentence is host silence. The tool ran.

# Mechanism

`get_smart_site` 404 sets MCP `isError: true`. The iframe `ui/message` reply handler treated `isError` as silence, painted the dead-Open sentence, and returned before `accept()`. The `accept()` path is what paints `Not on file in Bastrop` for a non-parcel result.

Rejected alternative: leftover step-1 paint. Step 1 was a two-row board. This frame has the three-row plant including `900099` and `reply-ok` after the 900099 Open. The sentence is from this turn.

# Next

Punch: `isError` calls `accept()`. Timer-only remains `Open did not reach me`. URI `app-p552.html`.
