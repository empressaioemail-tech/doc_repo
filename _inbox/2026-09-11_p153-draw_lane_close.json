{
  "lane": "P-153-DRAW",
  "planRow": "P-153",
  "closedAt": "2026-09-12T02:45:00Z",
  "missionPremise": "R-2 (operator, 2026-09-11): _decisions/2026-09-11_ruling_b_reversed_polygon_only.md. Ruling B (the 2026-08-28 refusal ruling) is reversed for the POLYGON only: the map and the MCP draw block both draw the modelled buildable envelope from the same place/buildable-envelope derivation, carrying that call's disclosure, wherever a zoning district and a setback table exist. The buildable area figure and percent stay refused on every surface until a buildable-envelope atom backs them. Ruling B still stands for the number -- the two surfaces must not disagree, and after this change they agree on drawing a polygon and agree on refusing the number.",
  "completionPredicate": "Stated in the mission: for 48021:34049, the map draws the modelled polygon and no surface prints a buildable figure or percent; for 48453:474034 (no district), every surface stays refused exactly as before. Falsifier (pre-registered before any code): (1) if the MCP draw block's polygon and the map's polygon differ in vertex count or in area by more than 1%, there are two implementations and the change is wrong; (2) if any surface prints a buildable figure or percent for 48021:34049 after the change, the change is wrong. Independently confirmed live in production for everything observable without a browser (see probe below); the map's own visual rendering could not be observed in this environment (no browser/Playwright tool available) and is the one predicate leg still open -- see whatRemainsOpen.",
  "scopeBasis": "Exactly the mission's five hauska-map changes plus its six legacy-design-tools/smartsite-mcp changes. Did not touch the entitlement gate (isEntitled/mayDraw), any pricing surface, the geocoder, or hauska-engine's own copy of buildable-display-vocab.ts. Did not mint or backfill any envelope atom. Widened scope ONCE, mid-mission, when live-probing hauska-map's own first deploy proved it functionally inert (see contradicted) -- that widening was still squarely within 'make the map draw the polygon', not a new feature.",
  "worktreesUsed": {
    "hauska-map": "P:/seat-worktrees/property/hauska-map-p153-draw, branch feat/p153-envelope-polygon-draw (PR #384) then fix/p153-wire-live-envelope-augment (PR #386), both created from origin/main",
    "legacy-design-tools": "P:/seat-worktrees/property/legacy-design-tools-p153-draw, branch feat/p153-mcp-draw-polygon (PR #661), created from origin/main"
  },
  "pullRequests": [
    {
      "repo": "empressaioemail-tech/hauska-map",
      "number": 384,
      "title": "feat(P-153/DRAW): Ruling B reversed for the polygon only",
      "headShaAtMerge": "8ab4579802a002f1a42e4692336bf7a1b0ba0822",
      "mergeCommitSha": "1aef5dd500c16f215713dee2fe1bfc76ef32d8b2",
      "mergeMethod": "squash",
      "ciConclusion": "success (gh api check-runs at the head sha: Typecheck=success, test=success, \"No double-encoded source\"=success)",
      "filesChanged": 12,
      "insertions": 362,
      "deletions": 18,
      "note": "Correct in isolation but found functionally inert by live-probing the deploy -- see contradicted and PR #386."
    },
    {
      "repo": "empressaioemail-tech/hauska-map",
      "number": 386,
      "title": "fix(P-153/DRAW): wire the live envelope augmentation so `modelled` is reachable",
      "headShaAtMerge": "891473cadddbd2c1992e517183bf04126a23b78",
      "mergeCommitSha": "8b44f68db96d11624ed08631e01baa3d7b6b9814",
      "mergeMethod": "squash",
      "ciConclusion": "success (gh api check-runs at the exact head sha, all three checks success)",
      "filesChanged": 2,
      "insertions": 170,
      "deletions": 8,
      "note": "The functional fix. Wires the already-implemented, already-unit-tested, previously-uncalled augmentFacetsWithLiveEnvelope into resolveSheet."
    },
    {
      "repo": "empressaioemail-tech/legacy-design-tools",
      "number": 661,
      "title": "feat(P-153/MCP): draw block carries the modelled buildable-envelope polygon",
      "headShaAtMerge": "22020f52752b5030b30e62263e02cc23dde09d84",
      "mergeCommitSha": "6b57902035d3bcd583ff01e6a6f06eb6aa738895",
      "mergeMethod": "squash",
      "ciConclusion": "success on the THIRD CI run at this exact head sha -- the first run's Test job failed with a genuine unrelated flake ('out of shared memory' / max_locks_per_transaction, in lib/submission-classifier's Postgres-schema-cleanup DDL under concurrent test-worker load, a completely different package from anything this PR touched); re-ran the job with gh run rerun --failed and it passed. The second push (after a second unrelated main-branch merge) needed no rerun.",
      "filesChanged": 20,
      "insertions": 1510,
      "deletions": 121,
      "implementedBy": "Fanned to one sub-agent (AGENT_CONTRACT section 1) with a complete, pre-researched spec -- I had already read the real function signatures (resolveAuthoritativeSetbacks, deriveBuildableEnvelope, reconcileWithAtomEnvelope, labelEdges, firstParcelRing) and worked out the exact coordinate-projection formula before writing the brief. The sub-agent produced a diff only and did not commit, push, or run any test/typecheck command as a final claim without me re-running it. I independently re-ran: tsc --noEmit for both artifacts/api-server and artifacts/smartsite-mcp (clean); the full targeted test set myself (177 + 94 tests, all passing); and verified its two most load-bearing claims myself rather than trusting the self-report -- (a) that ~189 DB-dependent test-file failures are pre-existing and unrelated, via git stash/re-run/stash-pop on brokeragePlaceBuildableEnvelope.test.ts (identical failure before and after), and (b) its flagged discrepancy that the top-level parcel `ring` field is actually OPEN, not closed as I'd told it in the brief -- confirmed by reading assembleParcelDraw's own .pop() call myself. Both held up; I found no defect in its diff."
    }
  ],
  "counts": {
    "hauska-map test suite (both PRs, final state)": "201 files, 2931 tests passed, 1 todo, exit 0 -- INDEPENDENTLY RE-RUN by me, not self-reported, both before and after each merge with origin/main (main moved three times under me from other lanes' concurrent PR #385/#155 merges; each merge was a clean fast-forward or conflict-free merge, re-verified by me every time before pushing)",
    "legacy-design-tools test suites": "api-server: 177 tests across the directly-relevant files (parcelDrawStub, buildableEnvelope/*, parcelDrawFromReads), independently re-run by me, all passing. smartsite-mcp: 612 passed / 9 pre-existing-and-unrelated failures (tests/records-extraction.test.ts, ECONNREFUSED against a Postgres this dev sandbox does not have -- confirmed identical on a clean git-stash baseline). Full api-server suite (388 files) could not be run end-to-end in this dev sandbox (189 files need a live Postgres this environment lacks) -- real CI (with a provisioned Postgres+PostGIS) is what actually confirmed the full suite, and it passed."
  },
  "constraintsHonored": [
    "Never built in the shared, dirty P:/hauska-map or P:/legacy-design-tools checkouts -- all work in dedicated worktrees created fresh from origin/main",
    "Sub-agent (LDT side) produced a diff only; did not commit. All commits, pushes, PR opens, merges, and deploys were done by me after reading every diff myself",
    "Merged only on the check-run conclusion STRING 'success', read via gh api at the exact head SHA every time, not gh pr checks' cosmetic pass label -- five separate merge attempts across the three PRs, each re-verified at its own exact head sha immediately before merging",
    "Did not touch the entitlement gate, any pricing surface, the geocoder, or hauska-engine's copy of the vocab file",
    "Did not mint or backfill any envelope atom",
    "Read the LDT serving revision by FIELD NAME (revisionName under the object carrying percent:100) from the traffic JSON, and the smartsite-mcp health body's 'status'/'revision' fields by name, never positionally -- for both the canary smoke test and the post-shift verification",
    "Traffic-shift for BOTH LDT services was NOT self-executed without asking, matching this fleet's own established norm (the workflow's own comments mark it a hard constraint: 'a separate deliberate operator action, do not relax') -- asked the session's own user explicitly before dispatching shift-traffic on cortex-api and smartsite-mcp and got an explicit go-ahead, even though the user had earlier given a broad 'full autonomous run' answer for the mission overall; treated the more specific, codebase-documented norm as controlling",
    "hauska-map's two production deploys (Vercel CLI) were done directly by me per the standing decision for that repo (no separate traffic-shift step exists in that deploy model -- vercel deploy --prod aliases immediately), each independently verified via vercel inspect confirming smartsite.cloud + www.smartsite.cloud aliased to the exact new deployment id before moving on"
  ],
  "deploys": {
    "hauska-map": {
      "mechanism": "Vercel CLI, vercel deploy --prod --yes, from the repo root, twice (once per PR, since PR #384 alone was insufficient -- see contradicted)",
      "deploymentIds": [
        "dpl_EA7zC22N75ntvsBtd2fSRMVfYfap (PR #384)",
        "dpl_5XLp1v9LpftaBWjzpXyQDxn4rjt4 (PR #386, the functional deploy)"
      ],
      "verification": "vercel inspect on both, independently: smartsite.cloud and www.smartsite.cloud aliased, status Ready, target production, both times.",
      "preExistingUnrelatedBuildErrors": "Both Vercel builds surfaced the same real, pre-existing TypeScript errors in api/pe-property-atoms.ts, api/pe-bastrop-per-parcel-setback.ts, and api/verdict-layer-merge.ts during the api/ functions build step (does not hard-fail the deploy). None of these files were touched by either PR; tsc --noEmit was independently confirmed clean on both branches. Same class of pre-existing debt a prior lane (P-151's close) already flagged -- still not fixed, still not this lane's files."
    },
    "legacy-design-tools": {
      "mechanism": "cortex-api and smartsite-mcp Cloud Run canary workflows: push-triggered build-and-push (automatic on merge, both confirmed success) -> workflow_dispatch deploy-canary for both, pinned to the merge sha 6b57902035d3bcd583ff01e6a6f06eb6aa738895 -> smoke-tested both canary tags' health endpoints (200/ok) -> asked the user before shift-traffic and got an explicit go-ahead -> workflow_dispatch shift-traffic for both, each with its own automated post-shift smoke gate that would have failed the workflow on an unhealthy result",
      "cortexApiServingRevision": "cortex-api-00776-wov at 100% traffic, read by FIELD NAME (the 'revisionName' key on the traffic-JSON object whose 'percent' field equals 100, printed in the shift-traffic workflow's own log), never positionally. Production https://cortex-api-tds7av26va-uc.a.run.app/api/healthz returned HTTP 200 after the shift (the workflow's own gate).",
      "smartsiteMcpServingRevision": "smartsite-mcp-00109-zip. Production https://smartsite-mcp-tds7av26va-uc.a.run.app/health returned {\"status\":\"ok\",\"authConfigured\":true,\"cortexConfigured\":true,\"revision\":\"smartsite-mcp-00109-zip\"} after the shift (the workflow's own gate), and /health/dependencies separately confirmed {\"hauska_mcp\":{\"state\":\"ok\"}}.",
      "canarySmokeFalseAlarm": "Before shift-traffic, I additionally hand-probed the cortex-api canary tag's own buildable-envelope endpoint directly (bypassing the smartsite.cloud proxy) and got a 503 property_brief_api_unconfigured. Traced this to brokerageAuth.ts's own documented behavior: an unauthenticated direct call (no session JWT) 503s whenever no static API keys are provisioned, which is true of BOTH the canary and the already-serving production revision -- confirmed by re-reading the middleware's own comment ('Production has never provisioned BROKERAGE_OPERATOR_API_KEYS/BROKERAGE_API_KEYS'). Not a canary-specific config gap; a false alarm from my own test methodology (the canary tag URL has no smartsite.cloud proxy in front of it to inject a session credential). The workflow's own /api/healthz gate is the correct canary check; my own subsequent live probes below went through the smartsite.cloud proxy, exactly like a real customer request, and succeeded."
    }
  },
  "probe": {
    "artifact": "_inbox/2026-09-12_180231_surface_probe.json",
    "artifactAsCited": "scripts/surface-probe.mjs --rows P-153 --observations observations_p153_2026-09-12.json --allow-unmeasured, run against LIVE production after both deploys, artifact at _inbox/2026-09-12_024023_surface_probe.json. Raw legs: 48021:34049 -- facets http 200, envelope status ok, zoning SF-1; env/address (POST place/buildable-envelope by address) http 200 status ok, 6 vertices, 1329 ms; env/point http 200 status ok, 1102 ms. 48453:474034 -- facets http 200, envelope declined, zoning absent (unincorporated); env/address http 422 geocode_miss (expected -- no zoning code, no address-derive path exists for this shape); env/point http 200 status declined. MCP leg (get_smart_site, depth node, called live by me -- I have authenticated MCP access to smartsite.cloud in this session): 48021:34049's draw.overlays includes {id:\"envelope\", state:\"present\", geom: <6-point closed ring>, draw:\"inset-fill\", basis:\"modelled-figure-withheld\", basisDisplayText:\"Buildable envelope modelled from setbacks — area withheld pending an atom\"} with NO reason/reasonDisplayText field at all (confirming the false \"Withheld, setbacks unruled\" text can no longer attach); derivedFigures.denies still includes buildable_area; no buildable-area number or percent appears anywhere in the brief sections or the draw block. 48453:474034's draw.overlays envelope entry is unchanged from before this mission: {state:\"refused\", geom:\"none\", reason:\"no-zoning-stamp\"}. Row verdict: UNMEASURED for both parcels (not FAIL) -- the map's own visual rendering (overlayDrawn) could not be observed by me in this environment; see whatRemainsOpen. Every other predicate the tooling can check without a browser passed: the MCP's 6 vertices match the place/buildable-envelope endpoint's own 6 vertices for the same parcel (the falsifier's core claim -- one derivation, not two), and figurePrinted is confirmed false.",
    "note": "The lane cited _inbox/2026-09-12_024023_surface_probe.json in its own worktree, which is not on main; the overseer's run of 2026-09-12 (PASS on both P-153 parcels, with the operator's screenshot and the lane's MCP observation) is the artifact of record."
  },
  "falsifier": "Pre-registered before writing any code, per the mission: (1) if the MCP draw block's polygon and the map's polygon differ in vertex count, or in area by more than one percent, there are two implementations and the change is wrong. (2) if any surface prints a buildable figure or percent for 48021:34049 after the change, the change is wrong. Observed: the MCP draw block's polygon (6 vertices) matches the place/buildable-envelope endpoint's own response (6 vertices) for the same parcel and point -- both derive from the identical composeBuildableEnvelopeDerivation/deriveBuildableEnvelope call, confirmed by reading the code, not merely by the numbers agreeing. No buildable area or percent appears anywhere I can observe: not in the MCP draw block, not in the MCP brief sections, not (per new unit tests) in hauska-map's Buildable row or X-ray/brief print path. The map's OWN rendered pixels are the one leg of falsifier (1) I could not measure -- I have no browser tool in this environment (checked via ToolSearch, confirmed absent) -- so falsifier (1) is only half-confirmed by direct observation; the other half rests on the code-level argument that both surfaces call the same pure derivation function, which I verified by reading every line of both call sites, not by assumption.",
  "contradicted": "Two things, both found by live-probing my OWN deploys rather than trusting a green CI run -- exactly what R-4 is for. (1) hauska-map PR #384, though internally correct and fully unit-tested, was live-verified to be completely inert: the raw /facets payload for 48021:34049 carries a live-derive disclosure (so sheetEnvelopeIsAtomPathPending was already true) but NO geojson field at all, so envelopeValue()'s new `modelled` branch could never actually fire in production. Root cause: augmentFacetsWithLiveEnvelope (live-envelope-augment.ts) was already fully implemented and unit-tested but had ZERO production callers -- matching a note in a prior lane's (P-151's) own close leaveBehind that I had NOT independently re-verified before assuming my own change would work end to end. Filed and deployed PR #386 in the same session to fix this before treating hauska-map as done -- I did not close on the first, insufficient deploy. (2) The mission's own dispatch told me (for the purpose of briefing the LDT sub-agent) that the top-level parcel `ring` field on the draw block is a CLOSED ring (first point repeats as last). This is wrong: assembleParcelDraw explicitly .pop()s the duplicate closing point before assigning draw.ring, so the shipped field is actually OPEN. The sub-agent caught this discrepancy, followed my explicit instruction for the NEW envelope-overlay field regardless (which I had specified independently, not merely inferred from the old field), and reported the contradiction rather than silently resolving it either way. I confirmed the sub-agent's reading was correct by reading assembleParcelDraw myself. No functional consequence -- the new geom field has no established prior convention to match, since it never carried real geometry before this mission.",
  "leaveBehind": [
    "The map's own visual confirmation (a screenshot of 48021:34049 on smartsite.cloud, signed in on an entitled Pro/Studio/dev-role account, showing the amber inset polygon) is the one predicate leg this close does not carry. I have no browser/Playwright tool in this environment (checked via ToolSearch under 'browser playwright screenshot chromium', confirmed absent) -- this is a real environment gap for this seat, not a design choice, and is being requested from the user directly rather than silently treated as satisfied. Until that screenshot exists, surface-probe.mjs will keep reporting UNMEASURED (never a false PASS) for both P-153 parcels.",
    "hauska-engine's own copy of buildable-display-vocab.ts (packages/engine-core/src/site-plan/buildable-display-vocab.ts) and its parity lock are now OUT OF SYNC with hauska-map's copy (which gained the modelled-figure-withheld token). hauska-map's own peer fixture + lock were refreshed to match hauska-map's new source (so hauska-map's own parity test passes), but the engine-side copy was never touched -- out of scope, different repo/seat. Whoever owns hauska-engine needs to sync packages/engine-core/src/site-plan/buildable-display-vocab.ts and its own parity lock to match hauska-map's new source, or the two copies will silently diverge further on the next hauska-map vocab change.",
    "A pre-existing, independent defect found and fixed as part of this mission's own step 4 (not newly introduced, not previously flagged in this program's docs as far as I found): hauska-map's R1 share-brief path (api/_lib/pe-share-brief.ts -> src/browse/brief-view-model.ts's deriveSetbacksEnvelope) reads the RAW baked envelope wire directly from cortex, entirely independent of the sealed sheet / sheetEnvelopeIsAtomPathPending -- a de facto sixth read path this program's own fact-sheet-resolver.ts header comment says was supposed to have been retired. Before this mission, that path would have printed the raw buildableAreaSqFt/Pct for ANY live-derive parcel with a non-declined envelope status, regardless of Ruling B -- I closed that specific hole (a new isLiveDeriveAtomPending guard, independent of the sheet) as part of satisfying this mission's own explicit ask ('the X-ray/brief print path must print no percent for this variant'), but did not attempt the larger, out-of-scope fix of migrating that whole read path onto the sealed sheet the way the InspectCard panel already is. Flagging this as a real, still-open architectural gap (invariant I1 is still violated by this one file) for whichever lane next touches the R1 brief.",
    "A live, pre-existing data-source disagreement surfaced by the surface-probe run, unrelated to this mission's own change: for 48021:34049, the raw /facets envelope.setbacks (25/5/25/15) disagrees with both the place/buildable-envelope endpoint's own response (30/10/30/20) and the MCP brief's setbacks-envelope section (30/10/30/20, from setbackRulesFact, citing the 2026-04-14 Bastrop Development Code). The facets payload's own embedded secondSource note already flags this as a real two-city-schedule conflict ('the two city schedules conflict — verify which is in effect with the city') -- this is squarely R-1 (most-current-source-wins) territory, already known to the program, and not something this P-153 mission touched or should have tried to resolve. Naming it here only because the probe surfaced it during this close and a future R-1 lane should know it is still live on this exact parcel.",
    "A live GIS finding from the same probe run: the Bastrop County GIS parcels layer, queried around 48021:34049's own record point, returned 44 features and NONE of them contains that point (nearest: 48021:34057 / 911 Farm St, 64 m away). This is not something this mission's own code path touched (P-153 never reads the GIS ring for placement -- that is P-151's territory), and the probe script itself declines to interpret it ('the probe does not choose' whether the record point or the layer is wrong) -- flagging for whoever owns the parcel-placement / GIS-layer accuracy program.",
    "48021:33223 ('P-91 gold parcel' in the shared probe set) was not part of this mission's own two named parcels, but shares the exact architecture this mission changed (real setbacks + live-derive disclosure). A hauska-map unit test fixture shaped like it was updated from 'refuses fully' to 'draws modelled' to match the new, generalized code behavior -- this was NOT independently live-verified against the real production parcel in this close (only the two mission-named parcels were). Whoever runs P-151 or P-152's own probes against 33223 next should expect its envelope to now behave like 34049's, and should verify that live rather than assuming it from this note."
  ],
  "checkpointArtifacts": [
    "_inbox/2026-09-11_p153-draw_cp1.json",
    "_inbox/2026-09-11_p153-draw_cp2.json",
    "_inbox/2026-09-12_024023_surface_probe.json"
  ],
  "whatRemainsOpen": "One thing, stated precisely: the map's own visual rendering of the amber inset polygon for an entitled session has not been observed by me or by the tooling -- surface-probe.mjs reports UNMEASURED (not PASS, not FAIL) for exactly this reason on both P-153 parcels. Everything else the mission's own falsifier and completion predicate name has been independently confirmed live in production: the polygon is drawn on the MCP draw block with the correct vertex count matching the underlying derivation endpoint, the figure is refused everywhere observable, the negative control is untouched, both services' health gates passed post-deploy, and the false 'Withheld, setbacks unruled' text can no longer attach to this parcel's overlay. Requesting the operator's screenshot directly in this session's own reply, per this close's leaveBehind.",
  "planRows": [
    "P-153"
  ],
  "status": "closed",
  "overseerAnnotation": [
    {
      "at": "2026-09-13T22:00:00Z",
      "by": "integration seat (overseer)",
      "note": "Filed 2026-09-13 at the operator's okay as the lane's own record (written 2026-09-12T02:45Z in the property seat worktree, found 2026-09-13). The close of record for P-153 is _inbox/2026-09-12_p153-draw_close.json (overseer-authored); the two agree on the PRs (#384, #386, #661) and the mechanism, and this record adds the lane's own live legs. Its observations file is filed beside it as _inbox/2026-09-12_p153_lane_observations.json."
    }
  ]
}
