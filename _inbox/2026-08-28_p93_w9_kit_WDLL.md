---
id: 2026-08-28_p93_w9_kit_WDLL
title: WDLL — P-93 Smart Site kit write-path (QA W9)
status: approved
last_updated: 2026-08-28
operator_approval: 2026-08-28 verbal go (compile W9, do not fan W8)
plan_row: P-93
---

# WDLL: P-93 Smart Site kit write-path (QA W9)

Date: 2026-08-28  Status: approved
Operator approval: 2026-08-28 (compile the W9 dispatch and hand-carry)
Plan row: P-93
Repo: `hauska-map` `apps/property-explorer`. Isolated worktree from `origin/main`.

Cites QA WDLL `_inbox/2026-08-27_smartsite_qa_program_WDLL.md` item 42 (W9.1–W9.3). Chrome v2 already shipped primitives and `pe-chrome-kit-gate.mjs`. This card freezes the law and arms the gate so a new write cannot silently paint hex or a raw button.

W8 stays queued. W8 depends on W4.P1. P-89 sent is not P-89 live. Do not start W8.

## Done looks like

A new PE chrome file that uses a raw hex or a native `<button>` fails CI. `pe-tokens.css` and `src/styles/pe-chrome.ts` are the only kit. Aug 3 `DESIGN_SYSTEM_BRIEF` and `docs/smart-site-brand` (except the v2 IMPLEMENTED note) are stamped HISTORICAL. Button, Card, Dock, Input, StatusChip, and Modal exist and every surface this card touches imports them. Gold stays a brand mark. Stripe checkout, map overlays, and print gold stay named islands. SmartCity kit is not imported.

## Acceptance items

1. **Kit freeze.** `pe-tokens.css` is the only CSS kit. `pe-chrome.ts` is the only TS primitive module. Aug 3 DESIGN_SYSTEM_BRIEF and pre-v2 `docs/smart-site-brand` files carry a HISTORICAL stamp in the header. Check: file headers. Grade: [ ]

2. **Six primitives.** Button, Card, Dock, Input, StatusChip, Modal exist under `apps/property-explorer/src` (chrome v2 `PE` / `TYPE` / `Button` / `Modal` count if they already export the same contract). Surfaces this card touches import them. Untouched files are not converted. Check: export list plus import count on the touched set. Grade: [ ]

3. **Hex and raw-button gate.** A new raw hex in chrome tsx, or a new native `<button>` in chrome, fails CI except named islands: map overlay, print gold, Stripe. Check: violate the rule in a fixture or a temporary file, watch the gate fail, revert the violation. A pass-only run is not a grade. Grade: [ ]

4. **Gate is armed.** The check that failed in item 3 is the same check CI runs today (`pe-chrome-kit-gate.mjs` and/or ESLint). A script that exists and is not in the PE CI job is dormant and is not met. Check: read the workflow that runs it; name the job. Grade: [ ]

5. **No SmartCity kit.** No import from the SmartCity design kit in PE chrome. Gold is never a button fill. Check: grep the touched tree. Grade: [ ]

## Out of scope

W8 ingest placement. W4 P1 / P-90 engine PDF. Stripe leftovers and the live-key swap. Restyling the whole app. Importing SmartCity kit. Relitigating right-rail vs left stack. Valuation. Feasibility or Comparison generate.

## leave_behind at close

Whole-app conversion of remaining native buttons and inline hex. Hosted-checkout kill if it still exists. W8 after P-89 refuse and P-90 packet.
