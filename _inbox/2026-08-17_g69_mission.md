# G-69 Plan Review product UI

WDLL `_inbox/2026-08-17_g69_plan_review_ui_WDLL.md` approved. Layouts `30c` queue and console. Kit on main. Cite WDLL items 3-7.

## Mission premise

Kit chrome on queue and console. Inverted matrix. Override disabled until reason. Documents stay Smart Files (picker if cheap, else honest empty). No persona-gate as product landing. Dashboards Review compose is a follow if the iframe still shows the gate; preferred: `/?embed=1` or skip gate when iframed.

## Completion predicate

PR on plan-review web/: queue and console use `--sc-*`. Override requires reason. No IBC body. icc-demo is not a cityKey. Planner owns merge and Vercel deploy. Dashboards iframe residual allowed if named.

## Scope basis

G-69 only. Do not rewrite F1-F7 API in `src/`. Do not re-grade G-51. Not G-52. Not atoms `--apply`.

## Workers

Do not spawn further agents. Isolated worktree from origin/main.

1. Shell + queue + console chrome (WDLL 3-5). Keep `renderTab` logic. PR.
