---
title: Planner handoff — next doc_repo planning session (post launch-gate program stand-up)
date: 2026-08-09
status: active-handoff
from: doc_repo planner session 2026-08-09 (second)
---

# Planner handoff (also pasted in chat for hand-carry)

You are the doc_repo planner (strategic plus execution, P:\doc_repo). Read in order: `_STATE.md`, `90_operations/OPS-14_texas_flush_game_plan.md`, `_decisions/2026-08-09_texas_flush_launch_gate.md` (+ footprint decision with full-state amendment), the two WDLLs in `_inbox/` (W1 writers, W5 depth factories, both approved-frozen), and `_sessions/2026-08-09_launch_gate_and_program_planning_claude_code.md` including its late-session addendum. CANON-PREAMBLE v0f465c77 regenerates via `node scripts/dispatch-preamble.mjs`.

## Standing rules (non-negotiable, all measured-in-anger this week)

Verify at source (`gh` / SQL / live endpoint) before acting on ANY state claim, including this file. Counts live behind queries, never prose. One bulk-writer slot per database, handoff recorded in `_STATE.md`; the atoms slot is a single-owner chain. Every dispatch: CANON-PREAMBLE, no-nesting first line, exit-bounded verification, machine-checkable close artifact, and IN-PROCESS adversarial review (independent reviewer, pre-registered expectations, two checkpoints). Merge only on CI conclusion string SUCCESS. Deploys are planner-owned; new revision != serving. CI failure explanations require independent log reads. Honesty rule everywhere: report what IS (the 6/7 cert finding is the model).

## The board (verify each before touching)

| Lane | State at handoff | What it owes / what you owe it |
|---|---|---|
| D (W1 slot chain) | Steps 1-2 CLOSED (48021 minted 62,394; scorer applied, ledger 38→89 cells / 0.897%). Slot with H. | On H6 close: resume sweep (~80 counties from 48457), re-run scorer, then D1 tranches (cad/land-use/flood, cost checkpoints on the 0072/0073 tables). Verify close artifacts at source. |
| G (cert) | DONE. #292 merged `fb64326e`; block13 7/7 txgio frame; fixture 8/8. | G2b queued: adversarial 15-of-188 sample, then re-promote the stale-role cohort in an atoms-slot window; W3 cert re-earn inherits the 188 list. |
| H (roads) | Holds atoms slot for H6 throwaway apply. PR #293 open DO-NOT-MERGE. Supersede simulation PASS (7,249 protected). | After H6: adversarial re-review of all six items (settle the 0.05ms vs 12.5ms benchmark discrepancy), merge #293, then the STATEWIDE ROADS GO decision is YOURS, evidence-based. Merged PR is not a go. |
| E3 (Elgin) | Dry-run anchors-clear (499/500) but NOT legacy-parity (75/98 vs 102/72). | E3-ADV re-review of the bucket delta (engine drift vs decline taxonomy) BEFORE any apply; apply needs slot + your go. |
| K (PMTiles bake) | K3 statewide bake running; K4/K5 continuing in-session per operator. | Verify K4 checkpoint + icon test (Harris west ~−95.96 renders); K5 deploy with rollback flag; ends READY-FOR-R6 — the operator browses the map as the final gate. |
| L (MCP P0) | CLOSED PASS. Catalog path restored (retrieval `00061-bib`, MCP `00041-x56`); gold parcel serves GC chain; honest-pending on fabric-only. | Two unmerged PRs owed (engine cloudbuild/DEPLOY.md; mcp health-probe branch) — chase to merge. Then dispatch P1 (chain schema 1.15.0 families, Cotality copy purge, contract pin bump, brief substrate wiring) per `_inbox/2026-08-09_76j_Workstream_F_mcp_revival_scope.md`. |
| J (billing) | Audit returned. Mechanics shipped; Stripe/PE/settled-ladder three-way misaligned. NO real-money mode. | Draft the code dispatch (ldt ladder setup script + PE pricing.ts to $0/$20/$40/$75 Smart Site branding). Operator owes: human promo checkout, dev_role smoke, claim smoke, and three product calls ($15 unlock keep/kill, Home/Team checkout routes, free-tier definition 5-briefs vs 3-chat). |
| D2 (footprint/easement writers) | PRs #294/#295 open with checkpoint-1 holds (ML zip streaming, registry routing). | Review to merge; no statewide apply without the WDLL acceptance path. |

## Planner debts (yours, not dispatchable)

1. **The two precondition hooks from OPS-14** (dirty-tree close gate; dispatch-template check). Ruled preconditions; the briefs outran them. Build before cutting the next wave of briefs.
2. **Doc cleanup lane** (OPS-14 list): ratify `_STATE.md` as successor to `00_current_state.md` + amend CLAUDE.md session protocol; move CLAUDE.md dated status paragraphs to a historical record; PHASE_C_HANDOFF:35 BCAD correction block; retire OPS-3 leak-1; memory store under git; white paper section 4 narrowing.
3. **Grade the Finish cards** (W1/W5 WDLLs) as lanes close; drift goes in the cards, not prose.
4. **Statewide roads go/no-go** when H's evidence lands.
5. **W3 cert re-earn planning** once G2b closes (188-parcel list + the nine jurisdiction-first counties, in the true frame).
6. Watch the pending question from OPS-12: wire the county-extent instrument per vintage (C2 prototype exists) before the next L2-class acquisition wave.

## Operator open items (do not nag; they are on his list)

Human checkout click + dev_role + anonymous-claim smokes (J runbook actions); R6 map browse when K reports ready; the three billing product calls; Donley CAD reply watch (public-records request sent).

## What the operator sees next

The Command Center ledger already jumped (0.897 percent). When K lands: the statewide map — every acquired county browsable, Harris rendering west of the old wall. That screenshot is the proof artifact of the whole program to date.
