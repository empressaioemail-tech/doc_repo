{
  "lane": "p221-xray-feasibility-subset",
  "planRows": ["P-221"],
  "checkpoint": "CP2 (pilot/mid-work)",
  "timestamp": "2026-09-15",
  "type": "not applicable: solo lane, no sub-agents spawned",
  "built": [
    "packages/engine-core/src/site-plan/dossier-author.ts: authorParcelPropertyDossierExport rewritten to call composeParcelReport (report-model.ts) once, deriving geometry/verdict/facts from the SAME ParcelReportModel authorParcelFeasibilityExport reads. New exported composeXrayBrief(model) implements the Solo-tier allow-list (jurisdiction/flood/footprint/specialDistricts/wellsPipelines/utilities -> a fact row ONLY when the section's own state is genuinely 'present' with real content; parcelOwnership/floodplainAcreage/firmPanel/soil/electricProvider/gasProvider/dischargePoint/drainage/terrain/narrative/hoa never contribute). content no longer accepts verdictLine/brief -- only address/countyName/chatSummary/notes/liveViewUrl remain caller-supplied. Site-plan sheet honesty flags (setbackDegenerate/setbackHonestAbsence/streetHonestAbsence/zoningHonestAbsence/floodZoneHonestUnavailable) preserved, sourced from model.geometry.model directly.",
    "services/engine-api/src/routes/parcel-terrain.ts: dossierRefreshBody zod schema no longer parses verdictLine/brief; the POST handler no longer forwards them. Hollow-download refusal's remedy text rewritten to name a retry (available identically on web app and MCP connector) instead of 'Open the property brief and try again' / 'Refresh with a resolved brief first' -- the second, smaller defect the dispatch named.",
    "packages/engine-core/src/site-plan/__tests__/dossier-author.test.ts: existing fixture updated (verdictLine/brief removed from the request content); existing assertions updated to the real derived values (briefFactCount 2: County + Lot area, given this fixture seeds no atoms); the geometry-unavailable test now asserts briefFactCount === 0 (Falsifier 1, proven at the engine-core level, not just described). New describe block composeXrayBrief (Solo/Studio allow-list) with two tests: (a) a fully-resolved model with every Studio-only family present carrying a distinct marker string -- asserts none of the 12 markers appear anywhere in the serialized derived sections (Falsifier 2), and that verdictLine is the model's package.verdict verbatim (Falsifier 4); (b) a model with every allow-listed family genuinely absent -- asserts zero derived sections (hollow case reachable by construction, not just by geometry failure).",
    "services/engine-api/src/__tests__/dossier-export-route.test.ts: route-level mock restructured -- hollowness is no longer request-content-driven (it is now a property of the parcel's data, resolved inside the author, which this route-level test correctly no longer simulates via request shape); the one hollow-download test now overrides the mocked author via mockImplementationOnce to return a hollow result directly, and still proves the SAME route-level 422/pipeline_output_absent refuse-on-download behavior fires."
  ],
  "verified": [
    "pnpm -r run typecheck: exit 0 across all 19 workspace projects with a typecheck script (full monorepo, not just the touched packages).",
    "packages/engine-core full suite: 206/210 test files pass, 1657/1668 tests pass. The 4 failing files (scripts/__tests__/cert-grade-and-report.test.ts and three siblings) fail on a pre-existing SyntaxError unrelated to this change -- confirmed via `git diff --stat origin/main -- scripts/` returning empty (zero lines touched under scripts/ by this lane).",
    "services/engine-api full suite: 19/19 files, 116/116 tests pass, including dossier-export-route.test.ts, feasibility-export-route.test.ts, site-plan-setback-gate.test.ts, flood-drainage-route.test.ts -- confirming no regression to any sibling route."
  ],
  "falsifierRunResults": {
    "falsifier1_hollowStillFires": "CONFIRMED. dossier-author.test.ts's geometry-unavailable case (no atoms, no resolvers, geometry composition throws) now asserts result.briefFactCount === 0 for real, driven by real composeParcelReport/composeXrayBrief execution, not a mock. The route-level 422 refuse-on-download path is proven separately in dossier-export-route.test.ts.",
    "falsifier2_noStudioLeak": "CONFIRMED. composeXrayBrief's dedicated test constructs a model with parcelOwnership, floodplainAcreage, firmPanel, soil, electricProvider, dischargePoint, terrain and the generated narrative all fully PRESENT with distinct marker strings, and asserts zero of the 12 markers appear in the derived output.",
    "falsifier3_derivedNotAssembled": "CONFIRMED BY CONSTRUCTION. authorParcelPropertyDossierExport's only path to a verdict/brief is composeParcelReport -> composeXrayBrief; verdictLine/brief are not present in AuthorParcelPropertyDossierExportOptions.content's type at all, so there is no code path left that could independently assemble them from caller input even by mistake.",
    "falsifier4_noCrossDocumentDisagreement": "CONFIRMED BY CONSTRUCTION AND TEST. xrayBrief.verdictLine is literally model.package.verdict with no transformation -- the same string authorParcelFeasibilityExport's own PDF prints for the same model. Asserted directly in the composeXrayBrief test."
  },
  "codeReview": "Self-reviewed via full re-read of the diff after writing it, plus the type-check/test runs above as the adversarial check a solo lane substitutes for a second reviewer per this lane's own CP1 (no sub-agent was spawned to review independently)."
}
