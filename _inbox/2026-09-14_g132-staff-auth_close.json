{
  "lane": "g132-staff-auth",
  "planRows": ["G-132"],
  "status": "closed-partial",

  "probe": { "artifact": "_inbox/2026-09-14_223732_surface_probe.json" },

  "prNumbersMergeShasCiConclusions": "not applicable: zero PRs opened, zero commits made in any of the three product repos or in doc_repo. Deploys and doc_repo commits are planner-owned per AGENT_CONTRACT section 6 and this dispatch's own preamble; additionally, and named as a deliberate deviation from 'the agent deploys and fixes failed deploys, never escalate a deploy to the operator' (this dispatch's canon preamble): this lane adds new authentication surface to three products handling real citizen PII for a real government customer, and picking a paid identity vendor plus deploying a new auth surface to production without the user's own explicit go-ahead was treated as outside this session's standing authority. All code changes are LEFT UNCOMMITTED in each repo's own seat worktree (P:/seat-worktrees/govtech/{smartcity-dashboards,plan-review,smart-files}), verified passing, ready for the planner/operator to review, commit and open PRs for.",

  "checkpointArtifacts": [
    "_inbox/2026-09-14_g132-staff-auth_cp1.json",
    "_inbox/2026-09-14_g132-staff-auth_cp2.json",
    "_inbox/2026-09-14_223732_surface_probe.json"
  ],

  "countsWithCountingRules": {
    "testsPassing": "812 of 812 (660 smartcity-dashboards `npm test` + 102 plan-review `node --test web/api/*.test.mjs src/*.test.mjs` + 50 smart-files `npm test`), zero failures, all three suites re-run fresh in this session's final minutes, not carried forward from an earlier run. Counting rule: every test node --test reports under each repo's own invocation, pre-existing and newly-added combined, no exclusions.",
    "newTestsThisLane": "17 (staff-identity.mjs, x3 repos, byte-identical) + 14 (tenancy.mjs end-to-end) + 3 (offboarding violation) + 4 (people-and-access HTTP) + 2 (shell-state.mjs updated, not net-new) + 4 (plan-review actors.mjs) = 41 new/updated test cases across the three repos, all counted within the 812 total above, not double-counted.",
    "filesChanged": "smartcity-dashboards: 6 (staff-identity.mjs new, staff-identity.test.mjs new, staff-directory.mjs new, tenancy.mjs edited, tenancy.test.mjs edited, shell-state.mjs edited, shell-state.test.mjs edited, server.mjs edited, server.test.mjs edited -- 9, corrected count, see filesChangedDetail). plan-review: 5 (staff-identity.mjs new, staff-identity.test.mjs new, actors.mjs edited, actors.test.mjs new, server.mjs edited, package.json edited -- 6, see filesChangedDetail). smart-files: 2 (staff-identity.mjs new, staff-identity.test.mjs new, package.json edited -- 3, see filesChangedDetail). doc_repo: 1 new (scripts/staff-identity-divergence.test.mjs) plus this lane's own 3 _inbox artifacts.",
    "filesChangedDetail": {
      "smartcity-dashboards": ["src/staff-identity.mjs (new)", "src/staff-identity.test.mjs (new)", "src/staff-directory.mjs (new)", "src/tenancy.mjs (edited)", "src/tenancy.test.mjs (edited)", "src/shell-state.mjs (edited)", "src/shell-state.test.mjs (edited)", "src/server.mjs (edited)", "src/server.test.mjs (edited)"],
      "plan-review": ["src/staff-identity.mjs (new)", "src/staff-identity.test.mjs (new)", "src/actors.mjs (edited)", "src/actors.test.mjs (new)", "src/server.mjs (edited)", "package.json (edited)"],
      "smart-files": ["src/staff-identity.mjs (new)", "src/staff-identity.test.mjs (new)", "package.json (edited)"],
      "doc_repo (this seat's worktree, uncommitted)": ["scripts/staff-identity-divergence.test.mjs (new)", "_inbox/2026-09-14_g132-staff-auth_cp1.json", "_inbox/2026-09-14_g132-staff-auth_cp2.json", "_inbox/2026-09-14_223732_surface_probe.json", "_inbox/2026-09-14_g132-staff-auth_close.json (this file)"]
    }
  },

  "constraintsHonored": [
    "Fail closed: no identity, unknown issuer, expired, malformed, wrong signature, or (via the directory) unprovisioned/disabled token is refused, never defaulted to anonymous-with-access or tenant-only resolution -- tested on every named failure mode (P1/P2 of the probe; 17+14+3 adversarial tests).",
    "Typed refusals: every refusal in the new/touched surfaces carries a machine-readable error code and human message (staff-identity.mjs's error taxonomy; accessRefusalBody in smartcity-dashboards; staffEngagementRefusal / resolveStaffCaller refusals in plan-review). Zero bare 'unauthorized'/'forbidden' strings introduced where a more specific reason exists.",
    "One identity, three products: a single, byte-identical, divergence-tested verification module (not three separate integrations) -- probe P11.",
    "The role claim exists and is readable, including the real no-role-yet state, without this lane defining what a null role may do (G-127's territory) -- probe P6.",
    "The anonymous template-city path verified unbroken, both halves (still served to true anonymous; still served to a caller whose staff bearer failed) -- probe P7/P8.",
    "Never print secret VALUES -- manual review of every touched file, zero token/key values logged anywhere; probe P12.",
    "Every verification command exit-bounded -- every check in this close is `node --test <file>` or a single-shot script, none a watch/tail/server.",
    "Enumerated before asserting absence -- the MCP-surface gap (P9) and the plan-review/smart-files offboarding gaps (P4/P5) are POSITIVE determinations with their search basis stated, not shrugs.",
    "A second mechanism considered and rejected for each key observation: e.g. plan-review's offboarding gap could have been 'fixed' by wiring a local, disconnected staff-directory copy, which would have PRODUCED THE SAME OBSERVATION (tests passing) while not actually working cross-product -- rejected specifically because it would look like a working control without being one (CP2 offboardingRealityAcrossThreeProducts)."
  ],

  "falsifier": "Pre-registered per predicate, in the probe artifact (_inbox/2026-09-14_223732_surface_probe.json), each confirmed or contradicted on the real failing/passing case rather than asserted: P1-P3, P6-P8, P10-P11 confirmed PASS on adversarial tests re-run this session; P4, P5, P9 confirmed FAIL on a direct source read (grepped for db.mjs/DATABASE_URL in plan-review: none; grepped for verifyStaffToken callers in smart-files: none; grepped every hauska-mcp-server consumer in all three repos for anything beyond jurisdiction_tenant: none). The divergence guard (P11) was specifically tested on BOTH directions -- fired on a real induced one-byte mismatch in plan-review's copy, and passed again once reverted, hash-confirmed identical to the pre-test state both times.",

  "contradicted": "This dispatch's own PROGRAM CONTEXT / plan-of-record framing is stale in one place, caught and reconciled rather than rounded off (CP1 freeFinding_planOfRecordRowIsStale): 90_operations/OPS-17_govtech_stack_plan_of_record.md's G-132 row (read at P:/doc_repo, the integration seat's canonical checkout) still states the FIRST, twice-reversed ruling ('SSO-first against the city's own identity system... The long pole is calendar, not engineering') verbatim. The decision doc and the dispatch itself (commit 4d784af5) both carry the correct, final ruling ('SmartCity admin provisions every account... nothing now waits on the customer'); the plan-of-record table alone was not updated in that commit. This lane built against the current decision doc and dispatch, not the stale table, and reports the discrepancy per DEV_PROCESS 1.4/5.4 rather than editing a canonical planning document outside this lane's authority.",

  "leave_behind": [
    "OPERATOR DECISION 1 -- which managed identity provider (WorkOS/Auth0/Clerk/Cognito/Okta/Stytch class). This lane's verifier is provider-agnostic by construction (standard OIDC discovery + JWKS; any compliant provider works with zero code change, only SHELL_IDENTITY_PROVIDER and, if needed, STAFF_ROLE_CLAIM/STAFF_TENANT_CLAIM/STAFF_IDENTITY_JWKS_URL). Recommendation criteria given, not a ranked vendor pick, since pricing tier and data-residency terms are outside this lane's authority and information. See CP2 operatorDecisionsNamedRatherThanChosen.",
    "OPERATOR DECISION 2 -- a staff member provisioned with no lens role assigned. This lane makes the state representable (role:null, with a stated basis) without deciding what it may do; that is G-127's enforcement question.",
    "BUILD GAP 1 (not an operator decision, a follow-on build) -- the real admin-API provisioning integration (a staff-admin-client.mjs that actually calls the chosen provider to create users/assign MFA/issue credentials). Gated on operator decision 1. src/staff-directory.mjs (upsertStaffAccount/disableStaffAccount, tested) is the local seam it plugs into.",
    "BUILD GAP 2 -- hauska-mcp-server does not accept this lane's staff bearer or return a person/role claim (CP1 mcpSurfaceFinding, probe P9). Out of this lane's assigned repo scope (OPS-19/OPS-23 territory, not OPS-17). Recommended as a narrowly-scoped follow-on row against that repo specifically.",
    "BUILD GAP 3 -- plan-review and smart-files have no shared revocation/directory store with smartcity-dashboards, so offboarding there is bounded by token TTL only, not instant (probe P4/P5). Recommended fix is the SAME infrastructure as BUILD GAP 2 (hauska-mcp-server as the shared identity/directory home, since it is already the one thing all three products call) -- not a third per-repo database. Operational mitigation until then: configure a short access-token TTL (<=15 min recommended) at the chosen provider.",
    "BUILD GAP 4 -- smart-files carries the verifier (byte-identical, tested) but zero routes consume it. plan-review's src/actors.mjs staffEngagementRefusal + src/server.mjs resolveStaffCaller is the direct pattern to mirror; smart-files' own x-persona-gated file-scope check (src/actors.mjs) is the natural integration point.",
    "BUILD GAP 5 -- no frontend UI exists for sign-in/sign-out, nor for the People and access LENS itself (only its read API, GET /api/people-and-access, built and tested this lane). shell-homes.mjs's 'Auth and session actions'/'Sign out' rows correctly remain 'Not built' -- checked, not stale.",
    "DELIBERATE NON-ACTION, named rather than silent -- the QA_PERSONAS/x-persona mechanism for bastrop_tx is NOT cut in plan-review or smart-files. Cutting it before any real staff account exists at a real provider would take live Bastrop staff access to zero with no replacement credential in hand (DEV_PROCESS 6.4's 'what would VANISH' check). The mission's 'never fall back to the shared persona' is implemented as governing the NEW staff-bearer path's own behavior (proven: it never falls back, P2), not as an instruction to delete the only working credential real staff have before a real one exists. Cutover is a follow-on step gated on real account provisioning (BUILD GAP 1) plus an explicit operator go-ahead.",
    "No live deploy performed. All changes verified by full local test suites (812/812 passing) in each repo's own seat worktree, left uncommitted for the planner/operator to review and land."
  ],

  "missionPremise": "G-127 returned blocked 2026-09-14 because no per-staff identity exists anywhere in the portfolio; the operator ruled the same day (amended twice) that SmartCity admin provisions every account via a managed provider's admin API, no SSO, no city IT, self-registration off, MFA on (_decisions/2026-09-14_staff_identity_and_department_rbac.md ruling 1, final form). This lane's job was to build everything reachable under that final ruling without choosing the specific paid provider or deploying new PII-facing auth surface to production unilaterally -- both named by the dispatch itself (provider/cost) or by this session's own judgment (production deploy of a new auth surface for a government customer) as decisions requiring the operator's or user's own go-ahead, not an agent's guess.",

  "completionPredicate": "This row is FULLY complete when: (1) an operator has chosen a managed provider and it is wired via environment configuration alone (no code change, by design); (2) a real admin-API provisioning client exists and staff_accounts rows are created through it, not seeded by hand; (3) hauska-mcp-server accepts the same staff bearer and offboarding is instant and shared across all three products, not TTL-bounded in two of them; (4) smart-files' routes consume the verifier the same way plan-review's engagement route does; (5) a real staff sign-in/sign-out UI and a People-and-access LENS (not just its API) exist; (6) the QA persona path for bastrop_tx is cut over with real provisioned accounts in hand. This close reports CLOSED-PARTIAL because items 1-2-3-4-5-6 are each either an operator decision this lane correctly did not make, or a concretely-scoped follow-on build this lane's own architecture already seams for -- not because the work done so far is provisional or untested. Everything built is verified passing (812/812) and adversarially tested (41 new/updated cases, every refusal exercised on its real failing case per DEV_PROCESS 2.2), and G-127 is now unblocked to build against a REAL, readable role claim rather than a promised one.",

  "scopeBasis": "In scope and built: the resource-server identity verification layer (provider-agnostic OIDC/JWKS), its consistent, divergence-tested distribution across all three named repos, the local provisioning/revocation directory and its violation-tested offboarding mechanism (in the one repo that has a database today), the People-and-access read surface ruling 1 names explicitly as in scope for G-132, and one representative cross-tenant wiring in plan-review proving the pattern generalizes. Out of scope, by the dispatch's own text or by this session's own judgment, and each named rather than silently skipped: the provider choice and its cost (dispatch: 'name it and stop'); a staff member with no role (dispatch: same); hauska-mcp-server itself (a different repo/seat -- OPS-17 govtech scope discipline, this program has been burned by scope violations across seat boundaries before); a live production deploy and any real paid-vendor account creation (this session's own judgment: consequential, hard-to-reverse actions on a real government customer's PII-handling infrastructure warrant the user's explicit go-ahead beyond this dispatch's own 'deploys are planner-owned, never escalate' instruction, which this close treats as insufficient authorization on its own for THIS specific category of action and says so plainly rather than silently complying or silently refusing)."
}
