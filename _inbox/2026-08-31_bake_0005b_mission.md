# Bake-migrate job for 0005b — identity alias on bake neondb

## How to work this card

Do NOT spawn sub-agents. Hand back, do not land. No commit, push, deploy,
or store write. Do not apply 0005b. Job executions are planner-run.

## Why this card exists

`0005a` is on Factory `migrations/` and is proven live
(`landing_setback_registry_absence_probed`, 23514, rolled back, survived 0).
`0005b` lives under `migrations/bake/0005b_landing_cad_txgio_alias.sql`.
`applyMigrations` reads `migrations/` only (non-recursive; bake-dir regex
rejects `bake/`). `factory-conformant-migrate` and `factory-publish-migrate`
are the Factory store. There is no job that applies `migrations/bake/` to
`STAGING_NEONDB_URL` / `PRODUCTION_NEONDB_URL`.

0005b is CAD `prop_id` ↔ TxGIO identity (`ownersAgree`,
`cad-roll-address-join`). It is not the `breadth_*` jurisdiction table.
Do not drop it. `landing_cad_txgio_alias` must exist where
`alias-persist --apply` writes (`resolveTargetStores` → bake `DATABASE_URL`).
Factory `FACTORY_DATABASE_URL` is unreachable from that function.

## Build

A Cloud Run job (or an existing job with a distinct args template) that:

1. Reads only `migrations/bake/*.sql` in filename order.
2. Targets bake `neondb` via `STAGING_NEONDB_URL` / `PRODUCTION_NEONDB_URL`.
   Refuses `FACTORY_DATABASE_URL`.
3. Refuses a missing `--target=` and a missing URL. `--name=value` form.
4. Leaves a durable record of the files applied (name, timestamp, invocation).
   A count is not a record. If the record cannot be written, the apply does
   not run.
5. Never sees `0005a`. A test asserts the Factory migrate file list has
   0005a and not 0005b, and this job's file list has 0005b and not 0005a.

## Acceptance (file-side; live apply is planner-run)

- Fail-arm: pointing this job at Factory URL refuses before connect.
- Fail-arm: `alias-persist --apply` against a store without 0005b still
  refuses relation-missing (already landed; do not weaken it).
- Pass-arm: dry plan names `0005b_landing_cad_txgio_alias.sql` and the bake
  target, not Factory.
- Both arms shown to fire. Do not apply.

## Do not

Laptop `psql`. Apply 0005 as drafted. Apply 0005b from this lane. Use the
Factory migrate job. Use `hauska-factory-main-migrate` for anything except
reading. Touch P4 rails or the containment join. Start Wave R.
