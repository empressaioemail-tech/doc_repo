{
  "checkpoint": "CP2",
  "lane": "P2b-serve",
  "card": "P2b serve honesty — X2 edge disposition WITH sourceVintage, five one-liners, PE scope",
  "planRows": ["P-92"],
  "writtenAt": "2026-08-30T22:45:00Z",
  "seat": "property P2b-serve in P:/seat-worktrees/property/legacy-design-tools-p2b-serve on seat/property-ctx-p2b-serve HEAD 28969a36; uncommitted; no sub-agents; no commit/push/deploy",
  "pilot": {
    "whatRan": "vitest run of parcelDrawStub, parcelDrawFromReads, p2bServeSourceContract, floodHazardFactRead, plus earlier well/pipeline/specialDistrict/boundary reads",
    "result": "56 tests in the four-file re-run, all passed. Broader fact-read suite 148/149 then 7/7 fromReads after the queryPoint-null helper fix.",
    "tscUnion": {
      "oldShape": "_leave_behind/tsc-fixtures/tsconfig.old-fails.json exit 2, TS2322 Property 'reciprocity' is missing",
      "newShape": "_leave_behind/tsc-fixtures/tsconfig.new-passes.json exit 0",
      "productionStub": "tsc --strict --noEmit src/lib/parcelDrawStub.ts exit 0"
    }
  },
  "bothArms": {
    "refutedNeighbour": "GOLD edge 1 with neighborCheck contradicted emits refused + agentGuidance, not present. Self-neighbour 34137→34137 refused from the payload.",
    "reciprocalGold": "48021:34137 edge 1 (34169) with neighborCheck reciprocal stays present + reciprocity pass.",
    "uncheckedNeighbour": "edge 2 (34121) with no check is unknown.",
    "vintagePresent": "flood/well/specialDistrict/pipeline-plain-absent with sourceVintage on the atom body emit absent-verified through tryAssembleParcelDrawFromReads (interpret*Rows, not a hand-built assemble input).",
    "vintageMissing": "the same four without sourceVintage emit unknown / provenance unknown; vintage unknown."
  },
  "oneLinersObserved": {
    "landUseBakeKeys": "code+description+vintage:2025 → attrs.landUse.desc and taxYear 2025",
    "yearBuilt": "bake facets.yearBuilt / baseFacts.yearBuilt ignored; only structural cad_property populates, with source on the wire",
    "manifest": "source contract: manifestLayers takes EnvelopeBriefRefusal, layers always [], degraded true, no envelopeGeojson read",
    "sourceAdapter": "descriptor-fixture on the gold edge",
    "anchor": "queryPoint → {lat,lng}; null queryPoint → frame.anchor null"
  },
  "adversarialMid": {
    "queryPointNullBug": "drawFrom used ?? so an explicit null still got the default gold point. Fixed. That is why the last fromReads test failed once.",
    "fullProjectTypecheck": "pnpm run typecheck against a junctioned node_modules from P:/legacy-design-tools (feat/s1-instrument-hardening, not 28969a36) is not a card result. Failures are missing workspace exports on that install. Card files: stub isolated tsc 0; union fixtures as above.",
    "p1Overlap": "P1-LDT is widening the same DrawEdge union in another tree. This tree also restores sourceAdapter. Planner lands; do not merge blindly."
  }
}
