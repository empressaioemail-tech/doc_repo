# OPS-23 wave 6 — interruption recovery (A-148 addendum)

    date: 2026-09-14
    authored: dispatch planner, `P:/seat-worktrees/dispatch-planner/doc_repo`
    trigger: operator/overseer addendum A-148, commit `5ac88890`
             ("finding(F24 resolved): Bastrop's current zoning layer says 30/10/30/20 in text and
             25/5/25 in stale numeric columns that the One Click join reads")
    basis: `90_runbooks/AGENT_CONTRACT.md` section 2 (interruption recovery) +
           `90_runbooks/interrupt_note_template.md`

## 1. What happened

New input arrived from the operator carrying the overseer's A-148 addendum. Per AGENT_CONTRACT
section 2, new input STOPPED the four running wave-6 lane agents. Their state was captured
BEFORE acting on the addendum, and this note is that capture (contract item 5: each stopped
agent's disposition, with evidence).

Doc repo integration state: planner worktree was at `d23edddc` with the wave-6 dispatches,
the CP1 and the baseline probe uncommitted. Addendum `5ac88890` was fetched and the worktree
fast-forwarded `d23edddc..5ac88890` (the only local modification, a regenerated date stamp in
`_catalog/DISPATCH_PREAMBLE.md`, was reverted first; it is regenerated by the compiler anyway).

## 2. Stopped-worker disposition (evidence captured at 2026-09-14 ~13:55Z / 08:55 local)

Every lane worktree was inspected with `git status --porcelain`, `git rev-parse` and
`git diff --stat`. Nothing was discarded; nothing was reconstructed.

| # | lane | worktree | branch | HEAD | state at stop | disposition |
|---|------|----------|--------|------|---------------|-------------|
| 1 | p154-conflict | `hauska-atom-contract-p154-conflict` | `feat/p154-conflict-row-vocab` | `2132cd2` | **1 commit, pushed to origin, NO PR** | recovered / resumed — must AMEND, not redo |
| 2 | p154-conflict | `hauska-engine-p154-conflict` | `feat/p154-conflict-row` | `14c7e85` | **32 files modified, 753 insertions / 124 deletions, UNCOMMITTED, 0 unpushed commits** — see correction (c) | recovered / resumed in place |
| 3 | p154-conflict | `hauska-map-p154-conflict` | `feat/p154-conflict-row-panel` | `9841c1b` | clean, no work yet | resumed |
| 4 | p154-conflict | `legacy-design-tools-p154-conflict` | `feat/p154-conflict-row-mcp` | `82857ebb` | clean, no work yet | resumed |
| 5 | p156-bastrop | `hauska-factory-p156-bastrop` | `feat/p156-city-completeness-declaration` | `8771c51` | clean, no work yet | resumed (with amendment) |
| 6 | p156-bastrop | `hauska-engine-p156-bastrop` | `feat/p156-city-declaration-consumer` | `14c7e85` | clean, no work yet | resumed (with amendment) |
| 7 | p178-publish | `legacy-design-tools-p178-publish` | `fix/p180-retire-excluded-with-current-run` | `82857ebb` | **3 files modified, 643 insertions / 11 deletions, UNCOMMITTED, not pushed** | recovered / resumed in place |
| 8 | p178-publish | `hauska-factory-p178-publish` | `chore/p178-pin-and-publish` | `8771c51` | clean, no work yet | resumed |
| 9 | p183-querypoint | `hauska-engine-p183-querypoint` | `fix/p183-querypoint-from-geometry` | `14c7e85` | clean, no work yet | resumed |
| 10 | p183-querypoint | `hauska-factory-p183-querypoint` | `fix/p183-geocoded-cells-crosswalk` | `8771c51` | clean, no work yet | resumed |

### Worker 1 — the p154 vocabulary commit (the addendum lands ON this work)

`2132cd2` "feat(vocab): one conflict-note sentence for the R-1 conflict row (P-154, OPS-23 wave 6)"
on `feat/p154-conflict-row-vocab`, `hauska-atom-contract`. Adds `setbackConflictNote`, the
`setback-conflict-second-source` VOCABULARY row and the snapshot/test; bumps 1.34.0 -> 1.35.0.
5 files, 172 insertions. **Pushed to origin** (`git ls-remote origin` confirms
`refs/heads/feat/p154-conflict-row-vocab = 2132cd26e114f423d5efbc27254c95332f2a11d9`), **no PR
opened** — so no limbo PR. Its committed test asserts the sentence the addendum supersedes:

    The city's One Click card shows 25/5/25/15 citing Ord. 2019-51 (repealed 2026-04-14).

A-148 rules that this mechanism is wrong (one current layer with unrefreshed numeric columns, not
two layers citing different ordinances, and no numeric corner column exists, so 25/5/25 not
25/5/25/15). Disposition: **resumed as a supervised continuation that amends 2132cd2's sentence
and its test/snapshot in place.** The type work, the token and the version bump stand.

### Corrections to this capture (append-only; the table above was amended in place for row 2)

**(c) The p154-conflict ENGINE worktree was NOT clean at the stop — the largest at-risk uncommitted
body in the wave.** The table's original row 2 said "clean, no work yet" because that was the true
reading at the capture instant. The work landed AFTER the capture and before the stop, so the
original row understated the risk. Corrected record, measured 2026-09-14 ~08:25 local:

    worktree: P:/seat-worktrees/property/hauska-engine-p154-conflict
    branch:   feat/p154-conflict-row
    HEAD:     14c7e85 (= origin/main; 0 unpushed commits)
    state:    31-32 files modified, 753 insertions / 124 deletions, entirely UNCOMMITTED

Nothing in it is pushed. It only stays recoverable if the worktree survives, so it is captured
twice, and the two captures are byte-identical (sha256
`52FD8079596F9E13202BF3994CB6AE027E5FE67B099587BA10A971D777EF3CA3`), which also proves the
resumed lane had stopped editing before taking its own capture rather than reconstructing over it:

- the lane's own capture: `_inbox/2026-09-14_p154-conflict_engine-uncommitted-full.diff` (143,072 bytes)
- the planner's snapshot: `_inbox/2026-09-14_p154-conflict_engine_planner_snapshot.diff` (143,072 bytes)
  plus `..._planner_snapshot.status.txt`

The predecessor's partial capture at 08:10 is also kept: `_inbox/2026-09-14_p154-conflict_engine-uncommitted.diff`
(22,542 bytes) — the diff as it stood when the predecessor first recorded it, and the evidence that
this body grew from clean at the planner's 13:55Z capture, to 22.5 KB by 08:10 local, to its
present size.

**Timeline note.** The capture in section 2 was taken at ~13:55Z; the predecessor's own small
capture is stamped 08:10 local and the resumed lane's full capture 08:21 local. So the engine body
was written AFTER the capture and BEFORE the resumed lane arrived. The resumed lane found it and
preserved it instead of rebuilding — the contract's "recover, never reconstruct" holding under a
real test, and only because the resumed lane was told to capture before acting.

**Lesson (for M0, not promoted here).** A state capture is a point-in-time reading, not a property
of the worker. A planner that captures worktrees at T and reports them as the stop state is wrong if
the workers keep writing after T. The safe form is to re-read each worktree at the moment of
resumption and let the resumed lane's own capture be the authority — which is what happened here,
and only because the resumed lane was instructed to capture before acting.

### Worker 7 — the only copy of the P-180 walk fix

`legacy-design-tools-p178-publish`, branch `fix/p180-retire-excluded-with-current-run` @
`82857ebb`, carries an at-risk uncommitted body (the p154 engine body above is larger, 753/124 over
31 files, but this one was the first found and the only one that had no other capture): **uncommitted
work, no other copy until this backup.**

    M artifacts/api-server/src/lib/__tests__/accountKeyedWork.test.ts   +207
    M artifacts/api-server/src/lib/accountKeyedWork.ts                  +314
    M artifacts/api-server/src/nodeFacetBakeTier1ConformantCli.ts       +133
    3 files changed, 643 insertions(+), 11 deletions(-)

Backed up out of the worktree so a crashed worktree cannot lose it (the worktree copy is left
untouched — recover, never reconstruct):

- `_inbox/2026-09-14_p178-publish_stopped_worker.diff.patch` (61,774 bytes,
  sha256 `0447390C0544A1F73DA8B6F90BDF9A0F705186C5D79A7421A501E7C101A0BBD3`)
- `_inbox/2026-09-14_p178-publish_stopped_worker.status.txt`

Disposition: **resumed in place from its own uncommitted diff**, not re-dispatched.

## 3. PR limbo check (contract item 4)

No wave-6 lane PR was open at the stop (verified with `gh pr list --state open` on
`hauska-atom-contract`, `hauska-engine`, `legacy-design-tools`, `hauska-factory`, `hauska-map`):
the only lane branch pushed is `feat/p154-conflict-row-vocab`, which carries no PR, so it is a
clean continuation point rather than an ambiguous one. Nothing to finish or mark draft.

## 4. Re-triage statement (contract item 2)

The addendum **ADDS** scope; it displaces nothing by implication.

- `p154-conflict` — the conflict VARIANT and the type work stand as written. What changes is the
  conflict note's sentence (now the A-148 sentence) and the mechanism the note names.
- `p156-bastrop` — scope added: the declaration reads `Zone_Types/25` **TEXT fields** as the
  district source, never the `FrontSetback_`/`SideSetback_`/`RearSetback_` numeric columns, and
  records the text-vs-numeric discrepancy per district.
- `p178-publish`, `p183-querypoint` — untouched by the addendum. All their items stay owned and
  are resumed as-is.

## 5. What was changed in the tree before resuming

1. `_catalog/dispatch_missions/mission_p154_conflict_row.md` — A-148 amendment block, corrected
   finding, the exact conflict-note sentence, two A-148 falsifiers, close updated.
2. `_catalog/dispatch_missions/mission_p156_bastrop_pilot_declaration.md` — A-148 amendment
   block, item 2 rewritten around the TEXT-vs-numeric rule and the per-district discrepancy
   record, two A-148 falsifiers, close updated.
3. Both dispatches recompiled with `scripts/dispatch.mjs` + `--program-preamble
   _catalog/program_preambles/OPS-23.md` (p154 `--plan-row P-154`; p156 `--plan-row P-156,P-124`),
   diff bounded to the mission sections (p154 +64/-22, p156 +43/-11).
4. Superseded history is retained in both missions under "Superseded by this amendment, do not
   act on these", so the amendment is auditable rather than silently rewritten.

## 6. Open item raised, deliberately NOT resolved in-lane

A-148's card text records the `Zone_Types/25` SF row as citing **Ordinance 2024-38**; the
sentence the overseer specified for the conflict note cites **Ordinance 2026-06** (the B3 Code
Repeal and Adoption ordinance our corpus already carries, and the ordinance the live panel
citation points at). Both readings now sit in the wave. Both lanes were instructed to build with
the overseer's ordinance (2026-06) and to flag the divergence in their closes rather than pick
silently. Operator: confirm which ordinance the city's current zoning layer cites.
