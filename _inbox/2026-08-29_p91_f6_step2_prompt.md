---
id: 2026-08-29_p91_f6_step2_prompt
title: F6 step 2 Connect paste
date: 2026-08-29
status: ready
plan_row: P-91
wdll: 6
screen_id: 4316b571-c7d2-4b9f-9e50-4f7a16dbfa94
plant: 48021:900099
---

# How to run

Same Connect chat is fine if the iframe is still `app-p551.html`. Do not open leftover-absent. Do not `create_screen`. Do not search the web. Do not call `ask_the_map`. Do not call `save_property`.

Paste everything below the line.

---

You are grading Smart Site MCP F6 step 2 only. Use only Smart Site tools.

Call `list_screens` with screenId `4316b571-c7d2-4b9f-9e50-4f7a16dbfa94`. That is the A13 walk board. It should have three resolved rows with Open: Pine `48021:34137`, neighbor `48021:34169`, and plant `48021:900099`.

The plant is `48021:900099`. Not `48021:900001` (that is leftover-absent, unresolved, no Open, different screen). Not `48021:34169` (query-alpha puts that row on top).

If `48021:900099` is missing, call `add_to_screen` on this same screenId with parcelNodeId `48021:900099` and source `walk`, then list again. Do not `create_screen` this id.

Ask the operator to click Open on the row whose NODE is `48021:900099` and then Send. Do not call `get_smart_site` yourself. If the composer names any other id, refuse and ask them to click the `900099` row.

Pass: `get_smart_site` runs once for `48021:900099`. Panel reads `Not on file in Bastrop`. Fail: `Open did not reach me`, or any ring, edges, or label for 900099.

Then stop.
