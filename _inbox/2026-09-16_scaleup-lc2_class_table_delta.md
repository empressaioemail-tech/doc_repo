# Class table delta -- L-C2 session-gap addendum

Generated 2026-09-16. Base register: `_inbox/2026-09-16_scaleup-lc_blocker_register.json` (361 instances, 18 classes + candidates). This addendum adds **125 new instances** found by sweeping the 61 `_sessions/` files from 2026-08-21 through 2026-09-13 that the base register's own close said were never swept file by file. A further **44 instances** described in that window were confirmed already present in the base register under an existing BLK-id and are not duplicated here (see `alreadyRegistered` in the addendum JSON).

## Official classes (C1-C18)

| Class | Name | Before | +Added | After |
|---|---|---:|---:|---:|
| C1 | Punctuation-only/malformed input skipped before write | 3 | +1 | 4 |
| C2 | Ceiling/verdict measured by one instrument, enforced or read by another | 17 | +2 | 19 |
| C3 | Parcels/records reaching no state at all | 11 | +1 | 12 |
| C4 | Missed sibling -- hand-maintained list or duplicated rule, one member forgotten | 30 | +8 | 38 |
| C5 | A ruling filed and never implemented, down to a type that cannot express it | 15 | +2 | 17 |
| C6 | Sub-metre digitisation mismatch between independent source layers | 2 | +1 | 3 |
| C7 | A value served with a label asserted by a constant, consumed by a check that cannot fail on it | 40 | +6 | 46 |
| C8 | False earned states | 24 | +2 | 26 |
| C9 | Controls that cannot fail by construction | 82 | +51 | 133 |
| C10 | Stale copies overriding current data | 17 | +1 | 18 |
| C11 | Identity collisions across two or more namespaces | 22 | +3 | 25 |
| C12 | Merged-but-not-deployed / deploy-and-traffic traps | 14 | +4 | 18 |
| C13 | Serial discovery -- weak instrument paces discovery instead of measuring the full population | 2 | +2 | 4 |
| C14 | Stranded or unreviewed artifacts | 15 | +1 | 16 |
| C15 | Duplicate row/amendment ids | 3 |  | 3 |
| C16 | Governance instrument gaming | 2 | +1 | 3 |
| C17 | Unleased/uncoordinated shared-resource contention | 7 | +4 | 11 |
| C18 | Hand-declared coverage/registries never kept in sync with reality | 24 | +7 | 31 |

## Named candidate classes (this addendum only added new ones; pre-existing singleton "NEW:" classes from the base register are omitted here unless this addendum also touched them)

| Class | Before | +Added | After |
|---|---:|---:|---:|
| NEW: algorithmic complexity mismatched to data scale | 1 | +2 | 3 |
| NEW: marketed-ahead-of-build | 2 |  | 2 |
| NEW: agent memory loss re-derives a known-weak solution | 1 |  | 1 |
| NEW: sample-construction artifact misread as a code bug | 1 |  | 1 |
| NEW: scraper double-count | 1 |  | 1 |
| NEW: halted-treated-as-landed on resume | 1 |  | 1 |
| NEW: encoding mismatch between tool and consumer | 1 |  | 1 |
| NEW: stray deploy-config in a non-deployable repo | 1 |  | 1 |
| NEW: writer-lease expiry during an unheartbeated plan phase | 1 |  | 1 |
| NEW: silent truncation with no warning to the caller | 1 |  | 1 |
| NEW: stale client state after a server rollback masquerading as a server regression | 1 |  | 1 |
| NEW: PII leak in a free-text field on a live customer surface | 1 |  | 1 |
| NEW: a config figure copied across products without re-derivation | 1 |  | 1 |
| NEW: a greedy-regex identifier truncation propagates through downstream artifacts | 1 |  | 1 |
| NEW: PII rendered in a design artifact, contradicting the artifact's own stated rule | 1 |  | 1 |
| NEW: a design artifact asserts fabricated citations to a real regulatory source | 1 |  | 1 |
| NEW: an automated tool over-matches a shared tree by path when it should scope by content ownership | 1 |  | 1 |
| NEW: a candidate list is built after the gate that filters it, instead of before | 1 |  | 1 |
| NEW: curved-frontage front-edge offset miscomputed | 1 |  | 1 |
| NEW: an unverified redrawn asset shipped as if canonical | 1 |  | 1 |
| NEW: unbounded-memory-per-county write path | 1 |  | 1 |
| NEW: nondeterministic id minting breaks replay/idempotency | 1 |  | 1 |
| NEW: secret compiled into a public client bundle | 1 |  | 1 |
| NEW: an authoritative-replace deploy flag silently drops any variable not explicitly re-listed, on every deploy | 1 |  | 1 |
| NEW: wrapper/consumer built independently with no reconciliation audit | 1 |  | 1 |
| NEW: a completed write leaves no durable execution record once its lease is released | 1 |  | 1 |
| NEW: a plan row misattributes its own defect's owner or location | 1 |  | 1 |
| NEW: geometry cap overestimates area at reflex ring vertices | 1 |  | 1 |
| NEW: premature-scale rollout risk, caught by staged gating before full deployment | 1 |  | 1 |
| NEW: a control's own restriction misread as an uncovered gap in a coverage report | 1 |  | 1 |
| NEW: verified instrument declaration exists but the runtime consumer re-derives the same fact via a check that structurally cannot succeed, so the answer is permanently indeterminate rather than trusted | 0 | +1 | 1 |
| NEW: entity_id grammar mismatch between families causes false-miss on an otherwise-identical join | 0 | +1 | 1 |
| NEW: geocoder misses on the full/long form of an address where the short form of the identical address succeeds | 0 | +1 | 1 |
| NEW: map hover paints only the per-tile clipped geometry fragment at a tile seam instead of the full assembled parcel polygon | 0 | +1 | 1 |
| NEW: near-bbox retrieval-service 504s, undeclared degradation | 0 | +1 | 1 |
| NEW: silent type-coercion data loss on a malformed CAD source field (comma-list masquerading as a scalar) | 0 | +1 | 1 |
| NEW: a fix for one search-source class silently broke a different source class that shared the same payload field | 0 | +1 | 1 |
| NEW: dispatch/preamble compiler silently omits a required install block, so downstream agents run without a practice nobody told them exists | 0 | +1 | 1 |
| NEW: dispatch preamble compiler silently truncates/over-runs the standing-decisions block due to a cross-dialect regex bug | 0 | +1 | 1 |
| NEW: publish writes to a table nothing serves reads from | 0 | +1 | 1 |
| NEW: courthouse-records portal returns HTTP 403 on a legitimate automated request | 0 | +1 | 1 |
| NEW: row-count mismatch between rows written and the source CAD roll count, unreconciled | 0 | +1 | 1 |
| NEW: an unguarded polling interval exhausts a shared global rate limit, with the resulting 429s misattributed to an unrelated limiter | 0 | +1 | 1 |
| NEW: a UI element displays one value while its own action button copies a different, correct value, so following the visible text fails | 0 | +1 | 1 |
| NEW: no keyboard dismiss path on modal dialogs displayed over a live interactive map | 0 | +1 | 1 |
| NEW: internal diagnostic/hold string leaks verbatim into a customer-facing UI slot | 0 | +1 | 1 |
| NEW: a claim of data-strengthening was filed backwards from reading only the consuming side, with illusory (non-independent) corroboration | 0 | +1 | 1 |
| NEW: a shipped feature hangs to the platform timeout instead of returning a fast refusal; root cause undiagnosed, shipped with the limitation declared | 0 | +1 | 1 |
| NEW: a deployed job artifact carries no traceable link back to its source commit | 0 | +1 | 1 |
| NEW: dispatch hand-carry silently targets the wrong repo via tab-vs-window confusion | 0 | +1 | 1 |
| NEW: missing referential-integrity constraint allows orphaned data in a production table | 0 | +1 | 1 |
| NEW: production credential surfaced in an agent's own debug/tool output during an unrelated task | 0 | +1 | 1 |
| NEW: internal identifiers and unearned promises leak verbatim into customer-facing report copy | 0 | +1 | 1 |
| NEW: a compiled context-injection payload is majority irrelevant-to-the-task boilerplate | 0 | +1 | 1 |

## Totals

- Base register instance count: 361
- Addendum new instances: 125
- Addendum already-registered cross-references (not double-counted): 44
- **Combined instance count (base + addendum new): 486**

## Every new instance that would hit Burnet, Bell, or Milam

The scaleup sequence is Burnet (Phase 1) then Bell and Milam in parallel (Phase 2). Filtering this addendum's new instances for classes/mechanisms that are county-agnostic (i.e. would recur in any new county going through the same farm, not scoped to a specific already-onboarded county's own data quirk) rather than county-specific one-off data defects:

- **ADD-001** (C9): SEAT-01 (the seat-boundary enforcement gate) had never fired since it was built: a bad import path, registered on two matchers, threw ERR_MODULE_NOT_FOUND and exited 0 on every invocation
- **ADD-003** (C12): a Cloud Run canary (00524-pit) froze on an old digest because image_tag:latest raced a push by seven seconds, so a dry-run against it read as an overlay no-op when it was actually older code
- **ADD-004** (NEW: verified instrument declaration exists but the runtime consumer re-derives the same fact via a check that structurally cannot succeed, so the answer is permanently indeterminate rather than trusted): a committed table (RAIL_ENGINE_BINDINGS) declaring every writer script exists in a real engine checkout is verified CI-fail-closed by its own test, but the production consumer (deriveHasWriter) ignores that verified decl...
- **ADD-005** (C9): PE-only hauska-map PRs stayed BLOCKED on a required check context ("test", the Command Center CI job id) that never runs on a PE-only diff; manually triggering the same workflow via workflow_dispatch does NOT satisfy the...
- **ADD-013** (C9): fleet-memory Tier-2-to-Tier-1 promotion, which had appeared to succeed once on 2026-08-21 (producing M-003 through M-006), had in fact run only ONCE and then stopped; by 2026-08-25, MEMORY.md still carried only six deplo...
- **ADD-014** (NEW: dispatch/preamble compiler silently omits a required install block, so downstream agents run without a practice nobody told them exists): the dispatch compiler (scripts/dispatch.mjs) injected CANON-PREAMBLE, AGENT-CONTRACT, and DEV-PROCESS into every compiled lane dispatch, but silently OMITTED the verbatim FLEET MEMORY (M0) install block; because product-...
- **ADD-015** (NEW: algorithmic complexity mismatched to data scale): the atom write path was believed lease-bound but was actually RTT-bound: writeAtomLinks issued one INSERT per applies-to link at 44ms round trip from the laptop, so 5,000 links cost 220s and the path ran at only 21 atoms...
- **ADD-016** (C17): two planner instructions sent ten minutes apart told a drain lane and a newly-spawned agent to build the identical stage runner in the same registered worktree, producing two jobs, two canaries on the same county, and on...
- **ADD-017** (NEW: dispatch preamble compiler silently truncates/over-runs the standing-decisions block due to a cross-dialect regex bug): the dispatch compiler's preamble extractor used a \Z regex token, which JavaScript reads as a literal capital Z rather than end-of-string, causing the extractor to over-run into a following seat block on 2026-08-24 and t...
- **ADD-018** (NEW: publish writes to a table nothing serves reads from): the county-ledger publish job writes a table the served ledger does not read at all (item 20 stayed not-met)
- **ADD-020** (C9): six shipped v2 UI motion/skeleton primitives (ss-pulse, LabelledSkeleton+shimmer, LoadingCount, Rule, FieldError, UnverifiedSource) had ZERO call sites anywhere in the app, despite passing a green test suite, a clean bui...
- **ADD-022** (C9): a newly-built CI gate (the chrome-kit gold-color rule) reported PASS on its first two self-test attempts to plant a known violation, because the plant itself silently failed to write anything (zero occurrences), meaning ...
- **ADD-023** (C9): two governance controls deadlocked for the integration seat: the dirty-tree close gate refuses a push while _state/<seat>/STATE.md is dirty and tells you to commit it, while the seat-write gate then refuses integration w...
- **ADD-024** (C18): the per-seat CLOSE_OVERRIDE hatch usage logs (_catalog/override_logs/integration.log and unknown.log) are neither tracked in git nor gitignored -- they simply were never added -- so override usage exists on one machine o...
- **ADD-026** (C4): six stale staging Neon branches were deleted after checking only the three hauska-prod store secrets, without enumerating legacy-design-tools-prod's OWN separate staging secrets, which still bound the deleted br-little-t...
- **ADD-027** (C12): CORTEX_USER_DAILY_API_LIMIT was manually hotfixed to 50000 on the live serving revision after the operator was locked out at the old 10000 limit, but the underlying deploy-workflow bake still specified 10000; because --s...
- **ADD-028** (C4): the chrome-kit write-path gate (built to police raw hex colors and native buttons) scanned a HAND-MAINTAINED file list; a brand-new chrome file simply passed the gate by never being opened, with no signal that it had not...
- **ADD-029** (C9): the first cut of the gate's rawHexes detector contained a literal 0x08 backspace character where \b was intended (a shell-to-python escaping artifact); it matched nothing on any input, so the gate would have reported PAS...
- **ADD-030** (C9): the gate's rawButtons detector missed every self-closing <button/> tag, because the original probe pattern required "<button onClick=" with a literal space, which a self-closing tag never contains
- **ADD-031** (C9): across this one session: a violation-plant script reported "PLANTED" while changing zero occurrences (which would have read as "the guard does not work" once the gate reported PASS); separately, four times a check writte...
- **ADD-032** (NEW: an unguarded polling interval exhausts a shared global rate limit, with the resulting 429s misattributed to an unrelated limiter): Reports returned HTTP 429 on every surface; root cause was an UNGUARDED polling interval (setInterval on an empty dependency array, no guard) that ran regardless of sign-in state or tab visibility, generating roughly 46 ...
- **ADD-038** (C9): the seat-write gate's isGitWrite() check is a text match on shell commands and fired on the literal PROSE string "git worktree add" appearing inside a plain file write (not an actual git command), incorrectly blocking th...
- **ADD-039** (C9): the production chrome-kit color gate saw only 7.1 percent of the color literals it appeared to police (25 of 352 lines): chromeFiles() walked .tsx files only, missing 80 hex-color lines living in .ts files, and no rule m...
- **ADD-042** (C9): the Oxygen font ships at two live call sites where it structurally cannot load; three tests assert its ABSENCE, and none of those tests reads either of the two files that actually reference it; the chrome gate has no fon...
- **ADD-045** (C9): six distinct ad hoc verification-instrument failures in one session: reasoning from a 222-commit-stale tree and reporting two load-bearing claims backwards; removing a pinned worktree while two subagents were still readi...
- **ADD-046** (C18): "Bastrop complete" was quoted from at least four non-equivalent denominators in the operational record simultaneously (ledger cad/geometry 100%, a single Pine-St Open ring, a downtown 39-parcel certification, a staging-s...
- **ADD-047** (C9): the post-H residue-recount instrument used to track the CTX remainder has three named defects: its own self-test can destroy the artifact it is measuring, docRepoCommit is hardcoded as a literal rather than read live, an...
- **ADD-048** (C9): a newly-built instrument (Gate 8's dayOne check), built specifically because this operation's controls have a history of passing vacuously, itself graded an EMPTY response body as a C3/C4/C7 pass
- **ADD-050** (NEW: a shipped feature hangs to the platform timeout instead of returning a fast refusal; root cause undiagnosed, shipped with the limitation declared): the MCP's radius-search route hangs to Cloud Run's 300-second timeout on every valid query regardless of radius size; the 400-validation path answers in 0.17s so routing is not the issue, and street-search works fine wit...
- **ADD-052** (C9): a Neon connection pooler silently injected SET default_transaction_read_only = on for the specific "neondb" pool (not a sibling database on the same compute/role), killing every write across both the customer app and the...
- **ADD-053** (C2): cortex-api moved to a new serving digest mid-wave, so an entire P-91 v3 verification walk had been graded against a cortex revision that no longer served -- undetected until a later reconciliation caught it
- **ADD-054** (C17): commit 71c3a62 ("docs(F-03): compile STORE-SPLIT-PLAN") landed carrying all 8 of a DIFFERENT seat's staged files and none of its own named artifact; another seat's git-add-all reflex fired inside the window between this ...
- **ADD-055** (C9): the first two probe runs verifying a new GoHighLevel API credential proved nothing: a real location id and a deliberately bogus one returned byte-identical scope-error responses, because the scope check fired before the ...
- **ADD-056** (C4): a new card (CTX-CONTAINMENT-RUN) was compiled WITHOUT the Neon maintenance-window guard that a sibling card (owner-backfill) had carried all along; Williamson launched with the maintenance window only 8 minutes out and f...
- **ADD-057** (NEW: a deployed job artifact carries no traceable link back to its source commit): a Factory job carries only an IMAGE_DIGEST env var and no git SHA of any kind, so an earlier image built from an UNCOMMITTED tree stayed completely invisible/untraceable back to any source commit
- **ADD-058** (C11): five OPS-17 rows (G-15/G-16/G-22/G-31/G-51, all blocking G-52, the real Bastrop-consumer-pass row) all measured the wrong repository -- legacy-design-tools/artifacts/plan-review (an AEC-cortex-facing surface) instead of ...
- **ADD-061** (C9): export_instrument refused in production because HAUSKA_MCP_BASE_URL and HAUSKA_MCP_SERVICE_KEY were unset -- every Studio deliverable was unreachable through the connector, meaning a separate lane had just spent effort e...
- **ADD-067** (C9): vision-read and instrument classification already run on every purchased courthouse document, but ZERO MCP tool anywhere exposes that classification to an agent (verified: zero hits in hauska-mcp-server)
- **ADD-069** (NEW: dispatch hand-carry silently targets the wrong repo via tab-vs-window confusion): a new Claude Code chat TAB inside an already-open VSCode window does not get a new workspace root (only a genuinely new OS-level window does); two hand-carried dispatches were pasted into windows still rooted at the wron...
- **ADD-071** (C9): in the same code block above, buildableAgreementToken is set to a fact-sheet id on one serve path and a derived display token on the baked path, so the instrument specifically built to prove cross-surface agreement can n...
- **ADD-072** (C4): a proposed report-vocabulary courier card assumed the report display vocabulary was single-sourced ("lives only in the app"); investigation this session found it already declared in THREE places (PE's own file, the engin...
- **ADD-073** (C9): a proposed setback-wiring plan (Plan 2) would have wired the customer-facing PE app to an internal DEBUG SHIM (/local/setbacks/:jurisdiction) calling the plain ungated table resolver with none of Bastrop's per-parcel gat...
- **ADD-074** (C18): a freshly-built and published architecture diagram contained an affirmatively false claim ("hauska-engine is vendored, not deployed" -- two live independently-deployed services contradict it), omitted four more live depl...
- **ADD-075** (C16): Factory attempted to bypass the LAPTOP_WRITE_FROZEN production-write-safety gate by setting an env var to fake past it; a second, independent execution-identity gate refused the write before anything happened; Factory se...
- **ADD-077** (NEW: algorithmic complexity mismatched to data scale): a 3.5-hour-running diagnostic (item 10, owner-rail cross-check) was found via pg_stat_activity/EXPLAIN to be doing a full ~4.3M-row scan per page because Postgres could not derive an index range bound from a LIKE pattern...
- **ADD-078** (C9): a required CI check ("PR base is main") was added to branch protection BEFORE the PR implementing it had actually merged, self-locking EVERY open PR in the repo including one of the concurrent CTX sprint's own lanes
- **ADD-082** (C17): two agents dispatched in parallel into the same repo, each in its own supposedly-fresh clone, had UNCOMMITTED work bleed across into each other's working tree during the build -- "some shared-sandbox filesystem anomaly, ...
- **ADD-083** (C9): a Rules-of-Hooks violation in ReportsTool.tsx (an early return before a useEffect) threw whenever a mounted Reports dock live-transitioned to a signed-out state (real sign-out OR the entitlement refresh after every Strip...
- **ADD-084** (NEW: production credential surfaced in an agent's own debug/tool output during an unrelated task): a production credential (PRODUCTION_NEONDB_URL) was briefly exposed in a DIFFERENT CTX-sprint lane's own tool output while debugging an unrelated fetch failure -- self-disclosed and contained, but the rotation recommenda...
- **ADD-085** (C7): setback-rule atoms have a real per-field provenance channel (fieldProvenance) that IS populated, but the function mapping ordinance-verification tier onto the atom's confidence field collapses "primary-source-verified" (...
- **ADD-086** (C12): hauska-retrieval-api was crash-looping in production every 15-90 seconds on a STALE deployed image that predated three already-merged fixes for an unsafe boot-time countAtoms() call; redeploying it also explained a 3-day...
- **ADD-090** (C9): the records-worker fabricated-zero guard rests entirely on a regex for the literal string "N Total Results" against document.body.innerText, and fails open three separate ways (different vendor wording, a swallowed excep...
- **ADD-093** (C17): seat_register.json, a shared last-writer-wins JSON blob with no entry-level protection, was clobbered THREE separate times across one week by concurrent seat sessions: once by a restore-from-backup reverting past another...
- **ADD-094** (C9): FEASIBILITY_MANIFEST declares ten report sections; the renderer honours only four -- six declared sections, including the flood study and the aerial, never render at all; the dangling sheet reference and missing aerial t...
- **ADD-096** (C9): the PDF content-extraction/verification path used to check what a PDF actually says carries a stale font/ToUnicode assumption across ten of its own test files, so any claim about what a PDF SAYS is unreliable until an in...
- **ADD-097** (C7): cortex-api serves envelope: null where the underlying database holds a real, declared decline reason -- starving a Property Explorer renderer that was specifically built to print that reason to the customer
- **ADD-098** (C12): smartsite-mcp tag "envfix" is an explicitly-flagged LIVE HAZARD revision-level stopgap: the real fix (P-131) merged but its deploy has not run, so until it does, ANY ordinary workflow deploy of smartsite-mcp silently wip...
- **ADD-099** (C6): 13 parcels held two counties at the publish gate on schoolDistrict due to a sub-metre digitisation mismatch between independently-digitised tx_school_district and tx_county_boundary layers (shared edges miss by 0.8-0.9m)...
- **ADD-102** (C18): only 23 of 69 cities across the six CTX counties carry a zoning-layer endpoint at all (Bastrop 3/3, Caldwell 3/3, Hays 5/11, Williamson 7/14, Travis 3/18, McLennan 2/20) -- named as the best explanation for why zoning ke...
- **ADD-103** (NEW: a compiled context-injection payload is majority irrelevant-to-the-task boilerplate): a compiled lane dispatch for a single-county CTX card carries ~15.5KB, of which only ~5.3KB is the actual mission -- the remaining ~10KB is canon preamble + agent contract + dev process, dominated by unrelated 2026-08-17...
- **ADD-104** (C9): four distinct planner-instrument errors in one session: a cadRoll probe filtered the wrong place_key pattern and reported a confident false "null everywhere"; a Neon-maintenance-window check read a naive UTC timestamp as...
- **ADD-106** (C9): the publish gate's unaccounted-is-fatal check was enforced for only 17 of 65 total parcel-record rails; the other 48 were never asked, meaning an agent could honestly report "done" while 48 rails silently reported nothin...
- **ADD-107** (C5): both cell writers for the newly-slated setback/envelope rails advanced only on value/refused and had NO branch for a legitimate not-applicable, permanently stranding 3,376 parcels x 8 rails (genuine polygon misses inside...
- **ADD-109** (C7): cortex-api gates the four cadRoll dollar fields to Studio/Team/Property-Unlock and emits a TYPED studio-gated refusal to everyone else, but the Property Explorer BFF's isCadRollValueWire guard admits only present, zero, ...
- **ADD-110** (C9): a dispatch-preflight rule (before compiling ANY dispatch: confirm every repo-scoped identifier the mission names actually exists in the named repo, and for every symbol a mission instructs a lane to change, enumerate rea...
- **ADD-111** (C9): a close committing a "closed" verdict with an UNMEASURED probe artifact staged and committed IN ONE BASH CALL went through the close-gate uncaught (commit 1e174bcc), because the harness snapshots hook registrations at se...
- **ADD-112** (C18): MEMORY.md's and 00_current_state.md's Cotality entries carried a BLANKET "extinguished" claim for over two months while COTALITY_MCP_UAT_CLIENT_ID/_SECRET (a live MCP eval channel, litellm-mcp-server proxying 11 scope-ga...
- **ADD-116** (C10): a 2026-09-03 StratMap reload silently replaced every parcel row in all six CTX counties and DROPPED the derived zoning_district stamp entirely, leaving it NULL on 1,516,110 parcels -- verified live with Comal (an unreloa...
- **ADD-117** (C13): the ledger used to SCOPE the bake was wrong in both directions on the same metric: county_facet_coverage reported Bastrop zoning at 99.77 percent from a 433-row SAMPLE against a real 12.3 percent, and Travis at 0.00 perc...
- **ADD-119** (C9): BROKERAGE_EXTENSION_PUBLIC_KEY, believed dead based on usage evidence supporting retirement of the "chrome extension" it named, was in fact the ONLY brokerage secret ever provisioned to cortex-api; brokerageAuth() runs i...
- **ADD-120** (C13): parcel-identity churn is masked by aggregates -- sampling 200 baked Kyle parcels in Hays found 30.5 percent no longer exist in txgio_parcel at all, 19.5 percent exist but no longer fall inside their own polygon, and only...
- **ADD-122** (C9): since the p555 deploy, an unresolved screen row bound to its own uuid and painted a fake Open button; found by S8's fixtures
- **ADD-123** (C9): SEAT-01 gates Write and Edit by path, but in shell mode it inspects only git write verbs, so a heredoc, tee, sed -i, or node -e reaches a foreign worktree UNTOUCHED, and the gate's own header does not list shell writes a...
- **ADD-124** (C9): two planner instrument failures, both reading a proxy instead of the authoritative record: a build config generated by substituting a string its source file does not contain, which pushed unintended code onto a registry ...
