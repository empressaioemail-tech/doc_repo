---
id: 2026-08-24_red_card_CP2
title: CP2 — red-card mid-work review
date: 2026-08-24
plan_row: P-60
wdll: _inbox/2026-08-24_red_card_search_bar_WDLL.md
snapshot: isolated P:/tmp/hauska-map-red-card fix/pe-red-card-search-bar uncommitted on 2ed062d
---

# CP2: Red-card mid-work

Planner read both implementer diffs. Did not take their pass reports as the grade.

## Store (implementer A)

`setSubjectByParcelNodeId` increments `subjectWriteGeneration` at call start and `set()` only when that generation is still current. Stale returns `{ kind: "stale", parcelNodeId }`. Unplaceable / throw still leave the standing subject.

Race test `does not let a slower earlier resolve overwrite a later call` is not-vacuous: A is held in resolve, B finishes, A is released. A claimed they violated the guard and the same test failed with last-write-wins (`fs_late` / 498770). Planner accepts that shape. Re-ran the file: 7/7.

`adoptSubject` and `runParcelLookup` already treat `kind !== "subject"` as a no-op. No crash on stale.

## Bar and Find (implementer B)

`createLookupIntent` + tests. ExplorerMap bumps on map-click adopt and at Find start. After lookup await, stale intent returns without camera fly.

Hole found on review: `inspectAsSoonAsIdKnown` paints `inspectInPlace` **before** the seal await. A click between lookup-ok and that paint would overwrite the click card. Planner wrapped the inspect callback in `isCurrent(started)`. Not committed.

Search bar: `subjectDisplay` from `subjectStore` via `useSyncExternalStore`. Present situs, else node id. `, TX` sentinel rejected. Focused typing not yanked (`nextSearchBarValue`).

ExplorerMap wiring test is still presence-shaped (file grep). The meaning-shaped checks are `nextSearchBarValue` and `subjectDisplayFromIdentity`. Named, not treated as a generation-guard equivalent.

## Tests planner ran

`pnpm exec vitest run` on subject-store, lookup-intent, SearchBar, parcel-lookup, search-landing, inspect-pending-card, situs-pin, search-kinds: **8 files, 79 passed**.

## Still open

- Operator visual on smartsite.cloud after ship: Find 17005, click neighbor, bar and card agree.
- Do not ship until the planner commits. Sub-agents do not commit.
- 3b and Travis join stay parked.
