---
id: 2026-08-30_ctx_chew_next
title: CTX chew next — three lanes, then a chain
date: 2026-08-30
last_updated: 2026-08-30
status: planning
depends_on: _inbox/2026-08-30_ctx_execute_waves_WDLL.md
operator_go: tee up only; do not start P1+ code
snapshot: integration P:/doc_repo; P0 landed; Wave R paused
---

# Chew next

Operating card: `_inbox/2026-08-30_ctx_execute_waves_WDLL.md`. This page is the order only.

Nothing runs today. P0 is docs. The next operator word starts three lanes. After those land, the rest is one scheduled chain.

## Why this order is short

Most of the gap is not work. Once setbacks, edges and envelope inherit city scope (P3), **357,269** of 981,410 parcels become `not-applicable` and **465,568** become `unmeasured` pending a city probe. Those are different states; do not collapse them. Edges actually owed are ~154,841. Wells and footprint apply on five counties, not six. Flood is a shape conversion. C-count already ran.

The only item people cannot compress is the `breadth_*` → `place_fips` alias table. Start it on the same go as P1.

## Click 1 (this session, done)

P0. Canon. Measured owe. OPS-1 A12. 72 cities. Collect card restamped as specification, not schedule.

## Click 2 (one go, three lanes)

| Lane | Repo | Why now | Exit |
|---|---|---|---|
| P1 controls | hauska-factory | Every later green is a lie until the walk can fail | Walk rejects all-null. F-18 refuses a missing county. Routing-pin holds. |
| P2 alias seed | property | Long pole. Cannot be derived. Every city rail waits on it. | Seed file exists. Bastrop's seven spellings map to `place_fips`. Self-test on two spellings. |
| P2b PE | hauska-map | Never blocks Wave R. Blocks parent close. | Live brief + deployed bundle marker. #310 is not the grade. |

Do not wait for P1 to finish before starting the alias table.

## Click 3 (one lane, after P1 refuse exists)

P2 job template + writer allowlist. One non-CAD writer runs as a Cloud Run job and refuses a missing county. F-11 writer exists. Easement writer stops live REST.

## Click 4 (cheap, huge)

P3. Unincorporated setbacks, edges, envelope → `not-applicable` (357,269). In-city without a table → `unmeasured` (465,568). Warmed → `value` (3,732). Four county easement absences.

## Click 5 (serialize heavy scans)

P4. Wells then footprint on five. Flood shape conversion. Land four `SETBACK_TABLES` artifacts. Edges ~154,841. Quarantine 188,103 placeholders and McLennan 65,814 envelopes-over-zero.

## Click 6–8 (serial)

P5 scrub both directions. P6 pin + six staging. P7 Wave R six production serial. P8 prove.

## Parallel forever (never on the critical path)

Easement four-point probe. Zoning-stamp remainder. Roads (parked). Recount instrument repair. W1 LDT #554 CI. Factory #37 walk grades after P1 replaces `hasKeyPath`.

## Do not pull

0005 as drafted. `landing-import`. F-18 while it defaults to 48021. Wave R. Laptop `--apply`. An absence without a probe. County-wide setback owe. A walk that cannot fail.
