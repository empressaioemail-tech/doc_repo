Do not spawn sub-agents. Fan is one level. You are the executor. The planner adversarially reviews, merges, deploys cortex-api canary, and deploys Command Center to Vercel. You do not merge. You do not workflow_dispatch. You do not take the atoms bulk-writer slot.

# Mission — Lane A4 / G-56: Smart Files Command Center done (data room)

You are the Lane A executor for Layer 1.5. Frozen WDLL (the grade): `_inbox/2026-08-15_a_wdll_cc_done_l15.md`. You are responsible for WDLL items 2, 3, 4, 5, 6, 7 as code and PRs. The planner owns item 1 (cortex-api canary of `015b15d6246ea6af12b6b25daa69eae8a75fc61b`) and item 9 (honest close). Item 8 is a non-close of G-19.

**Read first, in order:** the WDLL; `_decisions/2026-08-15_file_set_edges_not_identity.md`; `_decisions/2026-08-15_smart_files_module_identity.md`; `_decisions/2026-08-15_vision_data_room_and_sdk.md`; `_decisions/2026-08-15_icc_first_sdk_customer.md`; `portfolio_thesis/03_three_questions_data_room_and_sdk.md`; `80_adrs/adr_010_atom_graph_traversal.md` (link taxonomy; `placed-on` is new, `applies-to` already exists); `29_mcp_surface_tier_model.md` (Command Center is non-commercial: no metering, no take rate). Inherit L1: store applied, identity `smartfile:<scopeType>:<scopeId>:<docSlug>`, three tables plus absence. Do not re-derive G-14.

## What done looks like (observable)

Nick opens `cmdcenter-blush.vercel.app` `#panel=smart-files` and is inside a data room. Folders are twin nodes (jurisdiction, tenant, site; parcel-as-site nested). Files are file-shaped atoms (documents with attachments), not every atom. An atom appears in a folder via a `placed-on` edge (`smart_file_placements`), not because its entityId is the folder. One file-atom in two folders is two edges, same entityId. Opening a file shows the PDF (when present) plus a sidebar of the data behind it. Non-file atoms on the node sit in a record pane (reuse Atom Inspector; do not rebuild Compass). The same list/read operations work as Hauska MCP tools. accessPolicy on every list and read.

## What to build (WDLL items 2-7)

2. **MCP tools on the existing Hauska MCP server.** No new MCP server. Tools: list folders for a scope, list files in a folder (file-shaped atoms reached by `placed-on` edges, never by reconstructing entityId), read file (atom plus attachment stream), list edges/placements for an atom. Enforce accessPolicy at the tools layer (ADR-017). Typed absence is a first-class payload, never a silent empty list. Command Center product-key path; anonymous refused on tenant-private. Do not import `@hauska-sdk/payment`. Do not add Circle, VDA, or take-rate.

3. **Seed data room on cortex-prod.** Planner will apply/announce the writes if they need production DB. You write the seed script and fixtures. Required: two folders, three file-shaped atoms, one file-atom with `placed-on` edges to two folders (same entityId in both), one PDF-bearing document, one non-file atom on a folder that MUST appear in the record pane and MUST NOT appear in the file list, one typed absence. accessPolicy `platform-internal`. Not G-44. Small, named, no atoms-slot.

4. **Command Center panel** in `hauska-map/apps/command-center`. Panel id `smart-files`, hash-routed like `#panel=county-manifest`. Folder tree, file list, preview, sidebar, record pane. File rows are file-shaped atoms only. Isolated worktree off hauska-map `origin/main`. Never open `P:\smartcity-os`. Vercel deploy is planner-owned after merge.

5. **PDF travels with the atom.** Preview through the atom read (CID / version row). No parallel unsigned blob URL that can 200 after the atom is gone. Revise-once: new version current in every folder the document is placed in; prior version openable from the sidebar.

6. **Sidebar** shows entityId, accessPolicy, source, computedAt, servedAt, STALE, versions, edges. STALE must be mutation-testable (backdate) the same class as county-manifest. Prove the indicator can fire.

7. **RBAC.** Anon / wrong key cannot list or open tenant-private seed (401/403, not empty). Operator key reads platform-internal. MCP and CC same deny/allow on the same atom. Not G-11 city-staff RBAC.

## Repos and worktrees

- `hauska-mcp-server` for item 2. Isolated worktree. Fetch origin/main first.
- `hauska-map` for item 4. Isolated worktree.
- `legacy-design-tools` only if the store needs a thin serving adapter the MCP tools call. Isolated worktree off origin/main. NEVER touch `P:\legacy-design-tools` if it is on `feat/s1-instrument-hardening` (dirty). Prefer calling existing `smartFileStore` over inventing a second store.

If you need a serving path from MCP to Neon, file that design in CP1. Do not invent a second document table.

## Constraints

Do not spawn sub-agents. Isolated worktrees only. Zero brokerage files in any diff. No `--apply` against atoms. No heavy scan. No workflow_dispatch. No merge. No Vercel deploy. No cortex-api canary. `P:\smartcity-os` never opened. Doc_repo commits are planner-owned: write CP1/CP2 to the named `_inbox/` paths; leave them uncommitted. L26 holds the atoms slot; do not start a second neondb parcel reader.

A-013: MCP is a second consumer of the Smart Files type. If you must import the type across repos, STOP and file a FINDING in CP1 rather than silently copying the contract. Promotion is planner-owned.

## CP1 before you write schema or tools

File `_inbox/2026-08-15_a4_cp1.json` with: MCP tool names and payloads; how `placed-on` is read (placements table as edge, not entityId reconstruction); how the record pane gets non-file atoms without putting them in the file list; PDF fetch path; STALE backdate plan; what you will NOT do (payment SDK, G-19, G-44, G-53, smartcity-os, implied identity-as-folder). If the ruling is underspecified, STOP and write the finding rather than inventing a link type besides `placed-on` for files.

## CP2 after tests, before you consider the PR done

File `_inbox/2026-08-15_a4_cp2.json` with counts and counting rules, file-list-excludes-non-file-atom proof, dual-folder same-entityId proof, brokerage-untouched proof, remaining risks. Open PRs against origin/main. Do not merge.

## Out of scope

WDLL item 1 (planner canary). Item 9 (planner close). G-19, G-20, G-44, G-53. Circle / VDA / take-rate. G-11. Compass. Human/building/desk/asset/station folders. Lanes B/C/D. G-30. Texas flush. Cleaning the dirty LDT checkout. Atom-contract npm publish.
