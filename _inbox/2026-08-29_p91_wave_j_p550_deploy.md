---
id: 2026-08-29_p91_wave_j_p550_deploy
title: Open punch plus Wave J Honesty serving on p550
date: 2026-08-29
status: serving-not-graded
plan_row: P-91
items: [1, 5, 6, 7]
serving: smartsite-mcp-00049-duw
tag: p550
digest: sha256:5958d8a5a3020aa430aed85f4e633356c0fb77f081aba0ec947204f080ee3ab0
uri: ui://smartsite/app-p550.html
---

# Deploy

Nick said both in one go. Isolated tree `P:/tmp/legacy-design-tools-p91-stone` on `feat/p91-wave-h-stone` (dirty Open punch + Wave J on base `006fd009`). Cloud Build `f9be0538-7a13-4df4-b05d-8553dd22f5f6` SUCCESS. Image digest read from Artifact Registry `image_summary.digest`, then pinned on the revision.

Pre-shift: `smartsite-mcp-00047-vos` @100% tag `p549`. Production `/health` `revision` was `smartsite-mcp-00047-vos`.

Canary: `smartsite-mcp-00049-duw` @0% tag `p550`. Tag URL `/health` `revision` was `smartsite-mcp-00049-duw`. Revision image is the digest above. `autoscaling.knative.dev/minScale` is `1`. Then `--to-tags=p550=100`.

Post-shift `/health` on `https://mcp.smartsite.cloud/health`:

`{"status":"ok","service":"smartsite-mcp","name":"Smart Site","version":"0.0.1","authConfigured":true,"cortexConfigured":true,"revision":"smartsite-mcp-00049-duw","failureDomain":"smartsite-mcp"}`

Traffic 100% row: `revisionName=smartsite-mcp-00049-duw`, `percent=100`, `tag=p550`.

Cortex untouched: `cortex-api-00656-vek` @100% tag `p539`. Staging stays 0%.

No `--set-env-vars`. Cortex not deployed. Tools stay 13. No fourteenth tool.

Tests in that tree: 92 passed (15 mcp-app, including three Wave J cases).

# What shipped

Open draft punch: `Call get_smart_site once with depth node for this id. Do not call save_property. Do not search the web.` Ghost names `save_to_screen` and `find_listing_history` are banned in the iframe HTML. Button label stays `Find listing history` (spaces).

Wave J item 5: legend words stay present / absent, verified / unknown / refused / unread. Hatch caption `Not read yet` fails the HTML contract.

Wave J item 6: empty board is `No screen yet`. zzzz slot is `Nothing to open until this resolves`. Dead Open (`ui/message` error) is `Open did not reach me`. County miss after Open (tool result is not a parcel) is `Not on file in Bastrop`. Those two F6 sentences are distinct.

Wave J item 7: listing stays the Wave D host web turn on the drawn panel. Secondary is `Save property`. No `Save to screen`. Panel fingerprint unchanged.

# Not a customer-done grade

Reconnect required. An open iframe is still p549. Walk items 5-7 are serving, not walked. Send the Open draft. Then click Find listing history on the drawn gold panel.

Flood study calling is not in this ship. Nothing in the 13 tools ingests a document into the bake.
