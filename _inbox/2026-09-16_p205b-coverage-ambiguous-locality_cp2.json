{
  "lane": "p205b-coverage-ambiguous-locality",
  "planRows": ["P-205"],
  "checkpoint": "CP2",
  "filedAt": "2026-09-16T23:20:00Z",
  "seat": "p205b-lane",
  "fanDepth": 0,
  "subAgents": { "spawned": 0, "maxDepth": 0 },
  "status": "work complete locally, PR open, CI re-running after a comment-only second commit; PILOT RUN (playbook section, below) executed live against the deployed revision and against the store",

  "docRepoWorktree": {
    "writtenTo": "P:/seat-worktrees/p205b-coverage-ambiguous-locality/doc_repo/_inbox (this lane's own seat worktree, branch lane/p205b-coverage-ambiguous-locality, cut from origin/main 00cbf8c3). NOT P:/doc_repo, the integration seat's checkout.",
    "cp1Artifact": "_inbox/2026-09-16_p205b-coverage-ambiguous-locality_cp1.json",
    "cp1Sha256": "62D79CFBA48722BA05FEE9C4772CAA468DE9293F93E74DADAA870BE68D1D89B8"
  },

  "product": {
    "repo": "empressaioemail-tech/hauska-engine",
    "clone": "P:/tmp/p205b-coverage-ambiguous-locality",
    "branch": "fix/p205b-coverage-ambiguous-locality",
    "startCommit": "e7cd0aee8cd21ae1a03897edb741b8d7e9fd9cf1",
    "endCommit": "2f94422f51dc3fa5873f28092a62ff346985718b",
    "commits": [
      {
        "sha": "41588191066a24f32d644603ed0968597a64e7aa",
        "subject": "fix(P-205b): resolve an ambiguous ZIP with its city, and give a unanimous answer",
        "what": "the two rules, the one-scan ZIP+city statement, the candidate list on the two candidate-bearing verdicts, the fixture store brought through the SAME two pure decisions, and the tests."
      },
      {
        "sha": "2f94422f51dc3fa5873f28092a62ff346985718b",
        "subject": "docs(P-205b): record the measured one-scan cost pair and the live refusal's provenance",
        "what": "COMMENTS ONLY, no logic change. Those two comments were the only load-bearing statements in commit 1 that rested on prose; both are now measured. A source comment that overstates its own provenance is the same defect class as a verdict that overstates a read."
      }
    ],
    "diffstat": "2 files changed, 836 insertions(+), 22 deletions(-) against e7cd0ae",
    "pr": "https://github.com/empressaioemail-tech/hauska-engine/pull/463",
    "prStateAtFiling": "OPEN, base main, 2 commits ahead of main, 2 files",
    "mergeStateAtFiling": "BLOCKED (branch protection waiting on the one check), MERGEABLE; the check's literal conclusion string is recorded in the close, not here"
  },

  "filesTouched": [
    "services/retrieval-api/src/coverage-check.ts (the two rules as pure functions narrowByCity + decideAmbiguousLocality, the splitCityMatch fold, one statement for zip+city in resolveLocality, readCandidateCoverage, CountyCandidateCoverage + candidates? on the not-covered and indeterminate bodies, the combined-key locality cache entry, module-header record of the live measurements)",
    "services/retrieval-api/src/__tests__/coverage-check.test.ts (the measured 78654 rows verbatim as MEASURED_78654_ROWS, splitCityMatch/narrowByCity/decideAmbiguousLocality suites, the P-205b falsifier suites on the fixture store, and the fakeSqlFactory suites that drive the REAL store with no connection)"
  ],

  "constraintsHonoured": {
    "touchScope": "verified by `git status --porcelain` and `git diff --name-only e7cd0ae HEAD`: exactly the two files above, both under services/retrieval-api/. The two sibling lanes the dispatch names (P-293 remainder in parcel-record-db.ts, P-295's instrument) were never opened.",
    "dominanceShareUnchanged": "DOMINANCE_SHARE is still literally 0.9 at coverage-check.ts:118 and no test edits it. Ruling 3 honoured; widening it was not attempted.",
    "coveredDefinitionUnchanged": "countyServesAtLeastOneParcel (the Tier-1 bake row under TIER1_ADAPTER_KEY) is untouched, and the dispatch's instruction not to change it was treated as a hard boundary rather than a preference.",
    "noIndexAdded": "no DDL of any kind was issued; every statement this lane ran was a read-only SELECT. The unindexed scan is reported as leave_behind, not fixed.",
    "noDeploy": "nothing was deployed; no `gcloud run deploy` and no `gcloud run services update` was run. gcloud was used READ-ONLY (services describe, secrets versions access) and the deployed surface was only ever GET-probed.",
    "noSubAgents": "FAN-DEPTH 0 honoured at every depth: this session launched no sub-agents and no Task, so A-181's refusal cannot fire.",
    "heavyScanSerialization": "two leases taken on the cortex store, both announced before and released after; see heavyScanLeases. No scan was ever run holding no lease, and no lease was left open at filing (verified by `heavy-scan-lease.mjs list`)."
  },

  "pilotRun_live": {
    "why": "CP2 is the pilot/mid-work checkpoint, and a build lane's pilot is the deployed surface itself. The lane's whole premise (that the live check refuses a Marble Falls address) was first re-measured rather than inherited, because the dispatch attributed the finding to `hauska-retrieval-api-00092-lag` and that revision could not be reproduced.",
    "revisionReconciliation": {
      "dispatchClaim": "measured live on hauska-retrieval-api-00092-lag",
      "measuredAtFiling": "the service's URL is https://hauska-retrieval-api-h7gvu7rgcq-uc.a.run.app and the CURRENT ready revision is hauska-retrieval-api-00094-wed (gcloud run services describe, project hauska-prod-497015, region us-central1, 2026-09-16). 00092 was not and is not the serving revision; 00080-yax was.",
      "consequence": "the defect is ARMED at head-of-deploy rather than only in a superseded revision, which makes the dispatch's premise STRONGER than it was written. Recorded as measured, not matched to the brief (DEV-PROCESS 3.2)."
    },
    "authPath": "401 without credentials first; the route is not a public health path. RESOLVED, and the resolution is recorded because the sibling P-293 lane closed this as a DEAD-END: the live API key IS reachable from this machine as Secret Manager secret HAUSKA_ENGINE_API_KEY in project hauska-prod-497015, and the service mounts it AS RETRIEVAL_API_KEY (gcloud run services describe -> RETRIEVAL_API_KEY valueFrom secretKeyRef name HAUSKA_ENGINE_API_KEY). The secret literally named RETRIEVAL_API_KEY does not exist (NOT_FOUND), and the default gcloud project is smartcity-os-prod, so an unscoped lookup lands in the wrong project. Header is `Authorization: Bearer <key>`, not x-api-key (server.ts:171).",
    "probes": [
      { "query": "zip=76541&state=TX", "http": 200, "seconds": 18.40, "body": "{\"status\":\"covered\"}", "readsAs": "falsifier 3's live control: Bell still resolves with NO city given, on the deployed revision" },
      { "query": "city=Killeen&state=TX&zip=76541", "http": 200, "seconds": 17.55, "body": "{\"status\":\"covered\"}", "readsAs": "the city neither helps nor hurts a ZIP that already resolves -- ruling 1's 'when only one of the two is given, behaviour is unchanged' plus the narrower 'a resolved ZIP is never second-guessed'" },
      { "query": "state=OR&city=Portland&zip=97201", "http": 200, "seconds": 8.20, "body": "{\"status\":\"indeterminate\",\"reason\":\"state \\\"OR\\\" is outside Texas; this coverage source only covers the Texas market\"}", "readsAs": "falsifier 4's live leg. NOT a DB-cost measurement: 8.2s is container cold start on an idle instance, not a scan, so the timestamp is not cited as cost evidence. The unit test, not this probe, is what proves no database call is opened." },
      { "query": "zip=78654&state=TX", "http": 200, "seconds": 17.81, "body": "{\"status\":\"indeterminate\",\"reason\":\"locality resolves to multiple candidate counties with no dominant match: 48053 (11391), 48453 (1850), 48031 (61), 48319 (46), 48015 (2), 48501 (2), 48299 (1)\"}", "readsAs": "the ZIP alone is ambiguous on the live surface -- the finding" },
      { "query": "city=Marble+Falls&state=TX&zip=78654", "http": 200, "seconds": 35.14, "body": "identical to the line above", "readsAs": "THE DEFECT, live: the city Marble Falls is given and is IGNORED" },
      { "query": "city=Burnet&state=TX&zip=78654", "http": 200, "seconds": 0.13, "body": "identical to the line above", "readsAs": "served from the verdict cache (0.13s) -- a second spelling of the same input gets the same refusal, which is what a 30s INDETERMINATE_TTL looks like from outside" },
      { "query": "city=Nowhereville&state=TX&zip=78654", "http": 200, "seconds": 39.70, "body": "identical to the line above", "readsAs": "the strongest single piece of evidence for the bug: a city that does not exist at all produces the same refusal, because no city is read. This is also why the new rule must NOT answer `not-found` for an empty narrowing -- the deployed revision's answer for a nonexistent city and a real one is the same, and after this change a nonexistent-city narrowing still lands on the ZIP-only ambiguity rather than a new false absence." }
    ],
    "probesProve": "all seven answers are byte-identical between the city-given and city-omitted cases for 78654, which is the live statement of the defect the dispatch describes in prose. The three non-78654 probes are the paired controls: coverage still works where it always worked, so the new path was not built on a broken baseline."
  },

  "liveMeasurements": {
    "store": "ep-lucky-truth-apodo8hr.c-7.us-east-1.aws.neon.tech (alias cortex:neondb), the store behind txgio_parcel that the service's own CORTEX_DATABASE_URL mounts",
    "method": "read-only psql, `\\timing on`, statement_timeout 280s per connection, no DDL, no writes; the URL was read from the Cloud Run revision's secretKeyRef, never from a file",
    "heavyScanLeases": [
      {
        "id": "4d39abc7-9bd6-47fe-a512-0d60c8f8f8b7",
        "holder": "p205b-lane",
        "phase": "p205b-78654-locality-split",
        "startedAt": "2026-09-16T22:39:03.426Z",
        "releasedAt": "2026-09-16T22:43:34.268Z",
        "releaseKind": "released",
        "reapReason": "p205b 78654/76541 splits measured; no further scans",
        "covered": "the 78654 and 76541 splits: the cold 78654 GROUP BY at 67.8s and two warm repeats (recorded at the time; the raw \\timing output of THIS window was not retained and is reported as a gap rather than reconstructed)"
      },
      {
        "id": "1c0b6008-d74b-4fe7-a0c2-8cbdc95dfc0d",
        "holder": "p205b-lane",
        "phase": "p205b-live-http-verify-78654",
        "startedAt": "2026-09-16T23:06:21.673Z",
        "renewedTo": "2026-09-16T23:29:18.557Z",
        "releasedAt": "2026-09-16T23:11:18.319Z",
        "releaseKind": "released",
        "covered": "the paired cost measurement below -- the only window whose statement text and output are quoted verbatim"
      }
    ],
    "snapshotTime": "2026-09-16 23:11:10.231873+00 (SELECT now() inside the measured session)",
    "sql": {
      "combined": "SELECT county_fips, city_match, count(*)::int AS n FROM ( SELECT county_fips, (upper(situs_city) = upper('MARBLE FALLS')) AS city_match FROM txgio_parcel WHERE situs_zip = '78654' ) t GROUP BY county_fips, city_match ORDER BY n DESC;",
      "zipOnly": "SELECT county_fips, count(*)::int AS n FROM txgio_parcel WHERE situs_zip = '78654' GROUP BY county_fips ORDER BY n DESC;",
      "note": "these are the two statements this lane's code and the deployed revision issue, transcribed; the ordering keys are the measurement's, not the module's"
    },
    "costPair": {
      "countingRule": "wall-clock milliseconds from psql `\\timing`, one statement per measurement, no parallelism, same connection and same store; 'cold' means the first statement of its shape in the window after the lease was taken",
      "combinedCold": "72226.267 ms",
      "zipOnlyAfter": "19536.691 ms (already partly warm -- the combined statement had just scanned the same rows)",
      "combinedWarm": "3213.082 ms",
      "zipOnlyWarm": "3010.895 ms",
      "verdict": "the warm pair is the ONLY fair comparison in this set and it says the narrowing costs about 7 percent more (3.21s vs 3.01s), NOT free. The load-bearing number is the one: 72.2s of combined scan replaces 72.2s + 19.5s of two scans. Both cold figures stay under the module's 150s GEO_RESOLUTION_STATEMENT_TIMEOUT_MS, and the module header's older 2-3 minute worst case is deliberately NOT overwritten by them."
    },
    "splits": {
      "zipOnly": { "48053": 11391, "48453": 1850, "48031": 61, "48319": 46, "48015": 2, "48501": 2, "48299": 1, "total": 13353, "shareTo48053": "85.31 percent", "passes90": false },
      "zipAndMarbleFalls": { "48053": 6873, "48031": 61, "48319": 45, "48453": 6, "total": 6985, "shareTo48053": "98.40 percent", "passes90": true },
      "whyTheyDiffer": "48453's 1839 rows have situs_city IS NULL. A NULL city is not a match, so the Travis tail drops out of the narrowed split and Burnet clears 90 percent. That single fact is the whole fix, and it is why splitCityMatch must not treat NULL as a match.",
      "foldConsistency": "the ZIP-only fold of the COMBINED statement equalled the standalone ZIP-only statement row for row, in the same session -- the direct evidence that one scan carries both splits"
    },
    "fixtureFidelity": "the 12 rows the combined statement returned on 2026-09-16 are byte-identical to MEASURED_78654_ROWS in the test file, including the order-of-magnitude detail that matters (48453's 1839-row NULL bucket). The fixture is a transcription of a real statement's output, not a construction."
  },

  "falsifierEvidence": {
    "method": "each falsifier is a test in services/retrieval-api/src/__tests__/coverage-check.test.ts; its TEETH are established by a performed-and-reverted edit that must make it fail, never by asserting that the test exists. All four edits were reverted and the working tree verified identical to the pushed commit (`git diff` empty) with the full service suite back at 19 files / 162 tests green.",
    "F1": {
      "predicted": "78654 + city Marble Falls resolves to 48053 (Burnet) when the combined split clears 90 percent",
      "result": "PASS, on the measured counts (6873 of 6985 = 98.40 percent) and on the live split itself. Asserted on BOTH stores plus a 'future state' companion: the same call answers `covered` the day Burnet is onboarded, with no code change -- so the fixture does not silently encode 'Burnet is not served yet' as part of the rule.",
      "teethA": "narrowByCity's last line changed to `return zipOnly` (narrowing disabled) -> 5 failed | 36 passed, including BOTH F1 assertions and the rule-1 narrowing unit test. Reverted.",
      "teethC": "splitCityMatch's match predicate changed to count NULL city_match as a match -> 4 failed | 37 passed, F1 among them: the 1839 NULL-city rows come back and Burnet is 85.3 percent again. This is the proof that the NULL-is-not-a-match distinction is load-bearing rather than a nicety. Reverted."
    },
    "F2": {
      "predicted": "a split ZIP whose candidates are all covered answers covered; all uncovered answers not-covered with the list; mixed answers indeterminate with each county's status",
      "result": "PASS on the fixture store AND on the real store through fakeSqlFactory, driven through the SAME two pure decisions (one implementation, so the fake and the database cannot answer differently -- DEV-PROCESS 2.4). The mixed body names each county's own status inline.",
      "teethB": "decideAmbiguousLocality's unanimous-covered branch disabled -> 3 failed | 38 passed, including the rule-2 unit test, its 'not vacuous' companion (the same candidate list must decide all three ways), and the fixture-store rule-2 test. Reverted.",
      "finding": "the not-covered branch had to keep the pre-existing contract that ALL THREE of countyFips/countyName/state are present, so a plurality county with no name on record is a refusal (indeterminate), not a not-covered with a guessed name. That is why the not-covered path re-checks the name rather than assuming decision.countyFips always resolves."
    },
    "F3": {
      "predicted": "Bell 76541 (6897 in 48027 against 3 in 48319) still resolves to 48027 with no city given",
      "result": "PASS locally, and PASS LIVE on revision hauska-retrieval-api-00094-wed: `zip=76541&state=TX` -> {\"status\":\"covered\"}. The local fixture additionally carries a hostile zipCityRows entry (76541|KILLEEN -> 48319 with 6985) so that a future edit which consults the city before the ZIP resolves will FAIL rather than quietly change Bell's answer."
    },
    "F4": {
      "predicted": "an out-of-state locality still answers without a database call",
      "result": "PASS, and by construction rather than by reading the branch: openSql is replaced with a function that increments a counter and throws, and the test asserts the counter is 0. A future edit that opens a connection for a non-Texas locality fails on `opened` rather than passing on a matching status string.",
      "liveLeg": "state=OR answered the outside-Texas indeterminate on the deployed revision; recorded as a behavioural control, explicitly NOT as cost evidence (its 8.2s is cold start)."
    },
    "F5_addedHere": {
      "predicted": "exactly ONE txgio_parcel statement is issued for a zip+city lookup, and its text carries BOTH predicates",
      "why": "the dispatch's cost section is a constraint the design claims to satisfy ('do not add a second full scan per request'). A constraint asserted only in prose is a plan, not a control, so it gets an instrument.",
      "result": "PASS: the fakeSqlFactory records every statement's text and timeout bucket; the filter over the txgio_parcel statements has length exactly 1, contains situs_zip AND situs_city, and was issued under timeoutMs 150000 (the geo-resolution bucket, not the 5s fast-lookup one).",
      "teethD": "a second `SELECT ... FROM txgio_parcel WHERE situs_zip = $1` inserted into the zip+city branch -> 1 failed | 40 passed with `expected [ { timeoutMs: 150000, ... }, ... ] to have a length of 1 but got 2`. Reverted.",
      "control": "a ZIP-only lookup also issues exactly one statement, and that statement does NOT contain situs_city -- so the assertion cannot be satisfied by a lazy implementation that adds the predicate everywhere."
    }
  },

  "localVerification": {
    "retrievalApiTypecheck": "pnpm run typecheck (tsc --noEmit) -> exit 0, no output",
    "retrievalApiTests": "npx vitest run -> Test Files 19 passed (19), Tests 162 passed (162), repeatedly, and again after every teeth revert. coverage-check.test.ts is 41 of them.",
    "workspaceTypecheck": "pnpm typecheck at the root -> exit 0 across every project",
    "repoWideTestFailsIdenticallyAtBase": {
      "what": "pnpm test at the root fails at packages/engine-core: 4 suites that fail to COLLECT with `SyntaxError: Invalid or unexpected token` (scripts/__tests__/cert-grade-and-report.test.ts, detect-placeholder-setback-citation.test.ts, preflight-and-report.test.ts, retire-road-class-setback-table.test.ts) plus a 15000ms timeout in src/site-plan/__tests__/author.test.ts.",
      "provenPreExisting": "mechanically, at the base commit: `git checkout e7cd0ae` then the same root `pnpm test` produced the SAME five files and the same error strings (captured to P:/tmp/p205b-base-root-test.txt), then `git checkout fix/...` restored the branch. An independent lane (P-293's PR #462) reported this same engine-core block at d88cf65, so it is the known Windows-local encoding artifact class plus a pre-existing timeout, not this lane's.",
      "corpusFlakeDisclosedNotHidden": "one of the three root runs additionally failed packages/corpus's src/adapters/ecode360/__tests__/default-rate.test.ts at 15204ms (vs 908ms isolated) under concurrent load. It passed in the other two root runs INCLUDING at base, and in every isolated run. Recorded as an intermittent load-sensitive flake in a package this change does not touch; it is NOT offered as a reason to disregard CI, and CI is authoritative for it."
    }
  },

  "openRisks": [
    "CI had not concluded at filing, and a comment-only second commit restarted it. The literal conclusion strings of whatever run is final are recorded in the close; if that run is red the close stays closed-partial regardless of the local evidence.",
    "NOT DEPLOYED and NOT MERGED. The customer predicate is unchanged until the integration seat deploys retrieval-api AND a Marble Falls address reads a NAMED county out of the live find_parcel. The dispatch pre-declares the probe notApplicable for this lane and this lane honours that: `status` will be closed-partial.",
    "The unindexed scan is unfixed. A cold 78654 lookup still measured 72.2s against a 150s statement timeout and a 300s request timeout. This lane narrows the number of scans per request from (potentially) two to one; it does not make one scan cheap. On a colder or busier instance the single remaining scan can still time out, and the answer would then be an honest indeterminate.",
    "The mixed-answer path re-reads candidate coverage whenever the 30s INDETERMINATE_TTL expires, i.e. one cached locality lookup plus N indexed EXISTS per 30s per busy locality. Bounded, but it is new read traffic that no test measures in production, and no counter was added for it.",
    "An empty narrowing inherits the ZIP-only ambiguity, which for 78654 means an indeterminate rather than a not-found. That is the deliberate reading of DEV-PROCESS 4.3, but it does mean a genuinely misspelled city cannot be distinguished from a ZIP whose CAD city spelling differs. No instrument measures that difference today."
  ],

  "fleetMemoryScratchBlock": {
    "GROUND-TRUTH": [
      "2026-09-16: the LIVE retrieval service is hauska-retrieval-api-00094-wed at https://hauska-retrieval-api-h7gvu7rgcq-uc.a.run.app (project hauska-prod-497015, us-central1). It still returns the PRE-change coverage answer: ?city=Marble+Falls&state=TX&zip=78654 -> indeterminate 'no dominant match: 48053 (11391), 48453 (1850), 48031 (61), 48319 (46), 48015 (2), 48501 (2), 48299 (1)', and the SAME body for no city and for the nonexistent city 'Nowhereville'.",
      "The retrieval API key IS reachable from this machine, which a sibling lane closed as a dead end: Secret Manager secret HAUSKA_ENGINE_API_KEY in project hauska-prod-497015. The service mounts it under the name RETRIEVAL_API_KEY, so the secret you look for and the secret that exists have different names, and an unscoped `gcloud secrets versions access` searches the default project (smartcity-os-prod) and 404s. Auth header is `Authorization: Bearer <key>`.",
      "txgio_parcel, read-only on the cortex store 2026-09-16 23:11:10.231873+00: 78654 splits 48053 11391 / 48453 1850 / 48031 61 / 48319 46 / 48015 2 / 48501 2 / 48299 1 (13353 rows, 85.31 percent to Burnet). Narrowed by upper(situs_city)='MARBLE FALLS': 48053 6873 / 48031 61 / 48319 45 / 48453 6 (6985 rows, 98.40 percent). The gap is 48453's 1839 NULL-city rows.",
      "Unindexed-scan cost, same session and statement text: combined one-scan statement 72226ms cold then 3213ms warm; ZIP-only statement 19536ms then 3011ms. So the narrowing costs ~7 percent more when warm, and one combined scan replaces two."
    ],
    "LESSON": [
      "A secret's NAME and a service's env var NAME can differ (HAUSKA_ENGINE_API_KEY vs RETRIEVAL_API_KEY) and gcloud silently searches the wrong PROJECT when you omit --project. A lane that hits both traps at once records 'the credential is not reachable' when the credential is fine -- that is how a reachable surface becomes a documented dead end for everyone downstream.",
      "`\\timing` on a cold vs warm pair is not a comparison. The first statement warms the same rows for the second, so 'combined is 3.7x slower' from a cold/warm pair is an artifact of ORDER. The only fair number in this lane's set is the warm pair (3.21s vs 3.01s). Report the pair that is comparable and say which one is not.",
      "A comment that claims provenance it does not have is the same defect class as a check that cannot fire. The first commit's header quoted the deployed revision's refusal as if it had been read from the revision, when what existed at the time was a store-side split plus the dispatch's prose. It was cheap to fix and would have been expensive to inherit."
    ],
    "DEAD-END": [
      "`heavy-scan-lease.mjs` verbs are strict and its flag names are hyphenated: `acquire` is not a verb (`take` is), and it takes --window-kind/--ttl-seconds/--reason, never --kind/--seconds. Four of five attempts returned usage errors before the first successful take. The script refusing unknown flags rather than ignoring them is the P-229 convention working, but the correct invocation is worth copying from here.",
      "Plain `grep`/`find`-style globs in this PowerShell are a trap for listing repo files; the file-name filter had to be built from Get-ChildItem. Unrelated to this lane's work but it cost real time earlier."
    ],
    "OPEN": [
      "The integration seat deploys this, then the probe that matters: `GET /parcel-record-gate-verdict/coverage/check?city=Marble+Falls&state=TX&zip=78654` on the new revision must answer `{\"status\":\"not-covered\",\"countyFips\":\"48053\",\"countyName\":\"Burnet County\",...}` (not indeterminate), and LDT's find_parcel must name Burnet instead of coalescing to coverage_check_unavailable. The lease-recovered probe recipe and the secret name are in the GROUND-TRUTH block above.",
      "The index on txgio_parcel(situs_zip) / (situs_city) is still the real fix for a cold lookup and still belongs to legacy-design-tools' drizzle schema. Until it lands, a cold Marble Falls address can take ~72s to answer. Report as a follow-on with this lane's numbers attached.",
      "Whether LDT's reader wants the new `candidates` array is unasked. It is additive and its isValidBody only tests `status` (verified at source in placeCoverageSource.ts, merged df9180d5), so nothing breaks, but a consumer that could surface 'this ZIP spans Burnet and Travis' is a product decision nobody has made."
    ]
  }
}
