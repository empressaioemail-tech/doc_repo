{
  "lane": "p226-envelope-front-line-radius",
  "planRows": ["P-226"],
  "status": "closed",
  "seat": "cente-p226",
  "probe": {
    "artifact": "_inbox/2026-09-15_182314_surface_probe.json",
    "note": "scripts/surface-probe.mjs --rows P-226 run live from this seat's doc_repo worktree, 2026-09-15T18:23:14.333Z. Result: PASS 0 FAIL 0 UNMEASURED 0 -- the generic probe has no P-226-specific leg, the same predicate-debt gap P-216 and P-200 already named for themselves. Real completion evidence for this row is the diagnosis itself (see CP1/CP2): a full, verified static trace establishing that hauska-map's own code cannot be the source of the defect, plus a documented, code-cited candidate root cause in the actual owning repo. No hauska-map code was changed, so there is nothing for a deploy-side probe to measure this pass regardless."
  },
  "verdict": "WRITE-TIME. Established BEFORE any edit, per the dispatch's own 'first task': hauska-map's envelope-drawing code (apps/property-explorer/src/browse/envelope-overlay.ts's normalizeEnvelope + envelopeInsetOverlay, called from ExplorerMap.tsx's handleEnvelope) reads the server's polygon geometry verbatim and only ever DELETES zero-width reversal-spike vertices from it (stripRingSpikes: splice-only, never moves/adds a vertex, a spike by construction encloses zero area) -- this is architecturally incapable of relocating a front edge onto or past the frontage. The buildable-envelope polygon itself is 100% server-computed, reached via fetchBuildableEnvelope (src/lib/buildable-envelope.js) POSTing {cortexBase}/brokerage/v1/place/buildable-envelope.",
  "correctedRepoAttribution": "The dispatch's own premise names hauska-engine as the plausible write-time owner ('the bad front edge is plausibly computed server-side, in hauska-engine ... packages/engine-core/src/geometry / the edge-role labelling'). Live code tracing (git show/git grep against origin/main, read-only, no worktree created in either repo) found the ACTUAL route and implementation entirely inside legacy-design-tools, not hauska-engine: artifacts/api-server/src/routes/brokeragePlaceBuildableEnvelope.ts -> composeBuildableEnvelopeDerivation.ts -> derive.ts's deriveBuildableEnvelope() -> geometry.ts's insetPerEdge()/insetProjected(), with per-edge front/side/rear roles from edgeLabeling.ts's labelEdges()/insetFeetForLabeling(). hauska-engine's own labelEdgesFromRoads (packages/engine-core/src/depth-warm/edgeLabeling.ts) is a same-named-in-spirit but SEPARATE function serving hauska-engine's own depth-warm/atom ground-truth pipeline (what P-216 investigated), not this live-derive call. This is the same class of correction P-216's own close made to its dispatch's stated repo, and is offered with the same live-evidence-over-stated-premise discipline.",
  "candidateRootCause": "Named, not fixed, and not owned by this lane (repo: hauska-map only; no edit was made outside this lane's own doc_repo worktree). geometry.ts's buildForbiddenStrips() unions one independent, axis-aligned rectangle per ring edge (setbackStrip: offset along that edge's OWN inward normal only) with no mitred/rounded join at vertices, then differences the union from the parcel. edgeLabeling.ts's own module comment documents that a curved frontage digitizes as several short near-collinear chords (its own example: 48453:280239, '7 segments with ~0.6deg joints') -- every joint along such a curve is a convex ring vertex where two adjacent offset rectangles meet at two DIFFERENT offset points rather than one mitred point, which can leave a small uncovered wedge of buildable area at that vertex, closer to the true property line than the front setback allows. edgeLabeling.ts's groupCollinearEdges/expandGroupLabelsToEdges (WDLL P-60b item 3, same module, same cited parcel) already makes every chord along one frontage share a single 'front' label, so the per-edge label itself is likely correct; the rectangle-union under-coverage repeated at each of several closely-spaced joints along the same curve is the more likely mechanism for a front line that reads as running along/into the frontage across its whole length, distinct from the single-spike 48453:280239 incident (which stripReversalSpikes, present in this same file, already remedies). This is a read-code hypothesis, not a verified fix -- it needs a real ring for 48453:289990 run through insetProjected(), which this lane did not do (out of scope: this lane holds no write access and made no edit to legacy-design-tools).",
  "namedParcelsVerifiedOnTheMap": "None drawn live this pass. This session has authenticated mcp__claude_ai_Smart_Site__* MCP access (a real signed-in account against production) but no browser session against smartsite.cloud, so 'verified on the map' here means the live derive CALL CHAIN was traced from the map's own source down to its actual upstream, not that the wedge was watched paint on screen. get_smart_site(48453:289990, depth:node) was read live: envelope draw overlay came back {state:'refused', reason:'atom_path_pending'} -- that MCP surface reads from the LEDGER/atom-chain path (a different, baked serving route than the map's own live POST derive), so it does not carry this defect either way and is not cited as evidence for or against the front-line finding.",
  "liveExportAttemptBlocked": "The dispatch's own discriminator (generate the PDF site plan for 48453:289990, compare its front edge to the map's) could not be run: mcp__claude_ai_Smart_Site__export_instrument(kind:'siteplan') timed out identically 4/4 times at exactly 50000ms against hauska-engine-api's site-plan-export/refresh route. A same-parcel export_instrument(kind:'dossier') call in between returned fast with a real structured refusal (422 pipeline_output_absent), proving the MCP bridge, auth, and entitlement gate are all live and reachable -- the site-plan-export/refresh route specifically is not responding today. Treated as a live infra issue independent of P-226, not as evidence either way; recorded in leave_behind.",
  "falsifier": "Dispatch's four falsifiers pre-registered at CP1 before running anything (see CP1). F1 (PDF match -> not the overlay) could not be checked live (export blocked); the static trace substitutes as the deciding evidence and is stated as such, not silently upgraded to 'PDF confirmed'. F2/F3 (fitted-to-one-ring / area side effect) are not applicable -- no hauska-map fix was built or shipped this pass. F4 (uniform-inset masquerading as a fix) is satisfied by construction: insetParcelBySetbacks was read, confirmed still dead code (only its own test file references it), and not wired up, touched, deleted, or revived.",
  "contradicted": "The dispatch's own repo attribution for the write-time hypothesis (hauska-engine) was contradicted by live code tracing -- the actual owning repo is legacy-design-tools (artifacts/api-server), not hauska-engine; see correctedRepoAttribution. Nothing else the dispatch asserted was contradicted: the draw-time/write-time framing, the discriminator method, the 17005 Simsbrook precedent's description, insetParcelBySetbacks' dead-code status, and the entitlement-gate mechanism all held up against direct code inspection.",
  "leave_behind": [
    {"item": "The write-time front-edge-inset defect itself: named (legacy-design-tools artifacts/api-server/src/lib/buildableEnvelope/geometry.ts, buildForbiddenStrips()/insetPerEdge()/insetProjected(), candidate mechanism: no mitred/rounded vertex join on the per-edge rectangle-strip union, under-covering convex frontage joints on a multi-chord curved parcel ring) but not verified with a real fixture or fixed. Needs a lane with write access to legacy-design-tools and a real ring for 48453:289990 (or 48453:280239) run through insetProjected() to confirm the mechanism before fixing it.", "row": "candidate follow-on to P-226 in legacy-design-tools, UNCARDED"},
    {"item": "mcp__claude_ai_Smart_Site__export_instrument(kind:'siteplan') is not responding for 48453:289990 today (4/4 identical 50000ms timeouts against hauska-engine-api's site-plan-export/refresh), while kind:'dossier' on the same parcel returns fast. Worth a live re-check once the engine's own state changes; if it is unblocked, running the dispatch's own PDF discriminator would independently corroborate (or correct) this close's write-time verdict.", "row": "UNCARDED"},
    {"item": "scripts/surface-probe.mjs has no P-226-specific leg (ran --rows P-226, got PASS 0 FAIL 0 UNMEASURED 0), the same gap P-216/P-200 already named for themselves. Predicate debt against OPS-24 law 1, independent of this row.", "row": "UNCARDED"},
    {"item": "hauska-map's own draw-time stripRingSpikes/stripEnvelopeSpikes (envelope-overlay.ts) is now confirmed REDUNDANT: the server (geometry.ts's stripReversalSpikes, same 160deg/1.5m-mouth shape, called inside insetProjected before any caller sees the ring) already strips reversal spikes before the wire response is built. Not a defect -- a harmless duplicate safety net -- but worth noting for whoever next touches this file, since it is not actually load-bearing against anything the server doesn't already handle.", "row": "UNCARDED, informational"}
  ],
  "missionPremise": "Operator-reported live on smartsite.cloud: on a curved-frontage parcel (48453:289990, 2407 Princeton Dr, Travis), the drawn buildable envelope's front line lands on or inside the street frontage instead of offset by the district's front setback, reading as extending into the right of way -- a documented prior of the same geometry class exists (17005 Simsbrook / 48453:280239, 2026-08-24, remedied draw-time-only for its own zero-width-spike symptom, not the underlying write-time geometry).",
  "completionPredicate": "Per the dispatch's own words: 'Say which layer owns it and act accordingly' / '(b) Write-time, and diagnosed here. The file and function that computes the front-edge inset is named, the reason the front edge lands on the frontage is stated, and the row hands off ... with the evidence. No hauska-map code is changed to hide a server defect.' Met: the owning layer is named (write-time, legacy-design-tools not hauska-engine), the specific function chain is named, a candidate mechanism is stated and clearly flagged as unverified-by-fixture, and zero hauska-map files were edited.",
  "scopeBasis": "Scoped exactly to the dispatch's own FIRST TASK -- determine draw-time vs write-time before touching envelope-overlay.ts -- and stopped there per its explicit 'IF IT IS WRITE-TIME, STOP AND HAND BACK' instruction. No hauska-map edit, no PR, no fixture, no deploy, no merge. insetParcelBySetbacks was read and left untouched. The repo actually implicated (legacy-design-tools) was investigated strictly read-only (git show/git grep against origin/main; no worktree created, no file written) -- consistent with both the dispatch's 'do not write to hauska-engine' instruction and this lane's own single-repo (hauska-map) mandate, extended in spirit to the repo actually found to own the defect once that correction was established."
}
