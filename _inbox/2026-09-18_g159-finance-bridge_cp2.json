{
  "lane": "g159-finance-bridge",
  "planRow": "G-159",
  "seat": "cente-vsc-g159",
  "dispatch": "_dispatches/2026-09-18_g159-finance-bridge_dispatch.md",
  "generatedAt": "2026-09-18T12:25:00Z",

  "snapshot": {
    "statedInFirstOutput": true,
    "smartcity-os": {
      "repository": "empressaioemail-tech/smartcity-os",
      "worktree": "P:\\agent-worktrees\\g159-finance-bridge-os",
      "laneBranch": "fix/g159-finance-bridge",
      "laneCommit": "3cc1bc06",
      "baseWasMain": "92c73e39",
      "mergeCommitOnMain": "a400f7be (squash, PR #58)",
      "mainBeforeThisLane": "92c73e39 feat(scraper): enforce shared-secret header on every scraper-worker request (D-10) (#57)",
      "whyThatMatters": "The dispatch says main is one commit past production and that commit touches only the scraper, so the API is unaffected. Confirmed: 92c73e39's parent is 8bea7fa5, the production pin, and 92c73e39 touches the scraper only."
    },
    "smartcity-os-production": {
      "app": "walrus-app (2a2a3a1a-b441-4296-8628-a82b20ded1b2)",
      "url": "https://smartcityos.io",
      "githubBranch": "d9-api-8bea7fa",
      "sourceCommitHash": "e783f3510d8f582894c4317e511650af327ea0a1",
      "phase": "ACTIVE",
      "health": "HEALTHY",
      "deployedAt": "2026-09-17T19:09:06Z",
      "ancestry": "e783f351 is the tip of d9-api-8bea7fa, and 8bea7fa5 is an ancestor of it. a400f7be (this lane's merge) is NOT an ancestor of the deploy branch."
    },
    "smartcity-dashboards": {
      "repository": "empressaioemail-tech/smartcity-dashboards",
      "worktree": "P:\\smartcity-dashboards-worktrees\\g159-finance-bridge",
      "laneBranch": "g159-finance-bridge",
      "laneCommit": "6d2b113",
      "baseWasMain": "7dda6db (the G-154 squash, PR #68 — the merge that cleared this step's gate)",
      "mergeCommitOnMain": "53ade8a9dbdb649acbe04c721a54fa518779d0b1 (squash, PR #69)",
      "pullRequest": "https://github.com/empressaioemail-tech/smartcity-dashboards/pull/69",
      "checks": "ci: test PASS (17s), a11y PASS (8m42s). mergeStateStatus CLEAN at merge.",
      "originMainBeforeThisLane": "7dda6db G-154: make the live Development services lens match its ratified design (#68)",
      "blockClearedAt": "2026-09-18T12:12:26Z — G-154 merged, which is the dispatch's own precondition for STEP 2",
      "whyThisWasBlockedBefore": "The dispatch: 'Wait for G-154 to merge before starting this step.' The prior close (12:25Z) recorded smartcity-dashboards origin/main as fd8562c2 (the G-156 merge) and STEP 2 as UNMEASURED. G-154 landed after that, moving main to 7dda6db."
    },
    "smartcity-dashboards-uat": {
      "app": "d12-main-uat (7cd6f652-9e7e-4abc-abb4-e2a94623866c)",
      "liveUrl": "https://d12-main-uat-gqnjx.ondigitalocean.app",
      "deployedSourceCommitHash": "53ade8a9dbdb649acbe04c721a54fa518779d0b1",
      "deploymentId": "cc07754f-c5c7-4332-9d67-d51359111e41",
      "phase": "ACTIVE",
      "health": "HEALTHY",
      "phaseLastUpdatedAt": "2026-09-18T12:56:15Z",
      "readBackVia": "do-apps MCP apps-get-deployment-status (OPS-25 rule 13)",
      "whatWasDeployedBefore": "deployment 049a7de7 at 7dda6db — the pre-fix commit, which served the defect during the pre-fix probe leg",
      "nonProduction": "Non-production proof target only. Nothing was deployed to v1, to walrus-app, or to the GCP smartcity-api rollback copy."
    },
    "doc_repo": {
      "worktreeThisSessionIsRootedIn": "P:\\seat-worktrees\\g159-finance-bridge\\doc_repo",
      "branch": "lane/g159-finance-bridge",
      "commit": "e9a9064a",
      "artifactRootNote": "CP1, CP2, CLOSE and the probe artifact are all under this worktree's _inbox/. CP1 was first filed in P:\\doc_repo, the integration seat's checkout, and moved here when the dispatch's rule for a lane spawned by the dispatch planner was applied; that correction is recorded in CP1's addendum because the planner reads these paths."
    }
  },

  "step1": {
    "verdict": "PASS - four routes merged to main, proven answering and refusing live, proven NOT deployed.",
    "pullRequest": "https://github.com/empressaioemail-tech/smartcity-os/pull/58",
    "filesAdded": [
      "server/routes/platform-finance.ts (new, 4 routes)",
      "tests/api/platform-finance.test.ts (new, 10 tests)"
    ],
    "filesModifiedAdditively": [
      "server/routes.ts (one import, one registration call; nothing else touched)",
      "server/services/opengov-bnp.ts (the entity GUID const is now exported, so the route does not duplicate the literal)",
      "server/routes/mygov.ts (resolveBastropTenantId added; getBastropTenantId delegates to it - see defectsFixed[0])"
    ],
    "routes": [
      {
        "path": "GET /api/platform/opengov/budgets",
        "source": "OpenGov BNP budgets, live",
        "answersLive": "200, total 12, productionCount 6, entityId 3c8981ac-1a8b-463d-b26b-5ef37c66734f"
      },
      {
        "path": "GET /api/platform/opengov/budgets/:budgetId/amounts-summary",
        "source": "OpenGov BNP amounts-summary, live",
        "answersLive": "200, expenses 74349204 / -4769353 / 69579851, revenues 73425092 / +1143298 / 74568390"
      },
      {
        "path": "GET /api/platform/opengov/chart-of-accounts",
        "source": "OpenGov BNP chart-of-accounts, live",
        "answersLive": "200, 2 COAs; the primary carries 5 declared segments"
      },
      {
        "path": "GET /api/platform/finance/permit-revenue/summary",
        "source": "mygov_fees, live, tenant resolved by slug",
        "answersLive": "200, tenantId 2, FY2026 department breakdown"
      }
    ],
    "deliberateExclusions": [
      {
        "what": "The v1 REST leg (bastrop.opengov.com /api/v1/entities and /reports, i.e. what server/routes/opengov.ts calls).",
        "basis": "Both answer 200 with a 32-record GLOBAL catalog and ZERO of the 32 are Bastrop's. getReports() filters on BASTROP_ENTITY_SLUGS and returns []. Exposing that under a Bastrop-named platform route would put 32 non-Bastrop entities behind the bridge - the silent-fallback defect class with a Bastrop label. Measured in CP1, restated here so the exclusion is part of this file's contract and not just a claim in a message."
      },
      {
        "what": "A by-department / by-fund breakdown route (what the session route /api/opengov/bnp/budgets/:id/breakdown serves).",
        "basis": "It fans out to getAccountsByIds in 50-id chunks; at the measured 3,247 accounts that is ~65 upstream POSTs per call, which is why the session route caches it for 4 hours. A key-gated route with no session and no cache would put that fan-out on every read. Department spend is UNACCOUNTED in the lens design anyway."
      },
      {
        "what": "A pre-derived net position.",
        "basis": "amounts-summary returns six numbers and no net. The capture's $5.0M is revenues.proposed - expenses.proposed, so it ships under `derived` with the arithmetic named, never as a measured attribute."
      }
    ],
    "proofBothDirections": {
      "method": "The merged routes were booted from the lane worktree and hit over real HTTP against the LIVE vendor and the LIVE Bastrop database. 16 of 16 cells behave as specified.",
      "withKey": "4/4 routes answered 200 with live Bastrop data.",
      "noToken": "4/4 routes 401 platform_internal_required.",
      "wrongToken": "4/4 routes 401 platform_internal_required.",
      "noKeyConfigured": "4/4 routes 503 platform_internal_not_configured (fails closed, never open).",
      "adoptedBudgetMatchesCaptureToTheDigit": {
        "captureExpensesTotal": 69600000,
        "liveExpensesProposed": 69579851,
        "captureExpensesBase": 74300000,
        "liveExpensesBase": 74349204,
        "captureRevenuesTotal": 74600000,
        "liveRevenuesProposed": 74568390,
        "conclusion": "The design's 'adopted budget -> MEASURED' is true OF THE SOURCE. What was missing was the v1 platform route, which now exists."
      },
      "artifact": "_inbox/2026-09-18_122500_g159_platform_route_probe.json"
    },
    "verification": {
      "tests": "206 tests pass across 30 files, including the pre-existing platform-route tests, so the mygov.ts tenant refactor did not disturb the existing surface.",
      "typecheck": "tsc adds no new errors. The changed files add none; the repo has pre-existing errors in mygov-scraper.ts, opengov-transparency.ts, property-intelligence.ts and mygov.ts at untouched lines (all at least 700 lines away from this lane's edits).",
      "securityChecks": "Gitleaks pass (15s), Semgrep pass (1m41s), Trivy pass (37s) on PR #58.",
      "notDeployed": "Proven twice, independently: by commit ancestry (a400f7be is not on the deploy branch) and by HTTP (all four paths return the SPA shell in production, the same signature a deliberately fake /api/platform/ control path returns, while /api/platform/mygov/permits and /api/opengov/reports both still answer 401 JSON)."
    }
  },

  "defectsFoundAndFixed": [
    {
      "id": "tenant-default-wearing-a-resolved-label",
      "severity": "This lane's own code would have lied on its fallback path.",
      "what": "getBastropTenantId() in server/routes/mygov.ts ends in `row?.id ?? 1`. This lane's permit route PUBLISHES the tenant it read and how it resolved it. On the fallback path it would have announced 'resolveBastropTenantId() -> tenants.slug = bastrop' while actually having read tenant 1 - a silent default wearing a resolved label, which is worse than v1's bare `|| 1` because it is labelled.",
      "fix": "Added resolveBastropTenantId(), which returns null on a miss and writes its cache ONLY on a successful resolution. getBastropTenantId() now delegates to it and keeps its exact existing contract (`?? 1`) for existing callers, so no pre-G-159 caller changes behaviour. One query, not two (DEV_PROCESS 6.2).",
      "behaviouralChangeDisclosed": "A missing slug is no longer cached as tenant 1, so the lookup self-heals and can never poison the cache with a wrong city.",
      "provenByViolation": "tests/api/platform-finance.test.ts: with the resolver mocked to null the route answers 503 bastrop_tenant_unresolved, asserts tenantId is absent, and asserts the SQL never ran (executeMock not called) - so no other city's fees were read.",
      "liveResult": "On the live database the slug resolves: tenantId 2, not the fallback 1."
    },
    {
      "id": "permit-revenue-clamp-hid-a-conflict",
      "severity": "A wrong number would have been served as measured money.",
      "what": "The session route this derives from ends in Math.max(0, charged - collected), which renders 'collected exceeds charged' as a clean $0 outstanding. That is the identical move the lens design already caught in v1's own header: 'Outstanding balance is $0 on the header while the page-8 rows carry $918,547.40 outstanding on Building alone.'",
      "fix": "The difference ships UNCLAMPED, with an arithmeticConflict block naming the conflict, the rate, the surplus and the mechanism. The lens design already has the state for this: CONFLICT. The route carries the conflict instead of clamping it away.",
      "provenByViolation": "tests/api/platform-finance.test.ts carries the live FY2026 shape and asserts detected true, code collected_exceeds_charged, rate 150, and a NEGATIVE outstanding (-87,700,895.05) where v1 rendered $0. The clean case is asserted too (detected false), so the flag can be shown to fire and to not fire."
    }
  ],

  "thePermitRevenueDataFinding": {
    "headline": "The corruption the design captured as 157% is REAL, live, and reproducible - it is not a screenshot artifact and not this route's arithmetic.",
    "designAlreadyFoundIt": "capture-figures.json permitRevenue.findings: 'A collection rate of 157% is not a possible state. Collected cannot exceed charged. It is rendered in green with a success mark.'",
    "measuredLive2026_09_18": {
      "fiscalYear": 2026,
      "totalCharged": 174092822.20997974,
      "totalCollected": 261793717.25996742,
      "collectionRatePct": 150,
      "permitCount": 1491,
      "recordCount": 290077
    },
    "whyTheMagnitudesAreImpossible": "$261.8M collected in permit fees against a total city operating budget of $69.6M is nearly four times the whole budget. The design's own capture shows the same shape at $664.7M against $69.6M.",
    "likelyMechanismSourcedFromTheSchema": "mygov_fees is keyed by the unique index (tenant_id, report_id, permit_number, account_number, date_paid). Because report_id is part of the key, the same fee listed in more than one report window is stored once per window and therefore SUMMED once per window. 290,077 rows across 1,491 distinct permits is ~195 rows per permit, which is the signature of exactly that.",
    "whyThisLaneDidNotFixIt": "Choosing WHICH report window is authoritative is a finance-owner decision. Inventing a dedupe rule would silently change Bastrop's numbers, which is the failure this program exists to prevent. Selecting a report window is now a named, reproduced, owned question instead of a screenshot.",
    "whatIsOwedNext": "A finance-owner ruling on the authoritative report window (latest by report_id? latest by report_window_start? sum of distinct fee identities?), then a shared builder both the session route and the platform route call."
  },

  "theBudgetFilterHole": {
    "what": "The production filter this lane inherited - and deliberately reused verbatim so the platform route and the session route cannot disagree - excludes names containing Test, SANDBOX, training, Proof or UAT.",
    "hole": "It does NOT exclude DNU ('do not use'). The live budget list contains 'FY2027 Operating Budget (DNU)' and 'FY2027 Operating Budget (DNU2)', and both survive into productionCount 6.",
    "measured": "productionCount 6 of 12, with DNU and DNU2 among the 6.",
    "whyNotFixedHere": "The filter's rule is shared with the session route's cache warm-up. Expanding it in the platform route alone would make the two routes disagree about which budgets are real, which is worse than the hole. Recorded as a finding with its owner.",
    "note": "The rule is published on the wire in the response (productionRule), so a consumer can see which rule produced the count rather than being handed a bare number."
  },

  "theUnmergedBranchFinding": {
    "branch": "fix/w1-c-3-opengov-bnp-hardening in smartcity-os",
    "verdict": "REDUNDANT - it holds nothing this lane needed and nothing main lacks.",
    "basis": "Its content is already on main: it landed as PR #8. Verified by comparing the branch against main rather than by reading the branch name, which is what the dispatch asked for."
  },

  "step2": {
    "verdict": "PASS — run after G-154 cleared the gate, merged to main as 53ade8a, and proven on the deployed non-production app in both directions.",
    "gate": "The dispatch: 'Wait for G-154 to merge before starting this step. G-154 is working in smartcity-dashboards now, and two lanes in one product repo is how two changes to city-pack.mjs collide.'",
    "gateState": "CLEARED. G-154 (PR #68) merged 2026-09-18T12:12:26Z, moving origin/main from fd8562c2 to 7dda6db. This work branched from 7dda6db, so it never shared a working tree with G-154.",
    "step2aAdapterShape": {
      "verdict": "PASS",
      "what": "src/adapters.mjs: `opengov:budget` moves from `declared: false` to `declared: true, feedOnly: true`, with every field carrying a basis drawn from CP1's real records.",
      "whyFeedOnly": "The shape is read from a live feed and never generates a fixture. A domain that invented appropriations for template-city would put money on the demo pack — the failure class the feed/grant split exists to prevent.",
      "fieldsDeclared": "name (text), entityId (text), coaId (text), and six required integers: expensesBaseAmount, expensesAdjustmentAmount, expensesProposedAmount, revenuesBaseAmount, revenuesAdjustmentAmount, revenuesProposedAmount.",
      "fieldsRefusedAndWhy": [
        "fiscalYear — refused rather than parsed out of the budget NAME ('FY2027 Operating Budget (WORKING)'). The live budget record publishes no numeric year; parsing one from a display name and presenting a guess as a field is the defect the lens design names.",
        "netPosition — derivable as revenuesProposedAmount minus expensesProposedAmount, and deliberately not carried as a field. The bridge publishes it under `derived` with its own 'never consumed as measured' note, so it does not get promoted into a column that reads like a reading.",
        "fractional amounts — the shape declares integer, and the decoder refuses rather than rounds."
      ],
      "decoder": "src/opengov-budget.mjs — mapRealBudgetRecord() maps a v1 budget + amounts-summary pair and asserts the result against the declared shape via assertRecordShape(). It refuses on a missing, null, non-numeric or fractional amount rather than coercing one, so an upstream vendor change surfaces as a refusal at the boundary instead of a plausible number in the lens.",
      "validatedAgainst": "The verbatim live Bastrop record captured in CP1 (FY2027 Operating Budget (WORKING), id 130231, expenses 74349204 / -4769353 / 69579851, revenues 73425092 / +1143298 / 74568390) — not against a template-city fixture, as the dispatch requires."
    },
    "step2bGrantWithheld": "PASS AS A DELIBERATE NON-ACTION — no bastrop_tx grant was added. The dispatch: 'Do not add the bastrop_tx grant yet... The grant is the last step, after D-13 and D-14, and it goes in your leave_behind.' Confirmed absent from src/city-pack.mjs and src/adapters.mjs.",
    "step2cCityDefaultFix": {
      "verdict": "PASS — proven by violation, twice and independently.",
      "site": "src/server.mjs, GET /api/lenses/finance/sources. It read `url.searchParams.get('cityKey') || 'template-city'` (line 744 on 7dda6db), so a caller who named no pack was answered 200 with the DEMO pack's finance states under whatever city they meant.",
      "fix": "A missing, empty or whitespace-only cityKey is refused with 400 city_key_required, checked BEFORE the caller is resolved (a request naming no pack is malformed whatever identity presents it, and refusing it leaks nothing a 401 would withhold). A named-but-unknown pack still takes the pack path and still answers 404 'unknown city pack', so the two findings stay distinguishable.",
      "provenByViolationInTheTestRunner": "src/finance-route.test.mjs — 6 assertions. Against the pre-fix line, 3 fail (the two city-key-absence tests and THE VIOLATION test); against the fix, 6 of 6 pass. The violation test is a NEGATION: the keyless response must carry no finance block, none of the four source ids, and no money token, because the old behaviour was a complete, well-formed 200 payload that nothing else could distinguish from a real pack's states.",
      "provenByViolationOnTheDeployedSurface": "The paired control in _inbox/2026-09-18_125700_g159_step2_uat_proof.json: the same unauthenticated request to the same URL answered 200 with cityKeyEchoed 'template-city' and a full finance block at 7dda6db, and 400 city_key_required with no finance block at 53ade8a. See theUatProof below.",
      "controlThatKeepsItFromBeingAnOutage": "?cityKey=template-city answers 200 with all four source ids in BOTH legs, and ?cityKey=no-such-city stays 404 in both. The fix refuses an ABSENCE; it does not refuse the route."
    },
    "theUatProof": {
      "verdict": "PASS for the change under test",
      "artifact": "_inbox/2026-09-18_125700_g159_step2_uat_proof.json",
      "changedBehaviour": "The deployed surface now refuses a keyless finance request: 400 city_key_required, no payload, on all three absence shapes (absent, empty, whitespace).",
      "unchangedBehaviour": "A named pack still answers 200 with its four source states; an unknown pack still answers 404; a tenant-private pack still answers 401 to an anonymous caller.",
      "credentialsAreNeededToMoveTheLensState": "No. The budget moves to MEASURED only when bastrop_tx carries an opengov GRANT, which STEP 2 (b) deliberately withholds until D-13 and D-14 land. A credentialed read of bastrop_tx today would show four UNACCOUNTED states, because the grant — not the route — is what feeds them."
    },
    "filesChangedInSmartcityDashboards": [
      "src/adapters.mjs (declare opengov:budget, feedOnly)",
      "src/opengov-budget.mjs (new — mapRealBudgetRecord + refusal helpers)",
      "src/opengov-budget.test.mjs (new — shape, mapping, refusals by violation)",
      "src/fixtures.test.mjs (divergence test extended to understand feedOnly shapes)",
      "src/finance-lens.mjs (basis string updated: the adopted-budget source's UNACCOUNTED now rests on 'no grant, and nothing read' rather than 'no shape to map')",
      "src/finance-lens.test.mjs (pins RECORD_SHAPES.opengov.declared true; basis assertions updated)",
      "src/server.mjs (the city-default refusal)",
      "src/finance-route.test.mjs (new — 6 HTTP-level assertions)",
      "web/app.js (unread-source label now says 'an unnamed pack')",
      "web/index.html (rebaked via scripts/bake-finance-lens.mjs to match the lens change)"
    ],
    "verification": {
      "suite": "792 tests, 791 pass. The single failure is the pre-existing src/server.test.mjs:1086 ECONNRESET flake, which was reproduced on a pristine origin/main worktree before this branch existed, so it is not a regression from this work.",
      "webBytesUnchangedByNormalization": "D-9 pins the byte shape of web/**. Verified after committing: all 7 files under web/ hash identically with and without git filters (app.js e7e7eb67, index.html 5ce2d8b3, property-map.css e047d103, property-map.html aa55b276, property-map.js 9813d32d, sc-kit.css c0c0ffc1, shell.css a06651d0), and CR count equals LF count in each. The commit was made with core.autocrlf=false."
    }
  },

  "everyOtherTemplateCityDefaultFound": {
    "method": "git grep -n 'template-city' origin/main in smartcity-dashboards, read-only. The preamble's rule 3 (never default a city; a missing cityKey refuses) applies to every one of these; the dispatch fixes only the finance route and calls the rest a finding.",
    "requestScopedFallbacksInSrcServerMjs": [
      { "route": "POST /api/adapters/municode/calendar/run (declared :243)", "fallbackLine": 248, "note": "PARTIALLY DEFENDED: lines 249-250 immediately guard `cityKey !== 'template-city'` and refuse with 403, so the default cannot silently serve another city here. Still a default, but this one cannot leak." },
      { "route": "GET /api/lenses/development-services/pipeline (declared :392)", "fallbackLine": 394, "note": "Unguarded." },
      { "route": "GET /api/city-domains (declared :440)", "fallbackLine": 442, "note": "Unguarded." },
      { "route": "GET /api/domains/* (declared :497)", "fallbackLine": 499, "note": "Unguarded. Covers every domain endpoint." },
      { "route": "GET /api/city-identity (declared :550)", "fallbackLine": 552, "note": "Unguarded." },
      { "route": "GET /api/shell (declared :578)", "fallbackLine": 580, "note": "Unguarded." },
      { "route": "GET /api/lenses/city-manager/compose (declared :288)", "fallbackLine": 290, "note": "Unguarded, and it defaults via the imported DEFAULT_CITY_KEY rather than a literal, so a grep for the literal alone would miss it." }
    ],
    "theOneThatWasFixed": {
      "route": "GET /api/lenses/finance/sources (declared :742)",
      "wasFallbackLine": 744,
      "note": "This was the eighth entry in this list and the STEP 2 (c) target. It no longer defaults: a missing, empty or whitespace-only cityKey is refused with 400 city_key_required. Verified against the fixed tree rather than against the enumeration, because an enumeration gathered before a fix is exactly the kind of claim that goes stale silently — the remaining seven above were re-grepped on the fixed branch (commit 6d2b113) and are unchanged.",
      "whatTheFixLeftUntouched": "The seven remaining request-scoped fallbacks and the four module-level ones below are a finding, not scope, per the dispatch ('Fix only the finance route. The others are a finding, not scope.')."
    },
    "moduleLevelDefaults": [
      { "file": "src/compose.mjs", "line": 7, "detail": "export const DEFAULT_CITY_KEY = 'template-city'; applied at :306 and at :312, where `String(cityKey || DEFAULT_CITY_KEY).trim() || DEFAULT_CITY_KEY` defaults TWICE - an empty string also becomes template-city." },
      { "file": "src/staff-map.mjs", "line": 2, "detail": "Its own DEFAULT_CITY_KEY = 'template-city', applied at :13." },
      { "file": "src/municode-calendar.mjs", "line": 282, "detail": "Parameter default: cityKey = 'template-city'." },
      { "file": "src/run-municode-calendar.mjs", "line": 7, "detail": "CLI default: process.argv[2] || 'template-city'." }
    ],
    "notDefects": [
      { "file": "src/city-pack.mjs", "line": 35, "detail": "cityKey: 'template-city' is the TEMPLATE_CITY pack's own identity, not a fallback." },
      { "file": "web/index.html", "detail": "16 literals naming template-city on every pack, per city-identity.test.mjs:316. Worth a look when the city default is fixed, but the test asserts they are the demo's own identity rather than a fallback." }
    ],
    "whyThisListMatters": "The dispatch requires this enumeration in the close, and the count is the point: the finance route was not one leak, it was one of EIGHT request-scoped fallbacks in a single file, plus four module-level defaults. Fixing the finance route — which is all the dispatch scopes to this lane — leaves SEVEN request-scoped fallbacks in that same file. Six of the seven are unguarded outright; the municode one is the only one that cannot leak, because lines 249-250 immediately refuse anything that is not template-city."
  },

  "leaveBehind": [
    "THE bastrop_tx GRANT, which the dispatch names as the required leave-behind and which STEP 2 (b) deliberately withholds until D-13 and D-14 land. It is the last step, not an oversight.",
    "STEP 2 IS DONE, NOT OWED (corrected at 13:00Z): this entry previously read 'STEP 2 ITSELF, gated on G-154'. G-154 merged at 12:12:26Z, STEP 2 ran, and it is merged to smartcity-dashboards main as 53ade8a (PR #69) and proven on d12-main-uat. The dispatch's STEP 2 (c) is complete. What STEP 2 does NOT do — and was never meant to do — is move Bastrop's budget to MEASURED: that needs the grant below.",
    "THE DNU HOLE IN THE PRODUCTION BUDGET FILTER (see theBudgetFilterHole). It needs an owner because the rule is shared with the session route's cache warm-up.",
    "THE mygov_fees REPORT-WINDOW DEDUPE QUESTION (see thePermitRevenueDataFinding). Now reproducible, with a rate, a row count and a schema-level mechanism, instead of a screenshot. Finance-owner decision.",
    "CONVERGENCE: server/routes/finance.ts still carries the clamped Math.max(0, ...) difference and the bare `(req as any).tenantId || 1` default on its session routes. This lane built the honest form on the new platform route and deliberately did not change the production session routes. A v1-scoped follow-up should converge them onto one builder.",
    "OPENGOV_BNP_API_KEY IS STILL A LATENT TRAP (CP1's credential surprise, reported not fixed). The key NAMED for BNP does not answer for Bastrop; the key named for the v1 REST API does, and getBnpApiKey() reads OPENGOV_API_KEY first, which is why production works. Any future lane that 'tidies' that function to prefer its own namesake would silently take BNP to zero budgets and render UNACCOUNTED for a city whose budget is readable. Both variables are present in the walrus-app spec.",
    "THE PLATFORM ROUTES ARE UNREACHABLE UNTIL D-13 AND D-14 LAND. Merged to main, proven not deployed, by design."
  ],

  "subAgents": {
    "spawned": 0,
    "maxDepth": 0,
    "note": "FAN-DEPTH 0. This lane launched no sub-agents: the proofs, the merges, the live harness and the read-only enumeration were all run in this session."
  }
}
