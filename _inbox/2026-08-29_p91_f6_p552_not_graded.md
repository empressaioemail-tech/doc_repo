---
id: 2026-08-29_p91_f6_p552_not_graded
title: p552 regrade did not replace the iframe
date: 2026-08-29
status: not-graded
plan_row: P-91
---

# What the frames show

list_screens still paints `Open did not reach me` on the three-row A13 board. Composer then named `48021:900099`. One `get_smart_site`. Same `baked_snapshot_not_found`. No post-tool panel.

# Why this is not p552

A fresh `app-p552.html` document starts with `openFail` null. list_screens would paint the board with no fail line. This board still has the p551 fail sentence. Same document.

The `ui/message` ok path still does not `accept()`. list_screens can update the boot strip to `reply-ok` and leave the old board HTML in place. That is how a reconnect that did not replace the iframe looks like a reopen.

# Score

F6 step 2 on p552 is unmeasured. Do not take MET from the tool JSON. That 404 is unchanged from p551. The punch is the fail sentence after Open.
