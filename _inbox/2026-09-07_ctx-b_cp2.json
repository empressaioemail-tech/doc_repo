{
  "id": "2026-09-07_ctx-b_cp2",
  "kind": "cp2",
  "lane": "ctx-b",
  "planRows": ["P-124"],
  "filedAt": "2026-09-07T16:42:00Z",
  "seat": "planner-run session (Claude), same three worktrees as CP1, still no sub-agents.",
  "sinceCp1": "Item 5's first two halves (port, prove by violation) are DONE and verified. The remaining halves (repoint, divergence-test every table, retire) are BLOCKED on a real decision, not a technical unknown -- see blocker below. Filing CP2 now rather than waiting, since the port+proof is itself a real, testable milestone and AGENT_CONTRACT wants a mid-work checkpoint, not only a checkpoint at full completion.",
  "gatePort": {
    "action": "Copied gate.ts's full logic from legacy-design-tools into hauska-setback-corpus/src/setbacks/gate.ts, adapted to the corpus package's own table-types.ts (a structurally identical SetbackDistrict/SetbackTable shape -- no type mismatch to reconcile). Added a new rule, G7, enforcing the max_height_ft not_specified sentinel invariant from item 1's RULING 4 (the sentinel must be exactly 999, not a per-table convention) -- this is the 'type that cannot express the bad state' the dispatch asked for, implemented as a machine-checked gate rule rather than a JSON-schema annotation, because schema.json is not validated by anything at runtime (confirmed: no ajv or equivalent anywhere in either repo) and DEV_PROCESS 0 is explicit that an unmechanized convention is not a control. Exported via a new './gate' package subpath (package.json exports map) so a future consumer does not have to pull in the full data package just to run it.",
    "proveByViolation": "New test file src/setbacks/__tests__/gate.test.ts (ported from legacy-design-tools' own setbackGate.test.ts fixtures, so both gates are provably testing the same contract), 9 tests. Two are the literal 'known-bad table' the dispatch asked for, each named for the real incident it reproduces: (1) a table citing atom_did='kyle_tx/ch53/53-33' against an empty atom set -- the EXACT fabricated citation from the Kyle regression -- correctly BLOCKS on G2; (2) a table with max_height_ft=100 flagged not_specified -- the EXACT drift Round Rock and hauska-engine had -- correctly BLOCKS on the new G7. A third confirms the canonical 999 sentinel does NOT block. All 9 pass.",
    "fullVerification": "Corpus package: build (tsc -p tsconfig.build.json) clean, typecheck (tsc --noEmit) clean, lint (eslint --max-warnings=0) clean, test 24/24 (15 setback-tables.test.ts + 9 gate.test.ts)."
  },
  "blocker": {
    "what": "Repointing legacy-design-tools' (or re-verifying hauska-engine's already-repointed) consumer code onto @empressaio/setback-corpus only matters if that package actually contains the Round Rock/Kyle fixes from items 1-2. It does not yet: the real, installed, npm-resolved dependency is still the published 1.0.1, which predates every fix in this dispatch. My local corpus worktree has the fixes; npm does not.",
    "optionsConsidered": "(a) Publish a new corpus version (1.0.2) to npm now, bump both repos' dependency, then repoint -- fastest path to the dispatch's stated acceptance criteria, but `npm publish` is a public, irreversible, external side effect this dispatch's 'do not deploy' language does not explicitly cover either way. (b) Repoint against corpus source directly via a workspace/file: protocol dependency instead of the published package -- avoids publishing but diverges from the exact pattern hauska-engine's own already-merged repoint (PR #399) established, and would need to be undone again once a real version ships, which is its own footgun. (c) Do the repoint code (index.ts rewrite + LDT-side divergence test) now, written and passing against my LOCAL corpus build only (same swap-into-pnpm-store technique used to verify item 3), and leave it uncommitted/unmerged pending the real publish -- lets the code be ready and reviewed without taking the external action myself.",
    "askedDocRepo26": "Sent 2026-09-07 ~16:40 asking specifically whether publishing a new corpus version falls under this sprint's established self-merge precedent or needs confirmation first, given it is an external npm side effect rather than a deploy in the Cloud Run sense this sprint's DEPLOY RULE otherwise governs. Not yet answered as of this checkpoint.",
    "whileWaiting": "Proceeding with option (c): writing the LDT-side repoint (index.ts + a new corpus-divergence test mirroring hauska-engine's, proven against the local corpus build) so it is ready to land the moment the publish question is resolved either way -- this does not require deploying or publishing anything itself, and if the answer is 'repoint against a real published version', the same code lands unchanged once the dependency bumps."
  },
  "constraintsHonored": [
    "no npm publish executed",
    "no deploy, no bake",
    "did not touch lib/cad-ingest (CTX-A), cad-roll paths (CTX-C), or hauska-map",
    "did not edit seat_register.json",
    "every claim above independently re-run (build/typecheck/lint/test all shown passing, not assumed from the diff)"
  ],
  "leave_behind": "declared at close"
}
