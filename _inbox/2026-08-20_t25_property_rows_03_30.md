---
date: 2026-08-20
seat: property substrate
artifact: T-25 enumeration, rows 3 to 30, and the handover that was never filed
status: verified partial, filed retroactively
defect: F-0 — these rows existed only as dispatch prose from 2026-08-19 until this file.
        Same shape as S-21: a measurement whose content survived only outside version
        control. Produced by the enumeration that found S-21.
continues: rows 1-2 in _inbox/2026-08-19_ss-w16_close.json
continued_by: _inbox/2026-08-20_t25_property_rows_31_46.md
---

# T-25 property rows 3 to 30

## Snapshots

All rows read via `git show <sha>:<path>` at: hauska-map `204789f`, hauska-engine
`d3f3794`, legacy-design-tools `1113c649`, doc_repo `cc96276`.

**Evidence grade: READ, not executed.** Read-from-source beats grep and is weaker than
running the code. No row below was verified behaviourally.

## What this test cannot see — read before any row

T-25 enumerates checks that EXIST and asks what they admit. **A check that does not exist
has no row.** It finds weak checks by construction and missing ones only when a reader
remarks on an absence.

Three of the largest findings surfaced that way, not from the method reaching them: the
atom append boundary with no binding validation, the database with no constraint on
`entity_id`, and the geocode builder with no country predicate. **The filter is absent,
not weak.**

State coverage as *the weak checks that exist*, never as *every place the substrate can
accept garbage*. The closing inversion is field-driven: for each field carrying a claim,
ask what validates it, and record NOTHING as a row. Separate pass, not folded in here.

## Derivation states

1. **Available now** — two independent sources (only shape evidencing a source is right),
   or source vs our own derivation (catches our transformation error, never the source's).
2. **Available once a named dependency lands** — name it. **Zero rows in property**, looked
   for explicitly.
3. **Internal consistency only** — both values from one upstream; one party could satisfy
   both sides.
4. **None exists** — the purchasing list.

Not second derivations: a second implementation of the same computation over the same
input; a fallback branch computing a different value inside the same function.

## WRITE PATH — hauska-engine d3f3794

| # | Location | Predicate | Cheapest satisfier | Valid | Derivation state |
|---|---|---|---|---|---|
| W-1 | `parcel-terrain/parcel-geometry-resolver.ts:29` | `/^(\d{5}):([^:\s]+)$/` identifier FORM | `"00000:x"` | No | Available now, two sources: binding vs parcel-node table |
| W-2 | `retrieval/scripts/three-layer-audit.mjs:168` | `body->>'countyFips' <> split_part(entity_id,':',1)` | none | **Yes** | Available now, two sources. **TEMPLATE ROW** |
| W-3 | `flood-hazard-fact/geo.ts:142` | `ringCentroid` = `[sx/n, sy/n]`, vertex mean | any concave parcel | No | Available now, source vs our derivation: ring containment |
| W-4 | `flood-hazard-fact/geo.ts:157-163` | MultiPolygon centroid reads `coordinates[0]` only | any multi-part parcel | No | Available now, source vs our derivation: part count is in the source |
| W-5 | `flood-hazard-fact/geo.ts:167` | `sfhaTf === "T" \|\| "t" \|\| "true"` | **anything else** — `"Y"`, `"1"`, `null`, a schema change | No | **None exists.** A hazard flag that FAILS OPEN |
| W-6 | `storage/src/pg-storage.ts:112` | `if (!candidate.entityType \|\| !candidate.entityId) return null` | any non-empty string | No | Available now, two sources. **Returns null rather than raising** |
| W-7 | `storage/src/property-atom-batch-write.ts:87,116` | insert + `ON CONFLICT ... entity_id = EXCLUDED.entity_id` | any string; **no predicate at all** | No | Available now, two sources |
| W-8 | `storage/src/property-atom-batch-write.ts:50` | `buildAtomDid(entityType, entityId).raw` | any binding — DID inherits it | No | **Internal consistency only.** An identifier derived from a value cannot verify that value |
| W-9 | schema + `drizzle/*.sql` | no FK, CHECK or UNIQUE on `entity_id` | — | No | Available now (a migration, not a source). **UNCONFIRMED — negative claim by text search. Query information_schema before acting** |
| W-10 | `special-district-fact/geo.ts:162` | `geometryCentroid`, vertex mean | any concave district | No | Available now, source vs our derivation |
| W-11 | `utility-easement/geo.ts:14` | private `ringCentroid`, vertex mean | any concave easement | No | Available now, source vs our derivation. Private — not reachable by a shared-export fix |
| W-12 | `well-fact/geo.ts:153` | exists; ring path DIVERGES from flood's | unread | Unknown | **FLAGGED NOT FILED — needs reading.** Do not assert from a name match |

**W-6 is worse than an orphan.** Returning null drops a malformed candidate with no error
and no record — an ABSENT atom, uncounted. Atoms-attempted cannot be reconciled against
atoms-written, so every coverage number rests on a denominator that silently excludes its
own failures.

**Confirmed vs unmeasured:** the APPLICATION layer has no binding validation (W-6, W-7,
read at source). The DATABASE layer is UNMEASURED (W-9). If a foreign key exists, the
finding is substantially smaller.

## RESOLUTION PATH — hauska-map 204789f

| # | Location | Predicate | Cheapest satisfier | Valid | Derivation state |
|---|---|---|---|---|---|
| R-1 | situs address non-null | `IS NOT NULL` | `", ,"` — 1,248,412 parcels | No | Available now, two sources: CAD roll vs `tx_city_boundary` centroid containment |
| R-2 | `situs_city` non-null/non-blank | non-empty string | `","` `"@"` `"????"` (11); `"OFF E US HWY 290"` (1,305, Gillespie) — passes non-null, non-blank AND alphanumeric | No | Available now, two sources |
| R-3 | `baked-facets.ts:430` | `base.includes("/property-atoms")` | any string containing it | No | None exists. **DEAD CODE** — `PE_FACETS_PROXY_BASE` is a const, 0 overriding call sites. Type-expressible |
| R-4 | `atom-chain-to-facets.ts:229` | `env.PROPERTY_ATOM_PATH?.trim() === "1"` | **omission** → falsy → other branch | No | Internal consistency only |
| R-5 | `pe-property-atoms.ts:199-202` | spreads `tier2`, nulls ONLY `envelope` | any tier2 object | No | Internal consistency only |
| R-6 | `api/_lib/pe-geocode-core.ts` `buildPhotonUrl` | q/limit/bias/lang; **no country predicate** | **omission — the filter is absent** | No | Available now, two sources: `countrycode` on the wire feature vs queried viewport |
| R-7 | `api/_lib/pe-site-plan-export-core.ts:194` | `typeof flags.floodZoneHonestUnavailable === 'boolean'` | omission → default | No | Internal consistency only |
| R-8 | same site as R-4 | — | — | — | Duplicate of R-4, confirmed reachable by walker |

## SCORING PATH — legacy-design-tools 1113c649

Carried in from earlier verified work, unchanged: **S-1** column-absence read as
source-absence (fixed, merged); **S-2** `real-at-ceiling` at 0.00%; **S-3** unread
`rule.denominator`; **S-4** `hasWriter` uniform on 3,556 cells; **S-5**
`maxCountiesReachable:1` contradicting its own `sourceBasis`; **S-6** `honestCoveragePct:0`
with null source; **S-7** `areaShare:0`; **S-8** `NaN` carriers; **S-9** Bastrop zoning cell
carrying the ENVELOPE row's measurement. Plus the tier2 tile predicate
(`FEMA_TILE_DEG = 0.005`, `Math.round(lat/deg)*deg`, retired 2026-08-19 — predicate
preserved before removal).

| # | Location | Predicate | Cheapest satisfier | Valid | Derivation state |
|---|---|---|---|---|---|
| S-10 | `countyCoverageScoreCli.ts:400` | `countyNames[fips] ?? LEGACY_FALLBACK[fips] ?? fips` | the FIPS itself — a county named `"48021"` | No | Available now, two sources: roster vs `tx_city_boundary` |
| S-11 | `countyCoverageScoreCli.ts:498` | `total > 0 ? (matched/total)*100 : 0` | `total = 0` → **0%**, not unmeasured | No | Internal consistency only |
| S-12 | `countyGeometryScoreCli.ts:213` | `entity_id?.match(/^([0-9]{5})/)?.[1] ?? null` | any id starting with 5 digits | No | Available now, two sources — this is W-2's check, unenforced here |
| S-13 | `countyGeometryScoreCli.ts:361` | `absenceDetermination.source ?? "honest-absence-determination"` | omission → a literal masquerading as a source name | No | None exists |
| S-14 | `countyCoverageScoreCli.ts:366` | `Number(rows[0]?.n ?? 0) > 0` | no rows → 0 → "source absent" | No | Internal consistency only |
| S-15 | `railScoring/measure.ts:78-79` | `/^[a-z_][a-z0-9_]*$/i.test(value)` → **THROWS** | none | **Yes** | Internal consistency only. **PASS** |
| S-16 | `railScoring/measure.ts:114` | `Number(r.rows[0]?.features ?? 0)` | no rows → 0 | No | Internal consistency only. **The file's own comment at :95-98 says a missing table is "no denominator, not a zero". Comment and code disagree; the comment is right** |
| S-17 | `railScoring/engine.ts:136` | `if (!offered.basis \|\| offered.basis.trim() === "")` | any non-blank string | No | Available now, two sources: basis text vs the artifact it describes |
| S-18 | `railScoring/engine.ts:149` | `absenceProbeCoversCounty(probe, countyFips)` | — refuses | **Yes** | Available now, two sources. **PASS** |
| S-19 | `railScoring/run.ts:169` | `Number(row.honest_coverage_pct ?? 0)` | null column → 0%, indistinguishable from measured zero | No | Internal consistency only |
| S-20 | `railScoring/registry.ts:222` | `denominator: { kind: "none", basis: "no measurement spec yet" }` | the declaration | Honestly no | None exists — correctly declared unmeasurable |
| S-21 | `railScoring/registry.ts:197` | 253 live geometry rows scored against an "accounted features" denominator | — | — | **FLAGGED NOT FILED.** Producer `B2_cp2_geometry_scorer_apply.mjs`: zero commits add it, zero touch it, across four repos and ALL refs. **Never committed, not deleted.** Counting rule survives only as a regex-parsed artifact string. Needs the 152-ref bundle checked, then an operator decision. Wording to preserve: NOT RECOVERABLE FROM ANYTHING REACHABLE HERE, not "gone" |
| S-22 | `railScoring/registry.ts:197` | asserts the denominator is reconstructible from checked-in source | — | **No — the assertion is FALSE and it is in canon** | None exists. Same class as a test asserting a value no authority recognises, one level up |

## The sibling table — non-uniformity is what the reconciliation scopes against

**There is no shared module.** Six copies of the same helper names with divergent bodies.
Same name in several places is evidence of copying, not sharing. A shared-site fix is not
available.

| module | `geometryCentroid` | vertex-mean ring | `isSfhaFlag` |
|---|---|---|---|
| flood-hazard-fact | yes | **YES** | **YES** |
| special-district-fact | yes | **YES** | no |
| well-fact | yes | diverges (W-12) | no |
| utility-easement | no | **YES** (private) | no |
| rail-corridor-fact | no | no | no |
| building-footprint | no | no | no |

W-3 replicates in three of six. W-5 is unique to flood. The `coordinates[0]` pattern
appears in five of six but consequentially in fewer.

## Counts at row 30

```
Available now — two independent sources      13
Available now — source vs our derivation      4
Available once a dependency lands             0   ← looked for explicitly, confirmed zero
Internal consistency only                     9
None exists                                   5
Flagged, not filed                            2   (W-12, S-21)
                                            ----
verified rows                                30
carried in                                   10
type-expressible                              9
CHECKS THAT HOLD                              2   (S-15, S-18 — both REFUSE)
```

**Two checks hold in thirty rows.** Both refuse rather than defaulting. That is the shape
everything else is being converted toward.

**Zero dependency-pending rows means the entire remediable set is buildable now.** Nothing
waits on a source, a feed or another lane. The purchasing list is one line: **NFHL at
parcel resolution rather than tile.**

## Type-expressible subset — cheapest coverage, outside every failure state

R-3 and R-4 (discriminated route selector, exhaustive branch); S-11, S-14, S-16, S-19
(`number | null` rather than `?? 0`); S-13 (required field, no default); W-5 (parsed enum
over the SFHA domain, unrecognised raises); W-6/W-7 (a validated binding type). S-7 and
S-8 were already converted this arc and stand as precedent.

## Paths completed vs remaining at row 30

**Completed:** flood-hazard-fact geo; the three scorer CLIs' guard/default/fallback sites;
the PE resolution and facets-composition path; the atom append path; the six-module
writer-family `geo.ts` surface; `lib/railScoring/*`; PE backends under `api/_lib/`.

**Remaining:** `packages/retrieval` beyond the audit script; **the writer bodies'
atom-construction predicates — the writer family is NOT done, only its geometry helpers
were covered.**

## Standing instrument rules

Graph walker for structural questions — reachability, ownership and identity are not
answerable by text search. Walker at
`artifacts/api-server/scripts/checkBootGraphNoCliImports.mjs`, self-tests both directions,
344 modules from `src/index.ts`.

Mark READ versus VERIFIED. State the snapshot. Distrust your own negative results most.
