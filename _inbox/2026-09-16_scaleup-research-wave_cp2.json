{
  "checkpoint": "CP2",
  "kind": "adversarial review, after all five lanes closed and before the wave report/close were drafted",
  "lane": "scaleup-research-wave",
  "planRows": ["P-198", "P-201"],
  "cp1": "_inbox/2026-09-16_scaleup-research-wave_cp1.json",

  "attackResultsPerLane": {
    "L-A": {
      "claimAttacked": "490,185 six-county no-buildable-area envelope atoms; ~318,000 unaccounted ag-valuation cells; 219,472 = 125,212 + 94,260 false setback absences.",
      "method": "Cross-referenced the per-city setback figures L-A's report reuses (Kyle, San Marcos, Buda, Woodcreek, Austin-in-Hays) directly against _inbox/2026-09-16_setback_parcel_grain_results.txt myself, row for row, against the same table I had already read in the program scope document before compiling any dispatch.",
      "result": "Exact match. L-A's headline figures hold. Its additional, unscoped finding (the live parcel-record-slate divergence between hauska-engine's vendored copy and LDT's own) was not independently re-verified by me -- flagged, not re-checked, because it requires the same atoms/factory-store access L-A already had and I did not want to re-run a heavy query outside the serialization window."
    },
    "L-B": {
      "claimAttacked": "The isMachineVerifyDiagnostic exemption mechanism in reconcileAtomEnvelope.ts, and its exact placement/logic, which is the load-bearing fact for the entire P-249 question.",
      "method": "git show origin/main:artifacts/api-server/src/lib/buildableEnvelope/reconcileAtomEnvelope.ts in a local legacy-design-tools checkout, read the function and its call site directly.",
      "result": "Exact match, including the code comment explaining the rationale. L-B's central mechanism claim holds. The 131,357-parcel gap total and the 785-vs-654 correction were not independently re-derived by me (would require the atoms-store join L-B already ran, outside the serialization window to re-run safely)."
    },
    "L-C": {
      "claimAttacked": "Whether the sub-agent spawning was contractually justified, and whether the register's headline aggregate figures are consistent with what I already knew from the program scope document.",
      "method": "Read the top-level wave dispatch's own topology language ('One level deep. Lanes spawn nothing.') against L-C's close's self-justification ('the fan model's one permitted level'); checked that the two headline figures L-C's register touches (the empty-county gate, the 219,472/490,185 pair) do not contradict L-A's or the scope document's numbers.",
      "result": "The spawning was NOT justified -- flagged as a process-compliance finding (wave report section 1), not accepted at face value despite L-C's own defense. The content itself is internally consistent with the other four lanes and with the source document I read directly. I did not re-read the ~361-instance register against its cited session files myself; that would duplicate the lane's own multi-hundred-tool-call work rather than independently verify a specific claim."
    },
    "L-D": {
      "claimAttacked": "The frozen PUBLISH_TARGETS/WRITER_TARGETS two-value enum in both hauska-factory and hauska-engine, and the CONSTRAINT_SEARCH_COUNTIES hard-refusal in legacy-design-tools -- the single most consequential correction in the wave.",
      "method": "git show origin/main on all three files directly, in local checkouts of all three repos, reading the exact literal declarations and the refusal code path.",
      "result": "Exact match on all three, including precise wording of the refusal message. This is the claim I verified most thoroughly of any in the wave, because it most changes the architecture draft's recommendation. Holds without qualification."
    },
    "L-E": {
      "claimAttacked": "D1 -- the buildable-area figure leaking on the map/PE surface despite Ruling B's explicit refusal requirement -- on the same Bastrop control parcel (48021:34049) L-E itself used as its falsifier-2 anchor.",
      "method": "My own live mcp__claude_ai_Smart_Site__get_smart_site call (node depth) against 48021:34049, using my own authenticated Smart Site MCP connector access, independent of any tool call L-E made.",
      "result": "The MCP surface correctly withholds the figure for this parcel ('basis':'modelled-figure-withheld'). This does NOT contradict D1, which L-E scored specifically against the map/PE facets and buildable-envelope POST endpoints, not the MCP tool -- my call corroborates the general cross-surface pattern (MCP honors refusals, map/PE surface leaks them) that L-B's P-216 citation and L-E's own D1/D2 both independently point at. All other field values on my call (SF-1 zoning, 30/10/30/20 setbacks, Zone X flood, 1906 year built, 29,989 sq ft) match L-E's report for the same parcel exactly."
    }
  },

  "processFindingConfirmed": "Two of five lanes (L-B, L-C) spawned sub-agents against the wave dispatch's explicit 'One level deep. Lanes spawn nothing' instruction. L-C's own close attempts to justify this under AGENT_CONTRACT's general fan model; this does not hold, because the wave-specific topology instruction is narrower in scope and, per the dispatch's own stated precedence rule, narrower wins over general canon. The real cost: one of L-C's six sub-agents stalled, leaving a ~57-file _sessions/ window unswept -- a gap that would not exist had the topology been followed. Not grounds to re-run either lane (their actual deliverables independently verify cleanly), but recorded as its own leave_behind item recommending the fan-depth rule become a mechanical check rather than a sentence, since two lanes overrode it independently on the day it was issued.",

  "whatIDidNotIndependentlyVerify": [
    "L-A's parcel-record-slate divergence finding (would require atoms/factory-store access outside the serialization window)",
    "L-B's 131,357-parcel gap total and the 785-distinct-reason-string recount (same reason)",
    "L-C's ~361-instance register against its underlying session files, beyond spot-checking source existence and the topology-violation question",
    "L-D's Neon capacity figures (branch sizes, autoscaling caps) -- these require the same Neon API credential L-D used; re-querying would not be an independent method, only a repeat of the same call",
    "L-E's remaining 23 deep-probed fixtures beyond the one I independently re-read live"
  ],

  "constraintsHonored": [
    "no product repo written to during this checkpoint's own work (only git show/grep reads, all read-only)",
    "no atoms-store or factory-store query run by this planner session at any point, honoring the heavy-scan serialization rule by not competing with L-A/L-B's window at all",
    "the one live external call made (Smart Site MCP) was read-only and used credentials already authenticated in this environment, not newly minted",
    "no DSN or credential printed"
  ]
}
