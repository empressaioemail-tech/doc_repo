---
id: 2026-08-29_p91_a5_forty_500
title: A5 forty create_screen returned Express HTML 500
date: 2026-08-29
status: diagnosed
plan_row: P-91
wdll: 18
serving_mcp: smartsite-mcp-00049-duw
serving_cortex: cortex-api-00656-vek
---

# What was observed

Connect `create_screen` with the A5 forty list returned an HTML 500 Express default page. No error code. No `query_batch_cap`. `list_screens` had no `A5 forty` row. Failure was clean. Largest existing screen is three rows. A5 is unmeasured, not failed on verbatim.

F5 empty copy is now measured on the same session: `No screen yet` / `Paste addresses in the chat. This panel does not search.`

# Code reading

`create_screen` on product MCP is a passthrough POST to cortex `/api/property-explorer/v1/screens`. MCP `cortexFetch` aborts at 30 s.

On the write path (`peScreenSave.ts` in the isolated LDT tree, the last read of this handler):

- Cap is `CREATE_SCREEN_QUERY_CAP = 50`. Forty is under cap. Over-cap is JSON `{ error: "query_batch_cap" }`, not HTML.
- Resolve fan-out is 8. Per-row timeout is 8 s and writes `unresolved`. It does not cancel the resolver.
- `searchPlaceByPrefix` budget is 20 s. A row can return unresolved at 8 s while the search keeps a DB connection until 20 s.
- `resolver` throw is not caught. `withTimeout` rejects if the search throws before 8 s. `mapPool` then throws. The Express route has no `try/catch`. That is the HTML 500.
- The screen insert runs after all resolves. A throw in resolve never writes. That matches the clean `list_screens` miss.

Rejected: a silent 40-row product cap. The named cap is 50 and it already refuses in JSON.

Rejected: payload too large as the first reading. That is usually 413, and the body is forty short strings.

# Why forty 500s and three does not

Three known queries finish in a couple of seconds. Forty address lines at concurrency 8 can leave in-flight 20 s situs searches after the 8 s row timeout. That is enough to exhaust the pool or throw on a later query. One throw 500s the batch.

# Do not

Do not deploy cortex from this seat. Wave 1 is on another agent and may shift cortex for P-94.

Do not rerun forty. Four known-good already 500d.

Do not add a fourteenth tool. Do not treat this as a missing `create_screen` tool.

# Four-row result

A5 four also returned Express HTML 500. Request log on `cortex-api-00656-vek`:

| time UTC | method | path | status | latency |
| --- | --- | --- | --- | --- |
| 15:45:24 | POST | /screens | 200 | 1.84s |
| 15:58:18 | POST | /screens | 500 | 15.56s |
| 15:58:47 | POST | /screens | 500 | 7.36s |
| 16:12:37 | POST | /screens | 500 | 1.84s |
| 16:12:53 | GET | /screens | 200 | 0.69s |

Cortex traffic is still `00656-vek` @100% tag `p539`. Wave 1 has not shifted it.

The 16:12 four-row 500 is as fast as the earlier 200. That rejects a still-running 20 s search. It does not reject a starved pool that fails checkout fast. GET and `find_parcel` still 200, so reads can get a connection. POST `create_screen` fans resolver work.

Stdout around the 500s has no stack. Express default HTML is still the client shape.

# n=1 result

`A5 one` POST 200 at 16:39:14Z. One row. Pine `48021:34137`. Open painted. Rails unread. Write path is not down.

n=1 200 rejects a dead POST. It does not reject fan-out pressure. n=1 uses one resolver. n=4 uses four. The other chat's "starvation is out" is too strong.

# Bare node id is not the defect

n=1 `48021:34137` is 200. Query and node both `48021:34137`. Open painted.

n=4 of Pine + Rainmaker + that node + zzzz 500d again after n=1 succeeded. Leftover 20 s search is out. Schema has no max on `queries`. Cap is still 50.

n=3 Bastrop walk 200 at 15:45 is stale. It is before the forties. Do not treat three as a live ceiling until it is re-run now.

# Duplicate node, not bare-node-in-batch

n=3 walk replica 200. n=4 with two junk strings and no node id 200. Count >= 4 is out.

The n=4 that 500d was Pine (resolves to `48021:34137`) plus a bare `48021:34137` in the same insert. Schema `pe_screen_rows_screen_node_uidx` is unique on `(screen_id, parcel_node_id)` where the node is not null. Walk-add uses that for idempotency. `create_screen` inserts every resolved row and does not catch the unique violation. Express then emits HTML 500 and the transaction rolls back. That matches the clean miss.

The A5 forty list I filed is poisoned the same way: Pine, CAD Pine, and `48021:34137` all resolve to one node. Rainmaker Cv and Cove both resolve to `48021:8720522`.

Rejected: a node-id resolver bug. n=1 node is 200. n=4 without a second copy of that node is 200.

# Confirm

Name `A5 two same-node`. Predict 500.

```
908 Pine, Bastrop TX
48021:34137
```

Confirmed 500. Same mechanism as the poisoned forty.

# Punch (in-tree, not serving)

`createScreen` now refuses before insert when two planned rows share a non-null `parcelNodeId`. Shape: `{ error: "duplicate_resolved_node", node, queries }`. Unique-violation 23505 maps to the same error without a node. Express already returns that as JSON 400.

Written in isolated `P:/tmp/legacy-design-tools-p91-stone` `peScreenSave.ts`. Not deployed. Cortex stays `00656-vek` / p539. Wave 1 still owns cortex deploys.

Unique forty list: `_inbox/2026-08-29_p91_a5_forty_unique.md`. The first prompt file is poisoned. Do not paste it again.
