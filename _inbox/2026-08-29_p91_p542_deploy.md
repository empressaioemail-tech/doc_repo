---
id: 2026-08-29_p91_p542_deploy
title: p542 serving at 100% on cortex-api-00666-cuf
date: 2026-08-29
status: serving-100pct
plan_row: P-91
service: cortex-api
revision: cortex-api-00666-cuf
tag: p542
digest: sha256:da92e63a23054c68cf0e7ab22e627c41db810f31040c71536edbee8ff34904a8
tree: P:/tmp/legacy-design-tools-p91-cortex-leftover feat/p91-cortex-leftovers 1413936c
build: Cloud Build 25645912-3d25-4015-b1f3-52280b57d9a1 SUCCESS (project legacy-design-tools-prod, config cloudbuild.p542.yaml, root Dockerfile)
pr: empressaioemail-tech/legacy-design-tools #550
---

# Snapshot before this shift

`cortex-api-00664-hib` at 100% tag `p541` (digest `a93eb092...`), the leftover existence hunks. Staging `00646-luj` untouched at 0%.

# Deploy

Built from the leftover clone at `1413936c` (commits: `fca62463` p541 record plus fail-closed cut, `6351abaa` merge main, `7ab60a1c` shared resolve-core test, `1413936c` rails at first paint). Digest read from Artifact Registry `image_summary.digest` by field name.

`gcloud run deploy cortex-api --image ...@sha256:da92e63a... --no-traffic --tag p542`. Existing service config carried (runtime SA `api-server-runtime@`, minScale 1, secrets by `:latest` reference, no `--set-env-vars`, no migration).

Tag `https://p542---cortex-api-tds7av26va-uc.a.run.app/api/healthz` answered `{"status":"ok"}`. Shift `--to-tags=p542=100`. Prod `/api/healthz` `{"status":"ok"}`.

Traffic after shift, by field name from `gcloud run services describe --format=json`:

```
latestReady= cortex-api-00666-cuf
  revisionName= cortex-api-00664-hib percent= undefined tag= p541
  revisionName= cortex-api-00666-cuf percent= 100 tag= p542
```

Revision `cortex-api-00666-cuf` `status.imageDigest` equals the Artifact Registry digest above.

# After the shift

Branch commit `a8cf3ef2` (after the image) moves the brief-miss probe from a direct `txgioAddressResolve` import to the `cortexNodeLookup()` seam so the two records-request route suites collect on CI. The route behaviour is identical (same function, same arguments); the serving image `1413936c` is not rebuilt for it. The merge SHA of #550 is the SHA of record.

# What is on this image

A screen existence lookup that throws refuses (`lookup_unavailable`, 503) on both `add_to_screen` and `create_screen` and writes nothing; the resolve core takes `lookup` as a required argument. `add_to_screen` returns the existing row on a unique-index race. `research/brief` single-id miss probes existence: 404 `parcel_not_found` versus 404 `baked_snapshot_not_found`, 503 when the probe throws. `lookupParcelNodeForScreen` matches `ltrim(prop_id,'0')` as well as the raw id. `create_screen` declares a resolver timeout on the row and the screen. One JSON error boundary on `/api`. Resolved screen rows on POST /screens and GET /screens/:id carry `stub` and `stubRead`; the screen carries `stubsDegraded` when any row is not `ok`; a bake miss is `unknown`, never `unread`.

# Grade

Live probes through the planner's own connector recorded in the close artifact. Operator Connect walk: `_inbox/2026-08-29_p91_p556_connect_grade_prompt.md`.
