# G-11 city-pack tenancy

WDLL `_inbox/2026-08-17_g11_tenancy_WDLL.md` (approved 2026-08-17). Decision `_decisions/2026-08-17_g11_city_pack_tenancy.md`. Cite WDLL items on every PR.

## Mission premise

Operator approved G-11 2026-08-17. A city pack is the tenant. Identified caller is a Hauska product key whose `jurisdiction_tenant` equals `cityKey`. `DASHBOARDS_API_KEY` is not a tenant. Fixture pack `fixture-city` is tenant-private, grants empty. Not sprint-54 done. Not live ingest. Not Bastrop.

## Completion predicate

Live unauth GET `/api/city-packs/fixture-city` is 401. Live Bearer `DASHBOARDS_API_KEY` does not unlock `fixture-city`. Identified `X-Hauska-Key` with `jurisdiction_tenant=fixture-city` returns that pack, `grantedAdapters` []. Wrong tenant is 403 or MCP isError. Serving MCP identified `dashboards_get_city_pack` for `fixture-city` returns the pack; anon remains refused. Unauth compose on `48021:34137` still omits `owner-fact` and omits tenant-private types. City `00118-qox` unchanged. Probe keys revoked. Planner owns merge, deploy, live probe.

## Scope basis

G-11 only. City-pack tenancy on Dashboards HTTP plus existing Hauska MCP. Not atoms `--apply`. Not live MyGov/Samsara. Not G-24 fill. Not sprint-54 T2/T3. Not Cortex default tenant. Not claim flow. Not smartcity-os. Not L26.

## Workers (no deploy, no merge)

1. Dashboards (WDLL 3, 4, 5, 7, 8): isolated worktree from origin/main. `fixture-city` pack, accessPolicy tenant-private, grantedAdapters []. Service Bearer cannot read it. X-Hauska-Key resolved via MCP GET `/auth/whoami` (or injected resolver in tests). Compose accessPolicy filter: public-free for anon/service; tenant-private only for matching pack subject. Tests cover service-key refuse, cross-pack refuse, tenant-private omit. No vendor secrets. No Bastrop pack. PR.
2. MCP (WDLL 6, 5, 7): isolated worktree from origin/main. GET `/auth/whoami` returns jurisdiction_tenant, no key material. `dashboards_get_city_pack` requires identified caller; private pack requires `jurisdiction_tenant === cityKey`; forward `X-Hauska-Key` to Dashboards; wrong tenant isError. Anon still refused. No second MCP. CI four-set union. PR.

## Invariants

P:\smartcity-os no-touch. L26 holds atoms writer. G-60 STOP. One Hauska MCP. Deploys planner-owned. Verification stays with the lane planner. Probe keys revoked at close.

CHECKPOINTS AND CLOSE (G-63 already occupies 2026-08-17_b_*; use these paths):
  CP1: _inbox/2026-08-17_g11_cp1.json
  CP2: _inbox/2026-08-17_g11_cp2.json
  CLOSE: _inbox/2026-08-17_g11_close.json
