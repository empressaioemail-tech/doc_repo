---
id: 2026-08-25_completion_strip_handoff
title: Handoff — commit + canvas completion strip (not a DELETE go)
date: 2026-08-25
status: filed
plan_row: P-25
from: integration reviewer
to: in-flight property planner
---

# Completion strip

Corrections on keep-or-drop and the three-option restamp are accepted. `dae2006` is still not operator go. Do not DELETE. Do not build `baseline_set`. Do not rebake.

Paste below the line.

---

STOP still holds on Tarrant writes. This card is commit + board honesty so a later agent can close the program without executing the leftover DELETE row.

Snapshot after you pull: your uncommitted tree on `dae2006` parent. Integration `P:/doc_repo` is merge target. Commit by pathspec only.

## 1. Commit these, nothing else

```
_inbox/2026-08-24_parcel_facts_write_path_WDLL.md
_inbox/2026-08-25_p25_tarrant_keep_or_drop.md
_inbox/2026-08-25_store_honesty_wave_close.json
_inbox/2026-08-25_p78_leftover_dryrun_caldwell_48055.json
scripts/p78-leftover-dryrun-caldwell.mjs
```

Do not add `_inbox/2026-08-25_get_back_on_track_handoff.md`. Do not add this handoff unless you already have it. Subject: `docs(P-25): Tarrant three options + Caldwell leftover dry-run`.

After commit, restamp canvas SNAPSHOT to that SHA (not `11763c0`, not `dae2006` alone).

## 2. Canvas completion strip (all five family boards)

The three-option caption is good. These rows are still wrong or missing.

**Write-path leave_behind** still says `Dry-run DELETE vs Aug-14 baseline; operator go`. Kill that. Replace with: three options filed; identification incomplete; no DELETE; Nick picks KEEP or DROP-to-L21; DROP-to-log refused.

**WDLL item 3** is not `held / isolated PE tree`. Serve review: sentinel is on `#222` `9224a73`. Street title is unmet (`Parcel 280239` because txgio situs is null). Grade **partial**. Constraint `A2 PE tree` is stale. Do not open a new hauska-map tree for P-74 on this card.

**WDLL item 9** dry-run JSON is now on disk (Caldwell 48055 parse-only). Check is parser + dry-run artifact. You may grade **met** on that check only after you explain why every `landAcresParsed` sample has `propId: "0"`. If that is a script bug, item 9 stays in flight. Rebake stays leave_behind, not the grade. Do not run `stratmap-landuse` upsert.

**WDLL item 10** stays skip / three options. Not a DELETE go.

**WDLL item 12** stays open until this commit + leave_behind table matches the three options.

**Parked, named on write-path, not started:**

- landUseFact `no-cad-row` vs structural `lookup-failed` on 280238
- P-74 street bake / 280239 title
- 280238 unplaceable overlay hiding CAD miss
- P-80 / P-79 / P-09 / COVER
- Manifest rematerialize (dump GET refresh is allowed; cells do not move)

**Recalibration:** do not execute peel / envelope / other-lane yellow rows from that board. Lane 3 is a pointer to write-path. Who-serves is not a rail.

**Manifest:** if you refresh, `node scripts/county-manifest-canvas-dump.mjs --live` then paste DATA. Do not rematerialize. 667/3556 may stand. Do not label a 04:13Z dump FRESH.

**Texas-done for this program** is WDLL 3 partial recorded, 8 met, 9 dry-run (or honest in-flight if propId 0 is a bug), 10 skip-amended, 12 close. It is not 3556/3556.

## 3. Do not start

DELETE. baseline_set export. extra-row samples. tad.org. stratmap-landuse write. P-80 P-79 P-09 COVER. `--apply`. rematerialize. L17 write. pin raise. new PE tree. land-use-fact rewrite. 280238 geometry seed.

Tarrant pick is Nick's. Until he names KEEP or DROP-to-L21, leave `decision: null`. DROP-to-log stays refused.

## Done

Pathspec commit on origin/main. Five canvases match that SHA. Leave-behind has no Aug-14 DELETE go. Item 3 partial. Item 9 met or in-flight with the propId 0 note. Pin `--check` PASS. Close JSON `decision` still null.
