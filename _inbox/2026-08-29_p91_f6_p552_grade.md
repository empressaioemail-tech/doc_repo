---
id: 2026-08-29_p91_f6_p552_grade
title: F6 step 2 FAIL on p552, isError never reached the iframe
date: 2026-08-29
status: fail
plan_row: P-91
serving: smartsite-mcp p552
plant: 48021:900099
---

# Score

Fail. Fresh p552 board had no fail line. Open on `48021:900099` ran `get_smart_site`. Same `baked_snapshot_not_found`. Board then painted `Open did not reach me`.

# Mechanism

The host thought 16s. The 12s timer painted silence and cleared the wait. The 404 stayed in the chat as `isError`. The iframe never `accept()`ed it. p552 only helps when `isError` arrives on `ui/message`. This host does not deliver that shape for a tool error.

Rejected alternative: old iframe. The gate board in this chat had no fail line before Open.

# Next

Return a bake miss as a normal tool result (`isError` false) so the host forwards it like a gold Open. `accept()` then paints `Not on file in Bastrop` even if the timer already fired. URI `app-p553.html`.
