---
id: 2026-08-29_p91_a5_forty_grade
title: A5 unique forty Connect grade
date: 2026-08-29
status: met
plan_row: P-91
wdll: [18, 30]
serving_mcp: smartsite-mcp-00049-duw
serving_cortex: cortex-api-00656-vek
screen: A5 forty unique
---

# Snapshot

Connect `create_screen` 200 on the unique list. Cortex still `00656-vek` / p539. Product MCP still p550. This grade is the other chat's tool JSON plus the board frame. Traffic was not re-read.

# Scores

| Item | Score | Evidence |
| --- | --- | --- |
| 18 create_screen A5 | MET | 40 rows, ordinals 0-39. Pine `48021:34137`. Rainmaker `48021:8720522`. Eight real node ids and 24 synthetic fillers wrote as distinct rows. No unique-index 500. Last six unresolved, null node, no Open, slot `Nothing to open until this resolves`. |
| 30 A14 verbatim | MET | Last six query strings match the paste, including the trailing digit runs. Live unresolved rows now exist. |

# Rejected fail

A screenshot reading of `www-not-a-situs-66666` (three w) against the paste `wwww-not-a-situs-66666` (four w). Vision collapses repeated letters. The tool JSON comparison is the second derivation. Do not fail A14 on the frame OCR.

# Leftover, not a fail

All 24 `48021:90000x` rows came back `resolved`. That means the string parsed as a node id, not that a parcel exists. The board paints Open. A fat-fingered node id gets the same treatment. Item 18 asked for forty rows and six typed unresolved strings. It did not ask node ids to be existence-checked.

Saved-list isolation on this screen was not listed. Item 20 stays ungraded live. A12 already held on an earlier screen.

# Close

Do not rerun the poisoned first forty. Punch `duplicate_resolved_node` stays in-tree, not serving. Wave 1 still owns cortex. Next leftover on this card is A13 walk-add on `48021:34169`, or the node-id existence leftover if Nick wants that first.
