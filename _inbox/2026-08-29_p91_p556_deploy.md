---
id: 2026-08-29_p91_p556_deploy
title: p556 serving at 100% on smartsite-mcp-00061-zik
date: 2026-08-29
status: serving-100pct
plan_row: P-91
service: smartsite-mcp
revision: smartsite-mcp-00061-zik
tag: p556
digest: sha256:3de92afc156dba76182f36c2bf49c4d261921d54e9f5bfa603c0eff70fc4f559
uri: ui://smartsite/app-p556.html
tree: P:/tmp/legacy-design-tools-p91-stone feat/p91-wave-h-stone 66b543bb
build: Cloud Build 54e5a6b4-9ea1-4971-a5d8-f7a266963c59 SUCCESS (project legacy-design-tools-prod, config artifacts/smartsite-mcp/cloudbuild.p556.yaml)
pr: empressaioemail-tech/legacy-design-tools #551
supersedes: _inbox/2026-08-29_p91_p555_canary_deploy.md (p555 served at 100% for the interval between the two shifts)
---

# Why a p556 minutes after p555

A live probe through the planner's own connector on p555/p542 showed the batch `get_smart_site depth stub` result carrying each parcel's rails flat on the parcel record. The build plan's 4.2 had named a nested `stub` (planner error); the p555 parser read only that, so a batch board would have painted six `unread` per row. S1b reads `stub` when present, else the flat record; the verbatim live JSON is the fixture in both iframe suites and was observed failing first. Everything else on p556 is p555.

# Deploy

Built from the stone clone at `66b543bb`. Digest read from Artifact Registry `image_summary.digest` by field name. `gcloud run deploy smartsite-mcp --image ...@sha256:3de92afc... --no-traffic --tag p556`; existing service config carried; no `--set-env-vars`.

Tag `https://p556---smartsite-mcp-tds7av26va-uc.a.run.app/health`:

```
{"status":"ok","service":"smartsite-mcp","name":"Smart Site","version":"0.0.1","authConfigured":true,"cortexConfigured":true,"revision":"smartsite-mcp-00061-zik","failureDomain":"smartsite-mcp"}
```

Shift `--to-tags=p556=100`. Traffic after, by field name:

```
latestReady= smartsite-mcp-00061-zik
  revisionName= smartsite-mcp-00059-jab percent= undefined tag= p555
  revisionName= smartsite-mcp-00061-zik percent= 100 tag= p556
```

Prod `/health` names `smartsite-mcp-00061-zik`. Revision `status.imageDigest` equals the Artifact Registry digest above. Cortex stays `cortex-api-00666-cuf` / p542.

# Live wire on this pair (planner's connector, 2026-08-30)

`get_smart_site 48021:900099 depth node` -> `{"parcels":[],"notFound":["48021:900099"],"reason":"parcel_not_found","parcelExists":false}`.
`ask_the_map 48021:34137` -> `{"status":"not_ready","tool":"ask_the_map","reason":"P-91 item 34: ..."}`.
`create_screen` (three walk lines) -> resolved rows carry `stub` and `stubRead: "ok"`; `list_screens(screenId)` returns the same on read; screen `55b7067e-beb5-475c-888c-bc4741d19a9b` on the planner's account.
Batch stub on three ids -> two parcels with flat rails plus `notFound: ["48021:900099"]`; the exported parser now yields the six real states for each parcel.

# Grade

`_inbox/2026-08-29_p91_p556_connect_grade_prompt.md`. The panel paints (board line before and after Send, the sentence under the tool row, the batch board, O1 boot strip) are the operator's to observe; the wire half is recorded above.
