{
  "lane": "p228-report-chrome",
  "planRows": ["P-228"],
  "status": "closed-partial",
  "repo": "hauska-engine",
  "snapshot": {
    "docRepo": { "worktree": "P:/seat-worktrees/p228-report-chrome/doc_repo", "branch": "lane/p228-report-chrome", "commit": "134fd5772a90cc6cafff5c3c15c5f6aeb79d8b45" },
    "hauskaEngine": {
      "worktree": "P:/seat-worktrees/p228-report-chrome/hauska-engine",
      "branch": "feat/p228-report-chrome",
      "baseCommit": "4237c0bac6fca0e6de93bcb9c71b985da2749b95",
      "commit": "767a5cd0f800ef768a4fca41a382b071817d9fd4",
      "pr": "https://github.com/empressaioemail-tech/hauska-engine/pull/451",
      "ciConclusion": "SUCCESS",
      "ciCheckName": "typecheck + test",
      "ciRunUrl": "https://github.com/empressaioemail-tech/hauska-engine/actions/runs/35013963728/job/104532595775",
      "ciDuration": "3m31s",
      "merged": false,
      "prState": "OPEN, MERGEABLE -- left open per explicit dispatch instruction ('Do not deploy or merge. Open the PR green and hand it back.')"
    }
  },
  "deepLinkHost": {
    "chosen": "smartsite.cloud",
    "verifiedBy": "served content, not status code -- curl -s -D - both hosts, 2026-09-15T19:24Z: smartsite.app returns HTTP 200, Content-Length 114, Server header ABSENT, body is exactly `<!DOCTYPE html><html><head><script>window.onload=function(){window.location.href=\"/lander\"}</script></head></html>` (a parked-domain redirect stub -- reconfirms the integration seat's 2026-09-15 finding, independently, live, right now). smartsite.cloud returns HTTP 200, Content-Length 1856, Server: Vercel, real application bundle (title 'Smart Site — Explore your property', full CSP, Vercel cache headers).",
    "decision": "Absolute link, always. Caller-supplied liveViewUrl (share tokens etc.) prints verbatim, host-corrected only if it literally contains the parked smartsite.app host (defensive; zero current callers do this). When no liveViewUrl is supplied -- which was ALWAYS true for the standalone site-plan export before this row, since it never threaded one through at all -- the frame now falls back to an absolute https://smartsite.cloud/?parcelNodeId=<id> link built from the model's own id, so every sheet's footer always carries a working, resolvable deep link (report-chrome README integration note 3's own instruction to make this decision explicit rather than silently keeping the pre-existing relative form)."
  },
  "measuredSheetDimensions": "PAGE_WIDTH=612pt / PAGE_HEIGHT=792pt in packages/engine-core/src/site-plan/pdf/page-geometry.ts, unchanged by this row -- the pdf-lib equivalent of the CSS spec's 816x1056px @96dpi (612*96/72=816, 792*96/72=1056). This repo draws PDF points directly with pdf-lib; there is no CSS box model and so no literal box-sizing:border-box/content-box distinction to trip on, but the equivalent trap (margins added ON TOP of the declared page size rather than inset from it) does not recur: every drawingBox/frame computation in render.ts/dossier.ts/feasibility.ts/flood-drainage.ts insets from PAGE_WIDTH/PAGE_HEIGHT via MARGIN_*, never adds to them.",
  "falsifier": "All four dispatch-registered falsifiers run for real: (1) measured PAGE_WIDTH/PAGE_HEIGHT directly from the constant, unchanged at 612/792pt -- see measuredSheetDimensions above; the equivalent-trap check (margins inset from, not added atop) verified by code inspection of every frame/drawingBox call site touched by this row. (2) fetched both candidate hosts live via curl and read the BODY, not the status code -- see deepLinkHost above; smartsite.app would have passed a naive status check (HTTP 200) exactly as the spec warned, and its body proves it is still the 114-byte parked stub. (3) generated a REAL 10-sheet Feasibility PDF via authorParcelFeasibilityExport (production code path, not a vitest mock) and decoded every footer counter present: 01/10 through 10/10, all correct, none stale, none missing -- did not literally reach the dispatch's illustrative '13-sheet' count with this fixture, reported honestly rather than rounded to fit the example; the SAME invariant (a counter genuinely bound to the real total, not hardcoded) is independently reproven for an even larger real overflow case by the existing overflow-pagination.test.ts gate (n>3, every sheet checked against the true total, not just the last one) -- see CP2 for the exact assertion. (4) ran the full pre-existing engine-core test suite (1657 tests) after every source edit: zero drawing-value, legend-label, scale-bar-value, KV-grid, provenance-table, brief-fact, narrative-text, or honesty/absence-chip assertion needed changing -- every one of the ~20 updated assertions across 10 test files is a masthead/running-header/footer STRING (the deliberate copy/format change this row exists to make) or the Y-position shift the new shorter footer band causes, never a body value.",
  "contradicted": "CP1 named pdf-lib's built-in StandardFonts (Courier/Courier-Bold) as the mono-element substitution. This was wrong and self-falsified mid-build: the full test-suite run showed every mono string (subject meta, doc id, the ENTIRE footer) silently absent from decoded PDF text, traced to StandardFonts carrying no ToUnicode CMap while this system's own verification method remaps text through each embedded font's beginbfchar block. Corrected to alias mono/monoBold to the already-embedded, already-ToUnicode-mapped Barlow Regular/Medium -- see CP2's 'corrected_mid_build' for the full account. This is exactly the kind of finding DEV_PROCESS 3.2 rewards reporting rather than hiding.",
  "leave_behind": [
    "Wordmark letterforms substitute Barlow Condensed SemiBold for the spec SVG's Oxygen (not vendored in this repo; the SVG's own font-family stack already falls back to a system sans, which this honors). If pixel-exact Oxygen rendering is ever required, vendor the OFL font and embed it via fontkit the same way the other four faces are.",
    "Frame mono elements (subject meta, doc id, running-header meta, footer meta) use Barlow Regular/Medium, not a literal monospace face, for the ToUnicode reason in `contradicted` above. Digit/character alignment across sheets is not pixel-perfect the way a real monospace face would give. If genuine monospacing is ever required, vendor an OFL monospace TTF and embed it via fontkit (NOT pdf-lib's StandardFonts, which this row proved incompatible with this repo's own PDF-text verification method).",
    "The report-chrome README's own suggestion to name the footer's off-ramp 9.5px value `--ss-fs-print-fine` in a dedicated print-tokens.css was followed in spirit (report-chrome-tokens.ts's FOOTER.legalSize/metaSize) but the literal CSS custom-property naming convention does not apply to a TS module -- named as ordinary token-object fields instead.",
    "No surface-probe.mjs row exists for P-228 (or for any hauska-engine PDF-chrome check) -- named, not built. hauska-engine has no deploy workflow (this lane's own dispatch names this as a known trap, and P-219/P-221's closes independently confirm it), so a live-surface probe could not usefully PASS today regardless of whether a row existed: the served PDFs are generated by whatever code is actually deployed, and this PR is not merged, let alone deployed.",
    "The 4 pre-existing scripts/__tests__/*.test.ts failures (cert-grade-and-report, detect-placeholder-setback-citation, preflight-and-report, retire-road-class-setback-table) were found but not fixed -- out of this lane's scope (a scripts/ SyntaxError-at-collection unrelated to report-chrome) and not introduced by this diff (confirmed via `git diff --stat origin/main -- scripts/__tests__/` returning empty). Diagnosed, not merely noted: `core.autocrlf=true` on this Windows seat checks these specific files out with CRLF line endings, which some ESM/vitest collection path chokes on; the underlying .mjs scripts CI actually gates on (run directly via `node ...mjs`, not through vitest) both pass cleanly with exit 0 when run directly, and CI's own history on main (ubuntu-latest, LF checkout) is green through the commit this branch bases on -- AGENT_CONTRACT section 5 already names this exact class as a known artifact ('Windows-local CRLF test failures are a known artifact; CI is authoritative').",
    "This PR is OPEN, not merged, per explicit dispatch instruction ('Do not deploy or merge. Open the PR green and hand it back.') -- a deliberate deviation from this repo's usual self-merge-on-green-CI norm (P-219/P-221 precedent), followed here because the dispatch says so explicitly, which wins over the general norm.",
    "hauska-engine has no deploy workflow. Merging this PR would ship nothing on its own; a manual Cloud Build (or whatever mechanism the integration seat uses) is required before ANY customer-facing PDF reflects this change, merged or not."
  ],
  "missionPremise": "report-chrome README (doc_repo _design/report-chrome/, committed 2026-09-15, design reviewed and approved 2026-09-15): Option 1a 'Rule' is the one page frame -- masthead, running header, footer -- for Feasibility Study, Site Plan and Flood & Drainage, replacing three previously mutually-inconsistent chrome treatments (confirmed by direct code read before any edit: dossier.ts's kicker-eyebrow style, render.ts's own condensed 'SITE PLAN SHEET STANDARD' eyebrow, and flood-drainage.ts's near-duplicate of the same -- visibly different even when they appear back-to-back inside one combined Feasibility document, which is exactly the class of defect this mission exists to remove).",
  "completionPredicate": "Every sheet across all four PDF assemblers (render.ts's site-plan sheets, dossier.ts's Property X-Ray, feasibility.ts's Feasibility Study, flood-drainage.ts's Flood & Drainage) draws the masthead iff it is the document's printed sheet 1 and the running header otherwise, both from the one shared report-chrome.ts module; every sheet's footer carries a legal disclaimer, a generation timestamp, a resolved (never-parked) deep link, and a zero-padded sheet counter genuinely bound to the real page count; sheet-body content (drawing, legend, scale bar, tables, narrative, honesty chips) is provably unchanged (the pre-existing 1657-test suite passes with only frame-copy/geometry assertions updated); all four dispatch falsifiers run for real, not described. Met at the code level, verified above. The CUSTOMER-level predicate (a live probe on the deployed surface) is out of reach for this row specifically -- hauska-engine has no deploy workflow at all, so no merge, however green, would make ANY of this live -- which is why status is closed-partial rather than closed, matching P-219/P-221's own precedent for the identical reason.",
  "scopeBasis": "Scoped to the page FRAME only (masthead, running header, footer) per the dispatch's explicit boundary, across all four PDF assemblers rather than only the three report types literally named in the mission title. Deliberate inclusion, not scope creep: dossier.ts's drawDossierHeader was the SAME shared primitive feasibility.ts already called for its own FS- pages, so leaving the X-Ray product on the old chrome while upgrading Feasibility would have reintroduced the identical cross-document inconsistency defect this mission exists to fix, just relocated. render.ts's own site-plan sheets were included for the identical reason (embedded inside Feasibility, they must match its new chrome, and the mission names 'Site Plan' as one of the three explicit targets regardless). Explicitly NOT touched: the drawing itself, legend rows/order/labels, scale bar, north arrow, contour/street/setback rendering, segment table, provenance panel, brief-facts grid, KV rows, narrative text, buildable-area figures, honesty/absence chip logic -- every one of these is body content whose POSITION may shift (a shorter footer/masthead frees or costs vertical room) but whose VALUES, LABELS and ORDER do not, verified by the full pre-existing test suite passing unchanged except for frame-copy assertions. LOT/BUILDABLE/ZONING and CATCHMENT/PONDING/FLOW EXITS moved from inside the old header to a body-furniture row directly below the new chrome -- a repositioning, not a content change, since the new masthead/running header has no stats slot the old header did.",
  "constraintsHonored": [
    "Did not change sheet body content -- verified by the full pre-existing test suite passing with only frame-copy/geometry assertions updated, each one quoted and reasoned in CP2 and in the commit message.",
    "Did not deploy or merge -- PR #451 opened, left open per explicit instruction; hauska-engine has no deploy workflow and none was invoked.",
    "Did not spawn a sub-agent that itself spawns sub-agents -- the one flood-drainage.ts sub-agent's prompt carried the do-not-spawn clause and it did not spawn further; its full diff was adversarially reviewed (CP2) and one real issue found (a locally-duplicated shared symbol) was fixed by exporting the shared symbol from render.ts instead.",
    "Did not edit assets/logo.svg -- the two-gold ruling (ss-gold ring dot / ss-print-gold wordmark letterforms) is honored via report-chrome-tokens.ts's PRINT.gold/PRINT.printGold, drawn as separate pdf-lib primitives; assets/logo.svg itself was never opened for write.",
    "Did not add a gold rule, gold sheet counter, or gold accent anywhere outside the wordmark letterforms and the ring dot.",
    "Ported the print tokens into a dedicated module (report-chrome-tokens.ts) rather than inlining hexes, per the spec's own instruction -- the TS-module equivalent of 'print-tokens.css alongside pe-tokens.css' given this repo has no CSS pipeline at all."
  ],
  "leaseHeld": "None (no atoms-writer lease, no traffic-shift lease -- code-only lane, no writer/traffic action taken, no deploy invoked).",
  "probe": {
    "notApplicable": "hauska-engine has no deploy workflow at all (named as a known trap in this lane's own dispatch, independently confirmed by P-219's and P-221's closes for the same repo); this PR is not even merged, let alone deployed, per explicit dispatch instruction. No live surface exists for this row's code to be measured against today regardless of whether a scripts/surface-probe.mjs row were built for it, and none currently is. P-228 falls in OPS-24's PREDICATE_DEBT range (200-250 per probe-close-gate.mjs's current OPS24_RANGES, read directly from the enforcement script at close time), so this declaration is accepted by the gate rather than blocked; the predicate debt is named here, not hidden."
  },
  "laneClaim": { "seat": "cente-p228", "claimedVia": "node scripts/lane-claim.mjs claim --lane p228-report-chrome --seat cente-p228 --plan-row P-228, exit 0", "released": true }
}
