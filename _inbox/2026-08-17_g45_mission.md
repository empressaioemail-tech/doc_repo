# G-45 SmartSite staff map

WDLL `_inbox/2026-08-17_g45_smartsite_staff_map_WDLL.md` (approved 2026-08-17). Decision `_decisions/2026-08-17_g45_smartsite_staff_map.md`. Cite WDLL items on every PR.

## Mission premise

Operator approved G-45 2026-08-17. Dashboards staff map is the SmartSite embed of gold `48021:34137`. GET `/` auto-composes that parcel so a grader sees 908 PINE without a click. Query `?parcelNodeId=` may override. Do not cut live Leaflet. Do not clone PE. No second MCP.

## Completion predicate

Unauthenticated GET serving Dashboards `/` auto-loads `48021:34137` / `template-city`. Iframe `src` is `https://smartsite.cloud/?parcelNodeId=48021%3A34137` (encoding-normalized). Browser shows 908 PINE / APN 34137. Served `app.js` has zero `leaflet`. Anonymous MCP `dashboards_compose_city_manager` returns the same embed URL. City `00118-qox` unchanged. Planner owns merge, deploy, live probe.

## Scope basis

G-45 only. Dashboards staff-map default on existing compose + SmartSite embed. hauska-map only if live iframe is frame-blocked (as-found: not blocked). Not atoms `--apply`. Not live Leaflet cut. Not PermitFlow. Not G-24. Not smartcity-os. Not L26.

## Workers (no deploy, no merge)

1. Dashboards (WDLL 2, 3, 4, 6, 7): isolated worktree from origin/main. Auto-compose gold parcel on GET `/`. Query override. Unit test for default and override. Shape test still refuses leaflet. No parcel table. PR.

## Invariants

P:\smartcity-os no-touch. L26 holds atoms writer (idle; do not take the slot). G-60 STOP. One Hauska MCP. Deploys planner-owned. Verification stays with the lane planner.

CHECKPOINTS AND CLOSE (2026-08-17_b_* occupied by G-61; use these paths):
  CP1: _inbox/2026-08-17_g45_cp1.json
  CP2: _inbox/2026-08-17_g45_cp2.json
  CLOSE: _inbox/2026-08-17_g45_close.json
