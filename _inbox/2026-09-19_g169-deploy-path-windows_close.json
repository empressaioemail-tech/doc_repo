{
  "lane": "g169-deploy-path-windows",
  "planRows": ["G-169"],
  "status": "closed",
  "seat": "cente-vsc-g169",
  "filedAt": "2026-09-19T16:25:00Z",
  "kind": "CLOSE -- the deploy path's spawn is repaired so it can run its own tools on Windows, with the argv-array property kept at full strength and every failure given a KIND. Both repos, one PR each, every check-run conclusion SUCCESS. No deploy, no traffic shift, no merge, no sub-agent.",

  "probe": {
    "notApplicable": "read-only lane: NOTHING IS DEPLOYED by this lane, so no customer surface moved and a surface-probe artifact would measure nothing about this row. The dispatch itself forbids the deploy that would move a surface ('do not deploy production to prove this'), so this is not a lane choosing an easy posture: the completion predicate is a spawn-path measurement plus a self-test proven by violation, both of which are recorded verbatim in CP2, and OPS-24 Law 1's predicate debt for G-169 is unchanged until the integration seat merges and the deploy path's first real green run happens."
  },

  "prs": {
    "plan-review": { "number": 20, "url": "https://github.com/empressaioemail-tech/plan-review/pull/20", "base": "origin/main 4d3da57", "headCommit": "664e4b3312fedbb05f27d33ee902d6f14b47c453", "state": "OPEN, MERGEABLE, mergeStateStatus CLEAN", "ciConclusionStrings": ["test: COMPLETED / SUCCESS"], "mergedSha": "notApplicable: the merge is the integration seat's per the file header this row repairs" },
    "smart-files": { "number": 19, "url": "https://github.com/empressaioemail-tech/smart-files/pull/19", "base": "origin/main 5073f7f", "headCommit": "f35be5300133b63ffcf7c70e9265d995b1815c74", "state": "OPEN, MERGEABLE, mergeStateStatus CLEAN", "ciConclusionStrings": ["Unit: COMPLETED / SUCCESS", "HTTP round trip: COMPLETED / SUCCESS"], "mergedSha": "notApplicable: the merge is the integration seat's per the file header this row repairs" }
  },

  "checkpointArtifacts": [
    "_inbox/2026-09-19_g169-deploy-path-windows_cp1.json",
    "_inbox/2026-09-19_g169-deploy-path-windows_cp2.json"
  ],

  "falsifier": "F1, the one that mattered: a real read-only command run through the SHIPPED spawn mechanism must exit 0 and print the tool's own output. `gcloud run services describe plan-review --project plan-review-505715 --region us-east1 --format=value(status.url)` exits 0 and prints https://plan-review-ozx33wafia-ue.a.run.app; the same command against smart-files prints https://smart-files-padrd77ava-ue.a.run.app; `vercel --version` exits 0 and prints 54.20.1 in both repos. Four of four registered commands spawned. It would have falsified the fix if any of them failed with a spawn reason -- and the CONTRAST run makes the falsifier non-vacuous: the same self-test with the spawn reverted to the legacy bare-name call FAILS ('FAIL a tool that exists runs and returns real output threw kind=spawn could not be spawned (ENOENT)', exit 1) and passes again after a byte-identical restore (exit 0), in both repos.",

  "contradicted": "Four, all recorded in full in CP2.contradicted. (1) The dispatch's first repair -- 'the .cmd can be run through cmd.exe with the arguments still passed as an array' -- is true only when the program element handed to cmd.exe is the BARE NAME; handing it the RESOLVED path re-splits at the space in `C:\\Program Files\\...` (and this fleet's gcloud shim path itself contains a space), so the fix resolves on PATH to DECIDE and passes the bare name to EXECUTE. (2) In the self-test the legacy bare-name call surfaces as ENOENT rather than the EINVAL the dispatch measured for `gcloud.cmd`; both are spawn errors and both used to render as `(exit ?)`. (3) `vercel whoami` SPAWNED correctly and then exited 1 on a TLS certificate failure -- this host's environment, not this defect -- and the mechanism correctly reported it as kind=exit; it is flagged because served-commit-parity's console leg shells `vercel ls` and will hit the same wall here. (4) The dispatch's own `{shell:true}` success line is reproduced: it works, which is exactly why the header refuses it.",

  "leave_behind": [
    "MERGE AND THE FIRST REAL DEPLOY ARE THE ONLY THINGS LEFT, and they are not this lane's. Both PRs are OPEN, MERGEABLE and CLEAN with every check-run conclusion SUCCESS; the integration seat merges, and the deploy path's first real green run is the end-to-end proof this row is owed.",
    "THE TWO PRs ARE BYTE-IDENTICAL IN scripts/deploy.mjs (sha256 588F2B828C69C0C173BDF99C1BDEF8053328E53764BAE416567E3E7805E4FAAF) AND MUST BE KEPT SO. Merging one without the other leaves the two products' deploy paths diverging on the very mechanism this row repairs.",
    "`--self-test` NOW RUNS IN BOTH REPOS' CI as an explicit step in `.github/workflows/test.yml`. Dropping that step during a later refactor makes the instrument dormant, which is the shape the G-160 header warns about.",
    "THE RENDERED ERROR MESSAGE HAS NO TEST OF ITS OWN. The self-test asserts the ToolFailure KINDS that produce the message; nothing asserts the message's rendering. A later refactor that reintroduced a bare exit code would undo half this row without failing a test.",
    "`vercel whoami` AND ANY NETWORK-REACHING VERCEL COMMAND FAIL ON THIS HOST ON A TLS CERTIFICATE (`unable to verify the first certificate`). Environment, not this defect; it will keep served-commit-parity's console leg red on this host until the root CA situation is resolved.",
    "NO WATCH, NO DETACHED RUNNER, NO LEASE. Every command in this lane was exit-bounded; there is nothing left running to clean up."
  ],

  "missionPremise": "The row rests on G-160's own finding that the deploy path is the SETTER for SERVED_COMMIT and that the parity gate cannot PASS on any product without it -- so a deploy path that cannot spawn gcloud or vercel on the fleet's own machine means the gate's arm never runs and the row is un-exercisable by its own file. The dispatch states the mechanism from measurement (2026-09-19T12:24Z): `execFileSync('gcloud', ...)` -> ENOENT, `execFileSync('gcloud.cmd', ...)` -> EINVAL, `{shell:true}` -> OK. The premise was re-measured in this lane before the fix and reproduced, and the standing constraint that shapes the repair is the header's own sentence -- 'No shell string is ever built, so a commit or a project name can never be re-split into another flag' -- which is why the one-word fix is refused. Source: the dispatch itself plus the file's own header, and the preambles bound at `P:/doc_repo/90_runbooks/AGENT_CONTRACT.md` and `DEV_PROCESS.md`.",

  "completionPredicate": "DONE means, and was evaluated as, all four of: (1) `scripts/deploy.mjs` in BOTH repos spawns a real `.cmd`-shim tool end to end on Windows through the same mechanism `issue()` uses, with the argv still an array -- shown by four registered read-only commands, three exiting 0 with the tool's own output and the fourth spawning and exiting non-zero for a network reason; (2) a `--self-test` wired into each product's own test command that proves both directions -- a tool that exists returns real output, a tool that does not fails with a NAME -- and that FAILS when the spawn is reverted to the bare-name call; (3) `FAILED (exit ?)` replaced by a message naming the kind, the command and the remedy, with a spawn error and a non-zero exit distinguishable; (4) no bypass flag added and the clean-tree refusal, commit resolution, `--update-env-vars`, gate and exit codes untouched, with `served-commit-parity.mjs` not modified. A stranger can evaluate all four from CP2 without this session: each carries the verbatim output and the exit code.",

  "scopeBasis": "The boundary is the dispatch's: one spawn mechanism and one error message. The gate, the parity check, the lease law and the sequencing are declared out of scope because the dispatch says so explicitly, and the deploy itself is out of scope because shifting traffic without a P-170 lease is a contract violation and the row is not an exception. The two-PR shape follows from the file living in two repos with no drift between them. The `probe.notApplicable` posture follows from the deliverable being a local mechanism rather than a surface: nothing is deployed, so nothing is measured on a customer surface. `scripts/served-commit-parity.mjs` is excluded in the dispatch's own words because repairing it would MOVE the defect rather than remove it. What is deliberately NOT included, so a successor can tell an exclusion from an oversight: no assertion on the rendered error message (named in leave_behind), no CI run on Windows (CI is ubuntu-latest, so the shim branch is proven locally and the non-shim branch in CI, and CP2 says which is which), and no attempt at the first real deploy.",

  "subAgents": { "spawned": 0, "maxDepth": 0 },

  "countsWithTheirCountingRules": {
    "commandsRunThroughTheShippedSpawnMechanism": "4 registered, counted once each: 3 exited 0 with the tool's own output; 1 (vercel whoami) spawned and exited 1 on a TLS certificate failure; 0 failed with a spawn reason.",
    "selfTestLegsPerRun": "3, counted per run: 2 always-pass legs asserted on their own predicates (a tool that exists returns real output matching ^\\d+\\.\\d+; a tool that does not exist fails with a name-bearing spawn ToolFailure) and 1 refusal leg (a cmd metacharacter is refused rather than passed). 6 full runs recorded (3 states x 2 repos).",
    "violationRuns": "2, one per repo, each preceded and followed by a passing run -- so each repo has a PASS/FAIL/PASS triple with its exit codes 0/1/0.",
    "ciChecks": "3 check-runs across the two PRs, read from the `conclusion` FIELD, all COMPLETED / SUCCESS.",
    "filesChangedPerRepo": "3 (scripts/deploy.mjs, package.json, .github/workflows/test.yml). No other file in either repo.",
    "linesChangedInDeployMjs": "178 insertions / 3 deletions, identical in both repos."
  },

  "openItems": [
    "THE FIRST REAL DEPLOY IS THE OWED END-TO-END PROOF and belongs to the integration seat after the merge.",
    "THE TWO PRs MUST BE MERGED TOGETHER AND THE CI STEP KEPT IN BOTH -- see leave_behind.",
    "THE RENDERED MESSAGE HAS NO TEST -- see leave_behind."
  ],

  "docRepoEditsUncommitted": "Three files, all in this lane's own seat worktree P:/seat-worktrees/g169-deploy-path-windows/doc_repo and all LEFT UNCOMMITTED, per the contract: _inbox/2026-09-19_g169-deploy-path-windows_cp1.json, _inbox/2026-09-19_g169-deploy-path-windows_cp2.json, _inbox/2026-09-19_g169-deploy-path-windows_close.json. Nothing in P:/doc_repo was written."
}
