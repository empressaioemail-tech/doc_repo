{
  "lane": "g133-bastrop-verify-access",
  "planRows": ["G-133"],
  "status": "closed",
  "probe": { "artifact": "_inbox/2026-09-14_192904_surface_probe.json" },

  "crux": "Bastrop's map works for the operator in a browser and 401s for a lane with curl because the two things being tested are TWO DIFFERENT PRODUCTS gated by TWO DIFFERENT, UNRELATED CREDENTIAL MECHANISMS, and a lane's curl carries neither: v1 (smartcity-os, smartcityos.io) needs a real Google-OAuth-backed Passport SESSION COOKIE (the operator's browser has one from logging in; curl never does), and v2's native property map (smartcity-dashboards) needs a Hauska product key bound to jurisdiction_tenant=bastrop_tx presented as an x-hauska-key header -- a key that DOES exist, is active, and was used successfully as recently as 2026-09-14T20:31:55Z (hours before this dispatch), but has never reached any lane's own environment, Secret Manager, or documentation. Neither mechanism is broken. Both are working exactly as designed. Nobody handed a lane the two different keys those two designs require.",

  "falsifier": "If v1's /api/esri/* routes had answered 200 to an anonymous curl, the 'v1 needs no credential at all' reading would stand and this close would say so. They answered 401 instead (live-measured, twice, on smartcityos.io) -- tracing that to server/routes.ts's global isAuthenticated gate (not esri.ts's own middleware, which genuinely is open) falsified the simpler 'v1 is just open' reading and produced the correct, more precise one: v1 IS gated, just not by anything Hauska-shaped. Symmetrically: if the hauska-mcp-server api_keys table had returned zero rows for jurisdiction_tenant='bastrop_tx', the standing 'the key does not exist' conclusion from G-116/G-128 would have been confirmed rather than overturned. It returned one active row, used today, created the same day as the close that first called it missing.",

  "contradicted": "G-116's close (2026-09-03) and G-128's close (2026-09-14) both state 'a real Hauska product key scoped to bastrop_tx does not exist.' That statement is false as of this session: key_id 2a26c318-271d-414c-967d-caba619a6f73 is active, correctly scoped, and was used successfully today. It is not clear whether it was false the moment G-116 wrote it (the key's created_at, 19:23:47 UTC on 2026-09-03, may be before or after that close was filed the same day) or became false minutes later and was never re-checked -- but G-128, filed 2026-09-14, cited G-116 as its own basis rather than re-enumerating at source, which is exactly the failure this dispatch's own Method section warns about ('verify at source... including your own dispatch's context block'). Neither prior lane looked in hauska-mcp-server's own database, because neither lane's own dispatch scope included that repo -- see item 5 below.",

  "item1_gate": "src/tenancy.mjs's resolveCaller/canReadPack, called from 9 routes in src/server.mjs via packContentReadStatus. Deployed today (smartcity-dashboards-00072-cow, origin/main@c7174f8): two caller kinds, tenant (x-hauska-key -> hauska-mcp-server /auth/whoami -> jurisdiction_tenant) and service (Authorization: Bearer DASHBOARDS_API_KEY). Only 'tenant' unlocks a tenant-private pack; a bare service caller gets 403, not 200 (confirmed correct by code and by G-116's own prior live test). A third kind, staff (JWT bearer, G-132), exists ONLY in the local seat worktree, uncommitted -- not part of the deployed gate. Full quotes and grep evidence in CP1/CP2.",

  "item2_v1VsV2": "CONFIRMED with a correction: v1 (smartcity-os) needs NO Hauska credential of any kind for its Esri data (esri.ts's own middleware is genuinely open, hardcoded to Bastrop, no product key anywhere in the file) -- but it is NOT credential-free end-to-end, because server/routes.ts's global gate requires a real logged-in session (Passport/Google OAuth) for every esri route except geocode/suggest/geoenrichment, live-confirmed by two 401s against smartcityos.io with no cookie. v2's native property map (src/property-map.mjs, G-117) calls smartcity-os's own PLATFORM-INTERNAL route server-to-server using a THIRD credential (PLATFORM_INTERNAL_API_KEY, already correctly configured) -- but never reaches that call at all until the FIRST gate (item 1, the Hauska tenant key) is satisfied. The 'missing key' three lanes hit is exclusively the item-1 gate, blocking verification of v2, not access to Bastrop data itself.",

  "item3_credentialExists": "YES. Enumerated before asserting anything (per dispatch instruction): Secret Manager in smartcity-dashboards (31 secrets, none tenant-shaped) and hauska-prod-497015 (30+ secrets, none tenant-shaped, by design -- keys are DB rows, not per-tenant GCP secrets); Cloud Run env on the serving revision (HAUSKA_MCP_URL present, no key value, correct since this service validates presented keys rather than holding one); .env.example (silent on this whole mechanism -- a real documentation gap); smartcity-dashboards' own 'tenant-registry' Neon DB (one table, city_packs, no credential column -- this DB stores PACK definitions, a naming trap against the phrase 'tenant-registry'). The credential itself lives where the code says it must: hauska-mcp-server's own api_keys table (its own Neon DB, its own GCP project, hauska-prod-497015) -- one active row, jurisdiction_tenant=bastrop_tx, owner_email=empressaioemail@gmail.com (the operator's own account), owner_name 'Nick (bastrop_tx staff pilot)', notes citing G-116 by name, created 2026-09-03, last_used_at 2026-09-14T20:31:55Z. Never printed: key_hash (the only actually-secret column) was never selected in any query this session ran. A second, unrelated row (jurisdiction_tenant='bastrop-tx', hyphenated, revoked, from an unrelated 2026-07-07 card) was found and ruled out rather than left ambiguous.",

  "item4_howLiveServiceAnswersToday": "Two different answers for two different services, both live-verified. v1: a session cookie from a real Google-OAuth login via Passport (replit_integrations/auth), backed by a Postgres session store -- the operator's browser has this from having logged in at some point; curl never carries a cookie it was never given, hence the 401 this session reproduced live. v2: the x-hauska-key header carrying the raw value of the bastrop_tx key found in item 3, verified against hauska-mcp-server's /auth/whoami (itself live-confirmed: anonymous GET returns 200 {anonymous:true, jurisdiction_tenant:null} exactly as the code predicts). This session could not observe the exact client that presented the key at 20:31:55 UTC today (the raw key value is one-way-hashed in storage and was correctly never sought) -- most likely the operator or 'Nick' personally, via a manual tool (curl/Postman/a script), since a plain browser tab cannot inject a custom header without an extension or a bookmarklet. Reported as the most likely explanation, not asserted as directly observed.",

  "item5_whyThreeLanesReportedStanding": "Each of G-116, G-128, and this dispatch's own cited planner probes checked ONLY their own dispatch's assigned repos (smartcity-dashboards, plan-review, smart-files) and, finding no key-shaped thing in any of those repos' own Secret Manager/env/DB, concluded (correctly, for what they checked) that no key was reachable from where they were allowed to look. None of their dispatch scopes included hauska-mcp-server -- OPS-17 govtech scope discipline (a real, previously-earned discipline in this program) means a govtech-seat lane does not go read a substrate-seat repo uninvited. G-128 then compounded this by citing G-116's finding as precedent rather than re-enumerating at source, per this dispatch's own contradicted-with note. The missing MECHANISM, not a missing key: there is no documented, lane-reachable pointer from smartcity-dashboards' own repo (its .env.example, its README, its Secret Manager) to where a real verification credential lives or how a lane should request one -- the credential's only real address is a personal Neon DB row owned by one person's email, discoverable only by reading hauska-mcp-server's own auth code and querying its own DB directly, which is exactly the cross-seat step this dispatch, uniquely among the four, was explicitly authorized to take.",

  "verdict": "Neither a provisioning problem nor a misunderstanding about what the gate is FOR (both gates do exactly what they were built to do, correctly). It IS a distribution problem, precisely: a real, active, correctly-scoped credential exists and has simply never been placed anywhere a lane can find it. Recommended follow-on (not attempted here -- no credential created, per this dispatch's own constraint): either (a) document the existing mechanism in smartcity-dashboards' .env.example and README (name HAUSKA_MCP_URL, x-hauska-key, and where/how to request a scoped test key) and store a lane-usable copy of a bastrop_tx-scoped key in smartcity-dashboards' OWN Secret Manager, or (b) mint one additional, purpose-built 'lane verification' key (independent of the operator's personal 'Nick' pilot credential, so lane use and revocation don't collide with the operator's own testing) via hauska-mcp-server's admin API, then store THAT in Secret Manager per (a). Either is a same-day fix; the credential-minting mechanism itself (src/admin.ts, hauska-mcp-server, gated by HAUSKA_ADMIN_BOOTSTRAP_KEY) already exists and works -- the September 3rd key is proof.",

  "completionPredicate": "Met: what gates the routes is stated precisely and quoted from source (item 1); the v1/v2 data-path question is confirmed with a correction, not assumed (item 2); credential existence is a positive determination with full enumeration, not an assumed absence (item 3); the live-vs-curl difference is identified and live-reproduced for both services (item 4); the standing-vs-escalated question is answered with the actual missing mechanism named (item 5); and the provisioning/distribution/misunderstanding verdict is stated plainly with a costed follow-on. All five items measured against source (code read at exact commits, two GCP projects' Secret Manager, two live Cloud Run services curled, one live DB query against the actual authority), not inferred from a prior close.",

  "missionPremise": "Operator observation 2026-09-14: the v1 property map worked last week with real Bastrop data, yet G-116/G-128/the planner's own probes all hit 401 on every bastrop_tx route -- 'those two facts cannot both be casually true.' They are both true, simultaneously, for the reason in `crux` above: they were never testing the same gate.",

  "scopeBasis": "Read-only across smartcity-dashboards, smartcity-os (narrow read exception per _catalog/repo_intents.md, code-reading only, nothing changed), and the GCP projects/databases named in item 3 -- explicitly authorized by this dispatch's own Method section ('Secret Manager in each GCP project... the tenant-registry Neon store'), which is the one thing that made checking hauska-mcp-server's own database (a different seat's repo, normally off-limits per govtech scope discipline) legitimate for this specific lane. No file changed in any repo. No credential created, rotated, or printed. Lane claimed via lane-claim.mjs as cente-g133 before any investigation; release follows this close.",

  "checkpoints": [
    "_inbox/2026-09-14_g133-bastrop-verify-access_cp1.json",
    "_inbox/2026-09-14_g133-bastrop-verify-access_cp2.json"
  ],

  "constraintsHonored": [
    "Change nothing: zero files edited in any product repo; zero credentials created.",
    "Never print secret VALUES: every DB query selected only non-secret columns (key_id, tier, product, jurisdiction_tenant, status, owner fields, timestamps) and never key_hash; DATABASE_URL/DB connection strings were fetched into shell variables and unset immediately, never echoed to output.",
    "Enumerate before asserting absence: item 3 lists every location checked, with what was found at each, before concluding presence/absence.",
    "Read the authoritative record: Cloud Run traffic read via status.traffic[].revisionName/percent by field name from JSON, never a positional value() formatter; the serving revision (00072-cow) was confirmed against the exact commit its tenancy.mjs was read from.",
    "State a second mechanism and reject it: item 2's v1 finding explicitly considered and then confirmed (rather than dismissed) the 'a broader gate exists' alternative, which turned out to be true and became part of the answer rather than being discarded.",
    "Every verification command exit-bounded: every gcloud/curl/psql call in this lane ran under an explicit `timeout`.",
    "Doc_repo edits (this close + CP1/CP2 + probe artifact) left uncommitted in this seat's own worktree (P:/seat-worktrees/govtech/doc_repo) for the planner to review and commit, per AGENT_CONTRACT section 6."
  ],

  "leave_behind": [
    {
      "item": "Distribution fix: place a lane-usable bastrop_tx Hauska key in smartcity-dashboards' own Secret Manager and document x-hauska-key/HAUSKA_MCP_URL in .env.example. Either reuse the existing active key (owner: operator/Nick) or mint a dedicated lane-verification key via hauska-mcp-server's admin API (mechanism already proven working, see item 3).",
      "owner": "operator decision on which key to use, then govtech seat to wire the Secret Manager entry + docs",
      "plan_row": "not yet filed -- named follow-on, not attempted here (no credential created per dispatch constraint)"
    },
    {
      "item": "smart-city-os's .env.example / README does not surface that most /api/esri/* routes require a real login session (only geocode/suggest/geoenrichment are public) -- a future lane trying the same live-verify-Bastrop task without reading routes.ts in full would rediscover this 401 from scratch. Worth a one-line note in that repo's own docs, out of this lane's write scope (ABSOLUTE NO-TOUCH).",
      "owner": "property seat (smartcity-os owner)",
      "plan_row": "not yet filed, small follow-up"
    },
    {
      "item": "G-116's and G-128's closes now carry a stated-false claim ('key does not exist') that a future reader could still propagate if they read those closes instead of this one. Not edited here (closes are historical records, not live state per DEV_PROCESS 6) but flagged so the plan-of-record / planner points future readers at G-133 for the current truth.",
      "owner": "dispatch planner / plan-of-record maintainer",
      "plan_row": "OPS-17 plan-of-record table, G-133 row"
    }
  ],

  "fleetMemoryScratch": {
    "LESSON": [
      "A repo_intents.md phrase like 'tenant-registry Neon is this product's store' can describe a PACK registry, not a CREDENTIAL registry -- worth reading the actual schema (\\dt) before trusting the phrase to mean 'this is where the key would be.'",
      "'The v1 map worked in a browser' and 'a lane's curl gets 401' can both be true about the SAME service if there are two independent gates (esri.ts's own open middleware AND routes.ts's global session gate) -- reading only the route-handler file and stopping there would have produced a confidently wrong 'v1 needs nothing' conclusion; the global middleware registration file was the one that mattered.",
      "A prior lane's close saying a credential 'does not exist' is a claim scoped to what THAT lane's own dispatch authorized it to check -- OPS-17 govtech scope discipline means a govtech lane genuinely cannot see into a substrate-seat repo's own database, so 'not found' from three lanes in a row is evidence about scope boundaries, not necessarily evidence about the credential."
    ],
    "GROUND-TRUTH": [
      "(2026-09-14T~19:20 CDT / 00:20Z) smartcity-dashboards serving smartcity-dashboards-00072-cow @ 100%, built from origin/main@c7174f8 -- unchanged since G-128's close.",
      "(2026-09-14T~19:25 CDT) hauska-mcp-server serving hauska-mcp-server-00092-pur, /auth/whoami live and correct for an anonymous caller.",
      "(2026-09-14T20:31:55Z, per DB) the bastrop_tx Hauska key (key_id 2a26c318-271d-414c-967d-caba619a6f73) was used successfully -- active, not a historical artifact."
    ],
    "OPEN": [
      "Which specific client presented the bastrop_tx key at 20:31:55Z today -- not determinable without asking the operator directly, and not necessary to this close's completion predicate.",
      "Whether the credential-distribution follow-on (leave_behind #1) should reuse the operator's personal key or mint a dedicated one -- operator decision."
    ]
  }
}
