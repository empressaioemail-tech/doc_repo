---
id: 2026-09-11_ops23_wave1_verify_p158
date: 2026-09-11
row: P-158
kind: read-only fact verification (precedes the P-158 lane mission)
snapshot:
  doc_repo: main 0cacf4c5
  hauska-factory: origin/main 217b7dd
  legacy-design-tools: origin/main 3950ce9b
  hauska-engine: origin/main 79fa573
  hauska-map: origin/main 6ab6914
method: git fetch + git show/grep against origin/main only; no working tree read or modified
---

# P-158 verification: building footprints, the join, the predicate

## 1. The decline string is emitted by hauska-engine, not hauska-factory

In hauska-factory the string exists only as a test fixture: `test/parcel-building-footprint-reconcile.test.mjs:99` and `:105`. `git grep -i "below-10pct|overlap"` over `src/ sql/ migrations/ scripts/` finds no emitter.

Emitter: `hauska-engine/packages/engine-core/src/building-footprint/staged-footprint-join.ts:511` `planCountyFromStagedGeometryTrueJoin`, lines 569-576:

```
    } else {
      planned.push({
        outcome: "absent-per-parcel",
        parcelKey: record.propId,
        absenceKind: "no-footprint-feature",
        reason:
          "staged-geometry-true-join-below-10pct-overlap-threshold — no qualifying staged footprint for parcel",
```

The ratio: `spatial-join.ts:85-99` `footprintParcelOverlapRatio`. Numerator is the intersection area, denominator is the FOOTPRINT area, not the parcel area:

```
  const fpArea = polygonAreaM2(fpPoly);
  if (fpArea < 1e-6) return 0;
  const inter = polygonClipping.intersection(fpPoly, parPoly);
  if (!inter || inter.length === 0) return 0;
  return multiPolygonAreaM2(inter) / fpArea;
```

Thresholds: `constants.ts:21-22` `PRIMARY_OVERLAP_MIN = 0.5`, `STRADDLE_OVERLAP_MIN = 0.1`. `classifyOverlapRatio` (`spatial-join.ts:101-113`) attaches at >= 0.5 as `primary`, at >= 0.1 as `unknown` with `flag: "straddle-review"`, rejects below 0.1.

Finding the label hides: `joinStagedCandidatePairs` (`staged-footprint-join.ts:367-400`) attaches each footprint to exactly one parcel, the best ratio (`verdict.overlapRatio > bestRatio`, line 374). The absence branch at 569 fires for any parcel with zero joined footprints, which covers three distinct causes under one reason string: no candidate in the envelope prefilter (`stagedEnvelopeCandidatesSql` 231-248, `ST_Intersects(fp.geom, ST_MakeEnvelope(...))` with `fp.county_fips = $6 AND fp.geom IS NOT NULL`), a candidate below 10 percent, or a candidate that attached to a neighbouring parcel because that parcel scored higher. A parcel losing the best-parcel contest is labelled "below-10pct" when its ratio may be 0.49.

## 2. The staged layer

Table: `tx_building_footprint` (`staged-footprint-join.ts:37` `STAGED_FOOTPRINT_TABLE`). Store: the CORTEX store, `write-building-footprint-county.mjs:127-132` resolves `CORTEX_DATABASE_URL || TXGIO_DATABASE_URL || DATABASE_URL` with the fatal message "the store holding txgio_parcel"; atoms are written to a separate `DATABASE_URL / SUBSTRATE_DATABASE_URL` (line 219).

Source: Microsoft Global ML Building Footprints, `constants.ts:1-16` (`GLOBAL_ML_TEXAS_ZIP_URL`, citation "Microsoft Building Footprints (ODC-By 1.0) via ml-global-building-footprints-v1"). DDL: `packages/engine-core/scripts/migrations/0075_tx_building_footprint.sql:21-42` (`footprint_id`, `geometry jsonb`, bbox columns, `county_fips`, `source_tier DEFAULT 'ml-derived'`); PostGIS `geom` plus GiST added by `scripts/apply-tx-building-footprint-migration.mjs:65-72`.

Loader: NOT FOUND on origin/main. `0075:4` says "Loaded by ingest-tx-building-footprints.mjs"; `git ls-tree` and `git grep` on hauska-engine and legacy-design-tools origin/main find no such file and no `INSERT INTO tx_building_footprint`. `0075:11-19` records that the creating migration lived only on unmerged branch `feat/p2-4-tx-building-footprint-staging`; `git ls-remote` shows that branch still on the engine remote at `e7145a3`. The loader presumably sits there; not read (out of the origin/main scope).

County declaration: NOT FOUND as a file. Searched hauska-factory `DECLARED_COMPLETE` (zoning only, `src/config/zoning-layer-completeness.mjs:69`), `footprint` under `src/`, `config/`, `scripts/` (no `data/` directory exists), and hauska-engine `STAGED_COUNTIES|stagedCounties|declared` under `building-footprint/`. The ceiling is a live probe, `packages/retrieval/src/rail-scoring-spec/specs.ts:231`: "cortex: SELECT DISTINCT county_fips FROM tx_building_footprint. That is the ONLY source the county writer may read". Dated readings in the same file: 254 counties, 10,674,975 rows (lines 246-249, 276-277, VERIFIED 2026-08-19); atoms written in 174 of 254 counties, "Bastrop, Travis, Harris, Dallas, Williamson and Bexar all hold zero" (278-281, RELAYED 2026-08-19). Every one is a claim about its date.

Factory-side references: `migrations/0001_init.sql:117` `landing_tx_building_footprint`; `src/control/collect-complete.mjs:22` `footprint: "tx_building_footprint"`; `src/jobs/landing-import.mjs:17`; `src/control/plan.mjs:22` maps hold `P-09` to rail `footprint`.

## 3. Rail key and the Factory writer

`src/lib/parcel-record-engine/rail-keys.js:64`: `{ key: "buildingFootprint", grain: "companion", group: "companion", access: PUBLIC_RAIL_ACCESS }`.

Writer: `src/jobs/parcel-building-footprint-reconcile.mjs`, allowlisted at `src/control/writer-allowlist.mjs:182-187` (kind `atom-reconcile`, planRow P-124). It mints no atoms. It pages `parcel_record` (84-89), reads `hauska_mcp.atoms` via `ATOMS_DATABASE_URL` (`src/db/atoms-connect.mjs:19-27`) with:

```
export const REAL_ATOM_COUNTS_SQL = `SELECT body->>'parcelNodeId' AS entity_id, entity_type, count(*)::int AS n
  FROM atoms
 WHERE entity_type = 'building-footprint'
   AND NOT (body ? 'absence')
   AND body->>'parcelNodeId' = ANY($1)
```

(96-101) and `ABSENCE_ATOM_ROWS_SQL` (104-110), then upserts `parcel_record_cell` (169-186). Value cells come from the vendored `ENTITY_TYPE_TO_RAIL` (`ingest-existing.js:214`); absence cells from `ingestBuildingFootprintAbsenceOntoRecords` (119-142), which copies the atom's `absence.reason` verbatim into `basis.reason`. So the Factory cell repeats the engine label, including its ambiguity.

Its refusal codes: `COUNTY_REQUIRED` (68), `PAGE_SIZE_INVALID` (72), `WRITER_NOT_ALLOWLISTED` (263), `LAPTOP_WRITE_FROZEN` (265), `IDEMPOTENCY_DRIFT` (251), `ATOMS_URL_REQUIRED` / `POOLER_HOST_REFUSED` (atoms-connect 17-26). A still-unaccounted cell is never written (194).

Upstream atom absence reasons (all `absenceKind: "no-footprint-feature"`, `types.ts:74`): `no usable parcel ring for <fips>:<key>` (`staged-footprint-join.ts:541`), the 10pct string (575), and the legacy ML path's `ml-spatial-join-below-50pct-overlap-threshold` (`plan-county-building-footprints.ts:148`). Halts that emit no atoms: `STAGED_FOOTPRINT_COUNTY_EMPTY` (117), `STAGED_FOOTPRINT_GEOM_UNREADY` (124), `STAGED_FOOTPRINT_TABLE_MISSING`.

## 4. legacy-design-tools: the mission's Bastrop-only belief is REFUTED

Producer: `artifacts/api-server/src/lib/buildingFootprintFactRead.ts`, consumed by `routes/brokerageNodeFacets.ts:265,810` and `routes/propertyExplorer.ts:84,285`. Query, lines 154-162:

```
SELECT entity_id, body
  FROM atoms
 WHERE entity_type = $1
   AND (
     (entity_id >= $2 AND entity_id < $3)
     OR (entity_id >= $4 AND entity_id < $5)
   )
```

Params (484-490): `'building-footprint'` plus integer and `.00000000`-padded prefix ranges. Store is `ATOMS_DATABASE_URL` (179). `grep -i "48021|bastrop|county"` in the file: zero hits. It reads the statewide atoms table with no county filter and never touches `tx_building_footprint` (header 21-23).

State field is `state`: `present` | `absent` | `refused`; refusal `code` is `atom-miss` | `bind-conflict` | `atoms-store-not-configured` | `malformed-atom` (135-141).

Near-bbox footprint route in cortex: NOT FOUND. Searched `near-bbox|nearBbox|near_bbox|building-footprints/near` across `artifacts/api-server/src` and `artifacts/*/src`, non-test: zero hits.

## 5. hauska-engine retrieval-api

`services/retrieval-api/src/server.ts:477` `GET /building-footprints/near-bbox` (5-digit `countyFips`, bbox span <= 0.5 deg, optional `limit`) calls `packages/retrieval/src/index.ts:566` (default 400, cap 2000) which calls `packages/storage/src/pg-storage.ts:552-597`:

```
      SELECT body
      FROM atoms
      WHERE entity_type = 'building-footprint'
        AND body->>'parcelNodeId' LIKE ${parcelPrefix}
        AND COALESCE(body->>'status', 'active') = 'active'
        AND body->'footprintGeometry' IS NOT NULL
        AND body->'absence' IS NULL
```

Entity type `building-footprint`, present atoms only, store is the PgStorage `atoms` table. Siblings: `/road-nodes/near-bbox` (414), `/special-districts/near-bbox` (539).

## 6. hauska-map property-explorer

Overlay: `apps/property-explorer/src/browse/building-footprint-overlay.ts:50-87` builds the `building-footprint` layer (fill `#c4a882`). Fetch: `ExplorerMap.tsx:259-277`, URL `/api/spine/retrieval/building-footprints/near-bbox?countyFips=&westLng=&southLat=&eastLng=&northLat=&limit=400`, proxied by `apps/property-explorer/api/spine.ts:17-18,186-189` to `RETRIEVAL_API_URL` with a Bearer key.

Gate that matters for the mission: `ExplorerMap.tsx:731-745` fetches only when zoom >= `MIN_ROAD_ZOOM` and `countyFipsForViewportCenter` is non-null; `county-fips-viewport.ts:9-17` hardcodes four bounding boxes (48021, 48453, 48209, 48491). Anywhere else the layer is empty by construction, regardless of atoms. Toggle defaults off (`consumer-layers.ts:85`).

## Fleet memory scratch

LESSON: the "below-10pct" reason is one label for three join outcomes; a predicate fix must split them before any threshold is retuned.
LESSON: the overlap denominator is footprint area, so a large parcel never dilutes the ratio; a footprint straddling parcels loses to the neighbour, not to the threshold.
GROUND-TRUTH (origin/main SHAs above, read 2026-09-11): `tx_building_footprint` loader absent from main in both engine and LDT; branch `feat/p2-4-tx-building-footprint-staging` exists remotely at e7145a3.
DEAD-END: searching hauska-factory for the emitter; it only vendors the label through the atom body.
OPEN: whether Bastrop/Travis/Williamson still hold zero footprint atoms is a dated 2026-08-19 relay; re-probe before the lane scopes "acquire".

leave_behind: none
