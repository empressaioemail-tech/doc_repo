---
id: 2026-08-30_p91_p557_deploy
title: p557 serving at 100% on smartsite-mcp-00063-rej
date: 2026-08-30
status: serving-100pct
plan_row: P-91
service: smartsite-mcp
revision: smartsite-mcp-00063-rej
tag: p557
digest: sha256:a77d13c05b2642fe5be5505f5237d1592efb35024e66de36511fc138f90ab361
uri: ui://smartsite/app-p556.html (iframe unchanged from p556)
tree: P:/tmp/legacy-design-tools-p91-stone feat/p91-wave-h-stone 51213e67
build: Cloud Build 4bdc43e5-f4fb-4592-9973-85f61e7028c6 SUCCESS (config artifacts/smartsite-mcp/cloudbuild.p557.yaml)
pr: empressaioemail-tech/legacy-design-tools #552 (Test SUCCESS, Typecheck SUCCESS, squash-merged 7cbe0bc4 after main was merged into the branch at 3bf89750; the three conflicts were the #551 squash versus the p557 text and were resolved to the p557 side, suite 151/151)
qa_source: operator walk 2026-08-30 and _inbox/2026-08-30_p91_qa_triage.md
---

# What is on this image

`serverInfo` announces `websiteUrl: https://smartsite.cloud` and `icons` (the site's `apple-touch-icon.png` 180x180 PNG first, `icons/icon-512.svg` second), so the connector card follows the product mark; `/` and `/favicon.ico` on the MCP host redirect (302) to the product. `find_parcel` `hits` carry parcel records only; when nothing binds, address points move to a typed `located` list with `missClass: located-unbound` (QA D1). Descriptions: a located row is never a parcel; `bakedAt` / `asOf` / `stampedAt` are the bake's time and `runId` names the bake (QA D4 wording). Tests 151/151; icon assertion observed failing with icons removed; D1 fixtures observed failing with the split disabled.

# Deploy

Digest from Artifact Registry by field name. `gcloud run deploy smartsite-mcp --image ...@sha256:a77d13c0... --no-traffic --tag p557`; service config carried; no `--set-env-vars`.

Tag health: `{"status":"ok",...,"authConfigured":true,"cortexConfigured":true,"revision":"smartsite-mcp-00063-rej",...}`. Tag `/favicon.ico` 302 to `https://smartsite.cloud/favicon.ico`; tag `/` 302 to `https://smartsite.cloud/`.

Shift `--to-tags=p557=100`. Traffic by field name:

```
latestReady= smartsite-mcp-00063-rej
  revisionName= smartsite-mcp-00061-zik percent= undefined tag= p556
  revisionName= smartsite-mcp-00063-rej percent= 100 tag= p557
```

Prod `/health` (both `smartsite-mcp-tds7av26va-uc.a.run.app` and `mcp.smartsite.cloud`) names `smartsite-mcp-00063-rej`. `https://mcp.smartsite.cloud/favicon.ico` 302 to the site. Revision `status.imageDigest` equals the AR digest. Cortex stays p542.

# What the card will show

Whatever the site serves at the two icon URLs. Today that is the crosshair on the `--ss-void` tile; the tile moves to `--ss-ink #323234` under `_inbox/2026-08-30_smart_site_mark_tile_ink.md`, after which a reconnect refreshes the card with no MCP change.
