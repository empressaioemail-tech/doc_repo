# G-72 Smart Files embed chrome

WDLL `_inbox/2026-08-17_g72_files_embed_chrome_WDLL.md` approved. Cite WDLL items 2-4.

## Mission premise

When `?embed=1` or iframed, hide `#fixture-panel` and the Files product top bar. Direct `/` still shows the browser and fixture. Keep `api()` and `#share=`. Do not touch `src/*` or `web/api/files.js`. Do not edit `sc-kit.css`.

## Completion predicate

PR on smart-files web/: embed chrome live. Planner owns merge and Vercel deploy.

## Scope basis

G-72 only. Not G-70. Not G-71. Not OAuth.

## Workers

Do not spawn further agents. Isolated worktree from origin/main.

1. Embed detect + CSS hide (WDLL 2-3). PR.

## Invariants

Write path unchanged. Cortex stays 404. P:\smartcity-os no-touch. L26 idle. Do not reset P:\smart-files (dirty actors.mjs). Deploys planner-owned.
