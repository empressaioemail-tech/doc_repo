{
  "lane": "PREBAKE-ENG",
  "checkpoint": "CP2",
  "kind": "mid-work adversarial checkpoint, run against the finished register before close",
  "at": "2026-09-13",
  "planRows": ["P-181"],
  "snapshot": {
    "repo": "hauska-engine",
    "originMainSha": "112bccb8bed29da639955219511975bddbb425b4",
    "readVia": "git show origin/main:<path>",
    "artifact": "_inbox/2026-09-13_assumption_register_engine.md",
    "rows": 27,
    "filesOpened": 23
  },
  "adversarialReviewOfMyOwnFindings": [
    {
      "row": "ENG-01 (three writers cannot apply)",
      "whatWouldProveMeWrong": "handle.storage being anything other than PgStorage, or a default lease existing somewhere in the call chain.",
      "whatIDidAboutIt": "Read write-owner-fact-county.mjs:170 (handle = createPgStorage(...)), pg-storage.ts:1393 (createPgStorage returns new PgStorage(sql)), the lease refusal inside writePropertyAtomsBatch, and layered-storage.ts:46-51 (pure delegation, no lease injected). Then grepped all three scripts case-insensitively for 'lease': zero matches.",
      "residualDoubt": "Whether anything still invokes these CLIs. Recorded in COULD NOT ESTABLISH with both candidate mechanisms and why I rejected the second."
    },
    {
      "row": "ENG-04 (assertEdgesNotStarved is vacuous)",
      "whatWouldProveMeWrong": "Any input where expectedAppliesToCount(atoms) differs from appliesToLinksFromPropertyAtoms(atoms).length.",
      "whatIDidAboutIt": "Read both predicates line by line. Three skip conditions each, identical and in the same order. The only divergence available is resolveCanonicalParcelKey throwing inside appliesToLinkFromPropertyAtom, which aborts the call rather than producing a count mismatch. No input makes the throw fire.",
      "residualDoubt": "none on the vacuity; the fix shape is a suggestion, not a verified claim, because I did not read writeAtomLinks' body."
    },
    {
      "row": "ENG-05 (lease TTL)",
      "whatWouldProveMeWrong": "A ttlMs threaded from any county writer.",
      "whatIDidAboutIt": "grep for ttlMs and TTL over packages/engine-core/scripts/*.mjs returned NOTHING; railLeaseArgs' return object has three keys and no ttl. The only --ttl-sec is on the standalone storage CLI, which a writer does not consult because it takes its own lease.",
      "residualDoubt": "none on the code. The claim that a real run would hit 15 minutes is conditional and stated as conditional."
    },
    {
      "row": "ENG-02 (the collapse query)",
      "whatWouldProveMeWrong": "The SQL not matching normalizeForJoin's semantics.",
      "whatIDidAboutIt": "Deliberately avoided a lookahead regex in SQL and used ltrim plus an explicit all-zero filter and an all-digit filter, which is exactly equivalent to /^0+(?=\\d)/ over the admitted input set. Stated the one-digit floor and the all-zero exclusion inline so a reader can check the equivalence rather than trust it.",
      "residualDoubt": "The query is unrun. It is offered as an instrument, not a result, and the artifact says so."
    }
  ],
  "whatIDidNotDo": [
    "No database was queried. Every number in the artifact is either a code literal I read or a figure explicitly attributed to a prior artifact.",
    "No sub-agents were spawned.",
    "No file in hauska-engine was written, checked out, or branched. Local HEAD 8d8e880 was never read from.",
    "I did not read every line of the large files. Per the dispatch bound I read exports and function tops, and the file list records which blocks of the 480-line and 595-line files were opened."
  ],
  "preRegisteredFalsifierForTheLaneAsAWhole": "If a reviewer opens any cited file:line at 112bccb and finds the quoted lines absent or materially different, the row is wrong and the register's method failed. Every quote in the artifact was pasted from git show output in this session, not reconstructed from memory.",
  "contradictedByThisWork": [
    "The dispatch's framing that the Harris lease incident is an open defect: the plan phase is now OUTSIDE the lease at 112bccb (write-parcel-node-county.mjs:468 takes the lease after the atoms are built). The residual is different from the original defect and is recorded as ENG-05.",
    "The comment at plan-county-parcel-nodes.ts:165-168 asserting that the planner's normalization matches the resolver's on both sides. It does not (ENG-03).",
    "The docstring at parcel-node-atoms.ts:202 claiming the read-back check proves the pointer resolves in txgio_parcel. It proves internal consistency only (ENG-07)."
  ],
  "constraintsHonored": [
    "READ-ONLY: no commit, no checkout, no branch, no network, no database",
    "no sub-agents",
    "every command exit-bounded; no watch, no tail, no server",
    "no repo-wide grep returning hundreds of hits",
    "snapshot declared in the artifact's own output, per state-your-snapshot"
  ]
}
