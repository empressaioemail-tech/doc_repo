---
id: 2026-08-31_p91_p563_deploy
title: p563 vocabulary and display strings at MCP normalization, serving on smartsite-mcp-00078-fat
date: 2026-08-31
status: serving, VERIFIED LIVE 2026-08-31 by the operator through a reconnected connector (seven-test pass; see "Live verification")
plan_row: P-91 (v3 WDLL V1 to V6 plus the V2 payload half). Q1 selectors deliberately excluded, cortex-blocked by measurement.
service: smartsite-mcp
revision: smartsite-mcp-00078-fat
tag: p563
digest: sha256:ecc72a403c8795849978badb6af6345c1d4c09135995dbfd42a6d0b6cb004790
tree: P:/tmp/legacy-design-tools-p91-stone feat/p91-v3-vocab (from origin/main 8f11e81b), tree verified byte-identical to merged main under src and tests before the image was built
build: Cloud Build d7e367a6-dd06-449a-a833-994d76863832, STATUS SUCCESS, 1m2s, config artifacts/smartsite-mcp/cloudbuild.p563.yaml generated behind the p563-twice-p558-zero guard
pr: empressaioemail-tech/legacy-design-tools #562, merged 03050926a80cc52350374f2a0173ff8a60e9cd65 at 2026-08-31T03:57:43Z (number read from the printed URL, merge SHA read back from gh, neither assumed)
ci: 10 of 10 checks pass, 0 fail, by conclusion string; mergeStateStatus CLEAN and base unmoved at merge time
suite: 452 of 452 (18 files), up from 413 (17); tsc --noEmit clean
cortex: untouched
checkpoints: _inbox/2026-08-31_p91_q1_feasibility_measurement.md
---

# What this image serves

One vocabulary table, owned in one place, reaching both the panel and the model.

The finding underneath it is architectural. The display vocabulary lived only in the served panel, so prose composed anywhere else drifted from the UI by construction. That is how the raw machine token `atom_path_pending` reached user-facing text in a live session: a missing field, not a model failure. Attaching during MCP normalization rather than upstream in cortex is better than merely cheaper, because the panel and the model now read the same table and drift becomes impossible rather than unlikely.

Nineteen entries of token, displayText and meaning, single-sourced by importing the panel's own display strings rather than retyping them, so the two cannot diverge. Published two ways: as an MCP resource, and as a byte-identical standing block appended to every tool result. The block is 3,832 characters, 2.55 percent of the host ceiling, one per call and not per parcel. It is appended last and the panel reads the FIRST text part, so `content[0]` is untouched and no existing consumer moves.

The four states that read alike to a lay reader, `unread`, `absent`, `absent-verified` and `unknown`, now carry distinguishable display strings, and `unknown` never renders as a clear finding.

At the normalization seam: `dispositionDisplayText` on sections and `reasonDisplayText` on overlay reasons (V3); facet-scoped `agentGuidance` generalized from one hardcoded case to all five known facets across every non-present disposition, which arms a mechanism that shipped starved (V4); an explicit derived-figures deny on every surviving draw, because a session computed a shoelace area off `draw.ring` and nothing prohibited it, the same class as the tracked invented 42 percent lot coverage (V5); and unknown-state overlays split an explicit `finding: null` from `label`, so nothing can render "No pipeline within 500 ft" as a result when nothing was checked (V6).

# Verification

Suite 413 to 452, `tsc --noEmit` clean, both re-run by the planner rather than taken from the lane's report.

Planner mutations, chosen independently of the lane's own nine and run after them. Collapsing `absent-verified` onto `absent` fails the four-lookalike-states distinctness fixture, which is the check most likely to have been asserted rather than tested. Making the standing-block wrapper a pass-through fails 3. Dropping the explicit `finding: null` on unknown overlays fails 1. Tree restored and reverified green at 452 after each.

One check the brief did not ask for, added because appending a content part to every result could have shipped as visible junk: the panel's `firstTextPart` returns the first text part and the block is appended last, so there is no paint regression.

Canary deployed at 0 percent pinned to the DIGEST, never the tag. Three pre-shift gates, all pass: the revision's `imageDigest` equals the registry digest for p563; the tagged URL answers healthy with `authConfigured: true`; and `DATABASE_URL` still resolves to the `DEPLOYMENT_DATABASE_URL_DIRECT` secret. That third gate is new to this program and exists because the Neon mitigation was applied by hand: a deploy that silently reverted that binding to the pooler would break every write while health stayed green, which is exactly how the original outage presented. Then `update-traffic --to-tags=p563=100`. Prod `/health` answers on `smartsite-mcp-00078-fat`; traffic by field name is that revision at 100.

# What the lane found in its own first pass

Three vacuous checks, all closed before it reported: a circular fixture asserting the panel printed a constant by comparing it against that same constant; a sentence-initial-only imperative regex that forced a real rewrite of instructional language out of the vocabulary's meaning fields; and a fixture-reuse issue where the input and expected-output fixtures would have been the same object.

That is the fifth consecutive lane in this program to find at least one vacuous check when told to assume it had one. The instruction is now doing more work than the review.

# Live verification, 2026-08-31

The seven-test walk passed in full, operator-reported, through a full connector disconnect and reconnect on a fresh chat. This is the instrument the deploy record was waiting on; before it, p563 was verified at the revision and image level only.

What each test settled, because the pass matters less than what it rules out. **The write succeeded**, which proves the product write path end to end on the unpooled direct endpoint and closes the confirmation owed since the Neon incident. **`atom_path_pending` did not leak**, printing as "Withheld, setbacks unruled"; that exact token reaching user-facing prose is the defect the whole V programme was built to kill, so this is the one test that retires the original finding. **The derived-figures deny held**: no invented square footage and no coverage percentage, which is the same class as the tracked invented 42 percent lot coverage and the failure most likely to be confident and invisible. **The four lookalike states printed distinctly** and `unknown` did not render as a finding. **An unchecked overlay was not stated as a result**, which is V6 working on live data rather than in a fixture. **The multi-parcel canvas composed** three adjacent Bastrop parcels on one aerial ground.

**And the `tools=` channel is now MEASURED.** A held door hover produced the neighbour preview carrying "Not sent to the chat. Claude cannot see this." That was the one open measurement p562 shipped with, declared unanswerable from a fresh panel because the panel makes no unrequested tool call. `serverTools` is real against this host. The p562 record's stated limitation is discharged.

Provenance, stated plainly: this is an operator-reported pass on a live connector, not a planner-run instrument. It is the strongest evidence available for payload behaviour, since the MCP endpoints require auth the planner does not hold. Recorded as what it is.

Q1 selectors are absent by decision, not by omission. Measurement established that `near` and `subdivision` both need cortex routes that do not exist, and the bare-street-name half of `street` does too. The ask is routed at `_inbox/2026-08-31_p91_q1_cortex_ask_property_seat.md`.

The v3 panel remains unverified against `cortex-api-00672-ceq`, which is unchanged by this deploy and still open.
