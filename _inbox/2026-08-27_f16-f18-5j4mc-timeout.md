---
id: 2026-08-27_f16-f18-5j4mc-timeout
title: 5j4mc persist-zero at 3600 s timeout
date: 2026-08-27
last_updated: 2026-08-27
status: recorded
plan_row: F-16
execution: factory-conformant-5j4mc
run: 80d375af-6208-4d39-b3dc-43bb7b0d05cf
---

# 5j4mc timeout sit (amendment e)

Execution `factory-conformant-5j4mc` started 2026-08-27T12:23:51.699254Z on digest `hauska-factory@sha256:2af32780ce93b810eccb33c017007086c7099f4c561173cd6299ebd850c45d61`. Args `conformant,--apply,--replay`. Completed 2026-08-27T13:24:12.866557Z. Cloud Run: failed, exit 0, message "The configured timeout was reached."

Factory run `80d375af-6208-4d39-b3dc-43bb7b0d05cf` phase `stage-e-conformant` status `started`. No termination row (the process was killed). `run_events` 0. `aliases` 0. `write_stage_atoms` staged since 12:23Z: 0.

Factory `pg_stat_activity` during the sit was idle except `factory-control` `/gates`. Last job log 12:24:02Z (pg SSL warning after connect). Landing SELECT had already gone idle on earlier cancelled runs with the same query; this execution never reached `persistAliases`.

Where it sat: in-memory `pipelineFromRows` (first pass, or the replay second pass on the shared store) after `loadBastropLanding`. `adaptResolveMs` was never written because that clock is taken only after both pipelines return.

Old-shape `cad-parcel-roll` 48021 still 77,073 at 13:24Z (`hauska_mcp`, not `conformant-v1`). Flood stays blocked.

Expired Factory lease for this run (`taken_at` 12:24:02Z, `expires` 12:39:02Z) deleted 13:25Z. Recorded here. Next execute may steal if another orphan appears.

O(n squared) is not fully gone. Remaining hypothesized quadratic on this digest: `store.aliases.find` on every `resolveCandidate` (T2 skip, reconcile `openByKey`, and `aliasesByKey` match already landed; dedupe still scans the full alias array). Replay doubles that scan.

Next execute waits for per-leg clocks that split landing / pipeline1 / replay. No `--apply --flood` until those legs exist and the old 48021 count is still 77,073.
