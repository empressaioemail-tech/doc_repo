{
  "id": "p266p268-dryruns",
  "path": "P:/tmp/p266p268-tools/out/dryrun-progress.log",
  "maxQuietMin": 25,
  "note": "P-266/P-268 lane (p266p268-lane): the six-county dry runs of parcel-record-fill (no --apply) plus the parcel-r5-zoning dry run, which are this lane's Falsifier-1 evidence and the source of its per-county/per-rail predictions. READ-ONLY against cortex (default_transaction_read_only=on) under a heavy-scan lease taken through doc_repo's scripts/heavy-scan-lease.mjs; no writer, no --apply, no store write. Each county appends one line to the progress log, so a watcher sees a stall as a quiet log. Registered BEFORE the first run started, per AGENT CONTRACT section 5. This watch can be removed by the planner at close.",
  "startedAt": "2026-09-17T14:05:00Z"
}
