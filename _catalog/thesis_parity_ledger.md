---
id: thesis_parity_ledger
title: Thesis parity ledger — real estate spine vs trading cockpit
status: active
last_updated: 2026-08-17
applies_to: portfolio
related: [09_post_saas_substrate_thesis, 25_atom_architecture_reference, 80_adrs/adr_018_atom_contract_substrate_layer, 80_adrs/adr_028_contract_cross_vertical_adoption, _decisions/2026-07-20_cross_vertical_parity_program, _dispatches/2026-07-20_trading_cockpit_parity_adoption, _catalog/repo_intents]
---

# Thesis parity ledger

The atom and calibration thesis is implemented twice: the real estate spine (Hauska/Empressa stack, TypeScript/Postgres, repos hauska-atom-contract, hauska-engine, legacy-design-tools, hauska-mcp-server) and the trading vertical (Empressa Cockpit, Python/Postgres, repo empressa-trading, path `apps/cockpit/backend/app/spine/`). The two share zero code and share one theology. This ledger is the single document that keeps the two representations in sync.

Rule: when either vertical lands a finding, breakthrough, or improvement in a spine-model concept, one entry lands here (a parity-table row update plus a findings-log entry). The other vertical's next planning session reviews unadopted entries. The trading repo feeds this ledger via `docs/PARITY_NOTES.md` in empressa-trading; the doc_repo planner reconciles that feed into this file. Nothing else about the two systems is kept in sync; they remain separate ventures with separate infra (per `_catalog/repo_intents.md`), sharing only the atom spec and this ledger.

## The shared theology (implementation neutral)

1. All knowledge is captured as atoms: append-only observations with provenance, citation, confidence, access policy, and time.
2. Confidence is typed with a basis (asserted vs earned); earned claims require proof; asserted claims carry provenance and are never displayed as bare numbers.
3. Predictions pair with outcomes in an append-only evidence ledger; calibration is computed from the ledger with an honest fallback when sparse; calibrated confidence feeds back into serving.
4. Derived atoms carry lineage to their input atoms; calibration without lineage is rejected.
5. Access policy is enforced at one gate; tenant-private data never pools into shared or public aggregates.
6. Data-source license terms are atom-level facts enforced mechanically, not doc-level policy.
7. Absence claims state where the system looked (verified absence), never bare "none found".
8. Application surfaces are composed tiles over the spine; a workspace is a chosen set of tiles; nothing is rebuilt per customer.
9. Functions ship dual interface: app UI plus agent-consumable tools.
10. Capture runs on durable, idempotent, observable jobs, never fire-and-forget scripts.

## Parity table

State audited 2026-07-20 from source (four audit reports: empressa-trading full read; atom-contract at `25215fd`; hauska-engine content-identical to origin/main; legacy-design-tools at `7e6152b2`; hauska-mcp-server local HEAD b8e51d6). Verify against live source before acting on any row older than the concept it describes.

| Concept | Real estate state | Trading state | Canonical best | Adoption owed |
|---|---|---|---|---|
| Shared contract artifact + conformance validator | EXISTS: `@empressaio/atom-contract@1.7.0`, `validateAtomConformance` enforces invariants | ABSENT: model implicit in Pydantic (`app/spine/types.py`), no validator | Real estate | Trading (dispatch item 1) |
| Append-only store, single write path | IMPROVING: contract hash-chained event ledger for data-tier; **Postgres StoragePort is the retrieval serve path** (`PgStorage` on Neon `hauska_mcp`, retrieval `00016-ttp` postgres-only boot — snapshot-heap path retired G2 / F1 Phase 0); property + code atoms in `atoms` | EXISTS: `append_atom` sole write path, no update/delete functions | Trading write path + RE conformance enforcement | RE (full retire-not-overwrite audit; finish offline code-link load); Trading (conformance) |
| Confidence basis as a type | EXISTS, richer: `asserted\|backtest\|seed\|live` + three-axis split, bare scalars unrepresentable | EXISTS: `asserted\|backtest\|live`, fail-closed backtest proof (`BacktestBasisError`) | RE vocabulary + Trading fail-closed proof rule | Trading (seed basis); RE (wire read-back beyond findings surface) |
| Outcome evidence ledger | EXISTS with defects: hash-chained but no negative outcome kind (score degenerate at 1), grain (tenant, citedAtom), full-scan reads | EXISTS: `right\|wrong\|edited\|partial\|dismissed` + realized R score, (claim_type, worker) grain | Trading vocabulary and grain + RE hash chain | RE (negative kinds, generic grain, projection worker) |
| Calibration compute | EXISTS: prior-weight shrinkage, adaptive atom-vs-class grain, amendment-hazard decay | EXISTS: Beta-Binomial, flat grain, MIN_N=8 cliff | RE adaptive grain + hazard decay; Trading generic API shape | Trading (adaptive grain, hazard decay replacing vintage enum) |
| Lineage DAG | PARTIAL: citations jsonb without FK; deposit-to-atom leg unpersisted (the M1 gap) | EXISTS: `atom_input_lineage`, mandatory for calibration atoms, fail-closed at serving | Trading | RE (contract field + engine persistence); Trading (extend to all derived-family atoms) |
| Bitemporality | FRAGMENTS: ldt `knowledge_atoms` has valid_from/valid_to/knowledge_at; contract has valid_from only | EXISTS: valid_from/valid_to/knowledge_time + captured_at on every atom | Trading | RE (contract 1.8.0) |
| License/redistribution enforcement | ABSENT in all repos | EXISTS: per-feed license block, gate enforces accessPolicy x license most-restrictive | Trading | RE (contract 1.8.0 + gate enforcement) |
| Verified absence | PARTIAL: property `zoning-fact.absence` (`no-zoning-stamp`) live on named Bexar parcel via atom path; not yet a cross-family typed absence primitive | EXISTS: `evaluated` requires non-empty `provenance_scope`, store-enforced | Trading typed absence + RE property honest-absence | RE (generalize absence type beyond property); Trading (watch property pattern) |
| PII policy | POINT FIXES: PMTiles bake excludes owner_name, logger redact; no flags, ownerName flows unredacted in adapters | EXISTS: PII atoms rejected, separate encrypted per-subject store, crypto-shred erasure | Trading | RE (flags in 1.8.0; policy layer follows) |
| Consent | PARTIAL: workspace share edges only; export enforcement open (BLOCKED-54) | EXISTS: first-class consent, aggregate-pooling gate on calibration inputs | Trading | RE (when person-adjacent atoms arrive; tier 3) |
| Anchoring / audit integrity | ABSENT: contract TODO(M2-C); MCP audit sink has no integrity chain, drops rows silently | EXISTS, dormant: Merkle tree, root-chained batches, OpenTimestamps; deliberately unscheduled | Trading module | RE (harvest as shared audit-integrity layer); Trading (keep module cleanly bounded, WDLL before enabling) |
| Capture-plane orchestration | PARTIAL: engine pipeline-runner state machine designed, in-memory JobPort only, no worker/heartbeats/scheduler; ldt report_run + watchdog but in-request async and in-memory Maps | EXISTS: DB-backed JobRun queue, separate worker container, heartbeats, idempotent 900s scheduler | Trading | RE (Postgres JobPort + worker + scheduler) |
| Vendor hygiene + quota metering | PARTIAL: fallbacks and retry budgets exist; no per-vendor usage/quota ledger | EXISTS: fallback client, real/fake client pairs, `VendorUsage` daily metering | Trading | RE (VendorUsage analog) |
| accessPolicy | EXISTS: five-value union, gate-enforced for catalog reads, tenant-default-private; MCP free-anonymous rpm is a wall-clock UTC minute bucket (L19 2026-08-14: 429 band=rpm, first_429 72 when a ramp straddled :00) | EXISTS: four-value (missing tenant-shared analog), gate-enforced with license intersection | RE union + Trading license intersection | Trading (tenant-shared); both: name window type on rpm 429s |
| Tile/capability registry | EXISTS: serializable `TILE_CAPABILITIES` (requires/produces/modes/mcpTools), served registry endpoint, CI drift-lock, `compose_workspace` | PARTIAL: code-internal `TILE_DEFS` array in `FocusShell.tsx` | Real estate | Trading (dispatch item 4) |
| Dual interface (MCP) | EXISTS: 63 tools, four gates, key-resolved at call time | ABSENT: co-pilot tool loop is in-process only | Real estate | Trading (read-only MCP surface, dispatch item 5) |
| Current projection vs full-history reads | MIXED: supersession projections exist; calibration read is a full-table event scan per finding read | EXISTS: `CurrentAtom` projection per (node_id, claim_key) | Trading | RE (calibration projection worker) |

## Findings log (append-only)

- 2026-07-20 (origin: both). Cross-vertical audit executed: four source-level audit reports established this table. Real estate strengths confirmed: contract + conformance, confidence type vocabulary, adaptive-grain calibration with hazard decay, capability registry + drift-lock, MCP gate. Trading strengths confirmed: outcome vocabulary with negative kinds, license enforcement, verified absence, bitemporality, consent, PII split store, anchoring, durable job queue.
- 2026-07-20 (origin: real estate, adoption: trading SENT). Adoption dispatch hand-carried to the Cockpit agent (8 items: conformance module, seed basis + hazard decay, adaptive calibration grain, registry-as-data, MCP surface, derived-family lineage, anchoring module boundary + enable-WDLL, HANDOFF.md retirement). Record: `_dispatches/2026-07-20_trading_cockpit_parity_adoption.md`.
- 2026-07-20 (origin: trading, adoption: real estate PROPOSED). ADR-028 drafted: contract 1.8.0 additive field set (license block, verified absence, valid_to + knowledge time, typed outcome family with negative kinds, instance-level input lineage, PII flags). `80_adrs/adr_028_contract_cross_vertical_adoption.md`.
- 2026-07-20 (origin: trading, adoption: real estate QUEUED). Infrastructure adoptions named, not yet planned in detail: Postgres JobPort + worker + heartbeat + idempotent scheduler on the engine capture plane (Cockpit `app/jobs/` is the template); calibration projection worker in ldt; negative outcome kinds in the ldt ledger vocabulary; per-vendor usage metering; gate T1 fail-closed when signing key expected.
- 2026-07-23 (origin: real estate PE map-truth / planning feedback). Zoning→setback→envelope reasoning will become a first-class atom family (fact/rule/derived) with contract 3-axis confidence, authored in **hauska-engine** (spine), not cortex-api; sprint-56 reasoning lift is the atom refactor. Matcher binary invent (prefix/fallback) to be replaced by graded asserted confidence + honest absence. Hold cortex-local atomization. Decision: `_decisions/2026-07-23_pe_envelope_atom_spine_and_post_map_truth_pickups.md`. Trading: watch for same "derived envelope / rule-with-condition" pattern when Cockpit builds analogous composed claims.
- 2026-07-23 (origin: real estate, Gate C halt). Property atom fabric live behind dual-serve: `@empressaio/atom-contract@1.9.0` `./property` (zoning-fact / setback-rule / buildable-envelope) reuses ObligationAtomInstance + actor-record (no SourceAttribution); engine emitters + StoragePort bake on Hays `48209:156346` + Bexar honest-absence `48029:410119`; retrieval `GET /property-nodes/:id/atom-chain` on `hauska-retrieval-api-00014-84n`; MCP catalog tool `get_property_atom_chain` (gate `access_policy`) on `hauska-mcp-server-00023-j4s`. cook_county_il_stub golden CI green. **PE live read not flipped** (cortex `00428-fax` still serves). Overlay read-through (I-E serve) and SDK money boundary still open. Check-in: `_inbox/2026-07-23_GATE_C_checkin_property_atom_path.md`. Trading: adopt typed fact→rule→derived chain + honest-absence slot when composing rule-conditioned claims.
- 2026-07-23 (origin: real estate, Gate C cutover + Gate D stop). PE dual-serve flipped: `property-explorer-xi` BFF `X-Pe-Read-Path: atom-chain` on Hays+Bexar; cortex `00428-fax` kept warm. I-E hard bar: retrieval `00015-2x8` resolves calibratedConfidence from cortex Neon `atom_calibration_overlay` (0037) at READ (Hays envelope 0.71/backtest vs asserted 0.88). I-F: MCP `00025-bkp` `SDK_METERING=1` calls `McpMeteringGate.authorizeCall` authorize-time; Stripe meter path retired; public-free SDK-free. Circle overage PARTIAL. Closes: `_inbox/2026-07-23_GATE_C_cutover_close.md`, `_inbox/2026-07-23_GATE_D_checkin_sdk_money_boundary.md`. Trading: authorize-time metering gate + overlay read-through pattern.
- 2026-07-23 (origin: real estate, Phase-1 finish hand-back). Anti-zombie: LDT #351 / map #49 / engine #103 — multiply retired; PE envelope `atom-chain|atom-pending` only; gold bake 8 parcels/16 atoms. I-K inbound ICC meter on MCP `00026-fn5` (free_anonymous ledger). Permit-outcome Austin SODA adapter → overlay 0.73/backtest. Circle settlement still operator-seed. Phase-2 NOT opened. Check-in: `_inbox/2026-07-23_PHASE1_FINISH_checkin_property_reasoning_substrate.md`. Trading: inbound per-reference source meter + public-record outcome→calibration fuel.
- 2026-07-23 (origin: real estate, Phase-1 anti-zombie close / Master WDLL 3.7 I-A). Three-orchestrator multiply path retired in one cut: LDT derive/route/Tier-1/Tier-2 + CI grep gate (#351 → cortex `00430-hiz`); PE cortex envelope fallback removed (#49 → Vercel prod); named Central-TX gold atom bake 8 parcels / 16 atoms (engine #103). Live: Hays+Bexar+Travis atom-chain; missing atoms `X-PE-Read-Path: atom-pending` / `atom_path_pending` (no invent). Close: `_inbox/2026-07-23_GATE_C_anti_zombie_close.md`. Trading: never leave a second confidence composer when migrating a derived claim onto the atom chain.
- 2026-07-24 (origin: real estate, Gate X stop — terrain-export). Derived `parcel-terrain-model` / format-parameterized terrain-export atom in `@empressaio/atom-contract@1.10.0`; DEM as referenced-field (USGS 3DEP, not atomized); one shared triangulation → GLB + IFC4 `IfcTriangulatedFaceSet` + DXF 3DFACE + DXF contour polylines; LandXML TIN honest-defer; `accessPolicy: public-paid`; asserted confidence with provenance. Live on engine-api `00065-xox` for `48021:27303` (WDLL Gate X check-in; SDK metering / MCP catalog still Gate Y). Trading: watch format-keyed derived artifact map + reference-field DEM pattern when Cockpit emits multi-format derived exports.
- 2026-07-24 (origin: real estate, Gate Y stop — terrain-export). Pre–Y: adaptive DEM resolution (16px floor auto-tighten / honest decline) + GCS durable artifacts (`TERRAIN_ARTIFACT_BUCKET`). MCP catalog tool `refresh_parcel_terrain_export` on `00027-d95`: anonymous withheld; paid path one `authorizeCall` per export request (not per format); USGS I-K N/A. Check-in: `_inbox/2026-07-23_GATE_Y_checkin_terrain_export_sdk.md`. Surfaces + cortex retirement held. Trading: one-meter-per-derived-export (not per artifact) + durable blob refs for paid downloads.
- 2026-07-24 (origin: real estate, terrain-export surfaces finish — WDLL 7–9). Cortex mesh/IFC authoring retired (I-A): LDT #352 → cortex `00432-bob` returns 410 + spine pointer; grep-clean. Command Center Parcel Terrain Model tile (`parcel-terrain-model`, property-id + format) live at `cmdcenter-blush.vercel.app`; PE inspect card **Terrain export · public-paid** at `property-explorer-xi.vercel.app/?parcelNodeId=48021:27303`; both proven on `48021:27303`. Finish: `_inbox/2026-07-23_terrain_export_surfaces_finish_checkin.md`. Trading: retire product-local derived authoring when spine catalog tool is paid-live; surface only the atom.
- 2026-07-24 (origin: real estate, DXF Revit-import follow-up — WDLL item 10). Contour/3DFACE emitters now write AutoCAD 2000 HEADER (`$ACADVER=AC1015`, `$INSUNITS=6` meters) + LAYER tables; docs at `hauska-engine/.../parcel-terrain/CAD_IMPORT.md` (Link CAD → floor/site plan). Engine #109 → `hauska-engine-api-00069-rey`; PE Revit hint #54. Trading: bare ENTITIES-only DXF is not Revit-safe — always stamp version + units.
- 2026-07-24 (origin: real estate, IFC data-completeness — WDLL item 11). Terrain IFC must be a complete spatial model (`IfcProject→Site→placed IfcGeographicElement` + `IfcMapConversion`), not a floating mesh. Engine #112 → `hauska-engine-api-00075-vih`; fail-closed `validate_spatial.py` / worker assert. DXF #111 left structurally sound. Trading: derived BIM exports fail closed on empty spatial trees — do not ship unreachable elements.
- 2026-07-24 (origin: real estate, elevation integrity — WDLL item 12). USGS 3DEP Z declared as NAVD88 orthometric metres on IFC `VerticalDatum` + DXF comments; nodata/zero-fill dropped before triangulate; mesh min-Z asserted against DEM band; DXF `$EXTMIN`/`$EXTMAX` patched past ezdxf sentinel reset. Engine #113+#114. Trading: bare elevation numbers without datum are not survey-grade — stamp NAVD88/orthometric/metres on every BIM/CAD export.
- 2026-07-24 (origin: real estate, Central-TX property-atom breadth).
 Full Tier1 denom bake across 10 metro counties (~2.48M atoms) into StoragePort with honest-absence rate monitors + live spot-audit (zero ledger over-claim); geometry ceiling corrected to include Guadalupe/Bell/McLennan (live `txgio_parcel`). Permit-outcome fuels broadened (Austin, San Marcos, San Antonio, Cedar Park, New Braunfels). Master 3.12 MET; 3.10 still PARTIAL (bastrop/grand + countywide asserted-only). Phase 2 NOT opened. Milestone: `_inbox/2026-07-24_BREADTH_COVERAGE_MILESTONE_central_tx.md`. Trading: county-scale honest-absence rate as a control-plane metric, not a silent null.
- 2026-07-25 (origin: real estate, site-plan export Waves 1–3 code merge — live finish BLOCKED). Extends `parcel-terrain-model` with shared `SitePlanModel` → layered DXF/IFC (closed solid mass, site-plan path) + PDF (summary + provenance + exact honesty line); formats `dxf-site-plan`/`ifc-site-plan`/`pdf-site-plan`. Engine #116+#117; MCP sibling `refresh_parcel_site_plan_export` (#48) reuses terrain `authorizePaidCall` (one meter/request, no new SKU); PE inspect `SitePlanExportSection` (#56). Representative parcel `48029:105129` (not terrain-gold P-3). Finish check-in blocked on engine/MCP/Vercel deploys + live TxGIO/3DEP sample + Revit. Spec `75o`; WDLL `_inbox/2026-07-25_site_plan_export_WDLL.md`. Trading: one shared site model for CAD+PDF (cannot diverge); provisional buildable honesty when front-edge is heuristic; pay-gate sibling tools over new SKUs.
- 2026-07-25 (origin: real estate, F1 Phase 0 / Gate A — retrieval restore). Serving path reads Postgres only (`PgStorage`); in-memory snapshot boot retired (G2). Live: retrieval `00016-ttp`, `/healthz` corpus 3.61M from `storage:countAtoms`, PE `X-PE-Read-Path: atom-chain` on `48209:156346`. Coverage = live SELECT: Travis zoning_present **61.23%** (settles 5.8%-vs-61% prose dispute). Check-in: `_inbox/2026-07-25_GATE_A_checkin_f1_phase0_retrieval_restore.md`. Trading: never serve a growing corpus from a heap-loaded snapshot; ledger tally is a live query, not a bake summary.
- 2026-07-25 (origin: real estate, F1a / Gate B — console audit). Backend-healthy ≠ app-correct: PE inspect reads live atom-chain but stamps bake `fetchedAt` as "gate-passed"; Buildable/"not verified" fires when `buildableAreaPct` absent even with live zoning+setbacks (P-3 + Travis SF-2); site-plan BFF masks MCP/engine errors as "MCP response missing parcelNodeId" (`callMcpTool` drops `isError`; G6 regex drift MCP `\d+` vs PE `[^/]+`). CC LIVE/STUB badges are hand-set registry constants. Audit: `_inbox/2026-07-25_f1a_console_audit.md`. Trading: never conflate provenance timestamp with read-path health; never collapse transport errors into "missing id."
- 2026-07-25 (origin: real estate, F1b / Gate C halt). Node & Graph LIVE on shared `fetchAtomTrace` (no second tracer); parcel↔node binding via hash `node=`; `liveGis` de-forked into `@hauska/map-renderer` source; G6 id regex unified PE↔MCP (`^\d{5}:[^/\s]+$`) with CI assert; QA-3 facet vocabulary `present|absent|pending` (setbacks live ≠ "not verified"); Calibration honest STUB. **PE customer Map|Ledger flip held** (WDLL 5). Check-in: `_inbox/2026-07-25_GATE_C_checkin_f1b_wire_ledger_substrate.md`. Trading: one shared GIS/trace client across operator+customer surfaces; never badge LIVE on fixture zeros; distinguish honest-absence from pending-derived.
- 2026-07-25 (origin: real estate, F1b Gate C step-2 close). Node-inspect root-caused: CC SPA rewrite stole `/api/spine/*` (HTML 200) AND `/atoms/trace` 404s for property atoms that exist on atom-chain — ledger slots now `fetchPropertyAtomChain` (same path as PE). CC spine rewrite restored; PE Map|Ledger dock shipped to `property-explorer-xi` (WDLL 5); cold-start retry on atom-chain. Check-in updated same file. Trading: property ledger present/absent MUST read atom-chain (or StoragePort tally), never a graph-trace endpoint that 404s on leaf property atoms; SPA rewrites must never shadow `/api/*`.
- 2026-07-25 (origin: real estate, F1b Gate C customer-ready). Two-products correction: PE county ledger dock removed (operator balance sheet stays CC-only). Transient atom-chain failure must never render as honest-absence ("not verified") — BFF retries then 503 retryable; UI stays on loading. Operator export bypass shared terrain↔site-plan (`PE_EXPORT_DEV_BYPASS`). Check-in: same Gate C file. Trading: failed-read ≠ absence; customer surface must not show internal coverage tallies.
- 2026-07-25 (origin: real estate, F1 Gate C CLOSED). "Bounce" was a misdiagnosis — facets atom-chain 200 deterministic; the reload console error was `POST .../gtm/property-explorer/consent` **403** from PE spine anonymous browse gate. Fix: `gtmClient` → `/api/pe-gtm` (consent 200 / events 201). Soft-422 site-plan copy kept anti-fabrication. Setback S0'/R0'+corner lots stay post-F1 pickup. Check-in close: `_inbox/2026-07-25_GATE_C_checkin_f1b_wire_ledger_substrate.md`. Trading: funnel instrumentation must use its keyed BFF, never ride the anonymous browse proxy; console 403 ≠ data-plane bounce.
- 2026-07-25 (origin: real estate, F1c CLOSED — WDLL 7-9). Mechanical panel badges from live probes (degraded on backend fail; fixture-LIVE forbidden). Live smoke RED on unavailable atom-chain; GREEN gated. Coverage = live `GET /stats/central-tx-node-graph` (retrieval `00017-gns`, Travis 61.23%). Check-in: `_inbox/2026-07-25_GATE_F1c_checkin_mechanical_honesty.md`. Trading: never hand-set LIVE; never serve coverage from a committed artifact the DB can contradict.
- 2026-07-25 (origin: real estate, site-plan setback export gate). `not_specified` (code silent / build-to-line) is a valid export state, not `setback_rule_missing`. Engine #121 wires parcel-terrain routes to Postgres; #122 includes `parcel-terrain-model` in `listPropertyAtomsByParcelNodeId` (refresh-without-download loop). Live: `hauska-engine-api-00086-hoz` @ 100%; `48021:47595` PDF/DXF disclose F 15' + S/R not specified; true-missing still 422. Check-in: `_inbox/2026-07-25_setback_export_gate_false_refusal_checkin.md`. Trading: silence ≠ absence at every consumer; StoragePort list filters must include every entity type the write path persists.

- 2026-07-25 (origin: real estate, 27 master WDLL + M0 fleet memory). Scoped 27a/27b/27c + dev-fleet memory as ONE program with a nested done-line; reframed 27c as the DEPTH engine over 61a breadth (depth ratio verified-envelope/zoning-fact ~0.1% today). New spine concepts: the ROAD as a first-class node (centerline/edges/ROW/classification/provenance + attach points) = the geometric skeleton the aerial calibrates to AND the digital-twin foundation jurisdictions publish infra onto (uniform public-record sourcing, no relationship-privileged path); setback rule indexed by (road-class, edge-role) in the descriptor (jurisdiction-agnostic engine held). Memory as architecture: geometry calibration (gate for one answer), warm ledger (gate for parcel knowledge), M0 fleet memory (gate for build knowledge), confidence calibration (temporal depth) are one shape — cheap-can-be-wrong separated from promoted-provably-right by a mechanical, planner-gated promotion; strongest durable form is a TEST not prose. M0 installed as sprint zero (runbook + .cursor rule + _scratch convention). Commit `7000cda`. Trading: a wrong answer that re-derives every request is a memory failure not a math failure; promote the fix to a mechanical guard so it cannot silently rot.
- 2026-07-27 (origin: real estate, CC-A closed — Control-Tower organism port). Property Command Center (`cmdcenter-blush`) now ships a **walkable node/atom organism** ported from trading Control Tower `NodeGraphBrowser` + `AtomInspector` (not a re-derived UX): node-type-generic structured cards (parcel / road / property-boundary-edge), clickable edges OUT/IN, atoms-by-family → property-rich inspector with confidence `{n,width,basis}` never bare, `return=node-graph` back-nav. Live Amendment 1 walk MET on golds `48021:28286` / `33512` / `34785` (bundle `index-BTKuoNXu.js` @ map `91c6268`; retrieval `00033-wom`). Shared PE layered map chrome on Site Analysis (fork eliminated). Mechanical guard: `NodeGraph.smoke.test.tsx`. WDLL `_inbox/2026-07-27_CC_A_legible_node_atom_flow_WDLL.md` **closed**; verify `_inbox/2026-07-27_CC_A_post_deploy_planner_live_verify.md`. Trading: remains the reference organism — future RE surfaces must port, not invent; when Cockpit evolves the card/inspector shape, RE owes a parity re-walk.
- 2026-07-27 (origin: real estate, RECIPE-PROOF Caldwell #2 — county-onboarding mold measurement). County-onboarding recipe (27d) run on non-Bastrop FIPS 48055 as a MEASUREMENT (27f Amendment 2): **7/8 gates HELD**, **1 new decision baked** — **UNREACHABLE-CITY-GIS** (DNS-fail city GIS = absent source → OSM best-available; never invent authoritative). Live: depth_warm 337/5027=6.70% (cohort proof), roads 5171, cost ~$0.39 under commitment #3; M0-reach miss=none. Engine PR #145. Check-in: `_inbox/2026-07-27_RECIPE_PROOF_caldwell_planner_verify_checkin.md`. Trading: jurisdiction fan-out recipes must treat unreachable data endpoints as verified-absence of that source, not empty-authoritative; measure mold generalization as N held / M new-baked, not pass/fail.
- 2026-07-27 (origin: real estate, PROPERTY-LINE-TAGS on boundary primitive). Bastrop `property-boundary-edge` atoms carry GIS-computed bearing+distance (`propertyLineTags`, kind `gis-approximate`, honesty "not a survey"); shared `gis-property-line-tags` module with site-plan PDF (no formula drift). Live 26454/26454; gold 28286 reciprocal ~60×137′. Engine PR #150. Survey-grade remains v2/courthouse. Trading: derived geometric labels on graph edges must carry asserted GIS provenance and never display as survey-grade.
- 2026-07-30 (origin: real estate, BDC downtown drill R9/R10 mold gates). **R9 parcel-currency:** dead/re-platted `prop_id` must surface as superseded (re-key manifest), never silently-nulled envelope — BCAD existence check before warm; spatial layer-23 fallback when city GIS lags cadastral (specimen 34065 → Pecan Place 8741972/8741974). **R10 persisted==recompute:** cert asserts live PE `atom-chain-warm` matches fresh recompute (not stale declined); force-repromote skips stale boundary edges. Downtown drill **37/37 area-sweep PASS** (later superseded — false for map). Decision AMENDMENT 6; mold gates (h)(i). Trading: cadastral churn is constant at scale — onboarding must verify parcel identity before warm; persisted derived atoms must be re-gradeable against live recompute.
- 2026-07-30 (origin: real estate, BDC downtown CLOSE CLASS R13/R14/R15). **R13 repealed regime unreachable:** descriptor-fixture / breadth-bake / repealed-B3 setback path hard-gated fail-closed — missed parcels honest-decline (`setback-rule-pending`), never serve dead F15/S0/R5 (specimen 8723767). **R14 cert scope = rendered set:** area-sweep queries live BCAD bbox (PE path), not curated manifest alone; lookup misses flag fail-closed. **R15 re-plat completeness:** enumerate ALL cadastral successors (34065 → 8741972/8741973/8741974). Rendered-set sweep **36/36 PASS** on `property-explorer-xi` post deploy. Mold gates (j)(k)(l). Blast radius: county-wide ~10k+ nodes now fail-closed without layer-23 atoms — county re-warm is follow-on. Trading: cert must grade what the customer map renders, not a planner list; superseded edition atoms must be mechanically unreachable at serve time.
- 2026-08-08/09 (origin: real estate, Atom Families BUILD wave). Contract `@empressaio/atom-contract@1.14.0` adds `flood-hazard-fact`, `cad-parcel-roll`, `land-use-fact` with first-class typed absence (`verifiedAbsence` + `provenanceScope` fail-closed on `sourceTier: absent`; owner fields gated by `joinPassedOwnerMatchGate` on CAD roll). Engine registration PR #286 open (length 10). Specify-only (not published): owner public-paid, MUD membership, RRC via existing O&G + `atom_links`, soil-survey-fact. Adversarial PARTIAL HOLD: shapes on npm ≠ served atoms; no writers; **NFHL bulk table NOW LOADED (198,178 rows 2026-08-09)** — writers / MCP / manifest still open. Reports: `_inbox/2026-08-08_ATOM_families_program_report.md`, adversarial review sibling. Trading: never publish a contract shape without same-wave engine register + named writer + surface advertisement — UNPUB is a three-leg defect, not a package-version bump.
- 2026-08-09 (origin: real estate, multi-shapefile truncation / capture honesty). TxGIO land-parcel ingest used `files.find(/\.shp$/i)` and silently discarded every shapefile after the first. Harris 48201 (east+west halves) loaded east-only; every count gate agreed with the truncated input. Fix: discover all parts, fail closed unless `--multi-shp=concat`, never silent discard (PR #404). Grade is geographic (post-apply westmost vs Census), not row count. Sweep: only Harris among 254 StratMap archives is multi-`.shp`. Report: `_inbox/2026-08-09_MULTI_SHP_TRUNCATION_report.md`. Trading: any multi-file capture that takes `find`/first-match without naming discarded members is an honesty defect; completeness instruments must not share the truncated reader.
- 2026-08-12 (origin: real estate, B2 Wave 4 parcel-node sweep). Wave 4 reprojection landed 57 counties in `txgio_parcel` but geometry ledger still scored `not-yet` until parcel-node atoms existed. B2 sweep wrote **590,212** `parcel-node` atoms across those 57 FIPS using versioned `run_sweep.mjs`; geometry satisfied **194 → 251** under P1-2 accounted denominator (prop_id folding — raw feature_index would under-score seam-heavy counties). Adversarial: count+extent join on 48011/48301/48065 — no multi-shp truncation signature (atoms span full county bbox via `geometryStoreRef` → txgio join). Close: `_inbox/2026-08-11_B2_wave4_parcel_node_sweep_close.json`. Trading: ledger satisfaction must score written atoms against accounted parcel universe, not raw source feature count when account folding is intentional.
- 2026-08-12 (origin: real estate, Z1 zoning probe sweep / Factory 1.5). Zoning acquisition for 466 cities (Central TX + Dallas + Houston metros) stages into `tx_zoning_district_staging` (H1 seam) without atoms writes. **Verified absence must be SEARCHED-AND-ABSENT with probe notes** — `zoning_gis: null` and `NO_SEED_UNPROBED_DEEP` are the same never-looked defect (theology #7). Live: 104 found / 362 absent / 103 cities staged (318,832 rows); houston-tx correctly absent (no Euclidean zoning); san-antonio-tx found but size-deferred. Close: `_inbox/2026-08-12_Z1_zoning_probe_sweep_close.json`. Trading: absence claims require named look-locations; a "no seed" skip is not verified absence.
- 2026-08-14 (origin: real estate, L19 / P-29 DC-11b MCP limiter load test). Access-policy meter at the MCP gate is a **wall-clock UTC minute bucket** (`Math.floor(now/60000)`), not a sliding 60s from first request. Live on serving `hauska-mcp-server-00063-fic`: anonymous `POST /mcp` initialize 429 `band=rpm` / `tier=free_anonymous`; first 429 at cumulative 72 when a 1 rps ramp straddled :00 UTC (pre-registered 50-70 missed; cap still 60/min/IP). 36/36 `/health` samples `primary=postgres memory_fallback=false`; 20 rps after cap 301/301 429; zero 503. Artifact `_inbox/2026-08-14_76j_C4_loadtest_results.json`. Trading: a per-identity rpm cap must name the window type (calendar minute vs sliding) in the 429 body or clients will treat leftover-plus-fresh-60 as a broken cap.
- 2026-08-14 (origin: real estate, L22 / P-26 utility who-serves staging). Provenance identity across source editions requires **subject crosswalk evidence**, not ID or normalized-name matching alone. The TCEQ AGOL sibling (2,125 features) produced 14 same-name/different-ID candidates against `tx_special_district`; equal-area polygon overlap verified 13 as the same subject and rejected one despite matching name/county/type (Montgomery County MUD 140 overlap coefficient 0.262866). Crosswalk was filed before duplicate staging rows were removed; the rejected pair stays additive. Final acquisition-only staging: PUCT water 3,925, sewer 1,455, HIFLD electric 139, TWDB PWS 4,590 valid geometries plus 31 explicit empty-geometry exclusions, TCEQ additive 87. Close: `_inbox/2026-08-14_l22_close.json`. Trading: when feed editions change IDs, persist alias lineage backed by domain evidence before deduplication; same label is a candidate, not proof.
- 2026-08-14 (origin: real estate, L21 / P-25 CAD cross-vintage key mapping). Declared-vintage parcel reads may remap a 2025 key to a 2026 key only through the blessed resolver seam via a deterministic one-to-one `cad_property_vintage_crosswalk` (exact declared-year first; crosswalk on miss; refuse ambiguous). Measured parcel-linked coverage after seed: Tarrant 87.78% HOLD / Dallas 92.04% HOLD vs 99% flip gate; Tarrant's dominant orphan class was a load-filter defect (RP C/P dropped), not a key map. Vintage flips stay planner-gated; after-flip owner/landuse/cad-roll re-applies queued only. Close: `_inbox/2026-08-14_l21_close.json`. Trading: edition flips that change primary keys must pre-register deterministic alias coverage and named residuals before declaring the new edition current — never manufacture gaps by unilateral flip.
- 2026-08-14 (origin: real estate, L21 follow-up 3 / P-25 Tarrant named fallback). When deterministic same-year mapping cannot cover a residual, L9 rule 3 requires a **named, evidence-classed prior-vintage fallback**, never silent year fallback and never keyless mining. Companion table `cad_property_vintage_fallback` + resolver order exact → crosswalk → named-fallback → vintage-gap; consumer marker `vintageResolution`. Tarrant loaded 35,156 entries (34,298 absent GIS_Link / 845 RP=P-only / 13 norm-only) and flipped to declared 2026/cad-export; Dallas HOLD. Close: `_inbox/2026-08-14_l21_followup3_close.json`. Trading: residual coverage may authorize an explicit prior-edition read only with a durable evidence class and a visible provenance marker on the served row.
- 2026-08-15 (origin: real estate, OPS-17 Smart Files L1). Document-family identity is a DECLARED module key, not a city FIPS key. `smartfile:<scopeType>:<scopeId>:<docSlug>` with last-segment-is-slug so a site document can key by an existing parcel-node id that itself contains colons. The same-day FIPS-only shape is superseded before the store is applied (empty tables; apply-first would have hardened the wrong key). Typed absence (G-34) stays; promotion to `@empressaio/atom-contract` waits on this identity. Decision `_decisions/2026-08-15_smart_files_module_identity.md`. Trading: a document/artifact family that will be consumed by more than one product cannot freeze a tenant-shaped or venue-shaped key as if it were the only population.
- 2026-08-15 (origin: real estate, Empressa mountain / Smart Files CC-done). The data room is questions 1 and 2 of the agent transaction (who / what); the SDK is question 3 (how do I pay) and must not charge for a record the room cannot open with evidence. ICC is the first real SDK customer; Command Center is non-commercial. Decisions `_decisions/2026-08-15_vision_data_room_and_sdk.md` and `_decisions/2026-08-15_icc_first_sdk_customer.md`. Mapping `portfolio_thesis/03_three_questions_data_room_and_sdk.md`. Trading: do not attach settlement to an operator console; first paid counterparty should be a licensed source whose atoms already carry actor + evidence.
- 2026-08-15 (origin: real estate, Smart Files independent module). A filing-system atom family that can be sold a la carte cannot share a database or serving process with the public property spine. Application surfaces compose a public place-read with a private data room; the stores do not compose. Default file accessPolicy is tenant-private. Today's product save/share is not that family until the saves are file-shaped atoms in the isolated store. Decision `_decisions/2026-08-15_smart_files_independent_module.md`. Trading: a private workspace vault and a public market ledger must not share a writer role or migrate job; composition belongs in the application, not in the table.
- 2026-08-15 (origin: real estate, Smart Files file-set). Folder membership is an EDGE, not identity. ADR-001 identity is what the atom is; ADR-010 traversal is how it appears on a node. File-shaped atoms (attachments) are one type; not every atom is a file. `smart_file_placements` is the first `placed-on` edge. Parcel-keyed fact writers that imply applies-to via entityId are a FINDING, not doctrine. Decision `_decisions/2026-08-15_file_set_edges_not_identity.md`. Trading: do not use a record's primary key as its only graph location; multi-parent appearance is multiple edges, same identity.
- 2026-08-15 (origin: real estate, capability-mount composition). Application surfaces compose mountable modules (public spine, private files, later closing docs, optional tokenization) in one interface. Stores do not merge. Theology #8 (composed tiles over the spine) extends to composed modules over isolated stores. First module is `empressaioemail-tech/smart-files` (G-58). Tokenization is named, not scoped. Decision `_decisions/2026-08-15_capability_mount_composition.md`. Trading: a transfer or settlement UI that needs a private vault plus a public ledger must mount both; sharing a writer role to make them "appear together" is the defect this exists to kill.
- 2026-08-15 (origin: real estate, RWA mount-not-create). We do not mint or operate real-world-asset tokens. RWA operators adopt by mounting their asset onto public-record provenance, Smart Files, and the SmartSite map. That is the sell. Decision `_decisions/2026-08-15_rwa_mount_not_create.md`. Trading: a settlement rail that wants a place-backed claim should consume the provenance vault, not require the vault to become the issuer.
- 2026-08-15 (origin: real estate, Smart Files is a product). Isolation (own repo, own DB, mountable) does not demote a named sale to substrate. Smart Files is an Empressa product; Hauska remains the contract, engine, SDK, and MCP gate it consumes. Planner yellow "do not sell as a product brand" withdrawn. Decision `_decisions/2026-08-15_smart_files_is_a_product.md`. Trading: a vault that other surfaces compose can still be a first-class product with its own store and SKU.
- 2026-08-15 (origin: real estate, G-58 item 6 MCP retarget). Access policy stays at one gate (MCP). The files HTTP API is service-token only (anon 401); it does not become a second policy engine. MCP `canReadAccessTarget` still filters tenant-private. Client refuses `cortex-api` and does not share `LEGACY_BACKEND_URL` with Cortex tools. Live: MCP `00047-tpc` tools/call list/read of tenant-private seed; files `00002-wn5`. Trading: a private vault service-token is not an accessPolicy subject; the product gate remains the one place tenant-private is decided.
- 2026-08-15 (origin: real estate, G-58 item 8 SmartSite mount). Theology #8: the customer map mounts the isolated files service; it does not hold the files DSN and it does not stop reading the public spine. Live PE `dpl_CZCKscGZkiTBKDEs3rWBZ4LRCi1S` `GET /api/pe-smart-files-mount` 200 isolation folder on files Cloud Run; facets `48021:28286` still `atom-chain-warm`. Save/share stay a get-by. Trading: a customer surface that needs a private vault plus a public ledger mounts both over HTTP; putting the vault DSN on the map project is the store-merge defect.
- 2026-08-15 (origin: real estate, G-58 item 9 CC unmount). An operator console is not a product home. After the files service is live, a cortex-backed CC panel is zombie code even if the hash route still exists. Live CC `dpl_CSN8JfFa6jTU4qdiPn9dCrQ7JKN1` bundle has zero `/api/smart-files` strings; `#panel=smart-files` falls back to plan-review. Trading: when a vault leaves the shared ledger process, delete the console client that still pointed at the old process; remount later as an embed, do not keep the dead path.
- 2026-08-15 (origin: real estate, G-59 QA rooms). A private file room is first-class (folders table), not inferred from documents. Actor is `(orgId, userId)`; org list is tenant-scoped; default accessPolicy is tenant-private; share is a read-only token for one room, not an org dump. Bytes keyed by `sha256:` CID on the isolated files store. Product UI lives on the files service (https://smart-files-app.vercel.app); Cortex stays unmounted. Write MCP tools are a named follow-on on the existing Hauska MCP server (theology #9 partial). Close `_inbox/2026-08-15_a_smart_files_qa_rooms_close.json`. Trading: a vault workspace must be a real node with membership edges; a share link is a capability on one node, never a second policy engine and never a cross-tenant list.
- 2026-08-16 (origin: real estate, plan-review extract-and-remount). Second mountable module after Smart Files: Codex plan review leaves cortex-api as implementation home, keeps the cortex URL as a proxy mount. Elevate existing BFF callables; do not 404 `/api/plan-review` (files 404'd because no durable client contract depended on it). Calibration left partial. Decision `_decisions/2026-08-16_plan_review_extract_and_remount.md`. Live: LDT PR #436 MERGED `85c5d1a8`, serving `cortex-api-00519-muq` tag `g60`. Trading: when a function package already has callers, isolate the store and remount the old URL; when it does not, unmount with 404.
- 2026-08-16 (origin: real estate, plan-review is Smart Files first product consumer). Theology #8: the review product mounts the files module for every document, sheet, and dataroom atom. It does not copy cortex GCS/dataroom tables into a second store. PE isolation mount is a probe; live PE save/share is still not this consumer. Decision `_decisions/2026-08-16_plan_review_is_smart_files_first_consumer.md`. Trading: a workflow product that needs a private vault mounts the vault; it does not mint a sibling blob table so the old BFF compiles.
- 2026-08-16 (origin: real estate, G-60 MCP Codex retarget + files writes). One Hauska MCP server. Codex tools call plan-review Cloud Run, not LDT Cotality F2. Files writes (`create_smart_file_folder`, `upload_smart_file`) stay behind the same access-policy gate; Codex keys with `jurisdiction_tenant=icc-demo` may list/write tenant-private icc-demo rooms. Cotality adapters fail closed `extinguished` with zero outbound. Retrieval health 404 is degraded, not ok. Live: MCP `00072-puy` tag `g60`, 82 tools (public 13 / Codex 9 / reporting 53 / map 7). Origin PR #68 MERGED `12156a02`. Reporting `cortex_*` stay. Trading: a second MCP or a deleted reporting registry is not how you finish a product gate; retarget the live names and fail-close the dead vendor.
- 2026-08-16 (origin: real estate, G-60 walk: access-policy gate vs store stamp). Theology #5: the gate withholds ICC from anonymous callers even while existing store rows remain `public-free` (atomCount 4966, no UPDATE while L26 holds `--apply`). Anon `list_jurisdictions` omits `icc-model-code`. Anon `get_atom` on `did:hauska:jurisdiction-corpus:icc-model-code` is access-deny, no body. Reviewer key still reads. Close `_inbox/2026-08-16_icc_demo_close.json`. Live MCP `00074-tar` tag `g60d` PR #69 `0316d0a`. Trading: a catalog that is stamped public in the store can still be withheld at the product gate; do not treat a store column as the only access control, and do not claim the stamp is flipped until the UPDATE lands.
- 2026-08-16 (origin: real estate, ICC-demo is a separate portal). Licensed-IP access is its own Empressa product. Name: ICC-demo. Host https://icc-demo.vercel.app (alias of `icc-portal-app`). Plan review cites. Command Center is not the portal. Activity still mounts the plan-review table over HTTP (no second ledger, no DSN on the portal). Unauthed `/activity` 401, accessor BFF n=27. Apex DNS held. Decision `_decisions/2026-08-16_icc_demo_is_separate_portal.md`. Trading: the people who pay to access IP get a door; the citing product is not that door.
- 2026-08-17 (origin: real estate, SmartCity product line then Bastrop onboarding). Theology #8: city product surfaces are composed mounts over spine + SmartSite + Smart Files, not a unique city codebase. First tenant is a cutover onto the template, not a clone of live vendor wallpaper. Feeds write records (theology #1 provenance); they are not products. Decision `_decisions/2026-08-17_smartcity_product_line_then_bastrop_onboarding.md`. Trading: a venue-specific console that only renders vendor APIs is not a second product line; templatize the composition, then onboard the venue.
- 2026-08-17 (origin: real estate, G-13 consumer contract). Theology #8 and #9: application surfaces mount isolated suppliers over a caller split (agents = one MCP server; apps = HTTP service-token or embed). Never a DSN, never a copied table. Spine HTTP is atom-read (retrieval). SmartSite application mount is embed. Files application mount is the files service API. Decision `_decisions/2026-08-17_g13_consumer_contract.md`. Trading: a console that needs a public ledger plus a private vault plus a map mounts three services; it does not import their tables so they "appear together."
- 2026-08-17 (origin: real estate, SmartCity Dashboards housing). Theology #8: one product repo, cities as tenant packs. Isolation is accessPolicy plus tenant scope, not git. GitHub `empressaioemail-tech/smartcity-dashboards`. Live city repo stays the weld until cutover. Decision `_decisions/2026-08-17_smartcity_dashboards_housing.md`. Trading: do not fork the venue console per customer; onboard as a pack on the composed surface.
