You MAY spawn sub-agents. A sub-agent MUST NOT spawn, commit, merge, or deploy. You MUST NOT git add / commit / push. You MUST NOT deploy. You MUST NOT gcloud run. You MUST NOT vercel. You MUST NOT atoms --apply. You MUST NOT wells apply. You MUST NOT Harris PBF. You MUST NOT mint absence. You MUST NOT flip `texas-rrc` or `mud-pid` to live. You MUST NOT write hauska-map. You MUST NOT occupy P:/legacy-design-tools or P:/seat-worktrees/property/legacy-design-tools. You MUST NOT start P-51 / P-52 / rail serve.

Plan row P-50. Occupancy: isolated worktree P:/legacy-design-tools-worktrees/serve-p50 branch serve-p50 tracking origin/main. Create it if missing. Doc_repo writes: your _inbox JSON only.

WDLL: P:/doc_repo/_inbox/2026-08-22_serve_ident_qa_WDLL.md item 4 (P-50 wells). This lane is scout then cortex only. PE copy is planner follow-on. Pattern: `artifacts/api-server/src/lib/pipelineFactRead.ts` (LDT PR 453, serving). S3: `_inbox/2026-08-21_s2-family-scout_close.json` well-fact HOLD, same dormant `texas-rrc` GIS slot as pipelines, no inspect SELECT. Q8 well bind is UNMEASURED. Do not invent a percent. P-10 blocks new well apply; this card reads stored `well-fact` only.

## Mission

This card is scout then cortex read for S3 wells only. PE copy is planner follow-on. Do not share the `texas-rrc` key with pipelines. Do not flip `texas-rrc` live.

Wells are a spatial overlay at WRITE time, not a special-district parcel-key picker and not a copy of `pipelineFact` bind guesses. Read the well writer. Do not assume it matches pipelines.

### Scout (CP1, before any product edit)

Read the writer first: `write-well-fact-county.mjs` and `well-fact-writer.ts` from `P:/hauska-engine-worktrees/cover-p17-roads` or `P:/hauska-engine-worktrees/ident-p55`. Do not occupy `P:/hauska-engine`.

Name both halves of the serve: what you query, and how it attaches to a parcel. Quote the writer-derived key and the SQL. Point SELECT or a bounded near query. Never heap COUNT(*).

Name one live parcel that has a store hit. Prefer Bastrop `48021`. If gold `48021:34137` has no nearby `well-fact`, say so and name the substitute you actually hit. Quote `entity_id`, `entity_type`, and the bind/near rule. Confirmatory: a second county or a typed miss.

If you cannot name a hit, STOP and file. Do not invent a fixture that the store does not have.

### Cortex

Add a NEW sibling field on the cortex JSON ROOT, parallel to `pipelineFact`. Freeze the field name in CP1 (`wellFact` unless the existing facets route already reserved another). Inspect later will prefer that field.

Never SELECT well values from bake / `place_layer_snapshots` / CAD / GIS / `texas-rrc` / `tx_rrc_well` for this field. `ATOMS_DATABASE_URL` only. Unconfigured: `atoms-store-not-configured`.

Present: `state` present|absent|refused, `source=well-fact`, `boundAs`, `tried`, `entityId`, operator-visible name/id fields the writer actually stores. Do not invent a name. Zero hits: typed refusal `code=atom-miss`.

Dual grammar on the parcel prefix only if the writer keys by parcel. If the writer keys by well id, say so and use the near/bbox rule the writer implies. Do not pretend a parcel prefix join exists if the writer does not write one.

Do not copy pipeline operator/permit fields onto this family. Do not share one inspect key with pipelines.

Tests that fail if snapshots / CAD / GIS / texas-rrc are served as this field. Tests that pass on a fixture atom. A boot-proof that the facets route wires the new field.

Leave the diff uncommitted.

## Return

CP1 before edits: occupancy SHA, scout parcel, entity_id, bind SQL, field name, what you will violate. CP2 after tests. CLOSE quotes files, tests, fixture, and the live GET you could not run (no deploy). leave_behind: planner PR/deploy cortex-api; then a PE card to copy the root field onto inspect/map. WDLL item 4 is not met until smartsite.cloud cites well-fact. Cortex undeployed is not that probe. PE is not this lane. Do not start P-51.
