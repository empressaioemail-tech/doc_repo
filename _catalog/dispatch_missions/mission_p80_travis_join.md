You MAY NOT spawn sub-agents. You write LDT / property worktrees only after occupying those seats. You do NOT commit from this mission into doc_repo (planner-owned). You do NOT load Travis CAMA. You do NOT invent geo_id. You do NOT invent sqft. You do NOT run leftover apply, atoms --apply, rematerialize, or L17 flip.

Plan row P-80. Occupancy: property seat, isolated LDT tree from origin/main. WDLL `_inbox/2026-08-25_p80_travis_join_WDLL.md` items 5-10. Items 1-4 already met by P-77.

## Mission

This is the Travis JOIN FIX, not another honest-miss card.

P-77 already shipped `lookup-failed` + vintage `2026/cad-export` for `48453:280238` on cortex `00584-gaf` SHA `46e1a5a1`. Measure is `scripts/p77-travis-join-measure.mjs` live 10/1/0/0. Join is `prop_id`.

### 280238 first

Prove bind-or-cannot-bind on `PROP_ID=280238` against a public CAD source (live TCAD REST layer in `_catalog/tx_cad_source_registry.json` `tx-48453`, field `PROP_ID`). Control `PROP_ID=280239` on the same layer. Named-id only. No 48453 scan.

Directional evidence already filed: `_inbox/2026-08-25_p80_280238_tcad_rest_named_id.json` (280238 `features=[]`, 280239 one feature). Promote that probe to a file-based instrument that self-tests both directions and fails on a known violation before you treat cannot-bind as closed.

If the source has 280238: upsert `cad_property` at `2026/cad-export` on `prop_id` via the existing cad-export / REST-by-PROP_ID path. If it does not: file cannot-bind and do not write a row. Do not invent a `geo_id` join to force a bind. Do not start Travis CAMA. CAMA will not bind a StratMap-only node (P-73 T48453 CAMA row; game plan constraint 6).

### Cohort

Same split on a stated sample of store-miss nodes: REST-present / REST-absent / unmeasured. Gap-fill only REST-present store-miss, and only on `prop_id`. Re-grade with `scripts/p77-travis-join-measure.mjs`. `prop_id_bad_rate` 0.51 is not the grade.

### After any write

`48453:280239` and at least one of `280210` / `280211` stay HIT. Gold `48021:34137` does not regress. Facet rebake only after a real `cad_property` write. 280238 stays `lookup-failed` unless item 5 is reversed by a source feature.

## Return

CP1: instrument design + falsifiers (empty 280238 vs present 280239; refuse geo_id SQL; refuse county scan).
CP2: live named-id results + cohort split counts with denominator.
CLOSE: `_inbox/2026-08-25_p80_close.json` citing WDLL items 5-10. leave_behind: implementation residue, P-74 situs, CAMA (not this row).
