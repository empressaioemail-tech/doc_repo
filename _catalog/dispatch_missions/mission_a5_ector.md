You MUST NOT spawn sub-agents. Do not git add / commit / push. Do not deploy. Do not atoms `--apply`. Do not touch P:/legacy-design-tools.

Plan row P-02. Read-only. Code reads: P:/seat-worktrees/property/hauska-engine and P:/seat-worktrees/property/legacy-design-tools. Doc_repo write: your close JSON only.

WDLL: P:/doc_repo/_inbox/2026-08-21_wave_a_next_WDLL.md items 4, 5.

## Mission

Name the Ector 48135 re-key that must happen before any geometry re-score. P-02. Do not apply.

Coverage row (A1, Neon 2026-08-21T16:46:40Z): facet=geometry county_fips=48135 honest_coverage_pct=5.00 rail_state=not-yet rawFeatures=75891 accountedFeatures=3791 foldedExtraFeatures=72100 verified_by_instrument=B2_cp2_geometry_scorer_apply.mjs.

OPS-16 P-02: re-key on geo_id (~71,673 atoms). RAIL_SCORING_DECLARATION geometry: the accounted-features producer is not in LDT; do not guess a replacement denom.

## Do this

1. Find the re-key / parcel-node plan script for Ector geo_id. Quote path, invoke (dry-run), what it writes, whether it needs the atoms slot.
2. Compare foldedExtraFeatures=72100 to the plan's expected geo_id extra count. If they disagree, say so; do not round it off.
3. Slot: read `_STATE.md` L16B / L26 lease claims as STALE if dated 2026-08-15/17. Report whether you can see a live lease table/row. If unmeasured, say UNMEASURED. Do not take the slot.
4. Recurrence: a re-score of 48135 with DISTINCT feature_index would invent a denom the registry forbids. Confirm that countyGeometryScoreCli on 48135 today would still write 5% or would change the denom; quote the code. GO/NO-GO to score-apply (must be NO-GO until re-key).

## Return

File `_inbox/2026-08-21_a5-ector_close.json`. Dry-run command. Slot status. WDLL 4. leave_behind: the apply command after operator/slot go, not run. No product edits.
