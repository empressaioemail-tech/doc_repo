# Mission — REVIEW: the adversarial review of the program itself (ruling step 5)

`_decisions/2026-09-01_parcel_record_is_the_gate_to_everything.md` step 5: the PROGRAM
is attacked, not its output — "a program reviewed only by its own output measurements
applies the same predicates that admitted the defects." You produce FINDINGS ONLY; fixes
are step 6, separate cards. Read first: the ruling, both rails decisions, the gap
ledger, and every parcel-* close in `_inbox` (they carry the banked questions).

Pre-register a falsifier for every check BEFORE running it. Every finding carries: the
defect, the mechanism, a second mechanism you considered and why you rejected it,
severity in production terms (what a customer or downstream job experiences), and its
falsifier.

Attack surfaces, minimum set — go beyond it:

1. The crosswalk method: situs+owner corroboration false-match rate. Audit at least 50
   random crosswalked Williamson pairs against a THIRD derivation (geometry containment
   of the situs point, or deed references). A silent mis-join is the worst defect
   available in this program.
2. The cad-null-verified emission: blank-string semantics, the tax_year basis edge, and
   whether any emission path is reachable from a non-matched branch — attack the
   structural claim, do not trust the fixtures.
3. The publish-gate liveness derivation: gameability (one junk cell flips a rail live),
   the demotion edge, and whether the exclusion-set printing can be bypassed by any
   consumer that reads ok:true alone.
4. The gap ledger's counting rules: re-derive two of its headline numbers from scratch
   SQL written blind (do not read its queries first), then compare.
5. The R1B single-row residue discrepancy (24,966 vs 24,965): resolve it or bound it.
6. The banked semantics: exemption_codes text[] stamping; landUseSource/acreageMethod
   absent-verified treatment; non-spatial personal-property accounts; mobile-home
   companions; the two Bastrop orphans; PRIVATE ROAD keying; the b3 geometry gap
   against the "containment complete" claim.
7. Process: the stale-dispatch pattern (three cards ran against superseded premises
   this session — addenda saved them; is that a control or a memory?); the
   lease-lapse-while-working pattern; the store-read-timeout blind spot during writer
   load.
8. THE PROD PATH specifically: the operator has ruled prod = the record SERVES the
   product. Enumerate exactly what stands between the current state and a publish-gate
   PASS per county on live rails, and attack whether the gate's own definition
   (unaccounted fatal on live rails) produces the honest serve the tenant-sovereignty
   and quality-gate rules require. This section sizes step 7 and is the report's most
   important chapter.

READ-ONLY on stores (statement_timeout-guarded reads; the store times out under writer
load — schedule heavy scans accordingly or derive from run rows). Deliverable: findings
report `_inbox/2026-09-02_parcel_program_review.md` ranked most-severe first plus the
close JSON. Write it for the operator and the step-6 carding.
