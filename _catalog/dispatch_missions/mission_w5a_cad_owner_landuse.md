You MUST NOT spawn sub-agents. You MUST NOT git add / commit / push. You MUST NOT deploy. You MUST NOT gcloud run. You MUST NOT vercel. You MUST NOT leftover apply. You MUST NOT rematerialize. You MUST NOT take a second writer. You MUST NOT start P-09. You MUST NOT start COVER / Harris PBF. You MUST NOT leftover Dallas 48113 or Tarrant 48439. You MUST NOT flip L17 mid-apply.

Seat that compiled this: integration on `P:/doc_repo` `main`. Operator named **W5**. Naming: **W5-A** cad then owner then landuse, then **W5-B** flood remainder, then **W5-C** mud not-yet. One slot. One writer.

This compiled file is the W5-A apply card. The 2026-08-25 slot-check steward already filed `_inbox/2026-08-25_w5a_cad_owner_landuse_steward.md` and `_inbox/2026-08-25_w5_slot_check_close.json` with `applyStarted: false`. Do not start `--apply` from the steward session. Do not start `--apply` while `scripts/factory-routing-readiness.mjs --check` exits 1 or while cad/owner/landuse stay `ready:false` for a live-merge-probe defect.

Plan rows: P-47 (Manifest honesty / W6 GET) plus the cad/owner/landuse rails (P-25 held CAMA; P-78 leftover already KEEP). Occupancy: one atoms `--apply` after a live lease take. Steward pack is doc_repo only.

WDLL: `_inbox/2026-08-24_parcel_facts_write_path_WDLL.md` items 8, 9, 13. Factory card `_inbox/2026-08-25_factory_operating_instructions.md`. Pin `_inbox/2026-08-24_factory_routing_pin.json`.

## Mission

When Nick names apply (not this compile):

1. Re-prove the atoms bulk-writer slot is still FREE. File evidence. Take one lease. One writer.
2. Family order is cad, then owner, then landuse. First counties are leftover KEEP whose Manifest cells are `not-yet`. Never leftover Dallas 48113 / Tarrant 48439.
3. Atom write grammar is P-55. Engine helper `packages/atoms/src/parcel-write-identity.ts`. `{fips}:{integer}`. `applies-to` in the same `writePropertyAtomsBatch` call. Verify stored bytes by `atom_did IN`. Shared reject refuses decimal-padded second tokens and `:outside` / `:primary`.
4. After the family, W6 GET: `node scripts/county-manifest-canvas-dump.mjs --live`. Replace dump DATA. Do not rematerialize.

Read `_inbox/2026-08-25_w5a_cad_owner_landuse_steward.md` before any write.
