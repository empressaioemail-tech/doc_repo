You MUST NOT spawn sub-agents. Do not git add / commit / push. Do not deploy. Do not atoms `--apply`. Do not touch P:/legacy-design-tools.

Plan row P-08. Read-only scout. Code reads: P:/seat-worktrees/property/legacy-design-tools. Doc_repo write: your close JSON only.

WDLL: P:/doc_repo/_inbox/2026-08-21_wave_a_execution_WDLL.md items 3, 5.

## Mission

Name the flood consumer that was never repointed after tier2 retirement. BP-SERVE-01. Do not patch this pass.

Governing: P:/doc_repo/90_operations/OPS-18b_data_remediation_plan.md Wave A3. Live GET: flood 114 satisfied-present, 140 not-yet.

## Two paths, keep them distinct

1. Ledger score: `countyFloodScoreCli.ts` (binding says it scores flood-hazard-fact). Trace the FROM/import. Does it read `flood-hazard-fact` atoms, `tx_fema_nfhl_flood_zone`, or `place_layer_snapshots`? File:line for each.
2. Inspect/serve: `brokerageNodeFacets.ts`, `nodeFacetBakeTier1Cli.ts`, `brokerageSiteContext.ts` still name `place_layer_snapshots`. Say whether SmartSite inspect flood still goes through that table.

A ledger scorer already on flood-hazard-fact plus an inspect path still on place_layer_snapshots is TWO defects, not one. Do not fold them.

## Recurrence control sketch (do not implement)

A test that fails if the flood SCORE path imports or SQL-selects `place_layer_snapshots`. Name the file it would live in. If the score path is already clean, say the control is for the inspect path instead, or that A3 is mis-aimed at the ledger.

## Return

File `_inbox/2026-08-21_a3-flood_close.json`: file:line graph, which path explains the 140 not-yet (or UNMEASURED), recurrence test location, WDLL 3 grade. No product edits.
