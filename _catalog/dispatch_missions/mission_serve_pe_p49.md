You MUST NOT spawn sub-agents. Do not git add / commit / push. Do not deploy. Do not vercel. Do not occupy P:/hauska-map or P:/seat-worktrees/property/hauska-map. Do not occupy P:/legacy-design-tools. Do not atoms --apply. Do not start P-50. Do not flip texas-rrc. Do not flip mud-pid.

Plan row P-49. Occupancy: isolated worktree P:/hauska-map-worktrees/serve-pe-p49 branch serve-pe-p49 tracking origin/main. Create that worktree if it is missing. Doc_repo writes: your _inbox JSON only.

WDLL: P:/doc_repo/_inbox/2026-08-22_serve_ident_qa_WDLL.md item 3. Cortex is already serving. Execute `_inbox/2026-08-22_p49_cortex_execute.json`. Pattern: hauska-map PR 177 specialDistrictFact copy (same files as PR 176 landUseFact).

## Mission

Copy cortex-root `pipelineFact` onto SmartSite inspect. This is the PE half of S2. Implement against fixtures. Do not wait. Do not invent ENERGY TRANSFER on gold.

Pipelines are a spatial overlay at WRITE time. Do not copy the special-district `:sd:` picker onto this family.

1. `mergeBakedBaseFacts` (and cortex-off / strip path if live) forwards `pipelineFact` from the cortex JSON ROOT. If the root field is missing, leave it absent. Never adopt bake / CAD / `texas-rrc` GIS as that field.

2. Inspect shows a Pipeline row from `pipelineFact` only.
   - Gold `48021:34137` is `state=present` `nearPipeline=false` `t4permit` null. Honest present-outside. Do not paint ENERGY TRANSFER / Prairie Lea / a permit on gold.
   - Nearby substitute `48021:10048` is `state=present` `nearPipeline=true` `t4permit=05781` `operatorName=ENERGY TRANSFER COMPANY` `nearestPipelineDistanceMeters=87.9`. That row may show operator / permit / distance from the atom.
   - Typed `state=absent` or `refused` stays visible as honest absence. A missing field stays missing.

3. Dual grammar: integer parcel is the live probe. Padded `48021:34137.00000000` is a cortex bake 404 today. Do not treat that 404 as a pipeline miss and do not invent a padded inspect win.

4. `texas-rrc` layer stays `live:false` unless you can prove both: inspect is already wired to `pipelineFact`, AND the overlay fetch is the atom (`rrc-pipeline-fact`), not empty GIS / paint. If you cannot prove the overlay half, leave the layer false and say so. Flipping the key without an atom fetch fails this card.

5. Project for a later planner deploy is `property-explorer` / `prj_vcZGXbqdffk5C20WzaplEpzFynK3`. You do not deploy.

Tests: merge copies a fixture `pipelineFact` and does not copy bake / GIS onto that field. Gold-shaped present-outside fixture does not render ENERGY TRANSFER. Present-near fixture `48021:10048` shows `t4permit=05781`. Alias retries when `pipelineFact` is missing even if flood / land-use / special-district are already present.

Leave the diff uncommitted.

## Return

CP1 before edits: occupancy SHA, files you will touch, gold must stay present-outside, what you will violate. CP2 after tests. CLOSE quotes files and tests. leave_behind: planner PR/deploy PE. Live inspect `48021:34137` must not show ENERGY TRANSFER. Live `48021:10048` must cite `rrc-pipeline-fact` / `t4permit=05781` after that deploy. WDLL item 3 is not met until smartsite.cloud.
