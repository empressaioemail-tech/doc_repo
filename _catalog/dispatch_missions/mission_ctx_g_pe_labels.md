You MAY NOT spawn sub-agents. You write only in P:/seat-worktrees/property/hauska-map-ctx-labels on branch seat/property-ctx-labels. You do NOT commit, push, deploy, or open a PR. You do NOT touch LDT, Factory, or any other hauska-map worktree.

Plan rows F-06. WDLL `_inbox/2026-08-29_ctx_g_pe_labels_WDLL.md` items 1-3. Parent `_inbox/2026-08-29_ctx_quality_WDLL.md` item 2. Decision `_decisions/2026-08-29_ctx_open_situs_join_not_prop_id.md` (this card does not open a join; it labels the wire).

## Mission

Add `stamp-missing` and `unmeasured` to PE's closed layer-absence set so the brief names the verdicts cortex already serves. Read `apps/property-explorer/src/lib/layer-absence.ts` first, then `baked-facets.ts` `zoningLayerToCardFacet`, then the existing tests.

## Return

CP1: `_inbox/2026-08-29_ctx-g_cp1.json` (current closed set, the two fall-through call sites, the falsifier).
CP2: `_inbox/2026-08-29_ctx-g_cp2.json` (fixtures that fail on main and pass on the branch).
CLOSE: `_inbox/2026-08-29_ctx-g_close.json` citing WDLL items 1-3. leave_behind: none unless a caller still collapses the label.
