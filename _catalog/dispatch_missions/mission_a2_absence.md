You MUST NOT spawn sub-agents. Do not git add / commit / push. Do not deploy. Do not atoms `--apply`. Do not mint absence atoms. Do not touch P:/legacy-design-tools.

Plan rows P-11, P-12, P-06. Read-only scout. Code reads: P:/seat-worktrees/property/legacy-design-tools and P:/seat-worktrees/property/hauska-engine. Doc_repo write: your close JSON only.

WDLL: P:/doc_repo/_inbox/2026-08-21_wave_a_execution_WDLL.md items 4, 5.

## Mission

Find whether sparse uniform rails can become `satisfied-absent` without minting wells/pipelines/rail. BP-ABSENCE-01. Verified pair: `evaluated` true AND non-empty `provenanceScope`. Typed `absence` without the pair is the bypass (R-07 Q5d vs Q5e).

Governing: P:/doc_repo/90_operations/OPS-18b_data_remediation_plan.md Wave A2. Live GET: rrc-wells 254 not-yet, rrc-pipelines 254, rail-corridor 254, mud 45. L7 already proved geometry: Donley 48129 `satisfied-absent`. Read `_inbox/2026-08-12_L7_honest_absence_close.json` and `_inbox/2026-08-12_L7_sparse_rail_absence_doctrine.md`.

## Do this

1. Quote the geometry scorer write path that emits `satisfied-absent` (L7). Does it require the verified pair, or only `absence_basis` on `county_facet_coverage`?
2. For rrc-wells, rrc-pipelines, rail-corridor, mud: does a scorer exist that can write `rail_state='satisfied-absent'`? File:line. If the scorer would write absent when atom count is zero, that is presence-shaped and forbidden unless a positive determination exists.
3. Engine writers: do well/pipeline/rail writers ever write `verifiedAbsence.evaluated` + `provenanceScope`? If they only write typed `absence`, name that as the starved pair.
4. Recurrence control sketch: scorer throws if it would emit `satisfied-absent` without the pair. Name the file. Do not implement.

## Return

File `_inbox/2026-08-21_a2-absence_close.json`. WDLL 4 grade. leave_behind: the first patch that would let ONE named county on ONE sparse rail go honest-absent without minting. No product edits.
