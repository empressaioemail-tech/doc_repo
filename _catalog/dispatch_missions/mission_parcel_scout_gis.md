# Mission — SCOUT-GIS: public-GIS acquisition targets, priced read-only

Step-4 scouting for classes the gap ledger adjudicated GENUINELY-MISSING or never-sourced.
READ-ONLY everywhere: no store writes, no purchases, no credentialed portals. Deliver a
source inventory the planner can card acquisitions from directly.

Targets: (1) school districts — TEA/TxGIO district boundary layers; (2) utility service
areas — PUC CCN polygons (water/sewer certificated areas) plus electric/gas territory
sources; (3) overlay and historic districts — the six-county cities' open-GIS portals
(Austin and Waco are known publishers); (4) anything those portals expose that maps to a
declared-ahead rail (ag valuation layers, impervious regulations as data).

Per source report: URL, format, vintage, geographic coverage vs our six counties,
license/terms VERBATIM (never paraphrase a license), cost (expected zero — these are
public), fetch mechanism sketch (direct download vs API vs export), row/feature
magnitude, and the join key to parcels (spatial vs attribute). Cite everything — a claim
without a fetchable citation does not enter the inventory. Sample-fetch enough to verify
format claims (a schema read, not a bulk pull). eCode360-style 403 walls: note and move
on, never fight.

Close: `_inbox/2026-09-02_parcel-scout-gis_close.json` plus an inventory markdown beside it.
