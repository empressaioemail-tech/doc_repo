# Mission — TXGIO-REACQ: fresh fabric for six counties, through the real machinery

The txgio-refresh close (read it first) measured the vintage gap and BLOCKED on a
planner mis-scoping: the TxGIO/StratMap acquisition machinery lives in
legacy-design-tools, not hauska-factory. This card is the corrected half.

1. Find the ORIGINAL loader that populated txgio_parcel (read LDT's acquisition
   pipeline; do not build a parallel one — operate the frozen proven artifact).
2. Pull CURRENT TxGIO/StratMap land-parcel data for the six program counties —
   public uniform acquisition per standing doctrine, INCLUDING Caldwell 48055 (the
   county-wide geom gap; the caldwell-geom close proved the source carries it and
   the load skipped it).
3. Land it through the established path (staging discipline, provenance, vintage
   stamped). Do NOT touch containment, parcel_record, or any downstream store — the
   apply half is PARCEL-REFRESH-APPLY, a separate card.
4. Verify: per-county feature counts vs the vintage-gap measurements in the refresh
   close; the three B3-named Bastrop subdivisions present in the fresh data;
   Caldwell geoms present and non-null. Paste verbatim.

Close: _inbox/2026-09-03_parcel-txgio-reacq_close.json.
