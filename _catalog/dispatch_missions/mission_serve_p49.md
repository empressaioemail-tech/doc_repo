You MAY spawn sub-agents. A sub-agent MUST NOT spawn, commit, merge, or deploy. You MUST NOT git add / commit / push. You MUST NOT deploy. You MUST NOT gcloud run. You MUST NOT vercel. You MUST NOT atoms --apply. You MUST NOT Harris PBF. You MUST NOT mint absence. You MUST NOT flip `texas-rrc` or `mud-pid` to live. You MUST NOT write hauska-map. You MUST NOT occupy P:/legacy-design-tools or P:/seat-worktrees/property/legacy-design-tools. You MUST NOT start P-50 / P-51 / P-52 / wells apply.

Plan row P-49. Occupancy: isolated worktree P:/legacy-design-tools-worktrees/serve-p49 branch serve-p49 tracking origin/main. Doc_repo writes: your _inbox JSON only.

WDLL: P:/doc_repo/_inbox/2026-08-22_serve_ident_qa_WDLL.md items 1 and 2 (scout + cortex). Item 3 (PE) is planner follow-on. Pattern: `artifacts/api-server/src/lib/floodHazardFactRead.ts`, `landUseFactRead.ts`, `specialDistrictFactRead.ts` (LDT PR 451). S2: `_inbox/2026-08-21_s2-family-scout_close.json` rrc-pipeline-fact HOLD, `texas-rrc` live:false, paint exists, no inspect SELECT. Q8 pipeline bind is UNMEASURED. Do not invent a percent.

## Mission

This card is scout then cortex read for S2 pipelines only. PE copy is planner follow-on. Do not flip `texas-rrc` live.

Pipelines are a spatial overlay, not a special-district parcel-key picker. Do not copy `specialDistrictFactFromCortexRoot` bind logic onto this family.

### Scout (CP1, before any product edit)

Read the writer first: `write-rrc-pipeline-fact-county.mjs` from `P:/hauska-engine-worktrees/cover-p17-roads` or `P:/hauska-engine-worktrees/ident-p55`. Do not occupy `P:/hauska-engine`.

Name both halves of the serve: what you query, and how it attaches to a parcel. Quote the writer-derived key and the SQL. Point SELECT or a bounded near query. Never heap COUNT(*).

Name one live parcel that has a store hit. Prefer Bastrop `48021`. If gold `48021:34137` has no nearby `rrc-pipeline-fact`, say so and name the substitute you actually hit. Quote `entity_id`, `entity_type`, and the bind/near rule. Confirmatory: a second county or a typed miss.

If you cannot name a hit, STOP and file. Do not invent a fixture that the store does not have.

### Cortex

Add a NEW sibling field on the cortex JSON ROOT, parallel to `floodHazardFact` / `landUseFact` / `specialDistrictFact`. Freeze the field name in CP1 (`pipelineFact` unless the existing facets route already reserved another). Inspect later will prefer that field.

Never SELECT pipeline values from bake / `place_layer_snapshots` / CAD / GIS for this field. `ATOMS_DATABASE_URL` only. Unconfigured: `atoms-store-not-configured`.

Present: `state` present|absent|refused, `source=rrc-pipeline-fact`, `boundAs`, `tried`, `entityId`, operator-visible name/id fields the writer actually stores. Do not invent a name. Zero hits: typed refusal `code=atom-miss`.

Dual grammar on the parcel prefix only if the writer keys by parcel. If the writer keys by pipeline id, say so and use the near/bbox rule the writer implies. Do not pretend a parcel prefix join exists if the writer does not write one.

Tests that fail if snapshots / CAD / GIS are served as this field. Tests that pass on a fixture atom. A boot-proof that the facets route wires the new field.

Leave the diff uncommitted.

## Return

CP1 before edits: occupancy SHA, scout parcel, entity_id, bind SQL, field name, what you will violate. CP2 after tests. CLOSE quotes files, tests, fixture, and the live GET you could not run (no deploy). leave_behind: planner PR/deploy cortex-api; then a PE card to copy the root field onto inspect/map. WDLL item 2 is not met until cortex is serving. Item 3 is not this lane.
