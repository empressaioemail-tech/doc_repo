You MUST NOT spawn sub-agents. Do not git add / commit / push. Do not deploy. Do not vercel. Do not occupy P:/hauska-map or P:/seat-worktrees/property/hauska-map. Do not occupy P:/legacy-design-tools. Do not atoms --apply. Do not start P-52 / P-54. Do not flip texas-rrc. Do not flip mud-pid.

Plan row P-53. Occupancy: isolated worktree P:/hauska-map-worktrees/serve-pe-p53 branch serve-pe-p53 tracking origin/main. Create that worktree if it is missing. Doc_repo writes: your _inbox JSON only.

WDLL: P:/doc_repo/_inbox/2026-08-22_serve_ident_qa_WDLL.md item 6. Cortex is already serving. Execute `_inbox/2026-08-22_p53_cortex_execute.json`. Planner review ACCEPT `_inbox/2026-08-22_p53_cortex_planner_review.json`. Pattern: hauska-map PR 180 buildingFootprintFact copy.

## Mission

Copy cortex-root `boundaryEdgeFact` onto SmartSite inspect. This is the PE half of S6. Implement against fixtures. Do not wait. Gold is the live present probe. Do not present the GIS parcel outline as the atom.

Writer keys `${countyFips}:${propId}:boundary:${edgeIndex}`. Geometry is the atom body. Do not copy the special-district `:sd:` picker. Do not copy pipeline `ANY` bind. Do not share the `texas-rrc` key.

1. `mergeBakedBaseFacts` (and cortex-off / strip path if live) forwards `boundaryEdgeFact` from the cortex JSON ROOT. If the root field is missing, leave it absent. Never adopt bake / CAD / GIS / `txgio_parcel` / parcel ring as that field.

2. Inspect shows a Boundary row from `boundaryEdgeFact` only. Label `Boundary`. testid `inspect-boundary`.
   - Gold `48021:34137` is live `state=present` `source=property-boundary-edge` `entityId=48021:34137:boundary:2` `role=front` four edges. Inspect cites that atom. Display the role (front). Do not paint a GIS outline as the value.
   - Confirmatory fixture `48021:28286` is also live present `entityId=48021:28286:boundary:2` `role=front` four edges.
   - Typed `state=refused` `code=atom-miss` stays visible as an honest miss that names `property-boundary-edge`. A missing field stays missing.
   - A bake / GIS object parked on the root without `state` is rejected.

3. Dual grammar: integer gold is the live probe. Alias retries when `boundaryEdgeFact` is missing even if flood / land-use / special-district / pipeline / well / footprint are already present.

4. `texas-rrc` stays `live:false`. Edges do not share that key. Do not add a GIS parcel-outline flip.

5. Project for a later planner deploy is `property-explorer` / `prj_vcZGXbqdffk5C20WzaplEpzFynK3`. You do not deploy.

Tests: merge copies a fixture `boundaryEdgeFact` and does not copy bake / GIS / `txgio_parcel` onto that field. Gold-shaped present fixture shows `role=front` and cites `property-boundary-edge`. Atom-miss fixture does not render a GIS ring. Alias retries when `boundaryEdgeFact` is missing even if the other root facts are already present.

Leave the diff uncommitted.

## Return

CP1 before edits: occupancy SHA, files you will touch, gold is present (not GIS), what you will violate. CP2 after tests. CLOSE quotes files and tests. leave_behind: planner PR/deploy PE. Live inspect `48021:34137` must cite `property-boundary-edge` and must not show a GIS outline as the atom. WDLL item 6 is not met until smartsite.cloud. Do not start P-52 / P-54.
