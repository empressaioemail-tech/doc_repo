---
decision: enforcement_open_questions_ruled
date: 2026-08-19
status: active
owner: operator
related: [61_enforcement_doctrine, 90_enforcement_build_order, enforcement_vehicles, 51_ingestion_pipeline_reference]
---

# Enforcement open questions ruled (2026-08-19)

Four operator rulings closing doc 61 open questions and three text corrections.

## 1. C-00 category — ruled

**C-00 is internal consistency only.** One agent edits `ENFORCEMENT.md` and `.cursor/rules/enforcement.mdc`; the CI check passes. Two copies, one party. The vehicles doc predated the independently-derived rule; building C-00 anyway catches drift, transcription, and partial writes.

**C-00b is a separate control:** runtime doctrine-reach probe. Meaning shaped: repo files vs instruction payload the agent actually received. Design: `_inbox/2026-08-19_systems_c00b_runtime_probe_design.md`. Systems seat owns fleet coverage.

## 2. Doc 61 fourth state — text corrections taken

- **Two-party attestation required.** Agreement passes; disagreement is a finding; neither derivation available → refuse. Lone derivation may run but is labelled presence shaped, not counted toward meaning shaped coverage.
- **Deploy gate asks:** anchor exists in deployed tree AND content matches HEAD (survives rebase/squash), not introduction-commit archaeology alone.
- **Shared stamping point caveat** recorded in doc 61: re-verify derivation independence when deploy path changes.

## 3. Node type claim (doc 90 C-06) — corrected

Replaced "one hundred percent of markets nodes carry a defaulted type" with **path claim:** all three node-creating code paths supply defaulted asset class. Row count in held data block; do not unblock data block to produce count.

## 4. Removal as proof — adopted as T-24 standard

Delete, rebuild, run suite. Clean build = proof. Break = disproof naming gating call site. Unified with verify-by-violating in doc 61. **Language regime:** static typed → build sufficient; dynamic dispatch → pair with full suite + runtime path exercise.

## Additional routing (same session)

- **Hook commit-vs-push observation:** legitimate investigation for systems seat only (control owner). Determine fix-landing-revert vs fail-open swallowing payloads. Not property seat probing for bypasses.
- **Gate self-report red:** correct behaviour; use acknowledgment path with recorded reason and hard expiry if property flood retirement runs long — do not let red become scenery.
- **T-25 enumeration:** provenance/authority nine rows first; formula+inputs pull-forward; optional-field OMISSION column; remaining ~36 rows continue on property/markets seats (not systems).

## Reversal

Revert only with operator ruling and doc revision history entry.
