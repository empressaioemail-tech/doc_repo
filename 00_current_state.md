---
id: 00_current_state
title: Current state snapshot — 2026-08-30
status: pointer
last_updated: 2026-09-18 (21:55Z, integration seat checkpoint: P-263 applied in five counties, Williamson held for P-365, ten Wave 2 PRs merged, Property Explorer and cortex-api deployed; 21:25Z, SmartCity planner from the integration worktree: the next wave compiled, the design gate read at the moment it went GREEN, and G-148 closed with its findings disclosed. `g152-public-works-fire-ems` queued behind `g153` and `g163-opaque-platform-routes` behind `g162`, each compiled and deliberately held. The gate reached `exit 0` / `verdict: FINISHED` at 20:32:13Z reading 18 of 20 folders instrumented with zero findings, and **all four instruments it counted FAIL**; `g148` then closed at 20:43:47Z and disclosed the same thing as **11 findings, 4 + 1 + 4 + 2, recounted by the planner from each instrument's own total line**, listed by name as `leave_behind` and owned by no row. R3 tests that an instrument EXISTS and never that it passes, so `exit 0` means "instrumented" rather than "clean". A new instrument, `scripts/govtech/design-instrument-exits.mjs`, reports exit codes beside the gate verdict and derives design-vs-instrument ownership (self-test 6 of 6), and it caught this seat leaving a CLOSED RATIFIED design board modified during a batch sweep. OPS-17 A-154 through A-158. Start from `_inbox/2026-09-18d_HANDOFF_smartcity_planner.md`. Note: this worktree is the integration seat and refuses `_state/` writes, so `_state/govtech/STATE.md` was NOT updated. Earlier the same day, 18:40Z, integration seat session close: rest of Wave 2 compiled and nine lanes in flight, San Marcos re-check HOLDS, P-258 found a no-op (P-363), ruling 5 amended (A-224), migration 018 applied; start from _inbox/2026-09-18c_HANDOFF_integration_seat.md; earlier the same day, 18:35Z, SmartCity planner session: the D-13 ship to `dolphin-app` performed and accepted on the deployed surface (deployment `e98f127c`, serving `7487d7c0` with the platform base set), D-14 and D-13 both LANDED, G-142 CLOSED and G-147 CLOSED-PARTIAL so the design gate's two R4 findings are gone and it exits 1 on four R3 findings only, G-148 and G-160 compiled but not yet hand-carried, and one new row G-163 for the two platform routes that fail opaquely on `walrus-app`; earlier the same day, 16:20Z integration seat close: seat Wave 1 done, Wave 1 integrated, #175 / P-332 / LDT deployed and graded, A-217 to A-221, Wave 2 begun; start from _inbox/2026-09-18b_HANDOFF_integration_seat.md)
applies_to: portfolio
related: [_STATE, 90_operations/OPS-16_texas_market_plan_of_record, 90_operations/OPS-17_govtech_stack_plan_of_record, 90_runbooks/AGENT_CONTRACT, 90_runbooks/current_state_protocol]
---

# Current state snapshot

**2026-09-18 CHECKPOINT, LATE EVENING (integration seat; the only planning session for this program).** The evening handoff below is CONSUMED. Read `_sessions/2026-09-18c_p263_apply_wave2_merges_deploys_claude_code.md` first, then **`_inbox/2026-09-18_phase0_closeout_REGISTER.md`** (the durable list, updated to A-225). P-263 is applied in five counties (`_inbox/2026-09-18_p263_apply_RECORD.md`); **Williamson is held by the operator for P-365** (the apply never renews its write lease; dispatch compiled). Ten Wave 2 PRs merged (`_inbox/2026-09-18_wave2_merges_RECORD.md`); Property Explorer `fohg2os70` and cortex-api `00843-yir` deployed and graded (`_inbox/2026-09-18_wave2_deploys_RECORD.md`); P-362 customer-closed.

**2026-09-18 SESSION CLOSE, EVENING (integration seat; the only planning session for this program; start from the new handoff).** Read `_inbox/2026-09-18c_HANDOFF_integration_seat.md` first, then **`_inbox/2026-09-18_phase0_closeout_REGISTER.md`** and `_inbox/2026-09-18_p342_migration_018_RECORD.md`. **First job: P-263's apply**, the largest production write of Wave 2: migration 018 is live on `hauska_mcp` (18:12:08Z, verified by violation); the apply still needs an engine job (none exists), fresh dry runs, then one county at a time under ruling 11 (McLennan's 1.0 share needs its printed override token). **Done this session:** the rest of Wave 2 compiled (13 dispatches; the operator fired nine: P-333, P-334/P-329/P-330, P-352, P-361, P-351, P-358, P-359, P-362, P-363); A-222 (P-324 ships with its staged events, CAPI and scrubbing; all three Property Explorer variables set, the token by the operator at a masked prompt); the San Marcos coverage re-check HOLDS (88.38 percent under corpus 1.4.0); **P-258's re-run found to be a no-op** in all six counties, because the setback writer never reopens an earned value, so 387,237 cells still cite corpus 1.1.0 (P-363 carded); the P-255 census re-graded (false absences 219,472 to 0; 58,339 unaccounted remain); **A-224: ruling 5 amended**, so a default setback line applies only where the city's ordinance sets one (Manor and Lago Vista are zoned; P-364 carded). **Open lane PRs, none merged:** map #421 to #424, LDT #720 to #722, factory #179 to #182, setback-corpus #11, engine #474 (updated in place). Merge orders are in the handoff.

**2026-09-18 SESSION CLOSE, AFTERNOON (integration seat; the only planning session for this program; start from the new handoff).** Read `_inbox/2026-09-18b_HANDOFF_integration_seat.md` first, then **`_inbox/2026-09-18_phase0_closeout_REGISTER.md`**. **Done today:** the seat's Wave 1 (P-348 read, fix P-352; P-337 built, ledger 67 -> 55 open; P-347 partial, customer leg 0 -> 23 PASS of 45 with the MCP signed in, tier still unrecorded); dblink dropped; #175 deployed and graded (390/390, trigger resumed); P-332 live and graded; the LDT halves live on cortex-api `00841-jeh` (canary 90/90); the six Wave 1 lanes integrated (five PRs merged; engine #474 held until P-361 re-pins it to the 0.05 threshold). **Ruled:** A-218 (roads accepted for Phase 0; Georgetown served from its adopted rewrite, P-354 names both dates) and A-220 (destructive-write threshold 0.05 program-wide; P-335's pair authoritative). **Next:** the operator fires five compiled Wave 2 dispatches (P-354, P-339/340/341, P-353, P-270 address half, P-336); the seat finishes compiling Wave 2 (factory group first) and runs its own Wave 2 one write at a time, starting with the San Marcos coverage re-check before P-258's re-run. **Corrections:** P-264 closed partial at 00:34Z; P-206 was already deployed; the publish lane's LDT bake pin is stale (no bake publish until P-351).

**2026-09-17c SESSION CLOSE (integration seat; the only planning session; start from the new handoff).** Read `_inbox/2026-09-17d_HANDOFF_integration_seat.md` first, then the roadmap `_inbox/2026-09-16_texas_scaleup_ROADMAP.md`. **The incident:** Williamson's production publish (run `7b2540c8`) completed and retired ALL 602,050 served tier-1 rows for 48491, including the 319,480 numeric rows that were live, because the published parcel index is R-keyed while the served nodes are numeric-keyed and the retirement step differenced one keyspace against the other. P-306 had fixed the coverage FLOOR for exactly that split, so the floor passed at retention 1.1666 while the writer emptied the county: a control that measures one thing does not protect the thing next to it. Restored by copying 319,480 rows from a Neon point-in-time branch at 19:22:29Z over `dblink`, verified by an independent census and on the customer surface, with an R-keyed parcel shown still correctly retired. **The finding that outranks the defect:** ENFORCEMENT.md has said "a mass state change refuses before it lands" since the 2026-09-15 Bastrop reconcile retired 92.5 percent of that county the same way; nobody built the refusal, and two days later the class took a second county through a different writer. **No production publish of 48491 until P-319 ships.** **Ruled (A-212):** ship without Cotality — about two weeks out, struck from the Phase 0 exit, P-267 and P-283 deferred to Phase 1, P-322 ships the affected rails as declared labelled absences; recording it exposed that `_state/shared/STANDING_DECISIONS.md` still read "COTALITY IS EXTINGUISHED" at HEAD and was propagating that into every compiled dispatch. Also ruled: the stale-tag cleanup (P-323). **All six Phase 0 lanes merged** (factory #171, #172; engine #470, #471, #472; LDT #715, #716; map #416), each diff read before merge. Two rows their dispatches named were NOT built and are now their own lanes: P-270 (the widest customer defect, 29 of 31) and P-286. **Customer-done:** Property Explorer `3cpnl30o0` (P-272, P-291), verified on the live alias. **Blocked:** the LDT legs, because P-279's check exits 1 on 15 failing cortex-api tags until P-323 clears them. **Handed off:** the engine deploy. **Measured:** 0103 applied and verified at the data (backfill gap 0); six-county completeness re-graded against a current verdict table, VERDICT INCOMPLETE, of 65 rails Travis 40 / Bastrop 38 / Caldwell 38 / Hays 38 / McLennan 37 / **Williamson 33**, `false-earned: none` in all six — Williamson's gap is the D1 hold, three rails refusing on all 282,570 parcels. **CORRECTED LATER THE SAME SESSION:** D1 is the biggest lever on WILLIAMSON's number and the SMALLEST of the four blocks across the exit — counted properly, the 67 open cells are 30 mid-cutover (P-204), 30 setback (58,339 parcels refusing), 12 roads/edgeSignal (P-264) and 6 D1. Generalising from one county's headline was the error the ledger-headline rule exists to prevent. **Corrections to this seat's own records:** engine-api FOLLOWS LATEST and retrieval-api is PINNED (the reverse was recorded, in the dangerous direction); 0103 was blocked by a 7.3-hour query holding `AccessShareLock` on `txgio_parcel`, not by the publish; PE production was `5jtk8s0dw`, not `mfesp954e`. Amendment A-212; rows P-319 to P-324. Session: `_sessions/2026-09-17c_williamson_recovery_and_phase0_integration_claude_code.md`.

**2026-09-17 SESSION CLOSE (integration seat, the session after 06a91261; the only planning session; start from the new handoff).** Read `_inbox/2026-09-17c_HANDOFF_integration_seat.md` first, then the roadmap `_inbox/2026-09-16_texas_scaleup_ROADMAP.md`. **Live:** the gate (P-252, P-298); cell serving (P-297); the height guard and P-258 tables (P-299, corpus 1.4.0) and now its engine half with P-282's re-vendored slate (retrieval `00100-hut`, engine-api `00251-qed`); P-249 and P-303 (Waco-type parcels draw, figure withheld); P-304 (no area figure without a verified atom); P-296 (ETJ is a real read, cortex `00824-qay`); P-302; P-307 (a lease key names a real store, factory-control `00010-hax`). **Applied to the stores:** P-256 in all six counties (219,472 parcels, each county exactly at its A-199 ceiling) and P-266/P-268 in five counties plus the zoning writer; Williamson's own record-fill is held under ruling D1. **Republish:** five of six graded; Williamson's staging passed on P-306's keyspace fix and its production leg was running at close. **Running:** six Phase 0 lanes (card truth, Williamson identity, setbacks and envelopes, controls, tagged revisions, Burnet). **Open:** migration 0103 retrying behind the publish's locks, the Austin stamp, P-263's apply (operator's go), the gate and completeness re-grade, and an OAuth token for P-254's MCP leg. Amendments A-207 to A-211; rows P-306 to P-318. Session: `_sessions/2026-09-17b_phase0_applies_and_serving_claude_code.md`.

**2026-09-16h SESSION CLOSE (integration seat; the only planning session; start from the new handoff).** Read `_inbox/2026-09-17_HANDOFF_integration_seat.md` first, then the roadmap `_inbox/2026-09-16_texas_scaleup_ROADMAP.md` (live state, serving revisions, ordered queue). **Time-sensitive at close:** the gate scheduler apply `factory-publish-gate-sched-8x4jk` is still running (four counties graded clean; Travis 55 of 65; Williamson next), the hourly trigger is **PAUSED**, and the integration seat's factory-store lease expires 2026-09-17T02:40Z; the next session grades the apply, manages the lease and resumes the trigger only on a PASS, then runs the six-county republish. **Running:** six lanes the operator fired (P-297 LDT half, P-299, P-298, P-294, P-285, P-259); the next session waits for their closes. **Ruled this session:** the county verdict is a grade, not the serve switch (A-193, OPS-24 law 7, P-297); four setback-corpus rulings and three ledger-serving rulings (A-199, two decision records); P-256's apply authorised with a per-county ceiling after P-297 is live; P-284's cost basis. **Live:** the gate fix and both verdict readers (P-252, P-292, P-293; migrations `0011a` to `0014`), heavy-scan leases (P-281), stage records with a measured cost (P-284), the amendment-id gate (P-277), P-205 customer-done (retrieval `00096-div`). **Merged, not deployed:** P-258's 258 setback rows (corpus 1.3.0 published; LDT `44029db`), which wait on P-299's height guard; P-262. **Held:** P-256 (factory #160). Amendments A-191 to A-200. Session: `_sessions/2026-09-16h_gate_rollout_and_serving_ruling_claude_code.md`.

**2026-09-16g SESSION CLOSE (integration seat; the only planning session now).** The Texas scale-up scope is **rev 4** after a final teardown (A-183), with every Phase 0 item on a row (P-252 to P-296). **Live today:** hauska-engine `d88cf65` (engine-api `00247-san`, retrieval-api `00092-lag`) with P-248 footprints on the sheets, P-261 verified-only area figures and P-210 coverage, checked on exported PDFs and the live endpoint (A-188); the doc_repo close gates run inside git in every worktree (P-280). **Built as instruments:** the setback census (P-255), the repaired Phase 0 ledger leg (P-253, baseline INCOMPLETE) and the P-252 rollout dry run. **Found:** the bake customers read has no trigger, so five of the six counties serve 2026-09-10 data (P-230); the operator ruled both a republish trigger (P-294) and the ledger as the serving path (P-295), and gave a go to republish the six after the gate fix (A-190). **Open PRs awaiting the integration seat:** factory #157 (P-252), #158 (P-284), #159 (P-292); LDT #701 (P-249), #702 (P-293); hauska-map #409 (P-249). **In flight:** P-258 (setback campaign wave), P-256, P-262, P-281. **Operator owes:** the P-278 credential rotation go, the Cotality contract, an account check for P-243 and P-244a. **Start here:** `_inbox/2026-09-16_HANDOFF_integration_seat.md`, then the living roadmap `_inbox/2026-09-16_texas_scaleup_ROADMAP.md`. Session: `_sessions/2026-09-16g_texas_scaleup_rev4_and_phase0_wave_claude_code.md`.

**2026-09-16f SESSION CLOSE.** The three in-flight lanes from the prior close all landed: **P-243**
(smartsite-mcp binding, panel resource_link now attached to `get_smart_site`) merged and deployed to
`smartsite-mcp-00126-yak` — the code-level fix is live; **the customer predicate (a panel actually
rendering in a real Claude client) is NOT verified by this seat and needs the operator's own check.**
**P-244b** (declared-wait envelope) merged and deployed to `hauska-mcp-server-00100-cal`. **P-244a**
(site-plan compose misclassification) merged and deployed to `hauska-engine-api-00238-tax`, and the
underlying root cause was then also fixed same session: `--no-cpu-throttling` applied to
`hauska-engine-api`, now serving `hauska-engine-api-00240-qig` at 100 percent. **The real-fix
verification (the `48021:34049` job actually reaching `ready`) is still not independently confirmed** —
`engine-api` refuses direct calls (`gate_front_context_required`, correct posture) and the test account's
Solo tier blocks a live MCP trigger; owed next session or by the operator's own client.

**THE LARGEST UNVERIFIED BLIND SPOT ON THE BOARD (P-200 through P-213) IS NOW CLOSED (A-178).** An
internal read-only sub-agent re-verified all 14 rows against fresh clones and live probes. Four resolved
since filing (P-202, P-203, P-207, and **P-212, the SEV-1 mass false retirement, fully fixed and verified
three independent ways**); two ruled but not built (P-209, P-210); one improved but not customer-visible
(P-205); three unchanged (P-201, P-204, P-206); and **P-213 (the blast-radius refusal) was confirmed
STILL UNBUILT by reading the actual write path** — the exact code that caused P-212 was untouched.
**P-213 was re-dispatched and built same day**, merged into `hauska-engine` (PR #457): a generic,
reusable pre-write refusal (`writer-blast-radius-guard.mjs`, ported from hauska-factory's P-236 template),
wired into the parcel-node reconcile writer before OPS-24 runs Burnet through the same pipeline.
**New defect surfaced, not one of the 14:** two independent lanes (P-200, P-211) found `get_smart_site`
apparently serving a periodically-baked snapshot rather than a live ledger-cell dereference, contradicting
the "ledger is the serving path" canon. Flagged, not yet carded.

**Four more PRs merged same session, on top of the three above: P-246** (legacy-design-tools#697,
the `valueHistoryFact` dollar-value tier-gate bypass — a Solo caller was refused CAD dollars on
`onRecord.cadRoll` and handed the same dollars one field later; both routes now gate every entry),
**P-247** (hauska-map#408, the Smart Site sign-in redesign — three states, real data wired to three
live-verified sample parcels, two self-caught bugs fixed along the way), **P-238** (hauska-engine#456,
found the dispatch's own premise FALSE — the X-ray dossier footer defect it described had already been
fixed by P-228 the same day it was filed; the lane added the missing regression coverage instead of a
duplicate fix), and **P-213** (above). **ALL FOUR ARE MERGED, NOT YET DEPLOYED — deploy is owed next
session**, deliberately not started this close given context constraints; do not treat "merged" as
"customer-done" for any of the four (P-246 in particular is a live paid-tier bypass fix sitting merged but
unshipped).

Full detail: `_sessions/2026-09-16f_dispatch_cycle_and_p200_213_closed_claude_code.md`. Prior durable
card: `_inbox/2026-09-15_reports_and_envelope_WDLL.md` (now largely superseded by the above).

**2026-09-15e SESSION CLOSE. The serving revisions in this paragraph are the ones to trust, and
they were read by field on 2026-09-16.** Eleven rows shipped and verified on customer surfaces in
one session: P-220, P-222, P-235, P-236, P-237, P-239, P-240, P-242 and P-242b, plus
re-verification of the inherited set. Serving: `hauska-engine-api-00236-few` (digest
`sha256:87d7e580...f4d3`), `hauska-mcp-server-00098-rol`, `cortex-api-00807-wib`,
`smartsite-mcp-00124-bub`, property-explorer alias serving `index-Dm73unVg.js`, factory writer jobs
gen 5 / gen 4 and publish gen 46. **Nothing is merged-but-unshipped.**

P-240 was a three-repo coordinated cutover, run as migrations then engine then consumers because it
retired the synchronous response shape rather than leaving it coexisting. It worked and immediately
exposed **P-244**: site-plan compose fails on its own at roughly 116 seconds, which the 55,000 ms
client abort had been masking as a cold start. **That affects the STANDALONE export route only; the
composed feasibility path is healthy (9 of 9 jobs ready), which is how site plans actually reach
customers.** The planner initially recorded this as a product-wide failure and the operator
corrected it.

**P-245 is PARTLY SATISFIED (2026-09-16, A-171).** The operator moved the test account to paid
Solo, and the server confirms `subscriptionTier: solo`. That closed P-220's and P-242's live failing
directions and, on its first read, found **P-246**: `get_smart_site` refuses a Solo caller the 2025
dollar values in `cadRoll` and hands them over in `valueHistoryFact` in the same response. Still
owed under P-245: a free-tier identity, one CI can use, and a change to the Studio-tier default test
fixture. **P-243, P-244a and P-244b are in flight**, fired by the operator from the 2026-09-16
dispatches.

**P-200 through P-213 were NOT re-verified in this session and were not guessed.** That block is the
largest blind spot on the board. Related: **OPS-16 carries no status column** - every row from P-200
to P-245 reads `ADDED`, and real status lives scattered across 170 amendments, so "where are we"
cannot be queried, only re-read.

Durable card: `_inbox/2026-09-15_reports_and_envelope_WDLL.md`. Session:
`_sessions/2026-09-15e_qa_reconciliation_and_async_cutover_claude_code.md`.

Pointer doc. Live revisions and standing decisions live in `_STATE.md`. Do not treat this file as the serving-revision ledger. **Caveat added 2026-08-31: `_STATE.md` is a pure concatenation of the six `_state/<seat>/STATE.md` files and carries NO smartsite-mcp or cortex-api revision at all; every seat source file predates its 2026-08-29T01:39Z generation, so regenerating it would refresh only its timestamp. For Smart Site MCP and cortex-api revisions, read Cloud Run by field name, not this file and not `_STATE.md`.**

**Canon change 2026-08-22.** [`19_the_instrument_contract.md`](19_the_instrument_contract.md) is ratified and governs the model portfolio-wide: node, atom with an intensional subject, edge, layer, instrument, lens, access as two fields, and the four-property benchmark. It supersedes `77_place_graph_strategy` as north star and subsumes `05_living_lineage_thesis`; `_rd_disclosure_twin/09_twin_read_contract` and the atom contract package sit under it as its executable forms. Program at [`24_instrument_conformance_program.md`](24_instrument_conformance_program.md), positioning at [`03c_records_as_instruments_positioning.md`](03c_records_as_instruments_positioning.md), seat briefs in `_inbox/2026-08-22_{property,substrate}_seat_instrument_brief.md`. **Nothing in 19 may be claimed in present tense externally unless its armed table says armed.** This header's date predates the change; edited as a single pointer line because this file has concurrent writers.

**CTX containment COMPLETE 2026-09-01.** All six Central Texas counties have `landing_parcel_jurisdiction`, zero unresolved; A1 closed and TOTALS is unblocked (Williamson 282,570 run `2a40517b`, Travis 380,917 run `dd58e803`, both `covers-v1`; the earlier four are `intersection-v1` and the six must never be averaged across method versions). Work is now claimed from a card queue rather than hand-carried: `_queue/README.md`, loop spec `90_runbooks/seat_loop.md`, ruling `_decisions/2026-09-01_seat_queue_protocol.md`. The Neon Tuesday 05:00-06:00 UTC maintenance window is enforced as a refusal in `_queue/config.json` and is still owed a move in the console. Session `_sessions/2026-09-01_ctx_containment_and_seat_queue_claude_code.md`.

**P-124 CTX COMPLETION SPRINT opened and Wave 1 executed 2026-09-07.** Ten concurrent lanes, eight closed. THE BAKE MUST NOT RUN YET and the reason is not caution: a 2026-09-03 StratMap reload replaced every parcel row in all six Central Texas counties and dropped the derived zoning stamp, leaving `zoning_district` NULL on 1,516,110 parcels (verified live, with Comal 48091 as an unreloaded control still at 28,305 on the same column and predicate). The 582,442 baked snapshot rows still carrying a real district were the ONLY surviving copy of that value, and the conformant bake lane that wrote them has no monotonic guard, so a bake before a re-stamp would have overwritten the last copy with nulls. Wave 1 re-stamped all six (Bastrop 12,199, Caldwell 10,310, McLennan 55,425, Hays 51,503, Williamson 158,957, Travis 35,272 plus an Austin apply in flight), each verified with an out-of-city NULL check and the Comal control. **Nothing destructive has run; every write went to `txgio_parcel` or added atoms and the served snapshots are untouched.** ALSO ESTABLISHED, and each reverses something previously recorded: there are TWO bakes, and the LDT facet bake consumes no setback tables at all, so setbacks, envelopes, footprints and boundary edges come from elsewhere and three of the four from the hauska-engine atom bake, which has no runner in any of the 33 Cloud Run jobs across both projects. `county_facet_coverage` is wrong in both directions (Bastrop zoning 99.77 percent from a 433-row sample against a real 12.3; Travis 0.00 from `sampled=0` with `staleness_flag=false` against a real 46.6) and must not be used to scope anything. `parcel_record` is drawn from the StratMap parcel layer rather than the CAD account population, confirmed at row level. Parcel identity churns across reloads (Hays 30.5 percent gone and 19.5 percent drifted; Bastrop zero across 400 samples) and healthy-looking aggregates mask it because new parcels backfill the count. Read A-115 in OPS-16 before resuming: it is the complete resumption contract and Wave 2's preconditions, consolidated because the material is otherwise spread across four amendments and eight lane closes. Session `_sessions/2026-09-07_ctx_completion_sprint_claude_code.md`.

**P-124 WAVE 2 PRECONDITIONS CLOSED 2026-09-08, AND THE BAKE IS NOW BLOCKED ON ONE ARTIFACT THAT DOES NOT EXIST.** Both A-115 preconditions are done and neither closed as written. The readiness gate was refusing Williamson at 46.9 percent and Caldwell at 51.4 percent for a defect that does not exist: `parcel_record`'s intended population is `landing_parcel_jurisdiction` (what `parcel-record-fill`'s own `LANDING_PAGE_SQL` pages), not the `cad_property` ACCOUNT roll, and a parcel is not an account. Measured as real id-set intersections the two match EXACTLY in all six counties, so neither county was ever under-instantiated; the 0.99 floor is unchanged and the measurement was corrected instead (`c59965e`, PR #101, proven by violation with Dallas and Tarrant refusing on real data). The cadRoll card rested on a superseded premise (the CONFORMANT bake at the pinned SHA always reads `cad_property`; CTX-F's rationale was true of the LEGACY CLI only) and its unblock trigger had NOT fired (CTX-C's situs guard is unmerged in LDT PR #637), so the POST-condition was built without the unsafe fold-in (`df22ea88`, PR #102). **The bake then refused for a real reason:** `zoningDistrict` was unaccounted on 79,197 in-city parcels across all six counties. That was TWO populations, and `parcel-r5-zoning` already carried the honest resolution for the larger one but had not run since 2026-09-02, before the reload and before its own 2026-09-05 fix. Re-run: `refused` 0 to 73,321 (each carrying "a data-acquisition gap, not a join failure"), `unaccounted` 79,197 to **5,876**, with `value` and `not-applicable` both untouched. **THE REMAINING 5,876 ARE THE WHOLE BLOCKER.** The rail gate is zero-tolerance (`publish-gate.js`: `ok: unaccountedCount === 0`), so all six counties still refuse. Clearing them honestly requires a PER-CITY COMPLETENESS DECLARATION that exists nowhere in the stack: without it nobody can say a city's staged zoning layer is complete, and a parcel matching no polygon is indistinguishable from a hole in our own layer. That artifact is the last thing between P-124 and a bake. Bastrop's 181 is the pilot; Travis 2,758 and Williamson 1,271 carry most of the rest. Do not clear it by writing `not-applicable`. Decision, reversal criteria and a relabelling tripwire: `_decisions/2026-09-08_zoning_unaccounted_two_populations.md`. Session `_sessions/2026-09-08_wave2_gate_rebuild_claude_code.md`, amendment A-116.

**OPS-24 TEARDOWN FOLDED IN 2026-09-14 (A-150).** All four teardown findings held: the gate passes total absence (P-195 dispatched, first build row); Law 1 was unarmed (the close gate now covers P-151..P-174 and P-186..P-198 with a visible predicate debt; the probe side is P-197); "94 rows" was never counted (about 87); the atoms lease is already per county and the hole is identity. Burnet: parcels and a 2025 roll on production, zero address points (one loader run).

**OPS-24 COUNTY TO SERVING OPENED 2026-09-14 (A-149).** The thirteen-stage map is now a program: rows P-186 to P-198 with predicates and instruments, a preamble with six laws, and the order fixed (teardown, then the gate fix P-195, then Burnet end to end through the existing pipeline, then the farm). `_dispatches/2026-09-14_ops24-teardown_dispatch.md` is compiled for a fresh planner, read-only; nothing is built until it returns. Plan `90_operations/OPS-24_county_to_serving_program.md`.

**F24 RESOLVED 2026-09-14 (A-148):** Bastrop's current zoning layer (`Zone_Types/25`) says 30/10/30/20 in text and 25/5/25 in stale numeric columns that the One Click join reads; our card matches the city's words. Wave-6 P-154 and P-156 lanes amended by addendum.

**WAVE 6 COMPILED 2026-09-14 (A-147):** `p178-publish`, `p183-querypoint`, `p156-bastrop` (the CTX bake's blocker, per-city zoning declaration), `p154-conflict` (F24: Bastrop's One Click layer prints the repealed 2019-51 values; our card follows the ordinance and must say so). Dispatch `_dispatches/2026-09-14_ops23-wave6_dispatch.md`.

**WAVE 5 AND P-178 ACCEPTED 2026-09-14 (A-146).** P-152 passes on 1109 Pecan; P-155's refresh fix is live; P-179 armed; P-180 done on Hays; the certified Hays roll is on production; its publish waits on a P-180 walk fix (RULED option B). P-183 (query point through the crosswalk) and P-184 (Williamson) added; the vacant-lot label leg ruled. Next wave: P-180 walk fix, P-178 step 6, P-183, P-160, P-167 strings.

**SESSION CLOSE / HANDOFF, 2026-09-13T23:55Z, doc_repo main `ea3a7fd2`.** OPS-23 is the live
program; its durable card (`_inbox/2026-09-11_ops23_ledger_serving_path_WDLL.md`) now opens
with a dated STATUS block — **read that block first**, this paragraph only points at it. In
one sentence: wave 5 is in progress right now in the dispatch-planner seat worktree after a
mid-wave `429` killed all four running lanes simultaneously; the planner wrote a full recovery
checkpoint (`_inbox/2026-09-13_ops23_checkpoint_5.md`, now on main) and a successor planner
session has already resumed (confirmed live by the overseer via lease renewals at 23:46–23:55Z
and two open PRs, #678/#679, both green pending one check). **The single highest-consequence
open item:** lane `p180-hays-cells` asked to run `--apply` against 116,420 PRODUCTION rows
having shown only a dry run and no confirmed staging execution, and was killed before the
planner answered — do not approve that production write until the checkpoint's
staging-target question is settled. P-178 (the Hays declared-roll re-ingest) is separately at
step 4 of 6, staging done and verified at source, production load waiting on the operator's go
in that lane's own thread (the shape of the run is already signed off; the literal go on
staging's counts is not yet given). Three rows landed on main this session
(P-176, P-181, P-182) from sessions/commits **outside this thread's review loop and are
UNVERIFIED by the overseer** — read them before citing their numbers. Full reconciliation,
every ruling, every PR by number, and the five-lane wave-5 state are in the WDLL card's status
block and in checkpoint_5; this snapshot paragraph is a pointer, not a substitute.

**P-179 AMENDED 2026-09-13 (operator agreed): option 1, the engine gate is armed from the existing `HAUSKA_ENGINE_API_KEY` secret the four callers already send; no new secret minted; IAM door still its own go.** Wave 5 and P-178 back in flight.

**F23 and P-155 REOPENED 2026-09-13 (A-143):** the feasibility refresh produces no new document (byte-identical exports created before the engine deploy); wave-5 addendum lane `p155-refresh`. P-167 wave-5 lane closed-partial; the LDT 1.34.0 bump was omitted from its worktrees (overseer error) and is an addendum. P-178 items 1 and 2 landed; the staging load proceeds inside the dispatch; production waits for the go.

**APPROVALS 2026-09-13 (A-142):** the engine gate token is approved for mint and mount (IAM removal is a separate go); P-178's migration run is signed off as a watched run (0099 marked applied where verified, 0100 only where unrun and only watched, 0101 alone); the siblings ruling stands; the P-153 lane close is filed. P-178 item 1 landed (LDT #675, #676). Wave 5 SENT.

**OPS-23 WAVE 5 COMPILED 2026-09-13 (A-141):** `p152-siblings`, `p180-hays-cells`, `p179-gate` (stops for the operator at every secret and IAM step), `p167-strings`; dispatch `_dispatches/2026-09-13_ops23-wave5_dispatch.md`, hand-carried to a fresh planner window rooted in the seat worktree. Owed by the operator before p179-gate can execute: the go to mint `ENGINE_API_GATE_TOKEN` in both projects and mount it; before P-178 can load: a watched migration run.

**OPS-23 WAVE 4 REVIEWED and P-177 REVIEWED 2026-09-13 (A-140).** The shared resolver is live on the MCP path (30/10/30/20 dated 2026-04-14 for 1109 Pecan); the panel still prints the atom-chain axes (hauska-map axis override) and five allowlist siblings are unslated (RULED: per-rail entries, the representative-key grouping retired). P-152 lane 5 CLOSED (tier gate in the engine and the MCP). **F22: `hauska-engine-api` is public with no gate token in either project; P-179 ADDED (needs the operator to approve minting and mounting the secret).** P-177 closed-partial: all five Sturgeon nodes are right on the MCP path; the Property Explorer still fails because the record path reads `parcel_record` filled by a bare-number join; **P-180 ADDED**. P-178 in progress (LDT #675 merged, #676 open); its migration run must be watched and operator-signed (0100 is the Travis backfill). Rows still not dispatched: P-156, P-161 to P-166, P-168.

**TWO HAYS RULINGS 2026-09-13 (A-139).** Node identity: the parcel-map id stays the node id and `place_key`; the appraisal account is an attribute through the crosswalk; re-keying on the account REJECTED (P-177 executes; P-161 carries the type; OPS-21 identity law amended). Declared roll: the 2026-08-26 export is Hays' 2026 vintage, superseding the preliminary; **P-178 ADDED** (cloud re-ingest after P-177; 390 accounts marked, 375 inserted, identifiers digest-verified; dispatch `_dispatches/2026-09-13_p178-hays-roll_dispatch.md`, hand-carried, starts when P-177 publishes). Until it lands every Hays dollar is a notice value.

**OPS-23 WAVE 4 COMPILED 2026-09-13 (A-138) on the operator's ruling that the P-154 resolver is shared** (`_decisions/2026-09-13_share_the_most_current_setback_resolver.md`: corpus subpath `./resolve` 1.2.0, three consumers, engine copy retired, Bastrop cells re-run). Lanes: `p154-share`, `p152-slate` (after the re-run), `p152-entitlement`, `p167-pdf`; dispatch `_dispatches/2026-09-13_ops23-wave4_dispatch.md`. P-177 (Hays) is running hand-carried in parallel.

**OPS-23 WAVE 3 REVIEWED 2026-09-13 (A-137): P-157, P-158, P-173, P-174 CLOSED; P-152, P-154, P-167 PARTIAL.** Every predicate re-run live by the overseer; P-152's "closed PASS" in the wave close corrected to partial (panel and MCP setbacks still disagree on Bastrop; two of seven rails serve record). Fourteen merges this wave; F21 fixed. OPS-23 closed so far: P-151, P-153, P-155, P-157, P-158, P-159, P-169, P-171, P-172, P-173, P-174; partial: P-152, P-154, P-167; built: P-160 (probe), P-170 (lease gate); not dispatched: P-156, P-161 to P-166, P-168. Owed rulings: share the P-154 resolver (substrate grant or package publish); Hays node identity (P-177 runs option A meanwhile).

**P-175 REVIEWED 2026-09-13 (A-136): closed-partial stands, and the served cards are wrong on all five.** The backfill, H1 (P-145 CLOSED, 37,927 different-key) and the resolver are real; the tier-1 publish re-baked Hays but the rebind did not reach the served polygon: `48209:97658` still serves the Sturgeon ring, cells and flood AO under the Mesa Verde label and dollars, and ALL FIVE Sturgeon TxGIO ids collide with real Austin-area accounts, so the nodes the resolver routes to serve Austin dollars and anchors on Sturgeon polygons; the account-keyed `48209:84639` has no ring and no cells. The flood determination is right on every lot (Zone AO). **P-177 ADDED**: account attributes through the crosswalk for every gate-blocked node (option A); option B (mint on PropertyID) is an identity ruling owed. Also on main: P-176 cotality bake-off (another session, 2026-09-12).

**P-175 HAYS ONLINE opened 2026-09-12 on a customer request (A-135).** Five Sturgeon Dr lots in San Marcos: 629 resolves to node `48209:97658`, a chimera (the Sturgeon polygon and its geometry-derived facets, including flood Zone AO, under CAD account 97658's Mesa Verde Dr label, anchor and dollars: the CTX-HAYS-KEY collision as served); 615, 617, 619 and 627 are vacant lots with no house number on the roll, located-unbound in the Find box, though the CAPCOG address points fall inside exactly their TxGIO parcels. LDT #653 and #654 are merged and pinned into the factory publish job, migration 0099 is on both branches, and NOTHING has executed: `quick_ref_id` is filled on 0 of 134,606 Hays 2026 rows and the tier-1 bake has not re-run. P-175 runs the backfill (staging, then production on the operator's go), the H1 disposition instrument (P-145, built and held), the tier-1 publish on the pinned LDT, a situs-resolver containment fallback, and closes on `scripts/surface-probe.mjs --rows P-175` (FAIL on all five today). Dispatch `_dispatches/2026-09-12_p175-hays-online_dispatch.md`, hand-carried. The declared-roll ruling (preliminary vs 8-26-2026) stays open and is not needed for identity.

**OPS-21 SERVE COMPLETION: SETBACKS AND ENVELOPE ARE SERVING IN PRODUCTION, five counties, 2026-09-11.** Fourteen lanes, P-132..P-150 (OPS-16 A-122..A-127). Live on `cortex-api-00768-cus` @100% for 48021/48055/48309/48453/48491: the four setbacks plus setbackRules, parcelAreaSqFt, maxHeightFt, maxLotCoveragePct, maxFootprintSqFt, all served from `parcel_record` cells read live per parcel (no bake). Root cause was never data: `computeTier1Envelope` returns `declined` on BOTH branches and cannot produce a value, the cell writers had no branch for a legitimate `not-applicable`, and the publish gate graded 17 of 65 rails. All three closed. 351,872 parcels carry real setback values; 331,066 false absences corrected with 209,483 correct ones provably untouched. `buildableAreaSqFt`/`Pct`/`envelopeStatus`/`envelopeDisclosure` REFUSE by operator ruling (no approximation state; trigger is road-frontage acquisition, 3P-16). Hays 48209 declines entirely, held on P-145. **Open and invisible by construction: none of the new cells are atom-backed**, extending ADR-031's CTX seam; L4, the divergence tripwire its own reversal criterion depends on, is NOT COMPILED. Plan `90_operations/OPS-21_serve_completion_program.md`, architecture `90_operations/OPS-22_spine_architecture_map.md`, close `_sessions/2026-09-11_ops21_goal_met_five_counties_claude_code.md`.

**OPS-23 OPENED 2026-09-11: THE LEDGER IS THE SERVING PATH, AND THE CUSTOMER SURFACE IS THE PREDICATE.** OPS-21 met its goal at cortex and never repointed the browser: the Property Explorer panel reads hauska-map's own atom-chain adapter (bakedAt 2026-08-05 Bastrop, 2026-07-24 Travis), so record-served setbacks reach `get_smart_site` and not smartsite.cloud. Verified live on `48021:34049`: five setback producers disagree as present, the map refuses a polygon its endpoint returns (Ruling B), the PDF prints two buildable figures; on `48453:113408` the panel renders three of fourteen facts because placement depends on a geocoder that misses the address while the record carries its own point; the feasibility engine completes Travis refreshes in 85 to 154 s with 201 while both clients abort at 55 s. Seven operator rulings with records: most-current source wins; Ruling B reversed for the polygon only; cities are the unit; the surface probe is the predicate; overseer / dispatch planner / lane topology (proposed); cached rendering; and ADR-031 amended so that atoms are canonical, a cell is accounting with a pointer and a cached rendering, one reader lives in `hauska-engine/services/retrieval-api`, one writer, one vocabulary (`_decisions/2026-09-11_ledger_as_serving_path_seven_steps.md`). Rows P-151..P-167 (OPS-16 A-128, A-129). Dispatches compiled for P-151 SEAM and P-153 DRAW. P-160 built: `scripts/surface-probe.mjs` (self-tested both directions, four live runs, baseline PASS 0 FAIL 0 UNMEASURED 9), `scripts/ops23-lane-status.mjs`, and `probe-close-gate` which **did not fire on its first live trigger (`1e174bcc`) because hooks are snapshotted at session start; verified by direct invocation only; live verification owed next session.** Probe findings: the envelope endpoint's address form answered for a Williamson parcel when asked about a Bastrop one; cortex's cached Bastrop county-gis layer has holes at both Bastrop probe parcels while the county's own service has them. Plan `90_operations/OPS-23_surface_completion_program.md`, session `_sessions/2026-09-11_ops23_ledger_serving_path_and_probe_claude_code.md`. **PREAMBLES SCOPED BY PROGRAM (P-168, A-131):** the shared CANON-PREAMBLE is ten fleet-wide bullets; Dashboards law is in `_catalog/program_preambles/OPS-17.md`, Factory status in `OPS-19.md`; the compiler attaches `OPS-23.md` to rows 151 to 167 by row membership and refuses a row whose program file is missing; `generate-combined.mjs` refuses to regenerate `_STATE.md` while a shared bullet names a plan row. Any dispatch showing G-rows in its preamble is a stale compile. **THE DURABLE CARD is `_inbox/2026-09-11_ops23_ledger_serving_path_WDLL.md`**: every row, ruling, finding and owed item with its done-looks-like and instrument; read it before touching this program. **EVENING 2026-09-11, ALL LANES IN FLIGHT.** Topology R-5 is ACTIVE on the operator go; seat `dispatch-planner` is registered (worktree `P:/seat-worktrees/dispatch-planner/doc_repo`, branch `seat/dispatch-planner`, never commits). Wave 1 (P-155, P-157, P-158, P-159, P-167) went to it as one compiled dispatch (`_dispatches/2026-09-11_ops23-wave1_dispatch.md`); it re-synced to `a90adbb5`, compiled all five lane dispatches at 21:34Z, and the lanes cut their worktrees. The five missions were written from read-only verification of origin/main (`_inbox/2026-09-11_ops23_wave1_verify_p1*.md`) and name the plan-row errors up front: the TCAD PACS loader lives in LDT, not the factory, and treats the improvement-detail file as optional and silent, so `absent-verified` is stamped on a field nobody looked for; the 10pct footprint label is emitted by the engine and hides three join outcomes, and the county writer never ran for Bastrop or Travis (near-bbox returns zero live); the PDF has two figure sources rendered on four surfaces with nothing checking the narrative; five byte-identical vocab copies, two parity locks, a drift test that skips in CI, and no package dependency in hauska-map or smartsite-mcp. The probe carries predicates for those five rows (self-tested both directions; live baseline 4 FAIL; guarded so importing the module never probes production). P-151 PASSES on the surface for both Travis parcels (`_inbox/2026-09-11_214505_surface_probe.json`; operator screenshots as data in `_inbox/2026-09-11_p151_observations.json`; account tier Studio or Team from the owner row); its lane close JSON is the last input. F16: the Find box does not resolve `414 SPILLER LN`, a geocoder path P-27 demoted. P-153 DRAW and P-152 READER lane 1 are in flight (P-152 cp1 and cp2 filed; it stops for the RO secret mount). Commits: `0cacf4c5`, `a90adbb5`, `febe8f3e`. **WAVE 1 CLOSED AND REVIEWED, WAVE 2 COMPILED (2026-09-12 03:40Z).** Every lane landed: P-151 CLOSED (probe PASS both Travis parcels), P-159 CLOSED PASS (one atom-backed figure), P-152 lane 1 CLOSED with the row partial (`hauska-retrieval-api-00086-nur` and `cortex-api-00776-wov` at 100 percent by field; row predicate UNMEASURED until the panel consumes the reader), P-155, P-158 and P-167 CLOSED-PARTIAL with the open leg named, P-157 BLOCKED at CP1 (no cloud execution path for the LDT CAD loader; TCAD file names differ from the discovery pattern), P-153 still in flight. Every cited merge verified by conclusion string, every serving revision by field. **The close gate is verified through the harness:** a throwaway close claiming closed on an UNMEASURED artifact was refused at `git commit`; the first attempt, staging and committing in one string, went through (`213f5369`, reverted) and that bypass is now refused too; closes with `planRow` singular are no longer skipped and honest partial closes are accepted. Seven operator rulings, all yes (`_decisions/2026-09-12_*`, A-132): no break-glass, both loaders get Cloud Run jobs (P-169); the TCAD file-name shape is a source declaration; certified 07182026 for P-157; the planner merges engine #421 and P-158 counts the staged layer before any source is bought; one traffic shift at a time per service (P-170, built and verified by direct invocation, harness firing owed next session); the 2026-09-07 footprint write is audited (P-171); the Find box reads the situs index (P-172). Wave 2 (`_dispatches/2026-09-12_ops23-wave2_dispatch.md`: P-152 lane 2, P-157 after P-169, P-158 phase 2, P-167 after P-153, P-169, P-171, P-172) is compiled for a SUCCESSOR dispatch planner resuming from `_inbox/2026-09-11_ops23_checkpoint_1.md`. Correction: the panel's tax-assessed values are session-gated on the same facets path, and the BFF collapses cortex's typed studio-gated refusal to null (P-152 lane 2 fixes it). Live for P-172: the situs index row for `48453:113408` carries an empty street line, bare searches time out at cortex in 5 s, and the earlier Sterling Find landed through the geocoder unlabelled. Latest commits: `f5d4167c`, `ef48e339`, `63c8fd04`. **WAVE 2 REVIEWED (2026-09-12 afternoon).** The successor planner ran seven rows; every cited PR SUCCESS by conclusion, cortex-api `00778-peq`, two Cloud Run jobs (`ldt-cad-ingest`, `hauska-engine-atoms-writer`), and the panel now answers `X-Pe-Read-Path: record` live: the one reader reaches the browser. P-169 CLOSED; P-171 CLOSED outcome (b), the 2026-09-07 footprint write was the engine lane `cente-67` applying from a P:/ worktree under the operator's direction with no break-glass row, now filed as `_decisions/2026-09-07_footprint_writer_breakglass_five_counties.md` (PROPOSED; the operator accepts, amends or rejects); P-172 CLOSED (situs index backfilled, both forms resolve in under 50 ms, geocoder labelled; the 502 was a 5 s proxy budget against a 20 s route); P-152 lane 2 CLOSED-PARTIAL (panel reads the reader for 14 rails; setbacks and zoning still ride the atom chain, so F3 persists on two Bastrop parcels); P-157 BLOCKED (the load ran through the job and moved nothing: the shared PACS parser rolls living area from "MAIN AREA" segments, TCAD types them "1st/2nd/3rd Floor"); P-158 CLOSED-PARTIAL with the real mechanism, `tx_building_footprint.county_fips` MISTAGGED near county lines (29.6 percent of footprints inside Bastrop city limits carry 48021; the anchor has a footprint 1.63 m away), no second source needed; P-167 not started; CORRECTION: P-153's LDT half #661 `6b579020` merged 2026-09-12T00:00Z (a title search missed it; read PRs by number), so P-153 closes on the operator's screenshot plus one probe run and P-167 is unblocked. P-153 CLOSED 2026-09-12 18:00Z on that screenshot (amber inset drawn on 1109 Pecan, figure withheld) plus the lane's MCP observation; probe PASS both parcels. NEW F20 (operator): a search-landed subject shows no setbacks until the user clicks away and back; two placement paths; rowed as P-174 (A-134), a wave-3 addendum with its mission and probe predicate. WAVE 3 is compiled and carried (`_dispatches/2026-09-12_ops23-wave3_dispatch.md`: P-152 lane 3, P-154, P-157, P-158, P-167, P-173, plus P-174). Three new findings from the operator's own PDF-versus-card comparison (F17 to F19), mechanism corrected by verification: the report composer reads the engine substrate atoms store, not the reader (no cortex read, no entitlement reaches the engine), so the PDF lists TCEQ-membership districts where the card prints the record's, prints values UNAVAILABLE where the store has no roll atom and calls an improved parcel unimproved (the P-159 guard starved), and hard-codes city limits as unresolved on every PDF. NEXT WAVE: P-152 lane 3 (setbacks and zoning from the reader; the report composer as the third consumer with the requester's entitlement), P-157 parser (per-county segment vocabulary), P-158 phase 3 (retag by containment, writer, reconcile), P-167 after P-153, P-154. Operator-owed: accept or amend the break-glass row; the P-172 click; the P-155 retry answer; P-153's status.

**P-113 opened 2026-09-02** (OPS-16 A-077): courthouse-records reliability hardening plus the
never-built agent-exposure gap, audited against live `origin/main`, not the stale local
checkout. Two worktrees dispatched under the existing `property` seat
(`legacy-design-tools-p113-reliability`, `legacy-design-tools-p113-mcp`); this was the first
real use of the compiled hand-carry dispatch mechanism rather than a description of it. Both
windows reported idle 2026-09-02, DIAGNOSED 2026-09-03: a new Claude Code tab inherits its
VSCode window's root, so both attempts pasted the dispatch into windows still rooted at
`doc_repo` and each correctly refused to execute rather than build in the wrong repo. Fixed
prompts issued (explicit `code -n` new-window instructions); not yet confirmed successful as of
session close. Session closed for an operator workspace reorganization; restart point is
`_inbox/2026-09-03_PLANNER_HANDOFF_next_session.md`, read that before resuming this thread.
Tracker `_smartsite_gtm/09_crm_courthouse_agent_roadmap.md`. Smart Site dashboard-mode line
PARKED for v2 same session (`_decisions/2026-09-02_smartsite_dashboard_mode_parked_v2.md`);
shell kept as reference, nothing scheduled against it now. Sessions
`_sessions/2026-09-02_smartsite_gtm_courthouse_and_lane_mimicry_claude_code.md` and
`_sessions/2026-09-03_p113_dispatch_mechanics_and_workspace_handoff_claude_code.md`.

**ACQUIRE-GIS wave 1 dispatched, gas rail ruling closed, PE refusal-contract finding
2026-09-03.** Compiled dispatch `_dispatches/2026-09-03_parcel-acquire-gis_dispatch.md`
(PLAN-ROW F-01, repo `hauska-factory`) covers the four rails PARCEL-SCOUT-GIS found
acquirable (schoolDistrict, utilityService water/sewer, overlayDistricts, agValuation
Williamson-only); gas ruled permanently unaccounted
(`_decisions/2026-09-03_gas_utility_service_rail_closed_unacquirable.md`). Both
dispatch/ruling commits (`e813007`, `ed35457`) now pushed to origin/main. **Worktree
now registered** in `_catalog/seat_register.json` (`hauska-factory-acquire-gis`,
standalone clone at `P:/tmp/hauska-factory-acquire-gis`, branch
`feat/parcel-acquire-gis-wave1`) but not yet created on disk — hand-carry still
blocked until the clone exists. Same rooting problem named above applies: a new
OS-level window (`cursor -n "P:/tmp/hauska-factory-acquire-gis"`) is required once
the worktree exists, not a new tab in an already-open window. Separately,
a live PE `atom-miss` investigation confirmed `property-boundary-edge` atoms exist only
in Bastrop (26,846 rows; zero statewide elsewhere, verified live against `hauska_mcp`);
finding filed at `_inbox/2026-09-03_pe_refusal_contract_split.md` naming a legibility
gap between the Doc-19 layer-absence chip contract and the bare `atom-miss` refusal
used by seven atom-family fact reads. Setback rendering confirmed (by code trace only,
not live) correctly independent of boundary atoms. Handoff
`_inbox/2026-09-03_worktree_and_followon_handoff.md`. Session
`_sessions/2026-09-03_acquire_gis_dispatch_and_pe_refusal_finding_claude_code.md`.

**CTX pipeline wrap-up sprint (OPS-19b) CLOSED 2026-09-05** — integration seat,
coordinating four lane-planner sessions (Engine/Factory/LDT/a newly stood-up
Map lane) via direct messaging. Every item independently verified against live
production, not merged-and-trusted: F-11 setbacks now serving real data for
Bastrop/Elgin/Austin/Pflugerville/San Antonio through Property Explorer's own
runtime (two prior plans killed before shipping wasted code — an atom-emission
plan superseded by discovering PE's own existing mechanism, then a proposed
endpoint-wiring plan killed by adversarial review before it risked serving
repealed ordinance data to customers); Williamson/McLennan CAD data fully
reconciled (a real join-key bug caught by the integration seat's own dry-run
before any production write, fixed, re-verified); gate-eval wave un-parked
mid-sprint and closed (the 7 frozen-verdict CAD/utility rails now genuinely
re-evaluated on a recurring schedule, not a stale one-time verdict); a
production query-plan bug found and fixed along the way (~10,000x). Full
detail `_sessions/2026-09-05_ctx_wrapup_gate_eval_setback_close_claude_code.md`,
canonical record `90_operations/OPS-19b_ctx_pipeline_wrapup_sprint.md`. A
Smart Site architecture diagram published earlier the same session was found
materially incomplete by adversarial review (an affirmatively false deploy-risk
claim, several live surfaces omitted including Command Center and a
24-component shared tile framework); rebuild deferred to after launch,
findings at `_inbox/2026-09-05_smart-site-architecture-diagram_gaps.md`.

## 1. Active fires

- **Fire 1: GTM / checkout, not L26 fill.** Operator 2026-08-17: QA the live map at `https://smartsite.cloud`. Stripe checkout mechanically works; polish owed. Pipedrive with `smartsite` + tier tags. Pricing popup like lander signup. Hobby stays. Stand `_inbox/2026-08-17_l26_backfill_and_gtm_stand.md`. Do not restart Harris statewide-PBF. L26 lease heartbeat still live; do not start a second writer. Do not DROP cortex-prod `smart_file_*`.
- **Fire 2: G-60 demo path STOP.** Closed on demo path. ICC-demo https://icc-demo.vercel.app. WDLL 13 met. Residuals 7/18/19 held with L26. Pickup `_inbox/2026-08-16_icc_demo_planner_pickup.md`.
- **Fire 3: Doc_repo dirty tree.** Many uncommitted files from prior sessions. Commits are a named list, never the whole working tree.

## 2. In-flight sprints

- **THE FACTORY (OPS-19).** Plan `90_operations/OPS-19_factory_plan_of_record.md`. The six Central Texas counties serve `node-facets-tier1-conformant-v1` from walked card H production publishes (image `sha256:7bef3ce7`, LDT `889b1556`). That met A-020 shape. It is not county-complete. **Live remainder 2026-08-30T13:48:33Z: 232,770 unstamped 0,0. Do not quote pre-H 534,700 as live.** Travis `no-row` still 119,389 (situs never tried). Seed leak 0. Operator go on the remainder; cards at `_inbox/2026-08-30_ctx_facts_complete_WDLL.md` and `_inbox/2026-08-30_ctx_w1_bake_WDLL.md`. **Do not start W1 until the operator routes `_inbox/2026-08-30_ctx_remainder_deep_review.md`** (block-then-amend: landUse on W1, PE wiring card with a plan row, Wave R allowed but not customer-done). Rainmaker `48021:8720522` atom-miss is not a card H wipe; recon `_inbox/2026-08-30_rainmaker_open_complete_recon.md`. `_state/property/STATE.md` is stale (still card F / 534,700). Pickup `_sessions/2026-08-30_ctx_remainder_and_rainmaker_wiring_claude_code.md`. Deferred by A-020 stays deferred: wave 1 remainder, F-09, F-11 to F-14. Do not restart `scllr`. Do not lift the seed.

- **OPS-16 / L26** Fill factory stopped. QA/launch on current map (A-017). Backfill redesign later. Plan `90_operations/OPS-16_texas_market_plan_of_record.md`.
- **SmartCity (OPS-17, and OPS-25's SmartCity rows), refreshed 2026-09-18.** Live state is `_inbox/2026-09-15_roadmap_reconciliation.md` (the roadmap) and `_design/SMARTCITY_TRACKER.md` (generated; `node scripts/govtech/smartcity-tracker.mjs` refuses when a row disagrees with its close, when a plan row cannot be parsed, or when a tracked row's status word is outside its vocabulary). Bastrop is the proving pack (A-146). The dashboards run on DigitalOcean `dolphin-app` behind `app.smartcityos.io`, **serving `7487d7c0`, which IS `smartcity-dashboards` `origin/main`, read back byte for byte from deployment `e98f127c` (ACTIVE 2026-09-18T17:29Z) with `SMARTCITY_V1_PLATFORM_BASE` set in the same deploy.** G-159 (finance bridge) is closed-partial. **G-135's blocker is CLEARED: the `g135-mint` lane minted the verification-scoped `bastrop_tx` key at 2026-09-18T14:44Z** (`key_id 96e40316`, Secret Manager `hauska-prod-497015/hauska-tenant-key-bastrop-tx-lane-verification` version 1 ENABLED, recorded in `_catalog/credential_access_index.json`), verified at source by the planner's own instrument rather than on report (8 of 8 declared cells, including 403 against `fixture-city`, which is the cell that proves the key is tenant-scoped rather than a master key). G-135 stays CLOSED-PARTIAL: its second parcel, a full-shell probe mounting the map iframe, has still been run by no lane, and it is now ASSIGNED rather than orphaned (operator, 2026-09-18) to the next lens BUILD lane G-153, which runs once G-161 and D-13's change land (both landed 2026-09-18); that lane inherits parcel 2, and this row closes when the probe is reported. **G-154 (Development services) is CLOSED-PARTIAL**, regraded from its close and now merged to `main`: three of its four acceptance items PASS on the live `bastrop_tx` surface on `d12-main-uat` at 14:49Z, and the fourth is named unmeasured because the live surface draws no workload ranking to measure it on. **ALL THREE DISPATCHED LANES HAVE LANDED their change, read at source at 2026-09-18T16:26Z** (evidence `_inbox/2026-09-18_planner_four_row_regrade.md`, OPS-17 A-150, OPS-25 A-9). **D-14 is landed:** `walrus-app` `e243db0a` now builds from branch `main` and serves `1f0262f`, which IS `smartcity-os` `origin/main` HEAD, and the three G-159 routes answer `401 platform_internal_required` on `smartcityos.io` while a fake path answers the SPA shell. **D-13 IS LANDED, and its production step was run at 2026-09-18T17:29Z** (OPS-25 A-10): the code is in dashboards `main` `7487d7c0` (`src/platform-base.mjs`, one configured base with an unset base REFUSING, the old GCP host gone from `src/` and `web/`) and `dolphin-app` now serves it with the base set, so the production dashboards no longer read the GCP copy of v1 for any feed. **Graded on the surface, not on the merge:** the finance route no longer answers `unknown lens`; eight of eleven registered domains on `bastrop_tx` carry records with every record-less domain stating a basis; eight platform routes return payloads byte-identical to the host production read before the ship; and the work-order Subject column carries no resident free text, proven by comparing the served `(recordId, subject)` multiset against the vendor's `(workOrderNumber, type)` and finding them identical. **The ship was performed by the planner as A-149's named fallback**, because the `d14-d13-v1-reach` lane holds no claim and filed no close; D-13 stays LANDED and not CLOSED on its scope 5 bake, a window in which a lens is actually opened and GCP `smartcity-api` logs zero platform requests. G-161 is landed: its six `|| "template-city"` fallbacks are replaced and `d12-main-uat` at `7487d7c0` answers `400 city_key_required` keyless where the 15:09Z read answered 200 with the demo city. **No close, CP1 or CP2 is filed for the `d14-d13-v1-reach` lane**, so its own acceptance items and leave-behind are still owed; the generated tracker calls that state `landed, not graded` and does not count it done. **Later the same day, 2026-09-18:** the credential housekeeping is CLOSED at source. The operator deleted `P:\tmp\g154-hauska-key.txt` (verified ABSENT, with no other `g154` key material in `P:\tmp`) and ruled the dormant G-134 lane key out; the planner revoked `acf2cf9f` with its own fail-closed instrument (`_inbox/2026-09-18_planner_revoke_dormant_g134_key.mjs`, which refuses unless the target is the exact expected id, off a must-not-touch list, and genuinely dormant; it last ran three days earlier), and `2510a3ff` (G-154) already read `revoked` before the census was taken. The `bastrop_tx` census is now the minted verification key `96e40316` plus the operator's own pilot, and nothing else. **G-153 is unblocked and inherits G-135's second parcel.** **TWO OF THE FOUR NEW DISPATCHES HAVE CLOSED and two are still not handed over.** `g142-citizen-lens` (G-142) is **CLOSED 2026-09-18** and `g147-record-search` (G-147) is **CLOSED-PARTIAL**, the second parcel deferred by its own dispatch's ruling so Compass was not drawn; the planner re-ran all three of G-142's instruments and both of G-147's rather than taking either close's word, and both lanes released their claims. **The gate's two R4 findings are gone as a result:** `design-completion-gate.mjs` now reads `uncovered: 0` and no longer names `lens:citizen` or `work:records`, up from two findings this morning, and it exits 1 only on its four R3 findings (designs past DRAFT with no adversarial read). **Four dispatches were hand-carried and the wave moved:** `g153-fleet-police-lens` and `g148-design-instruments` both CLAIMED their lanes and are RUNNING, `g162-v1-finance-honesty` is RUNNING UNCLAIMED in `smartcity-os`, and `g160-served-commit-parity` STOPPED on a false "already done", so its parcel 2 was recompiled and re-handed (A-153). **Two more were compiled the same evening and are deliberately held** (A-154): `g152-public-works-fire-ems`, queued behind `g153`, and `g163-opaque-platform-routes`, queued behind `g162`. G-152 goes ahead of G-149 on a measured dependency rather than on the recommended row order, because G-149 INHERITS the flood-study instrument that `g148` is still delivering and still owns. **The gate fell from four R3 findings to ONE during the same session** as `g148` delivered three of its four instruments, and `smartcity-overview-lens` is the last line; see section 6 for why clearing it does not mean the designs are clean. **One new row came out of the ship's acceptance: G-163.** `firstdue/apparatus` and `goto/call-summary` answer an opaque Cloudflare `504` on `walrus-app` where the GCP copy answered a structured `503` naming the vendor permission to request; both feeds were already `unavailable`, so nothing broke, but the stated reason degraded. It is placed inside the handler rather than at the auth path by violation (a mutated bearer still returns the gate's 401 where the canonical one returns 504). **Finding, re-measured later the same day: the claim guard fired on two of four lanes.** `g153` (seat `cente-vsc-g153`) and `g148` (seat `cente-vsc-g148`) are live in the registry; `g162` is running UNCLAIMED; `g160` stopped without one; and the earlier wave's `d14-d13-v1-reach` and `g161-never-default-a-city` never appeared at all while both ran to a landing. One artifact note worth keeping: G-153's claim records a start of `18:54:22Z` where a planner read at `18:56Z` showed no live claim, so the record was written after the read while carrying an earlier start time. **Operator rulings recorded 2026-09-18:** do NOT rotate the `*_MCP_URL` secrets (the rename-or-split half stays open); M2 is HELD pending Khalid, so nothing was dispatched against G-137, G-157 or G-162; the ship/staff-word/WorkOS-credentials items are the operator's own and move at the operator's timing; and G-143's ratification is the highest-leverage item on the owed list because it alone unblocks G-127, G-144 and G-158.
- **OPS-17 G-60** CLOSED_ON_DEMO_PATH STOP. ICC-demo https://icc-demo.vercel.app. Plan-review `00012-pen` @100% tag `g60g`. Applicant `/applicant?token=` met. Smart Files QA personas restored. Held: store UPDATE, F4, G-58b, G-50.
- **OPS-17 Lane A Smart Files.** G-58/G-59 CLOSED serving path. G-58b OPEN. `smart-files-app` is G-59 QA plus icc-demo rooms. Plan-review owns the review files UI.
- **L25** not seated. **L24** flood remainder banked. Do not redo flood.

## 3. Open ADRs to be aware of

- ADR-008: Hauska substrate, Empressa products. Plan review is Codex. Smart Files is the files product and store. ICC-demo is the licensed-IP access portal. Dashboards is Empressa.
- ADR-017 accessPolicy. Files default `tenant-private`. Unauth Dashboards must not publish live Bastrop ops.
- ADR-018 atom contract is Hauska substrate.

## 4. Agent fleet assignments

- SmartCity planner: the next one starts from `_inbox/2026-09-18_HANDOFF_smartcity_planner.md`. It owns no product repo. Do not steal L26.
- L26 detached Node jobs: work root `P:/tmp/l26_flood_drain_20260815/`.
- Dirty `P:\legacy-design-tools` on `feat/s1-instrument-hardening`: never clean or stash.
- Dirty `P:\hauska-map` linked to Vercel `property-explorer`: never deploy Command Center or plan-review from it.
- `smartcity-os` (v1, `smartcityos.io`) changes only through lanes, as bridges proven on a non-production app first (A-146 rule 4). `walrus-app` builds from `main` as of D-14 (2026-09-18), serving `1f0262f`; it built from the side branch `d9-api-8bea7fa` before that.
- `P:\smart-files` has uncommitted `src/actors.mjs` + `web/` persona restore (live on Vercel). Do not lose it.
- `P:\icc-portal\web\.vercel\project.json` may still say `web`. Confirm `icc-portal-app` before deploy.

## 5. Recent session summaries

- 2026-09-18c **The SmartCity next wave compiled, and the design gate read at the moment it went green with its instruments failing.** From the integration worktree (which refuses `_state/` writes, so the govtech STATE was not touched and that is owed to the govtech seat). Two dispatches compiled and deliberately held: `g152-public-works-fire-ems` behind `g153`, `g163-opaque-platform-routes` behind `g162`. G-152 goes ahead of G-149 on a measured dependency, since G-149 inherits the flood-study instrument `g148` owns. The gate reached `exit 0` / `verdict: FINISHED` at 2026-09-18T20:32:13Z reading 18 of 20 folders instrumented with zero findings, down from 14 of 20 and four R3 findings at session start, and **all four instruments it counted FAIL**, each proven a design defect rather than an instrument defect by its own `violate.mjs`. **R3 tests that an instrument EXISTS and never that it passes**, so `exit 0` means "instrumented" rather than "clean". **`g148` then closed at 20:43:47Z and disclosed the same thing itself: 11 findings, 4 + 1 + 4 + 2, re-derived by the planner from each instrument's own total line, listed by name as `leave_behind` and owned by no row**, and the row was regraded to CLOSED with the tracker passing. A new instrument, `scripts/govtech/design-instrument-exits.mjs`, reports each instrument's exit code beside the gate's verdict and derives design-vs-instrument ownership (self-test 6 of 6), and it caught this seat leaving a CLOSED, RATIFIED design board modified during a batch `--violate` sweep: restored, verified, and guarded. OPS-17 A-154 through A-158. `_sessions/2026-09-18c_smartcity_next_wave_and_design_gate_claude_code.md`; handoff `_inbox/2026-09-18d_HANDOFF_smartcity_planner.md`
- 2026-09-16h **The gate rollout, the county-verdict ruling, P-205 customer-done, the corpus and ledger rulings.** P-252/P-292/P-293 merged and live with migrations `0011a`-`0014`; the gate apply running under watch; P-281, P-284, P-277 live; P-258 integrated (corpus 1.3.0); A-193 ruled; P-256 held. `_sessions/2026-09-16h_gate_rollout_and_serving_ruling_claude_code.md`
- 2026-09-16g **Scale-up rev 4, the first Phase 0 wave, the serving path has no trigger.** Teardown landed and re-checked (A-183); P-280, P-255, P-253 built; engine d88cf65 deployed and verified (A-188); P-230 finding and the operator's rulings (A-189, A-190); four lanes fired; handoff at `_inbox/2026-09-16_HANDOFF_integration_seat.md`. `_sessions/2026-09-16g_texas_scaleup_rev4_and_phase0_wave_claude_code.md`
- 2026-09-04/05 **Property seat's largest single session to date: P-118 shipped, CTX-sprint scope reconciled with zero collisions, five bugs root-caused and fixed, six operator rulings (A-103/P-119) audited and shipped end to end, P-102 reversed.** Full detail: `_sessions/2026-09-05_p118_ctx_reconcile_and_a103_package_parity_claude_code.md`. Seventeen OPS-16 amendments (A-094 through A-110) plus P-119 (authoritative package table). Eleven `legacy-design-tools` PRs and six `hauska-map` PRs merged, every one deployed to production the same session and independently re-verified live (never merged-and-trusted): #609/#610 (P-118), #613/#614 (P-110 export rewire + P-106 zoning/setback not-applicable, live-tested with a real seeded parcel and a real MCP call producing real PDF/GLB bytes), #615/#616 (screens-gate `upgrade_required` + P-107 out-of-coverage miss class, real merge conflict between the two resolved by hand and re-verified by a fresh CI run), #617 (P-111 account-deletion FK — migration found and cleaned 6 real orphaned rows in prod), #619/#620/#621/#622 plus hauska-map #352-#357 (the A-103 package-parity batch: extension retirement made fail-closed, dossier/X-ray connector gate corrected, a brand-new Feasibility-Study connector capability built and live-verified end to end against `hauska-engine-api` with a real generated PDF, owner-info/tax-valuation co-gated and widened to include Property Unlock, `get_smart_site` given real parity with the web Brief dock, `terms.html` given its first usage-data disclosure). White-screen bug (reported twice, unreproducible until the operator supplied a real DevTools screenshot) root-caused to a Rules-of-Hooks violation in `ReportsTool.tsx` and fixed with the app's first-ever error boundary added. P-102 (two-seat Studio) formally REVERSED, invoking its own pre-registered reversal clause. Two self-caught planner errors, both corrected before they shipped wrong: P-110's scope was misstated as web-UI-facing (it was MCP-connector-only) until the operator's own product usage caught it; A-096 wrongly assumed the CTX sprint's PR #87 already covered P-106's zoning fix, corrected by the integration seat's own diligence before any duplicate work was dispatched. CTX pipeline wrap-up sprint (four lane-planner worktrees under the integration seat) scope-checked twice against this seat's own work, zero collisions found either time. | Session `_sessions/2026-09-05_p118_ctx_reconcile_and_a103_package_parity_claude_code.md`.
- 2026-08-31 (2nd arc) **Q1 SHIPPED, `near` dead on arrival, and the Neon fault now has a detector.** Serving `smartsite-mcp-00083-dun` (p564+p565) and `cortex-api-00680-vog`; suite 506. `find_parcel` gains `near`/`street`; a 422 stays a declared refusal; 5 closed codes mapped. **`place/radius-search` HANGS** (504 at 300s, routing and auth fine) so `near` is shipped non-functional and declared; RADIUS-HANG dispatch compiled. **A planner defect card was WITHDRAWN** (disposition union: mechanism inverted, caught by a build lane reading api-server), and **a planner strategic read was corrected by A-060** (the clerk fork is per-request via P-85, not bulk acquisition). **A SECOND silent write outage:** smartsite-mcp ran 2.5h with every write failing after a pre-#564 workflow deploy poisoned the service spec; caught only by the pre-shift binding gate; failed-write count UNMEASURED. **Neon canary MERGED AND RUN** (`7fbcf56d`), probing the POOLED endpoint every 10 min and checking `pg_settings.source` not just the value. Posture: STAY UNPOOLED until 24-48h clean. The Neon console is NOT an instrument here. OPS-16 A-061. Session `_sessions/2026-08-31_p91_q1_shipped_and_the_neon_canary_claude_code.md`.
- 2026-08-31 **P-91 v3 MEETS ITS YARDSTICK.** W2 function-scored walk run live on a connector: capability 5/5, refusals 7/7, traps 3/3. Grades `_inbox/2026-08-31_p91_v3_W2_walk_results.md`; session `_sessions/2026-08-31_p91_v3_close_and_W2_walk_claude_code.md`. p563 vocabulary serving on `smartsite-mcp-00078-fat`. Q1 selectors measured CORTEX-BLOCKED and routed to the property seat, not built. Cortex-verify lane closed clean: the F-11 refusal basis is unmapped but reaches nobody today. **OUTAGE, found and fixed this session: the F-11 deploy at 02:52Z reverted cortex-api `DATABASE_URL` from the direct endpoint back to the Neon pooler, breaking every write (Google login `session_exchange_failed`) while reads stayed green. Fixed as `cortex-api-00674-rap`, identical digest, correct binding. The mitigation is still NOT structural: it must go in the deploy workflow file or the next workflow deploy reverts it again.** Planner correction filed: the earlier claim that the product write path was proven end to end generalised the smartsite-mcp path to the whole product and was wrong. Eight walk findings, none an honesty failure; the load-bearing one is the disposition union rewriting `unknown` to `absent` at node depth (`_inbox/2026-08-31_p91_disposition_union_defect.md`), found three independent ways.
- 2026-08-31 P-91 v3 MCP half SHIPPED and VERIFIED LIVE. p563 vocabulary and display strings serving on `smartsite-mcp-00078-fat` (digest `sha256:ecc72a40`, LDT #562 merged `03050926`): one 19-entry table owned in one place, reaching the panel and the model, plus `displayText`, `agentGuidance` on every non-present facet, a derived-figures deny, and the unknown-overlay label/finding split. Operator seven-test walk passed on a reconnected connector: **the Neon write path is PROVEN end to end** (incident mitigation confirmed working; the incident itself is still mitigated-not-cured and unpooled), `atom_path_pending` no longer leaks, no derived figures invented, and **the `tools=` channel is MEASURED** (a held door hover produced the preview, discharging p562's one open limitation). Q1 selectors are CORTEX-BLOCKED by measurement, not built: `near` and `subdivision` need routes that do not exist; ask routed at `_inbox/2026-08-31_p91_q1_cortex_ask_property_seat.md`. A silent-degradation defect found on the records path (`BLK(?:OCK)?` can never match `BLOCK`). OPS-16 A-053 opens the v3 rows. Deploy `_inbox/2026-08-31_p91_p563_deploy.md`. **Still open: the panel is unverified against `cortex-api-00672-ceq`.**
- 2026-08-30/31 P-91 v3 scoped, ruled and APPROVED, then four MCP-only cuts shipped: p559 probe (all three iframe gates open), p560 anchor plus aerial ground, p561 multi-parcel canvas, p562 paint-only preview; serving `smartsite-mcp-00076-fes` (tag `dbfix`, running the p562 digest `sha256:674baef6`, so the revision name moved but the code did not) and `cortex-api-00672-ceq` (corrected 2026-08-31 from `00670-bay`, which was the secret-only dbfix redeploy on an unchanged digest; `00672-ceq` carries a NEW digest deployed 02:52Z by another seat, most likely the F-11 setback refuse LDT #560, and the P-91 v3 panel has NOT been verified against it). M1 (a cortex shared frame) proven UNNECESSARY: the anonymous facets route already serves a parcel anchor, so the whole wave touched no api-server file. **LIVE ISSUE: the Neon pooler injected read-only on `neondb` and killed every write across both apps; MITIGATED not cured by routing both services to the direct endpoint, so we run unpooled until Neon explains it** (`_inbox/2026-08-31_neon_pooler_readonly_incident.md`). Claude Sync verified buildable (`claude.ai/new?q=` prefills and executes). Report styling and vocabulary filed as V1-V8. Session `_sessions/2026-08-30_p91_v3_mcp_wave_claude_code.md`.
- 2026-08-30 CTX remainder cards, Rainmaker Open/wiring recon, deep review (W1 blocked pending operator route): `_sessions/2026-08-30_ctx_remainder_and_rainmaker_wiring_claude_code.md`
- 2026-08-29/30 P-91 deep dive, ruling (Open stays a turn), build to deploy: `smartsite-mcp` p557 (`00063-rej`) and `cortex-api` p542 (`00666-cuf`) serving; `ask_the_map` blocked; rails at first paint; existence fails closed; PRs #550/#551/#552 merged; operator QA walk graded the panel half (miss sentence still unobserved); Smart Site mark tile to ink live on smartsite.cloud (hauska-map #315): `_sessions/2026-08-29_p91_mcp_app_deep_dive_and_build_claude_code.md` v2 (2026-08-30): five lanes built and serving on p558 (`00065-siv`) and p543 (`00668-cos`), paired shift, p555 uuid-binding regression repaired; PR #555 squash-merged `24553cfc`; card `_inbox/2026-08-30_smartsite_mcp_app_v2_WDLL.md`, close `_inbox/2026-08-30_p91_v2_build_close.md`, walk W1 `_inbox/2026-08-30_p91_p558_connect_walk_prompt.md`.
- 2026-08-29 Team seats 3 + P-96 chrome pile + live `/favicon.ico` ICO (operator confirmed serving). This seat idle. `_sessions/2026-08-29_p96_chrome_and_team_seats_claude_code.md`
- 2026-08-28 Smart Site design system: Stone exact port opened (P-95) + chrome defect pile (P-96); design system rebuilt and swapped in; the chrome gate sees 7.1% of colour literals: `_sessions/2026-08-28_smart_site_design_system_stone_port_claude_code.md`
- 2026-08-28 P-91 situs-search budget, daily-limit bake 50000, Rainmaker live on `00643-rib`: `_sessions/2026-08-28_p91_situs_budget_daily_limit_claude_code.md`
- 2026-08-28 CTX six counties served (OPS-19 A-020 to A-025; card F on `00643-rib`): `_sessions/2026-08-28_ctx_six_counties_served_claude_code.md`
- 2026-08-19 Smart Site QA to enforcement doctrine (18 lanes; tier2 retired and deployed; branch protection gap): `_sessions/2026-08-19_smartsite_qa_to_enforcement_claude_code.md`
- 2026-08-18 Smart Site QA remediation (OPS-16 A-018, P-39..P-44; six lanes; fact-sheet contract): `_sessions/2026-08-18_smartsite_qa_remediation_claude_code.md`
- 2026-08-17 Lane B G-64/G-65 plus UI-then-one-feed: `_sessions/2026-08-17_lane_b_dashboards_g64_g65_planner.md`
- 2026-08-17 L26 GTM rulings (Pipedrive, popup, Hobby): `_sessions/2026-08-17_l26_gtm_rulings_planner.md`
- 2026-08-17 L26 QA/launch on current map: `_sessions/2026-08-17_l26_qa_launch_current_map_planner.md`
- 2026-08-16 G-60 ICC-demo stop: `_sessions/2026-08-16_g60_icc_demo_session_close_planner.md`
- 2026-08-16 ICC-demo separate portal: `_sessions/2026-08-16_icc_demo_separate_portal_planner.md`

## 6. Cross-cutting watch list

- Two plans of record. Work that cannot name `P-xx` or `G-xx` is not scoped. G-66 is draft until approved.
- Cotality REST extinguished; the vendor is RE-ENGAGED as the farm purchase stage (operator 2026-09-16); the MCP **eval** channel is LIVE (creds written 2026-07-14, first probed 2026-09-12, 11 tools). Never rotate a REST credential; re-route. Deploys planner-owned. No privileged data. CTX/national HELD. Code-done is not customer-done.
- Gold parcel `48021:34137` on Dashboards is a demo fixture, not Bastrop onboarded.
- Plan-review files writes are reviewer uploads only. Planner does not seed Smart Files.
- Hauska inbound meter / ICC store UPDATE waits a quiet L26 slot.
- `MEMORY.md` exists (six prose rows, unenforced). Do not invent a second copy this close.
- cortex-api traffic is pinned. Read `status.traffic[]` before asserting serving. Baked `CORTEX_USER_DAILY_API_LIMIT=50000` in LDT deploy-canary (`ci-cortex-daily-limit-50000`). Do not shift staging `00644-soz`.
- **SmartCity design (2026-09-18):** every design is approved except People and access (G-143, DRAFT). **Citizen (G-142) is CLOSED and Records search (G-147) is CLOSED-PARTIAL**, so the gate's two R4 findings are gone: `design-completion-gate.mjs` reads `uncovered: 0` and names neither `lens:citizen` nor `work:records`, where it named both this morning. G-147's third parcel, Compass, is DEFERRED and not drawn, on its dispatch's own ruling that a sidebar must not be drawn before the surface it sidebars over exists, so the deferral goes back to the row rather than being rounded into a PASS. **The completion gate reached `exit 0` / `verdict: FINISHED` at 2026-09-18T20:32:13Z**, reading `20 design folders, with an instrument: 18` and zero findings under every rule, down from 14 of 20 with four R3 findings at the start of the session. The two instrument-less folders are `plan-review` and `smartcity-place-tab`, **both SUPERSEDED by dated rulings** (`plan-review` by the reasoner path 2026-09-15, `smartcity-place-tab` by the map dock 2026-09-14), so neither requires one: R3's instrument-required set is exactly `RATIFIED`, `APPROVED` and `IN REVIEW`. **And the green gate is the problem rather than the conclusion: ALL FOUR instruments it counted FAIL, and they are the four `g148` delivered, and `g148` has since CLOSED and disclosed the same thing itself.** Measured at the same disk state immediately after `exit 0`: `smartcity-overview-lens` exit 1 (`Sparse.dc.html` is granted 1 of 10 sources and still promotes Connections above the decision), `smartcity-flood-study` exit 1 (the rainfall claim below), `smartcity-map-dock` exit 1 (the tab nav drops "Licenses"), `plan-review-departments` exit 1 (the design says the product has no department model where the README's source-state section says otherwise). **They are design defects and not instrument defects, proven not assumed:** each folder's `violate.mjs` exits 0, catching its planted violations on a real artboard (`plan-review-departments` 17 of 17) and confirming a repaired copy passes with a matched-input count. **`g148`'s close at 20:43:47Z lists them as `leave_behind`, 11 findings, 4 + 1 + 4 + 2, recounted by the planner from each instrument's own total line rather than read from its prose, and records that the lane expected these four RATIFIED designs to be clean and they are not.** The row is regraded **CLOSED** (its done-condition was the instruments existing, self-testing, reporting a non-zero matched-input count and being proven by violation, all of which holds) and the tracker passes. **So R3 tests that a `check.mjs` EXISTS, never that it passes** (`design-completion-gate.mjs:157-163` reads `hasCheck` and no exit code), and `exit 0` from an artifact named `design-completion-gate` reads as "the designs are clean" while they are not: **11 real board defects, mostly stale NEGATIVES, now have a name and still no row.** The operator owes an owner, and the recommendation is one row for all 11, since the repair and the instrument that proves it are one unit of work. A new instrument carries the reading: `scripts/govtech/design-instrument-exits.mjs` prints every instrument's exit code beside the gate's verdict and derives design-vs-instrument ownership (self-test 6 of 6), and it caught this seat leaving the CLOSED, RATIFIED `smartcity-records-search` board modified during a batch `--violate` sweep, now restored, verified re-entrant, and guarded by refusing a dirty `_design`. **`smartcity-flood-study`'s finding is the one entangled with a parser:** the design says the local rainfall atlas is uncited while the engine cites NOAA Atlas 14 seven times, and G-125's leave-behind is that `parsePfdsDepthTable` never matches so every county silently falls back to Bastrop's 9.5in. A design-only correction would entrench the parser defect, so both facts travel to G-149. OPS-17 A-155 through A-158. **G-148 is CLOSED and its lane claim is released.** G-160 is a SEPARATE control rather than a design-gate finding: it fails a deploy when a product's console and its API serve different commits, which is the split G-150 found on `plan-review`.
