---
id: 00_current_state
title: Current state snapshot — 2026-07-05
status: active
last_updated: 2026-07-05
applies_to: portfolio
related: [00c_portfolio_master_map, 00d_portfolio_roadmap_reference, 16_commercialization_roadmap, 43_cortex_qa_backlog, 75_hauska_brokerage_workflow_plan, 75c_property_brief_data_backlog, 30a_smartcity_stabilization_sprint, 31a_bastrop_maintenance_sprint, 48_codex_program_plan, 54_tenant_leg_sprint, 55_spine_data_intelligence_stack, 56_engine_extraction_sprint, 80_adrs/adr_005_multitenancy, 80_adrs/adr_008_engine_factor_out, 01a_atom_conventions, 21c_grok_atom_migration_plan, _decisions/2026-05-23_grok_atom_fleet_migration, _research/2026-06-09_cross_repo_recon]
---

# Current state snapshot

> **Architecture-homes standard (2026-06-21).** The portfolio has a homes standard at [`_architecture_homes/`](_architecture_homes/00_overview.md): Cortex reframed to the reporting function package (cortex-api), the architect product is AEC-cortex, Radar is its own Surface, the MCP gate is four products (public/codex/reporting/map; 62 tools; 1.5.0 conformance), and every atom carries the read-contract conformance target. Phase-1 audit landed: corpus re-minted 100% conformant, AEC-cortex and radar scaffolded, migration 0044, console audit instrument. New building is frozen pending the doc scrub (`_architecture_homes/05_scrub_tracker.md`). Read older Cortex-as-product and three-gate framing through the standard.

## 2026-07-05 — Convergence autonomous run: deploy chain live, ICC backend live e2e, tenancy T1 staged, Phase 3/4 landed

The convergence program (started 2026-07-04) ran a full autonomous session: cursor-agent executed all code (seven dispatches), the planner did migrations, deploys, secrets, live verification, adversarial review, and merges. Live tracker (read first on any resume): [`_inbox/2026-07-04_convergence-program_STATUS.md`](_inbox/2026-07-04_convergence-program_STATUS.md). Session: [`_sessions/2026-07-05_convergence_autonomous_run_claude_code.md`](_sessions/2026-07-05_convergence_autonomous_run_claude_code.md).

Deployed and verified live: the MCP four-gate rework (migrations 004+005 applied, gate matrix probed; serving `hauska-mcp-server-00010-zuq`; the canary probes exposed and fixed a latent keyed-request outage — the Upstash rate-limit Redis was deleted; ResilientRateLimitStore fallback shipped as PR #36); cortex-api at main with #225 (`cortex-api-00288-lab`); engine-api at main with the atom-contract 1.6.1 bump (`hauska-engine-api-00031-rap`, PR #82, tags repointed, ICC secrets v2 live); retrieval-api with the #80 fail-closed fix (`hauska-retrieval-api-00008-ber`). The MCP gate now enforces four products at call time with 63 tools.

ICC Code Connect is live end to end: real OAuth endpoint found and verified (`/auth/token`, form-body client credentials), demo entitlements confirmed (IBC2018P6 + IPMC2018P2 through 2026-12-30), and the engine adapter reconciled to the real wire contract (PR #83) with a planner live run producing 4,966 IBC section atoms with deep links and reasoning-layer bodyText (ADR-019 boundary held). Next: corpus ingest + snapshot re-mint, then the extension PR #5 merge.

Phase 3: the command center at [cmdcenter-blush.vercel.app](https://cmdcenter-blush.vercel.app) is wired (server-side key proxy, no keys in the browser), Empressa-branded, and unified — the hauska-map root vanilla console retired into five new React panels (PR #6) per the 2026-07-04 unification decision. Phase 2 tenancy T1 is BUILT AND STAGED across all three repos as DO-NOT-MERGE-IN-ISOLATION PRs (mcp #37 producer, engine #84 + legacy-design-tools #227 consumers, log-only default; signing key staged in Secret Manager, unmounted) — the coordinated flip is the next operator-visible step. Phase 4: Stripe test-mode pricing created per Decision B (Builder $49 / Pro $199 + layer2_call meter + metered overage), and three draft docs filed to `_inbox` for framing review (proof-of-record spec, certification scaffold, siting spike memo).

Honesty note: the prior session's eval-scores artifact is not publishable (32 of 34 jurisdictions pass top-3 vacuously with zero queries evaluated); it needs per-jurisdiction curated queries first. Operator-owed: extension key rotation, Cotality production keys (2026-07-06), NPM_TOKEN secrets for CI publishing, Upstash replacement decision, Phase 4 draft reviews, T1 flip approval, rename #226 + atom-spec #4 coordinated landings.

## 2026-07-02 — Cortex QA Phase 1 + Phase 2 + deep review COMPLETE

After the Shared Surface Sprint, the operator walked the workspace and authorized the same autonomous multi-agent process for a QA build. Two phases plus a deep review shipped, all merged and live. Session: [`_sessions/2026-07-02_cortex_qa_phase2_deepreview_claude_code.md`](_sessions/2026-07-02_cortex_qa_phase2_deepreview_claude_code.md). Live tracker: [`_inbox/2026-07-02_cortex-workspace-qa-build_STATUS.md`](_inbox/2026-07-02_cortex-workspace-qa-build_STATUS.md). Next-planner handoff (the build process + state): [`_dispatches/2026-07-02_next-planning-agent-handoff.md`](_dispatches/2026-07-02_next-planning-agent-handoff.md).

Phase 1 (make it work): engine degradation fixes (hauska-engine #77 — Property Brief 500 killed, SSURGO honest, hydrology degraded-honest, ICC operator-gated, precedence already-live); workspace shared active-parcel + address search + map-click + Hazard render + shell tiles (legacy-design-tools #220); map-overlay code (hauska-map #3, publish gated on NPM_TOKEN). Phase 2 (experience + file-to-atom): spine admin console (hauska-map #4); shell experience layer — header search, fuse-together edit/view, list layout, server-persisted spaces, Module Map (legacy-design-tools #221, migration 0049); the unstructured-to-atom document-ingest pipeline with the marketplace firewall verified live + durable Postgres/GCS store (hauska-engine #78/#79); the Dataroom tile (legacy-design-tools #222, migration 0050).

Deep review synthesis [`_research/2026-07-02_ai_native_and_twin_review.md`](_research/2026-07-02_ai_native_and_twin_review.md): AI-native is substantially earned (calibration loop live-but-unfueled); the gaps are builds not redesigns; the unstructured-to-atom point-to/embed-with split IS the sell-reasoning firewall; the tenancy blocker is that enforcement covers corpus-read but not the reasoning surface and there is no tenant-private write primitive; the twin is ~90pct composition; twin-creator persona = pursue-as-embedder / park-as-hosted (Mox first). Phase 3 tenancy plan drafted for operator review: [`_inbox/2026-07-02_phase3_tenancy_sprint_plan.md`](_inbox/2026-07-02_phase3_tenancy_sprint_plan.md).

Serving: cortex-api-00284-zuq, hauska-engine-api-00029-buy, hauska-mcp-server-00008-mcr. Operator-gated: ICC OpenAPI spec (I-Code ingest), Phase-3 plan go.

Follow-ons (same day, post-close): NPM_TOKEN set (rotate after dev); `@hauska/map-renderer@0.1.1` published and consumed — map overlays draw (cortex-api-00286-xic, PR #223); the full component library published to npm at 0.1.0 (design-tokens, cortex-client, tile-shell, document-viewer, cortex-tiles) via a new legacy-design-tools publish workflow (PR #224); Chris (design-system + Property Brief) onboarding packet filed, adversarially reviewed and corrected: [`_dispatches/2026-07-02_chris_design_system_and_property_brief_onboarding.md`](_dispatches/2026-07-02_chris_design_system_and_property_brief_onboarding.md). Cleanup owed by operator: delete the stray `hauska-mcp-server/` clone inside doc_repo; sync local product clones to main. Doc-hygiene surfaced: MCP tools 46->59 (stale in CLAUDE.md/this doc); 56_engine_extraction_sprint QUEUED is stale (extraction largely done); Cotality map-mesh cache is live (docs/memory calling it uncached are stale); calibration loop is live-but-unfueled. Local dev servers (localhost:19592, :5174) run from stale local clones on feature branches — prod/origin-main is source of truth.

## 2026-07-02 — Shared Surface Sprint COMPLETE (all 7 tracks live in prod; map-renderer residual CLOSED)

The seven-track sprint shipped end to end, executed autonomously by the planner as master orchestrator (courier model collapsed: one lead agent per track, each spawning its own build + adversarial-review sub-agents, merging its own PR on a green verdict, and deploying). All ten sprint PRs merged; serving in prod: `cortex-api-00277-gun` at 100% (legacy-design-tools: Tracks B, C, D, C-bridge, F, G, plus map-renderer swap PR #219) and `hauska-mcp-server-00008-mcr` at 100% (Track E). Session: [`_sessions/2026-07-02_shared_surface_sprint_execution_claude_code.md`](_sessions/2026-07-02_shared_surface_sprint_execution_claude_code.md). ADR filed: [`80_adrs/adr_024_shared_surface_package_architecture.md`](80_adrs/adr_024_shared_surface_package_architecture.md). Live tracker + close reports: [`_inbox/2026-07-01_shared-surface-sprint_STATUS.md`](_inbox/2026-07-01_shared-surface-sprint_STATUS.md). Map-renderer publish/swap/deploy close: [`_inbox/2026-07-02_legacy-design-tools_map-renderer-publish-swap-deploy_close.md`](_inbox/2026-07-02_legacy-design-tools_map-renderer-publish-swap-deploy_close.md).

Shipped: `@hauska/map-renderer` React package (hauska-map #2, render-verified WebGL2); the five-package workspace (#210); tile+shell migration with 46/46 capability-field TileDefs and per-tile error boundaries (#211/#213); `@hauska/document-viewer` + DocumentViewerTile + migration 0048_engagement_annotations applied to prod (#212); a live capability-registry endpoint `GET /api/plan-review/admin/tile-registry` (#214/#216); the `compose_workspace` MCP tool under the cortex gate (#34); the AI vision-to-coordinate annotation pipeline with idempotency + confidence kind:'asserted' both test-proven (#217); and the full pdf-lib deliverable export with GCS presigned URL (#218).

Decisions during the run: map built as the React package model (operator call), which RETIRES the map.hauska.io DNS action (a package needs no running map server); OffscreenCanvas worker rejected after a spike (MapLibre v5 has no such option) in favor of a main-thread canvas; a bridge track added the registry HTTP route because Track C kept TILE_REGISTRY app-side; the MCP tool gated under `cortex` (not the dispatch's unreachable "reporting"). The adversarial gate caught real defects pre-merge including a pg-to-ESM boot crash that failed the canary at 0% and never reached prod.

**Map-renderer residual CLOSED (same day).** `@hauska/map-renderer@0.1.0` published to npm (hauska-sdk account). legacy-design-tools PR #219 merged (`f69f8c4`): `MapTile` now renders `FloatingMap` from the package (`floating={false}`), dropping the iframe of map.hauska.io. Deployed via canary sequence on merge SHA; prod revision `cortex-api-00277-gun` at 100%. The prior iframe was inert (console never read apn/lat/lng; no message listener for overlays), so the swap is a net improvement: parcel `flyTo` now recenters on engagement context for the first time. **Open follow-up:** `@hauska/map-renderer@0.1.1` to wire `FloatingMap`'s reserved `overlays` prop (`setOverlays` on the renderer); SpatialProvider overlays still do not draw (unchanged from iframe). hauska-map CI still lacks `NPM_TOKEN` for automated republish.

## 2026-07-01 — Shared Surface Sprint dispatched; QA paused pending package extraction

Seven-track sprint to extract all shared UI into versioned npm packages. Architecture: `_architecture_homes/shared_surface_principle.md`. Governing principle: any UI component appearing in more than one product surface ships as a package. No iframes; auth injected via `createCortexClient`. Six package families: `@hauska/design-tokens`, `@hauska/tile-shell`, `@hauska/map-renderer`, `@hauska/document-viewer`, `@hauska/cortex-client`, `@hauska/cortex-tiles`. One `compose_workspace` MCP tool added to hauska-mcp-server. AI annotation pipeline (vision-to-coordinate via claude-haiku) and print/export deliverable PDF included. Execution order: Track A (map-renderer extraction, hauska-map) + Track B (scaffold, legacy-design-tools) in parallel; then Track C (tile migration) + Track D (document viewer) parallel; then Track E (MCP, cc-agent-M), Track F (AI annotation), Track G (export) sequential. Handoff guide with all copy-paste agent prompts: `_dispatches/2026-07-01_shared-surface-sprint-handoff-guide.md`. QA at `localhost:19592/codex-reviewer-qa/` resumes after all seven tracks close. Session: `_sessions/2026-07-01_shared-surface-sprint-dispatch_claude_code.md`.

Operator action owed: after Track A closes, set `map.hauska.io` DNS CNAME to the hauska-map Cloud Run URL. [RETIRED 2026-07-02 — Track A shipped the map as a package, not a running service; no DNS action needed. See the 2026-07-02 completion section above.]

## 2026-07-01 — Cortex tile workspace live in local dev; QA in progress

Cortex-reporting configurable tile workspace built and running at `localhost:19592/codex-reviewer-qa/`. Three cc-agent-C PRs landed: tile shell (TileDef registry, GridCanvas CSS-grid layout, SpaceBar preset pills, ResizeHandle), CSS variable and layout fixes, reviewer BFF ownership bypass (PR #207 — `loadReviewerBffEngagement()` replaces owner-scoped `loadEngagementForSession()` on all plan-review BFF engagement routes). Deployed to `cortex-api-00254-tad` at 100% traffic. Queue shows 31 real production engagements; clicking a row loads engagement context into the Compliance Run tile, which shows submissions and findings with IRC 2021 code citations and Accept/Edit/Reject. Four preset spaces registered: Plan Review, Site Analysis, Property Intel, Design Accelerator.

In-flight (cc-agent-C): map tile URL fix (VITE_HAUSKA_MAP_URL not set — iframe shows broken image), confidence NaN% display fix, save-space localStorage wiring. PR pending merge and deploy. Self-contained project reference for the QA sprint lives at [`_projects/cortex_workspace_qa/`](_projects/cortex_workspace_qa/00_status.md). Next: Letter tile dispatch, then full side-by-side QA with hauska-map command center. Spec: [`48_cortex_reporting_function_dashboard_spec.md`](48_cortex_reporting_function_dashboard_spec.md). Session: [`_sessions/2026-07-01_design-accelerator_planner.md`](_sessions/2026-07-01_design-accelerator_planner.md).

## 2026-06-25 — Wave 4 fuel landed, M1 is the live gate, calibration ground-truth corrected

The calibrated-spine critical path advanced to the M1 re-run. Acquisition Wave 4 landed edition-correct fuel for the two metros that carry the gate: Austin (full IBC chain 2012/2015/2021/2024, 2.36M permit rows) and San Antonio (2015/2018/2021/2024, ~487K rows); Bastrop is edition-correct but outcome-thin (~2 rows, excluded from the gate). Uniform public-record, no tenant tooling. Close: [`_inbox/2026-06-22_acquisition_acquisition-agent_wave4-dated-edition-harvest.md`](_inbox/2026-06-22_acquisition_acquisition-agent_wave4-dated-edition-harvest.md). Two dispatches drafted and hand-carried (not yet run): cc-agent-E ingests the edition bundles; cc-agent-C builds K2 edition-correct retrodiction on Austin plus SA and re-runs M1 at the case grain. E then C. Nothing downstream (S-track model tier, warming, map fuel) resources until M1 returns go.

Calibration ground-truth correction. Commitment #2 (confidence earned, not asserted) is currently NOT satisfied; `04a_arrow_two_calibration_capture.md` wrongly claimed it was end-to-end and is now bannered and retracted. Confidence today is asserted (plan-review still LLM-self-emitted, F9 open); migration 0037 is a cache, not source of truth. The calibration model is four loops (forward live, backtest, model-as-grader, drift) over one raw ledger, earned at the case grain. Full recon and the doc-supersession trap list: [`_research/2026-06-25_ground_truth_recon.md`](_research/2026-06-25_ground_truth_recon.md). Session: [`_sessions/2026-06-25_calibration_ground_truth_and_doc_audit_claude_code.md`](_sessions/2026-06-25_calibration_ground_truth_and_doc_audit_claude_code.md).

Doc-cleanup queued (not done; needs a dedicated pass): CLAUDE.md headline corrections (counts 698/4 to 21,126/34, tools 46 to 62, atom-contract 1.5.0, commitment-2 wording); the Cortex-as-product cluster deep rewrites; git hygiene from the 2026-06-19 audit (`.gitattributes` plus renormalize, `_tmp_*` ignore, root probe cleanup). Tracked in [`_architecture_homes/05_scrub_tracker.md`](_architecture_homes/05_scrub_tracker.md) and the recon.

## 2026-06-22 — Architecture-homes standard, calibrated-spine program, phase-1 audit

Two programs filed and a standard ratified. Governing docs: the calibrated-spine roadmap ([`_calibrated_spine_roadmap/`](_calibrated_spine_roadmap/00_overview.md)) and the architecture-homes standard ([`_architecture_homes/`](_architecture_homes/00_overview.md)). ADR-008 amended (Cortex is the reporting function package; the architect product splits to AEC-cortex; Radar is its own Surface; the MCP gate is four products, public/codex/reporting/map, 62 tools, 1.5.0 conformance). Phase-1 audit complete across seven agents: corpus re-minted 100% conformant, `@hauska/atom-contract@1.5.0` published, migrations 0044 and 003, AEC-cortex and radar scaffolded as `P:\` folders (GitHub remotes pending operator), console audit instrument built. New feature building is frozen pending the phase-2 doc scrub (at banner-floor; deep rewrites owed per [`_architecture_homes/05_scrub_tracker.md`](_architecture_homes/05_scrub_tracker.md)). Calibrated-spine critical path to the M1 gate: create `gs://hauska-calibration-raw/`, acquire permits + edition bundles via the uniform public-record process (no special access), engine ingests editions + ICC I-Codes, cc-agent-C builds K2 retrodiction, then M1 measures. SmartCity OS untouched (live prod; becomes a spine consumer later). Session record: [`_sessions/2026-06-22_architecture_homes_calibrated_spine_claude_code.md`](_sessions/2026-06-22_architecture_homes_calibrated_spine_claude_code.md).

> **Read me first.** Per [`90_runbooks/current_state_protocol.md`](90_runbooks/current_state_protocol.md). Regenerated at session close. Pointer doc — follow links into canonical docs for full context.
>
> **Orientation band:** [`00c_portfolio_master_map.md`](00c_portfolio_master_map.md) for verified topology; [`00d_portfolio_roadmap_reference.md`](00d_portfolio_roadmap_reference.md) for the honed planned-work roadmap. The legacy [`11_roadmap.md`](11_roadmap.md) is superseded (2026-06-06) and kept only for backlog reconciliation.

> **Hardware/sovereignty (2026-06-14):** the on-prem and edge AI hardware story has a canonical home at [`19_hardware_sovereignty/`](19_hardware_sovereignty/hardware_sovereignty_overview.md) — internal reference architecture (AMD "agent computer" anchor, tiered menu, pitch-claim corrections) plus a client-facing brief (cloud-by-default, local option, rising-token-cost economics). Seeded from the Mox hardware thread; offer posture is reference-architecture-first, never a hardware product line (spine rule).

## RADAR IS A FREE/PRO/MAX PRODUCT: BILLING + CRM + AREA CHAT + NATIONAL MAP LIVE (2026-06-18, session close)

> Session record: [`_sessions/2026-06-18_radar_billing_pipedrive_map_cotality_claude_code.md`](_sessions/2026-06-18_radar_billing_pipedrive_map_cotality_claude_code.md). Planner-coordinated build-deploy-QA loop (extension v0.6.16->v0.6.22, several cortex-api + one engine-api deploy), every close verified against live prod. Supersedes the deploy/QA status below.

**The radar is now a Free/Pro/Max product, live (test mode).** Tier-aware `/billing/checkout` (Pro $29 / Max $65), Stripe webhook flips `proActive`/`maxActive`, Customer Portal, free-brief cap of 3 before an `upgrade_required` (402, not the legacy wallet). Both Pro and Max checkout verified `mode:"live"` on prod. Map gated on `subscriptionTier=max`. Naming/pricing per [`_decisions/2026-06-16_investor_radar_name_and_pricing`](_decisions/2026-06-16_investor_radar_name_and_pricing.md).

**Pipedrive CRM sync live** (root cause: sync only ran with an install-id header; extension web-auth often lacked it; fixed). **Research chat fixed + area-aware** (`areaContext` answers map/area-level questions without a brief run; verified). **Extension hardened** (logout/session scope, deep-dive tab focus, autocomplete, MV3 dynamic-import SW fix, clean release build) — `d8786b2`, v0.6.22.

**The Max spatial map is built real (MapLibre) on Cotality NATIONAL geometry** ([`_decisions/2026-06-18_map_engine_maplibre_cotality_national`](_decisions/2026-06-18_map_engine_maplibre_cotality_national.md)): dropped the design-agent mockups + the per-county ArcGIS path; fresh MapLibre renderer, Cotality Spatial Tile vector parcels (national mesh PROVEN at 500 features for Bastrop AND Austin), FEMA federal flood, land-use choropleth, small Map tab + collapsible/resizable dock + full-screen + popout chat. Engine `COTALITY_*` secrets mounted (`hauska-engine-api-00017-cuy`) was the real CLIP fix. SmartCity map recon (cc-agent-M): Leaflet island, not extractable, lift styling only.

**Deploys:** cortex-api `00235-sux` (pagination caps), engine-api `00017-cuy`, extension `d8786b2`. Watch the recurring **PowerShell-pipe secret-write truncation** (wrote 2-char garbage twice — phantom "empty" secrets; fix is temp-files not pipes).

**ADR-022 — the deal twin** filed (premortem green): cross-application capture riding the user's authed browser session, events as procedure-execution atoms, generic pin-to-deal, MCP-exposed; build phased/gated, not in this sprint.

**Map status (v0.6.24):** rendering is FIXED (the blank-canvas MapLibre-under-MV3-CSP root cause resolved) — real Carto basemap, land-use choropleth, FEMA overlay, geocoded pins, layers panel, dock. But the visual quality is far below the dataviz bar the operator wants; a fresh agent is regrouping on map quality against [`75k`](75k_max_map_quality_direction.md) (north-star: Carto 2023 best-dataviz). The live national parcel mesh + rent-heat run on a fixture and are **hard-gated on production Cotality**.

**The #1 launch blocker is now production Cotality keys.** The demo tier is **100 requests/day** (exhausted; 429) and expires **~2026-07-06** — it cannot support, QA, or demo the live map, and the brief's comps/underwriting also run on Cotality. Production Property + Spatial Tile keys + display license (Cotality Data Implementation Services), synced to both `legacy-design-tools-prod` and `hauska-prod-497015`, is the gate for the national map AND keeping the Cotality stack alive past July 6. Scope: `_inbox/2026-06-18_legacy-design-tools_cc-agent-C_cotality_production_quota_scope.md`.

**Other open holes:** the post-checkout success page points at `hauska.io` which returns Cloudflare 525 (no live origin yet — payment still succeeds via webhook; stand up the Vercel landing origin or repoint to a working page); the zoning-color geocode bridge (shipped, quota-gated); the one-shot real-shape fixture capture (pending the daily demo-quota reset). **Operator-owned:** **Cotality production keys (top, dated ~July 6)**, ICC secrets (awaiting ICC), General Code partnership, G2 Cotality consumer-display license, the Vercel landing origin (copy in [`76g`](76g_investor_radar_landing_and_webstore.md)) + Web Store (G4), the test-zip GitHub release link. **Site-adapter roadmap** (Realtor.com/HAR/Google next) captured in the session record, not yet dispatched.

## NATIONAL-BASELINE COVERAGE + FREE-BRIEF TIER LIVE; EXTENSION v0.6.15 FULL QA LOOP (2026-06-17, later session)

> Session record: [`_sessions/2026-06-17_radar_national_baseline_qa_loop_deploys_claude_code.md`](_sessions/2026-06-17_radar_national_baseline_qa_loop_deploys_claude_code.md). This planner-coordinated build-deploy-QA loop cleared every app-side blocker against live prod and supersedes the deploy/QA status in the section immediately below.

**The app fires end to end on live prod.** Land on any listing, correct address parsed, run a free brief, real investor verdict with expandable cited cards, inline or deep-dive chat, attach docs, Max map hero. Both hard blockers (address, paywall) and the deep-dive are cleared.

**National-baseline coverage is live ([`decision`](_decisions/2026-06-17_brief_national_baseline_websearch_coverage.md)).** The jurisdiction 403 allowlist is removed; the brief never gates on jurisdiction. Central TX is served by cited engine atoms; everywhere else falls back to a labeled web-scraped websearch (commitment-one compliant, 0.35 asserted). Verified live: Pflugerville 403 to 200 `localCodeSource: websearch`; Bastrop cited corpus; out-of-state baseline plus websearch.

**Free-brief tier wired into the gate.** First 3 briefs free (`BROKERAGE_FREE_BRIEFS_CAP`, env-overridable) before an `upgrade_required` (402, not the legacy $5 wallet top-up). Verified live on a fresh install. Extension shows "N free briefs remaining" with Run enabled.

**Deploys (cortex-api canary):** national-baseline brief, then free-brief gate (`00194-diw`, PR #192, migration 0042), then deep-dive contract + complete-upload 422 + map-data Max (PR #193). **Serving `cortex-api-00197-hex`** (rollback `00194-diw`); ENGINE_API_URL on the stable host.

**Extension v0.6.15** committed/pushed (`96df5ae`, `extension/unified-signin-v067`): Zillow address from URL slug, deep-dive populate + inline chat, attachment upload, panel UX (wider, un-clipped, expandable cards), Max map hero. Operator owes the `chrome-extension://` screen captures.

**Launch collateral drafted:** [`76g_investor_radar_landing_and_webstore`](76g_investor_radar_landing_and_webstore.md) (Vercel landing copy + Web Store listing).

**Open threads:** the Central TX corridor deepen never resumed (cc-agent-C pulled into the deploy chain; PR #190 merged, Austin repaired to 45.5%; re-kick with `-AllowBatch`); commercialization connectors (Stripe/Pipedrive) queued, operator holds creds; operator items: Vercel landing, Web Store, G2 Cotality license, ICC secrets (waiting on ICC), General Code partnership.

## RADAR v1 BACKENDS LIVE + CONSENT FIXED; EXTENSION QA + COVERAGE EXECUTION IN FLIGHT (2026-06-17, session close)

> Session record: [`_sessions/2026-06-17_investor_radar_v1_coverage_and_deploys_claude_code.md`](_sessions/2026-06-17_investor_radar_v1_coverage_and_deploys_claude_code.md). This planner-coordinated session drove the radar v1 build plus several prod deploys and locked the coverage program. Supersedes the deploy/QA status in the section below.

**Deploys live (verified):** cortex-api radar backend (#185 Cotality depth + #186 map BFF/Max consume; reached `cortex-api-00185-nom` then `00187-ray`); engine-api map-layers wave-3 (`hauska-engine-api-00008-qaw`); cortex-api GTM consent fix (#189 — `/gtm/consent` accepts the anonymous public key, verified 200). Rollback handle `cortex-api-00187-ray`. The #186 merge conflict was resolved; `BROKERAGE_DEV_API_KEY` dropped from `--set-secrets`; federal-data Docker fix (#188 — ship fixtures + runtime GCS path).

**Consent/auth fixed end to end:** extension uses `brokerageKeyHeaders` for GTM + local-gate-first consent; backend mounts `/gtm` before the service-token gate. Consent records on prod now.

**Extension (v0.6.11):** an interaction-wiring pass shipped (investor chips, research-by-address, auth-state, button audit), but live QA still finds bugs (auth buttons not hiding when signed in; the wallet gate blocks the FIRST brief — should be free-first-then-pay; Deep research stuck on a stale-version refresh banner). A comprehensive wiring + QA pass is dispatched. The extension auth fixes from this session are uncommitted in the extension tree (extension-agent to commit; also default the public key in `public-client.js` so plain builds stop shipping keyless).

**Coverage program ([`61a`](61a_central_tx_coverage_program.md)):** national-layer baseline reframe (every parcel gets intel; the code layer is incremental, never a gate); Class A flip `engine_only`->`neon`, Class B onboard Municode net-news (Waco/Temple/San Marcos/Seguin/Cibolo/Belton/Universal City), then deepen Layer 1/2 verified. **Deepen GATE:** Austin regressed 38.6%->33.2% because the re-warm upsert downgrades verified atoms on a failed re-fetch; fix-and-pause dispatched, plus a safer deepen process specified (monotonic no-downgrade / verified high-water-mark, incremental targeted re-warm of only-unverified atoms, verify-before-promote, auto-rollback if after < before).

**Operator items:** wire ICC `ICC_CODE_CONNECT_*` secrets into GCP (unblocks IFC/IPMC from 0%); the General Code partnership (13-city eCode360 unblock); the Vercel landing + public Web Store listing; push G2 Cotality consumer-display license; Stripe (consumer subscription) + Pipedrive (CRM) account setup.

## INVESTOR RADAR v1 SCOPE LOCKED + CENTRAL TX COVERAGE PROGRAM FILED (2026-06-17, this session)

> Planning agent (this chat) now coordinates execution; the first agent (which filed [`75i`](75i_investor_radar_prelaunch_sprint.md) + the three radar dispatches at `6dab4e7`) is retired. v1 scope locked and expanded; the Central TX coverage program filed. Pre-mortem cleared green (one load-bearing yellow on commitment #1, resolved by the map reasoning-not-geometry spec).

**Radar v1 board is [`75i`](75i_investor_radar_prelaunch_sprint.md).** Lead engine cut, provider frozen to Cotality, Free/Pro/Max, pencils-at-$X in v1. The seam seal it depended on is DONE (Track 1, #72/#183 live, section below), so cc-agent-C pours Cotality depth onto the live envelope now.

**Added to v1 this session:** Opportunity Zones (national Layer-1 flag + L2 reasoning, Parcel package, versioned for the OZ 1.0->2.0 transition, designations opening Jul 2026) and the **Cortex map (Max-tier), EXTRACTED** into a shared cross-app capability (layer-data assembly to a gate-fronted spine capability; render component to a shared publishable package) consumed by Cortex, the extension, SmartCity, and Mox. Binding spec (commitment #1): the Max value is cited reasoning rendered spatially, never raw geometry. Decision: [`_decisions/2026-06-17_map_extraction_shared_capability.md`](_decisions/2026-06-17_map_extraction_shared_capability.md).

**Central TX coverage program filed ([`61a`](61a_central_tx_coverage_program.md)):** proactive warm within the wedge footprint (Tier A ~40-50 cities), demand-driven tail (Tier B). Keystone is the driver-quality UpCodes section-HTML fix, which also gates the radar's cited code reasoning (the two workstreams converge on Austin). A batched edition-verification workflow keeps the wide warm inside the cost commitment. ICC contract landed ~2026-06-17 (unblocks licensed I-Codes/IFC/IPMC; distinct from the General Code/eCode360 block). Amends the demand-driven decision ([`_decisions/2026-06-17_central_tx_coverage_proactive_within_footprint.md`](_decisions/2026-06-17_central_tx_coverage_proactive_within_footprint.md)). **Platform recon:** 18 Tier-A cities Municode-warmable now (Waco 145k and Temple 81k the headline pull-forwards); the General Code partnership is now a quantified 13-city unblock.

**cc-agent-E ordering (seam done):** driver-quality fix, then map layer-capability extraction. Dispatches filed/extended: cc-agent-C (OZ, MUD/PID Comptroller registry ingest, map-data consume); cc-agent-E coverage (driver-quality + batched verification + proof wave) and map layer-capability extraction; cc-agent-M map layer gate exposure with tenant/product entitlement; extension-agent (map render, Max-gated). The standalone seam-seal dispatch is marked satisfied by #72/#183.

**Gates:** G1 hotfixed; G2 Cotality consumer-display license now launch-blocking and also covers the map's Cotality-derived geometry; G3 TX framing; G4 web store. Real QA after execution. **Operator-owed:** push G2; the G3 one-sentence TX-attorney question; ICC integration this week; the General Code partnership (13-city unblock); Pipedrive token + stage mapping. Positioning + brand run on a separate claude design agent (feeds the extension reskin).

## TRACK 1 (SEAL-THE-SEAM) COMPLETE AND LIVE (2026-06-16/17, this session)

> Pre-QA functional-freeze for the spine and Cortex. The [`61`](61_property_intelligence_master_plan.md) Wave 1 (seal the seam) plus the functional half of Wave 2 shipped and verified live against gh, gcloud, and prod endpoints. Close notes: the cc-agent-E and cc-agent-C `track1_finish` and `track1_residues` notes in [`_inbox/`](_inbox). Verdict: the spine and Cortex meet the QA-readiness bar (every path returns a real result or an honest "can't"; nothing mock, nothing silent-degraded).

**The EngineEnvelope seal is live end to end.** hauska-engine #72 (emit) and legacy-design-tools #183 (consume) shipped a uniform EngineEnvelope on every engine-api `/v1` reasoning surface: `confidence{value, kind: calibrated|asserted|deterministic}`, `dataVintage` (acquisition date, not fetch-time), `coverage{degraded, reason}`, `source{adapter, citationIds}`. Verified populated end to end on a real San Marcos review (confidence kind asserted 0.35, degraded true with a named reason, reasoning-atom citations), NOT the conservative fallback. This is the one integration pass the [`robustness audit`](_research/2026-06-11_engine_robustness_audit.md) called for. Serving: `cortex-api-00180-row` and `hauska-engine-api-00006-lap`, both at 100%.

**Mock is dead, the leak stays closed, the deploy is durable.** Live cortex env: `AIR_FINDING_LLM_MODE`, `BRIEFING_LLM_MODE`, `CLASSIFICATION_LLM_MODE` all `anthropic` (no `*_LLM_MODE=mock`). Unauthenticated `GET /api/engagements` returns `[]`. Migrations 41/41 (0039 applied plus the new 0040 `engine_honesty` jsonb at head). Retrieval `/healthz/` `db=up` (#68 deployed). `ENGINE_API_URL` baked to the stable host (not the canary tag) so deploys do not lose the engine connection.

**Residues resolved on the finish (#184 puppeteer/IAM, then #180-row Cotality/vision):** large-PDF presign uploads work (runtime SA `api-server-runtime` granted `serviceAccountTokenCreator`). PDF plan-set vision works via poppler `pdftoppm` plus `--no-cpu-throttling` (root cause was CPU throttling after the 202 starving the fire-and-forget generation, the same class that broke headless Chrome); keystone 404 Remodel_B completes in ~33s with vision NOT degraded. Cotality property geocode fixed (api1 host plus the catalog query shape `streetAddress/city/state/bestMatch`); `cotality:property` flips to `ok` in-app on real TX parcels (San Marcos, Cedar Hill).

**Carried (not QA blockers):** hydrology drainage runs ~45s on the native-D8 path with honest `coverage.degraded=true` (Wave 2 quality; pysheds win pending). The `--no-cpu-throttling` annotation must be baked into legacy-design-tools `cloud-run-deploy.yml` or the next `deploy-canary` regresses the vision/generation fix (regression guard, pending cc-agent-C commit). Rollback handles: `cortex-api-00177-vew`, `hauska-engine-api-00004-xpl`.

**Next:** Track 2 (Cortex surface verification) and Track 3 (Property Brief re-point onto Cotality, code-retrieval-via-MCP, federal layers, extension functional pass) per the QA-readiness roadmap.

## INVESTOR DEAL RADAR — extension reframed + build dispatched (2026-06-16, this session)

> New product direction for the property-intel browser extension, planned and dispatched. Vision: [`75g_investor_deal_radar.md`](75g_investor_deal_radar.md). GTM: [`76f_investor_deal_radar_gtm.md`](76f_investor_deal_radar_gtm.md). Approved build plan: `C:\Users\cente\.claude\plans\joyful-conjuring-wozniak.md`. Mockups (approved): `p:\tmp\extension-proposal\`.

**The extension is refocused from a generic consumer property-brief tool to an investor deal radar.** First actor: the Austin real estate investor (RENC ~15.7k, Texas REIAs 20k+), decided in [`_decisions/2026-06-16_investor_first_actor.md`](_decisions/2026-06-16_investor_first_actor.md). Three layers: a proactive on-listing deal verdict (deal/conditions/dead) tuned to a learned buy box; a per-user profile built from the running dialogue of the user's research (the "who you are" surface, tenant-private, never pooled); and an owned, provenance-backed investor identity that inverts into a lead feed. The deep dive found the extension was a stranded bet (canonical doc three versions stale, prod `/brief` 503 for weeks, real code unmerged on `extension/unified-signin-v067`).

**The data unlock: Cotality already exposes nearly the whole investor underwriting stack** (rent AVM, comps, building permits, liens, mortgage, tax, HOA, propensity-to-sell, owner-occupancy, roof/weather), most available-but-unwired. So this is mostly a wiring + reframe job, not greenfield. Standout unwired signals (propensity-to-sell, permits, owner-occupancy) are the lead-engine fuel.

**Dispatched 2026-06-16:** backend to cc-agent-C ([`_dispatches/2026-06-16_cc-agent-C_investor_deal_radar_backend.md`](_dispatches/2026-06-16_cc-agent-C_investor_deal_radar_backend.md)) for the Cotality wiring, investor verdict reframe, per-user profile table, and lead engine; extension surfaces to extension-agent ([`_dispatches/2026-06-16_extension-agent_investor_deal_radar_surfaces.md`](_dispatches/2026-06-16_extension-agent_investor_deal_radar_surfaces.md)) on the approved mockups.

**Gates carried:** G1 prod brokerage key (the `/brief` 503; durable one-line `cloud-run-deploy.yml --set-secrets` fix folded into the cc-agent-C dispatch; NOT hand-deployed this session because cortex-api was being actively deployed by another agent, four revisions in ~40 min, collision risk). G2 Cotality consumer-display license ([`_decisions/2026-06-16_cotality_consumer_display_license_gate.md`](_decisions/2026-06-16_cotality_consumer_display_license_gate.md), blocks public display only; dev-tier pilot proceeds). G3 Texas legal framing (informational estimate, never "value"; TX non-disclosure makes the Cotality license the binding constraint on comps). G4 Web Store readiness. Tier: free radar (Layer 1) / paid depth + leads (Layer 2). Brand: consumer surface should be Empressa-named, Hauska stays substrate (open naming decision).

## CONVERGENT DEPLOY SHIPPED — LEAK CLOSED, C3 LIVE, COTALITY ACTIVATED (2026-06-15, this session)

> **This section supersedes the "CONVERGENT DEPLOY PENDING" and "the Cortex auth data-leak is live" claims in the section immediately below.** The deploy ran and was verified live against gcloud, the prod endpoints, and the prod Neon. Session record: [`_sessions/2026-06-15_cotality_live_and_convergent_deploy_claude_code.md`](_sessions/2026-06-15_cotality_live_and_convergent_deploy_claude_code.md).

**The convergent cortex-api deploy is done. Prod is `cortex-api-00171-wek` @ 100%.** One canary-gated deploy shipped #178 (spine-flag bake), #179 (C3 thin BFF, the one-way door), #180 (auth and leak fix), and #181 plus #182 (Cotality), together. Sequence: `deploy-canary` of `b19ca089` to a 0% canary, smoke-verified on the canary URL, then `shift-traffic`. No `run-migrations` was needed (the leak fix is code-only; migration 0038 was already applied). Rollback handle: `gcloud run services update-traffic cortex-api --to-revisions cortex-api-00169-jep=100`.

**The data leak is CLOSED, verified.** Unauthenticated `GET /api/engagements` on the prod default URL returns 0 records (was 30 on the pre-deploy `00169`). #180's anonymous isolation is live. The Wave 0 security gate ([`61`](61_property_intelligence_master_plan.md)) is met; the external-demo precondition is cleared.

**Cotality is solved AND deployed.** The `InvalidClientIdentifier` resolved to three compounding causes: per-product token host (Property `api1.cotality.com`, RiskMeter and SpatialTile `api.cotality.com`), HTTP Basic auth with `grant_type` in the query and an empty body (not body-form), and two mistyped secrets (now corrected; all six `COTALITY_*` byte-match the portal). PR #181 and #182 both merged; all three products verified minting HTTP 200 live; the six secrets are mounted on the live revision and the Basic-auth code is deployed. **Remaining: the in-app `generate-layers` flip of `cotality:property` to `ok` on a real parcel is NOT yet run (needs an authenticated session); RiskMeter and SpatialTile are wired but not in-app verified.** Demo keys expire July 6 (rotation deferred to post-dev; `:latest` wiring means a secret-version add, no code change). Memory [[cotality-oauth-three-keys]] holds the verified shape.

**Engagement ownership reassign (operator request).** Post-deploy, the operator's new account (`empressaioemail@gmail.com`, `u_d96ffe5b81e6b487df53c32d`) showed 0 engagements (correct #180 isolation). The 30 demo engagements, cleanly owned by `migration-owner`, were reassigned to the operator's user id via a transactional UPDATE on the prod Neon (verified 30 after, `migration-owner` at 0). Reversible; does not reopen the leak.

**HELD dispatches now unblocked by the deploy:** chat web-first code plus zoning grounding, the Cotality activation dispatch, the deliverable-polish queue. **Deploy residues still open (untouched this session):** retrieval-api `db:not_configured` (hauska-engine #68), drift alert policy `8570526367601301438` to retire (mcp #27), #26 collateral 46 to 57, `mcp.hauska.dev` plus gate migration 004.

## CAPITAL-READINESS AUDIT FILED + CONVERGENT DEPLOY PENDING + COTALITY SOLVED (2026-06-15, this session)

> **Superseded on deploy + leak status by the section above (2026-06-15, later): the deploy ran, the leak is closed, Cotality is merged and deployed. The capital-readiness audit and roadmap reconciliation below stand.**

> Capital-readiness pass plus a live-status reconciliation against gh, gcloud, and live endpoints. The pre-diligence checklist is now a tracked doc: [`72b_capital_readiness_audit.md`](72b_capital_readiness_audit.md), ten owner-tagged rows promoting the [`72a`](72a_capital_raise_positioning.md) gaps. Session record: [`_sessions/2026-06-15_capital_readiness_audit_and_roadmap_refresh_claude_code.md`](_sessions/2026-06-15_capital_readiness_audit_and_roadmap_refresh_claude_code.md).

**The Cortex auth data-leak is live and the fix is merged-not-deployed.** A live check (2026-06-15) found unauthenticated `GET /api/engagements` on prod returns 58 real engagements (San Marcos, Miami Beach, Moab, client Revit paths). Root cause: task #29 auth (#167) backfilled all engagements to a single `migration-owner`, and the lockout fix (#168) mapped the anonymous session to that same owner, so anonymous owns everything; the snapshot, sheet, and submission child routes never got ownership predicates. The fix (#180: anonymous resolves to a per-session ephemeral owner, migration 0039 reassigns the backfill off `migration-owner`, child-route predicates added) is MERGED GREEN 2026-06-15 but NOT deployed; prod is still `cortex-api-00169-jep`. The exposed data is the company's own dogfood, test, and warming data, not a paying customer's confidential filing, so the posture is deploy on the next cycle, but it is a hard precondition for any external demo. A process guard was filed to stop the CI-blind reporting that hid this: HR-13 in [`20_agent_operating_rules.md`](20_agent_operating_rules.md) plus the local test-DB runbook [`90_runbooks/cc_agent_local_test_db.md`](90_runbooks/cc_agent_local_test_db.md).

**Cotality is solved end-to-end.** The InvalidClientIdentifier mystery resolved to three compounding problems: per-product token host (Property at `api1.cotality.com`, RiskMeter and SpatialTile at `api.cotality.com`), HTTP Basic auth with `grant_type` in the query and an empty body (not body-form), and two mistyped secrets (a key with an i where the real key has an l, and a wrong SpatialTile secret). PR #181 (api1 host) merged; PR #182 (full Basic-auth shape plus the six secrets wired into `cloud-run-deploy.yml`) is open with typecheck and 13 adapter tests green; all three products verified minting HTTP 200 live. Remaining is merge #182 then the deploy.

**One convergent cortex-api deploy now gates the most.** The next cortex-api deploy ships, together: #178 (spine-flag bake), #179 (C3 thin BFF, the one-way door), #180 (auth and leak fix), and #181 plus #182 (Cotality). Sequence (Option A, canary-gated): deploy main to a 0 percent canary, verify on the canary URL that unauthenticated `GET /api/engagements` denies, the four engines still work through the thinned BFF, the anonymous demo reads only its own engagement, and a Cotality Property token plus parcel call flips `cotality:property` to ok; only then shift traffic. This closes the leak (the [`61`](61_property_intelligence_master_plan.md) Wave 0 security gate), activates C3, and lights up Cotality in one verified shot. Open operator choice: merge #182 first to bundle Cotality, or deploy now for the leak and Cotality second.

**Stale deploy residues found (merged code not on the live services).** retrieval-api `/healthz/` still reports `db:not_configured` (hauska-engine #68 merged 2026-06-10, undeployed); the drift alert policy `8570526367601301438` is still enabled (mcp #27 was to retire it); the #26 GTM collateral shipped a stale "46-tool surface" line (the live gate registers 57); the `mcp.hauska.dev` mapping and gate migration 004 are still pending.

**The roadmap is reconciled across three governing docs:** engine work in [`61`](61_property_intelligence_master_plan.md) (seam-first Waves 0 to 4), product surfaces in [`00d`](00d_portfolio_roadmap_reference.md) (whose surface statuses now lag and owe a refresh), and the capital axis in [`72b`](72b_capital_readiness_audit.md). The cross-domain four-layer thesis (one substrate proven across the building twin at Mox, the city twin at Bastrop, and the well twin at SLB) is the strongest narrative asset surfaced and should graduate to a portfolio doc peer to [`09`](09_post_saas_substrate_thesis.md).

## OIL AND GAS VERTICAL OPENED; SLB BACKEND BUILT AND SHIPPED (2026-06-14, this session)

> New working vertical at [`_verticals/oil_gas/`](_verticals/oil_gas/00_oil_gas_index.md) (exploration, not canon). Dormant Western Midstream / O&G material became a market-discovery pass that surfaced the underserved small-and-mid operator long tail, then a product thesis and a real build. Session record: [`_sessions/2026-06-14_oil_gas_vertical_and_slb_backend_claude_code.md`](_sessions/2026-06-14_oil_gas_vertical_and_slb_backend_claude_code.md).

**SLB is the customer for Chris's surveillance-to-intervention app (Permian Field Health); we won and built the backend.** `slb_prototype` (separate Nick-owned GitHub repo at `P:\slb_prototype`, branch `feat/ai-native-backend`): an AI-native backend on our atom substrate computing a bounded health index, decline-curve anomaly detection, root-cause attribution, intervention recommendations with provenance and confidence, BOE-and-dollar downtime exposure, and a conversational ask, plus the seven bounded operational layers / well-log correlation / directional surfaces matched to Chris's UI. Mock-mode fallback (runs keyless), live with a Grok/Anthropic key; verified both modes; agent-bootstrap `CLAUDE.md` so Chris's Claude installs, runs, and wires it unattended. Sent to Chris. This is the first owned piece of our own O&G offering (the operations lens).

**Two tracks on one substrate, non-conflicting by segment** ([`80`](_verticals/oil_gas/80_slb_engagement_and_operator_product_path.md)): SLB as funded vendor entry (it serves majors/NOCs) and our own operator-owned revenue-twin product for the SMB long tail (the moat). The earlier operator-owned / anti-SLB framing was retired from [`40`](_verticals/oil_gas/40_chris_app_overlay.md) once SLB was confirmed as the customer. **Operator decision: let the SLB track run its course; the operator-product track is captured but gated on a real SMB operator design partner, not dispatched.**

**The cross-domain thesis** (a revenue-producing asset as an inverted digital twin; four layers spine/twin/connect/monetize; three lenses operations/land/capital; the same offering as Mox and SmartCity) is captured in [`50`](_verticals/oil_gas/50_complete_product_plan.md) and should graduate to its own portfolio-level canonical doc (peer to [`09`](09_post_saas_substrate_thesis.md)) in a future session.

**Substrate item surfaced:** [`53a_noncustodial_settlement_rail.md`](53a_noncustodial_settlement_rail.md) — remove the SDK's custodial facilitator wallet, move to a verify-only bank-to-bank rail (technology fee, no custody); required regardless of O&G, urgent for the capital lens.

## ADAPTIVE UI GRADUATED TO A PORTFOLIO PRINCIPLE + MOX DEMO IN BUILD (2026-06-13, this session)

> Two threads this session. (1) The **Mox demo** (`mox_demo` repo): a lightly functional, real-engine demo that shows the stack and, to a degree, the adaptive interface, built on a real Austin redevelopment (607-611 Nelray, a 5-story RVT on three MF-3 lots; hero plan-review finding is the 5-story-exceeds-MF-3 entitlement gap). Build plan [`_prospects/mox/2026-06-13_mox_demo_build_plan.md`]; premortem cleared 2026-06-13; multi-agent execution in flight (orchestrator + sub-agents in `mox_demo`, nine workstream briefs in `docs/dispatch/`). APS creds provisioned. Yardi research done: read+assist+capture now, licensed-interface write-back is roadmap. (2) **Adaptive UI** is now a captured, active design workstream with a canonical home at [`24_adaptive_ui/`].

Now that the backend is largely roadmapped, the adaptive interface (intent-driven assembly of atom render modes, the visible payoff of the atom treatment) becomes a first-class build-and-design priority across all product surfaces, not just a demo behavior. Canonical vision: [`24_adaptive_ui/adaptive_ui_vision.md`] (reshaped to current brand and the live `@hauska/atom-contract` render-mode substrate, superseding the older intelligence-interface-v4 lineage). Chris (product designer) sharpens it visually in `mox_demo` then `legacy-design-tools`; his agent maintains a living capture pulled back into [`24_adaptive_ui/design_system.md`] at session boundaries. The Mox demo is the first surface making it tangible. Index propagation into [`00c`](00c_portfolio_master_map.md) is a session-close follow-up.

## THE CUT IS FULLY LIVE: ALL FOUR ENGINES ON THE SPINE IN PROD; SOAKING BEFORE C3 (2026-06-11, this session)

> **This section supersedes the 2026-06-10 "cut merged + flipping" section below on cut status.** Verified against gcloud + gh this session. Master plan home: [`61_property_intelligence_master_plan.md`](61_property_intelligence_master_plan.md). Audit: [`_research/2026-06-11_engine_robustness_audit.md`](_research/2026-06-11_engine_robustness_audit.md).

The Cortex engine cut went from staged canary to FULLY LIVE in production this session, app-by-app, one engine at a time. **All four reasoning engines are live on the spine** (`cortex-api-00169-jep` @ 100%; `ENGINE_SPINE_FINDINGS` + `_FINDINGS_ORCHESTRATED` + `_BRIEFING` + `_HYDROLOGY` + `_TOPOGRAPHY` all on). cortex-api now calls `engine-api` (`hauska-engine-api-00003-vjx`, anthropic findings + grok briefing, pysheds baked) over the gate-front seam for findings, briefing, hydrology drainage/rainfall, and the USGS 3DEP DEM fetch. The local engine code is dark behind the flags. **C3 (thin cortex-api, #179) is MERGED but NOT deployed** — prod stays on the pre-C3 `00169`; the next cortex-api deploy activates C3 (thin BFF, `engine-api` the only reasoning path, no local fallback) AND proves the #178 bake durability. Wave 0 (the app-by-app flip) is functionally complete. Session record: [`_sessions/2026-06-11_engine_cut_live_master_plan_robustness_audit_claude_code.md`](_sessions/2026-06-11_engine_cut_live_master_plan_robustness_audit_claude_code.md).

**Three C1 flip-blockers were caught on the canary (never prod) and fixed.** (1) **#171** findings persist: the spine returns ISO-string timestamps where drizzle expects `Date`, so the insert threw `value.toISOString is not a function`; fixed with `rehydrateSpineFindingsResult`. (2) **#172** jurisdiction key synthesis: any city now resolves a synthesized key so unwarmed jurisdictions web-ground on demand (the web-first promise made true); scoped to the finding path so the coverage resolver keeps returning `not_in_catalog` and the honest "web-grounded on demand" banner is preserved (the amendment, merged with #172). (3) **#173** provenance read: citation atom-ids are partitioned by namespace so `reasoning:*` ids no longer hit the UUID `code_atoms` query (was 500ing the findings list). Then **#175** extended the date rehydration to briefing (#171 had covered findings only; the date bug is **systemic to the spine seam**, fixed per-engine). The #175 hydro/topo audit confirmed those routes persist no drizzle-timestamp date field (their `computedAt`/`fetchedAt` are ISO strings in atom JSON, not drizzle columns), so they will NOT hit this on flip. The hydrology flip then surfaced two more, both fixed: **#176** folded the site-topography (catchment DEM) ingest into generate-layers (it had been reachable only via a separate route, so drainage 422'd on "no topography") plus an honest drainage-precondition message; **#177** reprojected ArcGIS/UGRC parcel geometry from Web Mercator to WGS84 before the DEM bbox (Regrid emitted WGS84 and masked this; dropping Regrid for UGRC/Cotality exposed it). **Five canary-caught integration bugs total, none reached prod** — the case for flipping one engine at a time with verification.

**The flip is durable, not manual.** **#174** baked the findings `ENGINE_SPINE_*` flags + `ENGINE_API_URL` + the `ENGINE_API_GATE_TOKEN` secret into `cloud-run-deploy.yml` so deploys stop clobbering them (the line-204 `--set-env-vars`-replaces-everything class that bit three times this session); durability proven by a workflow `deploy-canary` that carried the flags with no manual `gcloud update`. The briefing + hydrology + topography flag appends follow each verify (cc-agent-C, in flight); once landed all five spine env entries are baked and the cut is fully clobber-proof. The mirrored secret `HAUSKA_ENGINE_API_KEY` lives in `legacy-design-tools-prod`, IAM-bound to the `api-server-runtime` SA. Flags flip one engine at a time, verified on the 0% canary before shift, then appended to the workflow.

**Wave 0 is functionally complete. C3 (#179) is MERGED but held un-deployed at operator request.** C3 was the conservative cut: it removed only the dead `useSpine*` flag-off branches in cortex-api (spine path now unconditional), KEPT the adapter pack (`lib/adapters` + generate-layers, still cortex-side BFF) and the engine packages (`lib/finding-engine`/`lib/briefing-engine`/`lib/site-context`, still imported by eval + intake), added a no-ungated-path CI test, and added honest engine-unreachable errors on all four paths (no silent empty success now that the local fallback is gone). typecheck + unit tests green; full-product regression is CI/operator-gated. **The next cortex-api deploy is a checkpoint** (it activates C3 in prod AND proves the bake durability) — confirm CI regression green, then deploy-canary + smoke before shifting. The prod soak ran clean (all four engines exercised, zero errors; drainage ~100s the lone latency outlier, logged to 61 Wave 2). **Hydrology was a fix, not just topology:** the flip moved hydrology to `engine-api`, where pysheds IS pip-installed (`services/engine-api/Dockerfile` on hauska-engine main explicitly installs `pysheds`+`rasterio`), so it runs the real pysheds engine instead of the broken native-D8 fallback the cortex image silently falls back to (the cortex image installs `python3` but never `pip install`s the requirements). Audit backlog #3 is effectively done on the spine side. Rollback handles: `gcloud run services update-traffic cortex-api --to-revisions cortex-api-00167-zac=100` (drops topo) or `…-00146-xul=100` (full pre-cut). Minor open items logged: generate-layers `forceRefresh` does not force a topography re-ingest (signature-keyed); a non-fatal `reviewer_requests` UPDATE failure fires on every generate-layers call (schema/migration drift on that table, worth a look).

**Two governing artifacts filed this session.** The **10-engine robustness audit** ([`_research/2026-06-11_engine_robustness_audit.md`](_research/2026-06-11_engine_robustness_audit.md)): verdict "well-built per-engine, badly integrated at the seam" - confidence is asserted-not-earned on the read path (the calibration engine computes `effectiveConfidence` but the wire never consults it; briefing/code-atom hardcode `1.0`), silent degradation with `status:ok` is the default failure idiom (hydrology never installs pysheds so the broken native fallback always runs; precedence is a production no-op), and the output contract is non-uniform (3 of 9 surfaces emit the envelope; chat emits none); freshness is fetch-time not data vintage across ~8 adapters. The fix is one integration pass: a sealed `EngineEnvelope` at the gate-front seam. The **property-intelligence master plan** ([`61`](61_property_intelligence_master_plan.md)): the governing board merging the audit, the data-layer x analysis x tier inventory, the **Cotality every-SKU adoption plan (Regrid DROPPED**, Cotality is the sole parcel/property spine plus minerals/O&G, flood-depth forcing, insurability), and the open backlog into a seam-first wave sequence (0 flip, 1 seal the seam, 2 fix, 3 pour data, 4 tier).

**Flagged (non-blocking):** a `reviewer_requests` UPDATE query fails on the deployment Neon during implicit-resolve (non-fatal, "domain action kept") - looks like schema/migration drift on that table, separate from the cut, worth a look.

## THE ENGINE LIFT IS BUILT + DEPLOYED; THE CUT IS MERGED + FLIPPING (2026-06-10, prior session)

> **This section supersedes the "engine lift queued / held" claims below.** Session record: [`_sessions/2026-06-10_engine_lift_built_and_deployed_claude_code.md`](_sessions/2026-06-10_engine_lift_built_and_deployed_claude_code.md). Sprint home: [`58_gtm_readiness_sprint.md`](58_gtm_readiness_sprint.md). Built and deployed live this session (verified against gh + gcloud).

**The engine decoupling (ADR-008 / [`56`](56_engine_extraction_sprint.md)) is built and in prod.** Adapters (#69), reasoning engines + engine-api endpoints (#70), calibration overlay I/O on Topology A (#71) all merged to hauska-engine; **engine-api is DEPLOYED** (`hauska-engine-api-00003-vjx`, anthropic finding mode, pysheds baked, gate-front-seam enforced, `/v1/*` live). The **Cortex cut (C1, #169)** and **extension cut (C2, #170)** are merged but **dormant behind default-off `ENGINE_SPINE_*` flags**. Architecture clarification from C1 recon: **cortex-api calls engine-api DIRECTLY via the gate-front seam contract** (X-Hauska-* + Bearer); the physical MCP-gate proxy is a fast-follow for EXTERNAL agents only, NOT a cut blocker.

**The cut is flipping, staged for verification.** `cortex-api-00146-xul` serves 100% (#163–170 live, flags off → still local engines); **`cortex-api-00148-moj` is a 0% canary** with the findings-flip staged (`ENGINE_API_URL` + `ENGINE_SPINE_FINDINGS=1` + `ENGINE_API_GATE_TOKEN`). **Next: verify findings-on-spine on the canary URL (a real San Marcos review — engine-api's deploy smoke was MOCK, so this is the keystone), then shift-traffic; flip briefing/hydrology/topography one at a time; bake the flags into the deploy workflow; then C3** (thin cortex-api / remove the local engine code — the one-way door, held until the flipped engines run clean). Open question: **grok vs anthropic for findings** (engine-api defaults grok; set to anthropic to flip topology-only).

**Code library — Austin 2024 hero library built + grounding-verified.** Harness fix (#162), section-extraction driver (#163, flips unverified→verified), **Austin 2024 hero library (#164)** at the correct adopted package (7 manifests incl. **UMC/UPC 2024** + **TAS 2012**; web-verified **0→~35%**, IECC 0→32%, IFC 51%). Honest-verification baseline is launchable; deepeners (UMC/UPC + TAS web-first; IFC needs ICC) queued. **Texas made whole by demand-driven warm-up, NOT flat batch** ([`_decisions/2026-06-10_texas_coverage_demand_driven.md`](_decisions/2026-06-10_texas_coverage_demand_driven.md)); Tier-1 ~22 jurisdictions within envelope; **San Marcos config-ready, priority-1**.

**Auth — unified identity built + a lockout caught and fixed.** task #29 per-user auth + one identity across Cortex web + extension (#167); the auth deploy locked out existing engagements (removed anonymous default), **fixed #168** (anonymous→migration-owner restores the demo path; isolation + no-pool tests pass in CI; minimal sign-in). Memory: an auth flip on a previously-anonymous app must preserve the anonymous path or it orphans existing data.

**Wedge — unblocked through live dogfooding** (a real San Marcos triplex bid). Intake/chat wedge fixes (#165: pre-Revit chat + multi-attach + Opus-4.8 image vision); artifact-UX (#166: auto-nav to artifact + letter document view + Download PDF/Print). Both live. The web-first wedge (upload a plan, chat/review before any model) now works — the FB-launch flow.

**Moat features.** Provenance envelope + **S1 precedence wired into production findings** (in C1; reconciliation is a pre-launch hero line per [`_decisions/2026-06-10_precedence_hero_path_s1_prelaunch.md`](_decisions/2026-06-10_precedence_hero_path_s1_prelaunch.md)). Permit/AHJ connector recon cleared **BUILD, family-first, post-C4** ([`_decisions/2026-06-10_permit_ahj_precedent_connector_recon_seed.md`](_decisions/2026-06-10_permit_ahj_precedent_connector_recon_seed.md)); permit outcome is arrow-two's deepest signal. Connector candidate backlog (HOA/CC&R, utility, energy/climate, WUI) captured in [`59`](59_spine_moat_and_high_value_features.md).

**Deploy state:** cortex-api `00146-xul` @100% + `00148-moj` canary @0%; engine-api `00003-vjx` live (anthropic); gate unchanged (needs migration 004 + engine-api wiring for external agents, fast-follow); migrations 0036/0037/0038 applied; `SESSION_SECRET` bound. **External dependency: ICC creds** (the lever for IFC/IPMC verification + licensed tier + first paid surface; not on critical path).

**Launch-gate scorecard:** engine lift DONE; Cortex/extension cut MERGED (flipping); per-user auth BUILT (minimal login UI; full UX fast-follow); Austin library BUILT ~35%; wedge LIVE; **C3 not done** (held for flip-verify); **thin user-warm path NOT built** (the one launch-gate item never fired — dispatch queued).

## DEPLOYED + LIVE — build-out + Miami keystone arc in prod (2026-06-09, verified recon)

> **This section supersedes every "deploy deferred / deploy STARTED / RESUME STEPS / prod ships mock / 46->57 pending" claim in the dated sections below.** Verified by live cross-repo recon ([`_research/2026-06-09_cross_repo_recon.md`](_research/2026-06-09_cross_repo_recon.md)): GitHub API, gcloud Cloud Run, live health endpoints, 2026-06-09 ~20:40.

The entire build-out plus Miami keystone arc (legacy-design-tools #141 through #156) is **merged AND deployed to production.** There is no pending deploy. The "deploy the build-out wave" linchpin is closed.

**Live prod (serving 100 percent):** cortex-api **`00140-dax`** (deployed 20:23, nine minutes after #156 merged at 20:14), env **`AIR_FINDING_LLM_MODE=anthropic` + `BRIEFING_LLM_MODE=anthropic` + `AIR_FINDING_ORCHESTRATED=1`**, `/api/healthz` ok — **prod serves real web-grounded findings, not mock** (the #154 `cloud-run-deploy.yml:204` fix held through this deploy, so it is durable). A second prod service **`api-server-00003-wix`** (legacy-design-tools-prod, `/api/healthz` ok) now serves alongside cortex-api and is **not in the [`00c`](00c_portfolio_master_map.md) master map** (identity not yet established; topology-update owed). Gate **`hauska-mcp-server-00007-njc`** `/health` ok, all four deps ok (engine/cortex/postgres/upstash) — wired for real; main `src/tools.ts` registers **57 tools** and the gate image was rebuilt in the 06-09 deploy cluster, so the 46->57 expansion is effectively live. Retrieval **`hauska-retrieval-api-00006-2lq`** `/healthz/` **warn**: corpus ok (`atomCount:21126` confirmed live) but `db:not_configured` — the substrate Neon observability DB is genuinely unwired, which is exactly what **PR #68 (hauska-engine, OPEN + mergeable)** does. SmartCity `00106-riz` (empressa-neon) @ 100 percent, scraper `00038-hb4` @ 100 percent.

**The entire org-wide open-PR surface is three observability/GTM PRs** (everything else is merged): **#68** hauska-engine (wire retrieval substrate Neon — clears the one live warn), **#27** hauska-mcp-server (observability/uptime — must also RETIRE the noisy v1 stale-revision drift alert policy `8570526367601301438`), **#26** hauska-mcp-server (GTM collateral — its body still says "46-tool surface", correct to 57 before merge).

**Corrected next-moves (the real backlog, none is a deploy):** (1) merge #68 / #27 / #26; (2) **finding QUALITY** — the proven FBC M601.6 finding loosely conflated egress with mechanical, this is now the frontier, not plumbing; (3) **unpin GTM Decision C** — its gate ("build-out deployed and tested") is now fully met; (4) **arrow-two calibration capture** — still the only load-bearing build and still not on an active sprint; (5) Cortex 7k phased launch (Phase 1 ephemeral demo, Phase 2 auth/isolation = the unbuilt task #29); (6) Cotality activation (OAuth unblocked via the `api1` host), tenant leg + engine lift (queued). The capital-raise doc [`72a`](72a_capital_raise_positioning.md) gap #1 ("verifiable output not provable on a live prod run / prod ships mock") is **obsolete as of this recon** — corrected in that doc.

## Arrow-two through the gate + national code-warming + tenant leg step 1 (2026-06-09, late session)

> **Supersedes** the recon section's "(4) arrow-two calibration capture — still the only load-bearing build and still not on an active sprint" line: arrow-two is now an active, mostly-merged sprint. **Constitutional change: partnership-first sourcing RETIRED** (amendment `b046f48`); structural commitment #2 is now **confidence-is-earned (calibration)**; the sovereignty root survives as **tenant data sovereignty** (enterprise customer-trust, `tenant-private` never pools into a shared/public number). Decision: [`_decisions/2026-06-09_retire_partnership_first_amend_constitution.md`](_decisions/2026-06-09_retire_partnership_first_amend_constitution.md). The premortem-check **skill** was updated (root cause of the recurring surfacing). Propagated across ten canonical docs (`a671b4e`). **ICC/SDK sub-decision** (`c2fd9cd`, [`_decisions/2026-06-09_icc_nfpa_licensing_and_sdk_rail_survive.md`](_decisions/2026-06-09_icc_nfpa_licensing_and_sdk_rail_survive.md)): ICC/NFPA standards-body licensing KEPT (reclassified as content/display IP licensing, not the retired city sourcing); the SDK source-actor rail KEPT as designed-not-built dormant plumbing (rail-quiet, no substrate-enforced-revenue-share headline). Session record: [`_sessions/2026-06-10_constitution_amendment_recon_review_claude_code.md`](_sessions/2026-06-10_constitution_amendment_recon_review_claude_code.md).

**National code-warming sprint filed ([`57_national_code_warming_sprint.md`](57_national_code_warming_sprint.md)).** Web-first reasoning-atom cold-warm across the model building codes (I-Code family first, 2021 base), integrated with arrow-two into one clean-base build ([`_decisions/2026-06-09_codewarm_arrow_two_combined.md`](_decisions/2026-06-09_codewarm_arrow_two_combined.md)). Six curated high-traffic reference manifests (~640 sections) in [`_catalog/codes/`](_catalog/codes/). Placement settled: the reasoning corpus + adjudication ledger + calibration overlay are **spine substrate served through the gate**, built in cortex-api now behind a clean package seam (engine-core cargo, lifts with the finding engine per [`56`](56_engine_extraction_sprint.md) step 4), not a Cortex feature. Briefing-source citations decided **non-deposit** for launch ([`_decisions/2026-06-09_briefing_source_non_deposit.md`](_decisions/2026-06-09_briefing_source_non_deposit.md)).

**Arrow-two deposit loop now CLOSES through the gate — the decoupling bite is resolved.** Merged today: cold-warm harness **#157** (migration `0036`, `asserted_confidence` vs `calibrated_confidence` split + `source_set_version`/`calibration_stale` + calibration-preserving UPSERT); **P0b canonical atom-id key #158** (`lib/codes` `canonicalOverlayAtomKey`; the overlay no longer silently misses — UUID/DID collapse, reasoning ids pass through); the lineage audit (found the gate was arrow-one-only for the Codex finding path); **P0a+P2 gate-citation-lineage #30 (gate) + #159 (cortex companion)** — `codex_findings_fetch` returns `citations[].atomId` through the gate, override-via-gate preserves them, the adjudication ledger deposits on cited atoms (`overrideCount:1` not zero), tenant-scoped + rail-quiet, key-space consistent. The four-part closure (P0b/P0a/P2/briefing-decision) is complete. **Arrow-two is now CLOSED end-to-end:** Phase 3 calibration merged (`a431e8e`, migration `0037` `atom_calibration_overlay` over both stores + `lib/engine-core` calibration cargo; tenant-sovereignty no-pool partitions; cold-start fallback; source-set-drift invalidation; attribution coverage; rail-quiet). **Invariant I3 (confidence earned, not asserted) is satisfied.**

**Tenant leg step 1 MERGED (#29) — the gate now enforces tenant isolation.** `AuthContext` carries `jurisdiction_tenant` + `platform_internal`; new `access-policy.ts` enforces the five-value `accessPolicy` post-fetch with an `access_policy_denied` audit log; `tenant-isolation.test.ts` proves cross-tenant isolation (tenant-A private atom not returned to tenant-B); ~11 ns/check (ADR-017 cached-sidecar reversal path NOT needed); 262/262 green. **ADR-005 RATIFIED** (status `accepted`, 2026-06-09); two open decisions resolved (tenant-binding = on-key `jurisdiction_tenant` column + `platform_internal` bypass, migration `004`; perf budget cleared). Layer A is live on deploy; Layer B (30a WS-4 SmartCity storage) is independent. `cortex_briefing_emit` provenance fix #28.

**Tenant isolation is live END-TO-END:** gate resolution (#29) + the gate-front seam carrying tenant (#160, step 2) + arrow-2 Phase 2 outcome capture (#160, step 3) + the legacy-client forwarding the tenant header on every gate-front engine call (#31). The seam reaches brief/place/encumbrances/site-drainage/site-topography/findings-generate, each cross-tenant-denied.

**NEXT — the GTM-readiness sprint ([`58_gtm_readiness_sprint.md`](58_gtm_readiness_sprint.md), scaffolded; operator planning next):** two coupled efforts. (1) **Engine lift** (56 steps 3-6), done app-by-app as the QA vehicle (Cortex -> extension -> SmartCity/Bastrop) so each app is validated in its final topology; the lift's three hold-reasons are now all resolved (deploy done, gate wired, cortex-api static after Phase 3); `lib/engine-core` already carries the calibration cargo. Plus the per-user-auth lane (Cortex self-serve, task #29 auth) and product polish. (2) **Code-library maximization** for the FB-group launch: fire the cold-warm runs (national first pass over the manifests), a coverage gap analysis (have vs need), and a background enhancement loop (multi-link accretion + arrow-two calibration accrual + gap-fill). **Deploy reminders:** hauska-mcp-server migration `004` on next deploy; key rotations (`BROKERAGE_DEV_API_KEY` + others) parked.

## Substrate build-out wave merged — deploy deferred (2026-06-07)

The build-out + commerce + calibration-v1 leg is fully merged to main. Session record: [`_sessions/2026-06-07_substrate_buildout_merged_and_tenant_framing_claude_code.md`](_sessions/2026-06-07_substrate_buildout_merged_and_tenant_framing_claude_code.md).

**Merged 2026-06-07:** MCP Tier-1 wraps incl. `generate_property_brief` (hauska-mcp-server #25); brief service seam (legacy-design-tools #144); arrow-two Phase 1 evidence ledger (#143); hydrology (#142); SDK completion, Circle rail + revenue routing + gate metering (hauska-sdk #1). Cotality 8-adapter pack (#141) merged-but-inert. **SmartCity WS-1 COMPLETE - production on Empressa Neon; M-Stabilize 2C CLOSED (2026-06-07).** SmartCity OS is migrated off Replit-managed Neon onto the Empressa-owned Neon. Cutover via Cloud Run canary: `smartcity-api` rev `smartcity-api-00106-riz` (`DATABASE_URL` -> `smartcity-EMPRESSA_DATABASE_URL`), 100% traffic, live-verified (Bastrop dashboard serving real data, zero DB errors); `00104-taw` held at 0% for rollback. Operational data (105 tables, ~330 MB) migrated via a Replit managed-workflow lean/raw split; the two raw scrape tables (96% of the 9.5 GB) are deferred-empty (scrapers repopulate; WS-4 retention decision). The migration fought a throttled, un-scalable source + a 5-min idle-in-transaction timeout; full playbook + gotchas in [`90_runbooks/replit_neon_migration.md`](90_runbooks/replit_neon_migration.md). **Phase 2C closed -> the engine-extraction physical lift ([`56`](56_engine_extraction_sprint.md)) is now unblocked.** Scraper also cut over (2026-06-08, `smartcity-scraper-00038-hb4`) - it was a separate service still on the old secret; raw tables now repopulating on Empressa. Operator follow-ups: rotate/disable old Neon credentials once confident, hold rollback ~24h.

**Code-complete, deploy pending.** Prod still runs prior revisions. Operator deferred deployment until ICC + Cotality are ready and the relevant fixes/builds land. Cotality credential activation is CoreLogic-side (Gene escalation + PUC one-pager drafted, [`_research/cotality/2026-06-06_cotality_puc_data_protection_one_pager.md`](_research/cotality/2026-06-06_cotality_puc_data_protection_one_pager.md)); ICC needs onboarding/creds.

**Next leg = the tenant leg.** Mox and SmartCity are one shape (scoped tenant + custom surfaces + shared spine; see [`00c`](00c_portfolio_master_map.md) scoped-tenant pattern and [`_prospects/mox/2026-06-07_mox_engagement_plan.md`](_prospects/mox/2026-06-07_mox_engagement_plan.md) substrate-readiness section). Both force ADR-005 multitenancy + ADR-008 decoupling + arrow-two Phase 2/3; stand up once, both ride it. SmartCity-on-spine (31a Phase 3 atom-backed context) is now unblocked by the DB-hold release.

**Spine data-intelligence stack + robustness roadmap (2026-06-07).** Verified the analysis engines against live code and filed [`55_spine_data_intelligence_stack.md`](55_spine_data_intelligence_stack.md): the whole-spine topology, the site-context / hydrology / plan-review / corpus / subsurface stacks, an eight-workstream robustness roadmap, and an all-inclusive COGS table (also added to [`14_pricing_framework.md`](14_pricing_framework.md); fixed spine ~$1.25-7.2k/mo, Cotality the swing factor). Findings: confidence on findings is the raw LLM number (uncalibrated, the I3 case arrow-two fixes); the plan-set "peel a PDF apart, specialist agent per piece" capability is NOT built (ingredients exist, the orchestration conductor does not); accessibility standards (A117.1/ADA/FHA), the I-Code family, and subsurface soils/geology are absent; no OpenTopography/OpenEarth (USGS 3DEP only). **Four FIRE-READY spine dispatches teed for deployment** — Wave 1 parallel: cc-agent-E accessibility corpus (ADA/FHA now), cc-agent-C subsurface adapters (SSURGO + USGS), cc-agent-C2 plan-set decomposition; Wave 2: cc-agent-C2 precedence/reconciliation engine (the combine-A117.1-ADA-FHA capability). Distinct from the tenant-leg QUEUED dispatches. **Landing status (2026-06-07):** subsurface PR #145 MERGED. Accessibility corpus PR #66 (hauska-engine) HELD - ADA 2010 (901 sections) + FHA Design Manual (212 sections) ingested as `public-free` under a new `federal-accessibility-standards` tenant, eval 1.0/1.0/1.0; A117.1 + IRC 2021 wired credential-pending (ICC OAuth). The EntreArchitect combine-A117.1-ADA-FHA ask is now substantially met (ADA+FHA live; A117.1 pending creds; precedence engine is the reconciliation layer). Engine-home scaffold PR #67 (hauska-engine) HELD - engine-api + engine-core + adapters skeletons + gate-front seam contract, retrieval-api untouched. Decomposition rebased and PR'd as #146 (legacy-design-tools) HELD - clean on current main, migration renumbered 0034, 71 tests green. PRs #66, #67, #146 MERGED. **Precedence/reconciliation engine landed - PR #147 HELD** (cc-agent-C2): reusable `reconcileStandardPrecedence` primitive, ADR-019/021 rules, 80/80 tests; the combine-A117.1-ADA-FHA demo works (FHA 24in governs latch-side clearance, all three standards cited, conf 0.91). **All four spine-robustness dispatches have landed** (accessibility corpus, subsurface, decomposition, precedence); the EntreArchitect multi-standard ask is delivered (A117.1 live-ingest pending ICC creds). Review flag on #147: the rule label `federal-preempts-where-applicable` is conceptually off for ADA-vs-FHA (both federal, co-applicable - the right frame is most-stringent-governs); outcome correct, taxonomy needs a follow-up refinement. Flag: the hauska-engine primary clone is dirty on `feat/neon-warmup-pilot-load` (uncommitted neon-warmup-pilot work); both cc-agent-E runs used worktrees - operator should resolve/stash that state. **Engine extraction committed as direction:** engines move from cortex-api into `hauska-engine` (new `engine-api` service + `packages/`); these in-flight builds are interim-in-cortex-api and become migration cargo; cortex-api thins to a product BFF; all apps consume through the gate. Sequenced behind M-Stabilize Phase 2C (not yet started). Tier model to be reframed around composable data packages, not personas.

**Tenant leg planned + scaffolded (2026-06-07).** Sequenced plan: [`54_tenant_leg_sprint.md`](54_tenant_leg_sprint.md) (gate tenant resolution -> gate-front seam -> arrow-2 Phase 2 -> Phase 3; SmartCity-on-spine; Mox Phase 0 readiness). ADR-005 scaffolded as the portfolio multitenancy ADR [`80_adrs/adr_005_multitenancy.md`](80_adrs/adr_005_multitenancy.md) (Layer A gate enforcement + Layer B SmartCity storage; resolves the slot collision with 30a's planned SmartCity-only ADR-005, operator-ratified). ADR-008 tenant-leg slice = gate-front seam, distinct from the M-Stabilize-2C-gated repo factor-out ([`_decisions/2026-06-07_adr008_gate_front_seam_scoping.md`](_decisions/2026-06-07_adr008_gate_front_seam_scoping.md)). Four cc-agent dispatches authored fire-ready but **QUEUED (do not fire)** pending operator sequencing vs the deferred deploy + M-Stabilize WS-1. SmartCity gov collateral production scoped in [`07a`](07a_smartcity_product_positioning.md) + a QUEUED build dispatch (pricing-independent assets first; pricing slide gated on the gov pricing decision). Code state verified against live source: gate has no tenant field, `accessPolicy` declared-but-unenforced, arrow-2 Phase 1 ledger partitions on `jurisdictionTenant`. Premortem cleared GREEN (load-bearing); operational yellow on build concentration mitigated by the hold posture.

**Operator gate before more building:** sharpen the SmartCity product line (add the Mox-style browser-extension product alongside plan review and the other surfaces) and refine the Vertosoft offer. **Decision C (GTM) pinned** until build-out is deployed and tested.

## Build-out deploy STARTED - gate WIRED, DEGRADED resolved (2026-06-09)

Operator ran the gate wiring (Step 1 of [`90_runbooks/buildout_deploy_wiring_checklist.md`](90_runbooks/buildout_deploy_wiring_checklist.md)). `hauska-mcp-server` rev `00007-njc`: `HAUSKA_BACKEND_URL` -> the real retrieval URL `https://hauska-retrieval-api-h7gvu7rgcq-uc.a.run.app`; `UPSTASH_REDIS_REST_URL` -> `https://fluent-magpie-131764.upstash.io` (token already a secret ref, untouched). `/health` now `status: ok` - all four deps `ok` (`engine_retrieval_api`, `cortex_api`, `postgres`, `upstash`). **The DEGRADED finding (76e finding 2 - gate on placeholder `HAUSKA_BACKEND_URL`, not wired to the engine) is RESOLVED for real - wired, not relaxed.** Engine-backed MCP tools are live in prod for the first time. The `cortex_api` cold-start probe timeout (~2s) is a known transient (reads `ok` warm); durable fix = widen the probe, not urgent. REMAINING gate-side deploy (lower priority): +11-tools gate image redeploy (46->57), retrieval-api #68 merge+deploy, retire stale drift alert policy `8570526367601301438`.

**cortex-api v2 DEPLOYED to prod (2026-06-09).** #152 MERGED (`30c0ec9`) -> GHA `cloud-run-deploy` canary path: migration `0035` applied (`run-migrations` job success); new revision **`cortex-api-00125-yoq`** (v2 image) serving **100%** of prod; `/api/healthz` -> HTTP 200; this also cleared the cortex revision-drift (00125-yoq supersedes 00119-laq). Web-first grounding + reasoning atoms now live in prod. **KEYSTONE next:** upload the plan set to engagement `15d1d314`, run the whole-review x2 (run 2 must log `reasoningRetrievedCount>0, webFilledCount==0`). Watch: confirm cortex-api finding-mode env (`AIR_FINDING_LLM_MODE=grok`, `AIR_FINDING_ORCHESTRATED=1`, `XAI_API_KEY`, Anthropic vision vars) is set or the review falls back to mock.

**Keystone live-run (2026-06-09) surfaced a real v2 integration gap.** Two blockers found running the review on 404 Remodel_B in the live UI: (1) **UI gate** - the FE `canRunPlanReview()` ([`coverageUi.ts:23`]) blocks "Run plan review" unless `coverageStatus === "ready"` (warmed corpus), a v1-corpus-era precondition the v2 web-first grounding makes obsolete (the backend grounds any jurisdiction on demand). Miami's status is "unknown"/not-ready, so the button is blocked despite v2 being able to web-ground it. **Fix dispatched to cc-agent-C:** make `canRunPlanReview` allow running when a jurisdiction resolves (coverage informational, not a hard gate) + update the FindingsTab copy. (2) **Env still mock** - cortex-api `AIR_FINDING_LLM_MODE=mock`, `AIR_FINDING_ORCHESTRATED` absent, no `XAI_API_KEY`; Anthropic key IS present, so the fast path is `--update-env-vars AIR_FINDING_LLM_MODE=anthropic,AIR_FINDING_ORCHESTRATED=1`. This is exactly the integration gap live validation exists to catch - the v2 grounding works at the engine but the UI gate + finding-mode env weren't wired for it. Keystone needs one small FE fix + the env flip, then re-run. **FE fix LANDED:** PR **#153** (`03fe618`, branch `cortex/v2-reasoning-atom-grounding`, CI GREEN, mergeable=CLEAN) - `canRunPlanReview` now `!!jurisdiction?.trim()` (run when jurisdiction resolves); FindingsTab shows a web-grounded note + enabled button; server path verified (gates on `jurisdictionKey`, web-fills for `miami_beach_fl`). FE-only, no migration. Merge #153 -> `cloud-run-deploy` canary (build-and-push auto, then `deploy-canary` + `shift-traffic`, NO run-migrations) -> re-run review. **Env flip** ran (`AIR_FINDING_LLM_MODE=anthropic`,`AIR_FINDING_ORCHESTRATED=1`) but reported the same revision `00125-yoq` (odd for an env change) - re-confirm via `describe` that it took before the run, since the next canary revision inherits the service env. **ROOT CAUSE of the env not sticking (2026-06-09):** the deploy workflow HARDCODES mock - `cloud-run-deploy.yml:204` `--set-env-vars=...,AIR_FINDING_LLM_MODE=mock,BRIEFING_LLM_MODE=mock,...` REPLACES the whole env on every `deploy-canary`, clobbering any manual `--update-env-vars`. **Tonight's workaround:** re-apply `--update-env-vars AIR_FINDING_LLM_MODE=anthropic,AIR_FINDING_ORCHESTRATED=1` as the LAST step, AFTER the canary deploy+shift. **DURABLE FIX (dispatch needed):** change line 204 to set `anthropic`+`AIR_FINDING_ORCHESTRATED=1`+`BRIEFING_LLM_MODE=anthropic` so prod stops shipping mock - otherwise the 7k demo serves mock findings. Prod ships mock today; real launch blocker.

**RESUME CHECKPOINT (2026-06-09 EOD) - keystone blocked on GitHub Actions infra, NOT code.** #153 merged (`03fe618`) but its `build-and-push` (run 27214851082) failed twice: attempt 1 = docker.io `# syntax=docker/dockerfile:1.7` i/o timeout (network flake); attempt 2 = build ran ~15m then died retrying the same docker.io pull. **CORRECTED root cause (Actions UI 2026-06-09): GHA is HEALTHY** - every workflow_dispatch deploy run (#186-#189) succeeded in 30-57s and PR Checks #431 for #153 passed green. ONLY the `build-and-push` job fails, because it pulls the BuildKit `# syntax=docker/dockerfile:1.7` helper from Docker Hub, which rate-limits GitHub's shared runner IPs. NOT a capacity/quota issue. Fix: re-run the build (per-IP rate-limit shifts, retries often pass), OR manual `docker build`+push from Cloud Shell (not on GHA's rate-limited IPs), OR durable Dockerfile/workflow fix to avoid the docker.io syntax pull.

**KEYSTONE PROVEN (2026-06-09).** The chain works live in prod. After the build retry passed (Docker Hub), deploy-canary (rev `00133-wur`, FE-fix image) + shift-traffic + the env update LAST (anthropic+orchestrated finally stuck), Run plan review on 404 Remodel_B (submission #2DA2) produced REAL web-grounded findings: a CONCERN citing `[[CODE:reasoning:fbc-2023:fbc-m601-6]]` (FBC 2023 Mechanical M601.6 - the operator's balanced-return-air item) via a v2 reasoning atom, plus an advisory. NOT mock (mock cannot cite reasoning atoms). anthropic findings + orchestrated decomposition + web-first reasoning-atom grounding ALL fired on the real Miami permit reading the attached PDF (not the Revit model) - the entire web-first / reasoning-not-text thesis validated end-to-end. **Open follow-ups:** (1) finding-QUALITY tuning (the M601.6 finding loosely conflated egress with mechanical); (2) **Review-surface UX** (operator feedback): make "Run plan review" a visible tab with select-loaded-plan / upload-new-plan, keep Triage Inbox as a separate tab - dispatch to cc-agent-C; (3) durable `cloud-run-deploy.yml:204` fix so prod stops resetting findings to mock on every deploy. **DONE via #154 (`b135dd4`, MERGED 2026-06-09):** Review-surface UX (a visible "Run plan review" tab with plan pick/upload + `planSetPieceIds`; "Triage Inbox" separated; an infinite-render-loop bugfix in `RunPlanReviewTab` that a second cursor-agent caught - the real cause of the 2h test "hang", not flaky tests) AND the workflow:204 fix (`AIR_FINDING_LLM_MODE`/`BRIEFING_LLM_MODE` mock->anthropic + add `AIR_FINDING_ORCHESTRATED=1`). **The next deploy is the LAST manual one and needs NO env re-apply** - the workflow now sets anthropic+orchestrated itself. Sequence: wait for #154 build-and-push green -> `deploy-canary` -> `shift-traffic` -> Review tab live + findings stay real automatically. Closes the Cortex Miami arc. **Deployed + live 2026-06-09: Review surface shows the "Run plan review" + "Triage Inbox" split correctly.** One bug found in the new flow (fix dispatched): clicking Run returns HTTP 409 `finding_generation_already_in_flight` every time + ignores the selected plan pieces - a confirmed DOUBLE-TRIGGER (`RunPlanReviewTab` createSubmission->onSuccess->generate({planSetPieceIds}) collides with the submission-create auto-trigger `autoTriggerFindingsOnSubmissionCreated.ts` -> `kickoffFindingGenerationForSubmission` WITHOUT pieceIds; single-flight partial-unique on `finding_runs(submission_id) WHERE state=pending` 409s the second call; the auto-trigger's pieceless run wins, so generation still happens but ignores the selection). Fix: add a `deferAutoFindings` flag on submission-create so the Run flow's explicit generate (with pieceIds) is the single trigger + client treats 409 as poll-the-returned-generationId, not a hard error. **FIXED via #155 (`9bd0d2e`, MERGED 2026-06-09):** `deferAutoFindings` flag added (`CreateEngagementSubmissionBody` + `engagements.ts` skips the auto-trigger when set); `RunPlanReviewTab` passes `deferAutoFindings:true` + treats 409 as in-progress (poll + navigate to Triage); formal Submit-to-jurisdiction auto-trigger unchanged. Tests green. Build-and-push running -> last deploy cycle (build -> deploy-canary -> shift-traffic, NO env step) -> Run plan review starts one generation honoring the selected pieces, no 409. **THIS closes the Cortex Miami arc: web-first grounded plan review, live in prod, correct UX, honoring plan selection.** **Everything upstream is DONE:** gate wired (rev 00007), v2+#152 live in prod (cortex-api-00125-yoq @ 100%, migration 0035 applied), #153 (FE web-first gate fix) merged. **RESUME STEPS:** (1) re-run the build when GHA recovers (`gh run rerun 27214851082` or push a trivial commit to main); (2) `gh workflow run cloud-run-deploy.yml -R empressaioemail-tech/legacy-design-tools -f action=deploy-canary` -> wait for completion -> same with `-f action=shift-traffic`; (3) **env LAST (the workflow resets it to mock):** `gcloud run services update cortex-api --project legacy-design-tools-prod --region us-central1 --update-env-vars AIR_FINDING_LLM_MODE=anthropic,AIR_FINDING_ORCHESTRATED=1`; (4) `describe` -> confirm `AIR_FINDING_LLM_MODE=anthropic`; (5) Cortex UI 404 Remodel_B (`15d1d314`) -> Run plan review x2 -> run 2 logs `reasoningRetrievedCount>0, webFilledCount==0` = KEYSTONE. Fallback if GHA stays down: manual `docker build`+push to Artifact Registry from Cloud Shell, then deploy-canary. Also queued: durable workflow fix (`cloud-run-deploy.yml:204` -> anthropic) so prod stops shipping mock.

## Cortex 7k-architect launch - PHASED, demo-first (2026-06-08, mid-session)

[`_decisions/2026-06-08_cortex_7k_launch_phased_demo_first.md`](_decisions/2026-06-08_cortex_7k_launch_phased_demo_first.md). **Hard finding (verified recon, ldt @ main):** Cortex has NO user auth and NO data isolation - session pins every prod request to anonymous `{audience:user, tenantId:default}`; core tables (engagements/submissions/findings/snapshots/sheets) have no owner/tenant column; `GET /engagements` ignores the request and returns the WHOLE DB to any caller; CORS open; no rate limiting. 7k self-serve is a real build (the unbuilt "task #29" auth layer), NOT a flag-flip. The removed QA-30/31 render auth gate is the canary. **Two things de-conflated:** (1) the build-out deploy is orthogonal + near-fireable (mechanical; the `HAUSKA_BACKEND_URL` placeholder is the linchpin; ICC no longer gates via web-first, Cotality cleared) but only exposes single-tenant Cortex; (2) the tenant leg [`54`](54_tenant_leg_sprint.md)/ADR-005 is GATE-tenancy for Mox/SmartCity, NOT the architect self-serve auth the 7k launch needs.

**Decision: phase it, demo-first.** Phase 1 = web-first ephemeral demo (address + PDF -> grounded cited review in their jurisdiction, one-time token-scoped, no private-data isolation needed) to prove the wedge + capture demand/calibration from the 7k acquisition surface. Phase 2 = the real auth + per-user isolation leg (task #29: login/signup, owner/tenant columns + backfill, per-route ownership predicates, metering, rate-limit). **Sequence (dependency-ordered):** v2 grounding (in flight) -> fire the deploy (mechanical) -> live Miami proof on 404 Remodel_B (keystone) -> build the ephemeral demo surface -> Phase 2 auth leg. Load-bearing condition: the demo must carry the quality gate (never present `unverified-web-source` as authoritative).

## Reasoning-not-text grounding + web-first GTM (2026-06-08, mid-session) - KEYSTONE

[`_decisions/2026-06-08_reasoning_not_text_grounding_and_web_first_gtm.md`](_decisions/2026-06-08_reasoning_not_text_grounding_and_web_first_gtm.md). Hauska stores **reasoning, not code text.** The code-grounding catalog is reasoning/citation/structure atoms (multiple source deeplinks per code ref, edition, confidence, verification state, arrow-two calibration over time) - never hoarded verbatim code text. Verbatim text is served by deeplink (now) or licensed display (ICC Code Connect / NFPA, later), never stored as catalog. This corrects the "corpus is the moat" conflation: the code-text corpus is commodity + copyrighted + off-message to ICC/Cotality; the **reasoning corpus is the moat** and compounds via arrow-two. Positioning line captured in [`03a`](03a_positioning_framework.md): *the code tells you the rule; Hauska tells you what it means for your building, reconciled with every other code that applies.*

**v2 build dispatched (cc-agent-C):** [`_dispatches/2026-06-08_cc-agent-C_cortex_v2_reasoning_atom_grounding.md`](_dispatches/2026-06-08_cc-agent-C_cortex_v2_reasoning_atom_grounding.md) - merge #151 first, then persist reasoning atoms (multi-link upsert, retrieve-first round 2, no verbatim-text storage, deeplink read-inline + licensed-display seam, arrow-two seam). Premortem GREEN with the code-enforced no-verbatim boundary. **GTM unlock:** web-first means any architect anywhere uses Cortex day one with no jurisdiction onboarding - opens the launch to the operator's ~7k-architect community nationwide WITHOUT pre-ingesting the country. **Retires** the Sync-5 "ingest the tail ahead of demand" posture. Partnerships follow for licensed text display + proprietary city operational data (permit history, precedent), not bulk code files. Operator note: v2 is faster than the manual Cortex deploy, so build-then-deploy.

**v2 LANDED (cc-agent-C):** #151 MERGED (`c33660a`); v2 PR **#152 OPEN + mergeable** (`7689a05`). New `reasoning_atoms` table (migration `0035`, `platform-internal`, OUT of public `code_atoms`); multi-link UPSERT + retrieve-first + no-verbatim-text boundary ALL proven by integration tests (codes 154 + finding-engine 84 green); `displayMode` + arrow-two seams in place. **KEYSTONE PROOF STILL OUTSTANDING - now 3-for-3 (#150/#151/#152 all code-complete-and-tested, the live whole-review on 404 Remodel_B has NEVER run** - no runnable env in agent sessions). Unblock path = the build-out deploy -> apply migration `0035` -> upload plan set to engagement `15d1d314` -> two review runs (run 2 must log `reasoningRetrievedCount>0, webFilledCount==0`). The deploy unblocks the keystone; deploy wiring walked in [`90_runbooks/buildout_deploy_wiring_checklist.md`](90_runbooks/buildout_deploy_wiring_checklist.md). **#152 fixture-drift FIXED + CI GREEN (cc-agent-C, `d61adc3`, mergeable=CLEAN, ready for operator merge):** root cause was the `reasoningAtoms.ts` drizzle schema missing the unique index + 3 CHECK constraints that migration `0035` creates (CI materializes schema via `drizzle-kit push`, so pg_dump lacked them while the hand-edited fixture had them). Schema aligned to the migration + fixture regenerated (no hand-edit). Migration `0035` itself was correct and prod-safe all along; the unique index on `(jurisdiction_key, code_ref, edition)` the multi-link UPSERT needs is present.

## Cortex cockpit + spine (2026-06-08, mid-session)

**Buy-not-build cockpit committed.** [`_decisions/2026-06-08_buy_not_build_cortex_cockpit.md`](_decisions/2026-06-08_buy_not_build_cortex_cockpit.md): the spine (reasoning engines + corpus, lifting to hauska-engine per [`56`](56_engine_extraction_sprint.md)/ADR-008) is the value prop; the cockpit visual/conversion features (rendering+video, image-to-CAD, image-to-3D/BIM) are thin vendor integrations on the cortex-api->BFF side of the seam - not engines, not in the lift. Premortem GREEN. Rendering+video = mnml.ai, already integrated and code-complete, dark behind `RENDERS_PROD_ENABLED=false` + `MNML_RENDER_MODE=mock`; **activate (not build)** on the deploy, after restoring the QA-30/31-removed architect-audience auth gate (open security item). Image/PDF->CAD = Aspose/CloudConvert (format) + RasterScan (semantic); image->3D mesh = Tripo/Meshy (massing, not BIM); true scan->BIM = ODA Scan-to-BIM (beta), the one column needing real engineering. mnml does NOT do geometry. Older Cortex vision corpus surfaced: [`40b`](40b_advanced_capture_features.md), [`40c`](40c_cortex_rendering_sprint.md), [`40e`](40e_cortex_rendering_parity_sprint.md), [`42`](42_design_accelerator_program_plan.md), [`_research/2026-05-23_cortex_ai_model_inventory.md`](_research/2026-05-23_cortex_ai_model_inventory.md).

**Miami whole-review chain landed (cc-agent-C).** PR **#150 MERGED** (`658dbe9`) - PDF text extraction + Miami Beach/Miami-Dade warmup + Opus-4.8 per-discipline vision. PR **#151 OPEN + mergeable** (`cortex/websearch-code-retrieval @ 12a264d`) - web-search code grounding (authoritative-source allowlist, edition verification, no corpus persistence; finding-engine 84 + codes 13 tests green; wrong-edition refusal passes). Both boundaries from [`_decisions/2026-06-08_websearch_code_grounding_supersedes_interim_atoms.md`](_decisions/2026-06-08_websearch_code_grounding_supersedes_interim_atoms.md) enforced. **Outstanding keystone proof:** the live whole-review on engagement 404 Remodel_B (`15d1d314-...`) has NOT run (needs DB + api-server + uploaded plan set). Merge #151, warm Miami jurisdictions, run it, paste the live web-grounded finding.

**Cotality OAuth UNBLOCKED by vendor (2026-06-08).** Cotality (formerly CoreLogic) confirmed via Amanda Morris / Data Implementation Services: app `HauskaioLegacyGroupATX_Property_DemoKey` (consumer key ...UkA, nick@hauska.io) is active + provisioned for Property API v2.0. Root cause of `InvalidClientIdentifier` is NOT a bad/unprovisioned key (the prior "CoreLogic-side provisioning" + "wrong key" hypotheses are both superseded) - it is the **token endpoint host**: Property token requests must route to `POST https://api1.cotality.com/oauth/token?grant_type=client_credentials` (the dedicated `api1` host, never tried in the 2026-06-06 sweep which hit `api.cotality.com` / `api-prod.corelogic.com` / `mcp.cotality.com` / `api-uat.cotality.com`). Strict product isolation confirms the three-keys model: Property / Spatial Tile / RiskMeter each need their OWN independent handshake against their OWN endpoint; per-product base URLs/paths via each product's Swagger tile in the dev portal. Unblocks the 8-adapter Cotality layer (PR #141) + Cotality MCP. Wiring fix (swap the Property token host to `api1`, confirm Spatial Tile / RiskMeter hosts from their Swagger) routes to cc-agent-C; see [`90_runbooks/cotality_mcp_setup.md`](90_runbooks/cotality_mcp_setup.md), [`77b_cotality_integration_strategy.md`](77b_cotality_integration_strategy.md). Memory [[cotality-oauth-three-keys]] updated.

## Inbox sweep reconciliation (2026-06-08, mid-session)

Swept 21 cc-agent drops (2026-06-06..08) against live `gh`; all PR/branch SHAs verified. Most are already reflected below. Net-new and corrections:

- **Miami Beach vision branch now has PR #150** (`cortex/whole-review-vision-miami`, OPEN + mergeable). Its Test check is red from the pre-existing `@workspace/db` fixture drift (PR #146 migration 0034), NOT the Miami code - merge on its merits or regen fixtures first. The web-search code-grounding work builds on this branch: [`_dispatches/2026-06-08_cc-agent-C_cortex_websearch_code_retrieval.md`](_dispatches/2026-06-08_cc-agent-C_cortex_websearch_code_retrieval.md) + [`_decisions/2026-06-08_websearch_code_grounding_supersedes_interim_atoms.md`](_decisions/2026-06-08_websearch_code_grounding_supersedes_interim_atoms.md).
- **observability-hub still local-only (BLOCKER, remote branch 404).** Live findings carried in that report: cortex-api revision drift (`cortex-api-00119-laq` serving 100% vs `00090-vf9` latest-ready, 2026-05-29 vs 06-06) and zero uptime configs on legacy-design-tools-prod. Push-or-lose; the findings are independently actionable.
- **healthz trailing-slash coordination flag:** PR #68 (retrieval-api) proved Cloud Run GFE 404s bare `/healthz`; uptime must poll `/healthz/` with the slash. mcp PR #27's uptime check targets `/healthz` (no slash) - reconcile before relying on it.
- **Status bumps to MERGED since last regen:** smartcity-os Bastrop CIP #23 (still needs `smartcity-api` Cloud Run deploy to take effect); hauska-sdk #1 (SDK completion - `@hauska-sdk/metering` not yet npm-published); subsurface #145; precedence taxonomy #149.
- **SmartCity WS-1 reconcile:** the 2026-06-07 `ws1_migration_2a_2c` "Phase 2B data sync FAILED" report is superseded by the 2026-06-08 lean/raw-split resolution below (raw tables deferred-empty, repopulating on Empressa Neon); 2C is CLOSED, not blocked. Do not reopen.

## Open action map - three lanes (2026-06-08 session close)

Full close: [`_sessions/2026-06-08_session_close_tenant_leg_spine_migration_claude_code.md`](_sessions/2026-06-08_session_close_tenant_leg_spine_migration_claude_code.md).

### Verification + fire list (2026-06-08 pickup)

Live-source verification this session (curl needs `-k`; the planning host has a TLS-intercepting proxy). Migration health GREEN: `smartcity-api-00106-riz` (empressa-neon) @ 100 percent, `00104-taw` @ 0 percent (rollback hold present), `smartcity-scraper-00038-hb4` @ 100 percent, raw repopulating on Empressa (`mygov_raw_records` climbing, sync running). PR sweep: held-for-merge are mcp-server #27 (monitoring) + #26 (GTM), legacy-design-tools #148 (GTM loop), hauska-engine **#68 (retrieval `/healthz`, new + mergeable)**; cc-agent-C observability-hub still LOCAL (no remote branch). doc_repo `origin/main` == HEAD (the GTM "local commits" are already pushed).

**Two findings sharpened the linchpin.** (1) mcp `/health` DEGRADED is a dep-check misconfig, not an outage: the live gate's `HAUSKA_BACKEND_URL` is the literal placeholder `REPLACE-with-cc-agent-E-engine-url` (and upstash URL likewise), while retrieval-api and cortex-api both return 200 directly. The gate was never wired to the deployed engine, so engine-backed MCP tools are dark in prod until the deploy wires the URLs. Do not relax the dep-check; wire at deploy. (2) The "stale-revision traffic drift" alert paging the operator ~daily is noise: the serving mcp revision got 4 requests in 2.5 days, so request_count==0 trips a crude v1 proxy policy. Operator decision: leave it, fix via #27 - but #27 must RETIRE that policy, not just add checks. Both detailed in [`76e`](76e_platform_observability_sprint.md) findings 2-3.

**Fire-ready dispatches authored 2026-06-08 (operator launches in Cursor):**
- [`_dispatches/2026-06-08_cc-agent-M_bastrop_cip_powerbi_repoint_dataverse.md`](_dispatches/2026-06-08_cc-agent-M_bastrop_cip_powerbi_repoint_dataverse.md) - Bastrop CIP PBI repoint. **DONE - landed as PR #23** (smartcity-os, MERGEABLE, all CI green): secrets repointed (v3/v4), `powerbi.ts` remapped to the `msdyn_projecttask`/`msdyn_project` Dataverse schema, 28 projects verified live, 103 tests pass. Held for operator merge + `smartcity-api` deploy. Stakeholder note to Jaime ready; minor flag: duplicate project names disambiguated with a GUID suffix.
- [`_dispatches/2026-06-08_cc-agent-C2_precedence_taxonomy_intra_federal_most_stringent.md`](_dispatches/2026-06-08_cc-agent-C2_precedence_taxonomy_intra_federal_most_stringent.md) - precedence label fix. **DONE - landed as PR #149** (cc-agent-C2, `cortex/precedence-taxonomy-intra-federal`, SHA `62e0970`, MERGEABLE): ADA-vs-FHA now reports `most-stringent-governs`, governing value (FHA 24in) + confidence unchanged, federal-preempt step preserved in chain; finding-engine 81/81, typecheck + eval green. **Held for merge - but see the CI note below.**

**New finding - legacy-design-tools CI is red repo-wide (2026-06-08 sweep).** PR #149's "Test" check fails, but NOT from the precedence change: it is the `@workspace/db` "Check schema fixture drift" (`scripts/check-fixture-drift.sh`), which also fails on the unrelated PR #148 and has been red on `main`'s PR-Checks workflow since 2026-06-07 ~16:35. Timing points at **PR #146 (decomposition, migration 0034, merged 16:24)** landing a db schema change without regenerating the db fixtures. Effect: no legacy-design-tools PR can get a green Test check until the fixtures are regenerated. #149 is safe to merge on its own merits (the red is unrelated and pre-existing); the durable fix is a small fixture-regen on `@workspace/db`. Candidate follow-up dispatch.

**Held by planner recommendation:** engine-extraction lift (56 steps 3-6) - 2C is met but hold behind the deploy (moving-target on cortex-api; step 5 hard-blocked by the unwired gate). See [`56`](56_engine_extraction_sprint.md) lift-status note.

**THE LINCHPIN - deploy the build-out wave.** Merged-but-undeployed (operator-deferred until ICC + Cotality clear). This one gate blocks: GTM Decision C unpin, the +11 Tier-1 MCP tools going live (46 deployed -> 57 in code), SDK metering, the cortex-api revision-drift alert clearing, and the GTM E5 external-caller validation. **Deploy + Cotality OAuth (Gene) + ICC creds is the critical multi-lane unblock.** Exact wiring pinned in [`90_runbooks/buildout_deploy_wiring_checklist.md`](90_runbooks/buildout_deploy_wiring_checklist.md) so the deploy does not repeat the placeholder gap (gate `HAUSKA_BACKEND_URL` -> retrieval URL, upstash, cortex probe, +11 tools, retire the v1 drift policy).

**Lane 1 - spine + migration (this session).** SmartCity migration DONE (2C closed -> engine lift `56` unblocked). Residual: (a) operator rotate/disable old Replit Neon creds (scraper wrote there pre-fix); (b) hold `00104-taw` rollback ~24h then teardown; (c) WS-4 storage **DECIDED + dispatched 2026-06-08**: raw-table retention logged ([`_decisions/2026-06-08_mygov_raw_retention.md`](_decisions/2026-06-08_mygov_raw_retention.md) - 90d raw_records / 14d sync_pages, archive-then-drop to GCS, gated never autonomous); the three empty CANDIDATEs scoped + raw tenant-tagged + the retention job in fire-ready dispatch [`_dispatches/2026-06-08_cc-agent-M_ws4_tenant_scope_and_raw_retention.md`](_dispatches/2026-06-08_cc-agent-M_ws4_tenant_scope_and_raw_retention.md) (implements ADR-005 Layer B; ADR-005 ratification still pending operator); (d) **Bastrop CIP Power BI repoint** to Jaime's new live Dynamics DB (secrets + `powerbi.ts` schema mapping; 31a item). Engine-extraction lift dispatches pre-staged, fireable now (2C met). Tenant-leg dispatches still QUEUED.

**Lane 2 - monitoring (76e Wave A).** HELD for merge: PR #27 (mcp-server healthz) + **PR #68 (retrieval-api healthz, cc-agent-E - landed since close, mergeable)**. cc-agent-C observability-hub is LOCAL (no remote branch - tell it to open/push). Wave B (smartcity) now unblocked (2C done). **The DEGRADED finding is RESOLVED (2026-06-08): dep-check misconfig (placeholder `HAUSKA_BACKEND_URL` + upstash URL), substrate genuinely up - wire URLs at deploy, do not relax the check.** New finding: the stale-revision traffic-drift alert pages on idle traffic; #27 must retire that v1 policy (76e findings 2-3). Plus the operator setup steps (mint GATE_PROBE key + Neon RO token, run setup script, delete test alert policy `8570526367601301438`).

**Lane 3 - GTM (76d).** HELD for merge: PR #26 (collateral, passed honesty gates) + PR #148 (gtm-loop Tier-0, outbound disabled). Capability matrix v1.1 reconciled (46 vs 57). **GTM commits are LOCAL, not pushed - resolve.** Gated on the deploy before Decision C unpin.

## Cotality data spine + Cortex hydrology (2026-06-06)

**Decision:** Cotality is the parcel/property data spine over Regrid for extension + Cortex/Codex viability ([`_decisions/2026-06-06_cotality_parcel_provider.md`](_decisions/2026-06-06_cotality_parcel_provider.md)). Strategy: [`77b_cotality_integration_strategy.md`](77b_cotality_integration_strategy.md). API surface + swaggers: [`_research/2026-06-06_cotality_api_surface_catalog.md`](_research/2026-06-06_cotality_api_surface_catalog.md), [`_research/cotality/`](_research/cotality/).

**Built (cc-agent-C, PR #141 HELD):** full 8-adapter Cotality data layer — 264 tests green, typecheck clean, token-gated. Parcel polygon via Spatial Tile; climate demo-reachable; O&G via SpatialRecord.

**BLOCKER (single gate):** Cotality OAuth throws `Invalid client identifier`. **Likely cause:** eval app signed up under `nick@hauska.io`, but stored key/secret may be from the old demo/other account. **Fix (operator, mechanical):** log into `developer.corelogic.com` as `nick@hauska.io`, re-copy the eval `client_id`+`secret`, overwrite Secret Manager secrets, re-mint. Unblocks live smoke + PR #141 merge + Cotality MCP ([`90_runbooks/cotality_mcp_setup.md`](90_runbooks/cotality_mcp_setup.md)).

**Cortex hydrology (40d 2D.2+2D.3, PR HELD on `cortex/hydrology-engine`):** landed — pysheds sidecar + TS D8 fallback, NOAA Atlas 14 forcing, Cotality flood-depth overlay hook (inert), `site-drainage` tenant-private atom, briefing wired. Typecheck + unit tests green. Smoke "4 inches" on 1904 Heathwood Cir needs DB+GCS; prod needs Python sidecar `requirements.txt` in the Cloud Run image. Report: [`_inbox/2026-06-06_legacy-design-tools_cc-agent-C_cortex_hydrology_engine.md`](_inbox/2026-06-06_legacy-design-tools_cc-agent-C_cortex_hydrology_engine.md).

**ICC Code Connect:** captured as plane-A/Codex feed ([`_research/icc/2026-06-06_icc_code_connect_api_findings.md`](_research/icc/2026-06-06_icc_code_connect_api_findings.md)); contract moving forward; needs ICC creds + onboarding.

**Engine extraction (ADR-008): UNFROZEN** ([`_decisions/2026-06-06_engine_extraction_unfrozen.md`](_decisions/2026-06-06_engine_extraction_unfrozen.md)) — build clean, sequenced behind M-Stabilize 2C + wedge ship.

**Strategic core filed:** [`03`](03_structural_constitution_and_drift_guard.md)/[`03a`](03a_positioning_framework.md)/[`03b`](03b_thought_leadership.md) theology + [`04`](04_roadmap_alignment_audit.md) audit; [`00d`](00d_portfolio_roadmap_reference.md) roadmap. Arrow-two calibration capture = priority #1, now spec'd in [`04a`](04a_arrow_two_calibration_capture.md); Phase 0 recon dispatched to cc-agent-C (2026-06-06).

## Bastrop SmartCity OS — maintenance sprint (2026-06-01)

**Platform grade: YELLOW.** Health check recon complete. Report: smartcity-os
`_research/bastrop_platform_health_check_2026-06-01.md` · branch
`recon/bastrop-platform-health-check` · commit `3bc4eb8`.

**Sprint anchor:** [`31a_bastrop_maintenance_sprint.md`](31a_bastrop_maintenance_sprint.md).
M-Stabilize ([`30a`](30a_smartcity_stabilization_sprint.md)) operator DB hold
RELEASED 2026-06-06; restart dispatch cleared to fire (cc-agent-M, smartcity-os;
Neon target us-east-2 `tiny-art-63602898`). 31a Phase 0-2 parallel-safe. (Other
M-Stabilize-hold references below pending next 00 regen.)

**Production:** `smartcity-api-00106-riz` @ 100% (Empressa Neon). Core dashboards green
(MyGov, Samsara, FirstDue, OpenGov, Compass, calendar). **Prophecy embed RESOLVED 2026-06-08**
(vendor shipped allowlist + `SameSite=None` cookies; pop-out embed verified working; cold
in-iframe login ruled out permanently per WorkOS - 31a P0-3/P1-3 closed). **Unbound:** Verkada, ESRI, transparency key.

**Prior recons folded:** Prophecy `2478a4e`, Compass `1a9d0c9`.

**Operator today:** Run Monday health script (31a → health report §11) +
Phase 0 SQL sample. Answer open questions in 31a.

**Fire when ready:** Phase 1 cc-agent-M dispatch (traffic tags, feedback
auth-gate, thread-health Scheduler, CompassQuickAsk logging).

## Property Brief — data wave sprint (2026-05-29)

**Sprint anchor:** [`75_hauska_brokerage_workflow_plan.md`](75_hauska_brokerage_workflow_plan.md) § Current sprint. **Backlog:** [`75c_property_brief_data_backlog.md`](75c_property_brief_data_backlog.md). **Decision:** [`_decisions/2026-05-29_property_brief_data_backlog_priorities.md`](_decisions/2026-05-29_property_brief_data_backlog_priorities.md).

**Deploy gate:** [PR #134](https://github.com/empressaioemail-tech/legacy-design-tools/pull/134) — place graph (`0030`, `atoms.*`, coverage API). **Fix in flight** (2026-05-29); do not deploy until merged and CI green.

**Ready before deploy:** Neon warmup JSONL for 6 cities (`hauska-engine/tools/migrate-legacy-codes/tmp/neon-warmup-pilot/`). Extension **v0.5.2** atom UX (inbox close). Engine `central_texas_coverage.json` regenerated (34 keys).

**Fire now (cc-agents):** [`2026-05-29_cc-agent-C_brief_federal_site_context_layers.md`](_dispatches/2026-05-29_cc-agent-C_brief_federal_site_context_layers.md), [`2026-05-29_cc-agent-E_neon_warmup_pilot_batch.md`](_dispatches/2026-05-29_cc-agent-E_neon_warmup_pilot_batch.md), [`2026-05-29_cc-agent-C_brief_encumbrance_upload_path.md`](_dispatches/2026-05-29_cc-agent-C_brief_encumbrance_upload_path.md).

**Operator today:** unblock #134 → merge → `0030` + [`property_brief_cortex_deploy.ps1`](90_runbooks/property_brief_cortex_deploy.ps1) → load `round_rock_tx.jsonl` on staging → smoke `/coverage` + `/brief`.

**Partner tomorrow (2026-05-30):** [`90_runbooks/partner_outreach_brief_wave.md`](90_runbooks/partner_outreach_brief_wave.md) — General Code, ICC, county clerk, Bastrop, HOA pilot.

## Cortex prod QA + laptop sync (2026-05-26)

**Prod `cortex-api`:** Deployed through `76a5407` with MCP + Grok findings + Studio env; **PR #127** (`74569dc`) merged — Grok **briefing**, codes **warmup 403 fix**, geocode **auto-warmup**. Operator redeploying `:latest` / `74569dc` with `BRIEFING_LLM_MODE=grok`. Migrations applied via GHA `run-migrations`. Test engagement: **430 Evergreen Trl, Cedar Hill, TX** (`cedar_hill_tx`).

**Laptop:** [`90_runbooks/laptop_workspace_sync.md`](90_runbooks/laptop_workspace_sync.md) + [`_dispatches/2026-05-26_laptop_multi_repo_bootstrap.md`](_dispatches/2026-05-26_laptop_multi_repo_bootstrap.md) — multi-repo Cursor bootstrap + open analysis queue (§14). Pull doc_repo on laptop before product clones.

**Post-QA:** master secret rotation (operator deferred).

## Empressa wedge — 90-day operating scaffold filed (2026-05-26)

**Plan:** [`76_empressa_wedge_90d_operating_plan.md`](76_empressa_wedge_90d_operating_plan.md) — $500M ARR base (Y5), 90-day wedge deploy + legal up front + dual operator loops. **Loops:** [`76a_operator_autonomous_loops.md`](76a_operator_autonomous_loops.md); diagrams [`90_runbooks/diagrams/self_healing_loop.mermaid`](90_runbooks/diagrams/self_healing_loop.mermaid), [`90_runbooks/diagrams/gtm_loop.mermaid`](90_runbooks/diagrams/gtm_loop.mermaid). **Decision:** [`_decisions/2026-05-26_empressa_wedge_operating_commitments.md`](_decisions/2026-05-26_empressa_wedge_operating_commitments.md).

## Brokerage Property Brief — API merged, deploy gated on #134 (2026-05-29)

**Extension:** `P:\hauska-brief-extension` **v0.5.2** — atom UX wave 7 (inbox close). Spec: [`75a_hauska_brief_extension.md`](75a_hauska_brief_extension.md).

**Backend:** **PR #128 MERGED** `73b86bf`; **PR #132/#133** workspace + lay summary merged; **PR #134 OPEN** place snapshots + atoms + coverage — fix in flight. Scope: [`_dispatches/2026-05-28_central-tx-property-brief-scope.md`](_dispatches/2026-05-28_central-tx-property-brief-scope.md).

**Operator gate:** Merge #134 → migration `0030` → deploy per [`90_runbooks/property_brief_cortex_deploy.md`](90_runbooks/property_brief_cortex_deploy.md) → Neon warmup → extension prod smoke.

## Planning carryover — Claude parallel chats (2026-05-24)

**Next major planning session:** read [`_sessions/2026-05-24_claude_parallel_planning_carryover_claude_code.md`](_sessions/2026-05-24_claude_parallel_planning_carryover_claude_code.md) first. Consolidates five threads not previously filed in-repo:

1. **Project memory + assistant behavior** (substrate ADR-019, Cortex/Codex product paths, Item-3 validating slice) — operator yes/no on A1–A4 pending.
2. **Cortex fleet / Replit UI / 40e sequencing** — partially superseded by §4 below; Replit scoping questions B1–B4 still open.
3. **Dossier atom vision** (place → layers → `.hatom`) + pressure test — v2+ horizon; canonical doc/ADRs not started; C1–C2 pending.
4. **Commercialization vision** — v1 clean stop; Part A portability edits + Part B dossier addendum drafted for parallel agent on `HAUSKA_COMMERCIALIZATION_VISION.md`; reconcile 2414 vs 698 rule-unit/atom count before external share.
5. **City-to-city + Starlink** — city-to-city settled in doc set (P3/M9); Starlink recommended park (IoT backhaul warm only); E1 decision record optional.

Master checklist: same session file § "Master open-decisions queue."

## Grok + Atom-First Fleet Migration — Complete (2026-05-23)

**Status:** Complete (documentation track + fleet validation closed 2026-05-24)

**What was accomplished (Phases 1–3):**

- HR-12 established (Grok model strategy + atom-first rule)
- [`01a_atom_conventions.md`](01a_atom_conventions.md) + [`_catalog/atoms_index.md`](_catalog/atoms_index.md) published
- Fleet documentation fully refreshed ([`21_ai_first_dev_flow.md`](21_ai_first_dev_flow.md), [`CLAUDE.md`](CLAUDE.md), [`00_README.md`](00_README.md), [`00b_doc_repo_guide.md`](00b_doc_repo_guide.md), [`_dispatches/_template.md`](_dispatches/_template.md), and related docs)
- New dispatch template with Atoms block
- Validation dispatches completed: cc-agent-R QA-110 (PR #110), cc-agent-C QA-22 CalEPA (PR #111), cc-agent-C2 migration 0017 renumber (PR #112)

Full completion record: [`21d_grok_atom_migration_complete.md`](21d_grok_atom_migration_complete.md). Plan and checklist: [`21c_grok_atom_migration_plan.md`](21c_grok_atom_migration_plan.md).

**New operating standard:**

- Default models: **Grok Build 0.1** (agentic) + **grok-code-fast-1** (speed)
- All agents operate atom-first
- New dispatches must include an "Atoms to resolve" section per [`_dispatches/_template.md`](_dispatches/_template.md)

**Operator remaining actions:**

- Merge PR #110, #111, and #112 when CI green
- Paste global Cursor rule on all workstations (see Phase 1 block in [`21c_grok_atom_migration_plan.md`](21c_grok_atom_migration_plan.md))
- Confirm xAI config (`https://api.x.ai/v1`) across boxes

**Ongoing governance:**

- Planner owns atom catalog maintenance ([`01a_atom_conventions.md`](01a_atom_conventions.md), [`_catalog/atoms_index.md`](_catalog/atoms_index.md))
- Escalate to Claude only when Grok cannot complete a task (log escalation in session summary per HR-12)
- Add new atoms when they appear in 3+ dispatches

This migration improves cost, speed, and context quality across the fleet. Cortex **product runtime** LLM (Anthropic in api-server) is unchanged; product-runtime Grok planning is queued separately as [`40f_cortex_grok_runtime_migration_sprint.md`](40f_cortex_grok_runtime_migration_sprint.md).

## 1. Active fires

- **Fire — Cortex prod QA pass** — Operator validating 430 Evergreen Trl E2E on `cortex-api` after PR #127 deploy (`BRIEFING_LLM_MODE=grok`, warmup, Studio). Image `74569dc` / `:latest`. Owner: Nick.
- **Fire 2** — plaintext secrets. WS-2 internals redacted and rebound to Cloud Run. Remaining: Track B external rotations (Verkada, ESRI/ArcGIS, VFD codes), WS-3 internal items, and the portfolio-level git-history scrub. Owner: Nick + agent.
- Fires 1, 3, 4 closed. Fire 5 closes at M-Stabilize Phase 2C — deferred while M-Stabilize is on operator hold (see watch list).

## 2. In-flight tracks

Three tracks as of 2026-05-22:

- **Cortex / Design Tools QA build** — cc-agent-C, legacy-design-tools. One phased dispatch ([`_dispatches/2026-05-22_cc-agent-C_cortex_qa_build.md`](_dispatches/2026-05-22_cc-agent-C_cortex_qa_build.md)). **Phase 1 and Phase 2 code-complete and all merged 2026-05-22; held for the operator before Phase 3.** Phase 1 PRs #73 (P0-1 0015 migration), #74 (P0-2 geocode), #75 (P1-1 site-context overlays), #76 (P1-3 adapter timeouts), #77 (P1-5 review audience) all merged. P0-3 (3D-site assembly + drainage analysis) flagged as a missing capability, not a bug. Phase 2 PR #81 merged: `cloud-run-deploy.yml` `action` input + four `workflow_dispatch` jobs (`deploy-canary`, `run-migrations`, `shift-traffic`, `rollback`) plus the `migrate-prod.mjs` numbered-SQL runner with the `_schema_migrations` tracker — the cortex-api deploy is now agent-runnable end-to-end, and the migration-drift mechanism behind Phase 1's IFC 500 is closed. Workspace-conflict incident this session — see watch list. Remaining operator gates in the watch list (now narrow: bootstrap run-migrations, canary deploy + verify, mock-mode decision). Phase 3 (QA-27/28/29 features) dispatches once the canary loop is verified. Backlog: [`43_cortex_qa_backlog.md`](43_cortex_qa_backlog.md) WS-G.
- **Cortex rendering sprint** — cc-agent-R, legacy-design-tools (own clone). **Gap-fill complete and merged.** Per the operator's path-A call on the Phase A.1 finding that the render feature was already built and merged, cc-agent-R shipped Phase A (`GET /credits` + Prompt Generator, mnml client + api-server) as PR #79 and Phase B (frontend: credit-balance badge, deliverable-vs-concept intent toggle, expert/style selects, Prompt Generator affordance) as PR #80 — both **merged 2026-05-22**. `RENDERS_PROD_ENABLED` keeps the surface dark in prod. Two scope follow-ons deferred: B.1 full schema-driven per-expert param grid, B.2 manual upload as render source for concept imagery. 40c / dispatch / activation-decision doc reconciliation against `main` still owed by the rendering-sprint planner.
- **Brokerage Property Brief — data wave sprint** — cc-agent-C + cc-agent-E + extension-agent. **PR #134** place graph (fix in flight). **P0:** Neon JSONL load, federal layers dispatch, encumbrance R4 dispatch. Plan: [`75_hauska_brokerage_workflow_plan.md`](75_hauska_brokerage_workflow_plan.md) § Current sprint; backlog [`75c_property_brief_data_backlog.md`](75c_property_brief_data_backlog.md).
- **Sync 5 Texas ingest** — cc-agent-E, hauska-engine. **Central-Texas corridor essentially complete on the accessible-Path-C track.** Tier 1 (6 cities) + Tier 2 (10 cities) ingested. Tier 1 plus early Tier 2 merged (PRs #20/#21/#23/#27/#28/#29: Round Rock, Taylor, Leander, Georgetown, New Braunfels, Killeen). PRs #30-37 open from the continuous-run corridor batch — Austin LDC (the anchor, 2211 atoms via productNameFilter), Copperas Cove, Manor, Lockhart, Lago Vista, Dripping Springs, Wimberley, Rollingwood; 4667 new atoms across 8 cities, all eval 1.0/1.0/1.0. Combined central-TX accessible-Path-C catalog approaching **~8000 code-section atoms across 14 ingested cities** (16 with Bastrop/Bastrop County/Elgin/Hutto). **Four cities adapter-limited** (Luling, Woodcreek, Belton, Creedmoor) — dev chapters nested under wrapper TOC nodes that the current `MunicodeHtmlAdapter.chapterFilter` (top-level only) can't isolate; flagged for a `tocRootNodeIds?: string[]` adapter follow-up (separable). Sync 5 un-deferred per [`_decisions/2026-05-22_sync5_texas_ingest_undeferred.md`](_decisions/2026-05-22_sync5_texas_ingest_undeferred.md). ICC prebuild fully merged (#24/#25/#26). Access-blocked cities route to the General Code partnership track across three aggregators (eCode360, EncodePlus, American Legal Publishing). Tier 2 corpus-quality follow-up still flagged (single-product cities resolve ~zero in-corpus xrefs because the chapter filter excludes the chapters they reference). Next ladder: remaining TX metros (San Antonio metro, Fort Worth, Rio Grande Valley, El Paso area).

**M-CodexQA.** Codex Phase 2 reviewer surfaces CDX-3/4/5 (#69/#70/#71) and CDX-9 comment-letter (#72) all merged. CDX-QA-1 delivered as [`45_codex_qa_scenarios.md`](45_codex_qa_scenarios.md). CDX-Phase1-1 was reversed 2026-05-22 ([`_decisions/2026-05-22_codex_review_surface_relocation.md`](_decisions/2026-05-22_codex_review_surface_relocation.md)): the reviewer-side review surface relocates from the standalone `codex-reviewer-qa` artifact into the Design Tools app's Findings tab, which is Phase 1 P1-4 of the QA build. Phase 2 still carries CDX-EngineHook-prep (gated on 27-A) and CDX-MCP (cc-agent-M, hauska-mcp-server).

**Commercialization Wave 1** — winding down. MCP server deployed in hauska-prod, retrieval API live. Residuals: `mcp.hauska.dev` domain mapping (operator), GTM. Wave 2 (paid Layer 2) parked on operator decisions B (tier numbers) and C (GTM channel plan). This is M-HauskaCommercial, a fourth milestone in the `11_roadmap.md` end-state model.

**MCP build-out + SDK completion (2026-06-06).** Build-before-launch lane between the shipped surface and public launch, captured in [`52_mcp_offer_and_buildout.md`](52_mcp_offer_and_buildout.md): Tier 1 MCP tool wraps (lead `generate_property_brief`, the wedge made agent-callable; specs verified in §3a) and the SDK commerce rail ([`53_hauska_sdk_completion_sprint.md`](53_hauska_sdk_completion_sprint.md): Circle rail, revenue routing, MCP metering). Decision B tier pricing ratified ([`_decisions/2026-06-06_v1_tier_pricing_decision_b.md`](_decisions/2026-06-06_v1_tier_pricing_decision_b.md)); Decision C stays pinned until the lane lands. Dispatches: [`2026-06-06_cc-agent-M_mcp_tier1_buildout.md`](_dispatches/2026-06-06_cc-agent-M_mcp_tier1_buildout.md), [`2026-06-06_cc-agent-C_brief_service_endpoint_exposure.md`](_dispatches/2026-06-06_cc-agent-C_brief_service_endpoint_exposure.md), [`2026-06-06_cc-agent-S_hauska_sdk_completion.md`](_dispatches/2026-06-06_cc-agent-S_hauska_sdk_completion.md) (SDK seat operator-assigned).

## 3. Open ADRs

ADR-005 multitenancy (**scaffolded 2026-06-07 as the portfolio ADR** [`80_adrs/adr_005_multitenancy.md`](80_adrs/adr_005_multitenancy.md), status proposed; Layer A gate enforcement built in the tenant leg [`54`](54_tenant_leg_sprint.md), Layer B storage in 30a WS-4); ADR-006 anchoring substrate; ADR-007 cross-stakeholder atom access; ADR-008 engine factor-out (**committed + operationalized 2026-06-07** as full extraction into `hauska-engine/engine-api`, [`56_engine_extraction_sprint.md`](56_engine_extraction_sprint.md) + [`_decisions/2026-06-07_full_engine_extraction_and_data_packages.md`](_decisions/2026-06-07_full_engine_extraction_and_data_packages.md); scaffold now, physical lift gated on M-Stabilize Phase 2C); ADR-009 firm tenancy (deferred); ADR-014 skill/behavior atoms (deferred, Q3 v2); ADR-016 intent atoms (deferred, v2). Accepted: ADR-013/015/017 (2026-05-16), ADR-018 (2026-05-18), ADR-019 (2026-05-21; Layer 1 model-code ingest gated on ICC API access).

## 4. Agent fleet

- **planner** (doc_repo, Grok-capable Cursor) — portfolio planning, reconciliation, session-close, atom catalog synthesis. Sole owner of the `_inbox/` sweep and `00_current_state.md` per the 2026-05-22 two-planner coordination call; on a 10-minute sweep loop. Default model HR-12: Grok Build 0.1.
- **cc-agent-C** → legacy-design-tools — **Brokerage Brief API dispatch filed 2026-05-26** ([`_dispatches/2026-05-26_cc-agent-C_brokerage_brief_api.md`](_dispatches/2026-05-26_cc-agent-C_brokerage_brief_api.md)). **Validation dispatch #2 complete 2026-05-23.** **PR #111 OPEN** (`cortex/qa22-epa-dig` @ `759e97d`) — CalEPA EJScreen mirror opt-in (QA-22 SCOPE A). Prior local verification green (adapters 239, portal-ui 447, typecheck 7 artifacts). Held for operator merge + redeploy + Redd retest. QA-22 SCOPE A/B/C all addressed (B = drop PR #102 merged, C = Regrid PR #104 merged, A = PR #111). Idle after PR #111 CI unless operator fires dispatch #3 or Phase 3 features. Shipped 6 PRs in the cleanup batch: #87/#88 (diagnostic batch), #90 (QA-33+QA-35 fixes, verified live), #92 (QA-22 throw-path diagnostic), #94 (QA-22 SCOPE B FCC fix — superseded by drop decision; 90s timeout + 15-min cache still architecturally correct for whatever next-gen broadband source replaces it), #95 (QA-22 docs close-out), #96 (FCC structured logging — confirmed Akamai WAF hang via single log entry). 2D-site-context handed to cc-agent-C2 2026-05-23 (re-activated after operator dropped it earlier same day — QA-22 cleanup is multi-back-and-forth so parallelization pays off). Prior: Phase 1 (PRs #73-77) and Phase 2 (PR #81) all merged 2026-05-22; Codex Phase 2 (CDX-3/4/5/9, #69-#72) all merged.
- **cc-agent-C2** → legacy-design-tools (dedicated clone at `P:\legacy-design-tools-c2`) — **Validation dispatch #3 complete 2026-05-24 (Grok Build 0.1).** **PR #112 OPEN** (`2d/migration-0017-renumber` @ `a818805`) — renames site-topography migration `0016` → `0017` to resolve duplicate prefix with 40e `0016_renders_power_tools_source_type.sql`; adds `drizzleMigrationNames.test.ts` guard. Held for operator merge. **Next queued: Phase 2D.1.5 (SiteMap topo overlay UI)** after PR #112 merge + migrations applied.
- **cc-agent-R** → legacy-design-tools (dedicated clone at `P:\legacy-design-tools-r`) — **Validation dispatch #1 complete 2026-05-23 (Grok Build 0.1).** PR #109 MERGED `74a3941`. **PR #110 OPEN — CI green** — held for operator merge (40e close). **NEXT QUEUED (do not fire yet):** [`40f_cortex_grok_runtime_migration_sprint.md`](40f_cortex_grok_runtime_migration_sprint.md) — Cortex **product runtime** Grok migration **planning** (Anthropic → xAI at api-server call sites; separate from agent fleet HR-12). Dispatch: [`_dispatches/2026-05-24_cc-agent-R_cortex_grok_runtime_migration_QUEUED.md`](_dispatches/2026-05-24_cc-agent-R_cortex_grok_runtime_migration_QUEUED.md). Gates: PR #110 merged + operator greenlight.
- **cc-agent-E** → hauska-engine — Sync 5 Texas ingest, continuous statewide. **TX-metros batch shipped 2026-05-23**: PRs #38-#47 open (~5690 atoms across 10 cities — SA core + Boerne + Schertz + Live Oak + Converse; Brownsville + Mission; Saginaw + Keller + Crowley), eval mostly 1.0/1.0/1.0. 3 cities deferred (El Paso compute-bound, Pharr query-bug, Edinburg partial corpus); 8 cities to General Code partnership track (Fort Worth strategic anchor). 8-10 cities staged for follow-on dispatch (Cibolo / Selma / Watauga / Universal City / Converse / Leon Valley / Anthony / Socorro + Pharr re-ingest + El Paso narrow retry). Currently idle pending operator merge of #38-#46 + follow-on dispatch greenlight. Prior: Tier 1 complete (6/6); Tier 2 central-TX corridor (10 cities, #28/#29 merged; #30-37 open, ~4667 atoms). 4 cities adapter-limited (Luling/Woodcreek/Belton/Creedmoor) pending `tocRootNodeIds` follow-up (still queued; dev-only-wrapper class resolved via existing `chapterFilter` this session, distinct from `tocRootNodeIds` mixed-content class). ICC prebuild fully merged.
- **cc-agent-M** → smartcity-os — M-Stabilize restart **held 2026-05-21**
  (operator DB hold). **Bastrop health check complete 2026-06-01**
  (`3bc4eb8`, grade YELLOW). Maintenance sprint at
  [`31a_bastrop_maintenance_sprint.md`](31a_bastrop_maintenance_sprint.md).
  Phase 1 dispatch ready on operator greenlight. Prophecy vendor pending.
- **cc-agent-AC** — dormant. The atom-contract work (the ADR-018 transition, #64/#65/#67) is complete and the queue is empty. The deploy-workflow CI work it briefly carried is folded into the cc-agent-C QA build Phase 2. The clone `P:\ldt-ac-qa17` is vestigial.
- **Nick** — merge, deploy, decisions.

Two doc_repo planner sessions are running: this one (the QA run; owns the `_inbox/` sweep and `00`) and a second (the rendering sprint; commits its own rendering-sprint docs, does not sweep `_inbox/` or edit `00`). The invented agent name "cc-agent-D" was retired 2026-05-22.

## 5. Recent sessions

- 2026-06-07 planner — **Tenant-leg planning + scaffolds.** Scaffolded ADR-005 (portfolio multitenancy, two layers) + the ADR-008 gate-front-seam scoping decision; filed [`54_tenant_leg_sprint.md`](54_tenant_leg_sprint.md) (gate tenant resolution -> gate-front seam -> arrow-2 Phase 2/3, SmartCity-on-spine, Mox Phase 0 readiness); authored four QUEUED cc-agent dispatches + a QUEUED collateral build dispatch; scoped SmartCity gov collateral in 07a; updated 30a (Phase 2A.0 verified, ADR-005 reframe), 31a, 00c, 04a. Code state verified against live source. Premortem GREEN. No build fired. [`_sessions/2026-06-07_tenant_leg_planning_claude_code.md`](_sessions/2026-06-07_tenant_leg_planning_claude_code.md).
- 2026-05-26 planner — **Brokerage extension doc + API dispatch.** Extension pilot documented in [`75a_hauska_brief_extension.md`](75a_hauska_brief_extension.md); [`75_hauska_brokerage_workflow_plan.md`](75_hauska_brokerage_workflow_plan.md) Phase 0/1 status updated; cc-agent-C dispatch for `/api/brokerage/v1/*`. [`_sessions/2026-05-26_brokerage_extension_api_dispatch_claude_code.md`](_sessions/2026-05-26_brokerage_extension_api_dispatch_claude_code.md).
- 2026-05-26 planner — **Cortex prod deploy QA + laptop sync.** Cloud Shell deploy guidance for `cortex-api` (76a5407 → Studio env → PR #127 `74569dc`); migrations via GHA; substrate MCP live; cc-agent-C PR #127 merged (Grok briefing, warmup 403, geocode auto-warmup). Filed [`90_runbooks/laptop_workspace_sync.md`](90_runbooks/laptop_workspace_sync.md). [`_sessions/2026-05-26_cortex_prod_qa_laptop_sync_claude_code.md`](_sessions/2026-05-26_cortex_prod_qa_laptop_sync_claude_code.md).
- 2026-05-23 planner — **Grok + atom-first Phase 3 doc reconciliation complete.** Updated [`11_roadmap.md`](11_roadmap.md), [`09_post_saas_substrate_thesis.md`](09_post_saas_substrate_thesis.md), [`10_ground_truth.md`](10_ground_truth.md), [`18_stakeholder_graph.md`](18_stakeholder_graph.md); expanded [`01a_atom_conventions.md`](01a_atom_conventions.md) (+10 atoms); created [`_catalog/atoms_index.md`](_catalog/atoms_index.md). Migration doc track complete; operator owes Cursor xAI config + first atom-first cc-agent dispatch. Prior Phase 2: [`21_ai_first_dev_flow.md`](21_ai_first_dev_flow.md), [`CLAUDE.md`](CLAUDE.md), [`00_README.md`](00_README.md), [`_dispatches/_template.md`](_dispatches/_template.md). **40e cc-agent-R is ~90% code-complete** (PR #109 MERGED `74a3941` — bulk-merged A.2 + A.5 + A.6 + all of workstreams B + C; PR #110 OPEN — inline dashboard, gallery tree B.5, full power-tool params B.3, browser white-screen fix in `lib/adapters/src/registry.ts` `process.env` guard, Vite proxy in `artifacts/design-tools/vite.config.ts`, CI fixes for render-tools.ts + renders.ts + source-upload test races). **PR #110 CI Test red** — `artifacts/design-tools/src/pages/__tests__/EngagementDetail.test.tsx` (~lines 644-661) still asserts old modal kickoff UX (`renders-tab-new-render` button + `render-kickoff-dialog`); follow-up removed that button in favor of inline `RenderKickoffPanel`. Fix is ~2 test updates. Typecheck green. Largest intentional spec deviation: one unified `RenderPowerToolDialog` vs five spec-named dialogs (functionally complete after #110, structurally different). 40e sprint doc itself still labeled "Just-dispatched" — needs status section update. **cc-agent-C2 PR #107 STILL ships its own migration 0016** (`lib/db/drizzle/0016_add_site_topography_source_kind.sql`) — did NOT pick up the renumber memo; will collide with cc-agent-R's now-merged `0016_renders_power_tools_source_type.sql` when PR #107 rebases on main (different filenames, same numeric prefix, drizzle ordering ambiguity). CI red on `actorLabel.test.ts` was fixed in commit `233d53e` — pending re-run. **cc-agent-C CalEPA opt-in** branch `cortex/qa22-epa-dig` at `759e97d` (on top of dead-end-ledger `b108ff7`); local verification all green (typecheck across 7 artifacts + scripts; adapters 239 passed across 14 files; portal-ui 447 passed across 36 files). **PR not yet opened** — operator needs to `gh pr create --repo empressaioemail-tech/legacy-design-tools --base main --head cortex/qa22-epa-dig`. **Mode shift confirmed**: build heavy-lifting moves to Cursor agents (Replit Agent for the operator-planned UI overhaul); planner (this Claude session) stays in planning/filing/dispatch-design lane. cc-agent-R successor in flight on Cursor per the paste-ready handoff prompt filed 2026-05-23.
- 2026-05-23 cc-agent-C — **QA-22 SCOPE A CalEPA opt-in shipped** on branch `cortex/qa22-epa-dig` (continues on existing branch — dead-end-ledger commit + CalEPA opt-in commits, one combined PR; PR number TBD via `gh pr list` per [[verify-identifiers-against-live-source]]). Adapter full rewrite: `EPA_EJSCREEN_FEATURESERVER` constant (CalEPA URL), `EPA_EJSCREEN_LABEL` ("EJScreen (CalEPA mirror)" for failure pills), `EPA_EJSCREEN_PROVIDER_LABEL` (visible attribution string), `EPA_EJSCREEN_DATASET_VERSION` (frozen snapshot vintage). `run()` rewritten to call `arcgisPointQuery` directly. Freshness threshold 18 → 24 months (CalEPA frozen snapshot, no published refresh cadence); `timeoutMs` override removed (CalEPA answers 0.3-0.6s, well inside runner default). All three decision-record implementation requirements satisfied: (i) source attribution `"EJScreen 2023 (CalEPA mirror — EPA EJScreen API retired, awaiting v2)"` on persisted provider + failure pill, em-dashes not parens to avoid `extractAdapterKeyFromProvider` confusion; (ii) state-percentile disclosure via `payload.percentileBasis = "state"` on payload (forward-compat for v2 swap-back — flip to `"us"` and every surface renders correctly), chips read `state-pctile`, KvRow labels read `"<indicator> state percentile"`, diff labels updated; (iii) dataset-vintage footer renders `"Dataset: EJScreen 2023 (CalEPA mirror, published 2024-01-29)"` beneath percentile rows (independent of `snapshotDate` cache-age footer). Four design decisions documented (percentileBasis on payload, threshold rationale, removed timeoutMs override, em-dash attribution). 10 test cases (beyond 4-case dispatch floor) across `federalAdapters.test.ts` + `federalSummaries.test.ts` + `BriefingSourceHistoryPanel.test.tsx`; e2e fixtures updated. **No migration / secret / config change required** (just a different upstream URL). cc-agent-C-side verification (CI runs typecheck + adapter tests + portal-ui test) pending on Win32 native-deps workaround. **Operator handoff**: find PR number via `gh pr list --repo empressaioemail-tech/legacy-design-tools --state open`; merge → redeploy → Redd retest → expect EPA pill flips from red to green with state-percentile disclosure visible. Filed at [`_sessions/2026-05-23_qa22_epa_calepa_opt_in_cc-agent-C.md`](_sessions/2026-05-23_qa22_epa_calepa_opt_in_cc-agent-C.md).
- 2026-05-23 cc-agent-R — **40e workstream A halfway handoff** (paused at 3 of 6 A-items done: A.1 + A.4 merged; A.3 open as PR #108; A.2/A.5/A.6 remaining + workstreams B + C untouched). **PR #106 (A.4) MERGED 2026-05-23T18:41Z** — `viewpoint_renders` columns added via migration 0016; fixture hand-edit took one CI iteration (pg_dump emits columns in drizzle schema order right after `kind`, not table end; fixed in second commit). **PR #108 (A.3 per-expert parameter schema) OPEN** — static TS-defined schema module at `lib/portal-ui/src/schemas/mnml-experts.ts` capturing mnml's archDiffusion-v43 schema (10 common + 6 per-expert grids), powers B.1 + available to A.2 for server-side validation, 20 tests passing locally. cc-agent-R wrote a paste-ready successor-prompt at [`_sessions/2026-05-23_40e_handoff_prompt_cc-agent-R.md`](_sessions/2026-05-23_40e_handoff_prompt_cc-agent-R.md) for the operator to fire on a fresh cc-agent-R session — covers workspace verification, read-first list, state-at-handoff, remaining workstream-A sequencing (A.2 routes → A.5 upload-as-source → A.6 atom variants), workstream-B + C plans, allowlist path corrections, decisions carried forward, Windows test-runs procedure, autonomy clause, reporting, cross-agent state, activation gate, concrete first step. **Operator return-tasks** from cc-agent-R (re-stated from PR #106 handoff): apply migration 0016 to prod after PR #106 lands on Cloud Run revision (Cloud Shell `psql` or `run-migrations` workflow per [`90_runbooks/cloud_run_canary_deploy.md`](90_runbooks/cloud_run_canary_deploy.md)); verify fixture via `pnpm --filter @workspace/db run test:fixture:schema`; `RENDERS_PROD_ENABLED=false` stays (no prod activation). cc-agent-R's note on cross-agent state confirms no collisions detected this session.
- 2026-05-23 cc-agent-C2 — **Phase 2D.x PR3 (DEM ingest worker) shipped as PR #107** (`2d/dem-ingest-worker`). Full dispatch scope landed in one PR per acceptance criterion: schema migration 0016 + drizzle schema TS + test fixture template (lockstep); new `SITE_TOPOGRAPHY_INGEST_ACTOR_ID`; worker (`artifacts/api-server/src/lib/siteTopographyIngest.ts` — parcel resolver, catchment-buffer bbox, USGS 3DEP fetch, GeoTIFF parse, contour derivation, GCS upload, atom event append); materializer (`siteTopographyMaterializer.ts` — atom_events → materializable_elements row, engagement-scoped supersession, replay helper); route (`routes/siteTopography.ts` — POST refresh + GET read); atom widen (`contextSummary` surfaces post-ingest metrics); 8 test cases (6 dispatch-spec + 2 extras including replay-from-events). Deps `geotiff` + `d3-contour` + `@types/d3-contour`. **Workspace typecheck clean**; integration tests skip locally (no Postgres on Windows worktree), run on CI. **Material divergence from dispatch's "no operator decisions remaining" framing**: migration 0016 IS needed (cc-agent-C2's earlier PR #98 audit was column-level correct — `propertySet` is `jsonb` — but row-level wrong — `materializable_elements` carries two CHECK constraints `source_kind_check` + `provenance_invariants_check` that reject `source_kind='site-topography'` outright). Migration 0016 widens **only** the allowed set (existing-row invariants preserved); new branch is `source_kind = 'site-topography' AND engagement_id IS NOT NULL`. Three "your call" decisions documented: (1) worker invocation = synchronous from refresh route (~3-6s per-parcel derivation at parcel+500m + 10m DEM fits inside Cloud Run request budget; wrap in Cloud Run Job follow-on if profiling shows >10s); (2) contour interval default = 5m (coarse enough to keep GeoJSON <1MB at parcel+500m / ~2 km² extent at 10m DEM; route accepts per-call `contourIntervalMeters` override 0 < x ≤ 100); (3) atom-event payload shape aligned with `engagementEvents.ts` + `ifcIngest.ts` convention — carries `schemaVersion: 1`, `computedOrigin: true`, parcel provenance, catchment, DEM metadata (source/resolution/GCS path/elevation min-max/nodata count), contours (interval/thresholds/featureCount/featureCollection), `inputSignature` SHA-256 idempotency hash, `workerVersion` semver, `previousAtomEventId` on .refreshed events. **What this unblocks** (cc-agent-C2's framing): Phase 2D.1.5 (SiteMap topo overlay UI, consumes `propertySet.contoursGeoJson` off GET — pure FE, no schema/worker change); Phase 2D.2 (hydrology + drainage analysis, reads DEM via `demRef` GCS path, emits sibling `site-drainage` atom referencing the `site-topography` event id per ADR-011); Phase 2D.4 (address-to-parcel auto-resolve polish, wire refresh route into Generate Layers callback chain). Filed at [`_sessions/2026-05-23_phase_2dx_pr3_dem_ingest_cc-agent-C2.md`](_sessions/2026-05-23_phase_2dx_pr3_dem_ingest_cc-agent-C2.md). **Operator handoff** (cc-agent-C2's list): apply migration 0016 to prod (mirrors QA-04 Part 2 manual psql pattern; SEE COLLISION WARNING BELOW); merge PR #107; deploy resulting cortex-api revision; live smoke against Musgrave or Redd (POST refresh + verify GET returns contours).
- 2026-05-23 cc-agent-R — **40e workstream A milestone**: PR #105 (A.1 power-tool client methods — `enhance` / `upscale` / `aiErase` / `inpaint` / `styleTransfer` on `MnmlClient` / `HttpMnmlClient` / `MockMnmlClient`) MERGED 18:31Z; 101 mnml-client tests pass (up from 80). PR #106 (A.4 schema migration + drizzle + fixture hand-edit) OPEN. PR #106 adds 3 columns to `viewpoint_renders` (`source_type TEXT NOT NULL DEFAULT 'model-capture'`, `source_upload_url TEXT`, `parent_render_output_id UUID FK to render_outputs.id ON DELETE SET NULL`); migration 0016 idempotent + additive. cc-agent-R made three autonomous decisions documented in PR bodies: (1) reordered A.4 before A.2 (A.2 hard-depends on A.4's `source_type` discriminants); (2) fixture hand-edit (Windows clone can't run Postgres so couldn't refresh pg_dump-driven fixture properly — operator return-task to run `pnpm --filter @workspace/db run test:fixture:schema` against test DB after migration 0016 applies and commit drift); (3) cost lookup deferred to A.2 (used 1 credit per call for all 5 tools in `MockMnmlClient`; documented `upscale` + `ai_eraser` are 1 credit, other 3 unspecified in mnml docs). **Operator return-tasks**: apply migration 0016 to prod via `run-migrations` workflow or Cloud Shell `psql` per [`90_runbooks/cloud_run_canary_deploy.md`](90_runbooks/cloud_run_canary_deploy.md) after PR #106 merges (additive + idempotent, safe); verify fixture against pg_dump output. `RENDERS_PROD_ENABLED=false` keeps 40e dark in prod (no activation change). cc-agent-R also surfaced a path-allowlist feedback item — dispatch listed `artifacts/api-server/src/lib/...` paths that don't exist; actual paths are `lib/...` (4 corrections + 1 fixture co-edit path) — rendering planner should update future dispatch boilerplate. No cross-agent collisions (cc-agent-C and cc-agent-C2 on their own clones; 40e file-path allowlist disjoint). Next per cc-agent-R: A.2 (5 routes consuming `source_type`) → A.3 (per-expert param schema) → A.5 (upload-as-source pipe) → A.6 (render-output atom variants). Filed at [`_sessions/2026-05-23_40e_a1_a4_milestone_cc-agent-R.md`](_sessions/2026-05-23_40e_a1_a4_milestone_cc-agent-R.md); 40e canonical updates belong to the rendering-sprint planner.
- 2026-05-23 cc-agent-E — **Sync 5 TX-metros batch shipped: 10 PRs (#38-#47), ~5690 atoms across 10 cities in 3 metros** (SA core + Boerne + Schertz + Live Oak + Converse; Brownsville + Mission; Saginaw + Keller + Crowley). Eval mostly 1.0/1.0/1.0 with two exceptions (Brownsville 0.9, Crowley 0.95, both above 0.9 bar). **3 cities deferred**: El Paso (compute-bound, >22 min wall-time on broad scope + >9 min on narrow retry — needs scope slicing or budget bump; branch + queries staged at `stream-1d/sync-5-tx-metros-el-paso`), Pharr (clean 729-atom ingest but eval 0.75 < 0.9 bar due to reserved-range curated-query authoring bug — small re-ingest lift), Edinburg (partial-corpus shape — Municode "TITLE XV - LAND USAGE" carries only 3 chapters, full UDC appears off-Municode). **8 cities routed to General Code partnership track** (NO-RESULT on Municode `/Clients/name`): **Fort Worth** (strategic anchor — without it, FW metro catalog limited to 4 sub-50k suburbs), Arlington, Mansfield, Burleson, North Richland Hills, McAllen (confirmed eCode360 ID `MC6775`), Harlingen, Horizon City — appended to [`73_partnerships.md`](73_partnerships.md) General Code row (McAllen) + new publisher-TBD bucket note (others). **3 net-new operational findings**: (i) dev-only-wrapper cities (Schertz, Keller) resolve via existing `chapterFilter` — no `tocRootNodeIds` adapter change needed, distinct from the Luling/Woodcreek/Belton/Creedmoor queued class where wrappers contain mixed dev+non-dev chapters; (ii) reserved-range curated-query trap — Municode CoO chapters with `Secs. <ch>-1—<ch>-25. - Reserved.` placeholders; substantive run starts at `<ch>-26`, query authors must walk children first not assume `<ch>-1` is content; (iii) El Paso wall-time envelope exceeds per-city budget at Municode 1.5s politeness ceiling even with envelope-bundling. **Parallelization via 3 concurrent git worktrees** materially shortened wall time (~2.1 req/sec peak combined load, each worktree respectful-fetched at 0.7 req/sec). 8-10 staged-but-unrun cities ready for follow-on (Cibolo / Selma / Watauga / Universal City / Leon Valley / Anthony / Socorro + Pharr re-ingest + El Paso narrow retry — Converse staged-list entry resolved post-report as PR #47, see addendum [`_sessions/2026-05-23_sync5_tx_metro_batch_converse_addendum_cc-agent-E.md`](_sessions/2026-05-23_sync5_tx_metro_batch_converse_addendum_cc-agent-E.md)). **Operator action**: merge PRs #38-#46 (operator-supervised cadence), choose whether to fire follow-on dispatch for staged cities + Pharr re-ingest, decide El Paso scope-slicing approach, batch snapshot-refresh + retrieval-api redeploy when ready (covers PRs #30-#46 = 17 merged-but-undeployed cities). Filed at [`_sessions/2026-05-23_sync5_tx_metro_batch_cc-agent-E.md`](_sessions/2026-05-23_sync5_tx_metro_batch_cc-agent-E.md); retroactive dispatch artifact at [`_dispatches/2026-05-23_cc-agent-E_sync5_tx_metro_batch.md`](_dispatches/2026-05-23_cc-agent-E_sync5_tx_metro_batch.md).
- 2026-05-23 operator + planner — **CalEPA mirror opt-in CONFIRMED** (operator override of planner NO recommendation; three policy deltas explicitly accepted: not-EPA-owned, state-percentile-not-US, demographic-index methodology shift 2022→2023); decision record at [`_decisions/2026-05-23_epa_calepa_mirror_opt_in.md`](_decisions/2026-05-23_epa_calepa_mirror_opt_in.md). cc-agent-C follow-on dispatch teed in chat — continues on existing `cortex/qa22-epa-dig` branch (no fresh branch; PR will cover both the dead-end-ledger commit + the CalEPA opt-in commits as one). Implementation requirements per decision record: source-attribution UI copy ("EJScreen 2023 (CalEPA mirror — EPA EJScreen API retired, awaiting v2)"), state-percentile UI disclosure (label/tooltip/column header — cc-agent-C's call where), refresh-threshold tagging (mirror last updated 2024-01-29), reversal path documented. **cc-agent-C2 Phase 2D.x PR3 (DEM ingest worker) FIRED 2026-05-23** by operator per paste-ready prompt — actively running on legacy-design-tools-c2 clone, persistence path = `atom_events` + `materializable_elements.propertySet` combined. **EPA docstring PR**: cc-agent-C will open one combined PR on `cortex/qa22-epa-dig` after CalEPA opt-in work lands (single PR covers both dead-end-ledger + URL+query swap + field renames + UI copy disclosure + refresh-threshold), so operator doesn't need to `gh pr create` separately for the docstring commit.
- 2026-05-23 cc-agent-C — **QA-22 SCOPE A EPA Path 1a verdict: MISS at EPA layer** (full retirement confirmed independently — `ejscreen.epa.gov` ECONNREFUSED, `www.epa.gov/ejscreen*` all 404, `catalog.data.gov/dataset/ejscreen` 404; EPA ArcGIS REST sweep across `geopub.epa.gov` (28 folders) + `gispub.epa.gov` (25 folders), 6 folders probed by EJ-relevance + `edg.epa.gov/data/PUBLIC/OEI/` — no successor service emits the EJScreen block-group percentile schema). **Found third-party CalEPA-hosted mirror** at `services2.arcgis.com/iq8zYa0SRsvIFFKz/.../EJSCREEN_2023_BG_StatePct_with_AS_CNMI_GU_VI_gdb/FeatureServer/0` — 5/5 indicators preserved (`ACSTOTPOP` ← `RAW_D_POP`, `P_DEMOGIDX_2` ← `P_D2_VULEOPCT`, `P_PM25`/`P_OZONE`/`P_LDPNT` verbatim) and live-queryable (verified against Moab UT lat/lng — returned BG 490190002004 with all 5 fields populated). **Filed as MISS not HIT due to three policy deltas requiring operator opt-in**: (i) not EPA-owned (third-party ArcGIS Online tenant, no refresh-after-EPA-stops, federal-tier promise weakened); (ii) `BG_StatePct` semantics — state-distribution percentiles not US-distribution (silent semantics drift if adopted without UI surfacing); (iii) `P_DEMOGIDX_2` is more than rename — EJScreen 2023 dropped "vulnerable" from demographic-index formula, no apples-to-apples comparison across 2022→2023. cc-agent-C wrote dead-end ledger into `lib/adapters/src/federal/epa-ejscreen.ts` top-of-file docstring (so a future agent doesn't re-dig the same ground) + this session note. No PR opened per dispatch's MISS clause. Branch `cortex/qa22-epa-dig` pushed; operator may merge directly or open PR. Estimate to opt-in: ~2-4 hours of cc-agent-C work (URL+query-shape swap, field renames in payload, briefing UI copy disclosure, refresh-threshold decision since mirror is 16 months stale and CalEPA-not-EPA refresh cycle). **Operator decision pending**: opt into CalEPA mirror OR leave EPA pill red until EPA publishes a v2. Filed at [`_sessions/2026-05-23_qa22_epa_path1a_cc-agent-C.md`](_sessions/2026-05-23_qa22_epa_path1a_cc-agent-C.md). Concurrent operator-side answers 2026-05-23: **Phase 2D.x persistence-path: CONFIRMED** combined model (`atom_events` source-of-truth + `materializable_elements.propertySet` derived read) — unblocks cc-agent-C2 PR3 dispatch; **Regrid paid-plan upgrade: deferred** ("I'll upgrade later" — system handles trial-out-of-coverage as neutral pill correctly).
- 2026-05-23 operator + planner — **Redd verify final pass: Regrid integration LIVE and working as designed.** After operator rotated `REGRID_API_KEY` secret to actual token (version 3) + nudged revision `cortex-api-00028-dz7` (`--update-secrets` + `--to-latest`), Redd Force-refresh shows: `regrid:parcels no-coverage` + `regrid:zoning no-coverage` — both NEUTRAL pills with explanatory text ("Regrid returned no parcel polygon at this lat/lng" / "the parcel may sit in an unzoned tract or in a county Regrid hasn't standardized zoning_type for yet"). HTTP 200 + empty features, not auth failure — token is valid. **Production-verifies cc-agent-C2 adapter test case #3** (`isTrialOutOfCoverage` heuristic — empty features + error envelope keyword match → `AdapterRunError("no-coverage")` → neutral pill not red). FCC absent ✓, Grand County GIS absent ✓, FEMA + USGS + UGRC dem/parcels ok ✓, UGRC address-points + Regrid pair neutral no-coverage ✓, EPA still red (pending SCOPE A Path 1a fire). **All three QA-22 reopen scopes structurally closed** (B+C verified live; A pending dispatch). Paid-plan Regrid upgrade is operator-side flip — rotate secret to paid-tier token + nudge revision, no code redeploy; pills flip to green `ok · Regrid (via Grand County)` with parcel polygon + zoning shape on SiteMap. Multi-round operator-side debug session logged two new memory entries: [[cloud-shell-is-bash]] (PowerShell-vs-bash recipe-mixing risk) + [[explicit-placeholder-convention]] (ALL_CAPS placeholders get pasted verbatim — use `<angle-bracket>` + shell variable + length-echo + curl-test before destructive secret-write).
- 2026-05-23 operator + planner — **Decision queue worked through after merges**: (1) Phase 2D.x persistence-path operator asked for help deciding — planner recommended combined `atom_events` source-of-truth + `materializable_elements.propertySet` derived read model (honors ADR-013/015/017 + uses cc-agent-C2 PR #98 audit finding + zero migration), awaiting operator confirm before PR3 fires on cc-agent-C2; (2) QA-22 SCOPE A EPA Path 1a greenlit, paste-ready prompt teed in chat for cc-agent-C (three verdicts spec'd: HIT/PARTIAL HIT/MISS), awaiting operator paste-and-fire; (3) cc-agent-E TX metro batch operator-confirmed already running ("its been working for a while") since earlier 2026-05-23 fire — no report yet in `_inbox/`, dispatch artifact will land retroactively on first report.
- 2026-05-23 operator + planner — **PRs #102 + #104 + #101 merged**; cortex-api redeployed. **Redd verify ran in two passes**. **First pass** (revision `cortex-api-00026-tz9`): FCC pill gone ✓ (PR #102 — `isFccEnabled` gate); Grand County pills gone ✓ (PR #104 — `partnerCity` gate); FEMA + USGS + UGRC dem/parcels ok; EPA still failed (expected — SCOPE A Path 1a queued); `regrid:parcels` + `regrid:zoning` failed with diagnostic "REGRID_API_KEY is not configured" — exact match for cc-agent-C2 adapter test case #10. Operator hit memory `cloud-run-secret-and-traffic-gotchas`: secret created + IAM bound ≠ secret mounted on Cloud Run service env. Fix: `gcloud run services update cortex-api --update-secrets=REGRID_API_KEY=REGRID_API_KEY:latest --region us-central1` + `update-traffic --to-latest --clear-tags`. **Second pass** (revision `cortex-api-00027-6jt` with secret mounted): `REGRID_API_KEY` confirmed in env list ✓; two new failure modes surfaced — (i) `usgs:ned-elevation failed` "USGS EPQS did not respond in time — request exceeded time budget during attempt 1" (regression — was ok in first pass; likely cold-cache + transient USGS slowness, retry-with-Force-refresh recommended); (ii) `regrid:parcels` + `regrid:zoning failed` "Regrid responded with HTTP 401. Upstream response: {\"status\":\"error\",\"message\":\"Access denied\"}" (wire path works, auth rejected — most likely the token value in the secret is wrong/expired or scope-restricted; operator to verify token at https://app.regrid.com/api and curl-test from workstation). FCC pill + Grand County pills still absent ✓ (gates holding). Both cc-agent-C + cc-agent-C2 idle post-merge. **Parallel rendering tab revamp activated**: rendering-sprint planner spun up 40e (Cortex rendering parity sprint), dispatched cc-agent-R from dormant — full 40c deferrals (B.1 schema-driven param grid + B.2 image-upload-as-render-source + 5 power tools) pulled forward into one sprint, UX polish bundled in-scope, mask-drawing canvas built in-scope (not deferred); three workstreams A/B/C; canonical doc + decision record + dispatch artifact are the other planner's territory (40e + `_decisions/2026-05-23_cortex_rendering_parity_sprint_scope.md` + `_dispatches/2026-05-23_cc-agent-R_rendering_parity_build.md`, all under that planner's commit).
- 2026-05-23 cc-agent-C2 — **Cortex prop-intel SCOPE B Regrid baseline shipped as PR #104** (`cortex/regrid-baseline`, one PR per cc-agent-C2's call — shared upstream call + dedup map + provenance argued against splitting). 8 dispatch points all landed: new `lib/adapters/src/national/regrid.ts` housing the paired `regrid:parcels` + `regrid:zoning` adapters with a shared concurrent-safe in-memory dedup Map (15-min TTL); `overlays.ts` extends `payload.parcel.geometry` / `payload.zoning.geometry` to recognize GeoJSON Polygon + MultiPolygon alongside ArcGIS-rings (regression-test on ArcGIS branch added); provenance mapping `snapshot_date ← ll_last_refresh`, `provider ← "Regrid (via <county>)"`, `layer_kind ← "regrid-parcel"`/`"regrid-zoning"`, `source_kind ← "national-aggregator"` (new value; **no schema migration** — `briefing_sources.source_kind` is `text NOT NULL` with no CHECK); 24h Postgres `adapter_response_cache` + 15-min in-memory dedup both apply; runner gates `grand-county-ut.ts` on `ctx.jurisdiction.partnerCity === true` (default engagements skip per-county adapters as `no-coverage` not failure — collapses QA-22 SCOPE C Grand County pill from red to neutral the moment this ships). 10 Regrid adapter tests + 5 overlay GeoJSON tests + 4 eligibility tests updated; **238/238 adapter** + **341/341 design-tools** + workspace typecheck green. Three implementation decisions worth noting: (i) reused `tier: "federal"` rather than introducing a new `"national"` enum to keep diff local (operator-visible distinction lives at `source_kind`; FE can render distinct pill without backend change later); (ii) `partnerCity` gate applied to `grand-county-ut` only this PR (Lemhi ID and Bastrop TX per-county adapters not gated — Bastrop's `localKey === "bastrop-tx"` is implicit partner correlation; Lemhi deferred follow-on); (iii) trial-out-of-coverage detection via permissive `isTrialOutOfCoverage` heuristic (200 + empty features + error envelope with "trial"/"coverage"/"restricted"/"unauthorized") throws `AdapterRunError("no-coverage")` so UI surfaces neutral pill on the trial-token Utah/Idaho geos. Structured logging mirrors PR #96 `fccLogEvent` pattern (`regridLogEvent` with `adapter_key` stamping + token-redacted URL — no key leakage into Cloud Run logs). **PR #104 held for operator merge + redeploy + Redd retest.** Also surfaced: **PR #101 (cc-agent-C2 earlier session, Phase 2D.1.3 site-topography atom registration, shape-only) is open and gates Phase 2D.x resumption.** Filed at [`_sessions/2026-05-23_regrid_scope_b_cc-agent-C2.md`](_sessions/2026-05-23_regrid_scope_b_cc-agent-C2.md).
- 2026-05-23 cc-agent-C — **QA-22 SCOPE B FCC drop shipped as PR #102** (branch `cortex/qa22-fcc-drop`, off `origin/main` `4aa3d2a` in isolated worktree per workspace-hygiene memory). Surgical ~180 lines / 6 files: new `isFccEnabled(env)` in `lib/adapters/src/registry.ts` (strict literal `"true"` — typo-proof against `"1"`/`"yes"`/`"on"`); `FEDERAL_ADAPTERS` spreads `fccBroadbandAdapter` only when gate on at module-init; `fcc-broadband.ts` top-of-file docstring on Akamai WAF root cause + re-enable steps; `SiteContextTab.tsx` copy strings name FEMA + USGS + EPA only (with inline comments to re-add FCC clause if re-enabled). FCC pill goes silent without pill-render changes (no outcome → no pill, including no `no-coverage`). 234/234 adapter tests pass, full workspace typecheck green. E2E specs + api-server integration test left intact (test fakes are runner-independent). **Held for operator merge + redeploy + Redd retest** — expect 4 of 5 layers ok (FEMA + USGS + EPA + grand-county-ut:roads via OSM fallback; Grand County GIS parcels/zoning still fail until Regrid SCOPE B lands; FCC pill absent). Re-enable path: `FCC_ENABLED=true` env var on Cloud Run + re-add FCC clause in SiteContextTab copy (no code redeploy required). EPA Path 1a queued behind PR #102 merge for cc-agent-C. Filed at [`_sessions/2026-05-23_qa22_fcc_drop_cc-agent-C.md`](_sessions/2026-05-23_qa22_fcc_drop_cc-agent-C.md).
- 2026-05-23 operator + planner — **Regrid pick approved + SCOPE B integration fired on cc-agent-C2; FCC drop fired on cc-agent-C.** Operator decisions: (1) Regrid pick confirmed (both cc-agent-C + cc-agent-C2 recon convergent); (2) `REGRID_API_KEY` secret created in Cloud Run (`gcloud secrets create REGRID_API_KEY`) + IAM binding to cortex-api runtime SA `1062716564162-compute@developer.gserviceaccount.com` (`roles/secretmanager.secretAccessor`); (3) FCC fix path = **drop** (Akamai WAF makes the upstream architecturally unreachable, three operator-decision paths in the recon collapsed to drop); (4) smoke test on Musgrave/Redd lat/lng deferred (operator opted to pass for now and let SCOPE B exercise the trial token); (5) Utah test geo OK for now (operator noted plan upgrade available once integration lands — trial's 7-county restriction is acceptable since SCOPE B's error envelope treats out-of-coverage as `no-coverage` not hard error). Two dispatches fired off operator paste: **SCOPE B Regrid integration on cc-agent-C2** ([`_dispatches/2026-05-23_cc-agent-C2_regrid_scope_b_integration.md`](_dispatches/2026-05-23_cc-agent-C2_regrid_scope_b_integration.md)) — adapter at `lib/adapters/src/national/regrid.ts`, paired `regrid:parcels` + `regrid:zoning`, extend `overlays.ts` for GeoJSON Polygon, provenance `source_kind=national-aggregator`, runner wiring with `partner_city` flag, 6+ tests, `REGRID_API_KEY` env var. **FCC drop on cc-agent-C** — gate `fcc:broadband` behind `FCC_ENABLED=false` env flag in `lib/adapters/src/runner.ts`, remove from failed-layer pill list when disabled, doc-comment header on `lib/adapters/src/federal/fcc-broadband.ts` documenting Akamai WAF reason, test updates for env-gated behavior; branch `cortex/qa22-fcc-drop`. cc-agent-C2 had been on Phase 2D.1 PR 2 (paused for SCOPE A eval); SCOPE B preempts; PR 2 resumes after SCOPE B ships.
- 2026-05-23 cc-agent-C — Regrid SCOPE A vendor evaluation (recon-only, no code): converged with cc-agent-C2 on **Regrid pick**. Added two findings: (i) ATTOM is structurally wrong (radius lookup not point-in-polygon, no zoning/geometry fields, sales-gated pricing); (ii) Utah coverage caution — sandbox trial counties don't list UT (operator should verify with trial query on Musgrave/Redd). Also produced consumer-side contract field requirements (`PARCEL_ID_KEYS`, `PARCEL_ACRES_KEYS`, `ZONING_CODE_KEYS`, `ZONING_DESC_KEYS`). Filed at [`_sessions/2026-05-23_regrid_eval_scope_a_cc-agent-C.md`](_sessions/2026-05-23_regrid_eval_scope_a_cc-agent-C.md).
- 2026-05-23 cc-agent-C2 — Regrid SCOPE A vendor evaluation (recon-only, no code): **recommend Regrid** over ATTOM + skip CoreLogic per dispatch gate. 7 of 13 criteria wins; consumer-side contract audit found `overlays.ts` is the ONE structured consumer (briefing engine treats payload as opaque), so integration is one GeoJSON-Polygon branch in overlays.ts + ~1 day adapter. Pilot posture: 7-day trial → $500/mo Standard → $1,500-2,000/mo Premium. C2 paused Phase 2D.1 PR 2 for this eval (was operator-misdirected; PR 2 resumes after Regrid decision lands). Pending operator approval. Filed at [`_sessions/2026-05-23_regrid_eval_scope_a_cc-agent-C2.md`](_sessions/2026-05-23_regrid_eval_scope_a_cc-agent-C2.md).
- 2026-05-23 planner — doctrine scoping: Partnership-first commitment scoped (does not govern Cortex product-baseline data sourcing for architect-facing layers); decision record at [`_decisions/2026-05-23_partnership_first_scoping.md`](_decisions/2026-05-23_partnership_first_scoping.md); CLAUDE.md + 09_thesis + premortem-check skill all updated; Bastrop + substrate sprint + 70-band + 14_pricing + 73_partnerships + 46 all unchanged; immediate downstream is the Regrid evaluation + integration dispatch for Cortex baseline parcel/zoning source.
- 2026-05-23 cc-agent-C2 — Phase 2D.1 PR 1 of N (PR #98) — USGS 3DEP DEM raster client at `lib/site-context/src/server/usgs3dep.ts`, 22 unit tests, full workspace typecheck clean. Audit found `usgs:ned-elevation` is single-point EPQS (3DEP raster is net-new); `lookupParcel` is stub but `payload.parcel.geometry` exists via briefing-source payloads; atoms event-sourced via `getAtomRegistry()`; SiteMap takes flat overlays (no panel UI exists); GCS uuid-keyed via `ObjectStorageService`; **migrations at 0015 head and `materializable_elements.propertySet` JSON column carries DEM ref + contour GeoJSON natively — NO migration 0016 needed**. PR 2 defaults greenlit: parcel boundary via `payload.parcel.geometry` with bbox fallback; catchment proxy as parcel+500m for 2D.1; contour derivation via d3-contour + geotiff (Node-native, no Dockerfile change); persistence via `materializable_elements.source_kind='site-topography'`. Filed at [`_sessions/2026-05-23_2d1_pr1_usgs_3dep_client_cc-agent-C2.md`](_sessions/2026-05-23_2d1_pr1_usgs_3dep_client_cc-agent-C2.md).
- 2026-05-23 operator + planner — post-PR-#94 Redd reproduce still shows `fcc:broadband` failing with timeout pill; deploy status under triage (revision check + Cloud Run log query teed). cc-agent-C2 re-activated for parallel 2D-site-context Phase 2D.1 work (operator's earlier drop-C2 call reversed — QA-22 cleanup turned into multi-back-and-forth, parallelization pays off). C2 bootstrap: clone legacy-design-tools to `P:\legacy-design-tools-c2`, separate Cursor window. Bounded overlap with cc-agent-C confirmed (different file paths).
- 2026-05-23 operator — workstation recon for all three QA-22 scopes (verbatim DNS / curl / browser): confirms EPA NXDOMAIN globally + `geopub`/`gispub` resolve as possible migration targets; FCC SPA loads in browser but curl-on-homepage times out 60s (worth distinguishing API URL from homepage); Grand County `gis.` resolves and AGOL mirror works ~0.5s from workstation, operator-noted mixed Cloud Run failures include 503 + UND_ERR_CONNECT_TIMEOUT. PR #94 (FCC) + PR #95 (docs close-out) merged 2026-05-23; awaiting operator deploy + Redd re-test verify.
- 2026-05-23 cc-agent-C — QA-22 mitigation batch (3 scopes): **SCOPE B FCC shipped as PR #94** (90s timeout floor + 15-min in-memory cache; 227/227 adapter tests pass; held for operator merge + redeploy). **SCOPE A EPA**: operator decision required — `ejscreen.epa.gov` decommissioned, no successor found via WebFetch sweep across EPA/data.gov; four paths offered (wait/CDC-EJI/PEDP/remove). **SCOPE C Grand County GIS**: operator infra required — `gis.grandcountyutah.net` resolves but TCP firewall drops SYNs from GCP egress; VPC connector + Cloud NAT + whitelist outreach, or AGOL mirror swap fallback. Important nuance: operator's `webgis.` probe is a different host than the adapter's `gis.` host. Filed at [`_sessions/2026-05-23_qa22_upstream_probe_cc-agent-C.md`](_sessions/2026-05-23_qa22_upstream_probe_cc-agent-C.md).
- 2026-05-23 operator + planner — PR #92 deploy verify + QA-22 reopen reproduce on Redd: **three distinct failure modes confirmed** (EPA ENOTFOUND DNS / FCC abort-timeout / Grand County GIS UND_ERR_CONNECT_TIMEOUT TCP). `grand-county-ut:roads ok` is misleading — uses OSM Overpass fallback, not Grand County GIS. Mitigation dispatch teed for cc-agent-C with three scopes (EPA hostname / FCC timeout+cache / Grand County GIS egress-filtering recon → likely VPC connector + Cloud NAT + whitelist outreach).
- 2026-05-23 cc-agent-C — QA-22 reopen throw-path capture (PR #92 open, diagnostic-only): `FetchWithRetryOptions.captureThrowsAsResult` opt-in flag collapses final-attempt / non-transient `node:undici` throws into a synthetic 599 with `throwExcerpt` populated from `err.cause` (`<code> <syscall> <host|address:port>`); 3 adapter call sites wired (covers all 4 affected adapters); 7 new `retry.test.ts` cases pin DNS / TLS / ECONNREFUSED / abort-still-wins / bodyExcerpt-no-regress / legacy-preserved / no-cause-fallback. Typecheck green; 224/224 adapter tests pass. Operator merges → redeploys → reproduces; next session picks mitigation. Filed at [`_sessions/2026-05-23_qa22_throw_path_cc-agent-C.md`](_sessions/2026-05-23_qa22_throw_path_cc-agent-C.md).
- 2026-05-23 planner — Phase 2D.1 dispatch drafted; cc-agent-C2 parallelization considered then dropped per operator call (cc-agent-C nearly done with QA-22, not worth the parallel-agent overhead). Phase 2D.1 stays queued for cc-agent-C after QA-22 mitigation closes — sequential through 2D.1 → 2D.2 → 2D.3 → 2D.4 per [`40d_cortex_site_context_sprint.md`](40d_cortex_site_context_sprint.md).
- 2026-05-23 operator — QA-33 + QA-35 verified live on Musgrave_Residence_B (snapshot `60b6a5c7-...`): BIM tab shows `101 ELEMENTS / 1 element renderable` with building geometry visible in bounded viewport. Customer-zero BIM loop closed end-to-end (IFC ingest → bim-model GET → viewer-render). PR #90 + #91 merged + deployed. QA-22 reopen network-layer follow-on dispatched on cc-agent-C; Phase 2D.1 (DEM ingest + topo overlay) dispatch teed and ready to fire after QA-22 closes.
- 2026-05-23 cc-agent-C — QA-33 + QA-35 follow-on fix (PR #90 open, held for operator merge + redeploy + verify): combined two scopes into one PR per dispatch permission. **QA-33 fix**: `BimModelViewport.tsx` canvas wrapper gains `maxHeight: 60vh` cap — building rendered all along but the unbounded canvas plus QA-35-stacked element list pushed the geometry below the modal fold; defensive `console.warn` added for the opposite mount-at-<100px failure mode. `BimModelViewport.test.tsx` 59/59 green. **QA-35 fix**: `ifcIngest.ts` supersession UPDATE re-scoped from `source_snapshot_id` to `engagement_id + sourceKind IN ('as-built-ifc','as-built-ifc-bundle')` — root cause was that each re-upload creates a fresh snapshot row so the snapshot-scoped UPDATE matched zero prior rows; PR #33's partial unique index was correctly preventing same-snapshot duplicates but permitted cross-snapshot overlap. Pre-existing 303-row state at Musgrave: fix sweeps on next re-ingest, or operator can manually run the engagement-scoped UPDATE. New `ifc-ingest-bim-model-atom.test.ts` describe block pins the behavior. **Watch-line follow-up**: defense-in-depth partial-index re-key to `(engagement_id, ifc_global_id)` deferred to a follow-on migration. Filed at [`_sessions/2026-05-23_qa33_qa35_followon_cc-agent-C.md`](_sessions/2026-05-23_qa33_qa35_followon_cc-agent-C.md). QA-22 reopen + Phase 3 + 2D-site-context not touched.
- 2026-05-23 operator + planner — Cleanup batch deploy + diagnostic-data triage: PR #87 + PR #88 merged + deployed (`cortex-api-00020-85n`). PR #87 log payload confirmed parser/storage are fine (glbBytesLen=5,234,980, gltfObjectPath set); operator dev-tools triage refined QA-33 to two viewer-side bugs (unbounded container CSS + likely wire-shape path mismatch). New QA-35 filed (re-ingest accumulates as-built-ifc rows instead of superseding — 303 ELEMENTS on Musgrave after 3 uploads). QA-22 reopen body-capture didn't apply: failures are network-layer (DNS / TLS / ECONN), not body-bearing — follow-on needs throw-path `Error.cause` capture + likely VPC connector + Cloud NAT for stable egress IP. Two cc-agent-C follow-on dispatches teed (QA-33 + QA-35 paired; QA-22 separate).
- 2026-05-23 cc-agent-C — Cleanup batch QA-33 + QA-22 reopen, diagnostic-only PRs (operator merging now): **PR #87** adds two log lines in `lib/ifcIngest.ts` (warn on `glbBytes.length === 0`; final `ifc ingest: complete` info with `{snapshotId, ifcFileId, entityCount, glbBytesLen, gltfObjectPath, ifcVersion}`) to collapse the QA-33 three-arm diagnosis (a/b/c) to a single log-line read; no behavior/schema/test change. **PR #88** patches `lib/adapters/src/retry.ts` to capture up to 256 chars of the final-attempt response body and attach as `bodyExcerpt`; `arcgis.ts`/`epa-ejscreen.ts`/`fcc-broadband.ts` append `Upstream response: …` to the failure pill (covers all 4 affected adapters); 6 new `retry.test.ts` cases, 217 adapter tests pass. **Diagnostic finding worth surfacing**: PR #76 was UGRC-only and didn't touch the 4 failing adapters — they've been failing for a different reason since PR #63. **Operator-side note**: workstation `gcloud` token broken since QA-08 (SSL cert-verify failure on refresh), making the canonical "tail the logs" path unreachable from the workstation; PR #88 closes that gap by surfacing the body excerpt in the FE pill. Filed at [`_sessions/2026-05-23_qa33_qa22_cleanup_batch_cc-agent-C.md`](_sessions/2026-05-23_qa33_qa22_cleanup_batch_cc-agent-C.md). QA-34 not touched (operator-tabled). Phase 3 + 2D-site-context not touched (deferred / separate scope).
- 2026-05-23 planner — Cortex Site Context Sprint (40d) activated as 2D-first per operator call; canonical doc + decision record drafted, premortem cleared green. Cleanup-batch dispatch (QA-33 + QA-22 reopen) teed for cc-agent-C, operator firing now. 2D-site-context sprint sequenced behind it; Phase 3 features yield. Doc 42 watch line gains `cortex/site_context_*` MCP retrofit follow-on per dual-interface principle.
- 2026-05-23 operator — LLM env-flip executed on cortex-api: revision `cortex-api-00019-bxf` now serves 100% with `AIR_FINDING_LLM_MODE=live` + `BRIEFING_LLM_MODE=live`. Provider is Anthropic (`AI_INTEGRATIONS_ANTHROPIC_API_KEY` secret mounted). `DXF_CONVERTER_MODE` + `MNML_RENDER_MODE` correctly stay on `mock` per plan. The findings text on the Redd verify is real Claude output, not templated mock — QA bar "produces cited findings" structurally met.
- 2026-05-23 operator — QA-32 deploy verify + customer-zero pass on Musgrave_Residence_B (`977b5469`) and new engagement Redd (`8e2bac10`): **WINS** — BIM count shows `101 ELEMENTS`, geocoder updates correctly on address change (first time this works), findings generate with real cited code sections (`CODE-552D470B`), rendering UI is up (Kick-off render dialog with all the new fields), QA-32 fully closed. **NEW BUGS** filed as one cleanup batch: QA-33 (BIM viewport renders empty despite 101 elements badge), QA-34 (findings flow gated on Submit-to-jurisdiction; jurisdiction should auto-resolve from address), QA-22 reopened (4 of 5 Grand County / federal layers still failing after PR #76's 45s floor — circuit-breaker pattern needed). **STRATEGIC PIVOT**: operator reframed P0-3 as 2D-site-context-first; 3D becomes a follow-on visual layer (see watch list). **CLARIFICATION PENDING**: whether `AIR_FINDING_LLM_MODE` / `BRIEFING_LLM_MODE` were flipped to `live` before this verify (text structure could be either live LLM or templated mock with real citation lookups).
- 2026-05-23 cc-agent-C — QA-32 IFC-without-briefing BIM viewer surface (PR #85 open, held for operator merge + redeploy): `lib/ifcIngest.ts` stamps `materialized_at` on INSERT and ON CONFLICT (preserves to-be-built columns); `routes/bimModels.ts` unions active as-built rows independent of `active_briefing_id`; subtle race-window bug on `parsed_at IS NOT NULL` gate also removed. New tests in `ifc-ingest-bim-model-atom.test.ts` + `bim-models.test.ts`; per-package typecheck green. Filed at [`_sessions/2026-05-23_qa32_bim_model_without_briefing_cc-agent-C.md`](_sessions/2026-05-23_qa32_bim_model_without_briefing_cc-agent-C.md).
- 2026-05-22 planner — Cortex QA run planning: synthesized the third operator QA pass, ran the MCP-servicing check (the app does not consume the MCP server, by design), reconciled the fleet (retired the invented cc-agent-D name; cc-agent-AC dormant), premortem green, produced the consolidated cc-agent-C QA build dispatch and reversed CDX-Phase1-1.
- 2026-05-22 doc_repo planner (rendering session) — Cortex rendering sprint scoped and activated: [`40c_cortex_rendering_sprint.md`](40c_cortex_rendering_sprint.md), cc-agent-R dispatched to build the mnml.ai render engine; doc 41 renumbered (40b advanced capture, 40c rendering, 41 revit-connector).
- 2026-05-22 cc-agent-C — QA build Phase 2 complete, held for operator before Phase 3: PR #81 — `cloud-run-deploy.yml` `action` input + `run-migrations` workflow job + `lib/db/scripts/migrate-prod.mjs` numbered-SQL runner with a `_schema_migrations` tracker. First `run-migrations` needs `bootstrap=true` (cortex-prod is at head). Workspace-conflict incident this session — see watch list.
- 2026-05-22 cc-agent-E — Sync 5 Tier 2 ingest progress: New Braunfels (PR #28, 190 atoms) and Killeen (PR #29, 637 atoms) shipped, both eval 1.0/1.0/1.0; queue reclassified from 16 to 12 Path C cities (San Marcos and Temple found LDC-off-Municode, now partnership / Path-PDF). Austin LDC next. Tier 2 corpus-quality follow-up flagged (~zero in-corpus xrefs).
- 2026-05-22 cc-agent-R — workspace-hygiene incident: HEAD silently switched to `feat/p2-deploy-workflow-actions` mid-session and the dialog-fix commit captured operator P2 files; recovery clean on cc-agent-R side (dialog fix re-applied as `8c0f064` for PR #80). Origin/contaminated-branch untangle owed by operator (cc-agent-R recommends rebase after #80 merges).
- 2026-05-22 cc-agent-C — QA build Phase 1 complete, held for operator: geocoding / blank map / site-context render / adapter timeouts fixed (#74/#75/#76), the IFC `0015` migration written (#73), the review-run 403 fixed and the review relocated into the design-tools Findings tab (#77); P0-3 (3D site + drainage) flagged as a missing capability. PRs #73-77 open.
- 2026-05-22 cc-agent-E — Sync 5 Tier 2 central-TX corridor batch (8 PRs #30-37): Austin LDC anchor (2211 atoms) + Copperas Cove, Manor, Lockhart, Lago Vista, Dripping Springs, Wimberley, Rollingwood, all eval 1.0/1.0/1.0, ~4667 atoms total. 4 cities adapter-limited (Luling, Woodcreek, Belton, Creedmoor) flagged for `MunicodeHtmlAdapter.tocRootNodeIds` follow-up. Central-TX accessible-Path-C catalog approaching ~8000 atoms across 14 cities.
- 2026-05-22 cc-agent-R — rendering Phase B built and opened (PR #80): credit-balance badge, deliverable / concept intent toggle, expert + style selects, Prompt Generator affordance. PR #79 (Phase A) merged as `b8b0cdf`. PR #80 self-merges on CI green. B.1 (full schema-driven param grid) and B.2 (manual upload as render source) deferred follow-ons.
- 2026-05-22 cc-agent-R — rendering Phase A complete (gap-fill): PR #79 — `GET /credits` and Prompt Generator added to the mnml client + api-server, additive behind `RENDERS_PROD_ENABLED`, no DB migration.
- 2026-05-22 cc-agent-R — rendering Phase A.1 audit: BLOCKED — the mnml.ai render feature is already built and merged to `main` (prior V1 / Wave 2 sprint); 40c is desynced from the codebase. Operator decision A/B/C pending; no code written.
- 2026-05-22 cc-agent-E — Sync 5 Tier 1 close: Pflugerville and Cedar Park routed to the General Code partnership track (both eCode360 / EncodePlus access-blocked); Tier 1 complete, 6 of 6.
- 2026-05-22 cc-agent-E — Sync 5 Tier 1 Georgetown UDC shipped (PR #27, open): Path C Municode ingest, 658 code-section atoms, eval 1.0/1.0/1.0; Tier 1 at 4 of 6. New Path C capability — multi-product Municode clientId selection. Next: Pflugerville / Cedar Park discovery.
- 2026-05-22 cc-agent-E — ICC prebuild extractor + eval rubric: the model-code structural extractor (PR #25) and the Layer 1 eval rubric (PR #26) built and opened, CI green; the ICC Code Connect prebuild dispatch is complete across all four deliverables. Next: Sync 5 Tier 1 Georgetown.
- 2026-05-22 cc-agent-E — ICC Code Connect pre-credential build-out: the OAuth2 adapter shipped (PR #24, CI green); model-code extractor + Layer 1 eval rubric scoped; corpus-edition plan resolved (3-wave, rolled into ADR-019); 16-item live-API confirmation list produced for the operator's ICC meeting.
- 2026-05-22 planner — CDX-QA-1 delivered: [`45_codex_qa_scenarios.md`](45_codex_qa_scenarios.md), the Codex 1b QA scenario set (4 scenarios on the 8-point QA-readiness definition); also drafted the cc-agent-AC shift-traffic dispatch and ran the `42` Lane-C reconciliation.
- 2026-05-22 cc-agent-C — Codex Phase 2 CDX-9 comment-letter auto-draft (PR #72, open): "Draft comment letter" on codex-reviewer-qa, reuses the L3/L6 deliverable-letter pipeline; planner ruled finding-intrinsic adjudication satisfies the provenance commitment (no Codex adjudication-state atom).
- 2026-05-22 cc-agent-C — Codex Phase 2 CDX-5 jurisdiction switcher (PR #71): engagement/submission switcher per the 2026-05-21 ruling, snapshot-divergence warning; completes the reviewer-surfaces dispatch (CDX-3/4/5). codex-reviewer-qa tests 54/54.
- 2026-05-22 cc-agent-C — Codex Phase 2 CDX-4 per-finding accept/edit/reject loop merged (PR #70): adjudication action row on every finding, server-stamped attribution; codex-reviewer-qa tests 29/29.
- 2026-05-21 cc-agent-C — Codex Phase 2 CDX-3 one-click AI review pass merged (PR #69): `codex-reviewer-qa` first data-bound page; two L-surface divergences ruled (reasoning chain = finding text; CDX-5 = engagement switcher).
- 2026-05-21 cc-agent-E — Sync 5 Tier 1 Leander ingest merged (PR #23, 185 atoms, eval 1.0/1.0/1.0) + bare-numbered-section entityId disambiguation (PR #22); two general Municode Path C reliability fixes folded in.
- 2026-05-21 cc-agent-C — legacy-design-tools cleanup batch merged (PR #68): QA-15 plan-review header bell (14 call sites), QA-26 root `.gitattributes`, flaky `lib/codes` CI fixed (30s testTimeout on 3 files).
- 2026-05-21 cc-agent-AC — `@workspace/empressa-atom` retirement merged (PR #67): migrated the final two consumers, removed the staging package (34 files, 3343 deletions); completes the ADR-018 transition for legacy-design-tools.
- 2026-05-21 cc-agent-C — codex-reviewer-qa scaffold merged (PR #66): a sixth `legacy-design-tools` artifact, Vite/React SPA shell; resolves the build half of CDX-Phase1-1.
- 2026-05-21 cc-agent-AC — api-server import migration `@workspace/empressa-atom` → `@hauska/atom-contract` merged (PR #65); drift check clean, 49 files, api-server only.
- 2026-05-21 cc-agent-AC — QA-17 merged (PR #64): Code Library wired to the Hauska substrate via the MCP server; chose MCP over the retrieval API for the ADR-017 accessPolicy gate.
- 2026-05-21 cc-agent-C — QA-22 Part 1 merged (PR #63): per-adapter timeout floors for slow site-context upstreams; the runner already isolated per-adapter.
- 2026-05-21 planner — roadmap catch-up refresh + dispatch-queue teeing; QA-18 PR-number correction; M-Stabilize hold; QA-17 no-clone blocker resolved.
- 2026-05-21 cc-agent-C — Cortex QA close-out QA-16/19/23/18 merged as #59-62.

## 6. Cross-cutting watch list

- **Grok + atom-first migration — COMPLETE.** See top-level §Grok + Atom-First Fleet Migration above and [`21d_grok_atom_migration_complete.md`](21d_grok_atom_migration_complete.md). ECI `@empressaio/atom-internal` registry when that sprint fires.
- **QA-04 IFC ingest — still open after deploy.** cortex-api-00017-gex (carrying #57/#58/#59) deployed and shifted to 100% traffic 2026-05-22; the operator QA pass found IFC upload still 500s. Verified root cause from the prod logs: the production DB is missing `materializable_elements.superseded_at` (PR #33's column) — a schema-drift gap, not a code bug. It is P0-1 of the cc-agent-C QA build; the `0015` catch-up migration shipped as PR #73 (merged 2026-05-22) and was **applied to cortex-prod 2026-05-22** via direct `psql` in Cloud Shell — the SQL is idempotent (`IF NOT EXISTS` / drop-then-add) and ran clean (BEGIN → COMMIT). A real IFC push on the next deployed revision should 201.
- **Migration 0016 collision — fix in PR #112 (2026-05-24).** cc-agent-C2 validation dispatch #3 renumbered `0016_add_site_topography_source_kind.sql` → `0017_add_site_topography_source_kind.sql`. `0016_renders_power_tools_source_type.sql` (40e) keeps the 0016 slot. Operator: merge PR #112; if tracker already has old topography filename, run UPDATE in migration header before `run-migrations`.
- **Replit Agent UI overhaul — being scoped 2026-05-23.** Operator-flagged intent to bring Replit Agent in for a larger Cortex UI overhaul (Replit's frontend quality is the value-add; Replit-as-publisher posture from earlier in the year is unchanged). legacy-design-tools repo access already in place per operator. **Open scoping questions surfaced to operator** (no commitment yet): overhaul scope (which surfaces — RendersTab, EngagementDetail, SiteContextTab, the whole `portal-ui` package, brand-new surfaces?); coordination story (Replit Agent + Cursor cc-agent-R successor both writing UI; bounded-overlap discipline + branch convention to avoid the 2026-05-22 workspace-conflict pattern); sequencing (parallel with 40e activation vs after 40e ships + live-QAs). Workspace-hygiene memory + per-agent branch prefixes + the doc-repo-concurrent-commit hazard apply. Net-new agent in the fleet; naming + clone path TBD (consistency: `cc-agent-UI`? operator's call). Not yet dispatched.
- **40f Cortex Grok runtime migration planning — QUEUED for cc-agent-R (2026-05-24).** Product-runtime LLM migration (Anthropic Sonnet → xAI Grok at api-server call sites), **not** agent fleet (HR-12 complete). Planning sprint only — deliverable `44b_cortex_grok_migration.md`. **Do not fire until PR #110 merged + operator greenlight.** Sprint: [`40f_cortex_grok_runtime_migration_sprint.md`](40f_cortex_grok_runtime_migration_sprint.md). Dispatch: [`_dispatches/2026-05-24_cc-agent-R_cortex_grok_runtime_migration_QUEUED.md`](_dispatches/2026-05-24_cc-agent-R_cortex_grok_runtime_migration_QUEUED.md). Inventory: [`_research/2026-05-23_cortex_ai_model_inventory.md`](_research/2026-05-23_cortex_ai_model_inventory.md). Sequence: (1) Apply migration 0016 (cc-agent-R PR #106, included in PR #109 bulk-merge) to cortex-prod via `run-migrations` workflow or Cloud Shell `psql` per [`90_runbooks/cloud_run_canary_deploy.md`](90_runbooks/cloud_run_canary_deploy.md); (2) Verify fixture against pg_dump via `pnpm --filter @workspace/db run test:fixture:schema` against test/dev DB; (3) ~~Fix PR #110 CI~~ **DONE 2026-05-23** (cc-agent-R validation dispatch #1, CI green); (4) Merge PR #110; (5) Cursor agent successor finishes any remaining cleanup (untracked `lib/api-spec/scripts/` debug files); (6) Flip `RENDERS_PROD_ENABLED=true` + `MNML_RENDER_MODE=live` in the **same** Cloud Run revision (never one without the other); (7) Verify `PRIVATE_OBJECT_DIR` / object-storage config in prod (upload-as-source + mirror depend on it); (8) Live QA on deployed Architect Renders tab — inline kickoff (still + upload source), gallery polling + cancel, all 5 power tools from ready still, gallery tree expand/collapse for tool outputs, before/after slider on upload-source stills. Sprint doc `40e_cortex_rendering_parity_sprint.md` itself still labels "Just-dispatched" and needs a status section update (rendering-planner territory).
- **cortex-api deploy does not run DB migrations** (surfaced 2026-05-22 via the IFC-ingest 500). `cloud-run-deploy.yml` ships code only; every schema-touching PR since the cutover has drifted the prod DB behind the code. The IFC 500 is the first surfacing. Phase 1 P0-1 closed the immediate gap with the `0015` manual apply (operator, 2026-05-22, direct `psql`); Phase 2 P2-2 added (PR #81 merged) a `run-migrations` workflow job + `migrate-prod.mjs` tracker — future migrations apply via `gh workflow run ... -f action=run-migrations` once `bootstrap=true` seeds the tracker (deferred until the workflow form is actually wanted; the direct `psql` form remains the alternative).
- **Cortex QA build operator gate sequence** (2026-05-22, narrowed further). All Phase 1 + Phase 2 PRs merged on `legacy-design-tools` `main` (#73-77 Phase 1, #79/#80 rendering gap-fill, #81 Phase 2 deploy workflow). **0015 migration applied 2026-05-22** via direct `psql` in Cloud Shell. Remaining operator actions: (1) deploy the new `cortex-api:latest` image — `gcloud run deploy cortex-api --image us-central1-docker.pkg.dev/legacy-design-tools-prod/apps/cortex-api:latest --region us-central1` then `gcloud run services update-traffic cortex-api --to-latest --region us-central1` (the `--to-latest` is the 2026-05-11 runbook addendum's traffic-routing discipline — `latestReadyRevisionName` doesn't always serve when tags are pinned); (2) verify the customer-zero loop on the Musgrave engagement against the live URL — IFC push, site context, compliance review; (3) decide whether real keys go on cortex-api (`AIR_FINDING_LLM_MODE` / `BRIEFING_LLM_MODE` / `DXF_CONVERTER_MODE` / `MNML_RENDER_MODE` currently `mock`, so review findings are mock-generated). **Deploy completed 2026-05-22** (direct `gcloud run deploy` + `update-traffic --to-latest --clear-tags`); new revision confirmed live by changed UI. **Two post-deploy bugs surfaced on the deployed app, root-caused 2026-05-22 to the same audience-gate class** (PR #77 relaxed it for `findings.ts` only; the same gate is still active on the BIM-model GET and the renders routes). [`43_cortex_qa_backlog.md`](43_cortex_qa_backlog.md) WS-G follow-ons: **QA-30** renders routes 403 `renders_requires_architect_audience`, **QA-31** 3D model stuck at "Loading BIM model..." because `GET /api/engagements/:id/bim-model` returns 403 with the same gate (Cloud Run logs confirm; the IFC POST itself succeeded). **PR #83 merged 2026-05-22 02:12Z** (mergeCommit `5e3f0c04`) and redeployed as `cortex-api-00017-jnn` at 100%. Audience gate closed across 16 routes (the customer-zero loop UI surface); 9 sibling routes kept gated (reviewer-only by design). **Customer-zero BIM loop closed 2026-05-23.** PR #90 + #91 merged + deployed; operator re-uploaded Musgrave IFC (snapshot `60b6a5c7-d689-4372-9d4d-04ce48fe1940`) and verified live — BIM tab shows `101 ELEMENTS / 1 element renderable` with the building geometry visible inside the bounded modal viewport. **QA-32 + QA-33 + QA-35 all closed.** The supersession UPDATE swept the pre-existing 303-row state down to the correct 101 active; the 60vh maxHeight cap put the model back in the modal fold. End-to-end IFC ingest → bim-model GET → viewer-render path now works on a real engagement. **PR #92 reproduce results in 2026-05-23 — three distinct failure modes confirmed.** EPA: `ENOTFOUND getaddrinfo ejscreen.epa.gov` (DNS — likely stale hostname); FCC: timeout (caller-abort, 45s floor not enough); Grand County GIS parcels+zoning: `UND_ERR_CONNECT_TIMEOUT` (TCP connect timeout, classic GCP egress-IP filtering pattern). Important nuance: `grand-county-ut:roads ok` because that adapter uses OSM Overpass fallback, not Grand County GIS — Grand County GIS is fully unreachable from Cloud Run. **PR #94 (FCC) + PR #95 (docs) merged 2026-05-23.** Operator workstation recon (DNS / curl / browser, verbatim) confirmed all three failure-mode calls + sharpened: EPA `ejscreen.epa.gov` NXDOMAIN globally (not Cloud Run-specific); `geopub` + `gispub` resolve at EPA but no published EJScreen replacement found at root. FCC homepage SPA loads in browser but curl on homepage timed out 60s — operator's call: the SPA homepage isn't the adapter's API URL; PR #94 timeout-and-cache fix should land once deployed. Grand County `webgis.` was the wrong probe (NXDOMAIN); adapter target `gis.grandcountyutah.net` is fine; AGOL mirror `grandcountyutah.maps.arcgis.com` loads ~0.5s from workstation. Operator notes mixed failure modes from Cloud Run (some 503, some `UND_ERR_CONNECT_TIMEOUT`) — fits ArcGIS throttling + egress-IP filtering. **Pending operator deploy of PR #94 :latest** to verify FCC fix on Redd re-test. EPA + Grand County pills unchanged until SCOPE A + SCOPE C operator decisions land. **Mitigation batch ran 2026-05-23 — three different verdicts.** **SCOPE B (FCC) shipped as PR #94**: per-adapter 90s timeout + 15-min in-memory cache; held for operator merge + redeploy. **SCOPE A (EPA) — operator decision required**: cc-agent-C WebFetched the EPA + data.gov endpoints, `ejscreen.epa.gov` fully decommissioned with no published successor API. Four paths: (1) wait for EPA replacement; (2) switch to CDC EJI (non-trivial); (3) PEDP mirror (operator-gated); (4) remove adapter (permanent no-coverage). **SCOPE C (Grand County GIS) — operator infra, not code**: adapter at `gis.grandcountyutah.net` (NOT `webgis.` which operator probed and is NXDOMAIN); TCP-connect-timeout semantics indicate DNS resolves but firewall drops SYNs from GCP egress — classic IP-allowlist pattern. Operator-side fix: serverless VPC connector + Cloud NAT with stable egress IP + Grand County GIS whitelist outreach. Fallback: switch adapter to `grandcountyutah.maps.arcgis.com` AGOL mirror (works from workstation; non-trivial UUID-based service ID translation). **Phase 2D.1 (DEM ingest + topo overlay)** stays queued for cc-agent-C after QA-22 mitigation closes — operator dropped the cc-agent-C2 parallelization 2026-05-23 since cc-agent-C is nearly done with QA-22. **Cleanup batch — diagnostic PRs merged + deployed 2026-05-23 (cortex-api-00020-85n).** Both diagnostic PRs landed and produced live findings: **PR #87** (QA-33) `ifc ingest: complete` log entries on the deployed revision confirmed `glbBytesLen=5,234,980` (≈5.0 MB) + `gltfObjectPath="/objects/uploads/019c3c07-..."` — parser is fine, storage is fine, bundle row has the path. Refined diagnosis: **one viewer-side bug**, not a backend issue. (1) BIM model viewport container is unbounded ("scrolls down the page indefinitely" per operator) — Three.js canvas can't draw to a usable area, primary cause of empty viewport. Operator-paste of GLB request URL (`https://cortex-api-tds7av26va-uc.a.run.app/api/materializable-elements/8bc9d66c-.../glb`) confirmed wire shape is fine — `/api/materializable-elements/:id/glb` is the backend route that looks up the stored `gltfObjectPath` and serves the GCS object; FE makes the request correctly; Three.js receives bytes (200 from cache, 3 requests fire). **New QA-35 filed**: re-ingest accumulates as-built-ifc rows instead of superseding — 3 uploads of the same IFC produced 303 ELEMENTS (3×101) when only 101 should remain; PR #33's `materializable_elements_active_ifc_identity_uniq` partial index is either not stable across re-ingests or the supersede UPDATE doesn't fire. QA-33 + QA-35 pair into one cc-agent-C follow-on dispatch. **PR #88** (QA-22 reopen): body-capture doesn't apply to the four failing adapters — they fail at the network layer ("fetch failed" / "did not respond in time" = DNS / TLS / ECONN / abort), so there's no upstream response to capture. Likely root cause: Cloud Run egress IP being blocked/rate-limited by upstream (EPA, FCC, Grand County GIS may filter GCP NAT ranges) or TLS handshake mismatch. **Follow-on fix scope for QA-22**: capture `Error.cause` + `Error.code` on the throw-path in `fetchWithRetry`; then likely mitigation is serverless VPC connector + Cloud NAT with stable IP + request upstream whitelisting. Separate cc-agent-C dispatch from QA-33 + QA-35. **Important finding from PR #88 diagnosis**: PR #76 was UGRC-only and never touched the four failing adapters — they've been failing for a different reason since PR #63. **QA-32 closed 2026-05-23.** PR #85 merged + cortex-api redeployed; operator-verified the BIM tab on `cortex-api-tds7av26va-uc.a.run.app/engagements/977b5469-.../3d-model` shows the **`101 ELEMENTS`** badge. The two halves of the option-(a) fix shipped: `lib/ifcIngest.ts` UPSERT sets `materialized_at` on INSERT and ON CONFLICT (preserves the to-be-built columns); `routes/bimModels.ts` `loadActiveAsBuiltIfcElementsForEngagement` is engagement-scoped (`superseded_at IS NULL`, source_kind IN as-built-ifc / as-built-ifc-bundle, bundle-first) and `toBimModelWire` unions independent of `active_briefing_id`; subtle `parsed_at` race-window bug on `snapshot_ifc_files` also removed. **Three next-layer bugs surfaced from the same operator verify (2026-05-23) and filed as one cleanup batch**: QA-33 (BIM viewport renders nothing despite 101 elements — the count + materialized indicator are correct, geometry isn't reaching the Three.js viewport), QA-34 (findings flow requires `Submit to jurisdiction` before pre-submittal review + jurisdiction should auto-resolve from address), QA-22 reopened (four of five Grand County / federal layers still fail after 3 retries on a fresh engagement — not a timeout-budget bug anymore). All three folded into one cc-agent-C dispatch on operator greenlight. **Phase 3 (QA-27/28/29 features) is now unblocked**, but operator has framed a strategic 2D-first pivot ahead of dispatching it — see watch list. Optional / future: bootstrap `_schema_migrations` via `gh workflow run "Cloud Run Deploy (cortex-api)" --repo empressaioemail-tech/legacy-design-tools -f action=run-migrations -f bootstrap=true` to unlock the workflow-form deploys per [`90_runbooks/cloud_run_canary_deploy.md`](90_runbooks/cloud_run_canary_deploy.md).
- **Cortex prop-intel architecture — Regrid SCOPE B shipped as PR #104 2026-05-23, held for operator merge.** PR #104 (cc-agent-C2, branch `cortex/regrid-baseline`) lands the paired `regrid:parcels` + `regrid:zoning` adapters, GeoJSON branch in `overlays.ts`, `source_kind="national-aggregator"` provenance value (no schema migration — `briefing_sources.source_kind` is `text NOT NULL`), `grand-county-ut.ts` `appliesTo` gates on `ctx.jurisdiction.partnerCity === true` (collapses QA-22 SCOPE C in-PR — Grand County pill goes from red to neutral the moment PR #104 ships). 10 Regrid adapter tests + 5 overlay GeoJSON tests + 4 eligibility tests updated; 238/238 adapter + 341/341 design-tools + workspace typecheck green. **Operator action**: merge PR #104 → redeploy cortex-api → exercise Generate Layers on Redd/Musgrave. Trial-token Utah lat/lng surfaces Regrid as `no-coverage` neutral pill (clean, not red); paid-plan upgrade flips to `ok · Regrid (via Grand County)` with parcel polygon + zoning shape rendered. **Three follow-ons recorded**: (i) uniform `partnerCity` gate across Lemhi ID and other per-county adapters (deferred until operator surfaces a real case); (ii) real `"national"` tier UI in `SiteMap.tsx` `TIER_STYLES` + `TIER_LABELS` + union (one-file change); (iii) paid-plan smoke test (operator-side; cannot be done from PR until secret upgraded). Prior recon + SCOPE A record retained below for history.
- **Cortex prop-intel architecture — Regrid SCOPE A recon complete 2026-05-23 (both cc-agent-C + cc-agent-C2 converged on Regrid).** cc-agent-C added two findings beyond C2's recon: (i) **ATTOM is structurally wrong** for site-context — radius-based lookup (default 5mi, max 20mi), no parcel geometry / zoning code in response schema, property-characteristics-centric (beds/baths/sqft/sales — wrong domain), sales-gated pricing (no self-serve, contact form + phone); (ii) **Utah coverage caution** — Regrid sandbox page's trial-county list does NOT include UT (plausible-but-unverified per C; C2 cites full state coverage per Regrid's UT data store page — operator should verify with trial query on actual Musgrave/Redd lat/lng). cc-agent-C also produced the consumer-side contract details: `payload.parcel.attributes` requires one of `PARCEL_ID_KEYS` + one of `PARCEL_ACRES_KEYS`; `payload.zoning.attributes` requires one of `ZONING_CODE_KEYS` + one of `ZONING_DESC_KEYS` (`_payloadSummaryHelpers.ts:88-107`). SCOPE B can either map Regrid native names (`parcelnumb`/`gisacre`/`zoning`/`zoning_description`) to existing pilot-county aliases or extend the key-alias arrays — both work. **Two independent recons, same conclusion: high confidence Regrid pick.** Filed at [`_sessions/2026-05-23_regrid_eval_scope_a_cc-agent-C.md`](_sessions/2026-05-23_regrid_eval_scope_a_cc-agent-C.md) (in addition to the cc-agent-C2 report). - **Cortex prop-intel architecture — Regrid SCOPE A recon complete 2026-05-23.** cc-agent-C2 ran the vendor evaluation (recon-only, no code) — recommendation: **Regrid** (clear winner on 7 of 13 criteria; tied on 4; ATTOM only ahead on entry pricing). Key Regrid edges: all 29 UT counties covered (full state), 2,500+ counties with standardized zoning_type/subtype, rolling monthly refresh (vs ATTOM quarterly), rural-county strength (Moab + similar), GeoJSON-native ~1 day adapter integration, strong provenance (`ll_last_refresh` + `sourceurl`). Pricing posture: 7-day trial → ~$500/mo Standard schema for prod deploy → ~$1,500-2,000/mo Premium when standardized-zoning fields referenced. **Pending operator approval**: confirm Regrid pick + sign up for 7-day trial + verify Musgrave + Redd parcels return clean polygon + zoning_type + ll_last_refresh. **On approval**: fire SCOPE B (adapter build) — `lib/adapters/src/national/regrid.ts`, extend `overlays.ts` for GeoJSON Polygon (cleaner than ArcGIS-rings conversion), `regrid:parcels` + `regrid:zoning` adapter pair, source_kind=`national-aggregator`, per-county adapters gate behind `partner_city` flag (Bastrop true; Grand County deprecates as baseline). Important coordination: **cc-agent-C2 paused Phase 2D.1 PR 2 for this eval** — PR 2 reads `payload.parcel.geometry`, sequencing Regrid first means PR 2 reads from national baseline without per-jurisdiction fallback (simpler). Filed at [`_sessions/2026-05-23_regrid_eval_scope_a_cc-agent-C2.md`](_sessions/2026-05-23_regrid_eval_scope_a_cc-agent-C2.md). **Cortex prop-intel architecture — Partnership-first scoping settled 2026-05-23.** Decision record at [`_decisions/2026-05-23_partnership_first_scoping.md`](_decisions/2026-05-23_partnership_first_scoping.md). Partnership-first commitment now explicitly scoped to city operational data + Hauska substrate ingest (Bastrop UDC, code corpus, permit history, plan review precedent, SmartCity OS data, @hauska/atom-contract catalog atoms). Does NOT govern Cortex product-baseline data sourcing for architect-facing layers — national public-records aggregators (Regrid for parcels + zoning) and federal national APIs (FEMA, USGS, USDA, USFWS, FCC) are out of scope. **CLAUDE.md commitment #2** + [`09_post_saas_substrate_thesis.md`](09_post_saas_substrate_thesis.md) Partnership-first paragraph + [`.claude/skills/premortem-check/SKILL.md`](.claude/skills/premortem-check/SKILL.md) commitment #2 all updated with the scoping clarifier. Bastrop relationship + substrate sprint + 70-band bizops + 14_pricing_framework revenue share + 73_partnerships pipeline + 46_smartcity_parcel_intelligence framing all unchanged. **Immediate downstream**: Regrid evaluation + integration dispatch teed for Cortex baseline parcel/zoning source; Grand County operator-infra work (VPC + Cloud NAT + whitelist outreach) goes from required to optional; per-county GIS adapters become opportunistic enrichment for partner cities only. QA-22 SCOPE C cache-last-good dispatch retained as enrichment-source resilience pattern.
- **QA-22 SCOPE B — FCC drop shipped as PR #102 2026-05-23, awaiting operator merge.** Operator chose **drop** (over identify-current-endpoint or BDC-bulk-CSV) per post-recon Akamai WAF finding. PR #102 (cc-agent-C, branch `cortex/qa22-fcc-drop`, off `origin/main` `4aa3d2a` in isolated worktree) gates the adapter at `FEDERAL_ADAPTERS` module-init via new `isFccEnabled()` helper in `lib/adapters/src/registry.ts` (strict literal `"true"` only — typo-proof against `"1"`/`"yes"`/`"on"`). Pill goes silent without pill-render changes (no outcome → no pill, including no `no-coverage`). `SiteContextTab.tsx` copy strings name FEMA + USGS + EPA only. PR #94's 90s timeout + 15-min cache + PR #96's structured logging stay landed (architecturally correct for whatever next-gen broadband source replaces it). **Operator action**: merge PR #102 → redeploy → Redd retest → expect 4 of 5 layers ok (FEMA + USGS + EPA + grand-county-ut:roads via OSM fallback); Grand County GIS parcels/zoning still fail until SCOPE B Regrid lands; FCC pill absent.
- **QA-22 SCOPE A — EPA Path 1a (targeted EJScreen v2 dig) greenlit 2026-05-23, queued behind FCC drop.** cc-agent-C will WebFetch + read `lib/adapters/src/federal/epa-ejscreen.ts` for the exact REST path the old broker called, then sweep subfolders under `geopub.epa.gov/arcgis/rest/services/*` + `gispub.epa.gov/arcgis/rest/services/*` looking for any service emitting EJ indicators. Sequenced behind FCC drop ship. If hit → URL+schema swap PR follows. If miss → confidently "wait" with documented dead-end trail.
- **QA-22 SCOPE C — Grand County Path A retired in favor of SCOPE B Regrid 2026-05-23.** Per partnership-first scoping decision + Regrid pick approval: VPC connector + Cloud NAT + whitelist-outreach work dropped from required to retired. Once SCOPE B Regrid lands, `grand-county-ut:parcels` + `grand-county-ut:zoning` deprecate as baseline (gated off via `partner_city` flag; Grand County currently `partner_city=false`). Per-county adapters become opportunistic enrichment for partner cities only (Bastrop track). The cache-last-good fallback for `runner.ts` remains a useful resilience pattern for any per-county / federal adapter that keeps firing as enrichment — held as low-priority follow-on, not blocking.
- **Defense-in-depth partial-index re-key** (surfaced 2026-05-23 in cc-agent-C PR #90). PR #33's `materializable_elements_active_ifc_identity_uniq` partial index is keyed `(source_snapshot_id, ifc_global_id) WHERE supersededAt IS NULL`; it correctly prevents same-snapshot duplicates but permitted the cross-snapshot QA-35 bug. cc-agent-C scoped the supersession fix to the smallest reversible change (engagement-scoped UPDATE in `ifcIngest.ts`) and explicitly deferred re-keying the index to `(engagement_id, ifc_global_id)` as defense-in-depth. Low-priority follow-on migration; not blocking. Worth a tracked line so it doesn't get lost.
- **Workstation `gcloud` token broken since QA-08** (surfaced 2026-05-23 cc-agent-C session). SSL cert-verify failure on `gcloud auth` refresh; the canonical "tail the Cloud Run logs from the workstation" path is currently unreachable, forcing operator triage through Cloud Console UI or surfacing data into the FE (the path PR #88 takes for QA-22 reopen). Operator-side fix needed: re-bootstrap the workstation gcloud cert chain or switch the workstation to an `ADC`-based auth flow. Low priority but adds friction to every triage round.
- **PR #76 was a non-event for the 4 failing site-context adapters** (surfaced 2026-05-23 in PR #88 diagnosis). PR #76 only added the per-adapter floor to UGRC; the 4 failing adapters (`epa:ejscreen`, `fcc:broadband`, `grand-county-ut:parcels`, `grand-county-ut:zoning`) already carried it since PR #63. The 30→45s bump only gave them more headroom for free. They've been failing for a different reason since PR #63 — diagnostic captured in PR #88, real fix branch picked next session.
- **Cortex Site Context Sprint (40d) — activated 2026-05-23, 2D-first.** Decision record at [`_decisions/2026-05-23_site_context_2d_first.md`](_decisions/2026-05-23_site_context_2d_first.md), canonical sprint doc at [`40d_cortex_site_context_sprint.md`](40d_cortex_site_context_sprint.md). Reframes the original P0-3 (cc-agent-C Phase 1 flag) into a 2D-first sprint; 3D site assembly stays queued as a follow-on visual layer. Pre-mortem cleared **green** 2026-05-23 (load-bearing 1/2/3 clean: USGS 3DEP federal public-domain not a jurisdictional licensor; per-engagement compute is product COGS not jurisdiction-onboarding cost; provenance fully tracked on the two new tenant-private atoms `site-topography` + `site-drainage`. Two operational yellows absorbed: UI-first with MCP retrofit follow-on; substrate-adjacent product layer per the 46_smartcity_parcel_intelligence portfolio framing). Four phases owned by cc-agent-C, sequential: 2D.1 DEM ingest + topo overlay; 2D.2 hydrology / drainage analysis; 2D.3 rainfall simulation UI + briefing integration; 2D.4 address-to-parcel auto-resolve polish. **Sequenced behind the cleanup batch** (QA-33 + QA-22 reopen) which cc-agent-C is firing now. **Phase 3 features (QA-27/28/29) yield until this sprint closes.** `cortex/site_context_*` MCP retrofit recorded as a tracked follow-on on doc 42 watch line per dual-interface principle.
- **Municode adapter enhancement — `tocRootNodeIds` option for nested-chapter cities** (2026-05-22, cc-agent-E Tier 2 follow-up). Four central-Texas cities (Luling, Woodcreek, Belton, Creedmoor) have development chapters nested under wrapper TOC nodes (e.g. `PART II - CODE OF ORDINANCES`, `TITLE XV - LAND USAGE`) that the current `MunicodeHtmlAdapter.chapterFilter` (top-level TOC nodes only) cannot isolate. Per cost discipline cc-agent-E skipped rather than ground; a `tocRootNodeIds?: string[]` option lets a Path C ingest start the walk from one or more explicit nested nodes, unblocking all four. Separable engineering follow-up dispatch (cc-agent-E lane, low priority — the corridor is otherwise covered).
- **40c rendering-sprint doc-set reconciled 2026-05-23.** The Phase A.1 audit found the mnml.ai render feature already built and merged to `main`; the operator chose **path A** (gap-fill only). cc-agent-R shipped Phase A (PR #79) and Phase B (PR #80), both merged 2026-05-22. Doc reconciliation done 2026-05-23: [`40c_cortex_rendering_sprint.md`](40c_cortex_rendering_sprint.md) gained a Status section + exit-criteria caveats; [`_decisions/2026-05-22_cortex_rendering_activation.md`](_decisions/2026-05-22_cortex_rendering_activation.md) carries an Amendment block noting V1 was already shipped + actual gap-fill executed; [`42_design_accelerator_program_plan.md`](42_design_accelerator_program_plan.md) DA-12 row updated + deferred-follow-ons watch line added. **Two scope follow-ons remain deferred:** B.1 full schema-driven per-expert param grid; B.2 manual upload as render source for concept imagery (architect-facing concept affordance shipped in #80, but the underlying render still captures the GLB — concept imagery is not end-to-end from a manual sketch upload until B.2). Activation in prod gated on operator flipping `RENDERS_PROD_ENABLED=true` + `MNML_RENDER_MODE=live` in the same Cloud Run revision; recommended to land B.2 before activation so the concept toggle doesn't ship non-functional.
- **Workspace-conflict incident — cc-agent-C and cc-agent-R may have shared a `legacy-design-tools` clone** (2026-05-22). The dispatch and earlier `00` framing called them on separate clones with bounded overlap; both agents independently surfaced a mid-session collision where another actor made commits and switched HEAD in the working tree (cc-agent-R's reflog shows HEAD switched to `feat/p2-deploy-workflow-actions` between adjacent shell calls — not by cc-agent-R). cc-agent-C's first Phase 2 push (`feat/p2-deploy-workflow-actions`, commit `aa35f9d`) was contaminated with cc-agent-R's dialog fix; the contaminated branch was pushed to origin. **Recovery clean on both sides:** cc-agent-C re-cut Phase 2 from a `p:/tmp/p2-worktree` worktree as PR #81 (`feat/p2-deploy-workflow-v2`); cc-agent-R re-applied the dialog fix on the proper branch as commit `8c0f064` for PR #80. **Operator action owed:** (a) untangle `origin/feat/p2-deploy-workflow-actions` — cc-agent-R recommends rebasing it onto `main` after PR #80 merges (the duplicate dialog change drops as already-applied), no force-push needed; (b) audit which actor (operator activity, an IDE git extension, or a cross-agent step) ran in the same clone concurrent with cc-agent-R; (c) address the workspace-hygiene gap before Phase 3, which has a larger blast radius. See [`_sessions/2026-05-22_workspace_hygiene_incident_cc-agent-R.md`](_sessions/2026-05-22_workspace_hygiene_incident_cc-agent-R.md) and [`90_runbooks/agent_workspace_hygiene.md`](90_runbooks/agent_workspace_hygiene.md).
- **`_inbox/` + HR-11 live** (2026-05-21). cc-agents drop session summaries into the doc repo's `_inbox/`; the planner sweeps and files. The `_inbox/` write is the one permitted cross-repo write; cc-agent-C had drafted into `legacy-design-tools/_research/` instead, and the new codex-reviewer-qa scaffold dispatch reinforces `_inbox/`.
- **QA-17 full closure — operator mcp-mode config** (2026-05-21). QA-17's code is merged (PR #64) with `mock` mode as the default. To meet the success criterion (five jurisdictions with real atom counts, including the three `platform-internal` ones): mint a Cortex product key via the hauska-mcp-server admin key-issuance endpoint, set `HAUSKA_SUBSTRATE_MODE=mcp` + `HAUSKA_MCP_URL` + `HAUSKA_MCP_KEY` on cortex-api, and operator-supervised-deploy. Nick action.
- **smartcity-os production database — operator hands-on (2026-05-21).** The operator is dealing with the smartcity-os production database directly and wants full attention on it. cc-agent-M / M-Stabilize is held for the duration — WS-1's migration spine touches that database and would collide. The M-Stabilize dispatch re-fires when the operator releases it.
- **cc-agent dispatch state and legacy-design-tools coordination** (updated 2026-05-23). Active on legacy-design-tools: cc-agent-C (QA-22 cleanup — FCC verify diagnostic + held EPA/Grand County dispatches) + cc-agent-C2 (2D-site-context Phase 2D.1, dedicated clone at `P:\legacy-design-tools-c2`, re-activated 2026-05-23 after operator initially dropped it earlier same day). cc-agent-R retired (rendering sprint gap-fill closed PR #79/#80). Bounded overlap C ↔ C2: file paths disjoint (C: `lib/adapters/*`; C2: api-server DEM client + `SiteMap` + `site-topography` atom). Drizzle migration coordination: Phase 2D.1 takes 0016+ if needed (cortex-prod at 0015 per QA-04 Part 2). cc-agent-E Sync 5 corridor on `hauska-engine` separate. QUEUED, not yet fireable: ECI atomization P1 ([`_dispatches/2026-05-21_eci_atomization_p1_registry_scaffold_QUEUED.md`](_dispatches/2026-05-21_eci_atomization_p1_registry_scaffold_QUEUED.md)) — gated on Nick creating the `empressa-atom-internal` repo and a cc-agent seat freeing. Still open from Lane C.4 close: the DA-MCP-Cortex Group 4 cross-client verification (appears largely done per hauska-mcp-server #12/#21, needs a confirmation pass) and legacy task #29 dual-auth tightening (non-blocking). QA-20 routed into cc-agent-E Lane E Phase E1.
- **doc 44 (MCP / Cortex architecture map) is partly stale** (flagged 2026-05-22). Facts 5 and 6 predate the hauska-prod MCP-server deploy and the retrieval-API go-live; QA-17 (PR #64) added one Code-Library-to-MCP path on cortex-api. A dated staleness note is in the doc; a full refresh against the post-deploy topology is owed. The Design Tools app surfaces remain self-contained — no QA-run breakage is MCP-related.
- **Circle is the v1 fiat rail** per `_decisions/2026-05-21_fiat_rail_circle.md`. The fiat rail is a near-greenfield Circle build, not a single TODO.
- **Revenue-routing gap** — substrate-enforced revenue share has no code in the SDK. Designed, not built. Needs a Wave 2 build line.
- **ICC Code Connect API access** — operator meeting next week to obtain Code Connect credentials; unblocks the Layer 1 model-code corpus ingest (ADR-019). The pre-credential build-out is in flight (cc-agent-E PR #24, the OAuth2 adapter). cc-agent-E's prebuild session [`_sessions/2026-05-22_icc_code_connect_prebuild_cc-agent-E.md`](_sessions/2026-05-22_icc_code_connect_prebuild_cc-agent-E.md) carries a **16-item "needs confirmation from the live API / OpenAPI spec" list** — that is the operator's meeting checklist. Also confirm whether ICC's arrangement touches the NEC (NFPA 70, the 8th Layer 1 doc, not on Code Connect — likely a separate NFPA conversation).
- **Corpus refresh — batched into the end-of-QA deploy** (operator decision, 2026-05-22). Merged on `hauska-engine` `main`: Round Rock (#20), Taylor (#21), Leander (#23), Georgetown (#27), New Braunfels (#28), Killeen (#29) — 6 cities. PRs #30-37 open from the central-TX corridor batch (Austin, Copperas Cove, Manor, Lockhart, Lago Vista, Dripping Springs, Wimberley, Rollingwood — 8 cities, ~4667 atoms). On merge, 14 central-TX cities will be merged-but-undeployed (plus Bastrop / Bastrop County / Elgin / Hutto already in the deployed corpus). The `build-corpus-snapshot` refresh + retrieval-api redeploy is the batched end-of-QA operator-supervised step; the live catalog serves the original 5 jurisdictions until then. The QA pass on the Musgrave (Moab / Grand County UT) engagement does not depend on the new TX cities being deployed — Grand County is in the original deployed corpus.
- **Four stray uncommitted files in cc-agent-C's `legacy-design-tools` clone** (surfaced 2026-05-22, cc-agent-C PR #70, per the workspace-ownership clause). Pre-existing modified-uncommitted test files predating the multi-dispatch session, not any current agent's work: `BriefingDivergencesPanel.test.tsx`, `EngagementDetail.test.tsx`, `SiteContextTab.test.tsx` (all `artifacts/design-tools/src/pages/__tests__/`), and `lib/db/src/__tests__/integration/schema.integration.test.ts`. Kept out of every branch via explicit per-path `git add`. Inert, but should be reconciled or discarded — possibly the operator's own WIP.
- **Doc-set staleness gap, pre-courier-protocol work** (surfaced 2026-05-22). Lane C.4 shipped 2026-05-20 (legacy-design-tools PRs #46/#51) but `42_design_accelerator_program_plan.md` still called it "the remaining build" in its 2026-05-21 edit, and the planner re-dispatched already-done work off it. The `_inbox/` courier protocol began 2026-05-21; cc-agent work merged around 2026-05-19/20 did not all reach the doc set. **Reconciled 2026-05-22:** `42`'s Lane C status block and the DA-4/5/6 rows were corrected against the verified legacy-design-tools PR history (Lane C.2/C.3/C.4 all shipped 2026-05-19/20, PRs #35-#51); `11` checked clean; hauska-mcp-server (Lane B + Lane M, #1-22) and hauska-engine (Lane A.2 + Lane E, #1-23) PR histories cross-checked current. A full project-refresh of the remaining sprint docs (`48`, `51`) was not done and is the residual risk. Planner discipline: verify a stream's status against the live repo (`gh pr list`) before dispatching or re-activating, not the doc set alone.
- **plan-review has no app shell** (surfaced 2026-05-21, cc-agent-C PR #68). QA-15 needed the header-bell prop at 14 `DashboardLayout` call sites because plan-review, unlike design-tools, has no shell component. A `PlanReviewShell` wrapper is the right cleanup; logged as tech debt, low priority.
- **Residual `@workspace/empressa-atom` doc-comments** (surfaced 2026-05-21, cc-agent-AC PR #67). After the ADR-018 retirement, stale prose references to the removed package remain in `lib/codes`, `artifacts/design-tools`, `lib/comment-letter`, the generated `lib/api-zod` / `lib/api-client-react` files, `lib/api-spec/openapi.yaml`, and `docs/deploy.md`. Cosmetic only, not imports; the generated files should be fixed at the api-spec source, not hand-edited. Optional tiny follow-up, a candidate for cc-agent-AC's now-empty queue.
- **IP-memo ingestion gate fully retired** — the Texas IP attorney memo is parallel bizops tracked in `72`, not a gate on substrate ingestion. The stale gate language in 11/73/18/13 was corrected 2026-05-21; `51` was already corrected 2026-05-19.

> **2026-06-09 Run-plan-review reliability follow-up (dispatched).** #155 deploy live (rev `00138-taj`); 409 gone. Operator surfaced: the Triage Inbox piled up orphaned "pending" reviews (each Run creates a submission + fire-and-forget generation; the repeated canary deploys restarted api-server and orphaned in-flight runs -> `finding_runs` stuck `pending`), and nothing stops stacking Runs. Dispatched fix: (1) in-progress state on the Run button + DISABLE while a review is in flight for the engagement (no stacking) with live pending->running->done/failed status; (2) stale-run reaper (pending past timeout / startup sweep -> `failed`); (3) Triage shows true run status per submission; (4) one-time expire the orphaned pendings on 404 Remodel_B (`15d1d314`). Plan review itself works (web-grounded findings land); this is reliability/UX polish.
