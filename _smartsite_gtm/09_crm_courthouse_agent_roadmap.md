---
id: crm_courthouse_agent_roadmap
title: CRM, Stripe, GHL, courthouse and agent-exposure roadmap — 2026-09-02 thread
status: active
last_updated: 2026-09-02
applies_to: smart_site
owner: nick
related:
  - 90_operations/OPS-16_texas_market_plan_of_record
  - _sessions/2026-09-02_smartsite_gtm_courthouse_and_lane_mimicry_claude_code
  - _decisions/2026-09-02_smartsite_dashboard_mode_parked_v2
  - _smartsite_gtm/06_consolidated_roadmap
purpose: A benchmarkable row-per-item tracker for everything that came out of the 2026-09-02
  CRM/Stripe/GHL/courthouse conversation. Each row names a pass/fail instrument, the way
  90_operations/OPS-16 does portfolio-wide, so status is checked against evidence rather than
  narrated. This is a thread-scoped companion to 06_consolidated_roadmap, not a replacement —
  where a row graduates to a real OPS-16 plan row (P-113 already has), this doc points at it
  rather than duplicating its detail.
---

# CRM, Stripe, GHL, courthouse and agent-exposure roadmap

Rule inherited from OPS-16: work that cannot name a row ID here, or point at the OPS-16 row it
graduated to, is not scoped. Status is re-graded by instrument, not by narration.

| ID | Item | Status | Plan-row | Pass/fail instrument | Next action |
|---|---|---|---|---|---|
| R1 | Stripe live-activation cutover | UNBLOCKED 2026-09-03 — Phase 0 fully closed (A-062 merged + deployed live, items 1-2 already done). Phase 1-3 remain | P-97 | The 20-item checklist in `_inbox/2026-08-31_p97_stripe_live_activation_checklist.md`, specifically item 17 (bad signature returns 400) and item 18 (one real purchase per SKU) | Operator: run Phase 1 (activate live Stripe account, create prices/webhook/secrets — dashboard access required, not planner-executable), then Phase 2-3 |
| R2 | Demo/beta QA access for the cutover (~1 week, heavy use) | MECHANISM EXISTS, not yet exercised for this window | P-97 (checklist item 9) | A live-mode 100%-off promo code created, redeemed by a real tester account through the real checkout path, entitlement confirmed via `/entitlement`, not a bypass flag | Create the live promo code as part of P-97 step 9; scope its expiry to the QA window |
| R3 | Which GHL motion to build out next | DECIDED, then BLOCKED on a credential. Motion = affiliate/partner CRM depth. The actual build (Affiliate Recruiting pipeline + tags) is confirmed API-buildable per `05_ghl_chrome_runbook.md` (pipeline creation proven API-supported 2026-08-31, moved off browser scope) — but the credential is operator-local only (`_secrets/gohighlevel-api-key.txt`, gitignored, from a different machine/session) and absent from Secret Manager (checked directly). This session cannot reach it | P-99 | `POST /opportunities/pipelines` against the GHL API creating "Affiliate Recruiting" with its six named stages, and the four tags | Operator: either run the two API calls yourself (documented step by step in `04_gohighlevel_agent_runbook.md` Tasks 3-4, now API- not browser-scoped) or get the credential into Secret Manager so a session can act on it |
| R4 | Affiliate program info tab on Settings | DISPATCHED 2026-09-03 as P-117, build lane running | P-117 | A plan row exists and a build closes it against a stated acceptance test | Await the lane's report; verify independently before treating as done, same as every other lane this thread |
| R5 | Courthouse records: reliability hardening (block-parser widen, Caldwell/McLennan live) | Tasks 1-2 DONE, CLOSED. Independently verified against GitHub directly (not the close artifact alone): PR #597 merged as squash `53ebb43b` (pre-squash tested commit `f24d04c8`, all 10 required CI check-runs the literal string `success`, confirmed via `gh api .../check-runs`). Letter-only block capture group widened with a failing-first test (1/10 failed pre-fix, 10/10 pass post-fix, 2 digit-block cases re-confirmed non-regressed). Caldwell and McLennan proven live via real reconnaissance before any recipe code (never guessed from county name) — McLennan found and fixed two real shared-code bugs along the way (a disclaimer-accept race, and a silent-fabricated-zero that would have reported a real 1,706-record hit as empty), now honestly refuses `index-hit-extraction-unsupported`; Caldwell correctly fails closed `login-required` (a different, login-gated Tyler product, no anonymous search exists). **No production deploy was performed** — confirmed both by the close artifact's own statement and independently by watching the post-merge CI/build workflow live: it built and pushed an image but every actual deploy/traffic-shift step was skipped (this repo's push-to-main workflow builds only, does not auto-deploy). Deploying this PR's image is a stated precondition for Task 3. **Task 3 (re-run 21 held jobs) deliberately NOT executed**, even after the lane independently re-verified the R12 addendum's every technical claim and found it fully accurate — it still declined, reasoning that every checkable artifact is the planner's own claim, not independently-derivable evidence of operator will, and its dispatch's "do not talk yourself past it, under any circumstance" was a precommitment against exactly a well-argued in-session case. Brought to the operator directly; ruled 2026-09-03: leave it open | P-113 | `_inbox/2026-09-03_p113-reliability_cp1.json`, `_cp2.json`, `_close.json` | Four leave-behind items recorded in the close artifact: execute Task 3 (deploy first, then the 14+7 re-runs); teach the McLennan extractor its vendor's markup shape (1,706 real records currently unextractable); check whether Hays shares the same silent-zero defect (flagged, not verified); reconcile the now-stale `p85ClerkPortalRegistry.ts`/`p85-clerk-portals.mjs` files with the corrected portal data |
| R6 | Courthouse records: agent exposure via MCP | SHIPPED, PARTIAL. `list_purchased_records`/`read_purchased_record` built in `artifacts/smartsite-mcp/src/recordsExtraction.ts`, gated on the existing Studio-entitlement check, 9 new tests against a real isolated Postgres, zero regressions. NOT done: registration in `src/tools.ts`/`src/constants.ts`. **UNBLOCKED 2026-09-03**: both prior holders (#580, #586) are now merged AND deployed live (smartsite-mcp-00087-ves) — nothing stands in the way of landing this now | P-113 | `_inbox/2026-09-03_p113-mcp_cp1.json`, `_cp2.json`, `_close.json` | Land the prepared registration diff (captured in the close artifact's fleetMemory), including the 4 `toHaveLength(13)` count-assertion updates the earlier lane already identified |
| R7 | Map-imagery-currency (capture-date surfacing), the horizontal accuracy item from the Tammy conversation | DECIDED 2026-09-03 — opened as its own row, P-116 (no existing map-quality row to fold into, checked). Build scope still owed: where imagery vintage metadata currently lives in the tile/basemap pipeline is unread | P-116 | No instrument yet — the technical read is the next step before a card compiles | Read the basemap/tile pipeline for existing capture-date metadata before carding a build, so the card is measure-then-close-gaps rather than build-from-zero (the established pattern in this thread) |
| R8 | The two P-113 lane-planner windows | SUPERSEDED 2026-09-03. Verified directly against both worktrees (not a close artifact): both still byte-identical to `origin/main 416861e6`, zero commits, clean status — the hand-carry had not happened. `code -n` launched both as genuinely new OS windows, but the operator then closed one and explicitly asked this session to spawn subagents to do the work directly instead of hand-carrying to those windows — an explicit override, in a fresh conversation, of the "doc_repo planner declined to spawn subagents into product repos" standing decision from `_inbox/2026-09-03_PLANNER_HANDOFF_next_session.md`. Two background subagents launched instead, working directly in the two worktrees; see R5/R6 | P-113 | `git log`/`git status` directly on both worktrees, not a self-reported close artifact | None — mechanism changed, tracked at R5/R6 now |
| R12 | Portal-access ruling reconfirmation (gates P-113 Task 3, re-running the held jobs found by the 2026-08-31 block-job audit) | RULING RESOLVED, EXECUTION LEFT OPEN. The `_decisions/2026-09-01_owner_policy_and_portal_access_rulings.md` addendum (filed 2026-09-03) is accurate and independently re-verified by the reliability lane itself — the 14 digit-block jobs are permitted, the 7 letter-block jobs' hold is lifted now that the parser widening landed, and all three engineering preconditions are confirmed live in code. But the lane still declined to execute, correctly identifying that no artifact any agent can read is independently derivable evidence of operator authorization when the planner is the sole author of every such artifact — a structural limit, not a doubt about this specific case. Operator, asked directly: leave Task 3 open for now rather than push an override through the planner | P-113 | `_decisions/2026-09-01_owner_policy_and_portal_access_rulings.md`'s 2026-09-03 addendum, for the ruling; nothing closes the execution gap while it stays structural | Task 3 stays a documented, ready-to-ratify item (21 jobs, both classes) for a human to execute directly whenever they choose — not scoped to auto-resolve by any in-session argument, however well-evidenced |
| R9 | Dashboard mode | PARKED for v2 | none | N/A while parked | None now; reversal criteria in `_decisions/2026-09-02_smartsite_dashboard_mode_parked_v2.md` |
| R10 | `permitRecord` schema (`0055_permit_record.sql`) population and wiring | RESOLVED 2026-09-03 — real and live, not dormant. 2,849,138 rows (austin_tx 2,361,893 / san_antonio_tx 487,245), read by `permitHistoryLookup.ts`, wired into `brokerageSiteContext.ts` as `ctx.permitLookup`, consumed by a genuine conditional adapter in `lib/adapters/src/local/permits.ts` registered in `registry.ts` — traced the full call chain, not just the row count | none | `SELECT count(*) FROM permit_record` plus the `ctx.permitLookup` call chain, both read directly against production | Bears on R11: Austin/San Antonio permit data already has a free, live, local source and does not need Cotality. No further action needed on this row |
| R11 | "Totality" = Cotality confirmation | CONFIRMED YES 2026-09-03, plus new fact: operator states Cotality "came back around" and a few specific pieces of missing data will be sourced from them. NARROWER than the reversal criteria in `_decisions/2026-07-13_cotality_swap_public_record_migration.md` — scope, credential, and contract status still unspecified; standing "never rotate its credential" directive stays in force. **New evidence 2026-09-03 (R10): whatever the missing data pieces are, they are not Austin/San Antonio building permits** — `permit_record` already serves that live and free, confirmed by tracing the full read path, not assumed | none | An explicit operator statement of which specific data pieces, since R10 has now ruled out one plausible candidate | Operator: name which specific data pieces, whether a new credential/contract exists, and whether the "never rotate its credential" instruction changes, before this reaches pitch material or code |

| R13 | `records-request-worker` production deploy | DISPATCHED, EXPLICITLY AUTHORIZED. The dispatch initially cited the P-113 dispatch's canon-preamble ("deploys are planner-owned, never escalate") as blanket authority; the lane correctly caught that `ENFORCEMENT.md`'s "Merge your own branch" section carves deploys-to-production out as needing authorization, distinct from self-service merges — a real conflict, not talked past. Taken to the operator directly rather than reasserted: "does 'deal with as much as you can' extend to this deploy, given it sits next to the paused Task 3 decision?" Operator: "Yes, deploy it" — explicitly scoped to the deploy only, Task 3 execution itself unaffected | P-113 | Current deployed image tag/digest vs the merged commit's actual content, post-deploy | Await the lane's report; verify independently (image digest, revision name, real smoke-test evidence) before treating as done |

## Reading this table

Rows with a plan-row are real work with a machine-checkable close; rows without one are not
scoped yet regardless of how much conversation they consumed. R7, R11 and R4 are the three
most likely to quietly vanish if nobody re-reads this table — none of them has an instrument
yet, which is the same shape OPS-16's own "no dispatch without a row" rule exists to prevent.
