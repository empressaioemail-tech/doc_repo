# Icon
Lucide glyph wrapper — never hand-draw icon paths.

```jsx
<Icon name="ticket" size={20} />
```

Needs `<script src="https://unpkg.com/lucide@latest">` on the page. Colour by setting `color` on the parent. No emoji anywhere in this brand.
