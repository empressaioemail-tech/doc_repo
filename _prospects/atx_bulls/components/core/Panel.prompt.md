# Panel
Any grouped surface. Replaces the idea of a "card" — this brand has no rounded cards.

```jsx
<Panel variant="hairline">…</Panel>
```

Prefer `hairline` on photography, `panel` on flat ink. Reach for `chamfer` only on modals and one hero-scale panel per view.
