/** Lucide glyph wrapper — the only icon source. Stroke 2.25 to match the site's arrow. */
export interface IconProps {
  name: string;
  size?: number;
  strokeWidth?: number;
  style?: React.CSSProperties;
}
export declare function Icon(props: IconProps): JSX.Element;
