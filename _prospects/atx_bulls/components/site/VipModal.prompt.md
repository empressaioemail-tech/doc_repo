# VipModal
Email capture modal behind Shop / Reserve / Save seats / Meet team.

```jsx
<VipModal open={open} onClose={close} onSubmit={save} />
```

Panel is orange with the bottom-right chamfer; fields are 2px ink borders over a 13% white fill. No rounded corners anywhere.
