# NavBar
Marketing site header.

```jsx
<NavBar links={["Tryouts","Story","Team","Uniform","Merch"]} solid={scrolled} onCta={openVip} />
```

Never more than five links. The CTA is always the small 42px chamfered button. Over the hero it is fully transparent with no border.
