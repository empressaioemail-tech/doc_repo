# Tabs
Filter row inside a screen.

```jsx
<Tabs items={["All","Tickets","Merch"]} active={tab} onChange={setTab} />
```

Matches the TextLink underline weight (3px). Scrolls horizontally on mobile rather than wrapping.
