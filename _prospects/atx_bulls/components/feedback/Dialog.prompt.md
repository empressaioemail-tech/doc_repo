# Dialog
Confirmation and detail modal.

```jsx
<Dialog eyebrow="Confirm" title="Redeem sideline pass?" onClose={close} footer={<Button size="sm">Redeem</Button>} />
```

Positions against the nearest positioned ancestor so it works inside a phone frame. Body copy stays uppercase condensed.
