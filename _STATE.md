# _STATE — living program state (read this FIRST, every session)

Single source of truth for WHERE WE ARE RIGHT NOW. Not decisions (those are in memory / _decisions/), not history (those are in _sessions/). This is live state a fresh agent picks up from. Update it as state changes; it is meant to be edited constantly. **Last updated: 2026-08-10 (five rails merged w/ zero coverage; rail split LIVE at 14 rails / 0.7689%; sweep 123 landed / 9 metros left; harvest-completeness ruled).**

## THE ONE-LINE

**Sweep at 123/132 counties (9 metros left, ~8 h) after a 4.7x fix that was NOT the write path — it was a verify SELECT seq-scanning 10.7M atoms per batch, solved by `--batch=5000`. FIVE RAILS MERGED (owner / well / rail-corridor / footprint / special-district, PROPERTY_ENTITY_TYPES=14) and ALL have ZERO coverage — every apply is queued behind the atoms slot. R1 rail split LIVE in prod: 14 rails / 3,556 cells / 0.7689%. Harvest completeness RULED (take everything a source carries). Market layer PARKED. Q13 found stranded honesty work whose 0.74 fixture is still serving on ldt main.** Session close: `_sessions/2026-08-10_write_path_rails_and_harvest_claude_code.md` + the mid-session capture `_sessions/2026-08-10_five_rails_and_write_throughput_claude_code.md` (read BOTH). Handoff: `_inbox/2026-08-10_PLANNER_HANDOFF_next_session.md`.

## LAUNCH GATE RULED 2026-08-09 + CLEANUP BATCH IN FLIGHT

**Texas-flush launch gate = measured-everywhere** (`_decisions/2026-08-09_texas_flush_launch_gate.md`, per-rail split ratified): L2 at 254, statewide roads/NFHL/footprints, all 12 rails with writers, cert frame reconciled, 76j capacity items. Filled-everywhere = program completion, runs post-launch. **Game plan: `90_operations/OPS-14_texas_flush_game_plan.md` (adversarially reviewed v2). All five operator decisions RESOLVED 2026-08-09 evening** (footprint = Central TX + Dallas metro; pricing ladder of record = 76_wedge plan $0/$20/$40/$75; second-state set = UT/NM/CO/AZ; owner+easement classifications ratified; heavy-scan slot = one write slot per database).

**ATOMS BULK SLOT — TAKEN BY D (parcel-node sweep) 2026-08-09T23:15Z.** Operator confirmed K3 is the ONLY other thing in flight; the "slot with H for H6" recorded earlier NEVER HAPPENED (H6 was never dispatched, no H PRs beyond #293 open DO-NOT-MERGE). Sweep resumed: queue **77 counties / 10,372,552 features** (store truth from `sizing.json` minus `progress.landed`, NOT the "~80" prose figure). Baseline recorded at resume: `texasCompletenessPct=0.897 / satisfiedCells=89`. Runner `P:/tmp/parcel_node_sweep_20260809/run_sweep.mjs`; progress `progress.json`.

**RESUME DEFECT FOUND AND FIXED (record it — this class recurs):** the runner's skip set was `landed ∪ halted.countyFips`, and **48457 was the halted county that never landed** (dry interrupted at the boundary). Resuming as-written would have SILENTLY SKIPPED it — a one-county hole in a 254-county fabric that no count-based gate would catch, same class as the Harris multi-shp truncation. Fix: cleared the `halted` object (48457 is absent from `landed`, so it re-enters at queue position 1); pre-resume backup at `progress.pre_resume_backup.json`; clearance recorded in-file under `haltCleared[]`. Verified live: the resumed run opened on 48457. NOTE the progress file carried a **UTF-8 BOM** (PowerShell write) that breaks `JSON.parse` — strip `^\uFEFF` on read, write without BOM.

**⚠ PE "PARCEL FACTS TEMPORARILY UNREACHABLE" — FIXED 2026-08-10. THE P0 AUTH SEAM BIT A SECOND SURFACE.** Operator R6 browse found EVERY parcel — including gold Bastrop 34137 — showing "Parcel facts temporarily unreachable". Root cause traced by probing the real BFF path `/api/spine/property-atoms/:id/facets`: **`retrieval_auth_failed`, HTTP 503 wrapping an upstream 401** — PE's `HAUSKA_RETRIEVAL_API_KEY` (set 7 days ago) predated the P0 key rotation and no longer matched retrieval-api's `RETRIEVAL_API_KEY`. **THE STORE WAS NEVER SICK:** the same parcel with the live `HAUSKA_ENGINE_API_KEY` secret (59 chars) returned HTTP 200 with the full chain — SF-1, setbacks 25/5/25, height 35, impervious 50%, 9,350 sq ft envelope, Pine + Jefferson roads, terrain exports. This is the durability debt recorded this morning under the L lane ("the fix lives only in Cloud Run env") landing on the PE surface instead of MCP. Fix: synced PE's key to the live secret and redeployed. **Verified recovered:** 34137 serves `readPath: atom-chain-warm`, SF-1, 25/5/25; fabric-only 48121:145609 (Sanger/Denton) serves honest base facts (APN, situs, landUse A1 cad-roll) rather than a 401.

**TWO TRAPS HIT WHILE FIXING IT — both worth knowing:** (1) **`hauska-map` was linked to the `cmdcenter` Vercel project, not `property-explorer`.** The first `vercel env add` landed on the WRONG project and a `vercel deploy` would have pushed PE's build to the Command Center. ALWAYS `vercel link --project property-explorer` first and re-read `.vercel/project.json` before any env or deploy op in this repo. (2) The two projects use DIFFERENT var names — PE reads `HAUSKA_RETRIEVAL_API_KEY` (with a `RETRIEVAL_API_KEY` fallback in code), cmdcenter had `RETRIEVAL_API_KEY`. Key written with `printf '%s'` to a 59-byte file, no trailing newline (the PS5.1 stdin-newline trap). **STANDING FIX OWED: this key is synced by hand in two places and will rotate out of sync again — put it in the deploy workflow or a shared secret reference.**

## ⏭ QUEUED WORK — NOT DISPATCHED, NOT FORGOTTEN (added 2026-08-10)

Everything here is scoped with a written artifact and is waiting on a lane, a slot, or a ruling. **If it is in this list it has NOT been executed.** A fresh agent picks work from here.

| # | Lane | Blocked on | Artifact |
|---|---|---|---|
| Q1 | **Apply the five built rails** — owner, well, rail-corridor, footprint, special-district. All registered on engine main, all with ZERO coverage. | the atoms bulk-writer slot (parcel-node sweep holds it) | OPS-15 + each rail's close artifact |
| Q2 | **Geometry scorer re-run** after the sweep closes | sweep completion | `_inbox/2026-08-09_W1_D0_geometry_scorer_CLOSE.md` |
| Q3 | **HARVEST TAKE-LIST EXECUTION** — persist the Class A/B fields the inventory found. Catalogue is DONE and CORRECTED; nothing has been harvested. Top: `GEO_ID` 158, `BLOCK` 151, `DEED_DATE` 148, `SCHOOL` 147, `HOOD_CD` 147, `MAP_ID` 147, `ABS_SUBDV_CD` 143, `TRACT_OR_LOT` 142. **The Class A cluster is parcel identity + plat lineage — the SAME job address-to-parcel needs; dispatch them as ONE lane, not two.** | a free lane | `_catalog/source_field_inventory.json` (1,330 entries), `_inbox/2026-08-10_HARVEST_field_inventory_report.md` |
| Q4 | **Address-to-parcel resolution** — `txgio_parcel.situs_address` is 99.3% populated across 196 counties; no resolver exists. Blocks MCP/API address lookup, paste-a-list, and may repair the 10 hold counties. | a free lane (read-only, slot-safe) | `_inbox/2026-08-10_address_to_parcel_resolution_scope.md` |
| Q5 | **CAMA bulk-export routing + per-vendor parsers** — structural data (`living_area_sqft` 10.5%, metros at 0.0%). Registry already flags `bulk_primary: true` on Dallas + Tarrant and the ingest ignores it. Separate motion from Q3. | a free lane | `_inbox/2026-08-10_cad_structured_data_gap.md` |
| Q6 | **B2 footprint metro join** — Dallas join is O(fp x parcels); sized 482 B ops (~6.7 h), Harris 2,285 B ops (~32 h). Grid index drops both to seconds. | DISPATCHED, awaiting return | brief in session log |
| Q7 | **IDX atoms index audit** — nine indexes / 2,550 MB on a 10.9M-row table. Sharpened by the reconciliation: `code-section` is 0.26% of the store yet `atoms_section_number_idx` is maintained on every insert. | DISPATCHED, awaiting return | brief in session log |
| Q8 | **Statewide roads GO/NO-GO** — eng #293 open, DO-NOT-MERGE. H6 was never dispatched. | planner decision on evidence | `_inbox/2026-08-09_F5_roads_unblock_status.md` |
| Q9 | **E3 Elgin apply** — dry-run anchors-clear but NOT legacy-parity (75/98 vs 102/72). | E3-ADV re-review, then slot | `_inbox/2026-08-09_E3_elgin_dryrun_artifact_post_anchors.json` |
| Q10 | **Retrieval-key durability** — the key is hand-synced in TWO places and has drifted twice, breaking MCP once and PE once. Needs to live in the deploy workflow or a shared secret ref. | a free lane | session log 2026-08-10 |
| Q11 | **QA polish** — Q1 flood PDF missing the `SMART SITE` header prefix; Q2 chat not rendering markdown. | batched by design | `90_operations/QA_polish_register.md` |
| Q12 | **Market layer** — PARKED until after Texas launch. Do not open. | operator ruling | `_decisions/2026-08-10_market_layer_thesis_parked.md` |
| Q13 | **⚠ STRANDED: six commits on `origin/feat/oz-crossfilter-derivation` (ldt), PR #276 CLOSED-NOT-MERGED 2026-08-08.** Operator surfaced an old chat 2026-08-10; planner traced it. The branch replaces FOUR synthetic fixtures with real derivations — OZ from live CDFI/HUD (8,765 designated tracts verified), `deriveOzDealCrossfilter`, buildable-envelope (retires a 78% fixture), constraint-density (retires a 4-overlay fixture), and motivated-seller (retires the **0.74 propensity fixture**). **VERIFIED: the 0.74 fixture is STILL ON MAIN** (`brokerageGisCompositeLayers.ts:177`) and `deriveOzDealCrossfilter` is NOT on main. So the honesty work described in that session was reviewed clean, never merged, and the synthetic values it was written to retire are still what the composite layers serve. Branch is 6 commits ahead of main, last commit 2026-07-16; the PR was closed 3 weeks later with no successor PR found. **Decide: rebase-and-merge, or close deliberately with the fixtures documented as known-synthetic.** Do NOT leave it ambiguous — a fixture that survived its own replacement is exactly the class the L1 buildable-header defect belonged to. | planner decision | `origin/feat/oz-crossfilter-derivation`; ldt PR #276 |

**⭐ HARVEST COMPLETENESS RULED 2026-08-10 — TAKE EVERYTHING A SOURCE CARRIES, ON THE PASS WE ARE ALREADY MAKING (`_decisions/2026-08-10_harvest_completeness_ruling.md`).** Operator: *"while we are doing this heavy backfill effort let's make sure to grab all the data from all the sources we are already touching. If that means more cells on the manifest so be it"* — and explicitly *"that goes for EVERY source."* **The expensive part is the VISIT, not the payload.** Finding the source, negotiating access, authenticating, paginating and normalizing identity are paid whether we take four fields or forty; going back later pays the whole visit cost again against a source that may have changed vintage, moved, or gained an auth wall. Rules: take the whole payload with provenance; prefer the RICHEST tier the registry knows about (falling back is fine, falling back SILENTLY is the defect); new cells are acceptable and expected (R1 split rule still governs HOW — split on source+geometry, subcategorize on attribute); the manifest must record WHICH TIER satisfied a cell. **This does NOT license new acquisition targets** — it is completeness at sources we already touch; the 2026-08-01 scale ruling still governs which new layers get wired. Reversal: a source's full payload carrying licensed or PII material we cannot lawfully hold — harvest completeness never overrides tenant-sovereignty or no-privileged-data.

**THE INSTANCE THAT PRODUCED IT — CAD STRUCTURAL DATA IS NOT IN THE FACTORY PATH (`_inbox/2026-08-10_cad_structured_data_gap.md`).** Verified live: `cad_property` carries `owner_name` 98.4% and `market_value` ~98%, but **`living_area_sqft` 10.5%**, `year_built` 10.2%, `land_acres` 16.7% — and it is ALL-OR-NOTHING per county. Williamson 76.9% / Hays 69.3% / Bastrop 52.7% / Caldwell 27.9%, and **Bexar, Dallas, Tarrant, Travis, Collin, Denton ALL 0.0%** (~3.3M parcels). Cause is a SOURCE TIER, not a defect: counties with structural data came from **direct CAD exports** (`DATA-EXPORT-01.14.2026.zip`, `property.csv`); counties without came from **TxGIO StratMap** (`stratmap25-landparcels_*`), which carries geometry/owner/value but NO building characteristics — Bexar shows 703,258 rows, 697,088 owners, **zero** square footage. **The registry ALREADY KNOWS and the ingest ignores it:** `_catalog/tx_cad_source_registry.json` is 30 `arcgis_rest` / 1 `bulk_export` / 4 `honest_absent`, with `bulk_primary: true` on exactly Dallas 48113 and Tarrant 48439 — **both of which have 0.0% sqft in the store.** Same shape as the 2026-08-05 T3 finding that CAMA tabular holds the sqft; we recorded it then and did not go get it. Fix is a routing-precedence change plus a per-vendor CAMA parser (`adapter_kind` is already recorded), NOT a new capability. Audit list of every other source to field-inventory is in the ruling doc — **none of those are asserted gaps, they are the list to probe.**

**MARKET LAYER THESIS — PARKED 2026-08-10 (`_decisions/2026-08-10_market_layer_thesis_parked.md`).** Explored in discussion, documented, and deliberately NOT opened as a build lane; revisit after Texas launches. The frame: everything we hold is DURABLE (what the land IS — zoning, setbacks, flood, geometry; true for years). The unbuilt second class is the MARKET layer (what is HAPPENING — listed, sold, foreclosed, tax-delinquent, probate, permits; perishable, often licensed, frequently asserted by an interested party). **The parcel is the join key between them, and we already own the durable side statewide.** Build a MARKET-SOURCE ADAPTER CONTRACT (one discriminated `signalKind`), not "an MLS adapter" — every source carries parcel key + event type + date + status + asserted attributes, so MLS is the first implementation, not the only one. **The defensible asymmetry is that we can answer buildability for parcels that are NOT listed** — flip the axis from listing-search-with-extra-data to opportunity-search-with-a-listing-filter. Notably **market × market may beat durable × market**: tax-delinquent AND buildable-for-a-second-unit AND not-listed is a lead list nobody can generate, needs NO vendor license, and is entirely public record. Portal APIs checked: Zillow retired its public API (Bridge Interactive is the partner path and is an MLS licensing motion), Redfin has none, Realtor.com is partnership-only — **the portals own attention, not the answer.** Valerie holds MLS creds for the eventual test; a paste-a-list version needs no license at all. NON-GOALS while parked: no build lane, no adapter contract, no vendor applications.

**ADDRESS-TO-PARCEL RESOLUTION — PROMOTED TO CURRENT TRACK 2026-08-10 (`_inbox/2026-08-10_address_to_parcel_resolution_scope.md`), scoped, not yet dispatched.** It is a market-layer prerequisite but it is NOT parked with it, because it already blocks work we ship today: PE resolves an address to a COORDINATE (Photon) then finds the parcel by point-in-polygon on the map — fine for a human clicking, useless for a list, an API caller, or an agent. **We cannot currently answer "give me the parcel for this address" as a service.** The same defect recurs in costume: the 2026-07-29 situs front-role arc (a comma tail surviving normalization silently defeated the restamp on ALL parcels), CAD `prop_id` zero-padding, and the 8 CROSSWALK_HOLD + 2 LANDUSE_JOIN_HOLD counties that emit whole-rail honest absences because the prop_id join is unsafe. **The data is already ours — verified live: `txgio_parcel` carries `situs_address` on 15,370,111 of 15,479,206 rows (99.3%) across all 196 counties.** Not an acquisition problem; a normalization-and-index problem. Design cautions: a WRONG match is worse than none (fail closed, return candidates, verdicts `exact|ambiguous|not-found|out-of-coverage`); normalize with the IDENTICAL function at write and read; do NOT build a geocoder (Photon keeps the coordinate path); and benchmark the 500-address BATCH shape, which is exactly the shape that seq-scanned 10.7M rows in the sweep. Unlocks before any market source exists: MCP/API address lookup, paste-a-list, a second join key that may repair the hold counties, and canonical addresses in briefs.

**⚡ SWEEP 4.7x FASTER — AND THE W1 WRITE FIX WAS NOT THE REASON. ~54 h -> ~10 h (measured 2026-08-10).**

The operator asked whether the sweep could run on the new fast write path. It could not, and finding out why exposed an error in the planner's own earlier diagnosis.

**FIRST: the sweep tree was stale.** `P:/hauska-engine` sat on `fix/buildable-zero-inset-envelope`, which PREDATES W1 (#302) — it had been writing at the old 47 atoms/sec since that fix merged. Moved to `origin/main`.

**THEN THE SURPRISE: on the new write path it measured 22 atoms/sec — SLOWER.** `EXPLAIN ANALYZE` on the write-then-verify SELECT: `Seq Scan on atoms`, `Rows Removed by Filter: 10,708,795`, **Execution Time 10,143 ms**. Every 500-atom batch was seq-scanning all 10.7M atoms. **The bottleneck was the VERIFY READ, never the write.** The planner's earlier claim that "87.7% of wall time is the apply, caused by single-row INSERTs" was half wrong: the apply phase contains BOTH the write and the verify, and all of it was attributed to the write.

**THE INDEX HYPOTHESIS WAS WRONG — recorded so nobody retries it.** `atoms_property_parcel_node_idx` carries `WHERE entity_type = ANY(zoning-fact, setback-rule, buildable-envelope, parcel-terrain-model)` — it EXCLUDES `parcel-node` — plus `text_pattern_ops`, which does not serve `= ANY` equality. Planner created `atoms_parcel_node_lookup_idx` (122 MB, valid). It helps SINGLE-VALUE equality enormously (**8,700 ms -> 3 ms**) but does NOT help the 500-value `= ANY` form, which still seq-scans. Measured alternatives, all WORSE: forcing it via `enable_seqscan=off` **39/sec**; `unnest()` JOIN **57/sec**; 100 serial single lookups **20/sec** (a ~49 ms Neon round-trip floor dominates). Seq scan at 55/sec was the best. **The index is harmless and kept** (it serves single-parcel reads), but it is NOT the fix.

**THE ACTUAL LEVER IS BATCH SIZE.** Verify cost is ~CONSTANT per batch because it scans the whole table regardless of how many ids it carries — **measured 9,128 ms for 500 ids vs 9,296 ms for 5,000**. `run_sweep.mjs` now passes `--batch=5000` (was the 500 default). **Measured on 48183 Gregg: 57,903 atoms in 259 s = 223 atoms/sec, 4.7x.** Remaining 31 counties / 8.1M features: **~10 h, down from ~54 h.**

**THE GENERAL LESSON, worth more than the speedup:** *a phase-level timing (`applyWallMs`) attributes cost to the phase's NAME, not to what it actually does.* "Apply" contained a hidden full-table read. Profile the STEPS inside a phase before optimizing the one the phase is named after — and when a fix produces no gain, that is data, not a setup error.

**⚡ THE SCALING CONSTRAINT WAS THE WRITE PATH, NOT THE DATABASE — W1 MERGED 2026-08-10 (eng #302).** Operator asked whether one-DB writing was the bottleneck and proposed separate ingestion DBs. Planner MEASURED across 97 sweep counties: dry-run 2.51 h (1,281,764 features/h) vs **APPLY 17.91 h (179,463 features/h) = 87.7% of all wall time**, running at **47 atoms/sec / 21.14 ms per atom**. Root cause: `writePropertyAtomsBatch` did 32 CONCURRENT SINGLE-ROW INSERTs. **Sharding would have bought a second store writing at 47 atoms/sec** — double the infrastructure, a 60x inefficiency carried into all fifty states. Fix: one multi-row `INSERT ... ON CONFLICT` per 5,000-row chunk + unbounded pin-Map leak removed + legacy path kept as a differential oracle + in-batch dedupe on `atom_did`.

**HEADLINE CORRECTED 47→2,961 IS ACTUALLY ~765 (16x, not 63x).** The executor benchmarked on a THROWAWAY schema; its own artifact also showed the LEGACY path at 182/sec there vs the planner's 47/sec in production — same code, **3.9x apart**, unreconciled. Planner queried the live table: **10,656,683 rows / 20 GB / NINE indexes (2,550 MB), no triggers.** Index maintenance IS the gap. Realistic production ≈765 atoms/sec → TX remainder 49.6 h → **3.1 h**; 150M US parcels ~1.2 months → **~2.3 days**. Note: `_inbox/2026-08-10_W1_atom_write_throughput_PLANNER_NOTE.md`. **MOLDED: a write benchmark on an empty table measures the CODE; on the production table it measures the SYSTEM. State the environment or the extrapolation factor.** Planner also self-corrected mid-diagnosis: the IPFS pin is `InProcessIpfsPin` (a Map.set), NOT a network round trip — the brief said so explicitly and told the executor to verify rather than trust the planner. Separate-ingest-DBs remains right for BACKGROUND RE-WARM (a quality-of-service fix so ingest never competes with customer reads), just not for throughput.

**THE SKELETON-HUNTING METHOD (operator asked; it IS systematic): LOOK FOR TWO NUMBERS THAT SHOULD AGREE AND DON'T.** Four finds today, none from a bug report — 47-vs-182 (nine indexes), 89-vs-107 satisfiedCells (NOT a bug, different definitions), CP1 28k-55k vs 100,187 Bastrop footprints (ML density ≠ land area), and a site plan whose legend contradicted its own header. **Reconciliation finds skeletons; usage finds symptoms.** When two of your own numbers disagree, that is a free finding — do not round it off.

**FIVE RAILS BUILT, ZERO COVERAGE — the applies are all queued behind the sweep's atoms slot.** `owner-fact` (1.16.0, #296/#297), `rail-corridor-fact` (1.17.0, #299), `well-fact` (1.18.0, #300), `building-footprint` HOLD-1 closed (#294), `special-district-fact` (1.19.0, #301). `PROPERTY_ENTITY_TYPES` = **14**. The manifest reads 0.7689% no matter how many PRs merge. **We are building faster than we can apply.**

**MERGE-RESOLUTION DEFECT CLASS — verify with tsc AND a test-FILE count, never a green total.** Naive keep-both-sides produced a semicolon mid-union (twice), a type alias whose body became the next doc comment, `fact-writer-ids.ts` cut mid-array THREE times, and a package.json edit that silently DROPPED 2 of 17 dependencies. Three times a file failed to TRANSFORM and never loaded while the suite reported everything else passing — once as "168 passed" with two files absent.

**PARALLEL-LANE VERSION COLLISION, TWICE.** A and C both claimed contract 1.17.0 and **GitHub reported BOTH `MERGEABLE`** (it compares each PR against main, never against each other). D2 then declared 1.17.0 while its report said 1.18.0 — both already published — and its engine PR pinned EXACT `"1.18.0"`, a DIFFERENT rail's contract, which would have installed a package lacking the type it registers. **Assign versions UP FRONT in the dispatch or serialize contract merges.**

**Full mid-session capture: `_sessions/2026-08-10_five_rails_and_write_throughput_claude_code.md`.**

**✅ R1 RAIL SPLIT IS LIVE IN PRODUCTION 2026-08-10 — THE CONSOLE NOW SHOWS 14 COLUMNS.** `countyRailRefreshCli --apply` run against the deployment Neon (3 inserts: rrc-wells / rrc-pipelines / rail-corridor; 1 delete: old `rrc`; 2 updates: mud ordinal 12→14, owner flipped to present/hasWriter=true). cortex-api canary `00497-cep` deployed at image `a8318cdf2bac...`, smoked at 0% traffic, then shifted to 100%. **LIVE NOW: `totalRails: 14`, `totalCells: 3556`, `texasCompletenessPct: 0.7689%`** (was 12 / 3,048 / 0.897%). The drop is the denominator growing, not a regression — same 89 satisfied cells against an honest grid.

**Per-rail display states on the live grid confirm the manifest is telling the truth about each rail's REAL state:** `owner` = `not-yet` ×254 (atom family present, writer wired, awaiting the apply) while `rrc-wells` / `rrc-pipelines` / `rail-corridor` / `mud` = `no-atom` ×254. The distinction between "built but unapplied" and "not built" is now visible on the console instead of hidden.

**DEPLOY GOTCHA (mold this):** the cortex-api Artifact Registry tags are FULL 40-char SHAs. Passing the 8-char prefix `a8318cdf` to `deploy-canary` fails with `Image ... not found` and burns a run. Use `git rev-parse origin/main`.

**RAILS A + C LANDED 2026-08-10 — TWO NEW ATOM FAMILIES REGISTERED, BUT ZERO COVERAGE UNTIL THE APPLY.** `rail-corridor-fact` (contract **1.17.0**, eng #299) and `well-fact` (contract **1.18.0**, eng #300) are both merged and registered; `PROPERTY_ENTITY_TYPES` is now **13**. Sources: rail-corridor = NTAD NARN (16,522 TX segments + 16,102 grade crossings, public domain, July 2026 vintage; FRA direct was DOWN at probe and TxDOT is TX-only with 13,155 segments — both correctly rejected). rrc-wells = the RRC public-GIS Harris mirror already wired in `lib/adapters/src/federal/texas-rrc.ts`. Both carry a NAMED buffer/radius in the atom body (152.4 m / 500 ft) so the adjacency claim is auditable, and both subcategorize via body fields rather than new rails, per the R1 split rule.

**⚠ PARALLEL-LANE VERSION COLLISION — THE LESSON FOR EVERY FUTURE FAN-OUT.** A and C were built simultaneously and **BOTH claimed contract 1.17.0**, both touching the same four files (`package.json`, `property/index.ts`, `property/fixtures.ts`, `property/__tests__/property.test.ts`). **GitHub reported BOTH `MERGEABLE`** because it compares each PR against main independently and cannot see that two PRs collide with each other. Resolution: C merged as 1.17.0, A rebased to **1.18.0**; the engine pin `^1.17.0` semver-resolves 1.18.0 so one range covers both. **When fanning parallel lanes that touch a shared package, assign versions UP FRONT in the dispatch, or serialize the contract merges.** Do not trust `mergeable` as evidence that two open PRs are compatible.

**Three resolution defects caught by verification, not by the merge** (record these — naive conflict resolution produces syntactically-plausible garbage): keep-both-sides concatenation produced `| "rail-corridor-fact";` followed by `| "well-fact";` (a semicolon mid-union, twice); it spliced a type alias so `RailCorridorFactAtomInstance`'s body became the well-fact doc comment, leaving the alias unterminated; and `parcel-node-registration.test.ts` kept LIVE conflict markers that surfaced as an **esbuild transform failure**, not a test failure — the file never loaded, so a green-looking "168 passed" was hiding a file that never ran. The count guard moved 12→13 deliberately and did its job. Final: `pnpm -w typecheck` clean, **184 atoms tests**, **277 contract tests**.

**Dry-run reality check (do not read these as coverage):** A returned 0 present atoms on both 48113 and 48029 — the Harris-mirror wells layer has no features in those bboxes, which is honest, not a defect. C returned 21/200 near on 48021 and 16/200 near on 48113. **No `--apply` was run for either rail; the atoms slot is still the sweep's.**

**R3 RULED 2026-08-10: OWN IS PRE-LAUNCH-GATE SCOPE.** Owner must be done before the Texas-flush launch gate closes. Build chain is COMPLETE (O1 contract 1.16.0 / O2 registration / O3 county writer, all merged); **what remains for R3 satisfaction is the APPLY across the 15 CAD counties, which needs the atoms bulk-writer slot** — currently held by the parcel-node sweep. Sequence: sweep closes → slot handoff recorded here → `write-owner-fact-county --apply` per CAD county → coverage scorer → OWN cells flip off zero. The 15 CAD counties are a SUBSET of the 196 geometry counties, so OWN can never reach 100% of 254 — the gate must read OWN as "done where CAD exists", not as full statewide coverage, or it can never close.

**K LANE CLOSED THROUGH K5 — STATEWIDE TILES LIVE (agent report received 2026-08-10, planner-verified at source).** `parcels.b692c6534d26.pmtiles` — **13,710,413 features / 196 counties / 2.96 GiB** (3,175,452,566 bytes), ~7 h bake. Planner-verified independently: GCS HEAD 200 with that exact byte count, **range request 206** with `Accept-Ranges: bytes`, `cache-control immutable`, and — the check the agent report did NOT make — **CORS from the PE origin returns `Access-Control-Allow-Origin: *` and exposes `Content-Range`**, which is the header MapLibre actually needs and which curl alone would never have caught. PE prod bundle `index-NZimi-_b.js` verified to contain `b692c6534d26` and **zero** occurrences of the old `3431529a2e8d`. **DEFECT THE REPORT BURIED: the K5 code changes were UNCOMMITTED, on the already-merged branch `fix/cc-manifest-panel-gaps`.** Vercel serves the built bundle so prod was correct, but the source of truth did not carry the change — any redeploy from a clean checkout would have silently reverted PE to the 19-county tileset with no PR trail. Same class as the workflow-env-revert trap: correct live state resting on something not durable. Fixed: changes moved to `feat/statewide-parcel-tiles` off main, **hauska-map PR #157** open. Rollback remains `VITE_PARCEL_PMTILES_HASH=3431529a2e8d` + redeploy. **R6 (operator browses the statewide map) is the remaining gate** — Harris west of −95.96, Bastrop regression, Dallas/Valley/Panhandle sanity.

**R1 + R2 + R4 RULED AND BUILT 2026-08-09 — RAIL SPLIT 12 → 14 (ldt PR #407, CI: Rubric SUCCESS / Typecheck SUCCESS / Test in-progress at time of writing — DO NOT MERGE until Test reports the conclusion string SUCCESS).** Operator ruled: make it three rails, rail-corridor now. `rrc` splits into **`rrc-wells`** (point) + **`rrc-pipelines`** (line); **`rail-corridor`** added (ordinal 13); `mud` shifts to 14. **DENOMINATOR MOVES 254×12 = 3,048 → 254×14 = 3,556 cells**, so every completeness number quoted before this changes: cell ratio 2.92% → 2.50%, parcel-weighted headline ~0.897% → **~0.78%**. Nothing regressed — the grid stopped hiding two layers we always owed. All three new rails ship `hasWriter=false` so the cells read honestly as gaps.

**THE SPLIT RULE (recorded in the declaration header so the list is extended the same way next time): SPLIT where the SOURCE and GEOMETRY differ; SUBCATEGORIZE via atom body fields where only the ATTRIBUTE differs.** Producing-vs-plugged is an attribute and stays inside the cell.

**R2 RESOLVED FROM LIVE CODE, not from a guess:** `lib/adapters/src/federal/texas-rrc.ts` ALREADY fetches two distinct ArcGIS endpoints (`TEXAS_RRC_WELLS_LAYER` = TXRRC/Wells/MapServer/0, `TEXAS_RRC_PIPELINES_LAYER` = TXRRC/Pipelines/MapServer/0, a Harris County mirror carrying statewide coverage) and `brokerageGisFederalLayers.ts` ALREADY tags each feature `rrcAsset: 'well' | 'pipeline'`. The data arrives split — the rail split matches the source shape rather than imposing on it. **PHMSA NPMS is NOT needed for v1** (it restricts precise alignment for security reasons). Pipeline fields already pulled: P5_NUM, OPER_NM, SYS_NM, COM_CARRIE.

**R4 RATIFIED AND ENFORCED IN THREE LAYERS** (not merely documented): contract schema pins `public-paid` and rejects anything else; the writer fail-closes before any write; `verifyStoredOwnerFactAtom` re-checks the STORED bytes so a row mutated in place is a verify failure. Exemption codes reduce to four booleans inside the writer seam and never reach an atom.

**O4 DONE in the same PR:** `owner` flipped `missing`/false → **`present`/true** (`owner-fact` contract 1.16.0, writerRef `write-owner-fact-county.mjs`). Owner coverage stays 0 until the apply runs — the apply needs the atoms slot.

**No migration needed:** `countyRailRefreshCli` reconciles `county_rail` against the declaration (INSERT/UPDATE/DELETE) and `readManifestGrid` is a `CROSS JOIN county_rail`, so cells materialize automatically. **The refresh CLI run is the deploy step after merge**, then cortex-api redeploy to serve the new denominator. **R3 (is OWN launch-gate scope) is the ONLY ruling still owed.**

**OPS-15 OWN + RRC RAIL GAP ANALYSIS (2026-08-09, scoped, R1–R4 owed):** `90_operations/OPS-15_owner_and_rrc_rail_gap_analysis.md`. Operator-initiated off the County Manifest screenshot (OWN `NO ATOM`, RRC `HALF`+`NO WRITER`). Verified at source: **OWN data is already ours** — `cad_property` carries `owner_name` on **4,525,073 / 4,599,477 rows (98.4%), 15 counties**, plus mailing/exemptions/value stack; there is NO owner atom, so "we don't display owners by default" is not yet true — nothing is served at all. **RRC has ZERO O&G atoms in the property spine** (live `atoms` GROUP BY entity_type: 15 types, none from ADR-025), so the declaration's `atomFamilyState: "partial"` is STALE-OPTIMISTIC against this store — the ADR-025 shapes DID ship (`@empressaio/atom-contract@1.15.0` exports `./og`) but are written nowhere; `og-twin` last pushed 2026-07-08. **RRC ≠ railroad tracks** — the rail declaration's "RRC" is the Texas Railroad Commission (oil/gas regulator); the operator's "rr pats and geometry" is rail-corridor infrastructure, a genuinely NEW rail. Do not let the name collide. Proposed restructure: split at the source-and-geometry boundary into `rrc-wells` (point) + `rrc-pipelines` (line) + `rail-corridor` (line+ROW), subcategorize WITHIN each via body fields (plugged/disposal/orphaned; carrier/status; active/abandoned/rail-trail) — **12 rails → 14, denominator 3,048 → 3,556 cells, which moves every completeness number quoted to date.** ADR-025's land/capital lenses (leases, interests, DOI, obligations, production) stay OUT — per-jurisdiction-assembly + tenant-private, HELD per the 2026-08-01 scale ruling; that is the og-twin vertical, not the county manifest. **Owner is launch-gate scope as the gate is currently written** (gate = "all 12 rails with writers"; OWN has no atom family) — R3 asks whether that was intended.

**OWN LANE — O1 + O2 DONE 2026-08-09 (operator-sequenced: OWN first, RRC after county backfill).** O1: `owner-fact` contract type MERGED (contract PR #15, `4ab1185`) and **`@empressaio/atom-contract@1.16.0` PUBLISHED on npm** (tagged from merged main, not from the branch — the 1.15.0 lesson); published tarball verified to carry `dist/property/owner-fact.{js,d.ts}`. O2: engine registration + writer seam MERGED (**eng #296**, main tip `f8472e1`) — `PROPERTY_ENTITY_TYPES` 10→11, `owner-fact-writer.ts`, contract dep bumped across nine sites + lockfile. **`owner-fact` is the ONLY non-`public-free` property type**: the contract schema pins `public-paid` and rejects anything else, so the R4 privacy posture is STRUCTURAL, not documented — a writer cannot leak owner identity onto the free tier, and both absence shapes carry it too (an absence still reveals that we looked). Exemption codes are reduced to four BOOLEAN FLAGS inside the writer seam before reaching an atom (homestead/over-65 codes imply occupancy and household composition), prefix-matched not exact-matched so a local variant cannot silently produce a false "no homestead". `owner-withheld` is a first-class absence kind for Texas statutory confidentiality elections. Tests: contract 265 green (8 new), atoms 178 green (9 new), `pnpm -w typecheck` clean. **O3 BUILT + LIVE-DRY-RUN CLEAN (branch `feat/owner-fact-county-writer`, built in worktree `P:/tmp/own-o3-wt` — NOT the live tree, per the break above).** `write-owner-fact-county.mjs` + `src/owner-fact/` module (planner / atoms / verify), mirroring the land-use writer: app-code join on one neondb pool, dry-run-predicts-apply, batched write-then-verify, `OWNER_FACT_PATH=1` guard. Three things it does that siblings do not: (1) **fail-closed paid gate BEFORE any write** — atoms scanned for non-`public-paid` and the run aborts rather than persisting; `verifyStoredOwnerFactAtom` re-checks the STORED bytes so a row mutated in place is a verify failure; (2) **no PII in logs** — the dry-run sample prints `hasOwnerName`/`hasMailingAddress` booleans, never the name or address; (3) **`owner-withheld` is NEVER inferred** — a blank owner is the weaker true claim `no-owner-name`, and only a district's PUBLISHED suppression list (`withheldKeys`) promotes a parcel to withheld, because guessing would fabricate a legal status. **LIVE DRY-RUN 48021 (read-only, no slot taken): 77,073 CAD rows @ taxYear 2025 all with owners; 200/200 present, 200 with mailing address, 0 absences, 0 errors.** 16 planner tests green (7 new); tsc clean. **APPLY STILL NEEDS THE ATOMS SLOT — held by the sweep. Do not apply until the sweep closes.**

**Hook fix same session:** the dirty-tree close gate blocked a `hauska-engine` worktree push because "no `cd` in the command" was treated as evidence of doc_repo. Now falls back to the shell's actual CWD. Verified both directions: other-repo push passes, doc_repo push with dirty `_STATE.md` still blocks. R1–R4 still owed and do NOT block O3 build; O4 (rail declaration flip) DOES depend on R1.

**⚠ PLANNER-CAUSED SWEEP BREAK 2026-08-10T01:03Z — THE LESSON: A RUNNING LANE EXECUTES YOUR WORKING TREE.** The sweep halted at 48277 `apply-failed` with `SyntaxError: ... does not provide an export named 'OWNER_EXEMPTION_FLAGS_SCHEMA'`. Cause: OWN item O2 edits to `packages/atoms/src/property-instances.ts` in `P:/hauska-engine` — the SAME checkout the sweep runs `tsx` against. `pnpm install --lockfile-only` rewrote the lockfile but never put contract 1.16.0 in `node_modules`, so the next county's apply imported a symbol that did not exist yet. **An edit in a repo with a long-running lane is a live deploy to that lane.** Either work in a worktree, or accept that every save is executed by whatever is running. Fail-closed behavior HELD: 48277 wrote **zero** rows (verified by SQL, not by the artifact), `atomsWritten: null`, no partial state; 60 counties landed clean. Fix: real `pnpm install`, then re-smoked the writer entry point by EXECUTING it (`--county=48277 --limit=1`) rather than trusting tsc. Halt cleared with reasoning in `progress.json.haltCleared[1]`; 48277 absent from `landed` so it re-entered the queue rather than being skipped. Sweep resumed 2026-08-10T01:1xZ.

**Metro tail warning:** the remaining queue is smallest-first, so the last five counties are Travis 828,773 / Tarrant 757,161 / Bexar 709,541 / Dallas 694,160 / Collin 407,126 features — landed counties averaged ~31k, so ~3x all work-to-date sits in five counties. Expect the tail to dominate wall time; do not read a slow tail as a hang.

**CLEANUP BATCH CLOSED 2026-08-09 + OPS-14 follow-up lanes F1–F6 (2026-08-09 evening):** #404/#403/#406 merged earlier; **#393 MERGED** `a7d8da8a` (Test SUCCESS @ `45cf0e8b`); **cortex-api REDEPLOYED** serving `cortex-api-00494-gok` @100% tag `canary` (shift-traffic run 31331678002). **Geometry scorer APPLIED 2026-08-09T22:12Z:** live ledger `satisfiedCells=89`, `texasCompletenessPct=0.897%` (was 38 / 0.213%); close `_inbox/2026-08-09_W1_D0_geometry_scorer_CLOSE.md`. **Parcel-node sweep HALTED** at 48457 boundary (~80 counties remain); resume after H6 slot return. **Nueces 48355:** SOURCE_DEFECT recorded (`_inbox/2026-08-09_Nueces_48355_manifest_honest_absence.md`). **Donley:** outreach draft at `_inbox/2026-08-09_F6_donley_cad_outreach_draft.md` (operator sends). **Cert 48021:34177:** fix = re-promote then merge #292 — report `_inbox/2026-08-09_F3_34177_root_cause.md`. **Roads statewide:** all six unblockers still OPEN — status `_inbox/2026-08-09_F5_roads_unblock_status.md`.

## THE FOUR NUMBERS THAT MATTER

| Measure | Value | Where to verify |
|---|---|---|
| Counties with parcel geometry | **196 / 254** | `SELECT count(DISTINCT county_fips) FROM txgio_parcel` on `DEPLOYMENT_DATABASE_URL` |
| Parcel rows | **~15,479,206** (Harris reload +1,037,083 net; re-verify before quoting) | same table |
| Counties with `parcel-node` atoms | **119** (~1.66M+ atoms; sweep halted at 48457) | `SELECT count(DISTINCT body->>'countyFips') FROM atoms WHERE entity_type='parcel-node'` on `DATABASE_URL` (atoms DB, database `hauska_mcp`) |
| Texas completeness | **0.897 percent**, 89 satisfied cells of 3,048 | `GET https://cortex-api-tds7av26va-uc.a.run.app/api/county-ledger` |

**Geometry scorer:** applied 2026-08-09 against 119 atom-written counties — 88 geometry cells `satisfied-present`, 31 `not-yet` (below 95%). Re-run on sweep close. Close: `_inbox/2026-08-09_W1_D0_geometry_scorer_CLOSE.md`.

## MULTI-SHAPEFILE TRUNCATION — HARRIS RELOADED (2026-08-09)

**Root cause:** `files.find(/\.shp$/i)` kept the first shapefile; Harris ships east+west; west was discarded silently. Count-based gates could not see it (same truncated input).

**Lane closed with holds.** Report: `_inbox/2026-08-09_MULTI_SHP_TRUNCATION_report.md`. Reader fix PR **#404 OPEN** (`4fafeed3`, `--multi-shp=concat` fail-closed). Sweep of all 254 archives: **Harris is the only multi-`.shp` county** (Donley 404). Harris re-ingested alone: dry=apply **1,523,641** features / **1,602,031** rows. Post-apply geographic proof (`_inbox/multishp_harris_logs/48201_post_apply_geo.json`): westmost **−95.960827** (was −95.4364; Census −95.960733); **769,053** parcels west of −95.44; **26,613** west of −95.80. Atoms: Harris **0** parcel-nodes (no contamination). Adversarial **HOLD** on coastal-as-water narration for eight other short counties (multi-shp ruled out; data holes unresolved) and on `--limit` partial-apply; post-apply geo proof produced after the HOLD. Review: `_inbox/2026-08-09_MULTI_SHP_adversarial_review.md`.

## WHERE THE PROGRAM IS

The operating sequence pivoted from **jurisdiction-first** to **layer-first** (`_decisions/2026-08-08_layer_first_statewide_fabric_sequence.md`): lay the statewide-uniform layers across all of Texas first, then backfill jurisdiction-specific rails into a framework that already has shape. Nine counties were completed jurisdiction-first and 245 were not; the statewide layer inventory found why — NOTHING was statewide-complete, and every layer held was either per-parcel on-demand or per-city hand-scripted. The machine was built to enrich one parcel at a time.

| Layer | State |
|---|---|
| L0 seam reconciliation | mechanical, deferred to read time (the tile bucketing IS the spatial index) |
| L1 city + county boundaries | **DONE, LIVE** — 1,222 city + 254 county polygons |
| L2 parcel geometry | **WAVE 3 COMPLETE — 196 counties.** Harris multi-shp truncation **reloaded** 2026-08-09: 1,602,031 rows, westmost −95.960827 (PR #404 open for reader fail-closed). Sweep: only Harris ships N>1 `.shp`. Eight coastal geographic-short counties remain unresolved (not multi-shp). Bosque owes clean idempotency re-run. Donley 48129 is a 404 at source. Reports: `_inbox/2026-08-09_MULTI_SHP_TRUNCATION_report.md`, `_inbox/2026-08-09_L2_W3R2_RESUME_REPORT.md`. **Concurrency: 1-2 ONLY** (`40P01` on shared index). |
| L3 roads statewide | PBF reader DECIDED (pyosmium sidecar). Product PR **#290 OPEN** (`6ba439a`). Resolver #288 MERGED. One-county Bastrop extract proves PBF≅city Overpass (recall 1.0, +3 service). Peak RSS **2842 MB**. Fixture identity: live Overpass slice = city-bbox (4893). Adversarial **REFUTED** statewide (`_inbox/2026-08-09_STATEWIDE_ROADS_adversarial_review.md`): boundary coin-flip under 1e-18 collinear eps; Node ingest unexecuted; silent upsert would clobber Bastrop; synthetic-id landmine. Report: `_inbox/2026-08-09_STATEWIDE_ROADS_build_prove_report.md`. **No statewide ingest.** |
| L4 FEMA NFHL | ldt #398 MERGED, migration 0071 APPLIED. **LOAD COMPLETE 2026-08-09:** `tx_fema_nfhl_flood_zone` holds **198,178** unique rows (S_FLD_HAZ_AR stream 198,240; 62 source dupes collapsed). Backpressure fix **#403 OPEN** (`streamGeoJsonSeqWithBackpressure` pauses stdout at high-water 32). Primary apply peak RSS **962.6 MB** (flat); crossed prior OOM death line at 116k. Idempotency net change 0. Projection: 0 outside Texas WGS84 degrees envelope. Archive deleted from disk. |
| L4 SSURGO | weakest link; no working gSSURGO bulk URL found |
| L4 topo | 251 tiles / 64.3 GB via USGS 3DEP; existing pipeline is AOI-scoped and would silently produce a Central-Texas mosaic |
| PMTiles bake | **DONE 2026-08-10** — statewide `parcels.b692c6534d26.pmtiles` (13.7M features, 196 counties, 2.96 GiB) live on GCS + PE prod. Rollback hash `3431529a2e8d`. Report: `_inbox/2026-08-10_K6_statewide_pmtiles_bake_report.md`. R6 visual QA owed. |

**THE TWO-FACTORY JOINT — consumption FATAL gaps closed in engine #285 (merged).** Kenedy parcel-node writer #284 merged earlier. Atom families BUILD wave 2026-08-08/09: contract **1.14.0** published (`flood-hazard-fact`, `cad-parcel-roll`, `land-use-fact`); engine registration **PR #286 OPEN** (CI green). Adversarial: PARTIAL HOLD WITH FATAL GAPS — shapes on npm, nothing serves them yet (no writers, no NFHL table apply, no manifest refresh, no MCP slots). Report: `_inbox/2026-08-08_ATOM_families_program_report.md`.

## LIVE NUMBERS (re-verified 2026-08-09 where noted)

- **Texas completeness ~0.0395 percent** (live ledger). **12 rails × 254 = 3,048 cells.** displayState: no-atom **1524**, no-writer **1016**, not-yet 489, satisfied-present 19. One satisfied cell: Bastrop zoning.
- **cad_property LIVE:** **4,599,477 rows / 15 counties** (CORTEX_DATABASE_URL SELECT 2026-08-09). Not 15 rows; not ~1.07M.
- **NFHL bulk table:** LIVE **198,178** rows on deployment Neon (`tx_fema_nfhl_flood_zone`); backpressure fix #403 open.
- **Parcel store:** 19 counties + Kenedy = 20 loaded (earlier session; re-verify before wave planning quotes).
- **Contract `@empressaio/atom-contract@1.14.0`** on npm (tag `v1.14.0`). Engine **main** still registers 7 property types; PR #286 adds three → 10 (not merged).

## GOVERNANCE — the canon gate is LIVE

`.claude/hooks/canon-gate.ps1` on the `Agent|Write` matcher blocks (a) dispatches into repos marked retiring or no-touch, and (b) dispatches missing the hash-pinned `CANON-PREAMBLE` marker, with paste-ready text in the block message. Regenerate after editing the standing decisions below: `node P:/doc_repo/scripts/dispatch-preamble.mjs`.

**M2 divergence detector:** `_catalog/canon_divergence.md` (machine report). Checks: `_catalog/repo_intents_checks.json`. Runner: `node P:/doc_repo/scripts/canon-divergence.mjs`. Cadence hook: `.claude/hooks/canon-divergence-run.ps1` refreshes the report on Read of this file when the report is older than 12 hours (fail-open). Historical replay of the ldt failure: `_inbox/2026-08-08_M2_historical_replay.md`.

Built because `_catalog/repo_intents.md` went 35 days stale while legacy-design-tools took 387 commits and became the factory repo, and the master planner dispatched ten new database tables into a repo the canon declared retiring. **legacy-design-tools is now ruled the FACTORY REPO** (`_decisions/2026-08-08_ldt_is_the_factory_repo.md`); only the root SPA and the Cortex console still retire.

Measured base rate in doc_repo: **hook-shaped controls 1-for-1; protocol-step-shaped 0-for-3.** The session-close grading rung ran 0 of 215 sessions and was DELETED rather than repaired, replaced with a grep-able `memory_graded:` frontmatter field.

## PRIOR HEADLINE (superseded, kept for continuity)

**T1 cohort re-persist (2026-08-08):** Recovery re-pair DONE @ `dba7a82`. Apply promoted 1668; block13 7/7, saga twelve 12/12. The "extended parity 2438 == 1668 + 770" framing is RETRACTED — it is arithmetically true but diagnostically empty (`computePassNotPersisted` is defined as the residual), and the pre-fix dry leg was inflated by the 472 parcels the apply refused. The `!dryRun` compute fork that caused it is CLOSED (engine #279): dry-run now predicts apply for the first time in this program. **Warm runner consolidation OPEN (engine PR #287):** three per-city scripts collapsed into `depth-warm-city-batch.mjs --row-id=…` with registry `warmRunner` config; Bastrop gate-path dry-run counters match legacy 0-mismatch; adversarial **PARTIAL HOLD**. Elgin still queued — needs merge + parcel-node anchors + unified Elgin dry-run before apply. Report: `_inbox/2026-08-08_WARM_RUNNER_CONSOLIDATION_report.md`.

## STANDING DECISIONS (these govern every dispatch — paste into fresh-agent handoffs)

- COTALITY IS EXTINGUISHED — when code hits it (502/OAuth/fallthrough), re-route to county-gis/public-record, NEVER rotate the credential. Regrid also dead.
- DEPLOYS ARE PLANNER-OWNED — the agent deploys and fixes failed deploys; never escalate a deploy to the operator; "failed on X, fixing X".
- NO PRIVILEGED DATA — everything via uniform public-record; any path must work for a no-relationship jurisdiction.
- CTX / national HELD until Bastrop QA-done + operator go.
- CODE-DONE != CUSTOMER-DONE — a grade is a live probe on the deployed surface across multiple different-data parcels, never a merged PR.
- Full standing-decisions detail: `MEMORY.md` (auto-memory) + `_decisions/`.

## LIVE INFRA (serving revisions — verify before quoting; they churn)

- engine-api: `hauska-engine-api-00174-zus` @100% tag `ws1-serve-truth-12` (T1 WS1 2026-08-06: #265 + stale-edge retire + export R28/R30 guard for depth-warm edges; twelve re-persist 12/12 serveTruthOk; image `ws1-serve-truth-12`; `/health` verified). Prior `00171-vol` tag `ws1-fd91b54`. Project hauska-prod-497015.
- retrieval: `hauska-retrieval-api-00061-bib` @100% tag `mcp-p0-auth` (76j-F-P0 2026-08-09: RETRIEVAL_API_KEY←`HAUSKA_ENGINE_API_KEY:latest`; CORPUS_SNAPSHOT_PATH=`/app/services/retrieval-api/corpus/snapshot.json`; MCP catalog 401 eliminated). Prior `00059-lir` tag `pooling-fix`. Project hauska-prod-497015.
- MCP: `hauska-mcp-server-00041-x56` @100% (76j-F-P0 2026-08-09: `probeRetrievalCatalogSeam` auth-seam health probe; gold+fabric chain live-verified). Prior `00040-ctj` tag `postgres-limiter`. Project hauska-prod-497015.
- cortex-api: `cortex-api-00494-gok` @100% tag `canary` (2026-08-09 shift-traffic after deploy-canary 31331609692; picks up #406 ledger breakdown fields — live `satisfiedPresentCells=56`, `satisfiedPresentPartialCells=18`, `satisfiedAbsentCells=0`). Prior serving `cortex-api-00472-web` tag `smoke4`. Project legacy-design-tools-prod.
- CC: `cmdcenter-blush.vercel.app` (redeployed 2026-07-31 Phase 0A `dpl_jxh5onnQ5UsKJGy7uz5DoJhvyyeB`). PE: `property-explorer-xi.vercel.app` with `PROPERTY_ATOM_PATH=1` (redeployed **2026-08-06 T2 WS2 reopen** pedestrian v2 `586ef16` / bundle `index-C1Sc6_H7.js` — `#8fd0ff`, opacity 0.9, larger dots; prior T2 `index-h2GW8147.js`). Deploy from hauska-map ROOT linked to Vercel project `property-explorer`. NOTE: Vercel does NOT auto-deploy on merge.

## WHAT IS DONE + LIVE-VERIFIED

- Depth engine (Bastrop 99.59% place-type), boundary primitive (temporal + adjacency), road-as-node, property-line-tags (26454/26454 honest).
- CC-A legible node/atom flow (but see QA-CC-PORT/BUG below — flow is thinner than the target + a timeout bug).
- Track B customer UI: road render, site-plan design pass, map/PDF vocab one-truth, viewport road network.
- Mold hardening (D1): zoning provenance backfilled (cites real AGOL), health monitors live (/health/spine/run), dual-table hash-lock, contract pin. Bastrop APPROVABLE.
- Topo fidelity: 1-ft LiDAR contours + hydrology + terrain export live (config-to-1m + Contour1Ft2017).
- No-setback export fix + Cotality-decommission (envelope 502 gone, honest 404).
- Recipe generalizes: Caldwell #2 (7 held / 1 new-baked). Recipe-proof track CLOSED.

## OPEN PRs AND THEIR STATE (2026-08-09 — verify with `gh pr checks`, do not trust this list blind)

| PR | State | What it is |
|---|---|---|
| eng **#287** | **MERGED 2026-08-09T18:09Z** (`41cfdb4c`, cleanup lane B) | Unified city-batch warm runner. F3/Elgin unblocked; Elgin still owes parcel-node anchors + unified dry-run before apply. |
| ldt **#404** | **MERGED 2026-08-09T18:05Z** (cleanup lane A) | Multi-shapefile fail-closed reader. |
| ldt **#403** | OPEN, CI green | NFHL backpressure fix; table already loaded to 198,178. |
| ldt **#393** | **MERGED 2026-08-09** `a7d8da8a` (Test SUCCESS @ head `45cf0e8b`) | Observability tables (`rail_state_history`, `rail_verification`, run-state, cost metering). Standing-factory cost gates unblocked. |
| map **#118** | OPEN, NO CI checks, CONFLICTING | Revert hazard (would resurrect pre-#113 flood overlay). Cleanup lane C owns rebase-or-close. |

**MERGED 2026-08-09 after the last close:** eng #289 (contract-shaped envelope absence, 1.15.0), #290 (pyosmium PBF worker), #291 (writers for cad-parcel-roll / land-use-fact / flood-hazard-fact), ldt #401 (geometry rail scorer), #402 (decline defective features with identity), map #156 (absenceBasis rendered). Contract `@empressaio/atom-contract@1.15.0` published — merge-then-tag, after a 1.14.0 tag was pushed from an unmerged branch and npm ran ahead of trunk for hours.

## THE PATTERN A FRESH AGENT MUST KNOW

**Five executors on 2026-08-08 and 2026-08-09 did real, correct work and returned with NO REPORT** — one after downloading 1.81 GB and applying a migration, one after loading 36,000 NFHL rows, one after repairing Wilbarger and advancing the sweep seven counties. CI green, work sound, close-out absent. Each armed a Monitor and stopped.

Consequence: **the planner became the reporting layer**, querying stores and merging PRs instead of planning. If you dispatch a long-running data lane, VERIFY AT SOURCE on your own schedule and do not wait for the agent's summary. Every dispatch should carry the constraint explicitly.

Related: an executor once burned 48,000 tokens writing a briefing for a sub-agent that never ran, and reported it as "dispatched and running." Caught only by checking the live endpoint. Every dispatch now carries a no-nesting clause as its FIRST line.

## OPEN — ACTIVE (what a fresh agent picks up)

### W5 DEPTH FACTORIES — CLOSED 2026-08-09 (operator-authorized)

Dispatch pack: `_dispatches/2026-08-09_W5_depth_factory_dispatch_pack.md`. Program close: `_inbox/2026-08-09_W5_program_close.json`.

| Lane | Factory | Verdict | Evidence |
|---|---|---|---|
| E1 | F2 corpus (Smithville) | **PARTIAL CLOSE** — corpus PASS | `_inbox/2026-08-09_E1_lane_close.json`; stamp honest-absent |
| E2 | F1 CAD registry tranche 1 | **CLOSED PASS** | 35 rows; `_inbox/2026-08-09_E2_lane_close.json` |
| E3 | F3 Elgin dry-run | **DRY-RUN PASS (apply HOLD)** | Post-anchor run: 499/500 anchors, verifyPass=75 verifyFail=98; `_inbox/2026-08-09_E3_elgin_dryrun_artifact_post_anchors.json`; E3-ADV re-review pending; apply waits H6 slot return + planner go |

### T3 RAILS — FOOTPRINTS + EASEMENTS (2026-08-05 track close — spec DONE, live BLOCKED)

Catch-up program T3 executed end-to-end for recon/spec/recipe/Phase 2 plan. Close report: `_inbox/2026-08-05_T3_track_close_report.md`. **Key finding:** BCAD operator-visible "footprints" are EagleView ortho + CAMA tabular sqft, not public vector REST; **0/11** onboarded counties have CAD footprint layers — Microsoft Global ML fallback is the default rail (`sourceTier=ml-derived`). Easements: county **honest-absence** everywhere except McLennan CAD easement linework (44k/16k); City of Bastrop municipal easements (148 polys) for city limits only. **ADR-029 ACCEPTED** (`_decisions/2026-08-05_adr029_rails_rulings.md`). **Slot-independent build DONE** (`_inbox/2026-08-05_T3_unblock_build_status.md`): contract **merged main** PR #11 (`335a462`); **npm publish 1.12.0 HELD** — ADR-028 field groups not in published lineage (`_inbox/2026-08-05_T3_adr028_publish_gate_verdict.md`); engine apply mint + retrieval chain on `feat/t3-footprint-easement-overlays`; PE overlays + BFF facets on branches (PRs pending). **Apply BLOCKED:** Slot 1/48021 granted but queued behind T1 WS1 — wait for explicit slot release; `--apply` fail-closed on `T3_SLOT_RELEASED=1`. **Next:** open/merge engine+map+ldt PRs, resolve ADR-028 publish gate, full-county dry-run, then pilot apply → Jones/Higgins cert.

### SPINE-LEDGER REMAINING FIXES — HANDBACK (2026-08-01) — PLANNER MERGE/DEPLOY OWNED

Coordinator handback: `_inbox/2026-08-01_spine_ledger_remaining_fixes_HANDBACK.md`. Monitoring draft (not applied): `_inbox/2026-08-01_spine_ledger_monitoring_draft.md`. Four PRs open, CI green on HEAD, coordinator-verified; **no merges/deploys/traffic/monitoring-apply by coordinator**.
- Lane 1 MCP `/health/ready`: hauska-mcp-server [#55](https://github.com/empressaioemail-tech/hauska-mcp-server/pull/55) @ `457e26d`
- Lane 2 cortex functional health: LDT [#371](https://github.com/empressaioemail-tech/legacy-design-tools/pull/371) @ `cdf44f18`
- Lane 3 substrate fail-closed observable: LDT [#372](https://github.com/empressaioemail-tech/legacy-design-tools/pull/372) @ `b175a0e9`
- Lane 4 SmartCity empty-success + scraper health: smartcity-os [#32](https://github.com/empressaioemail-tech/smartcity-os/pull/32) @ `65eae19`
Pre-deploy live: MCP `/health/ready` 404; cortex `/api/health/ready` SPA HTML catch-all; scraper `/health` IAM 403; dark projects still 0 uptime/policies/channels.

### ⚠ CROSS-SEAT HAZARD — PE PR #118 WOULD REVERT THE FLOOD OVERLAY (flagged 2026-07-30 by the hydro seat)

`hauska-map` PR #118 (`fix/pe-site-plan-no-setback`, BDC setback seat, 100 files, +31,498/-1,160) has **merge-base `d5c175d`** — BEFORE the flood feature existed (pre-#113). It therefore carries `apps/property-explorer/src/browse/flood-map-overlay.ts` **as a NEW FILE containing the OLD FD2 gradient/scrim/animation version**, plus a stale `flood-map-overlay.test.ts`. GitHub already reports it `CONFLICTING` / `DIRTY`, so it cannot merge unrebased. **If it is merged or force-resolved carelessly it silently reverts PRs #114/#116/#117 (and the in-flight FD5 amber restyle).** REQUIRED: rebase #118 onto current `origin/main` and, on conflict in `flood-map-overlay*.ts`, **take main's version wholesale** (the BDC work has no legitimate change to that file — its lane is InspectCard/facets/setback display). Owning seat: BDC/setback. Flagged by: hydro/flood seat.

### HYDRO OVERLAY FD5 (ACTIVE — operator-approved redesign, hydro seat)

Design pack: `P:\tmp\Flood study map rendering redesign` (FloodMap.dc.html + Hydro Overlay Redesign.dc.html). Diagnosis doc: `_inbox/2026-07-30_hydro_vs_fema_rendering_handoff.md` — root cause is GEOMETRY not paint: both mask→GeoJSON converters (`artifacts/hydrology-worker/run.py` `_mask_to_geojson_polygons`, `packages/adapters/src/hydrology/hydrologyNative.ts` `maskToGeoJson`) emit one subsampled SQUARE per grid cell → checkerboard.
- **FD5-engine** #190 **MERGED** (`2b1e57d`) + built (`fd5-2b1e57d`) + **CANARIED at 0% traffic as `00154-gaf`** — geometry fix CONFIRMED WORKING on live smoke of 48021:36249 (144 squares/720 verts → 2 dissolved features/9 verts). Merged with `--admin` override on a RED CI: the sole failure was `depth-warm/__tests__/warm-verify-loop.test.ts:191` "714 Spring warm", which the planner independently reproduced on CLEAN origin/main in a separate worktree — pre-existing, BDC-lane, not hydro. Every hydro test green (maskRegions 16/16, hydrologyNative 9/9). Traced cell EDGES not centres (marching-squares centre-sampling would under-count area ~5% and these polygons feed headline stats); new areas within 0.03% of true masked area vs old emitter wrong in BOTH directions (+4.6% solid / under-count sparse). Also fixed a REAL latent PDF bug: `catchmentBoundaryRings` read only `coordinates[0]`, so dissolved regions' interior holes would have drawn a boundary around area NOT in the catchment. Python worker ships INSIDE the engine-api image (Dockerfile:69) — no separate worker deploy.
- **FD5b HOLD — TRAFFIC NOT SHIFTED (2026-07-30).** Canary smoke found two defects on real data; prod stayed on `00152-nuz` throughout. (1) **PONDING CRITERION NOT CREDIBLE**: `pondedAreaSqFt` 396,134 on a ~398,813 sq ft parcel = **99.3% of the parcel ponding**, narrated as "about 9.1 acres", in the SAME briefing that says the parcel "sits at or near a local high point... drains away from it" — self-contradictory and indefensible. Root cause: native ponding rule ≈ `pondDepth > 0.005` (~5mm), so on slope nearly every cell qualifies. It passed #190's own `ponding ≤ parcel area` honesty test — the test was too weak. (2) **BANDS COLLAPSED**: 2 zone features both `concentration: 0`, basis "modeled catchment extent" — the tiny real catchment (3,223 sq ft ≈ a few 10m cells) made 70th/90th-percentile banding degenerate, and the guard silently fell back to catchment-as-band-0, which reads as a rendering bug. FIX DISPATCHED: `fix/flood-ponding-criterion` — defensible depth/depression criterion (both TS + python backends), coherence tests (high-point parcel cannot report near-total ponding; genuine bowl MUST still report it — fix the criterion, never clamp the number), explicit honest small-catchment banding state, and a test banning self-contradictory briefing prose.
- **FD5b #191 MERGED (`6c87576`) + CANARIED `00155-cor` — STILL HELD AT 0%. It OVER-CORRECTED.** #191 found the old rule was not merely a low threshold but **INVERTED** (ponding rose as accumulation FELL — it ponded hilltops) and the pysheds condition reduced to unconditionally-true; it also fixed a non-converging `fillDepressions` (a real 3m bowl filled 0 at centre, so genuine floods read dry) by switching to priority-flood. New rule: `pondDepth = min(filled − raw, rainfall)`, ponded at ≥0.1m. **Planner two-sided honesty probe on the canary using the CITY's own `SFHA` flag (Zoned_Parcels/83) as ground truth found ponding now reports ZERO EVERYWHERE:** 48021:65702 (SFHA, 5.14ac, catchment 4,567) = 0; 25436 (SFHA, 13.68ac, catchment 58,247) = 0; 33637 (SFHA, 3.73ac, catchment **146,287**) = 0; 36607 (SFHA, 2.63ac, catchment 41,356) = 0; control 36249 (high point) = 0. A 9.5in storm producing zero ponding on FOUR FEMA-designated flood-hazard parcels is not credible and makes flood parcels indistinguishable from a high point. #191's own bowl test passed because its synthetic depression does not represent real 10m/px terrain. GOOD parts of #191 retained: coherent briefing, honest small-catchment banding basis string ("too small to band at this resolution (N DEM cells, under the 12-cell minimum)"), priority-flood convergence. FD5c DISPATCHED `fix/flood-ponding-calibration`: diagnose the real `filled−raw` distribution on Bastrop floodplain terrain (hypothesis: at 10m/px, floodplain inundation is driven by channel stage + low-lying elevation, NOT closed micro-depressions, so depression storage alone structurally cannot represent it), then a physically-defensible model where SFHA parcels pond substantially AND the high point does not — with the ABSOLUTE constraint: no parcel special-casing, no Bastrop constants, and NEVER read the SFHA flag as an input (validation, not input; must generalize to counties with no such layer).
- **FD5c #192 MERGED (`c3c8b73`) + CANARIED `00156-zop` — STILL HELD AT 0%; planner probe says NOT-YET-SHIPPABLE, but for an HONEST reason.** #192 diagnosed the FD5b zero correctly and decisively: on real 10m 3DEP tiles `filled − raw` is EXACTLY ZERO for 90-94% of cells (p50=p90=0.0000m) — a floodplain is not a closed depression, so a depression-only model reports floodplains dry BY CONSTRUCTION (missing mechanism, not a threshold). New model = HAND (Rennó/Nobre) + storm-derived channel stage (rational method → `d=0.27·Q^0.39`), taking the deeper of depression storage and inundation; also fixed grid-border cells treated as impoundments and the channel flooding itself with its own stage. **PLANNER PREMISE CORRECTION (verified independently against the city layer): 48021:36249 — which the planner used as the "high point control" — is itself `SFHA=1` (FEMA Zone A/AO/AE/AE-FLOODWAY). An intuition-picked control was worthless; the agent was right to re-source controls and NOT tune toward the planner's framing.** LIVE PROBE ON CANARY (7 parcels, both polarities, planner-sourced from `Zoned_Parcels/83`): the mechanism FIRES REGIONALLY (`pondedAreaModeledRegionSqFt` 195k-852k, 60 ponding features) but ON-PARCEL is 0 for 6 of 7 — 25436 alone returns 4,740 sqft, which proves the stat is NOT clamped. So the honest read: local-storm ponding rarely intersects a specific small parcel at 10m/px, and the number is defensible but LOW-INFORMATION as a headline. **#192's own 24-parcel FEMA test found the model CANNOT discriminate flood zones (wet 2.0% vs dry 2.6%) and it correctly REFUSED to fit** — in-window contributing area 2-12 ha vs a ~3,000,000 ha Colorado River watershed (5-6 orders of magnitude); every candidate signal ranked in the WRONG direction (AUC 0.71-0.85 with SFHA parcels scoring HIGHER HAND). Payload now ships `riverineFloodHazardModeled: false` + briefing text stating a low number means "no modeled local storm ponding", NOT "outside the floodplain", deferring to the NFHL (verified live in the canary briefing, enforced by tests in BOTH backends). Note: serving path is `library: "pysheds"` (python worker), which DOES carry the calibration — confirmed in `run.py` (HAND + `_channel_stage_meters`).
- **⚠ DEPLOY OWED BY THE DEPLOY-OWNING SEAT (hydro seat is NOT deploying, per operator 2026-07-30): PROD ENGINE IS STILL `00152-nuz`, WHICH PREDATES THE DISSOLVE.** This is why the operator's latest screenshot still shows SQUARES: PE already serves the amber palette (bundle `index-DEunhayO.js`) but the engine serving prod has the OLD square-per-cell geometry. The dissolved-geometry image is BUILT, MERGED and CANARY-PROVEN: image `hauska-engine-api:fd5c-c3c8b73` on revision **`00156-zop` at 0% traffic** (contains #190 dissolve + #191 criterion + #192 HAND calibration). To make the redesign visible: shift traffic to `00156-zop` (or rebuild from main tip), then **RE-REFRESH each parcel** — the cached study GET serves whatever the last refresh stored, so geometry will not change until a refresh runs against the new revision. Deploy discipline: engine images MUST build via `--config cloudbuild.engine-api.yaml` + `_IMAGE` substitution (a bare `--tag` builds the wrong root Dockerfile); canary `--no-traffic --tag` → smoke → shift; the python hydrology worker ships INSIDE the engine-api image (no separate worker deploy).
- **FD6 MERGED (PE #121, `67db89d`) — NOT DEPLOYED (deploy lane is another seat's; hydro seat built + verified only).** Seam mesh ELIMINATED: `pe-flood-zone-line` stroked EVERY zone polygon including edges SHARED with adjacent same-band regions — that shared-edge stroke IS the grid mesh. Removed outright rather than kept as an outer outline, because MapLibre `line` layers have no "outer edge of the union" primitive, so ANY surviving stroke re-meshes wherever two same-band regions abut; `fill-outline-color` pinned ABSENT so the seam cannot return through that channel; the retired layer id is swept by BOTH apply and clear so a pre-FD6 stroke left on a live map is reclaimed. Opacity ramp now fully inside the FEMA 0.4-0.55 envelope (bands 0.45/0.5/0.55 — the medium band was out of range at 0.6) and ponding 0.8 → **0.55**, staying the deepest class via a heavier **2px** `#7a3f12` rim rather than raw alpha. RELIABILITY: `validFeatureGeometry` (type + array coords + all-finite, depth-capped) on all three polygon branches → partial render, NEVER a throw; hardened `fcFeatures`; NaN parcel-ring vertices filtered BEFORE the geometry predicates (a NaN silently poisons every comparison and would drop all flow); `applyFloodMapOverlay` ROLLS BACK if it throws partway (nothing half-drawn claims to be the study); teardown sweeps a superset with per-removal guards, idempotent after a partial apply; **honest empty-ponding state** in FloodTool ("No modeled ponding on this parcel at the design storm — the drainage zones and flow paths below are the result") counted via the SAME validation the map runs, so the dock can never claim ponding the overlay drops. 796 tests green.
- **✔ RESOLVED — the doc's "three unrelated hues (green/orange/brown)" are NOT the flood overlay.** Planner-verified independently in `packages/map-renderer/src/map/gis-map-paint.js` `LAND_USE_COLORS` (~line 85): `singleFamily #2fd07a` (green), `commercial #ff8c1a` (orange), `other #c98f5e` (brown) — the LAND-USE CHOROPLETH on `hauska-parcel-tiles-fill`, drawn directly beneath the flood overlay, base opacity 0.32 rising to 0.6 inspected / **0.92 subject** — i.e. STRONGEST on exactly the parcel the study was run for — bleeding through the semi-transparent amber. On by default (`zoning` in `DEFAULT_VISIBLE_LAYERS`, survives `filterConsumerLayers`). Basemap ruled out (CARTO light_all at `raster-saturation: -0.12` renders no saturated green). **REMAINING WORK CLOSED 2026-07-31 (Phase 0A #122+#123+#124):** zoning DATA off-by-default; live-parcels line-only; taxonomy paint authority; Flood preset no longer competes with choropleth wash.
- **FD6 PAINT CLEANUP + RELIABILITY dispatched (hydro seat, BUILD-ONLY, no deploy):** `feat/pe-flood-paint-cleanup` — the operator's design doc names paint defects that are wrong regardless of geometry: the zone layer strokes EVERY feature (`rgba(168,95,34,0.45)` @0.8) which draws an internal seam MESH (the "visible internal grid lines"), and ponding sits at 0.8 opacity vs the doc's required 0.4-0.55 range. Plus client reliability: honest empty-ponding state, malformed/partial-payload resilience (null geometry, NaN coords → partial render, never a thrown blank map), complete idempotent teardown after a partial apply. NOTE for the record: the doc's "three unrelated hues (green/orange/brown)" — green is almost certainly the ZONING/LAND-USE choropleth showing through, not the flood overlay (agent instructed to confirm and report, NOT to restyle another layer out of lane).
- **OPERATOR DECISION OWED (product, not engineering): local-storm ponding and riverine flood hazard are TWO DIFFERENT PRODUCTS.** We can honestly sell local-storm ponding now, but on-parcel ponding will read 0 for most parcels — a truthful but thin headline. Riverine flood risk (what a buyer actually means by "does it flood") needs a different source (FEMA NFHL depth grids / a watershed-scale model), NOT more tuning of this one. Options for the operator: (a) ship as-is with the honest disclaimer and demote ponding from the headline; (b) re-frame the report around what IS discriminating (flow paths/exits, catchment, drainage lines) and present ponding as secondary; (c) hold the paid report until a riverine source is wired. NO further engine tuning is warranted — the model is at its honest ceiling for this data.
- **LESSON (mold candidate): a paid report's headline number needs a TWO-SIDED live honesty probe against an independent authoritative signal, not just a one-sided sanity bound.** #190's test (`ponding ≤ parcel area`) passed a 99.3%-of-parcel absurdity; #191's tests passed a zero-everywhere absurdity. Both survived green CI. The catch each time was a canary probe on real parcels chosen from the CITY's own hazard designation — cheap, generalizes to any county with a public hazard layer, and belongs in the mold as a gate for every modeled headline stat.
- **FD5-PE** `feat/pe-flood-amber-zones` (hauska-map): hydro moves OFF FEMA blue into the approved AMBER family (`#e8b579`/`#d98a3d`/`#a85f22` bands, `#c46a2b`→`#7a3f12` pooled ponding, dashed `#a85f22` catchment, `#7a3f12` diamond exits) so hydro and FEMA stay legible on the same map; parcel-relevance filter + 6-arrow cap PRESERVED.
- Lane discipline: engine BDC seat touches setbacks/boundary/corpus ONLY (verified: zero hydrology/flood files in #185-#189); hydro seat touches hydrology/flood ONLY.

### C7 — RE-APPS CHIP UX + RADAR USER-AWARE ENTITLEMENT (MERGED — deploy + live verify owed)

**WDLL:** `_inbox/2026-07-31_C7_re_apps_chip_ux_and_radar_entitlement_WDLL.md` (approved). **Merged:** C7b [legacy-design-tools #366](https://github.com/empressaioemail-tech/legacy-design-tools/pull/366) @ `978a6f70` (CI green); C7a [hauska-brief-extension #35](https://github.com/empressaioemail-tech/hauska-brief-extension/pull/35) @ `19d03b9` (manifest **0.6.33**). **Next:** planner deploy cortex-api + extension pack → live verify WDLL 3–11 (chips, pro mode, accordion, lineage, cross-install Max + workspace history).

### TXGIO TERRAIN — ADDITIVE 3D VIZ FOUNDATION (ACTIVE — T-003 gate CLOSED 2026-07-31)

Statewide TxGIO LiDAR/DTM as **additive** terrain-tile layer for 3D map viz — NOT a repoint of live flood/site-plan reports. Accelerates stub viz half (doc 42) + de-risks Phase 1 extrusion anchoring.
- **WDLL:** `_inbox/2026-07-31_txgio_terrain_additive_3d_viz_WDLL.md` (**approved** 2026-07-31)
- **T-003 DONE:** `40j_hauska_map_tile_build_pipeline.md` — parcel bake in `legacy-design-tools/artifacts/api-server/src/parcelsPmtilesBakeCli.ts`; terrain-RGB = sibling in `artifacts/tile-pipeline/`
- **T-008/T-009 MERGED** [legacy-design-tools #367](https://github.com/empressaioemail-tech/legacy-design-tools/pull/367) @ `9c891ced`; **GCS LIVE** `terrain-rgb.09ee4eaa72ca/`
- **T-010 MERGED + PE DEPLOYED** [hauska-map #130](https://github.com/empressaioemail-tech/hauska-map/pull/130) @ `21bde8e` → **https://property-explorer-xi.vercel.app**
- **Pitch-gate #131** + **hillshade relief #132** merged 2026-07-31 — redeploy PE for QA
- **Operator QA:** enable 3D terrain → hillshade + tilt; downtown subtle (~6 m @ 1:1); pan west to river bluff for obvious depth; extrusion screenshot grades WDLL 5
- **Safety:** additive-only; flood/drainage stays on 3DEP/contours; datum trap NGVD29 vs NAVD88 on any future migration
- **Extrusion probe:** MapLibre 5.24 shader anchors at centroid — T-010 must live-verify before Phase 1/3 cert

### TX AUTHORITATIVE SOURCE REGISTRY + SCRAPER FLEET (ACTIVE — Batch 1 CAPCOG close)

PRE-onboarding ONLY (no corpus ingest/bake/warm). WDLL: `_inbox/2026-07-29_tx_authoritative_source_registry_and_scraper_fleet_WDLL.md`.
- Track A **DONE**: `_catalog/tx_jurisdiction_source_registry.json` (55/55 attempted; 54 complete / 5 gaps) + `_catalog/tx_jurisdiction_source_registry_coverage_summary.md`. Seed `_catalog/tx_capcog_batch1_seed.json`.
- Track B1 Smithville eCode360: **PASS** (planner fidelity regrade 100%/0 missing spans; 836 sections; 12,793 blocks). Branch `feat/ecode360-scraper-header-first` uncommitted.
- Track B2 Pflugerville: **STOPPED** (encodeplus robots Disallow `/regs/`). `muni-site-scraper` pattern landed; city PDF/open-records path owed.
- Commits not yet cut — await operator go.

### BASTROP DOWNTOWN DRILL — CLOSE CLASS (2026-07-30)

**CERT REVOKED** (prior 37-list false pass); **rendered-set re-sweep PASS 36/36** after R13/R14/R15 class fixes.  
**WDLL:** `_inbox/2026-07-30_BASTROP_DOWNTOWN_DRILL_WDLL.md` (approved).  
**Test area:** `_catalog/bastrop_downtown_drill_test_area.json` — **39 seed** / **36 rendered** in bbox (`cert_scope: rendered-set-r14`); 34065 retired → successors 8741972/8741973/8741974; 8723767 split-zoned honest-decline.  
**Dispatches:** `_dispatches/2026-07-30_BDC_CLOSE_CLASS_R13_R15` **CLOSED-VERIFIED**; prior 2-parcel close `_dispatches/2026-07-30_BDC_2PARCEL_CLOSE`.  
**Audit:** `_inbox/2026-07-30_BASTROP_DOWNTOWN_DRILL_area_sweep_audit.md` **PASS 36/36 rendered set** (2026-07-30).  
**Class fixes:** R13 repealed descriptor-fixture path unreachable (fail-closed honest-decline); R14 sweep = BCAD bbox rendered set; R15 full Pecan Place successor set + manifest lookup fail-closed.  
**8723767:** split-zoned GC+SF-1; warm geometry fails (null inset); PE serves `setback-rule-pending` honest-decline — NOT repealed F15.  
**Blast radius (flagged):** county-wide ~10k+ parcels now fail-closed without layer-23 atoms — county-wide re-warm is follow-on (all-Bastrop expansion, NOT this pass).  
**Operator live block-QA (R6) owed** — click non-obvious/successor/split-zoned parcels before cert restore. CTX HELD.

### BASTROP DOWNTOWN DRILL — prior 37-list pass (SUPERSEDED 2026-07-30)

False cert: curated 37-list passed while map rendered 8723767 with repealed code. Superseded by rendered-set gate above.

### BASTROP BDC + SETBACK CORRECTION — prior cert (REVOKED 2026-07-30)

Superseded by downtown drill above. Prior audit revoked; do not cite CERTIFIED-CLEAN as current.

### Prior OPEN (still true unless superseded)

2026-07-28 QA cluster RESOLVED (all merged + deployed + live-verified same day; verification evidence in _sessions/2026-07-28):
- B1 EXPORT: FIXED. MCP timeout 30s→50s deployed (rev 00033-khs); live MCP download of gold-parcel PDF = 200 in 5.8s (591KB real PDF). PE honest-timeout classes live. Residual: customer-click confirm by operator (needs signed-in paid session).
- F HYDROLOGY: FIXED + live-verified on PE prod, same bbox: 40.6s/1379 channels → 6.3s/169 channels, status ok. Worst-case cold start can still exceed 60s once; UI degrades honestly and recovers.
- G INSPECT CARD: deployed; live facets payload confirmed carrying landUse (A1 cad-roll) + acreage (0.1886 shoelace-wgs84) on 48021:28286; deriver now renders present values with provenance captions. Visual confirm owed by operator.
- C+D CC NAV: /nodes live-verified end-to-end THROUGH the CC proxy (q=28286 finds gold parcel in 2.3s; roads roster with display names; gold-parcel detail 195ms — was 20s-timeout). CC bundle carries nav action + list + label changes. Flow-level UI walk owed (browser).
- A BRIEF: deployed (bundle markers verified). Rendered-brief visual + PDF-export confirm owed by operator (needs paid session).
- E LABEL: deployed in CC bundle ("unincorporated" framing live).

Original blocker notes (superseded, kept for context):
1. SITE-PLAN EXPORT block — NOT auth. Root cause (live-verified): MCP engine-api-client DEFAULT_TIMEOUT_MS=30s; warm refresh measured 23.2s live, cold exceeds 30s → abort → "Engine API unreachable … requires engine-api" → PE classifyEngineFailure pattern-matches that string into the GATE class → misleading "needs an engine-api gate token" message. PE prod env + deploy are fine (prod at main tip w/ #88+#90; MCP_PRODUCT_KEY etc. all set; both services share GATE_CONTEXT_SIGNING_KEY secret; engine gate mode defaults to log). Fix in flight: MCP timeout 50s + PE honest-timeout classification (branches fix/siteplan-engine-timeout on hauska-mcp-server, fix/pe-export-honest-timeout on hauska-map).
2. HYDROLOGY 504 — operator hypothesis CONFIRMED: default DEM went to 1m (usgs3dep.ts), hydrology-flow slot reuses the shared raster plan (fetches 1m it doesn't need), accumulationThreshold fixed at 50 cells regardless of resolution, pysheds worker budget 120s vs PE Vercel fn cap 60s → upstream 504. Fix in flight: hydrology-specific 10m resolution floor + resolution-scaled threshold (branch fix/hydrology-resolution-floor on hauska-engine).
3. LAND USE + ACREAGE — data IS in the fetched baked payload; deriveBakedCardModel suppresses present values when facetCoverage flag is false; live branch has no acreage row at all. Fix in flight (branch fix/pe-inspect-landuse-acreage).

DISPATCH STATE 2026-07-28 (planner session, six executor agents in parallel worktrees; planner merges on green CI, deploys, live-verifies):
- W1 fix/siteplan-engine-timeout (mcp) + fix/pe-export-honest-timeout (map) — B1 export unblock.
- W2 feat/retrieval-node-list (engine) — GET /nodes list endpoint + jsonb expression indexes migration (also hardens gold-parcel inspect timeout). Contract pinned in dispatch; planner must APPLY MIGRATION to substrate Neon (hauska_mcp) before live-verify (merged ≠ applied).
- W3 qa/cc-nav-linkage (map/CC) — map-click→focus Node&Graph, real node list + multi-id search + back-nav (Trading port), getJson timeout alignment, 9.27%-zoned label reframe. Feature-detects /nodes (honest-empty fallback if endpoint not deployed yet).
- W4 fix/hydrology-resolution-floor (engine) — hydrology 10m floor + threshold scaling.
- W5 fix/pe-inspect-landuse-acreage (map/PE) — inspect-card land use + acreage surfacing.
- W6 feat/pe-brief-alder-render (map/PE) — FLAGSHIP: Alder-style brief renderer replacing raw-JSON dump (4-section R1 contract from cortex propertyExplorer.ts buildR1Brief), close + print-CSS PDF export, no fabrication (field→payload scrub table in PR).
- B2 (drawings+aerial with site plan) DONE + LIVE-VERIFIED: #166 (aerial page 3) + #167 (1024px cap) + #168 (cached-LOD resolution floor 0.33 merc-units/px — the REAL constraint: Esri export 500s finer than level 19 ≈ 0.3 units/px, found by live bisection; pixel count alone was a red herring). Gold-parcel smoke on serving revision: refresh 201 with aerialImageryEmbedded:true, 3-page 508KB PDF, embedded 238x263 aerial extracted + visually confirmed (201 Maynard St). Honest-unavailable panel verified working when the fetch fails.
- Deploy notes: retrieval-api = `gcloud run deploy hauska-retrieval-api --source .` from engine root (env preserved when flags omitted; canary tag + smoke + shift); MCP = cloudbuild-mcp.yaml; migration apply = packages/storage/scripts/apply-migration.mjs w/ DATABASE_URL from Secret Manager (check script covers new migration file).

(The 2026-07-28 spec's QA/PORT cluster items A-G + B2 are ALL RESOLVED — see the block above. Spec kept at `_inbox/2026-07-28_pe_cc_qa_and_reports_spec.md` for history.)

QA register status (register: `_inbox/2026-07-27_bastrop_qa_defect_register.md`):
- QA-CC-PORT: SHIPPED (#94 CC nav/linkage + #165 /nodes endpoint; live-verified with screenshots). Residual polish only if operator flags it.
- QA-CC-BUG: inspect-timeout FIXED (migration 008 indexes; gold-parcel detail 195ms live, was 20s-timeout). Tally freshness: live tally loads (~20-45s cold on the stats query); artifact fallback + STALE banner remain the honest degrade — not reworked, flag if operator wants the stats query made fast.
- QA2.2 (site-plan template-match): WAS ALREADY DONE — PR #159 squash-merged 2026-07-27 (cf7e700); the ledger's "in-flight branch" read was the squash-merge false-stranded signal (branch showed unmerged commits; `gh pr view` showed MERGED). Remote branch deleted 2026-07-28 to stop the false signal. Verify PR state via gh, not branch commits.
- More QA polish changes: operator resuming QA 2026-07-28; findings incoming.
- SETBACK HOLISTIC DIAGNOSIS (2026-07-28 evening, planner + 2 recons + live-atom probes): data chain is RIGHT (descriptor + setback atoms carry front:15 + side/rear not_specified honestly); the site-plan EXPORT ran its own pre-primitive per-edge geometry (vertex-count branch: n==4 geometric front guess, n!=4 uniform-min FABRICATION of side/rear from the front value) + false-degenerate gates on jog edges + a header regex fallback printing a warm number over a degenerate drawing. A FABRICATED FIXTURE ROAD (48021:road:123456789 "Spring Street", seeded by a pilot ingest defaulting to fixture against live DB) was IN THE PRODUCTION STORE poisoning frontage/front-edge anchors — planner DELETED it (verified 0 remaining; other Spring Streets are real). Master review caught the first fix attempt hardening the old per-edge path — STOPPED; re-dispatched as fix/export-consumes-boundary-primitive (export routes through the boundary primitive; one truth by construction; reconciliation gate). Build-to-line RULING ratified: not_specified side/rear = zero inset + provisional label (in mold). ACREAGE: store+cortex endpoint are 100% complete (live-verified); the card's "not verified here" is the ATOM-PATH card flow hardcoding coverage false (atom-chain-to-facets.ts) — fix in the W14 UX cluster.
- SETBACK CLUSTER RESOLVED (2026-07-29 early AM): #172 merged+deployed (engine 00126) — export consumes the boundary primitive (reconciliation gate: one computation); LIVE-VERIFIED on all four evidence parcels: 34177 false-collapse GONE (real 15,414 SF envelope, header==drawing, PECAN/PINE STREET labels back), 34737 map==export (19,907 vs atom 19,906, front-only on the primitive front edge). Old-heuristic wrong-front-edge defect (14,629 vs 13,632 on 4-edge rings) also killed. RESIDUAL (honest): export vs elder promoted-atom areas can skew by data vintage (34177: 15,414 now vs 16,046 promoted 07-26) — converges on next depth-warm re-promote since both read the same primitive; ALSO the stored front-edge ROLE for 34177 faces Pine (not Pecan situs) — per-parcel role adjudication is a primitive-data question, now uniform across all surfaces. W14 UX cluster (#95) MERGED+DEPLOYED to PE prod: unified toolset, transient chips (honesty states persist as layer-row dots), true layer toggles incl. zoning-fill unbind, personas removed, brief docked top-center, atom-path base-facts merge LIVE-VERIFIED (34729 card now serves A1 + 0.263 ac). W13 finding: facet store 100% acreage-complete; card gap was the atom-path hardcode (fixed in #95).
- SHEET STANDARD v1.0 SHIPPED (2026-07-28 evening): operator's binding 20-rule site-plan renderer standard (design pack at P:\tmp\Siteplan style template design (1)) implemented as engine PR #169 (+2831/-888; Barlow fonts replace Inter; new deps pngjs/@types/pngjs; duotone aerial; collision cascade + draw-once MarkRegistry; checklist-as-tests on gold+degenerate fixtures; standard committed in-repo at engine-core/src/site-plan/pdf/SHEET_STANDARD_v1.html). Live-verified on rev 00117 (gold parcel, 3-page PDF rendered + graded). REFINEMENT ROUND 1 (operator finding: text crowding rules page-wide) shipped as #170 / Standard v1.1: §21 VERTICAL RHYTHM — metrics-driven lineBox() from fontkit (13px body: rule→cap 13.3px, was ~2.9px; baseline→rule 12.0px, transcribed from the operator's mock: space-2 pads + lh 1.6), applied to all text-against-rule placements on all 3 sheets; NUMERIC rhythm gate (result.rhythm seam + vertical-rhythm.test.ts — raster golden-diff skipped: no pure-JS rasterizer without native canvas); §11 fixes (flood machine string → plain sentence w/ detail in provenance only + isCleanReasonSentence now rejects colons; setbacks cell → one grey qualifier). Live-verified on rev 00120 on the operator's OWN screenshot parcel 48021:34769 w/ address (rhythm correct, qualifier correct, flood resolved zone X live). FORMATTING/RHYTHM APPROVED by operator. REFINEMENT ROUND 2 (#171 / Standard v1.2, operator-directed): context layers GEOMETRICALLY CLIPPED to the drawing frame (bleed gate: every mark bbox ⊆ frame, tested on gold+dense+synthetic-wide-context; gate also caught+fixed an uncapped degenerate-callout size), road labels NAMED STREETS ONLY (machine road-IDs a forbidden test string; unnamed roads draw unlabeled), aerial NATURAL COLOR (duotone+pngjs removed; §20 rewritten; paper outlines kept), header meta never prints raw FIPS. Live-verified on rev 00123 on the operator's bleed parcel 48021:47719 (806 Pine St): sheet 1 fully framed, PECAN/PINE STREET labels only, county name in meta; sheet 3 natural imagery, overlay legible. OPERATOR TESTING NEXT.

PE reports / functions (NEW — needs a plan):
- The customer app (PE) exposes ~3-4 reports; the spine can back ~15+ (brief, hazard, encumbrance, dossier, plan-review, deliverable letters). Audit: `_inbox/2026-07-27_app_vs_cc_report_audit.md`. DECISION OWED: which reports PE offers + is PE the unified report surface. This is a product+design+data program, not a QA ticket.

Public-data completeness (NEW — recon done):
- Only ingested ~4 of 26 county folders. Ungrabbed public layers: aerial imagery (2019-2024), hydrography (Creeks_Streams), address points, subdivisions, city water/utility CCN. Recon: `_inbox/2026-07-27_bastrop_public_data_completeness_recon.md`. Ingest-prioritization dispatch HELD pending report-set decision.

## PE WORKBENCH BUILD (started 2026-07-29, spec: _inbox/2026-07-29_pe_workbench_concept_spec.md)

ALL FOUR WAVES SHIPPED + DEPLOYED SAME-DAY (PRs #100 chassis / #101 verdict+reports / #102 chat / #103 properties+share; live-verified on prod):
- Chassis: top-right bubble cluster + ONE shared dock, per-property persistent tool state (pe:workbench:tool-state:v1, latest-10 cap), honest coming/select-first states. Pinned API: WorkbenchToolDef + useDockToolState + WorkbenchHostActions (types.ts docstring).
- Brief leads with a deterministic VERDICT LINE (red flags lead; "no red flags" only when all four facts present+clean; absences never earn it). Reports bubble took site-plan+terrain exports off the card.
- AI CHAT (extension flow port, recon-grounded): verbatim starter chips + id remap, areaContext.subject + visibleParcels eligibility anchor, last-8 history, numbered-citation chips → IN-THREAD expansion + freshness badge (client-side, no fetch), 401/402/400/5xx honest; proxy allowlist +api/brokerage/v1/research/chat (session Bearer; extension's install-id wedge NOT ported). NOTE: {{atom:}} markup is DEPRECATED corpus-wide — chips derive from citations+brief inlineRefs (the spec's markup assumption was stale).
- My Properties (cortex saved-properties routes surfaced; allowlist TIGHTENED to exact/one-segment; the card's old Save button was DEAD — now unified through one save flow) + SHARE: HMAC-signed 30-day single-parcel token (PE_SHARE_SECRET minted in Vercel prod 2026-07-29 — first mint was a PS5.1 zero-byte trap, caught+replaced), no-account /share#<token> view serving verdict+cited brief (anonymous owner-stripped snapshot projection, buildR1Brief parity-pinned) + site-plan/terrain DOWNLOAD-ONLY; expired≠invalid distinct 403s. Live checks: garbage token 403, unauth mint 401, five tools in bundle.
- DEFERRED (unchanged): extension-as-funnel, county-records-docs, broader report-set decision, atom-seeded starter chips.
- OWED: operator's signed-in live pass (chat round-trip, save/reopen, mint+open a share link).
- POLISH + DOSSIER EXPANSION (2026-07-29, operator-directed, all six suggestions greenlit): #104 polish (saved-reopen FLIES the map — also fixed the search parcel-path's identical latent no-fly bug; chat SELF-SOURCES property context from facets, brief no longer a prerequisite; dock scrolls w/ pinned header instead of clipping). #105 DOSSIER core (master→detail+back in the one dock; drawings saved as GeoJSON + redrawn via new MapToolsController getDrawings/setDossierOverlay seams; Save-chat-to-property w/ labeled AI summary via one extra chat call; exports auto-attach; notes 4k; snapshot size discipline — no blobs; ", ," label fallback fixed). #106 COMPARE dock tool (two saved properties side-by-side cited facts, verdict composer reused, law-compliant one-dock). #107 PINS+STATUS (server-truth star pins w/ save-time coords + status accent colors, LAYERS toggle, pin-click reopen; researching/offer/passed chips + >5-entries filter). All merged+deployed+bundle-verified 2026-07-29.
- IN FLIGHT: WB7a cortex share-dossier service-key route (ldt PR at CI; then cortex deploy + PE share-view leg renders drawings/chat-summary/notes) + WB7b engine dossier-PDF export (Standard-styled cover/brief/summary/notes + appended site-plan sheets; then engine deploy + MCP tool + PE Reports leg). Follow-up flagged: freeform tags.
- W16 SITUS FRONT-ROLE ARC CLOSED (2026-07-29 night): #173 rule + #178 planner-caught regression fix (txgio situs is a FULL address; the comma tail survived normalization so the first county restamp silently fell back to the heuristic on ALL parcels — street-segment cut before matching, regression test w/ the exact txgio form). Corrected restamp (3,156 city parcels, 22,442 edges) + rewarm re-promote (3,605 re-promoted, 37 kept-prior). FINAL VERIFY on 48021:34177: front on the Pecan (west) edge w/ frontBasis situs-street-match; envelope atom 16,028 SF == fresh export sheet 16,028 SF (90%), FRONT SETBACK 15' drawn on the west edge. Data staleness + role-correctness arc fully closed.
- F&D 10x REDESIGN (operator-directed): FD2 LIVE (PE #114: water overlay ON THE MAIN MAP — gradient image layer below parcel tiles + animated flow-dash + bold haloed arrows + catchment glow, WB6-style lifecycle, feature-detect fallback to polygon fills pre-FD1; flood folded INTO the Reports bubble, registry back to 6; whole-tool $15 lock w/ Pro-only terrain section). FD1 (engine #179, merging): water-gradient raster contract {pngBase64,bbox,note} (ramp documented, pngjs re-added as producer), PARCEL-AWARE pour point (max-acc on/adjacent ring; "0 sq ft delivers runoff" narration TEST-BANNED), sheet 1 rebuilt = Esri imagery backdrop + gradient composite + bold arrows + haloed ring. FD1 MERGED (#179) + DEPLOYED (engine 00139-tuc @100%) + LIVE-SMOKED on the operator's 1408 Chestnut: gradient in payload, honest local-high-point narration (banned phrasing gone), sheet 1 = aerial imagery + water gradient tracing real drainage channels + haloed ring + flow lines + attribution. The live PE map overlay now upgrades to the gradient automatically (FD2 feature-detect). 10x arc COMPLETE pending operator visual pass. Residual tuning candidates for the next design round: ponding-stat-vs-visual coherence (396k SF ponded reads bigger than the gradient shows on-parcel), cold refresh ~99s.
- R3 FLOOD & DRAINAGE LIVE — THE FIRST PAID REPORT (2026-07-29 night): engine #176 (parcel-scoped drainage study route + Standard-styled 2-sheet PDF; rainfall 9.5in documented regional default, source honest "default" w/ NOAA-unavailable fine print) + #177 CANARY-CAUGHT FIX (the ponding headline summed the whole modeled bbox — 3.47M SF on a 0.19-ac parcel; now PARCEL-CLIPPED: 17,519 SF live-verified, legend "Ponding on parcel"; the canary-smoke-shift discipline caught it at 0% traffic) + PE #113 (flood bubble: LockedToolPanel two-choice gate — flood IS in the $15 unlock; run-in-dock; graded SVG viz w/ provenance; PDF export; dossier auto-attach; DIRECT BFF transport w/ evidence — global gate middleware, no per-family signing; folded into pe-site-plan-export ?report= — still 11/12 functions). Engine 00136-kop @100%; PE deployed; BFF unauth 401 verified. NITS for operator visual pass: sheet-1 ponding fill renders faintly; cold refresh ~75-82s (warm ~8s) → honest-timeout+retry UX, same class as site-plan cold. MCP dossier tools LIVE (00034-cr5; refresh 4.1s + real-PDF download smoked). PATTERN SET: run-in-dock → viz → Standard PDF → paid gate — every future report inherits.
- R2 CITATIONS LIVE (2026-07-29 eve): PE #111 deployed+bundle-verified (professional presentation mode — consumer stripped [n] and is GONE from the bundle; fetch-on-tap BRIEF/FULL accordion off retrieval atoms/:did; client-composed lineage walk envelope→setback→code with back-stack; reserved atom color; honest 404-degrade) + ldt #364 anti-fabrication constraint tests merged. WB7a share-dossier route LIVE (cortex 00446-zij @100%; service-key 401/404 semantics verified in prod; planner resolved the #362/#363 route-file conflict). WB7b dossier PDF LIVE (engine 00132-mur @100%; smoked: 6-sheet Standard-styled dossier for 34177 — cover w/ verdict + contents manifest + honest UNAVAILABLE chips + AI-summary/notes pages + site-plan sheets appended; NIT: §11 sanitization strips middots from the verdict line — run-on but readable, polish later). IN FLIGHT: R3-engine flood&drainage document+study route; the MCP-dossier-tools + PE dossier-PDF-action + share-view-dossier wiring legs. LESSON: agents' CI monitors went quiet after green — planner now sweeps PR states directly instead of waiting on agent notifications.
- R1 PAYWALL SHIPPED + LIVE (2026-07-29 eve, per the LOCKED plan 0165d55): cortex #363 (pe_property_unlocks + pe_chat_message_counts migration 0063 APPLIED via workflow train; property-scoped server gates on brief/chat; signed-in free counter 3/property atomic; summary=paid never counted; dev-unlock stub at /entitlement/dev-unlock — planner aliased the PE-pinned path; NO Stripe) + PE #110 (usePropertyEntitlement proactive hook w/ client-soft feature-detect; unified two-choice unlock flow $15-property/$99-sale-$149-Pro from one pricing config; per-bubble lock states; terrain Pro-only; share folded; chat meter + wall; old Pro-hardcoded copy gone, absence-tested). Cortex 00444-xak @100%; PE deployed; anon shape verified live; signed-in matrix server-test-pinned (23 tests incl. exactly-3 concurrency). OWED: operator signed-in pass of the line. LIVE-PAYMENTS wave (Stripe one-time + webhook → createPePropertyUnlock) is the isolated follow-on.
- SHARE-FUNNEL + OVERFLOW WAVE SHIPPED + LIVE (2026-07-29, operator screenshots): (1) PE #115 MERGED+DEPLOYED (bundle-verified) — /share#<token> now lands recipients in the FULL app: map flies to the shared property (reuses the saved-reopen coordinate chain via runParcelLookup), dossier docked read-only (ShareView content extracted as ShareAnalysisContent), standard Google sign-in prompt w/ pe_signup_intent GTM (funnel: recipients become users; token stashed in sessionStorage across the OAuth redirect — server-side returnTo in api/auth.ts flagged as sturdier follow-up), expired/garbage token = map + notice + prompt (never a dead end); Satellite/aerial checkbox moved TOOLS→LAYERS first row (MapToolset.tsx; MapTools.tsx is CC-only dead code for PE, untouched). Existing share URLs unchanged; still 11/12 functions. (2) engine #180 MERGED+DEPLOYED (00142-fum @100%, envelope-canary repointed) — overflow-aware pagination: measure-before-draw off the SAME §21 line-box composers (planned height == drawn height), continuation sheets w/ repeated table header + "(CONTINUED)", tables split only between rows, no-orphan-header (≥2 rows or push section), dynamic "SHEET k OF n" everywhere (dossier TOTAL_SHEETS hard-code replaced by countSitePlanSheets seam); 7-test mechanical gate suite (32-segment fixture → 4 pages, frame containment) + existing fixtures byte-stable (537 passed). LIVE-VERIFIED on the operator's repro 48021:81432 "1409 SH 95": now 4 sheets, summary sheet 2 clean, sheet 3 = "PROVENANCE / CITATIONS (CONTINUED)" w/ repeated header; re-refreshed w/ address descriptor so the stored atom is clean. DEPLOY GOTCHA (mold'd): a bare `gcloud builds submit --tag` on hauska-engine builds the ROOT retrieval-api Dockerfile — engine-api images must go through cloudbuild.engine-api.yaml (--config + _IMAGE substitution); planner caught + cancelled the stray build before it poisoned the tag. Operator hydro feedback LANDED (2026-07-29): overlay must OVERPOWER other layers when active + flow lines need watershed-corridor graphics. FD3 build agent dispatched (engine feat/flood-flow-paths: flowPaths[{coords,strength,kind}] + catchmentSwaths from the SAME D8 model, backward-compatible; PE feat/pe-flood-overlay-dominance: dim-scrim w/ exact paint restoration, strength-scaled flow ribbons w/ casing, swath corridors, feature-detect).
- SETBACK ACCURACY RED FLAG (2026-07-29, operator screenshots, PROBE-ONLY — operator ruled NO work until discussion): 1010 Jefferson St Bastrop (APN 105054, P-3) — PE says F 15' / side+rear "not specified (build-to-line governs)"; the CITY's daily-use map (operator-verified vs adopted code) says Front 25 (porch encroach 10), Side 5 (corner 15), Rear 25, height 35, impervious 50%, min lot 1/3 ac. Prime suspect: setback basis DID `code-section:storage-port-proof/phase-1a` (proof-era placeholder?). Two read-only probes dispatched: (A) pipeline trace — zoning stamp source, dump ALL bastrop setback-rule atoms + provenance, the not_specified emit path, envelope math soundness, where 15' comes from, per-jurisdiction scope count; (B) ground truth — fetch adopted B3 text + city ArcGIS REST attributes, extract the real dimensional table per district w/ citations (setback vs build-to framing is load-bearing), diff vs our corpus atoms, edition/amendment check. Operator: "way more work to be done on Bastrop before we can scale." PROBES COMPLETE (2026-07-29): (A-pipeline) DATA problem, math clean — envelope insets whatever the atom says + geometry gate; TWO forked Bastrop tables in-engine: human-verified adapters/setbacks/bastrop-city-tx.json (P-3 front 25 w/ verbatim quote) vs depth-warm descriptor fixture bastrop_tx_descriptor.json (P-3 front 15 = P-4/P-5 transcription error, "transcribed", no quote) — depth-warm batch/rewarm imports the FIXTURE, so rewarms restamp the wrong number; storage-port-proof/phase-1a = smoke-test atom stamped as code source by the 07-23 breadth bake (fixed in code 07-24, store NEVER re-baked): 213,621/774,902 setback atoms (27.6%, 9+ jurisdictions) cite it; side/rear/height/impervious largely unmodeled corpus-wide. (B-ground-truth) THE REAL BOMB: Bastrop REPEALED B3 via Ord. 2026-06 (2nd reading 2026-04-14, effective immediately) → Bastrop Development Code (BDC, Ch.14, Euclidean: P/OS,RR,SF-1/2/3,MU,GC,PI,IND,PDD; Place Types DEAD). Our corpus ingested B3 2026-05-26 = SIX WEEKS POST-REPEAL; the K1 edition-watcher SAW the repeal (bdc-2026-adopted stub in snapshot since 2026-06-22) but sectionIds:[] and currentEditionId never flipped — silent edition failure. Zoning stamp reads the city's ABANDONED 2023 Zoning_Place_Type layer (still returns P-3); current law lives in Zoned_Parcels/83 + Zone_Types/25 (actively edited 2026). 1010 Jefferson is SF-1: code-true 30/10/20/30, 35ft, 50%, min lot 1/3ac (Sec 14.02.003, verified ordinance-verbatim AND live REST agree); porch encroach 5ft front (14.04.001(C)), contextual neighbor-average front OPTIONAL (14.02.006), +30ft TxDOT highway-corridor setback (14.02.007). NOTE: the operator's city-card 25/5/25 matches NO adopted district — a stray data-entry variant inside the city's own GIS; ordinance text is the only clean source. B3 ingestion itself was FAITHFUL (not_specified was honest for the dead code). BDC full text = ordinance PDF our corpus already links (191pp, extracted to scratchpad bdc.txt). Extra: adapters/setbacks/bastrop-tx.json (county) carries R-MD/R-LD/etc districts matching neither code, unverified legacy. AWAITING OPERATOR RULING on the remediation program (BDC ingest + stamp re-point + one-truth-table + rebake + provenance re-bake + edition/mold gates). NO fixes dispatched.
- HYDROGRAPHY SWAP DONE + LIVE-VERIFIED (2026-07-29, spec addendum aa24c98): engine #175 county hydrography slot (registry-driven, Bastrop Creeks_Streams MapServer — NO FeatureServer/NO pagination quirks coded around, live-probed) + PE #108 layer swap ("Hydrography" replaces the D8 customer layer; D8 stays engine-side as report input) + #109 BFF CONSOLIDATION (pe-map-layers dispatches topography/hydrology/hydrography — the #108 deploy FAILED on the Vercel Hobby 12-function cap; now 11 functions, one slot reserved for the Flood&Drainage report BFF; gotcha in mold). Engine 00129-how @100%; end-to-end: PE prod serves Piney Creek + county streams in 660ms. NEXT (queued behind WB7b's document pattern): H3 engine Flood&Drainage Standard-styled report document (catchment/zones/rainfall-ponding/flow, public-paid) + H4 PE report bubble (run-in-dock, sharp viz, PDF export) — the FIRST paid report bubble, pattern-setter.

## HELD / NOT STARTED (by design — do not start without operator go)

- CTX / Hays / national county fan-out (until Bastrop QA-done + go).
- Track C: thin CC engine-control panel + "start county X" launch surface (last by design).
- Fidelity v2 beyond topo: true ROW, plats, easements, courthouse records (Vertosoft channel product — Bastrop gift-demonstrator, records-as-downloadable-docs).
- Living-layer sensing engines (zoning-change/annex/ownership/permit/subdivision — temporal atoms exist, sensors not built).
- Marketplace / write-back contract (vision).
- Bastrop infra-twinning (streetlights/traffic — city layer, likely needs city data handoff).

## THE MOLD (engine-build prerequisite — keep it current as QA continues)

`28_THE_BASTROP_MOLD_engine_build_spec.md` = the specification the CTX/national engine build reads. What a complete county contains + every baked decision/gotcha the engine must replicate. LIVING doc: any agent that fixes/builds a COUNTY-GENERAL thing must add the lesson to it (capture protocol at the doc's end). When the operator returns to build the fan-out engine, this doc + _STATE.md + MEMORY.md are the input — no archaeology.

## KEY DOCS (the map)

- Program: 27_MASTER_WDLL, 27a (engines), 27c (road node), 27d (recipe), 27e (multi-track), 27f (Bastrop-through-v2 stack).
- Bastrop definition: `_inbox/2026-07-27_bastrop_composition_inventory.md` (what's IN Bastrop, verified against code).
- QA plan: `_inbox/2026-07-27_bastrop_qa_defect_register.md`.
- Reports: `_inbox/2026-07-27_app_vs_cc_report_audit.md` + `_inbox/2026-07-16_command_center_function_matrix_bizdev_handoff.md`.
- Strategy: `_decisions/2026-07-26_base_layer_connecting_tissue_thesis_and_tracks.md`, `_decisions/2026-07-27_county_records_channel_and_bastrop_demonstrator.md`.
- Last session: `_sessions/2026-07-27_bastrop_completion_multitrack_and_hardening_claude_code.md`.

## HOW TO USE THIS FILE

Fresh agent: read this + MEMORY.md FIRST, then the specific doc for your task. Update the OPEN and LIVE INFRA sections as you change state. This file is the pickup point — keep it true.

## MCP + ICC thread (2026-07-29 — audits in flight)

The strategic frame: PE made the property-intelligence stack HUMAN-consumable; MCP makes the SAME stack AGENT-consumable + discoverable + metered. ICC is the first licensed SOURCE and the worked example / demo account for the metering model. MCP + ICC share one root: the metered-citation pipe (inbound = what we owe ICC per reference; outbound = what agents pay us per call; both fire on one atom's provenance).

OPERATOR RULINGS (2026-07-29):
- ICC = an ON/OFF SWITCH. ON = demo mode (show ICC "here's how it works in the app" + "here's how we meter/pay on the backend" — likely a Command Center ICC usage/account screen where ICC logs in and sees usage). OFF = default, so PE commercializes/launches WITHOUT ICC (respects the license: ICC content is NOT customer-facing until the SaaS agreement is signed post-demo; accessPolicy platform-internal until then). The switch elegantly solves the license constraint AND enables the demo.
- MCP VISUAL OFFERING: agents should get VISUAL answers. Achievable-today = return a RENDERED map/site-plan IMAGE (MCP supports image content; ChatGPT/Claude render inline) alongside the cited data — reuses the renderer we built; nobody's property-MCP does this well = the outstanding offering. Frontier (design toward, not v1) = a LIVE INTERACTIVE embedded map (OpenAI Apps SDK / MCP-UI — emerging, platform-specific). Lead with the image (universal), layer interactive as platforms mature.

IN FLIGHT (read-only audits): MCP tool-surface-vs-PE-stack gap audit; ICC actual-live-state + license-compliance verification (agent said done, unverified). Results pending.

## THE NEXT MAJOR PROGRAM (after PE lands) — the fan-out engine / national onboarding

DO NOT LOSE THIS. The single largest roadmap item: build the ENGINE that fans agents to onboard the rest of the nation — CTX first, then all of TX, then national. It has been correctly HELD on ONE gate: "wait until the Bastrop PE build is complete so we fan from a PROVEN, COMPLETE reference county." That gate is about to clear (the 2026-07-29 PE coordinated session is the last piece of Bastrop-market-ready).

STATUS: gated-and-ready, NOT behind.
- The mold is defined + adversarially hardened: `28_THE_BASTROP_MOLD_engine_build_spec.md`.
- The recipe generalizes: Caldwell #2 (7/8 gates carried).
- Bastrop is the proof, now essentially complete.

ENGINE-BUILD PREREQUISITES (from the mold's own review — do before/with the fan-out):
- Build the PHANTOM GATES: recipe gate 7 (TALLY+COST) and gate 8 (SMOKE/end-to-end-live-availability) are PROSE not mechanical today — a fan-out without a real smoke gate = the scan-fix trap at national scale. Build them fail-closed first.
- Harden M0 cc-agent-REACH so county-lane agents start warm (standing decisions travel in the recipe dispatch).

SEQUENCE: PE lands (human + agent MCP-v1) -> THEN the fan-out engine (CTX -> TX -> national) becomes the next major program. This is what makes the positioning ("the national property-intelligence layer") TRUE at scale, not just for Bastrop. It runs in parallel with / ahead of the MCP-scale and ICC-plan-review threads.
